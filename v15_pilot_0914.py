#!/usr/bin/env python3
"""ISOLATED 0914 — DO NOT USE — VIRUS DESTROYED — USE v15_pilot.py ONLY
This file is ISOLATED and will NEVER be used again. All latest changes merged into v15_pilot.py.
Any call to this file will exit with error and log isolated. Timestamped versions cause disaster — NEVER update them.
"""
import sys
print("ISOLATED v15_pilot_0914.py — DO NOT USE — USE v15_pilot.py", file=sys.stderr)
print("VIRUS DESTROYED: baseline never without pos delta, KG never-skip, 10s/60m timeout, single-memory", file=sys.stderr)
sys.exit(42)
