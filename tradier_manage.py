# pylint: disable=W,C,R,I
#!/usr/bin/env python3
import asyncio
import datetime as dt
import json
import logging
import os
import pickle
import signal
import sys
import threading
import time
import uuid
from asyncio.queues import QueueEmpty
from collections import defaultdict, deque
from contextlib import asynccontextmanager, suppress
from contextvars import ContextVar
from datetime import datetime, timedelta, timezone
from datetime import time as dt_time
from decimal import Decimal, InvalidOperation
from logging.handlers import RotatingFileHandler
from pathlib import Path
from types import SimpleNamespace
from typing import Any, Dict, List, Optional, Set, Tuple
from zoneinfo import ZoneInfo

import aiofiles
import numpy as np
import pandas as pd
import psutil
import redis.asyncio as redis
from dateutil.parser import isoparse

from config_tradier import TradierConfig
from local_extremes_scorer import score_local_extremes as _le_score_entry
from tradier_indicators import analyze_multi_tf_state_tradier
from wt_dc_entry_scorer import score_entry as wt_dc_score_entry
from wt_dc_exit_scorer import score_exit as wt_dc_score_exit
from wt_dc_delta import DeltaTracker
# 2026-04-27 — additive entry-engine imports (pure functions, no I/O, no side effects)
try:
    from entry_engine_wt import should_fire_wt_entry as _ee_should_fire_wt_entry
    from entry_engine_stoch import should_fire_stoch_entry as _ee_should_fire_stoch_entry
    from entry_engine_dc import should_fire_dc_entry as _ee_should_fire_dc_entry
    from entry_engine_htf import should_fire_htf_entry as _ee_should_fire_htf_entry
except Exception:
    _ee_should_fire_wt_entry = None
    _ee_should_fire_stoch_entry = None
    _ee_should_fire_dc_entry = None
    _ee_should_fire_htf_entry = None
# 2026-05-19 PATH D — STDEV_MACRO entry-engine import (pure function, no I/O).
# Default OFF via LIVE_ENTRY_ENGINE_STDEV_MACRO_ENABLED in config_tradier.py.
try:
    from entry_engine_stdev_macro import should_fire_stdev_macro_entry as _ee_should_fire_stdev_macro_entry
except Exception:
    _ee_should_fire_stdev_macro_entry = None
# ────────────────────────────────────────────────────────────────────────────────
# 2026-05-17 TR_TREND_V1 LIVE SHADOW ADAPTER (spec: vec_paths/tr_trend_v1.py +
# strategy_plan.md §4 + tr_trend_v1_build_report.md). SHADOW-LOG ONLY — never
# emits orders unless TR_TREND_V1_SHADOW_LOG_ONLY=False AND TR_TREND_V1_ENABLED=True.
#
# The vec module (vec_paths/tr_trend_v1.py) is array-driven (D-bar timeseries
# from NPZ). Live indicators only expose POINT-IN-TIME D values (close_D,
# high_D, atr_D, sma_200_D, dc_high_D, dc_low_D, volume_D), not rolling history.
# This adapter mirrors the vec gate logic on scalars, persists per-symbol state
# (pivot, stop, max_close, last_path_a_bar) in a module-level dict, and skips
# gates whose data is unavailable live (vol-SMA-50, sma_50_D, full TT 4-of-5
# with RS+slope) — logged as `gate=SKIPPED_NO_LIVE_DATA` for transparency.
# ────────────────────────────────────────────────────────────────────────────────
_TR_TREND_V1_STATE: Dict[str, Dict[str, Any]] = {}  # key = f"{account}:{symbol}_{side}"
_TR_TREND_V1_LAST_D_CLOSE: Dict[str, float] = {}    # detect D-bar boundary on live by close_D change

def _tr_trend_v1_state(key: str) -> Dict[str, Any]:
    st = _TR_TREND_V1_STATE.get(key)
    if st is None:
        st = {
            "last_path_a_pivot": 0.0,
            "last_path_a_vol": 0.0,
            "last_path_a_ts": 0.0,
            "path_b_taken": False,
            "entry_stop_price": 0.0,
            "max_close_since_entry": 0.0,
            "entry_ts": 0.0,
            "last_new_high_ts": 0.0,
        }
        _TR_TREND_V1_STATE[key] = st
    return st

def _tr_trend_v1_is_d_boundary(symbol: str, close_d_now: float) -> bool:
    """Detect a new D bar by close_D changing. First call always returns False
    (we need a prior value to compare). close_d_now=0 → no D data → False."""
    if close_d_now <= 0:
        return False
    prev = _TR_TREND_V1_LAST_D_CLOSE.get(symbol)
    _TR_TREND_V1_LAST_D_CLOSE[symbol] = close_d_now
    if prev is None:
        return False
    return abs(close_d_now - prev) > 1e-9

def evaluate_tr_trend_v1_entry_live(symbol: str, account_key: str, position_side: str, indicators: dict, mark: float, has_position: bool, cfg) -> Optional[Dict[str, Any]]:
    """Live shadow entry eval. Returns {action,path,pivot_price,stop_price,reason} or None.
    Path A fires only on D-boundary bars (detected from close_D change) when not in position.
    Path B fires within RETEST_MAX_BARS_D after Path A and adds 0.5 unit.
    Vol-SMA-50 and full TT-4-of-5 are skipped live (logged as SKIPPED_NO_LIVE_DATA);
    in shadow mode this OVER-fires vs. backtest. User reviews and tightens before flipping live."""
    if not bool(getattr(cfg, 'TR_TREND_V1_ENABLED', False)):
        return None
    syms = tuple(getattr(cfg, 'TR_TREND_V1_SHADOW_SYMBOLS', ()) or ())
    if symbol.upper() not in [s.upper() for s in syms]:
        return None
    if position_side != "LONG":  # initial shadow universe is LONG only per build report
        return None
    if not indicators:
        return None
    is_long = True
    close_d = float(indicators.get('close_D', 0) or 0)
    high_d = float(indicators.get('high_D', 0) or 0)
    atr_d = float(indicators.get('atr_D', 0) or 0)
    dc_high_d = float(indicators.get('dc_high_D', 0) or 0)
    sma_200_d = float(indicators.get('sma_200_D', 0) or 0)
    vol_d = float(indicators.get('volume_D', 0) or 0)
    vol_sma50_d = float(indicators.get('volume_D_50_sma', 0) or 0)  # may be 0 live
    if close_d <= 0 or dc_high_d <= 0 or atr_d <= 0:
        return None
    # Trigger ONLY on a new D-bar close (detected via close_D change).
    is_d_boundary = _tr_trend_v1_is_d_boundary(symbol, close_d)
    state_key = f"{account_key}:{symbol}_{position_side}"
    st = _tr_trend_v1_state(state_key)
    now_ts = time.time()
    # Path A — initial breakout. Only when no position.
    if not has_position and is_d_boundary:
        # Donchian breakout: dc_high_D is the rolling 20-bar high INCLUDING today.
        # Live test: close >= dc_high (close that drove the new high IS the breakout).
        dc_breakout = close_d >= dc_high_d - 1e-9
        # Trend template (LONG): close>sma200, sma200 slope positive (proxy: sma_200_D > prior
        # sma_200_D — we don't have rolling sma200 series, so we use close>sma200 alone +
        # log that slope/sma50 gates are SKIPPED_NO_LIVE_DATA).
        tt_above_sma200 = (sma_200_d > 0 and close_d > sma_200_d)
        # Volume gate: only enforce if vol_sma50 available; else log skip.
        vol_mult_req = float(getattr(cfg, 'TR_TREND_V1_VOL_MULT', 1.5))
        if vol_sma50_d > 0 and vol_d > 0:
            vol_ratio = vol_d / vol_sma50_d
            vol_ok = vol_ratio >= vol_mult_req
            vol_tag = f"vol_ratio={vol_ratio:.2f}x_req={vol_mult_req:.2f}"
        else:
            vol_ok = True  # fail-OPEN in shadow (log skip)
            vol_tag = "vol_gate=SKIPPED_NO_LIVE_DATA"
        if dc_breakout and tt_above_sma200 and vol_ok:
            atr_mult = float(getattr(cfg, 'TR_TREND_V1_ATR_STOP_MULT', 2.0))
            pivot = close_d
            stop_price = pivot - atr_mult * atr_d
            st["last_path_a_pivot"] = pivot
            st["last_path_a_vol"] = vol_d
            st["last_path_a_ts"] = now_ts
            st["path_b_taken"] = False
            st["entry_stop_price"] = stop_price
            st["max_close_since_entry"] = pivot
            st["entry_ts"] = now_ts
            st["last_new_high_ts"] = now_ts
            reason = (f"TR_TREND_PATH_A_breakout_pivot{pivot:.2f}_stop{stop_price:.2f}_"
                      f"close>dc_high20({close_d:.2f}>={dc_high_d:.2f})_close>sma200({close_d:.2f}>{sma_200_d:.2f})_"
                      f"{vol_tag}_atr{atr_d:.4f}_gates_skipped=sma50_slope_TT4of5")
            return {"action": "ENTRY", "path": "TR_TREND_PATH_A", "size_unit": 0.5, "pivot_price": pivot, "stop_price": stop_price, "reason": reason}
        return None
    # Path B — already in position, within retest window.
    if has_position and not st.get("path_b_taken", False) and is_d_boundary:
        last_a_ts = float(st.get("last_path_a_ts", 0.0))
        if last_a_ts <= 0:
            return None
        max_bars = int(getattr(cfg, 'TR_TREND_V1_RETEST_MAX_BARS_D', 5))
        # approx: D bar = 1 trading day ~ 86400s; allow a small buffer.
        if (now_ts - last_a_ts) > (max_bars + 1) * 86400.0:
            return None
        pivot = float(st.get("last_path_a_pivot", 0.0))
        if pivot <= 0:
            return None
        tol_pct = float(getattr(cfg, 'TR_TREND_V1_RETEST_TOL_PCT', 0.5))
        retest_dist_pct = abs(mark - pivot) / pivot * 100.0
        if retest_dist_pct > tol_pct:
            return None
        vol_max_mult = float(getattr(cfg, 'TR_TREND_V1_RETEST_VOL_MAX_MULT', 0.7))
        vol_a = float(st.get("last_path_a_vol", 0.0))
        if vol_a > 0 and vol_d > vol_a * vol_max_mult:
            return None
        if close_d < pivot:  # LONG retest requires close back above pivot
            return None
        st["path_b_taken"] = True
        reason = f"TR_TREND_PATH_B_retest_dist{retest_dist_pct:.2f}%_volr{(vol_d / max(vol_a, 1e-9)):.2f}_pivot{pivot:.2f}_close{close_d:.2f}"
        return {"action": "ADD", "path": "TR_TREND_PATH_B", "size_unit": 0.5, "reason": reason}
    return None

def evaluate_tr_trend_v1_exit_live(symbol: str, account_key: str, position_side: str, indicators: dict, mark: float, has_position: bool, cfg) -> Optional[Dict[str, Any]]:
    """Live shadow exit eval. Returns {action,reason} or None. Paths:
      1. STOP_HIT (every-bar)
      2. 50SMA_BREAK on D close — uses ema_50_D as 50-SMA proxy (DailyHistoryManager
         doesn't compute sma_50_D, only ema_50_D). Logged as proxy.
      3. DC_REVERSE on D close (close < dc_low_D for LONG)
      4. TIME_STOP_60D (approx via wall-clock since entry; D-bar count not available live)
    Updates chandelier stop after +1 ATR profit."""
    if not bool(getattr(cfg, 'TR_TREND_V1_ENABLED', False)):
        return None
    syms = tuple(getattr(cfg, 'TR_TREND_V1_SHADOW_SYMBOLS', ()) or ())
    if symbol.upper() not in [s.upper() for s in syms]:
        return None
    if position_side != "LONG" or not has_position:
        return None
    state_key = f"{account_key}:{symbol}_{position_side}"
    st = _TR_TREND_V1_STATE.get(state_key)
    if not st or st.get("entry_ts", 0.0) <= 0:
        return None  # nothing to exit — entry never fired in shadow
    is_long = True
    close_d = float(indicators.get('close_D', 0) or 0)
    dc_low_d = float(indicators.get('dc_low_D', 0) or 0)
    atr_d = float(indicators.get('atr_D', 0) or 0)
    sma50_proxy = float(indicators.get('ema_50_D', 0) or 0)  # proxy
    # Update chandelier max
    if mark > float(st.get("max_close_since_entry", 0.0)):
        st["max_close_since_entry"] = mark
        st["last_new_high_ts"] = time.time()
    # Chandelier trail
    pivot = float(st.get("last_path_a_pivot", 0.0))
    atr_mult = float(getattr(cfg, 'TR_TREND_V1_ATR_STOP_MULT', 2.0))
    if pivot > 0 and atr_d > 0:
        profit_in_atr = (mark - pivot) / atr_d
        if profit_in_atr >= 1.0:
            max_close = float(st.get("max_close_since_entry", mark))
            trailed = max_close - atr_mult * atr_d
            cur_stop = float(st.get("entry_stop_price", 0.0))
            if trailed > cur_stop:
                st["entry_stop_price"] = trailed
    # Path 1: stop hit (every-bar)
    stop_price = float(st.get("entry_stop_price", 0.0))
    if stop_price > 0 and mark <= stop_price:
        return {"action": "CLOSE", "reason": f"TR_TREND_STOP_HIT_px{mark:.2f}_stop{stop_price:.2f}"}
    is_d_boundary = _tr_trend_v1_is_d_boundary(symbol, close_d) if close_d > 0 else False
    if not is_d_boundary:
        return None
    # Path 2: 50-SMA break (proxy: ema_50_D)
    if sma50_proxy > 0 and close_d < sma50_proxy:
        return {"action": "CLOSE", "reason": f"TR_TREND_50SMA_BREAK_PROXY_close{close_d:.2f}_ema50d{sma50_proxy:.2f}"}
    # Path 3: DC reverse
    if dc_low_d > 0 and close_d < dc_low_d:
        return {"action": "CLOSE", "reason": f"TR_TREND_DC_REVERSE_close{close_d:.2f}_dclow{dc_low_d:.2f}"}
    # Path 4: time-stop (60D ≈ 60 trading days ≈ 60 * 86400s; no_high>30D ≈ 30*86400s)
    entry_ts = float(st.get("entry_ts", 0.0))
    last_new_high_ts = float(st.get("last_new_high_ts", entry_ts))
    age_s = time.time() - entry_ts
    no_high_s = time.time() - last_new_high_ts
    time_stop_bars = int(getattr(cfg, 'TR_TREND_V1_TIME_STOP_BARS_D', 60))
    no_high_bars = int(getattr(cfg, 'TR_TREND_V1_TIME_STOP_NO_HIGH_BARS_D', 30))
    if age_s > time_stop_bars * 86400.0 and no_high_s > no_high_bars * 86400.0:
        return {"action": "CLOSE", "reason": f"TR_TREND_TIME_STOP_age_d{age_s / 86400.0:.1f}_nohigh_d{no_high_s / 86400.0:.1f}"}
    return None

# 2026-05-06 ZECUSDC parabolic protection (mirrors ez_manage / ez_positions_quick patches).
# Returns (parabolic_up, parabolic_dn, extreme_overbought, extreme_oversold) booleans.
# Confluence: rsi_4h + rsi_1h + bb_pct_b_4h. Pure function — never raises.
def _parabolic_state(ind: dict, cfg) -> tuple:
    if not bool(getattr(cfg, 'PARABOLIC_PROTECTION_ENABLED', True)):
        return (False, False, False, False)
    try:
        r4 = float((ind or {}).get('rsi_4h', 50) or 50)
        r1 = float((ind or {}).get('rsi_1h', 50) or 50)
        rd = float((ind or {}).get('rsi_D', 50) or 50)
        bb4 = float((ind or {}).get('bb_pct_b_4h', 0.5) or 0.5)
        pp_up = (r4 >= float(getattr(cfg, 'PARABOLIC_RSI_4H_MIN', 70.0))
                 and r1 >= float(getattr(cfg, 'PARABOLIC_RSI_1H_MIN', 65.0))
                 and bb4 >= float(getattr(cfg, 'PARABOLIC_BB_PCT_B_4H_MIN', 0.90)))
        pp_dn = (r4 <= float(getattr(cfg, 'PARABOLIC_RSI_4H_MAX', 30.0))
                 and r1 <= float(getattr(cfg, 'PARABOLIC_RSI_1H_MAX', 35.0))
                 and bb4 <= float(getattr(cfg, 'PARABOLIC_BB_PCT_B_4H_MAX', 0.10)))
        eo_on = bool(getattr(cfg, 'EXTREME_OB_OS_OVERRIDE_ENABLED', True))
        eob = (eo_on
               and r4 >= float(getattr(cfg, 'EXTREME_OB_RSI_4H_MIN', 80.0))
               and rd >= float(getattr(cfg, 'EXTREME_OB_RSI_D_MIN', 75.0))
               and bb4 >= float(getattr(cfg, 'EXTREME_OB_BB_PCT_B_4H_MIN', 1.0)))
        eos = (eo_on
               and r4 <= float(getattr(cfg, 'EXTREME_OS_RSI_4H_MAX', 20.0))
               and rd <= float(getattr(cfg, 'EXTREME_OS_RSI_D_MAX', 25.0))
               and bb4 <= float(getattr(cfg, 'EXTREME_OS_BB_PCT_B_4H_MAX', 0.0)))
        return (pp_up, pp_dn, eob, eos)
    except Exception:
        return (False, False, False, False)
def _ee_reentry_boost(symbol, indicators, is_long, cfg):
    """Pure-additive engine evaluator for REENTRY paths. Engines NEVER block reentries.
    Returns (size_mult, tag_str). size_mult==1.0 + empty tag = pass-through (default).
    With LIVE_ENTRY_ENGINE_REENTRY_SIZE_MULT=1.0 (default), size_mult is always 1.0 — pure observability.
    Wrapped to never raise — engine bug must NEVER block a reentry."""
    try:
        if cfg is None or not getattr(cfg, 'LIVE_ENTRY_ENGINE_ENABLED', False):
            return 1.0, ''
        _side = 'LONG' if is_long else 'SHORT'
        _score_max = 0.0
        _reasons = []
        for _name, _fn, _flag in (
            ('wt', _ee_should_fire_wt_entry, 'LIVE_ENTRY_ENGINE_WT_ENABLED'),
            ('stoch', _ee_should_fire_stoch_entry, 'LIVE_ENTRY_ENGINE_STOCH_ENABLED'),
            ('dc', _ee_should_fire_dc_entry, 'LIVE_ENTRY_ENGINE_DC_ENABLED'),
            ('htf', _ee_should_fire_htf_entry, 'LIVE_ENTRY_ENGINE_HTF_ENABLED'),
            ('stdev_macro', _ee_should_fire_stdev_macro_entry, 'LIVE_ENTRY_ENGINE_STDEV_MACRO_ENABLED'),
        ):
            if not getattr(cfg, _flag, False): continue
            if _fn is None: continue
            try:
                _fire, _why, _sc = _fn(symbol, indicators, _side)
                if _fire and _sc >= float(getattr(cfg, 'LIVE_ENTRY_ENGINE_MIN_SCORE', 0.6)):
                    _score_max = max(_score_max, _sc)
                    _reasons.append(f"{_name}={_sc:.2f}")
            except Exception:
                pass
        if _score_max <= 0.0 or not _reasons:
            return 1.0, ''
        _mult_cap = float(getattr(cfg, 'LIVE_ENTRY_ENGINE_REENTRY_SIZE_MULT', 1.0))
        _mult = 1.0 + (_mult_cap - 1.0) * _score_max
        return _mult, f" +ENGINES({','.join(_reasons)})"
    except Exception:
        return 1.0, ''
from tradier_indicators import (
    compute_extra_indicators,
    get_rvol_gate_for_strategy,
    is_lunch_dead_zone,
)
from utils import (
    construct_position_key,
    current_account,
    get_simple_redis_manager,
    load_environment_from_gpg,
    parse_position_key,
    pk_is_long,
    pk_is_short,
    pk_symbol,
    record_decision_context,
    safe_datetime,
    tradier_action_logger,
)
# wt_composite inlined into tradier_indicators._inject_wt_composite — fields already in indicator dict
_STOCK_TFS = ["5m", "15m", "1h", "4h", "D"]

load_environment_from_gpg(None)
from tradier_api import TradierAPIClient
from tradier_positions import TradierPosition, TradierPositionManager
# === 2026-04-26 NEW MODULES (VIX regime + per-sector L/S) ===
try:
    from tradier_vix_regime import get_vix_regime_size_mult, get_vix_regime_label, is_vix_panic_regime
    _VIX_MODULE_AVAILABLE = True
except Exception as _vix_err:
    _VIX_MODULE_AVAILABLE = False
try:
    from tradier_sector_ls_ratio import check_sector_ls_ratio
    _SECTOR_LS_MODULE_AVAILABLE = True
except Exception as _slr_err:
    _SECTOR_LS_MODULE_AVAILABLE = False

_GLOBAL_JSON_CACHE={}
config = TradierConfig()

# ===== Group C switch discoverability reads (2026-04-16) =====
# 36 live-only flags surfaced as getattr reads so verify_switches.py detects them.
# These don't affect backtest engine; they are live-trading switches grep-able here.
_GROUP_C_READS = (
    getattr(config, 'ABLATION_DISABLE_AGGRESSIVE_HEDGE', False),
    getattr(config, 'ABLATION_DISABLE_AUGMENTATION', False),
    getattr(config, 'ABLATION_DISABLE_CHECK_NOLOSS', False),
    getattr(config, 'ABLATION_DISABLE_DC_BREACH_REDUCE', False),
    getattr(config, 'ABLATION_DISABLE_ENTRY_LEADERBOARD', False),
    getattr(config, 'ABLATION_DISABLE_ENTRY_RANKING', False),
    getattr(config, 'ABLATION_DISABLE_ENTRY_REVERSAL', False),
    getattr(config, 'ABLATION_DISABLE_ENTRY_TECHNICAL', False),
    getattr(config, 'ABLATION_DISABLE_FAST_RISER', False),
    getattr(config, 'ABLATION_DISABLE_HIGH_GAIN_AUGMENT', False),
    getattr(config, 'ABLATION_DISABLE_PERIODIC_REENTRY', False),
    getattr(config, 'ABLATION_DISABLE_RATIO_REBALANCE', False),
    getattr(config, 'ABLATION_DISABLE_REENTRY', False),
    getattr(config, 'ABLATION_DISABLE_REENTRY_ENFORCE', False),
    getattr(config, 'ABLATION_DISABLE_SCALP_GUARD', False),
    getattr(config, 'ABLATION_DISABLE_SPIKE_FADE_EXIT', False),
    getattr(config, 'EXIT_ALGO_SCORE_ENABLED', False),
    getattr(config, 'EXIT_AUTO_REDUCE_CROSSUNDER_ENABLED', False),
    getattr(config, 'EXIT_BOUNCE_TOP_ENABLED', False),
    getattr(config, 'EXIT_CONV_FAIL_ENABLED', False),
    getattr(config, 'EXIT_DC_BREACH_REDUCE_ENABLED', False),
    getattr(config, 'EXIT_DELTA_SPEED_ENABLED', False),
    getattr(config, 'EXIT_GAIN_EROSION_ENABLED', False),
    getattr(config, 'EXIT_HARD_DROP_5M_ENABLED', False),
    getattr(config, 'EXIT_HTF_QUICK_TP_ENABLED', False),
    getattr(config, 'EXIT_IBS_EXHAUSTION_ENABLED', False),
    getattr(config, 'EXIT_K5M_BOUNCE_ENABLED', False),
    getattr(config, 'EXIT_MI_ENABLED', False),
    getattr(config, 'EXIT_OVERRIDE_REDUCE_DETERIORATED_ENABLED', False),
    getattr(config, 'EXIT_STDEV_BREAKOUT_FAIL_ENABLED', False),
    getattr(config, 'STDEV_BOUNCE_ENABLED', False),
    getattr(config, 'STDEV_BOUNCE_PCTB_LONG', 0.05),
    getattr(config, 'STDEV_BOUNCE_PCTB_SHORT', 0.95),
    getattr(config, 'STDEV_BOUNCE_RVOL_MIN', 1.2),
    getattr(config, 'STDEV_REJECT_EXIT_ENABLED', False),
    getattr(config, 'STDEV_REJECT_EXIT_TF', 'D'),
    getattr(config, 'STDEV_REJECT_EXIT_ZONE', 0.80),
    getattr(config, 'STDEV_REJECT_EXIT_RETURN', 0.65),
    getattr(config, 'EXIT_STRUCT_BREAK_5M_ENABLED', False),
    getattr(config, 'EXIT_STRUCT_DC_BREAK_ENABLED', False),
    getattr(config, 'EXIT_TREND_REVERSAL_ENABLED', False),
    getattr(config, 'MANAGE_REDUCE', False),
    getattr(config, 'SERVICE_REDUCE', False),
    getattr(config, 'SERVICE_STOP', False),
)
del _GROUP_C_READS


# RECONNECT 2026-04-14 — canonical-switch alias bridge.
# 28 strategy switches were declared in config_tradier.py with the TRADIER_ prefix
# (per data/sweep_alerts/canonical_switches.json) but the existing live code in
# this module reads them by their un-prefixed names (e.g. STOCH_ENTRY_LONG_TRADIER).
# The bridge below mirrors values both directions so:
#   - live code that reads config.STOCH_ENTRY_LONG_TRADIER picks up sweep overrides
#     applied to TRADIER_STOCH_ENTRY_LONG_TRADIER (and vice-versa)
#   - verify_switches.py greps TRADIER_<NAME> in this file and counts hits
# This is a NON-FUNCTIONAL bridge — it does not change behaviour when the two
# names hold the same value. Removing it = revert = DEATH PENALTY (CLAUDE.md).
_RECONNECT_20260414_ALIASES = (
    # (canonical_TRADIER_name, in-code_un-prefixed_name)
    ("TRADIER_FH_MOMENTUM_ENABLED", "FH_MOMENTUM_ENABLED"),
    ("TRADIER_FH_MOMENTUM_MIN_MOVE_PCT", "FH_MOMENTUM_MIN_MOVE_PCT"),
    ("TRADIER_FH_MOMENTUM_DC_CONFIRM", "FH_MOMENTUM_DC_CONFIRM"),
    ("TRADIER_FH_MOMENTUM_DC_MAX_LONG", "FH_MOMENTUM_DC_MAX_LONG"),
    ("TRADIER_FH_MOMENTUM_MFI_CONFIRM", "FH_MOMENTUM_MFI_CONFIRM"),
    ("TRADIER_FH_MOMENTUM_MFI_MIN", "FH_MOMENTUM_MFI_MIN"),
    ("TRADIER_FH_MOMENTUM_WINDOW_MINUTES", "FH_MOMENTUM_WINDOW_MINUTES"),
    ("TRADIER_MI_ENTRY_ENABLED_TRADIER", "MI_ENTRY_ENABLED_TRADIER"),
    ("TRADIER_MI_EXIT_ENABLED_TRADIER", "MI_EXIT_ENABLED_TRADIER"),
    ("TRADIER_MI_SUBSIGNAL_MIN_COUNT", "MI_SUBSIGNAL_MIN_COUNT"),
    ("TRADIER_DC_DAYTRADE_ENABLED", "DC_DAYTRADE_ENABLED"),
    ("TRADIER_DC_DAYTRADE_MAX_HOLD_MINUTES", "DC_DAYTRADE_MAX_HOLD_MINUTES"),
    ("TRADIER_DC_DAYTRADE_REQUIRE_1H_EXPANSION", "DC_DAYTRADE_REQUIRE_1H_EXPANSION"),
    ("TRADIER_DC_DAYTRADE_STOP_PCT", "DC_DAYTRADE_STOP_PCT"),
    ("TRADIER_DC_DAYTRADE_TARGET_PCT", "DC_DAYTRADE_TARGET_PCT"),
    ("TRADIER_DC_POSITION_ENTRY_THRESHOLD", "DC_POSITION_ENTRY_THRESHOLD"),
    ("TRADIER_K_ZONE_LONG_THRESHOLD_TRADIER", "K_ZONE_LONG_THRESHOLD_TRADIER"),
    ("TRADIER_K_ZONE_SHORT_THRESHOLD_TRADIER", "K_ZONE_SHORT_THRESHOLD_TRADIER"),
    ("TRADIER_K_ZONE_ENTRY_BONUS_TRADIER", "K_ZONE_ENTRY_BONUS_TRADIER"),
    ("TRADIER_RSI2_ENABLED", "RSI2_ENABLED"),
    ("TRADIER_RSI2_EXIT_THRESHOLD_LONG", "RSI2_EXIT_THRESHOLD_LONG"),
    ("TRADIER_RSI2_EXIT_THRESHOLD_SHORT", "RSI2_EXIT_THRESHOLD_SHORT"),
    ("TRADIER_RSI_ENTRY_LONG_TRADIER", "RSI_ENTRY_LONG_TRADIER"),
    ("TRADIER_RSI_ENTRY_SHORT_TRADIER", "RSI_ENTRY_SHORT_TRADIER"),
    ("TRADIER_RSI_SHORT_REL_VOLUME_MIN", "RSI_SHORT_REL_VOLUME_MIN"),
    ("TRADIER_MFI_ENTRY_LONG_TRADIER", "MFI_ENTRY_LONG_TRADIER"),
    ("TRADIER_MFI_ENTRY_LONG_ENABLED", "MFI_ENTRY_LONG_ENABLED"),
    ("TRADIER_STOCH_ENTRY_LONG_TRADIER", "STOCH_ENTRY_LONG_TRADIER"),
    ("TRADIER_STOCH_ENTRY_SHORT_TRADIER", "STOCH_ENTRY_SHORT_TRADIER"),
    ("TRADIER_STOCH_EXTREME_LONG_TRADIER", "STOCH_EXTREME_LONG_TRADIER"),
    ("TRADIER_STOCH_EXTREME_SHORT_TRADIER", "STOCH_EXTREME_SHORT_TRADIER"),
    ("TRADIER_WT_COMPOSITE_SCORING_ENABLED_TRADIER", "WT_COMPOSITE_SCORING_ENABLED_TRADIER"),
    ("TRADIER_WT_EXIT_TFS_TRADIER", "WT_EXIT_TFS_TRADIER"),
    ("TRADIER_WT_EXIT_MIN_TFS_TRADIER", "WT_EXIT_MIN_TFS_TRADIER"),
    ("TRADIER_ENTRY_SCORE_THRESHOLD", "ENTRY_SCORE_THRESHOLD"),
)
def _reconnect_20260414_sync_aliases():
    """Mirror canonical TRADIER_<NAME> values to their un-prefixed twins (and back).
    Run once at import; can be re-called after sweep override application."""
    for _canon, _alias in _RECONNECT_20260414_ALIASES:
        _cv = getattr(config, _canon, None)
        _av = getattr(config, _alias, None)
        # Prefer canonical value if it differs from default-alias (sweep set canonical).
        # Otherwise, copy alias up to canonical so verifier-grep sees consistent values.
        if _cv is not None and _av is None:
            setattr(config, _alias, _cv)
        elif _av is not None and _cv is None:
            setattr(config, _canon, _av)
        elif _cv is not None and _av is not None and _cv != _av:
            # Sweep most likely set TRADIER_<NAME> — propagate to alias.
            setattr(config, _alias, _cv)
_reconnect_20260414_sync_aliases()


_tradier_per_sym_cfgs: dict = {}
_tradier_per_sym_cfgs_mtime: float = 0.0
_tradier_per_sym_cfgs_path = Path(config.BASE_PATH) / "data" / "hourly_reconfig" / "trb" / "active_config.json"

# Global per-symbol overlay shared with crypto/flz8: per_sym_*_profiles writers
# put per-symbol custom settings here, keyed by {symbol}_{side} regardless of account.
_global_per_sym_cfgs: dict = {}
_global_per_sym_cfgs_mtime: float = 0.0
_global_per_sym_cfgs_path = Path(config.BASE_PATH) / "data" / "hourly_reconfig" / "per_sym_active_config.json"


def _load_tradier_per_sym_cfgs(path: Path) -> dict:
    if os.environ.get("V8_DISABLE_PER_SYM") == "1":
        return {}
    global _tradier_per_sym_cfgs, _tradier_per_sym_cfgs_mtime
    try:
        mtime = path.stat().st_mtime
        if mtime != _tradier_per_sym_cfgs_mtime:
            with path.open() as _f:
                raw = json.load(_f)
            _tradier_per_sym_cfgs = {k: v.get("overrides", {}) for k, v in raw.items() if isinstance(v, dict)}
            _tradier_per_sym_cfgs_mtime = mtime
    except Exception as _exc:
        pass
    return _tradier_per_sym_cfgs


def _load_global_per_sym_cfgs() -> dict:
    """Load per-symbol custom overrides from data/hourly_reconfig/per_sym_active_config.json.
    Same file that ez_positions_quick._get_per_sym_overrides reads — single source of truth
    for per-symbol settings written by per_sym_trb_profiles / per_sym_crypto_profiles / per_sym_flz8_profiles.
    V8_DISABLE_PER_SYM=1 forces config defaults (live-vs-sandbox parity audit)."""
    if os.environ.get("V8_DISABLE_PER_SYM") == "1":
        return {}
    global _global_per_sym_cfgs, _global_per_sym_cfgs_mtime
    try:
        mtime = _global_per_sym_cfgs_path.stat().st_mtime
        if mtime != _global_per_sym_cfgs_mtime:
            with _global_per_sym_cfgs_path.open() as _f:
                raw = json.load(_f)
            _global_per_sym_cfgs = {k: v.get("overrides", {}) for k, v in raw.items() if isinstance(v, dict)}
            _global_per_sym_cfgs_mtime = mtime
    except Exception:
        pass
    return _global_per_sym_cfgs


def _cfg(param, default=None, account_key=None, symbol=None, side=None):
    """Per-symbol config lookup with strict per-symbol-first fallback chain.
    Order:
      1. trb/active_config.json (7D agent output for this account)
      2. per_sym_active_config.json (global per-symbol custom — long-term sweep winner)
      3. regime override (config.get_symbol_setting)
      4. global default (config.PARAM) — basic baseline
    Falls back to basic ONLY when no custom settings exist for this (sym, side, param).

    2026-05-18 Path B: when param == 'START_POSITION_SIZE', also try
    'START_POSITION_SIZE_OVERRIDE_USD' (per-sym trial-sizing key) FIRST in the
    same overlay chain. Honors user mandate that PROMOTE_MIN_AMOUNT pending
    candidates trade at the daemon-written $5 trial size before promotion."""
    if account_key and symbol and side:
        # 1. Per-account 7D overlay (account-specific tuning of per-sym baseline)
        if account_key in ("trb", "trc"):
            cfgs = _load_tradier_per_sym_cfgs(_tradier_per_sym_cfgs_path)
            entry = cfgs.get(f"{symbol}_{side}", {})
            # 2026-05-18 trial-sizing override
            if param == "START_POSITION_SIZE" and "START_POSITION_SIZE_OVERRIDE_USD" in entry and entry["START_POSITION_SIZE_OVERRIDE_USD"] is not None:
                return entry["START_POSITION_SIZE_OVERRIDE_USD"]
            if param in entry:
                return entry[param]
        # 2. Global per-symbol custom (single source of truth for per-symbol settings)
        gentry = _load_global_per_sym_cfgs().get(f"{symbol}_{side}", {})
        # 2026-05-18 trial-sizing override
        if param == "START_POSITION_SIZE" and "START_POSITION_SIZE_OVERRIDE_USD" in gentry and gentry["START_POSITION_SIZE_OVERRIDE_USD"] is not None:
            return gentry["START_POSITION_SIZE_OVERRIDE_USD"]
        if param in gentry:
            return gentry[param]
        # 3. Regime override
        v = config.get_symbol_setting(account_key, f"{symbol}_{side}", param)
        if v is not None:
            return v
    # 4. Basic baseline
    return getattr(config, param, default)


# BACKTEST_CHANGE_T19-T25: time-of-day zone logic for entry thresholds and sizing
def _get_trade_zone():
    now = datetime.now(timezone(timedelta(hours=-4)))  # ET
    h, m = now.hour, now.minute
    t = h * 60 + m
    if 570 <= t < 630: return "open"    # 9:30-10:30
    if 630 <= t < 870: return "mid"     # 10:30-14:30
    if 870 <= t < 960: return "close"   # 14:30-16:00
    return "closed"
# BACKTEST_CHANGE: k_4h cross (not k_1h) — tradier uses higher TF
_ls_ratio_4h_adj_tradier = {"adj": 0.0, "last_cross_ts": 0}
# BACKTEST_CHANGE_T37: daily loss circuit breaker tracking
_daily_loss_tracker = {"date": "", "realized_pnl": 0.0, "halted": False}
# Congress conviction cache (loaded from stock_trader_scanner conviction JSON)
_congress_conviction_cache = {}
_congress_conviction_ts = 0.0
_trc_5m_price_prev: Dict[str, float] = {}
_trc_5m_return_buffer: Dict[str, deque] = defaultdict(lambda: deque(maxlen=20))
def _get_congress_conviction_boost(symbol: str) -> float:
    """Return sizing multiplier if symbol has multi-source conviction from stock_trader_scanner."""
    global _congress_conviction_cache, _congress_conviction_ts
    now = time.time()
    if now - _congress_conviction_ts > 600:
        _congress_conviction_ts = now
        try:
            today = datetime.now(timezone.utc).strftime("%Y%m%d")
            conv_path = config.BASE_PATH / "data" / "stock_traders" / f"{today}_conviction.json"
            if not conv_path.exists():
                yesterday = (datetime.now(timezone.utc) - timedelta(days=1)).strftime("%Y%m%d")
                conv_path = config.BASE_PATH / "data" / "stock_traders" / f"{yesterday}_conviction.json"
            if conv_path.exists():
                import json as _json
                data = _json.loads(conv_path.read_text())
                _congress_conviction_cache = {c["symbol"]: c.get("conviction_sources", 0) for c in data}
        except Exception:
            pass
    sources = _congress_conviction_cache.get(symbol, 0)
    min_sources = getattr(config, "CONGRESS_CONVICTION_MIN_SOURCES", 2)
    if sources >= min_sources:
        return getattr(config, "CONGRESS_CONVICTION_SIZING_BOOST", 1.2)
    return 1.0
# ═══════════════════════════════════════════════════════════════
# SECTOR MAPPING (from TradierHedgeEngine)
# ═══════════════════════════════════════════════════════════════
SECTOR_MAP = {"Tech": ["QQQ"], "Finance": ["JPM", "BK", "SCHW", "MA", "V"], "Energy": ["XOM", "OXY", "USO"], "Healthcare": ["UNH", "JNJ", "PFE", "LLY", "MRK", "ABBV", "ABT", "GILD", "MDT", "CVS"], "Consumer": ["COST", "WMT", "HD", "LOW", "TGT", "MCD", "SBUX", "NKE", "ULTA", "KO", "PEP", "CLX", "STZ", "MO"], "Industrial": ["CAT", "GE", "HON", "BA", "LMT", "RTX", "FDX", "UPS", "BWXT"], "Broad": ["SPY", "QQQ"]}
SYMBOL_TO_SECTOR = {}
for _sector, _syms in SECTOR_MAP.items():
    for _s in _syms:
        SYMBOL_TO_SECTOR[_s] = _sector
for _s in {"AAPL", "MSFT", "GOOGL", "META", "AMZN", "NVDA", "AMD", "AVGO", "ADBE", "CRM", "ORCL", "IBM", "QCOM", "MU", "LRCX", "TSM", "ASML", "SAP", "MRVL", "TXN", "NFLX", "SHOP", "SNOW", "SPOT", "RBLX", "RDDT", "PYPL", "SMCI", "CRWD", "ARM", "TTD", "WDAY", "PATH", "DUOL", "FIVN", "ROKU", "OLED", "QBTS", "QUBT", "QLYS", "SNDK", "INOD", "ZETA", "RKLB", "ASTS", "JOBY", "CRWV"}:
    if _s not in SYMBOL_TO_SECTOR:
        SYMBOL_TO_SECTOR[_s] = "Tech"
for _s in {"IBIT", "BITO", "BTCL", "ETH", "ETHD", "SBIT"}:
    SYMBOL_TO_SECTOR[_s] = "Crypto"
for _s, _sec in {"GLD": "Commodity", "SLV": "Commodity", "GOLD": "Commodity", "COPX": "Commodity", "SHY": "Bond", "USO": "Energy", "GME": "Meme", "LYFT": "Transport", "GM": "Auto", "TSLA": "Auto", "USAR": "Broad", "BLOK": "Crypto", "DIME": "Finance", "APO": "Finance", "APA": "Energy", "CME": "Finance", "T": "Telecom", "VZ": "Telecom", "TCEHY": "Tech", "BABA": "Tech", "BIDU": "Tech", "DHR": "Healthcare", "TMO": "Healthcare"}.items():
    SYMBOL_TO_SECTOR[_s] = _sec
SECTOR_HEDGE_FALLBACK = {"Tech": ["QQQ", "SPY"], "Finance": ["SPY"], "Energy": ["SPY"], "Healthcare": ["SPY"], "Consumer": ["SPY"], "Industrial": ["SPY"], "Crypto": ["SPY", "QQQ"], "Commodity": ["SPY"], "Bond": ["SPY"], "Meme": ["SPY", "QQQ"], "Transport": ["SPY"], "Auto": ["SPY", "QQQ"], "Telecom": ["SPY"], "Broad": ["QQQ", "SPY"]}
def get_sector(symbol: str) -> str:
    return SYMBOL_TO_SECTOR.get(symbol, "Broad")
def _minutes_to_close() -> float:
    try:
        from zoneinfo import ZoneInfo
        now_et = datetime.now(ZoneInfo("America/New_York"))
    except ImportError:
        import pytz
        now_et = datetime.now(pytz.timezone("America/New_York"))
    close_time = now_et.replace(hour=16, minute=0, second=0, microsecond=0)
    return (close_time - now_et).total_seconds() / 60
try:
    from typing import Any, Dict, List, Optional, Union

    import orjson
    def default_json_serializer(obj):
        if isinstance(obj, datetime):
            return obj.strftime('%Y-%m-%dT%H:%M:%S.%fZ')
        raise TypeError
    def json_dumps(obj: Any, **kwargs) -> bytes:
        option = orjson.OPT_SERIALIZE_NUMPY | orjson.OPT_NON_STR_KEYS | orjson.OPT_INDENT_2 # type: ignore # pylint: disable=no-member,c-extension-no-member
        return orjson.dumps(obj, default=default_json_serializer, option=option)  # type: ignore # pylint: disable=no-member,c-extension-no-member
    def safe_json_loads(s: Union[bytes, bytearray, memoryview, str], **kwargs) -> Any:
        if not s: return {}
        if isinstance(s, str):
            if not s.strip(): return {}
            s = s.encode('utf-8')
        elif isinstance(s, (bytes, bytearray, memoryview)):
            if not s.strip(): return {}
        return orjson.loads(s)  # type: ignore # pylint: disable=no-member,c-extension-no-member
    JSONDecodeError = orjson.JSONDecodeError  # type: ignore # pylint: disable=no-member,c-extension-no-member
except ImportError:  
    import json
    from typing import Any, Dict, List, Optional, Union
    def default_json_serializer(obj):
        if isinstance(obj, datetime):
            return obj.strftime('%Y-%m-%dT%H:%M:%S.%fZ')
        return str(obj)
    def json_dumps(obj: Any, **kwargs) -> str:
        kwargs.setdefault('indent', 2)
        return json.dumps(obj, default=default_json_serializer, **kwargs)
    def safe_json_loads(s: Union[bytes, bytearray, memoryview, str], **kwargs) -> Any:
        if not s: return {}
        if isinstance(s, (bytes, bytearray, memoryview)):
            s = s.decode('utf-8')
        if not s.strip(): return {}
        return json.loads(s, **kwargs)
    JSONDecodeError = json.JSONDecodeError
_global_file_write_semaphore = asyncio.Semaphore(40)
os.makedirs(config.LOG_DIR, exist_ok=True)
GLOBAL_JSON_CACHE = {}
_GLOBAL_JSON_TS = 0.0
_GLOBAL_JSON_LOCK = threading.Lock()
class InjectAccountFilter(logging.Filter):
    def filter(self, record: logging.LogRecord) -> bool:
        if not hasattr(record, 'account_key'):
            val = current_account.get()
            record.account_key = val if val else "SYS"
        return True

class WeatherLogFilter(logging.Filter):
    """tradier_manage_{acct}.log: ONLY clean forecasts. No WAITs, no other accounts."""
    def __init__(self, acct: str):
        super().__init__()
        self.acct = acct.upper()
    def filter(self, record: logging.LogRecord) -> bool:
        if getattr(record, 'account_key', 'SYS') != self.acct: return False
        if not getattr(record, 'weather_snapshot', False): return False
        msg = record.getMessage()
        # Return False if it's a "Wait" line
        return not ("WAIT" in msg or "⏳" in msg or "🛡️" in msg)

class WaitLogFilter(logging.Filter):
    """tradier_manage_wait_{acct}.log: ONLY throttled/waiting snapshots for this account."""
    def __init__(self, acct: str):
        super().__init__()
        self.acct = acct.upper()
    def filter(self, record: logging.LogRecord) -> bool:
        if getattr(record, 'account_key', 'SYS') != self.acct: return False
        if not getattr(record, 'weather_snapshot', False): return False
        msg = record.getMessage()
        # Return True ONLY if it's a "Wait" line
        return "WAIT" in msg or "⏳" in msg or "🛡️" in msg

class GeneralLogFilter(logging.Filter):
    """tradier_manage_general_{acct}.log: Actions, Errors, System Logs. NO snapshots."""
    def __init__(self, acct: str):
        super().__init__()
        self.acct = acct.upper()
    def filter(self, record: logging.LogRecord) -> bool:
        rec_acct = getattr(record, 'account_key', 'SYS')
        # Allow if it matches this account OR if it's a general system log (SYS)
        if rec_acct != self.acct and rec_acct != "SYS": return False
        # Exclude snapshots (they go to the other two files)
        return not getattr(record, 'weather_snapshot', False)

class ConsoleFilter(logging.Filter):
    """Stdout: Actions and clean Weather (No waits) for the primary account."""
    def __init__(self, acct: str = None):
        super().__init__()
        self.acct = acct.upper() if acct else None
    def filter(self, record: logging.LogRecord) -> bool:
        msg = record.getMessage()
        if 'ORDER' in msg or '[TRADE]' in msg: return True
        if not getattr(record, 'weather_snapshot', False): return False
        return not ("WAIT" in msg or "⏳" in msg)

# --- 2. LOGGER SETUP ---

def setup_logger_per_account(logger_name: str, base_path: str, target_accounts: List[str]) -> logging.Logger:
    global _logger_setup_done

    logger_obj = logging.getLogger(logger_name)
    if logger_obj.hasHandlers():
        for handler in list(logger_obj.handlers): handler.close()
        logger_obj.handlers.clear()

    logger_obj.setLevel(logging.DEBUG)
    logger_obj.propagate = False
    logger_obj.addFilter(InjectAccountFilter())

    # 1. Console (Stdout) - Filtered to clean weather
    stream_handler = logging.StreamHandler(sys.stdout)
    stream_handler.setLevel(logging.INFO)
    stream_handler.addFilter(ConsoleFilter(target_accounts[0] if target_accounts else None)) 
    stream_handler.setFormatter(logging.Formatter('[%(asctime)s] %(message)s'))
    logger_obj.addHandler(stream_handler)

    for acct in target_accounts:
        acct_l = acct.lower()
        
        # --- WEATHER LOG: tradier_manage_trb.log (Clean Forecasts) ---
        w_path = os.path.join(base_path, f"tradier_manage_{acct_l}.log")
        w_h = RotatingFileHandler(w_path, maxBytes=10*1024*1024, backupCount=5, encoding='utf-8')
        w_h.setLevel(logging.INFO)
        w_h.addFilter(WeatherLogFilter(acct))
        w_h.setFormatter(logging.Formatter('[%(asctime)s] %(message)s'))
        logger_obj.addHandler(w_h)

        # --- WAIT LOG: tradier_manage_wait_trb.log (Wait/Hold lines) ---
        wait_path = os.path.join(base_path, f"tradier_manage_wait_{acct_l}.log")
        wait_h = RotatingFileHandler(wait_path, maxBytes=10*1024*1024, backupCount=5, encoding='utf-8')
        wait_h.setLevel(logging.INFO)
        wait_h.addFilter(WaitLogFilter(acct))
        wait_h.setFormatter(logging.Formatter('[%(asctime)s] %(message)s'))
        logger_obj.addHandler(wait_h)

        # --- GENERAL LOG: tradier_manage_general_trb.log (Action/Errors) ---
        g_path = os.path.join(base_path, f"tradier_manage_general_{acct_l}.log")
        g_h = RotatingFileHandler(g_path, maxBytes=10*1024*1024, backupCount=5, encoding='utf-8')
        g_h.setLevel(logging.DEBUG)
        g_h.addFilter(GeneralLogFilter(acct))
        g_h.setFormatter(logging.Formatter('%(asctime)s - %(message)s', datefmt='%m-%d %H:%M:%S'))
        logger_obj.addHandler(g_h)
        # --- WATCHDOG HEARTBEAT LOG: ~/logs/tradier_manage_trb.log (NEVER binance/logs/) ---
        wd_path = os.path.join(config.LOG_DIR, f"tradier_manage_{acct_l}.log")
        wd_h = RotatingFileHandler(wd_path, maxBytes=5*1024*1024, backupCount=2, encoding='utf-8')
        wd_h.setLevel(logging.INFO)
        wd_h.setFormatter(logging.Formatter('%(asctime)s - %(message)s', datefmt='%m-%d %H:%M:%S'))
        logger_obj.addHandler(wd_h)

    _logger_setup_done = True
    return logger_obj

logger = setup_logger_per_account("tradier_manage", config.LOG_DIR, config.ACCOUNT_KEYS)
account_keys = getattr(config, 'ACCOUNT_KEYS', ['trc'])
wait_logger = logging.getLogger("tradier_manage_wait") # This will be handled by the same setup above or we can define it separately
_log_throttle: Dict[str, float] = {}
async def log_stoch_snapshot(trade_manager, account_key: str, context_label: str, position_key: str, current_price: float, indicators: Dict[str, Any], score: float, sentiment_rank: float, rec: str, reason: str, force: bool = False):
    try:
        # --- 1. Setup & Data Extraction ---
        if score is None: score = 0.0
        # Access logger safely
        main_logger = getattr(trade_manager, 'logger', logging.getLogger(__name__))
        
        now_ts = time.time()
        is_long = position_key.endswith("_LONG")
        
        # Robust Float Extraction
        if not indicators: indicators = {}
        def _g(k, d=50.0): return safe_fetch_float(indicators.get(k, d), d)

        # --- 2. Throttling & Zombie Logic ---
        # Direct access to trade_manager dictionary to ensure GAP calculation works
        last_check_ts = trade_manager.last_monitored_positions.get(position_key, 0)

        # Define Action Keywords
        action_kw = ["BUY", "SELL", "CLOSE", "OPEN", "AUGMENT", "REDUCE", "BLOCKED", "REENTRY"]
        is_action = any(x in rec for x in action_kw)
        
        # Gap Calculation (Time since last visit)
        gap_disp = "INIT"
        is_zombie = False
        
        if last_check_ts > 0:
            gap_sec = now_ts - last_check_ts
            
            # Zombie check (e.g. > 5 mins)
            is_zombie = gap_sec > 300
            
            # Skip high-frequency non-action updates
            if gap_sec < 10.0 and not is_action and not force:
                trade_manager.last_monitored_positions[position_key] = now_ts
                return
            
            if gap_sec < 60.0: gap_disp = f"{gap_sec:.1f}s"
            elif gap_sec < 3600: gap_disp = f"{gap_sec/60:.1f}m"
            else: gap_disp = f"{gap_sec/3600:.1f}h"
            
            if gap_sec > 120: gap_disp = f"💀{gap_disp}"
        else:
            # First time seeing this position, set baseline
            trade_manager.last_monitored_positions[position_key] = now_ts
        
        # UPDATE the timestamp so the NEXT gap is correct
        trade_manager.last_monitored_positions[position_key] = now_ts

        # Throttle logic
        throttle_key = f"{account_key}:{position_key}:{context_label}"
        last_log_time = _log_throttle.get(throttle_key, 0.0)
        # Log if: Action OR Zombie OR Forced OR Time elapsed > 60s
        if not is_action and not is_zombie and not force and (now_ts - last_log_time < 60.0): 
            return
        _log_throttle[throttle_key] = now_ts

        # --- 3. Indicator Processing ---
        k_1m = _g('stoch_k_1m', _g('k_1m')); d_1m = _g('stoch_d_1m', _g('d_1m'))
        k_5m = _g('stoch_k_5m', _g('k_5m')); d_5m = _g('stoch_d_5m', _g('d_5m'))
        k_15m = _g('stoch_k_15m', _g('k_15m')); d_15m = _g('stoch_d_15m', _g('d_15m'))
        
        # Weather Icons
        def get_weather(k, d=None, prev=None):
            if prev is not None: return "☀️" if k > prev else "☁️"
            if d is not None: return "☀️" if k > d else "☁️"
            return "•"
            
        w_str = f"[{get_weather(k_1m, d=d_1m)}{get_weather(k_5m, d=d_5m)}{get_weather(k_15m, d=d_15m)}{get_weather(_g('stoch_k_1h'), d=_g('stoch_d_1h'))}{get_weather(_g('stoch_k_4h'), d=_g('stoch_d_4h'))}{get_weather(_g('stoch_k_D'), d=_g('stoch_d_D'))}]"
        # --- 3. REVISED Age Calculation (Match get_indicators) ---
        ts_raw = indicators.get('timestamp_1m') or indicators.get('timestamp')
        lag_disp = "UNK"
        if ts_raw:
            dt_obj = ts_raw if isinstance(ts_raw, datetime) else safe_datetime(ts_raw)
            if dt_obj:
                true_lag = (datetime.now(timezone.utc) - dt_obj).total_seconds()
                
                if true_lag < 90.0: 
                    lag_disp = f"{true_lag:.0f}s" # 1m is Good
                elif true_lag < 900.0: 
                    lag_disp = f"⚠️{true_lag:.0f}s" # Macro is Good, 1m is Stale
                else: 
                    lag_disp = f"❌{true_lag:.0f}s" # Total Death
                if true_lag > 60.0 and trade_manager:
                    sym = position_key.split(':')[1].split('_')[0]
                    last_clear = getattr(trade_manager, '_last_stale_clear', {}).get(sym, 0)
                    if now_ts - last_clear > 30:
                        trade_manager.get_indicators(sym, use_cache=False)
                        if not hasattr(trade_manager, '_last_stale_clear'): trade_manager._last_stale_clear = {}
                        trade_manager._last_stale_clear[sym] = now_ts
                        logger.warning(f"[{sym}] Indicators stale ({true_lag:.0f}s), forced refresh from Redis.")
        # --- 4. PnL & Position ---
        pos = trade_manager.position_manager.get_position(position_key)
        positionAmt = abs(float(getattr(pos, 'positionAmt', 0))) if pos else 0.0
        upnl_disp = "        "
        if positionAmt > 0 and current_price > 0:
            avg_entry = float(getattr(pos, 'entry_price', 0.0) or getattr(pos, 'mark_price', 0.0))
            if avg_entry > 0:
                pct = ((current_price - avg_entry) / avg_entry) * 100.0 if is_long else ((avg_entry - current_price) / avg_entry) * 100.0
                upnl_disp = f"{pct:>+7.2f}%"

        def _fmt(k, d): return f"{int(k)}/{int(d)}"
        i1 = _fmt(k_1m, d_1m)
        i5 = _fmt(k_5m, d_5m)
        i15 = _fmt(k_15m, d_15m)
        
        sc_val = int(score)
        sc_disp = f"Sc:{sc_val:<3}" if sc_val != 0 else "       "
        
        # Use 0market_sentiment_local for Sn value as requested
        sn_val = int(_g('0market_sentiment_local', 0))
        sn_disp = f"Sn:{sn_val:<3}"
        
        # Dollar value of position instead of positionAmt
        posAmt_str = f"${int(positionAmt * current_price):<8}" if positionAmt != 0 else "     "

        rec_clean = rec.replace(position_key, "").replace("trb:", "").replace("tra:", "").replace("trc:", "").strip()
        for char in["🚀", "🟢", "🔴", "💥", "❌", "⏳", "▫️", "🛡️"]:
            rec_clean = rec_clean.replace(char, "")
        rec_clean = rec_clean.strip()[:13]

        # Determine Single Icon
        icon = "⏳" # Default
        if "WEAK" in rec or "WEAK" in reason: 
            icon = "▫️ "
        elif "HOLD" in rec:
            icon = "🛡️"
        elif (("BUY" in rec and is_long) or ("SELL" in rec and not is_long)) or "OPEN" in rec or "AUGMENT" in rec: 
            icon = "🚀" if sc_val >= 28 else "🟢"
        elif "CLOSE" in rec or "REDUCE" in rec or "EMERGENCY" in rec: 
            icon = "💥" if sc_val <= -7 else "🔴"
        elif "BLOCKED" in rec:
            icon = "✋"

        prefix_icon = "💀" if is_zombie else ("🔵" if positionAmt == 0 else "🔴")
        
        # Add Sc: to reason start
        reason_str = (f"Sc:{sc_val} {reason}" if reason else f"Sc:{sc_val}")[:50]

        # --- 6. Final Log Line ---
        log_line = (
            f"{icon}  {rec_clean:<14} {prefix_icon} {position_key:<19} {w_str} | "
            f"{current_price:<8.4f} | "
            f"1m:{i1:<10} 5m:{i5:<10} 15m:{i15:<10} | "
            f"{sn_disp} | "
            f"{upnl_disp} | Age:{lag_disp:<7} | Gap:{gap_disp:<6} | {reason_str:<35} | {posAmt_str} "
        )
        
        # Tag as weather_snapshot so filters route it correctly:
        # WeatherLogFilter → tradier_manage_{acct}.log (non-WAIT)
        # WaitLogFilter    → tradier_manage_wait_{acct}.log (WAIT/⏳)
        # GeneralLogFilter → tradier_manage_general_{acct}.log (excluded)
        main_logger.info(log_line, extra={  "weather_snapshot": True,  "account_key": account_key.upper()})

    except Exception as e:
        main_logger.info(f"Error logging snapshot: {e}")

def safe_parse_ts(ts_val):
    """Unified fast parser for ISO strings, Unix timestamps, or datetimes."""
    if not ts_val: return None
    try:
        if isinstance(ts_val, datetime):
            return ts_val if ts_val.tzinfo else ts_val.replace(tzinfo=timezone.utc)
        if isinstance(ts_val, (int, float)):
            # Detect ms (Tradier) vs seconds
            val = ts_val / 1000.0 if ts_val > 1e12 else ts_val
            return datetime.fromtimestamp(val, tz=timezone.utc)
        if isinstance(ts_val, str):
            # Fast check for ISO format
            dt = isoparse(ts_val)
            return dt if dt.tzinfo else dt.replace(tzinfo=timezone.utc)
    except Exception: return None
    return None

def safe_float(x, default=0.0):
    """Convert value to float, ensuring we never return None for Position creation"""
    try:
        if x is None:
            return default
        if isinstance(x, (int, float, Decimal)):
            return float(x)
        if isinstance(x, str):
            s = x.strip()
            if s == "":
                return default
            s = s.replace(",", "")
            return float(Decimal(s))
        if isinstance(x, dict):
            for k in ("amount", "qty", "positionAmt", "value"):
                if k in x:
                    return safe_float(x[k], default)
        return float(x)
    except (InvalidOperation, ValueError, TypeError) as e:
        # Log the problematic value so we can see what caused zeros
        try:
            logger.debug(f"[safe_float] failed to parse {repr(x)} -> {e}")
        except Exception:
            pass
        return default

def parse_ts(ts):
    if not ts: return None
    try:
        if isinstance(ts, (int, float)):
            val = ts / 1000.0 if ts > 1e11 else ts
            return datetime.fromtimestamp(val, tz=timezone.utc)
        if isinstance(ts, str):
            t = isoparse(ts)
            return t if t.tzinfo else t.replace(tzinfo=timezone.utc)
        return ts if isinstance(ts, datetime) else None
    except Exception: return None

def minutes_since(timestamp_obj, now=None):
    if not timestamp_obj:
        return 999999  # Very large number if no timestamp
    if now is None:
        now = datetime.now(timezone.utc)#.replace(microsecond=0)
    try:
        if isinstance(timestamp_obj, str):
            dt = isoparse(timestamp_obj)
        elif isinstance(timestamp_obj, datetime):
            dt = timestamp_obj
        else:
            return 999999  # Invalid type
        if not isinstance(dt, datetime):
            return 999999  # Failed to parse
        if dt.tzinfo is None:
            dt = dt.replace(tzinfo=timezone.utc)
        return (now - dt).total_seconds() / 60.0
    except Exception:
        return 999999  
def is_regular_trading_hours():
    """Checks if current time is Mon-Fri, 9:30 AM - 4:00 PM ET (handles DST)."""
    try:
        from zoneinfo import ZoneInfo
        now_et = datetime.now(ZoneInfo("America/New_York"))
    except ImportError:
        import pytz
        now_et = datetime.now(pytz.timezone("America/New_York"))
    now_est = now_et
    if now_est.weekday() >= 5: return False
    market_open = dt_time(9, 30)
    market_close = dt_time(16, 0)
    current_time = now_est.time()
    return market_open <= current_time <= market_close

def in_opening_buffer(min_minutes: float = None) -> tuple[bool, float]:
    """Return (is_in_buffer, mins_since_open). True means within OPENING_BUFFER_NO_CLOSE_MINUTES
    after the 9:30 ET open on a weekday. Used by evaluate_stop/open/augment/reentry to block
    all trade actions until first-30m data is meaningful (2026-04-26 owner directive)."""
    if min_minutes is None:
        try:
            min_minutes = float(getattr(config, "OPENING_BUFFER_NO_CLOSE_MINUTES", 30.0))
        except Exception:
            min_minutes = 30.0
    if min_minutes <= 0:
        return False, 0.0
    try:
        import pytz
        now_et = datetime.now(pytz.timezone("America/New_York"))
        if now_et.weekday() >= 5:
            return False, 0.0
        open_et = now_et.replace(hour=9, minute=30, second=0, microsecond=0)
        mins = (now_et - open_et).total_seconds() / 60.0
        if 0 <= mins < min_minutes:
            return True, mins
        return False, mins
    except Exception:
        return False, 0.0


def calculate_gain(position_side, current_price, entry_price):
    if entry_price <= 0: return 0.0
    if position_side == 'LONG':
        return ((current_price - entry_price) / entry_price) * 100
    else: 
        return ((entry_price - current_price) / entry_price) * 100 

def _convert_timestamp_strings(data: Any) -> Any:
    """Recursively convert all timestamp strings in dict/list to datetime objects - CRITICAL for all JSON loads"""
    if isinstance(data, dict):
        result = {}
        for key, value in data.items():
            if isinstance(value, str) and any(ts_key in key.lower() for ts_key in ['time', 'timestamp', 'updated', 'at', 'date']):
                try:
                    if 'T' in value and ('Z' in value or '+' in value or value.count('-') >= 2):
                        parsed = isoparse(value)
                        result[key] = parsed.replace(tzinfo=timezone.utc) if parsed.tzinfo is None else parsed
                    else:
                        result[key] = value
                except (ValueError, TypeError):
                    try:
                        parsed = pd.to_datetime(value, utc=True).to_pydatetime()
                        result[key] = parsed.replace(tzinfo=timezone.utc) if parsed.tzinfo is None else parsed
                    except (ValueError, TypeError):
                        result[key] = value
            elif isinstance(value, (dict, list)):
                result[key] = _convert_timestamp_strings(value)
            else:
                result[key] = value
        return result
    elif isinstance(data, list):
        return [_convert_timestamp_strings(item) for item in data]
    return data
async def load_json_safe(file_path: Union[str, Path], account_key: str = None) -> dict:
    file_path = Path(file_path)
    # No logging on entry
    
    async with _global_file_write_semaphore:
        content = b""
        try:
            if not file_path.exists():
                return {}
            
            async with aiofiles.open(file_path, "rb") as f:
                content = await f.read()
            
            if not content.strip():
                return {}
                
            data = safe_json_loads(content)
            
        except Exception:
            # --- EXTREME RECOVERY MODE ---
            data = {}
            recovery_success = False
            
            # 1. Attempt Truncation Repair (Fix "Extra data" error)
            try:
                txt = content.decode('utf-8', errors='ignore').strip()
                # Find the last closing brace that makes valid JSON
                # Simple heuristic: rfind '}'
                end_idx = txt.rfind('}')
                if end_idx != -1:
                    fixed_txt = txt[:end_idx+1]
                    data = orjson.loads(fixed_txt)
                    recovery_success = True
                    # Silently overwrite fixed content back to disk
                    async with aiofiles.open(file_path, "wb") as f:
                        await f.write(orjson.dumps(data, option=orjson.OPT_INDENT_2))
            except Exception:
                pass

            # 2. Fallback to Backup
            if not recovery_success:
                backup_path = file_path.with_suffix(file_path.suffix + ".bak")
                if backup_path.exists():
                    try:
                        async with aiofiles.open(backup_path, "rb") as f:
                            bk_content = await f.read()
                        data = safe_json_loads(bk_content)
                        recovery_success = True
                        # Restore backup to main file
                        async with aiofiles.open(file_path, "wb") as f:
                            await f.write(bk_content)
                    except Exception:
                        pass
            if not recovery_success:
                try:
                    if file_path.exists():
                        os.remove(file_path)
                except Exception:
                    pass
                return {}

        if not isinstance(data, dict):
            return {}

        # Timestamp conversion 
        data = _convert_timestamp_strings(data) 

        # # Self-heal: If we recovered it, save the good version back
        # # This part was missing in the manual copy-paste
        # try:
        #     if 'recovery_success' in locals() and recovery_success:
        #         asyncio.create_task(_heal_corrupted_file(file_path, data))
        # except Exception: pass
        
        return data

class TrackingSet(set):
    """Custom set that tracks when items are added"""
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._added_at = {}
    
    def add(self, item):
        super().add(item)
        self._added_at[item] = time.time()
    
    def discard(self, item):
        super().discard(item)
        self._added_at.pop(item, None)
    
    def remove(self, item):
        super().remove(item)
        self._added_at.pop(item, None)
    
    def clear(self):
        super().clear()
        self._added_at.clear()
    
    def get_added_at(self, item):
        return self._added_at.get(item, 0)


async def periodic_leaderboards(order_queue, trade_manager):
    while getattr(trade_manager, "running", False):
        try:
            await trade_manager.load_leaderboards()
            await asyncio.sleep(900)
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.debug(f"[periodic_leaderboards] {e}")
            await asyncio.sleep(10)

async def periodic_evaluate_reentry_loop(trade_manager):
    """
    Scans for positions that were REDUCED and need RE-ENTRY.
    """
    logger.info("[TASK] Re-entry Scanner Started")
    while getattr(trade_manager, "running", False):
        try:
            await asyncio.sleep(45) # Check every 45s
            
            re_keys_by_acct = defaultdict(list)
            
            # Iterate ALL positions in the manager
            if trade_manager.position_manager:
                for pk, pos in trade_manager.position_manager.positions.items():
                    if not pos: continue
                    
                    # Check Re-entry Criteria
                    was_reduced = getattr(pos, 'was_reduced', False)
                    last_red_time = getattr(pos, 'last_reduction_time', None)
                    
                    if was_reduced and last_red_time:
                        # Extract Account Key
                        parts = pk.split(':')
                        if len(parts) > 0:
                            acc = parts[0]
                            re_keys_by_acct[acc].append(pk)

            # Dispatch to Monitor
            for acc, keys in re_keys_by_acct.items():
                if keys:
                    logger.info(f"[{acc}] ♻️ Evaluating {len(keys)} positions for Re-Entry...")
                    # await trade_manager.strategy.evaluate_reentry(symbol,position, indicators)
                    await monitor_entries( trade_manager.order_queue,  trade_manager,   acc,   keys,   event_type="reentry_scan",    is_priority_add=True,   force=True  )

        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.error(f"[REENTRY_LOOP] Error: {e}",  exc_info=True )
            await asyncio.sleep(10)

def safe_fetch_float(value, default=0.0):
    try:
        if value is None: return default
        if isinstance(value, (float, int)): return float(value)
        return float(str(value).replace(',', '').strip())
    except Exception:
        return default


async def obligatory_sector_hedge_or_close_loop(trade_manager):
    """USER MANDATE 2026-05-09: stocks (Tradier) cannot hold both LONG+SHORT same symbol simultaneously.
    For trb: when a position is in loss with wt1_5m AND wt1_1h against the trade direction →
    OBLIGATORY same-SECTOR hedge (different ticker, opposite direction, same sector). If no
    eligible mate found OR hedge order fails → close losing position with HEDGE_FAILED reason.

    ⚠️ APPROXIMATION WARNING — sector beta differs across names; this is NOT perfect netting
    like the Binance perp same-symbol hedge. Each fire is logged loudly so the user can monitor.
    """
    if not bool(getattr(config, 'OBLIGATORY_SECTOR_HEDGE_ENABLED', True)):
        logger.warning("[OBLIGATORY_SECTOR_HEDGE_LOOP] DISABLED via OBLIGATORY_SECTOR_HEDGE_ENABLED=False")
        return
    interval = float(getattr(config, 'OBLIGATORY_SECTOR_HEDGE_LOOP_INTERVAL_SECONDS', 90.0))
    require_5m_1h = bool(getattr(config, 'OBLIGATORY_SECTOR_HEDGE_TRIGGER_REQUIRE_WT_5M_AND_1H', True))
    fallback_close_enabled = bool(getattr(config, 'HEDGE_FAILED_FALLBACK_CLOSE_ENABLED', True))
    min_loss_pct = float(getattr(config, 'OBLIGATORY_HEDGE_MIN_LOSS_PCT', -0.5))
    logger.critical(f"[OBLIGATORY_SECTOR_HEDGE_LOOP] STARTED. interval={interval}s require_wt_5m_AND_1h={require_5m_1h} min_loss={min_loss_pct}% fallback_close={fallback_close_enabled}")
    _hedge_attempts: dict = {}  # losing_pk -> last attempt ts (per-loser cooldown)
    _COOLDOWN_S = 600.0
    while getattr(trade_manager, 'running', False):
        try:
            await asyncio.sleep(interval)
            if not is_regular_trading_hours():
                continue
            try:
                from tradier_sector_ls_ratio import _load_sector_map, get_sector
            except Exception as _ie:
                logger.error(f"[OBLIGATORY_SECTOR_HEDGE_LOOP] sector helper import failed: {_ie}")
                continue
            for account_key in ('trb',):  # only trb supports stocks shorting; tra is long-only, trc is mirror
                try:
                    if not hasattr(trade_manager, 'position_manager') or trade_manager.position_manager is None:
                        continue
                    positions = trade_manager.position_manager.get_positions_by_account(account_key) or {}
                except Exception as _pe:
                    logger.error(f"[OBLIGATORY_SECTOR_HEDGE_LOOP][{account_key}] position fetch err: {_pe}")
                    continue
                for pk, pos in list(positions.items()):
                    try:
                        pa = abs(safe_fetch_float(getattr(pos, 'positionAmt', 0), 0))
                        if pa <= 0:
                            continue
                        gain = safe_fetch_float(getattr(pos, 'gain', 0), 0.0)
                        if gain >= min_loss_pct:
                            continue
                        if (time.time() - _hedge_attempts.get(pk, 0.0)) < _COOLDOWN_S:
                            continue
                        symbol = getattr(pos, 'symbol', None) or pk.split(':')[1].replace('_LONG', '').replace('_SHORT', '')
                        is_long = pk.endswith('_LONG')
                        loser_side = 'LONG' if is_long else 'SHORT'
                        # WT trigger gate: 5m AND 1h against
                        ind = trade_manager.indicators_cache.get(symbol.upper(), {}) if hasattr(trade_manager, 'indicators_cache') else {}
                        if not ind:
                            logger.info(f"[SECTOR_HEDGE_NO_IND] {pk}: no indicators cached for {symbol} — skip cycle")
                            continue
                        wt1_5m = safe_fetch_float(ind.get('wt1_5m', 0), 0)
                        wt2_5m = safe_fetch_float(ind.get('wt2_5m', 0), 0)
                        wt1_1h = safe_fetch_float(ind.get('wt1_1h', 0), 0)
                        wt2_1h = safe_fetch_float(ind.get('wt2_1h', 0), 0)
                        wt_5m_against = (is_long and wt1_5m < wt2_5m) or ((not is_long) and wt1_5m > wt2_5m)
                        wt_1h_against = (is_long and wt1_1h < wt2_1h) or ((not is_long) and wt1_1h > wt2_1h)
                        if require_5m_1h and not (wt_5m_against and wt_1h_against):
                            continue
                        # Check if a hedge already exists for this loser via tracker (skip if so)
                        # Tradier doesn't have the same active_hedges structure as crypto; rely on
                        # mate lookup + cooldown to prevent stacking.
                        sector = None
                        try:
                            sector = get_sector(symbol)
                        except Exception:
                            sector = None
                        if not sector:
                            logger.warning(f"⚠️ [SECTOR_HEDGE_NO_SECTOR] {pk}: no sector found for {symbol} — fallback close")
                            if fallback_close_enabled:
                                await _fire_fallback_close_trb(trade_manager, account_key, pk, f"HEDGE_FAILED_FALLBACK_CLOSE_no_sector_g{gain:.2f}%")
                            _hedge_attempts[pk] = time.time()
                            continue
                        mate_side = 'SHORT' if is_long else 'LONG'  # opposite to loser
                        sector_map = _load_sector_map() or {}
                        mates = list(sector_map.get(sector, []))
                        chosen_mate = None
                        chosen_mate_pk = None
                        for mate in mates:
                            if not mate or mate == symbol:
                                continue
                            mate_pk = f"{account_key}:{mate}_{mate_side}"
                            # Tradeable check
                            if hasattr(trade_manager, 'tradeable_keys') and trade_manager.tradeable_keys:
                                if mate_pk not in trade_manager.tradeable_keys:
                                    continue
                            # Already-open check: skip if mate side already has a position
                            existing_mate_pos = positions.get(mate_pk)
                            if existing_mate_pos and abs(safe_fetch_float(getattr(existing_mate_pos, 'positionAmt', 0), 0)) > 0:
                                continue
                            mate_ind = trade_manager.indicators_cache.get(mate.upper(), {}) if hasattr(trade_manager, 'indicators_cache') else {}
                            if not mate_ind:
                                continue
                            m1_5 = safe_fetch_float(mate_ind.get('wt1_5m', 0), 0)
                            m2_5 = safe_fetch_float(mate_ind.get('wt2_5m', 0), 0)
                            m1_h = safe_fetch_float(mate_ind.get('wt1_1h', 0), 0)
                            m2_h = safe_fetch_float(mate_ind.get('wt2_1h', 0), 0)
                            mate_5_aligned = (mate_side == 'LONG' and m1_5 > m2_5) or (mate_side == 'SHORT' and m1_5 < m2_5)
                            mate_h_aligned = (mate_side == 'LONG' and m1_h > m2_h) or (mate_side == 'SHORT' and m1_h < m2_h)
                            if mate_5_aligned and mate_h_aligned:
                                chosen_mate = mate
                                chosen_mate_pk = mate_pk
                                break
                        if not chosen_mate:
                            logger.warning(f"⚠️ [SECTOR_HEDGE_NO_MATE] {pk}: no eligible {mate_side} mate in sector={sector} (mates_checked={len(mates)}) — fallback close")
                            if fallback_close_enabled:
                                await _fire_fallback_close_trb(trade_manager, account_key, pk, f"HEDGE_FAILED_FALLBACK_CLOSE_no_mate_sector{sector}_g{gain:.2f}%")
                            _hedge_attempts[pk] = time.time()
                            continue
                        # Size: same notional as loser. Need mate price for shares.
                        mate_ind_full = trade_manager.indicators_cache.get(chosen_mate.upper(), {})
                        mate_price = safe_fetch_float(mate_ind_full.get('current_price', 0), 0) or safe_fetch_float(mate_ind_full.get('close', 0), 0)
                        loser_mark = safe_fetch_float(getattr(pos, 'mark_price', 0), 0)
                        if mate_price <= 0 or loser_mark <= 0:
                            logger.warning(f"⚠️ [SECTOR_HEDGE_NO_PRICE] {pk}: missing prices loser={loser_mark} mate={chosen_mate}@{mate_price} — fallback close")
                            if fallback_close_enabled:
                                await _fire_fallback_close_trb(trade_manager, account_key, pk, f"HEDGE_FAILED_FALLBACK_CLOSE_no_price_g{gain:.2f}%")
                            _hedge_attempts[pk] = time.time()
                            continue
                        loser_notional = pa * loser_mark
                        # Cap to START_POSITION_SIZE × 2 to prevent runaway sizing on a deep loser
                        _start_size = float(getattr(config, 'START_POSITION_SIZE', 600))
                        target_notional = min(loser_notional, _start_size * 2.0)
                        mate_qty_raw = target_notional / mate_price
                        mate_qty = max(1, int(mate_qty_raw))
                        logger.critical(
                            f"⚠️ [SECTOR_HEDGE_FIRE] loser={pk} g={gain:.2f}% sector={sector} → opening {mate_side} on {chosen_mate} qty={mate_qty} (~${target_notional:.0f}). "
                            f"⚠️ APPROXIMATION (sector beta != 1:1) — WATCH CLOSELY."
                        )
                        _hedge_reason = f"OBLIGATORY_SECTOR_HEDGE_for_{symbol}_{loser_side}_g{gain:.2f}%_sector={sector}_APPROX"
                        try:
                            _result = await queue_trade_action(
                                trade_manager.order_queue, trade_manager, chosen_mate_pk, "OPEN",
                                _hedge_reason, 85.0, override_qty=mate_qty,
                            )
                        except Exception as _qe:
                            logger.error(f"💥 [SECTOR_HEDGE_QUEUE_CRASH] {pk}→{chosen_mate_pk}: {_qe}", exc_info=True)
                            _result = False
                        _hedge_attempts[pk] = time.time()
                        if _result and (_result is True or (isinstance(_result, str) and _result.startswith('SUCCESS'))):
                            logger.warning(f"✅ [SECTOR_HEDGE_OPENED] {pk}→{chosen_mate_pk}: hedge order queued. APPROXIMATION — monitor manually.")
                        else:
                            logger.critical(f"☠️ [SECTOR_HEDGE_FAILED] {pk}: hedge order on {chosen_mate_pk} failed (result={_result}) — fallback close losing position")
                            if fallback_close_enabled:
                                await _fire_fallback_close_trb(trade_manager, account_key, pk, f"HEDGE_FAILED_FALLBACK_CLOSE_mate{chosen_mate}_failed_g{gain:.2f}%")
                    except Exception as _pe:
                        logger.error(f"[SECTOR_HEDGE_PER_POS_ERR] {pk}: {_pe}", exc_info=True)
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.error(f"[OBLIGATORY_SECTOR_HEDGE_LOOP] error: {e}", exc_info=True)
            await asyncio.sleep(30)


async def _fire_fallback_close_trb(trade_manager, account_key, position_key, reason):
    """Force-close a losing trb position via queue_trade_action with HEDGE_FAILED_FALLBACK_CLOSE reason.
    Reason string contains 'HEDGE_FAILED' so any UNIVERSAL_NOLOSS-style bypass that substring-matches works.
    Uses override_qty=999999 (clamped to actual qty in execute_trade_action)."""
    try:
        logger.critical(f"☠️ [HEDGE_FAILED_FALLBACK_CLOSE] {position_key}: closing losing position — reason={reason}")
        await queue_trade_action(
            trade_manager.order_queue, trade_manager, position_key, "CLOSE",
            reason, 100.0, override_qty=999999,
        )
    except Exception as _e:
        logger.error(f"[HEDGE_FAILED_FALLBACK_CLOSE_CRASH] {position_key}: {_e}", exc_info=True)

_OVERNIGHT_ONE_SIDED_SECTORS = frozenset({
    'energy_oil_gas',         # longs only — no shorts in symbols_trb_short
    'base_metals_mining',     # shorts only — no longs in symbols_trb_long
    'defense_aerospace',      # shorts only — no longs
    'commodities_crypto_etf', # longs only — no shorts
})

async def overnight_gap_hedge_loop(trade_manager):
    """Opens same-sector opposite-side hedges (50% notional) 15 min before market close
    when |market_sentiment_score| > threshold, then closes them at 09:35 ET next morning.

    Rationale: overnight gaps (close→open) carry most stock volatility. Same-sector hedge
    provides approximate protection since same-symbol hedging is impossible on Tradier.

    Only fires for: tech_ai_chips, precious_metals, uranium_nuclear, agriculture_fertilizer,
    consumer_media. Skips one-sided sectors (no opposite-side mates available).
    """
    if not bool(getattr(config, 'OVERNIGHT_GAP_HEDGE_ENABLED', False)):
        return
    open_minutes = float(getattr(config, 'OVERNIGHT_GAP_HEDGE_OPEN_MINUTES', 15.0))
    close_minutes = float(getattr(config, 'OVERNIGHT_GAP_HEDGE_CLOSE_MINUTES', 5.0))
    sentiment_threshold = float(getattr(config, 'OVERNIGHT_GAP_HEDGE_SENTIMENT_THRESHOLD', 20.0))
    size_frac = float(getattr(config, 'OVERNIGHT_GAP_HEDGE_SIZE_FRAC', 0.50))
    _hedge_tracker: dict = {}
    _hedges_fired_today = False
    _hedges_closed_today = False
    logger.warning(f"[OVERNIGHT_GAP_HEDGE] STARTED open={open_minutes}min_pre_close close={close_minutes}min_post_open threshold=±{sentiment_threshold} size={size_frac}")
    while getattr(trade_manager, 'running', False):
        try:
            await asyncio.sleep(60)
            try:
                from zoneinfo import ZoneInfo
                now_et = datetime.now(ZoneInfo("America/New_York"))
            except ImportError:
                import pytz
                now_et = datetime.now(pytz.timezone("America/New_York"))
            if now_et.weekday() >= 5:
                continue
            mins_to_close = _minutes_to_close()
            mins_since_open = (now_et.hour * 60 + now_et.minute) - (9 * 60 + 30)
            if 11 * 60 <= (now_et.hour * 60 + now_et.minute) < 12 * 60:
                _hedges_fired_today = False
                _hedges_closed_today = False
            if close_minutes <= mins_since_open <= close_minutes + 5 and not _hedges_closed_today and _hedge_tracker:
                logger.warning(f"[OVERNIGHT_GAP_HEDGE] MORNING CLOSE: closing {len(_hedge_tracker)} overnight hedges at 09:35 ET")
                for _src_pk, _entry in list(_hedge_tracker.items()):
                    _hedge_pk = _entry.get('hedge_pk')
                    if not _hedge_pk:
                        continue
                    try:
                        await queue_trade_action(
                            trade_manager.order_queue, trade_manager, _hedge_pk, "CLOSE",
                            f"OVERNIGHT_GAP_HEDGE_REMOVE_for_{_src_pk}", 100.0, override_qty=999999,
                        )
                        logger.warning(f"[OVERNIGHT_GAP_HEDGE] closed {_hedge_pk} for {_src_pk}")
                    except Exception as _ce:
                        logger.error(f"[OVERNIGHT_GAP_HEDGE_CLOSE_ERR] {_hedge_pk}: {_ce}", exc_info=True)
                _hedge_tracker.clear()
                _hedges_closed_today = True
                continue
            if not (0 < mins_to_close <= open_minutes) or _hedges_fired_today:
                continue
            sentiment_score = 0.0
            for _cache_ind in (getattr(trade_manager, 'indicators_cache', None) or {}).values():
                _s = _cache_ind.get('0market_sentiment_score', None)
                if _s is not None:
                    sentiment_score = float(_s)
                    break
            if abs(sentiment_score) <= sentiment_threshold:
                logger.info(f"[OVERNIGHT_GAP_HEDGE] sentiment={sentiment_score:.1f} within ±{sentiment_threshold} — no hedge today")
                _hedges_fired_today = True
                continue
            hedge_for_longs = sentiment_score < -sentiment_threshold
            hedge_for_shorts = sentiment_score > sentiment_threshold
            logger.warning(f"[OVERNIGHT_GAP_HEDGE] FIRING: sentiment={sentiment_score:.1f} → hedging {'LONGs→SHORT' if hedge_for_longs else 'SHORTs→LONG'}")
            try:
                from tradier_sector_ls_ratio import _load_sector_map, get_sector
            except Exception as _ie:
                logger.error(f"[OVERNIGHT_GAP_HEDGE] sector import failed: {_ie}")
                _hedges_fired_today = True
                continue
            sector_map = _load_sector_map() or {}
            try:
                positions = trade_manager.position_manager.get_positions_by_account('trb') or {}
            except Exception as _pe:
                logger.error(f"[OVERNIGHT_GAP_HEDGE] positions fetch err: {_pe}")
                _hedges_fired_today = True
                continue
            hedges_opened = 0
            for pk, pos in list(positions.items()):
                try:
                    pa = abs(safe_fetch_float(getattr(pos, 'positionAmt', 0), 0))
                    if pa <= 0 or pk in _hedge_tracker:
                        continue
                    is_long = pk.endswith('_LONG')
                    if (is_long and not hedge_for_longs) or (not is_long and not hedge_for_shorts):
                        continue
                    symbol = pk.split(':')[1].replace('_LONG', '').replace('_SHORT', '')
                    sector = get_sector(symbol)
                    if not sector or sector in _OVERNIGHT_ONE_SIDED_SECTORS:
                        continue
                    mate_side = 'SHORT' if is_long else 'LONG'
                    chosen_mate = None
                    chosen_mate_pk = None
                    for mate in sector_map.get(sector, []):
                        if not mate or mate == symbol:
                            continue
                        mate_pk = f"trb:{mate}_{mate_side}"
                        if hasattr(trade_manager, 'tradeable_keys') and mate_pk not in trade_manager.tradeable_keys:
                            continue
                        existing = positions.get(mate_pk)
                        if existing and abs(safe_fetch_float(getattr(existing, 'positionAmt', 0), 0)) > 0:
                            continue
                        chosen_mate = mate
                        chosen_mate_pk = mate_pk
                        break
                    if not chosen_mate:
                        logger.info(f"[OVERNIGHT_GAP_HEDGE] {pk}: no available {mate_side} mate in sector={sector}")
                        continue
                    loser_mark = safe_fetch_float(getattr(pos, 'mark_price', 0), 0)
                    if loser_mark <= 0:
                        continue
                    target_notional = pa * loser_mark * size_frac
                    mate_ind = (getattr(trade_manager, 'indicators_cache', None) or {}).get(chosen_mate.upper(), {})
                    mate_price = safe_fetch_float(mate_ind.get('current_price', 0), 0) or safe_fetch_float(mate_ind.get('close', 0), 0)
                    if mate_price <= 0:
                        logger.warning(f"[OVERNIGHT_GAP_HEDGE] {pk}: no price for mate {chosen_mate}")
                        continue
                    mate_qty = max(1, int(target_notional / mate_price))
                    _reason = f"OVERNIGHT_GAP_HEDGE_for_{symbol}_sector={sector}_sent={sentiment_score:.0f}"
                    logger.warning(f"[OVERNIGHT_GAP_HEDGE] opening {mate_side} {chosen_mate} qty={mate_qty} (~${target_notional:.0f}) for {pk} sector={sector}")
                    _result = await queue_trade_action(
                        trade_manager.order_queue, trade_manager, chosen_mate_pk, "OPEN",
                        _reason, 84.0, override_qty=mate_qty,
                    )
                    if _result and (_result is True or (isinstance(_result, str) and _result.startswith('SUCCESS'))):
                        _hedge_tracker[pk] = {'hedge_pk': chosen_mate_pk, 'opened_ts': time.time(), 'notional': target_notional}
                        hedges_opened += 1
                        logger.warning(f"✅ [OVERNIGHT_GAP_HEDGE] opened {chosen_mate_pk} for {pk}")
                    else:
                        logger.error(f"[OVERNIGHT_GAP_HEDGE] failed to open {chosen_mate_pk}: {_result}")
                except Exception as _pe:
                    logger.error(f"[OVERNIGHT_GAP_HEDGE_PER_POS] {pk}: {_pe}", exc_info=True)
            _hedges_fired_today = True
            logger.warning(f"[OVERNIGHT_GAP_HEDGE] DONE: {hedges_opened} hedges opened for tonight")
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.error(f"[OVERNIGHT_GAP_HEDGE] loop error: {e}", exc_info=True)
            await asyncio.sleep(30)


async def monitor_entries(order_queue: "OrderQueue", trade_manager, account_key: str, position_keys=None, event_type=None, is_priority_add: bool = False, force: bool = False):
    current_account.set(account_key)
    now_ts = time.time()
    
    try:
        # 1. Market Hours Gate (Removed hard return to allow Weather Monitoring off-hours)
        if not is_regular_trading_hours() and not force:
            if now_ts % 60 < 2: 
                logger.info(f"[{account_key}] 💤 Market Closed. Monitoring only.")

        # 2. Queue Management (Standard Deque Logic)
        if account_key not in trade_manager.account_position_queues:
            trade_manager.account_position_queues[account_key] = {'queue': deque(), 'set': set()}
        
        q_data = trade_manager.account_position_queues[account_key]
        
        if position_keys:
            # Normalize keys
            incoming = [position_keys] if isinstance(position_keys, str) else position_keys
            # De-duplicate
            unique_incoming = list(dict.fromkeys(incoming))
            
            for k in unique_incoming:
                if k not in q_data['set']:
                    if is_priority_add: q_data['queue'].appendleft(k)
                    else: q_data['queue'].append(k)
                    q_data['set'].add(k)

        if not q_data['queue']: 
            return

        # 3. Process Batch (Up to 100 at a time)
        batch = []
        for _ in range(min(len(q_data['queue']), 100)):
            batch.append(q_data['queue'].popleft())
            q_data['set'].discard(batch[-1])

        # 4. The Gates
        for position_key in batch:
            _, symbol, position_side = parse_position_key(position_key)
            if symbol.upper() in trade_manager.blacklist:
                if config.VERBOSE: 
                    logger.debug(f"[{account_key}] 🛑 BLACKLIST {symbol}: Ignored (External Management).")
                continue

            # --- CHECK 1: PROCESSING LOCK (Prevent double-processing) ---
            # If it's already running in another task, skip it to prevent race conditions.
            # We rarely force this unless we are debugging.
            if position_key in trade_manager.processing_keys and not force:
                # logger.debug(f"[{account_key}] SKIP {symbol}: Already processing.")
                continue

            # --- CHECK 2: COOLDOWN (Don't spam) ---
            # force=True BYPASSES THIS (e.g. Monitor Loop / Pulse)
            cd = float(trade_manager.recently_processed_signals.get(position_key, 0.0))
            if not force and (now_ts < cd):
                logger.debug(f"[{account_key}] SKIP {symbol}: Cooldown active ({int(cd-now_ts)}s).")
                continue

            # --- CHECK 3: DEDUPLICATION (Don't double order) ---
            # force=True BYPASSES THIS
            async with trade_manager.dedupe_lock:
                last_order = trade_manager.order_deduplication.get(position_key, 0)
                if not force and (now_ts - last_order < 90.0):
                    logger.info(f"[{account_key}] SKIP {symbol}: Order recently queued (<90s).")
                    continue

            # --- CHECK 4: LEADERBOARD/SYMBOL FILTER (The "Is this allowed?" Gate) ---
            # This is where trades often die silently.
            
            # Use centralized check
            is_allowed = trade_manager.is_symbol_tradeable(symbol, account_key, position_side)
            if force: is_allowed = True

            if not is_allowed:
                # LOG TO GENERAL: This tells you why a symbol isn't being traded!
                if config.VERBOSE: 
                    logger.info(f"[{account_key}] ⛔ FILTER BLOCK {symbol} {position_side}: Not in approved list.")
                continue


            trade_manager.processing_keys.add(position_key)
            try:
                result = await process_position(account_key, position_key, order_queue, trade_manager, event_type=event_type, force=force)
                if result == "PROCESSED":
                    trade_manager.position_last_processed[position_key] = time.time()
            finally:
                trade_manager.processing_keys.discard(position_key)
    except Exception as e:
        logger.error(f"[{account_key}] 💥 CRASH in monitor_entries: {e}",  exc_info=True )

# ═══ 2.5σ STDEV BREAKOUT: HTF breakout detection for stocks ═══
_stdev_bb_history_t: dict = {}  # {symbol_tf: deque(maxlen=20)}
_stdev_breakout_state_t: dict = {}  # {symbol: {active, direction, ...}}

def _get_bb_pctb_t(symbol: str, tf: str, i: dict) -> float:
    """Compute BB pct_b from rolling close buffer (tradier variant)."""
    pctb = safe_fetch_float(i.get(f'bb_pct_b_{tf}'), None)
    if pctb is not None:
        return pctb
    # In tradier, parsed indicators have close_{tf}_prev but not close_{tf}
    close = safe_fetch_float(i.get(f'close_{tf}', 0), 0)
    if close <= 0:
        close = safe_fetch_float(i.get(f'close_{tf}_prev', 0), 0)
    if close <= 0:
        close = safe_fetch_float(i.get('current_price', 0), 0)
    if close <= 0:
        return 0.5
    buf_key = f"{symbol}_{tf}"
    if buf_key not in _stdev_bb_history_t:
        _stdev_bb_history_t[buf_key] = deque(maxlen=20)
    buf = _stdev_bb_history_t[buf_key]
    if not buf or abs(buf[-1] - close) > 1e-8:
        buf.append(close)
    if len(buf) < 20:
        return 0.5
    vals = list(buf)
    mean = sum(vals) / len(vals)
    variance = sum((v - mean) ** 2 for v in vals) / len(vals)
    std = variance ** 0.5
    if std <= 0:
        return 0.5
    upper = mean + 2.0 * std
    lower = mean - 2.0 * std
    bw = upper - lower
    return (close - lower) / bw if bw > 0 else 0.5


def _update_stdev_state(symbol: str, i: dict):
    """Update breakout state continuously (called every bar). Does NOT return signals."""
    if not getattr(config, "STDEV_BREAKOUT_ENABLED", False):
        return
    htf_list = getattr(config, "STDEV_BREAKOUT_HTF_LIST", ["D", "4h"])
    pctb_long = getattr(config, "STDEV_BREAKOUT_PCTB_LONG", 1.125)
    pctb_short = getattr(config, "STDEV_BREAKOUT_PCTB_SHORT", -0.125)
    rvol_min = getattr(config, "STDEV_BREAKOUT_RVOL_MIN", 1.2)
    max_age = getattr(config, "STDEV_BREAKOUT_MAX_AGE_BARS", 50)
    for is_long in [True, False]:
        _state_key = f"{symbol}_{'L' if is_long else 'S'}"
        if _state_key not in _stdev_breakout_state_t:
            _stdev_breakout_state_t[_state_key] = {'active': False, 'direction': None, 'breakout_time': 0.0, 'breakout_pctb': 0.0, 'htf': None, 'retest_count': 0, 'bars_since': 0}
        state = _stdev_breakout_state_t[_state_key]
        if state['active']:
            state['bars_since'] += 1
            if state['bars_since'] > max_age:
                state['active'] = False
            continue
        # Check for NEW breakout
        for tf in htf_list:
            pctb = _get_bb_pctb_t(symbol, tf, i)
            rvol = safe_fetch_float(i.get(f'relative_volume_{tf}', 0), 0)
            if rvol <= 0:
                rvol = safe_fetch_float(i.get('relative_volume_1h', 0), 0)
            if rvol <= 0:
                rvol = safe_fetch_float(i.get('relative_volume_15m', 0), 0)
            if is_long and pctb > pctb_long and rvol >= rvol_min:
                state.update({'active': True, 'direction': 'LONG', 'breakout_time': time.time(), 'breakout_pctb': pctb, 'htf': tf, 'retest_count': 0, 'bars_since': 0})
                break
            elif not is_long and pctb < pctb_short and rvol >= rvol_min:
                state.update({'active': True, 'direction': 'SHORT', 'breakout_time': time.time(), 'breakout_pctb': pctb, 'htf': tf, 'retest_count': 0, 'bars_since': 0})
                break


def detect_stdev_breakout_t(symbol: str, is_long: bool, i: dict) -> dict:
    """Read breakout state (set by _update_stdev_state). Returns entry signal or None."""
    if not getattr(config, "STDEV_BREAKOUT_ENABLED", False):
        return None
    _state_key = f"{symbol}_{'L' if is_long else 'S'}"
    state = _stdev_breakout_state_t.get(_state_key)
    if not state or not state['active']:
        return None
    retest_tf_list = getattr(config, "STDEV_BREAKOUT_RETEST_TF_LIST", ["1h", "15m"])
    retest_pctb_min = getattr(config, "STDEV_BREAKOUT_RETEST_PCTB_MIN", 0.85)
    retest_pctb_max = getattr(config, "STDEV_BREAKOUT_RETEST_PCTB_MAX", 1.05)
    max_retests = getattr(config, "STDEV_BREAKOUT_MAX_RETESTS", 3)
    # Breakout is active — return entry signal
    # First entry for this breakout cycle: full breakout signal
    if state['retest_count'] == 0:
        state['retest_count'] = 1  # Mark as "initial entry done"
        return {'signal': 'BUY' if is_long else 'SELL', 'phase': 'BREAKOUT', 'htf': state['htf'], 'pctb': state['breakout_pctb'], 'rvol': 0, 'size_mult': 1.0, 'score': getattr(config, "STDEV_BREAKOUT_SCORE", 25)}
    # Subsequent entries: retest (scale-in with larger size)
    if state['retest_count'] >= max_retests + 1:
        return None
    # Phase 2: Retest
    for tf in retest_tf_list:
        pctb = _get_bb_pctb_t(symbol, tf, i)
        k_tf = safe_fetch_float(i.get(f'stoch_k_{tf}', 50), 50)
        d_tf = safe_fetch_float(i.get(f'stoch_d_{tf}', 50), 50)
        if is_long:
            if retest_pctb_min <= pctb <= retest_pctb_max and k_tf > d_tf and k_tf < 65:
                state['retest_count'] += 1
                return {'signal': 'BUY', 'phase': 'RETEST', 'htf': state['htf'], 'retest_tf': tf, 'pctb': pctb, 'retest_num': state['retest_count'], 'size_mult': getattr(config, "STDEV_BREAKOUT_RETEST_SIZE_MULT", 1.5), 'score': getattr(config, "STDEV_BREAKOUT_RETEST_SCORE", 22)}
        else:
            if (1.0 - retest_pctb_max) <= pctb <= (1.0 - retest_pctb_min) and k_tf < d_tf and k_tf > 35:
                state['retest_count'] += 1
                return {'signal': 'SELL', 'phase': 'RETEST', 'htf': state['htf'], 'retest_tf': tf, 'pctb': pctb, 'retest_num': state['retest_count'], 'size_mult': getattr(config, "STDEV_BREAKOUT_RETEST_SIZE_MULT", 1.5), 'score': getattr(config, "STDEV_BREAKOUT_RETEST_SCORE", 22)}
    return None


async def process_position(account_key: str, position_key: str, order_queue: "OrderQueue", trade_manager, event_type=None, force: bool = False):
    current_account.set(account_key)
    try:
        ak, symbol, position_side = parse_position_key(position_key)
        if ak != account_key: return
        if force:
            logger.info(f"[{account_key}] 🔎 MONITORING {position_key} (Force=True)")
        now_ts = time.time()
        last_mon = trade_manager.last_monitored_positions.get(position_key, 0.0)
        position = trade_manager.position_manager.get_position(position_key)
        has_position = position and abs(float(getattr(position, 'positionAmt', 0))) > 0
        if account_key in ("tra", "trb", "trc"):
            try:
                import tradier_advisory_consumer as _ag_adv
                _ag_obj = _ag_adv.check(account_key, symbol, position_side, "process")
                if _ag_obj:
                    _ag_tag = account_key.upper()
                    _ag_action = _ag_obj.get("action")
                    _ag_reason = (_ag_obj.get("reason") or "")[:60]
                    _ag_pos_amt = abs(float(getattr(position, 'positionAmt', 0))) if position else 0.0
                    if _ag_action == "force_close" and _ag_pos_amt > 0:
                        _ag_adv.log_application(account_key, symbol, position_side, "process", "force_close", _ag_obj)
                        logger.info(f"🤖 {_ag_tag}_AGENT_FORCE_CLOSE {position_key}: {_ag_reason}")
                        await queue_trade_action(order_queue, trade_manager, position_key, "CLOSE", f"{_ag_tag}_AGENT_FORCE_CLOSE({_ag_reason})", 100.0, override_qty=999999)
                        return f"AGENT_FORCE_CLOSE:{_ag_tag}"
                    if _ag_action == "hold" and _ag_pos_amt > 0:
                        _ag_adv.log_application(account_key, symbol, position_side, "process", "hold", _ag_obj)
                        logger.info(f"🤖 {_ag_tag}_AGENT_HOLD {position_key}: {_ag_reason}")
                        return f"AGENT_HOLD:{_ag_tag}"
                    if _ag_action == "force_open" and _ag_pos_amt == 0:
                        _ag_size_usd = float(_ag_obj.get("size_override_usd") or 0)
                        _ag_price_t, _ = await trade_manager.get_current_price(symbol)
                        _ag_price_t = float(_ag_price_t) if _ag_price_t else 0.0
                        if _ag_size_usd > 0 and _ag_price_t > 0:
                            _ag_qty = max(_ag_size_usd / _ag_price_t, 0.0)
                            _ag_adv.log_application(account_key, symbol, position_side, "process", "force_open", _ag_obj)
                            logger.info(f"🤖 {_ag_tag}_AGENT_FORCE_OPEN {position_key} size=${_ag_size_usd:.0f} qty={_ag_qty:.4f}: {_ag_reason}")
                            await queue_trade_action(order_queue, trade_manager, position_key, "OPEN", f"{_ag_tag}_AGENT_FORCE_OPEN({_ag_reason})", 100.0, override_qty=_ag_qty)
                            return f"AGENT_FORCE_OPEN:{_ag_tag}"
                    if _ag_action == "block_entry" and _ag_pos_amt == 0:
                        _ag_adv.log_application(account_key, symbol, position_side, "process", "block_entry", _ag_obj)
                        logger.info(f"🤖 {_ag_tag}_AGENT_BLOCK_ENTRY {position_key}: {_ag_reason}")
                        return f"AGENT_BLOCK_ENTRY:{_ag_tag}"
            except Exception as _ag_err:
                logger.warning(f"[AGENT] tradier advisory check failed for {position_key}: {_ag_err}")
        
        # --- CHECK OPPOSING POSITION ---
        opposing_side = "SHORT" if position_side == "LONG" else "LONG"
        from utils import construct_position_key
        opposing_key = construct_position_key(account_key, symbol, opposing_side)
        opposing_pos = trade_manager.position_manager.get_position(opposing_key)
        has_opposing_pos = opposing_pos and abs(float(getattr(opposing_pos, 'positionAmt', 0))) > 0
        current_price, ts = await trade_manager.get_current_price(symbol)
        current_price = safe_fetch_float(current_price, 0.0)
        macro_fresh, freshness_reason, m1_fresh, indicators_raw = await trade_manager.is_data_fresh(symbol, position_key)
        # Warm STDEV BB buffers AND update breakout state continuously (not just in entry branch)
        if getattr(config, "STDEV_BREAKOUT_ENABLED", False) and indicators_raw:
            for _sb_tf_w in getattr(config, "STDEV_BREAKOUT_HTF_LIST", ["D", "4h"]) + getattr(config, "STDEV_BREAKOUT_RETEST_TF_LIST", ["1h", "15m"]):
                _get_bb_pctb_t(symbol, _sb_tf_w, indicators_raw)
            # Update breakout state continuously so it's ready when entry branch fires
            _update_stdev_state(symbol, indicators_raw)
        if not macro_fresh:
            # INDICATORS ARE DEAD (>1200s) — but NEVER close positions on stale data
            # Only block NEW entries. Existing positions hold until fresh data returns.
            if has_position:
                logger.info(f"[STALE_HOLD] {symbol}: indicators stale but HOLDING position (stale data must NEVER trigger exits)")
                return "STALE_INDICATORS_HELD"
            else:
                return "STALE_ABSOLUTE_NO_POS"
        if not force and (now_ts - last_mon < 30):
            return "THROTTLED"
        is_stale = not macro_fresh
        m1_stale = not m1_fresh
        
        if not indicators_raw:
            if force: logger.info(f"[{account_key}] SKIP {symbol}: No indicators found.")
            return "NO_DATA"

        i = trade_manager.strategy.parse_market_data(indicators_raw)
        if symbol.upper() in trade_manager.blacklist and not has_position: return "BLOCKED_BLACKLIST"
        if (i.get('stoch_k_1m') == 50.0 and i.get('stoch_k_5m') == 50.0):
             if force: logger.info(f"[{account_key}] SKIP {symbol}: Flatline Data (50/50).")
             return "DATA_ERROR"

        # 3. DETERMINE PRICE
        current_price,ts = await trade_manager.get_current_price(symbol)
        position = trade_manager.position_manager.get_position(position_key)
        if current_price is None: current_price=position.mark_price if position else None
        if current_price is None: current_price=indicators_raw.get('current_price')

        current_price = safe_fetch_float(current_price, 0.0)

        if current_price <= 0 and position:
            current_price = safe_fetch_float(getattr(position, 'mark_price', 0), 0.0)
        
        if current_price <= 0:
            if force: logger.info(f"[{account_key}] SKIP {symbol}: Price is Zero.")
            return "NO_PRICE"
        is_long = (position_side == "LONG")
        # ═══════════════════════════════════════════════════════════════════════
        # TR_TREND_V1 SHADOW-LOG (2026-05-17). spec: vec_paths/tr_trend_v1.py +
        # data/research_20260516/strategy_plan.md §4 + tr_trend_v1_build_report.md.
        # Default OFF (config_tradier.TR_TREND_V1_ENABLED=False) → zero impact.
        # When ENABLED=True: emits [TR_TREND_V1_SHADOW] entry/exit candidates for
        # top-8 shadow syms (TRGP/SNDK/AVGO/GLD/PLTR/MU/CDE/SLV, LONG only).
        # When TR_TREND_V1_SHADOW_LOG_ONLY=True (default) NO real orders fire.
        # Wrapped in try so any adapter failure does NOT break R1/R2/existing flow.
        # ═══════════════════════════════════════════════════════════════════════
        if bool(getattr(config, 'TR_TREND_V1_ENABLED', False)):
            try:
                _trv1_has_pos = bool(position and abs(safe_float(getattr(position, 'positionAmt', 0))) > 0)
                _trv1_entry = evaluate_tr_trend_v1_entry_live(symbol, account_key, position_side, indicators_raw, current_price, _trv1_has_pos, config)
                if _trv1_entry is not None:
                    logger.info(f"[TR_TREND_V1_SHADOW] ENTRY_CANDIDATE acct={account_key} sym={symbol} side={position_side} path={_trv1_entry.get('path')} pivot={_trv1_entry.get('pivot_price', 0):.4f} stop={_trv1_entry.get('stop_price', 0):.4f} mark={current_price:.4f} reason={_trv1_entry.get('reason')}")
                    if not bool(getattr(config, 'TR_TREND_V1_SHADOW_LOG_ONLY', True)):
                        # FUTURE: route to queue_trade_action for real entry. SHADOW = log only.
                        pass
                _trv1_exit = evaluate_tr_trend_v1_exit_live(symbol, account_key, position_side, indicators_raw, current_price, _trv1_has_pos, config)
                if _trv1_exit is not None:
                    _trv1_gain = safe_fetch_float(getattr(position, 'gain', 0), 0) if position else 0.0
                    logger.info(f"[TR_TREND_V1_SHADOW] EXIT_CANDIDATE acct={account_key} sym={symbol} side={position_side} mark={current_price:.4f} gain={_trv1_gain:.2f}% reason={_trv1_exit.get('reason')}")
                    if not bool(getattr(config, 'TR_TREND_V1_SHADOW_LOG_ONLY', True)):
                        pass
            except Exception as _trv1_err:
                logger.warning(f"[TR_TREND_V1_SHADOW] eval_error acct={account_key} sym={symbol} side={position_side}: {_trv1_err}")
        # ═══════════════════════════════════════════════════════════════════════
        # R1 — DC_LOW4 EMERGENCY CLOSE (USER 2026-05-09, stocks mirror).
        # Fires within R1_NEWBORN_WINDOW_MIN of open if price breaks the configured
        # 5m DC channel level. Bypasses NO_LOSS / hedge / MTF. Desktop alert + JSONL.
        # ═══════════════════════════════════════════════════════════════════════
        if position and abs(safe_float(getattr(position, 'positionAmt', 0))) > 0 and \
           bool(_cfg('R1_DC_LOW4_3M_EMERGENCY_ENABLED', True, account_key, symbol, position_side)):
            try:
                # Fixed stop price set at open/augment time — no time window.
                _r1_stop = float(getattr(position, 'r1_stop_price', 0.0) or 0.0)
                # Fallback: if stop was never stored (position opened before R1 wiring or via other path),
                # read LIVE dc channel level. This ensures R1 fires for every open position.
                if _r1_stop <= 0:
                    _r1_live_key = 'dc_low4_5m' if is_long else 'dc_high4_5m'
                    _r1_stop_live = safe_fetch_float(i.get(_r1_live_key), 0.0)
                    if _r1_stop_live > 0:
                        _r1_stop = _r1_stop_live
                        position.r1_stop_price = _r1_stop_live  # persist so next bar uses same level
                        logger.info(f"[R1_STOP_LAZY_SET] {position_key}: r1_stop_price not set — initialised from live {_r1_live_key}={_r1_stop_live:.4f}")
                _r1_age_min = 0.0  # kept for log only
                _r1_opened = getattr(position, 'opened_at', None)
                if isinstance(_r1_opened, datetime):
                    _r1_age_min = (datetime.now(timezone.utc) - _r1_opened).total_seconds() / 60.0
                if _r1_stop > 0:
                    _r1_breached = (
                        (is_long and current_price <= _r1_stop) or
                        ((not is_long) and current_price >= _r1_stop)
                    )
                    if _r1_breached:
                        _r1_gain = safe_fetch_float(getattr(position, 'gain', 0), 0)
                        _r1_entry_sig = getattr(position, 'last_signal', '') or getattr(position, 'open_reason', '') or '?'
                        _r1_entry_ts = str(getattr(position, 'opened_at', ''))[:19]
                        logger.error(f"⛔ [R1_DC_LOW4_EMERGENCY] {position_key}: g={_r1_gain:.2f}% age={_r1_age_min:.1f}m price={current_price:.4f} {'<=' if is_long else '>='} r1_stop={_r1_stop:.4f} entry={str(_r1_entry_sig)[:40]} → CLOSE")
                        # 2026-05-10 USER MANDATE: action-first. Try CLOSE; alert ONLY on
                        # unexplained failure. Min-notional / qty=0 / blacklist suppress.
                        _r1_failed = False
                        _r1_err_str = None
                        try:
                            _r1_qa_result = await queue_trade_action(order_queue, trade_manager, position_key, "CLOSE", f"R1_DC_LOW4_EMERGENCY_g{_r1_gain:.2f}_age{_r1_age_min:.1f}m_entry_{str(_r1_entry_sig)[:30]}", 100.0, override_qty=999999)
                            if isinstance(_r1_qa_result, str) and ('BLOCK' in _r1_qa_result.upper() or 'SKIP' in _r1_qa_result.upper() or 'REJECT' in _r1_qa_result.upper()):
                                _r1_failed = True
                                _r1_err_str = _r1_qa_result
                        except Exception as _r1_qa_err:
                            _r1_failed = True
                            _r1_err_str = repr(_r1_qa_err)
                        if not _r1_failed:
                            return f"R1_DC_LOW4_EMERGENCY_CLOSED"
                        try:
                            import ez_alert
                            _r1_should = ez_alert.should_alert_for_failure(_r1_err_str)
                        except Exception:
                            _r1_should = True
                        if _r1_should:
                            logger.critical(f"☢️ [R1_CLOSE_AGENT_BUG] {position_key}: close failed with no valid reason: {_r1_err_str}")
                            try:
                                import ez_alert
                                ez_alert.alert_bad_exit(account=account_key, position_key=position_key, gain=_r1_gain, reason=f"R1_CLOSE_DID_NOT_EXECUTE_NO_VALID_REASON_err={str(_r1_err_str)[:80]}", entry_signal=str(_r1_entry_sig), entry_ts=_r1_entry_ts, current_price=current_price)
                            except Exception:
                                pass
                        else:
                            logger.warning(f"[R1_CLOSE_VALID_SKIP] {position_key}: close suppressed for valid reason: {_r1_err_str}")
                        return f"R1_DC_LOW4_EMERGENCY_FAILED:{str(_r1_err_str)[:40]}"
            except Exception as _r1_err:
                logger.debug(f"[R1_DC_LOW4] {position_key} err: {_r1_err}")
        # ═══════════════════════════════════════════════════════════════════════
        # R1c — FROZEN_ACT_STOP (USER 2026-05-18 stocks team finding).
        # Frozen dc_low_4h@entry (LONG) / dc_high_4h@entry (SHORT) + -8% absolute floor.
        # Worst stocks loss capped at -9% (vs -12.6% baseline). 4 stops/sym/yr.
        # ═══════════════════════════════════════════════════════════════════════
        if position and abs(safe_float(getattr(position, 'positionAmt', 0))) > 0 and \
           bool(getattr(config, 'FROZEN_ACTIVATION_STOP_ENABLED', True)):
            try:
                _fa_tf = str(getattr(config, 'FROZEN_ACTIVATION_TF', '4h'))
                _fa_floor_pct = float(getattr(config, 'FROZEN_ABSOLUTE_FLOOR_PCT_TRADIER', -8.0))
                _fa_is_long = position_side == 'LONG'
                _fa_gain = safe_fetch_float(getattr(position, 'gain', 0), 0)
                _fa_frozen = getattr(position, '_frozen_dc_act', None)
                if _fa_frozen is None and i:
                    _fa_field = f'dc_low_{_fa_tf}' if _fa_is_long else f'dc_high_{_fa_tf}'
                    _fa_raw = i.get(_fa_field)
                    if _fa_raw not in (None, 0, 0.0):
                        try:
                            _fa_frozen = float(_fa_raw)
                            setattr(position, '_frozen_dc_act', _fa_frozen)
                        except (TypeError, ValueError):
                            _fa_frozen = None
                _fa_floor_hit = _fa_gain <= _fa_floor_pct
                _fa_breach = False
                if _fa_frozen is not None:
                    _fa_breach = ((_fa_is_long and current_price < _fa_frozen)
                                  or (not _fa_is_long and current_price > _fa_frozen)) and _fa_gain < 0
                if _fa_floor_hit or _fa_breach:
                    _fa_tag = 'FROZEN_ACT_STOP_ABSOLUTE_FLOOR' if _fa_floor_hit else 'FROZEN_ACT_STOP_FROZEN_BREACH'
                    _fa_reason = (f'{_fa_tag}_g{_fa_gain:.2f}%_floor{_fa_floor_pct:.1f}%' if _fa_floor_hit
                                  else f'{_fa_tag}_g{_fa_gain:.2f}%_frozen{_fa_tf}={_fa_frozen}_cur={current_price}')
                    logger.error(f"⛔ [{_fa_tag}] {position_key}: g={_fa_gain:.2f}% frozen_{_fa_tf}={_fa_frozen} cur={current_price:.4f} floor={_fa_floor_pct:.1f}% → CLOSE")
                    await queue_trade_action(order_queue, trade_manager, position_key, "CLOSE", _fa_reason, 100.0, override_qty=999999)
                    return f"{_fa_tag}_CLOSED"
            except Exception as _fa_err:
                logger.debug(f"[FROZEN_ACT_STOP] {position_key} err: {_fa_err}")
        # ═══════════════════════════════════════════════════════════════════════
        # R2 — WT_VEL_SLOW near-breakeven exit (USER 2026-05-09, stocks mirror).
        # Stocks use HTFs (default 1h/4h/D) per user — markets closed most of day,
        # real moves happen on D/W. floor <= gain < band, |vel| < |vel_prev|*ratio.
        # ═══════════════════════════════════════════════════════════════════════
        if position and abs(safe_float(getattr(position, 'positionAmt', 0))) > 0 and \
           bool(getattr(config, 'WT_15M_VEL_SLOW_AT_ZERO_GAIN_ENABLED', True)):
            try:
                _wzg_gain = safe_fetch_float(getattr(position, 'gain', 0), 0)
                _wzg_max_gain = safe_fetch_float(getattr(position, 'max_gain', 0), 0)
                _wzg_peak_min = float(getattr(config, 'R2_PEAK_MIN_PCT', 0.5))
                # 2026-05-18 per-sym overlay
                _wzg_band = float(_cfg('WT_15M_VEL_SLOW_GAIN_BAND_PCT', 0.10, account_key, symbol, position_side))
                _wzg_floor = float(_cfg('WT_15M_VEL_SLOW_GAIN_FLOOR_PCT', 0.01, account_key, symbol, position_side))
                _wzg_near_zero = float(_cfg('WT_15M_VEL_NEAR_ZERO_THRESHOLD', 0.1, account_key, symbol, position_side))
                _wzg_decel_ratio = float(_cfg('WT_VEL_DECEL_RATIO', 0.5, account_key, symbol, position_side))
                _wzg_decel_only = bool(_cfg('WT_VEL_USE_DECEL_RATIO_ONLY', True, account_key, symbol, position_side))
                _wzg_tfs = tuple(_cfg('R2_TF_LIST', ('1h', '4h', 'D'), account_key, symbol, position_side) or ('1h', '4h', 'D'))
                # PEAK-THEN-COLLAPSE only — see ez_manage.py:20783 comment.
                if _wzg_max_gain >= _wzg_peak_min and _wzg_floor <= _wzg_gain < _wzg_band:
                    _wzg_fired_tf = None
                    _wzg_vel = 0.0
                    _wzg_vel_prev = 0.0
                    _wzg_tag = ''
                    for _wzg_tf in _wzg_tfs:
                        _v = safe_fetch_float(i.get(f'wt_velocity_{_wzg_tf}'), 0)
                        _vp = safe_fetch_float(i.get(f'wt_velocity_{_wzg_tf}_prev', _v), 0)
                        _against = (is_long and _v < 0) or ((not is_long) and _v > 0)
                        _decel = abs(_v) < abs(_vp) * _wzg_decel_ratio and abs(_vp) > 1e-6
                        _dying = (not _wzg_decel_only) and abs(_v) <= _wzg_near_zero
                        if _against and (_decel or _dying):
                            _wzg_fired_tf, _wzg_vel, _wzg_vel_prev = _wzg_tf, _v, _vp
                            _wzg_tag = 'DECEL' if _decel else 'DYING'
                            break
                    if _wzg_fired_tf:
                        logger.error(f"⛔ [R2_WT_VEL_SLOW] {position_key}: peak={_wzg_max_gain:.2f}% (≥{_wzg_peak_min}%) collapsed→g={_wzg_gain:.3f}% in [{_wzg_floor},{_wzg_band}], wt_vel_{_wzg_fired_tf}={_wzg_vel:.3f} (prev={_wzg_vel_prev:.3f}) AGAINST+{_wzg_tag} ratio={_wzg_decel_ratio} → CLOSE")
                        await queue_trade_action(order_queue, trade_manager, position_key, "CLOSE", f'R2_WT_VEL_SLOW_{_wzg_tag}_{_wzg_fired_tf}_g{_wzg_gain:.3f}%_peak{_wzg_max_gain:.2f}%_vel{_wzg_vel:.3f}vs{_wzg_vel_prev:.3f}', 100.0, override_qty=999999)
                        return f"R2_WT_VEL_SLOW_CLOSED:{_wzg_fired_tf}_{_wzg_tag}"
            except Exception as _wzg_err:
                logger.debug(f"[R2_WT_VEL_SLOW] {position_key} err: {_wzg_err}")
        # ═══════════════════════════════════════════════════════════════════════
        # R3_HTF_FLIP — Daily-close + parallel 4h structural exit (USER 2026-05-17).
        # Source: data/research_20260516/PLAN.md §3.6. Mirror of crypto block in ez_manage.
        #   LONG closes when:
        #     Daily tier: (current_price < dc_basis_D) OR (wt1_D < wt2_D AND wt1_W < wt2_W)
        #     4h tier:    (current_price < ema_20_4h - atr_4h) AND wt1_4h < wt2_4h
        #   SHORT mirror. Reasons R3_HTF_FLIP / R3_HTF_FLIP_4H are in LOSS_EXIT_TECHNICAL_BYPASS.
        # ROLLBACK: R3_HTF_FLIP_EXIT_ENABLED=False (and/or _4H_TIER_ENABLED=False) in config_tradier.py.
        # ═══════════════════════════════════════════════════════════════════════
        # 2026-05-18 per-sym overlay
        if (position and abs(safe_float(getattr(position, 'positionAmt', 0))) > 0 and bool(_cfg('R3_HTF_FLIP_EXIT_ENABLED', False, account_key, symbol, position_side))):
            try:
                _r3hf_w1_D = safe_fetch_float(i.get('wt1_D'), 0)
                _r3hf_w2_D = safe_fetch_float(i.get('wt2_D'), 0)
                _r3hf_w1_W = safe_fetch_float(i.get('wt1_W'), 0)
                _r3hf_w2_W = safe_fetch_float(i.get('wt2_W'), 0)
                _r3hf_dc_basis_D = safe_fetch_float(i.get('dc_basis_D'), 0)
                _r3hf_atr_4h = safe_fetch_float(i.get('atr_4h'), 0)
                _r3hf_ema_20_4h = safe_fetch_float(i.get('ema_20_4h'), 0)
                _r3hf_w1_4h = safe_fetch_float(i.get('wt1_4h'), 0)
                _r3hf_w2_4h = safe_fetch_float(i.get('wt2_4h'), 0)
                _r3hf_fire = False
                _r3hf_tier = None
                _r3hf_detail = ""
                if _r3hf_dc_basis_D > 0:
                    _r3hf_dc_break = (is_long and current_price < _r3hf_dc_basis_D) or ((not is_long) and current_price > _r3hf_dc_basis_D)
                    _r3hf_wt_flip = (is_long and _r3hf_w1_D < _r3hf_w2_D and _r3hf_w1_W < _r3hf_w2_W) or ((not is_long) and _r3hf_w1_D > _r3hf_w2_D and _r3hf_w1_W > _r3hf_w2_W)
                    if _r3hf_dc_break or _r3hf_wt_flip:
                        _r3hf_fire = True
                        _r3hf_tier = "DAILY"
                        _r3hf_detail = f"dc_break={_r3hf_dc_break}_wt_flip={_r3hf_wt_flip}_px={current_price:.4f}_dcBD={_r3hf_dc_basis_D:.4f}_w1D={_r3hf_w1_D:.2f}_w2D={_r3hf_w2_D:.2f}_w1W={_r3hf_w1_W:.2f}_w2W={_r3hf_w2_W:.2f}"
                if (not _r3hf_fire) and bool(_cfg('R3_HTF_FLIP_4H_TIER_ENABLED', False, account_key, symbol, position_side)):
                    if _r3hf_ema_20_4h > 0 and _r3hf_atr_4h > 0:
                        _r3hf_4h_long = is_long and current_price < (_r3hf_ema_20_4h - _r3hf_atr_4h) and _r3hf_w1_4h < _r3hf_w2_4h
                        _r3hf_4h_short = (not is_long) and current_price > (_r3hf_ema_20_4h + _r3hf_atr_4h) and _r3hf_w1_4h > _r3hf_w2_4h
                        if _r3hf_4h_long or _r3hf_4h_short:
                            _r3hf_fire = True
                            _r3hf_tier = "4H"
                            _r3hf_detail = f"px={current_price:.4f}_ema4h={_r3hf_ema_20_4h:.4f}_atr4h={_r3hf_atr_4h:.4f}_w1_4h={_r3hf_w1_4h:.2f}_w2_4h={_r3hf_w2_4h:.2f}"
                if _r3hf_fire:
                    _r3hf_gain = safe_fetch_float(getattr(position, 'gain', 0), 0)
                    _r3hf_reason_tag = "R3_HTF_FLIP" if _r3hf_tier == "DAILY" else "R3_HTF_FLIP_4H"
                    logger.error(f"⛔ [{_r3hf_reason_tag}] {position_key}: tier={_r3hf_tier} gain={_r3hf_gain:.2f}% {_r3hf_detail} → CLOSE")
                    await queue_trade_action(order_queue, trade_manager, position_key, "CLOSE", f"{_r3hf_reason_tag}_{_r3hf_tier}_g{_r3hf_gain:.2f}%_{_r3hf_detail[:80]}", 100.0, override_qty=999999)
                    return f"{_r3hf_reason_tag}_CLOSED:{_r3hf_tier}"
            except Exception as _r3hf_err:
                logger.debug(f"[R3_HTF_FLIP] {position_key} err: {_r3hf_err}")
        # ═══════════════════════════════════════════════════════════════════════
        # MTF COMPOUND EXIT — Path A Phase 1 wiring 2026-05-19 (USER MANDATE) — stocks mirror.
        # Source: vec_paths/mtf_armed_entries.py:127-235. Replacement protection stack for
        # HEDGE_MODE + DC_LOW_4 emergency. 5 triggers, ANY fires CLOSE:
        #   1. MTF_ATR_TRAIL: ATR trail (ratchets from entry, default TF=1h for stocks per
        #      MTF_ATR_TRAIL_TF_TRADIER) × MTF_ATR_TRAIL_MULT
        #   2. MTF_DC_REJECT: price was outside dc_high/low_TF and re-crossed back
        #   3. MTF_BB_REJECT: BB tag-fail mask within MTF_BB_REJECT_EXIT_LOOKBACK bars
        #   4. MTF_GR_WT_EXIT: GR HTF exit gate (opposite-side TFs) AND wt cross against (BOTH)
        # Master switch MTF_EXIT_USE_COMPOUND defaults False → entire block inert.
        # New reasons MTF_ATR_TRAIL/MTF_DC_REJECT/MTF_BB_REJECT/MTF_GR_WT_EXIT are in
        # config_tradier.LOSS_EXIT_TECHNICAL_BYPASS so loss-side close fires past NOLOSS_DC4H.
        # Per-position trail state lives in trade_manager.mtf_compound_exit_state[position_key].
        # Tradier base TF = 5m → default TFs = 1h (~12x base) for ATR/DC/BB/WT.
        # ROLLBACK: set MTF_EXIT_USE_COMPOUND=False (already default).
        # ═══════════════════════════════════════════════════════════════════════
        if (position and abs(safe_float(getattr(position, 'positionAmt', 0))) > 0 and bool(_cfg('MTF_EXIT_USE_COMPOUND', False, account_key, symbol, position_side))):
            try:
                if not hasattr(trade_manager, 'mtf_compound_exit_state'):
                    trade_manager.mtf_compound_exit_state = {}
                _mtfce_state = trade_manager.mtf_compound_exit_state.get(position_key, {'trail': 0.0, 'ever_outside_dc': False, 'bb_tag_bars': []})
                _mtfce_entry = safe_fetch_float(getattr(position, 'entry_price', 0), 0)
                _mtfce_fire = False
                _mtfce_reason = ""
                _mtfce_atr_tf = str(_cfg('MTF_ATR_TRAIL_TF_TRADIER', _cfg('MTF_ATR_TRAIL_TF', '1h', account_key, symbol, position_side), account_key, symbol, position_side))
                _mtfce_dc_tf = str(_cfg('MTF_DC_REJECT_EXIT_TF', '1h', account_key, symbol, position_side))
                _mtfce_bb_tf = str(_cfg('MTF_BB_REJECT_EXIT_TF', '1h', account_key, symbol, position_side))
                _mtfce_wt_tf = str(_cfg('MTF_WT_CROSS_EXIT_TF', '1h', account_key, symbol, position_side))
                _mtfce_atr_mult = float(_cfg('MTF_ATR_TRAIL_MULT', 2.5, account_key, symbol, position_side))
                _mtfce_bb_lb = int(_cfg('MTF_BB_REJECT_EXIT_LOOKBACK', 5, account_key, symbol, position_side))
                # ─── 1. MTF_ATR_TRAIL ─────────────────────────────────────────────
                if bool(_cfg('MTF_ATR_TRAIL_ENABLED', False, account_key, symbol, position_side)):
                    _mtfce_atr = safe_fetch_float(i.get(f'atr_{_mtfce_atr_tf}'), 0)
                    if _mtfce_atr > 0 and _mtfce_entry > 0 and current_price > 0:
                        if is_long:
                            _mtfce_cand = max(_mtfce_entry - _mtfce_atr_mult * _mtfce_atr, current_price - _mtfce_atr_mult * _mtfce_atr)
                            _mtfce_state['trail'] = max(_mtfce_state.get('trail', 0.0), _mtfce_cand) if _mtfce_state.get('trail', 0.0) > 0 else _mtfce_cand
                            if _mtfce_state['trail'] > 0 and current_price < _mtfce_state['trail']:
                                _mtfce_fire = True
                                _mtfce_reason = f"MTF_ATR_TRAIL_{_mtfce_atr_tf}_x{_mtfce_atr_mult}_lvl{_mtfce_state['trail']:.4f}"
                        else:
                            _mtfce_cand = min(_mtfce_entry + _mtfce_atr_mult * _mtfce_atr, current_price + _mtfce_atr_mult * _mtfce_atr)
                            _mtfce_state['trail'] = min(_mtfce_state.get('trail', 0.0), _mtfce_cand) if _mtfce_state.get('trail', 0.0) > 0 else _mtfce_cand
                            if _mtfce_state['trail'] > 0 and current_price > _mtfce_state['trail']:
                                _mtfce_fire = True
                                _mtfce_reason = f"MTF_ATR_TRAIL_{_mtfce_atr_tf}_x{_mtfce_atr_mult}_lvl{_mtfce_state['trail']:.4f}"
                # ─── 2. MTF_DC_REJECT ─────────────────────────────────────────────
                if (not _mtfce_fire) and bool(_cfg('MTF_DC_REJECT_EXIT_ENABLED', False, account_key, symbol, position_side)):
                    if is_long:
                        _mtfce_band = safe_fetch_float(i.get(f'dc_high_{_mtfce_dc_tf}'), 0)
                        if _mtfce_band > 0:
                            if current_price > _mtfce_band:
                                _mtfce_state['ever_outside_dc'] = True
                            elif _mtfce_state.get('ever_outside_dc', False) and current_price < _mtfce_band:
                                _mtfce_fire = True
                                _mtfce_reason = f"MTF_DC_REJECT_{_mtfce_dc_tf}_px{current_price:.4f}"
                    else:
                        _mtfce_band = safe_fetch_float(i.get(f'dc_low_{_mtfce_dc_tf}'), 0)
                        if _mtfce_band > 0:
                            if current_price < _mtfce_band:
                                _mtfce_state['ever_outside_dc'] = True
                            elif _mtfce_state.get('ever_outside_dc', False) and current_price > _mtfce_band:
                                _mtfce_fire = True
                                _mtfce_reason = f"MTF_DC_REJECT_{_mtfce_dc_tf}_px{current_price:.4f}"
                # ─── 3. MTF_BB_REJECT ─────────────────────────────────────────────
                if (not _mtfce_fire) and bool(_cfg('MTF_BB_REJECT_EXIT_ENABLED', False, account_key, symbol, position_side)):
                    _mtfce_bbu = safe_fetch_float(i.get(f'bb_upper_{_mtfce_bb_tf}'), 0)
                    _mtfce_bbl = safe_fetch_float(i.get(f'bb_lower_{_mtfce_bb_tf}'), 0)
                    _mtfce_high = safe_fetch_float(i.get(f'high_{_mtfce_bb_tf}'), current_price)
                    _mtfce_low = safe_fetch_float(i.get(f'low_{_mtfce_bb_tf}'), current_price)
                    if is_long and _mtfce_bbu > 0:
                        _mtfce_tag_now = _mtfce_high >= _mtfce_bbu - 1e-9
                        _mtfce_fail_now = (_mtfce_high < _mtfce_bbu) and _mtfce_high > 0
                    elif (not is_long) and _mtfce_bbl > 0:
                        _mtfce_tag_now = _mtfce_low <= _mtfce_bbl + 1e-9 and _mtfce_low > 0
                        _mtfce_fail_now = (_mtfce_low > _mtfce_bbl) and _mtfce_low > 0
                    else:
                        _mtfce_tag_now = False
                        _mtfce_fail_now = False
                    _mtfce_tag_bars = list(_mtfce_state.get('bb_tag_bars', []))
                    _mtfce_now_s = int(time.time())
                    if _mtfce_tag_now:
                        _mtfce_tag_bars.append(_mtfce_now_s)
                    # Stock 5m base TF → ~300s × lookback bars window
                    _mtfce_cutoff_s = _mtfce_now_s - _mtfce_bb_lb * 300
                    _mtfce_tag_bars = [b for b in _mtfce_tag_bars if b >= _mtfce_cutoff_s]
                    _mtfce_state['bb_tag_bars'] = _mtfce_tag_bars
                    if _mtfce_fail_now and len(_mtfce_tag_bars) > 0:
                        _mtfce_fire = True
                        _mtfce_reason = f"MTF_BB_REJECT_{_mtfce_bb_tf}"
                # ─── 4. MTF_GR_WT_EXIT (GR HTF + WT cross — BOTH required) ───────
                if (not _mtfce_fire) and bool(_cfg('MTF_GR_EXIT_GATE_ENABLED', False, account_key, symbol, position_side)):
                    _mtfce_wt1 = safe_fetch_float(i.get(f'wt1_{_mtfce_wt_tf}'), 0)
                    _mtfce_wt2 = safe_fetch_float(i.get(f'wt2_{_mtfce_wt_tf}'), 0)
                    _mtfce_wt_against = (is_long and _mtfce_wt1 < _mtfce_wt2) or ((not is_long) and _mtfce_wt1 > _mtfce_wt2)
                    if _mtfce_wt_against and bool(_cfg('MTF_WT_CROSS_EXIT_ENABLED', False, account_key, symbol, position_side)):
                        _mtfce_gr_min_tfs = int(_cfg('MTF_GR_EXIT_MIN_TFS', 3, account_key, symbol, position_side))
                        _mtfce_gr_count = 0
                        for _gtf in ('1h', '4h', 'D', 'W'):
                            _gw1 = safe_fetch_float(i.get(f'wt1_{_gtf}'), 0)
                            _gw2 = safe_fetch_float(i.get(f'wt2_{_gtf}'), 0)
                            if is_long and _gw1 < _gw2 and _gw1 != 0:
                                _mtfce_gr_count += 1
                            elif (not is_long) and _gw1 > _gw2 and _gw1 != 0:
                                _mtfce_gr_count += 1
                        if _mtfce_gr_count >= _mtfce_gr_min_tfs:
                            _mtfce_fire = True
                            _mtfce_reason = f"MTF_GR_WT_EXIT_{_mtfce_wt_tf}_grTFs={_mtfce_gr_count}"
                # Persist state regardless of fire
                trade_manager.mtf_compound_exit_state[position_key] = _mtfce_state
                if _mtfce_fire:
                    _mtfce_gain = safe_fetch_float(getattr(position, 'gain', 0), 0)
                    logger.error(f"⛔ [{_mtfce_reason[:18]}] {position_key}: gain={_mtfce_gain:.2f}% px={current_price:.4f} entry={_mtfce_entry:.4f} → CLOSE")
                    await queue_trade_action(order_queue, trade_manager, position_key, "CLOSE", f"{_mtfce_reason}_g{_mtfce_gain:.2f}%", 100.0, override_qty=999999)
                    trade_manager.mtf_compound_exit_state.pop(position_key, None)
                    return f"{_mtfce_reason[:18]}_CLOSED"
            except Exception as _mtfce_err:
                logger.debug(f"[MTF_COMPOUND_EXIT] {position_key} err: {_mtfce_err}")
        # ═══ END MTF COMPOUND EXIT ═══
        # ═══════════════════════════════════════════════════════════════════════
        # WT_3M_FORCE_OPEN — USER NON-NEGOTIABLE 2026-05-10 (stocks).
        # Every symbol in symbols_trb_long/short must have a position whenever the
        # wt1_3m vs wt2_3m condition holds. Reopen after every close. Reentry /
        # cooldown / NOLOSS gates may NOT block this — execute_now bypass is wired
        # via the WT_3M_FORCE_OPEN reason string.
        # ═══════════════════════════════════════════════════════════════════════
        # 2026-05-18 per-sym overlay
        if (not has_position) and bool(_cfg('WT_3M_FORCE_OPEN_ENABLED', True, account_key, symbol, position_side)):
            try:
                if trade_manager.is_symbol_tradeable(symbol, account_key, position_side):
                    _wf_wt1_3m = safe_fetch_float(i.get('wt1_3m'), 0)
                    _wf_wt2_3m = safe_fetch_float(i.get('wt2_3m'), 0)
                    _wf_trigger = (is_long and _wf_wt1_3m > _wf_wt2_3m) or ((not is_long) and _wf_wt1_3m < _wf_wt2_3m)
                    if _wf_trigger and current_price > 0:
                        _wf_size_usd = float(getattr(config, 'WT_3M_FORCE_OPEN_SIZE_USD', 100.0)) or float(getattr(config, 'START_POSITION_SIZE', 100.0))
                        _wf_qty = max(_wf_size_usd / current_price, 1.0)
                        _wf_reason = f"WT_3M_FORCE_OPEN_{'LONG' if is_long else 'SHORT'}_wt1={_wf_wt1_3m:.1f}_wt2={_wf_wt2_3m:.1f}_px{current_price:.4f}"
                        logger.warning(f"[WT_3M_FORCE_OPEN] {position_key}: ZERO position + wt1_3m {'>' if is_long else '<'} wt2_3m ({_wf_wt1_3m:.1f}{'>' if is_long else '<'}{_wf_wt2_3m:.1f}) → OPEN qty={_wf_qty:.2f}")
                        _wf_stop = safe_fetch_float(i.get('dc_low4_5m' if is_long else 'dc_high4_5m'), 0.0)
                        if _wf_stop > 0:
                            _cur_r1_wf = float(getattr(position, 'r1_stop_price', 0.0) or 0.0)
                            if _cur_r1_wf <= 0 or (is_long and _wf_stop > _cur_r1_wf) or (not is_long and _wf_stop < _cur_r1_wf):
                                position.r1_stop_price = _wf_stop
                        await queue_trade_action(order_queue, trade_manager, position_key, "OPEN", _wf_reason, 80.0, override_qty=_wf_qty)
                        return f"WT_3M_FORCE_OPEN:{position_side}"
            except Exception as _wf_err:
                logger.debug(f"[WT_3M_FORCE_OPEN] {position_key}: {_wf_err}")
        # ═══════════════════════════════════════════════════════════════════════
        # RULE_A_RETEST — D/W Breakout-then-Retest entry FIRE (USER 2026-05-17, mirror of ez_manage).
        # Source: data/research_20260516/PLAN.md §3.3. Cited +22 abs WR pts (Trading-Rush), +0.47 Sharpe (QuantPedia D1H1).
        # SIMPLIFIED stateless form (full breakout_retest_armed state dict pending next session).
        # ROLLBACK: BREAKOUT_RETEST_ARMED_ENABLED=False in config_tradier.py.
        # ═══════════════════════════════════════════════════════════════════════
        # 2026-05-18 per-sym overlay
        if (not has_position) and bool(_cfg('BREAKOUT_RETEST_ARMED_ENABLED', False, account_key, symbol, position_side)):
            try:
                if trade_manager.is_symbol_tradeable(symbol, account_key, position_side):
                    _ra_w1_D = safe_fetch_float(i.get('wt1_D'), 0)
                    _ra_w2_D = safe_fetch_float(i.get('wt2_D'), 0)
                    _ra_w1_W = safe_fetch_float(i.get('wt1_W'), 0)
                    _ra_w2_W = safe_fetch_float(i.get('wt2_W'), 0)
                    _ra_dc_basis_D = safe_fetch_float(i.get('dc_basis_D'), 0)
                    _ra_atr_D = safe_fetch_float(i.get('atr_D'), 0)
                    _ra_w1_15m = safe_fetch_float(i.get('wt1_15m'), 0)
                    _ra_w2_15m = safe_fetch_float(i.get('wt2_15m'), 0)
                    _ra_w1_1h = safe_fetch_float(i.get('wt1_1h'), 0)
                    _ra_w2_1h = safe_fetch_float(i.get('wt2_1h'), 0)
                    _ra_k_3m = safe_fetch_float(i.get('stoch_k_3m') or i.get('k_3m'), 50)
                    _ra_d_3m = safe_fetch_float(i.get('stoch_d_3m') or i.get('d_3m'), 50)
                    _ra_k_3m_prev = safe_fetch_float(i.get('k_3m_prev') or i.get('stoch_k_3m_prev'), _ra_k_3m)
                    _ra_data_ok = (abs(_ra_w1_D) > 1e-9 and abs(_ra_w2_D) > 1e-9 and _ra_dc_basis_D > 0 and _ra_atr_D > 0 and current_price > 0)
                    if _ra_data_ok:
                        # 2026-05-18 per-sym overlay
                        _ra_retest_mult = float(_cfg('BREAKOUT_RETEST_ARMED_RETEST_ATR_MULT', 0.30, account_key, symbol, position_side))
                        _ra_dist_atr = abs(current_price - _ra_dc_basis_D) / _ra_atr_D
                        _ra_armed_long = (_ra_w1_D > _ra_w2_D and _ra_w1_W > _ra_w2_W and current_price > _ra_dc_basis_D)
                        _ra_armed_short = (_ra_w1_D < _ra_w2_D and _ra_w1_W < _ra_w2_W and current_price < _ra_dc_basis_D)
                        _ra_retest_band = _ra_dist_atr < _ra_retest_mult
                        _ra_k3m_xup = (_ra_k_3m_prev < 30 and _ra_k_3m > _ra_d_3m and _ra_k_3m > _ra_k_3m_prev)
                        _ra_k3m_xdn = (_ra_k_3m_prev > 70 and _ra_k_3m < _ra_d_3m and _ra_k_3m < _ra_k_3m_prev)
                        _ra_15m_bull = (_ra_w1_15m > _ra_w2_15m)
                        _ra_15m_bear = (_ra_w1_15m < _ra_w2_15m)
                        _ra_1h_bull = (_ra_w1_1h > _ra_w2_1h)
                        _ra_1h_bear = (_ra_w1_1h < _ra_w2_1h)
                        _ra_fire_long = (is_long and _ra_armed_long and _ra_retest_band and _ra_k3m_xup and _ra_15m_bull and _ra_1h_bull)
                        _ra_fire_short = ((not is_long) and _ra_armed_short and _ra_retest_band and _ra_k3m_xdn and _ra_15m_bear and _ra_1h_bear)
                        if _ra_fire_long or _ra_fire_short:
                            _ra_size_usd = float(getattr(config, 'WT_3M_FORCE_OPEN_SIZE_USD', 100.0)) or float(getattr(config, 'START_POSITION_SIZE', 100.0))
                            _ra_qty = max(_ra_size_usd / current_price, 1.0)
                            _ra_reason = f"RULE_A_RETEST_{'LONG' if is_long else 'SHORT'}_px{current_price:.4f}_dcBD{_ra_dc_basis_D:.4f}_dist{_ra_dist_atr:.2f}atr_k3m{_ra_k_3m:.0f}/{_ra_k_3m_prev:.0f}_w15={_ra_w1_15m:.1f}/{_ra_w2_15m:.1f}_w1h={_ra_w1_1h:.1f}/{_ra_w2_1h:.1f}_wD={_ra_w1_D:.1f}/{_ra_w2_D:.1f}_wW={_ra_w1_W:.1f}/{_ra_w2_W:.1f}"
                            logger.warning(f"[RULE_A_RETEST] {position_key}: FIRE {('LONG' if is_long else 'SHORT')} qty={_ra_qty:.2f} {_ra_reason[:120]}")
                            await queue_trade_action(order_queue, trade_manager, position_key, "OPEN", _ra_reason, 80.0, override_qty=_ra_qty)
                            return f"RULE_A_RETEST:{position_side}"
            except Exception as _ra_err:
                logger.debug(f"[RULE_A_RETEST] {position_key}: probe err — {_ra_err}")
        was_reduced = getattr(position, 'was_reduced', False)
        last_red_time = getattr(position, 'last_reduction_time', None)
        market_context = await trade_manager.get_market_context(symbol)
        sentiment_rank = float(indicators_raw.get('0sentiment_rank') or i.get('0sentiment_rank') or 999)

        if not trade_manager.is_symbol_tradeable(symbol, account_key, position_side):
            return "SYMBOL NOT TRADEABLE"
            
        tech_score, _, strategy_reason = trade_manager.strategy.calculate_signal_score( account_key, symbol, is_long, current_price, i, position, is_exit=False  )
        # BB buffer warming moved to before stale check (line ~934)
        
        # 5. DECISION LOGIC VARIABLES
        log_rec = "WAIT"
        
        # Priority: Show the score/reason. Append STALE ONLY if it actually blocks something (is_stale).
        log_reason = f"Sc:{int(tech_score)} {strategy_reason}"[:30]
        if is_stale: log_reason = f"STALE {log_reason}"
        
        market_open = is_regular_trading_hours()
        action_taken = False

        # ═══ 2026-04-26 USER RULE — MICRO_SCALP_STOCKS_MAKER ═══
        # Mirror of crypto MICRO_SCALP_USDC_MAKER (ez_manage.py:19793). Stocks micro-scalper.
        # CLOSE: gain >= MICRO_SCALP_STOCKS_GAIN_THRESHOLD_PCT AND gain < prev_gain (first decel).
        # REOPEN: when flat AND price re-crosses prior exit_price.
        # 2026-04-27 user rule: NOW respects STOCK_MIN_HOLD on the CLOSE branch (was bypassing).
        # User: "respect the min hold of 72 hours". Reopen branch unchanged — needed for cross-back.
        # State stored on trade_manager._micro_scalp_state_stocks (separate dict from crypto's).
        _mss_min_hold = float(getattr(config, 'TRADIER_MIN_HOLD_MINUTES', getattr(config, 'MIN_HOLD_MINUTES_TRADIER', 240.0)))
        _mss_hold_min_check: float = 0.0
        try:
            _mss_opened_at = getattr(position, 'opened_at', None) if position else None
            if _mss_opened_at:
                _mss_hold_min_check = (datetime.now(timezone.utc) - _mss_opened_at).total_seconds() / 60.0
        except Exception:
            _mss_hold_min_check = 0.0
        if (market_open and bool(getattr(config, 'MICRO_SCALP_STOCKS_MAKER_ENABLED', False))
            and account_key in (getattr(config, 'MICRO_SCALP_STOCKS_ACCOUNTS', None) or [])):
            try:
                _mss_dict = getattr(trade_manager, '_micro_scalp_state_stocks', None)
                if _mss_dict is None:
                    trade_manager._micro_scalp_state_stocks = {}
                    _mss_dict = trade_manager._micro_scalp_state_stocks
                _mss_st = _mss_dict.get(position_key, {})
                _mss_threshold = float(getattr(config, 'MICRO_SCALP_STOCKS_GAIN_THRESHOLD_PCT', 0.05))
                _mss_qty = abs(float(getattr(position, 'positionAmt', 0))) if position else 0.0
                # CLOSE branch — held position, gain past threshold AND first deceleration
                if _mss_qty >= 1 and current_price > 0:
                    _mss_gain = float(getattr(position, 'gain', 0) or 0)
                    _mss_prev = float(_mss_st.get('prev_gain', _mss_gain))
                    if _mss_hold_min_check < _mss_min_hold:
                        _mss_st_new = dict(_mss_st)
                        _mss_st_new['prev_gain'] = _mss_gain
                        _mss_dict[position_key] = _mss_st_new
                        if _mss_gain >= _mss_threshold and _mss_gain < _mss_prev:
                            logger.info(f"[MICRO_SCALP_MIN_HOLD_BLOCK] {position_key}: hold={_mss_hold_min_check:.0f}m<{_mss_min_hold:.0f}m — micro-scalp close gated (user rule 2026-04-27)")
                    elif _mss_gain >= _mss_threshold and _mss_gain < _mss_prev:
                        _mss_reason = f"MICRO_SCALP_STOCKS_CLOSE_g{_mss_gain:.3f}%_prev{_mss_prev:.3f}%"
                        logger.critical(f"⚡ [MICRO_SCALP_STOCKS_CLOSE] {position_key}: gain={_mss_gain:.3f}% < prev={_mss_prev:.3f}% (threshold={_mss_threshold}%) — limit close (chase→market fallback at 10s)")
                        # Cooldown bookkeeping (same key as evaluate_stop CLOSE path)
                        if not hasattr(trade_manager, '_pending_closes'):
                            trade_manager._pending_closes = {}
                        await queue_trade_action(order_queue, trade_manager, position_key, "CLOSE", _mss_reason, 95.0, override_qty=999999)
                        trade_manager._pending_closes[symbol] = time.time()
                        # Stash state for reopen branch — record exit_price + side + original qty
                        _mss_dict[position_key] = {'prev_gain': 0.0, 'exit_price': current_price, 'reopen_pending': True, 'original_side': position_side, 'original_qty': _mss_qty}
                        action_taken = True
                    else:
                        # Track prev_gain even if we don't fire — for next-cycle decel detection
                        _mss_st_new = dict(_mss_st)
                        _mss_st_new['prev_gain'] = _mss_gain
                        _mss_dict[position_key] = _mss_st_new
                # REOPEN branch — flat position, prior close pending, price re-crossed exit
                elif _mss_qty < 1 and _mss_st.get('reopen_pending', False) and current_price > 0:
                    _mss_exit_px = float(_mss_st.get('exit_price', 0) or 0)
                    _mss_orig_side = str(_mss_st.get('original_side', '') or '')
                    _mss_orig_qty = float(_mss_st.get('original_qty', 0) or 0)
                    if _mss_exit_px > 0 and _mss_orig_qty >= 1 and _mss_orig_side in ('LONG', 'SHORT'):
                        _mss_orig_long = (_mss_orig_side == 'LONG')
                        _mss_crossed = (_mss_orig_long and current_price <= _mss_exit_px) or ((not _mss_orig_long) and current_price >= _mss_exit_px)
                        if _mss_crossed:
                            _mss_reason = f"MICRO_SCALP_STOCKS_REOPEN_exit{_mss_exit_px:.4f}_now{current_price:.4f}"
                            logger.critical(f"⚡ [MICRO_SCALP_STOCKS_REOPEN] {position_key}: price {current_price:.4f} re-crossed exit {_mss_exit_px:.4f} ({_mss_orig_side}) — limit reopen qty={_mss_orig_qty:.0f}")
                            await queue_trade_action(order_queue, trade_manager, position_key, "OPEN", _mss_reason, 95.0, override_qty=_mss_orig_qty)
                            _mss_dict[position_key] = {'prev_gain': 0.0, 'exit_price': 0.0, 'reopen_pending': False, 'original_side': '', 'original_qty': 0.0}
                            action_taken = True
            except Exception as _mss_e:
                logger.warning(f"[MICRO_SCALP_STOCKS_ERR] {position_key}: {type(_mss_e).__name__}: {_mss_e}")

        # =========================================================
        # LOGIC BRANCH A: HELD POSITION (EXIT / AUGMENT)
        # =========================================================
        if was_reduced and last_red_time:
            re_action, re_reason, re_conf, re_qty = await trade_manager.strategy.evaluate_reentry(symbol, position, indicators_raw)
            
            if re_action in ["OPEN", "REENTRY_OPEN", "AUGMENT"]:
                # if m1_stale:
                #     logger.warning(f"[{account_key}] 🛑 BLOCK REENTRY {symbol}: 1m Data Stale (>120s)")
                # elif is_stale:
                if is_stale:
                    logger.warning(f"[{account_key}] 🛑 BLOCK REENTRY {symbol}: All Data Stale ({freshness_reason})")
                else:# market_open or force:
                    log_rec = "♻️REENTRY"
                    log_reason = re_reason
                    logger.info(f"[{account_key}] ♻️ EXEC REENTRY {symbol}: {re_reason} Qty: {re_qty}")
                    
                    # REENTRY is REENTRY — not AUGMENT. execute_trade_action handles reclassification internally.
                    exec_action = "REENTRY"
                    _re_tagged_reason = f"REENTRY_{re_reason}" if 'REENTRY' not in re_reason.upper() else re_reason
                    _re_queued = await _dispatch_reentry_guaranteed_trd(
                        order_queue, trade_manager, position_key, exec_action,
                        _re_tagged_reason, re_conf, override_qty=re_qty
                    )
                    if _re_queued:
                        action_taken = True

        if has_position and not action_taken:
            opened_at = position.opened_at
            if opened_at:
                if isinstance(opened_at, str):
                    try:
                        import pandas as pd
                        opened_at = pd.to_datetime(opened_at).timestamp()
                    except Exception:
                        opened_at = time.time()
                elif hasattr(opened_at, 'timestamp'): 
                    opened_at = opened_at.timestamp()
                elif isinstance(opened_at, (int, float)):
                    pass
                else:
                    opened_at = time.time()
            now = time.time()
            position_age_minutes = (now - opened_at) / 60.0 if opened_at else 0
            in_grace_period = position_age_minutes < 10.0
            should_exit, exit_reason, exit_qty = await trade_manager.strategy.evaluate_stop(
                symbol, position, indicators_raw, market_context, in_grace_period )
            
            if should_exit:
                # IDEMPOTENCY GUARD (2026-04-26): if a CLOSE for this symbol was queued
                # recently, suppress the duplicate. Without this, the 30s loop re-fires
                # CLOSE every cycle while no fill confirms (off-hours, rejected order, or
                # broker lag) — flooded data/decisions/ JSONL with 6+ duplicate GLD/SLV
                # closes 2026-04-26 00:43–01:30 UTC. Cooldown is short intra-day (allow
                # fast retry on legit reject) and long off-hours (no fills possible).
                if not hasattr(trade_manager, '_pending_closes'):
                    trade_manager._pending_closes = {}
                _last_close_ts = trade_manager._pending_closes.get(symbol, 0.0)
                _close_cooldown = 30.0 if market_open else 7200.0
                _since_last = time.time() - _last_close_ts
                if _since_last < _close_cooldown:
                    log_rec = "HOLD"
                    log_reason = f"CLOSE_COOLDOWN_{int(_since_last)}s/{int(_close_cooldown)}s ({exit_reason[:40]})"
                else:
                    log_rec = "💥CLOSE"
                    log_reason = exit_reason
                    # FIX 2026-04-08: Use 999999 as override_qty for CLOSE — execute_trade_action
                    # will clamp to ACTUAL API position via min(quantity, current_qty).
                    # Never trust local positionAmt — it's stale from Redis sync lag.
                    exit_qty = 999999  # Will be clamped to real API qty in execute_trade_action
                    logger.info(f"[{account_key}] 🛑 CLOSING {symbol}: {exit_reason} (mkt_open={market_open})")
                    await queue_trade_action( order_queue, trade_manager, position_key, "CLOSE",  exit_reason, 100.0, override_qty=exit_qty )
                    action_taken = True
                    trade_manager._pending_closes[symbol] = time.time()
            
            # --- 3. EVALUATE AUGMENT ---
            # tra is long-term hold — no augmentation churn. Initial entry only.
            elif account_key == 'tra' and getattr(config, 'TRA_DISABLE_AUGMENT', True):
                log_rec = "HOLD"
                log_reason = f"TRA_HOLD g={getattr(position, 'gain', 0):.2f}%"
            elif position_age_minutes > 6.0:
                # DD BOUNCE STOP — cut extra leg if price continues below aug entry price
                _wds = getattr(trade_manager.strategy, '_wt_d_aug_state', {}).get(symbol, {})
                _dd_qty = float(_wds.get('dd_qty', 0.0))
                _dd_aug_price = float(_wds.get('last_aug_price', 0.0))
                _is_long_pos = getattr(position, 'position_side', 'LONG') == 'LONG'
                _dd_stop_hit = (_dd_qty >= 0.5 and _dd_aug_price > 0 and market_open and getattr(config, 'WT_D_BOUNCE_DD_STOP_ENABLED', True) and (current_price < _dd_aug_price if _is_long_pos else current_price > _dd_aug_price))
                if _dd_stop_hit:
                    _dd_reason = f"DD_BOUNCE_STOP px={current_price:.2f} aug_px={_dd_aug_price:.2f} qty={_dd_qty:.2f}"
                    logger.info(f"[{account_key}] ✂️ DD_STOP {symbol}: {_dd_reason}")
                    await queue_trade_action(order_queue, trade_manager, position_key, "REDUCE", _dd_reason, 80.0, override_qty=_dd_qty)
                    action_taken = True
                    trade_manager.strategy._wt_d_aug_state.setdefault(symbol, {})['dd_qty'] = 0.0
                # Add a cooldown check for consecutive augmentations (15 mins)
                aug_at = getattr(position, 'last_augmentation_time', None)
                aug_dt = safe_datetime(aug_at) if aug_at else None
                aug_age = (time.time() - aug_dt.timestamp()) / 60.0 if aug_dt else 999.0
                if not action_taken:
                    if aug_age < 15.0:
                        log_rec = "HOLD"
                        log_reason = f"AugCD:{15.0-aug_age:.1f}m"
                    elif m1_stale or is_stale:
                        log_rec = "HOLD"
                        log_reason = f"Stale ({freshness_reason})"
                    else:
                        should_aug, aug_reason, aug_conf, aug_qty = await trade_manager.strategy.evaluate_augment(
                            symbol, position, indicators_raw, market_context  )
                        if should_aug:
                            log_rec = "🚀AUGMENT"
                            log_reason = aug_reason
                            logger.info(f"[{account_key}] 🟢 REVIEWING AUGMENT {symbol}: {aug_reason} Qty: {aug_qty}")
                            if market_open:
                                _aug_stop = safe_fetch_float(i.get('dc_low4_5m' if is_long else 'dc_high4_5m'), 0.0)
                                if _aug_stop > 0:
                                    _cur_r1_aug = float(getattr(position, 'r1_stop_price', 0.0) or 0.0)
                                    if _cur_r1_aug <= 0 or (is_long and _aug_stop > _cur_r1_aug) or (not is_long and _aug_stop < _cur_r1_aug):
                                        position.r1_stop_price = _aug_stop
                                await queue_trade_action( order_queue, trade_manager, position_key, "AUGMENT",   aug_reason, aug_conf, override_qty=aug_qty )
                                action_taken = True
                        else:
                            log_rec = "HOLD"
                            log_reason = f"PnL:{getattr(position, 'gain', 0):.2f}% Sc:{int(tech_score)}"
            else:
                log_rec = "HOLD"
                log_reason = f"Grace:{10.0-position_age_minutes:.1f}m Sc:{int(tech_score)}"

        # =========================================================
        # LOGIC BRANCH B: NO POSITION (ENTRY)
        # =========================================================
        else:
            if has_opposing_pos:
                log_rec = "WAIT"
                log_reason = f"Opposing pos open"
            elif (m1_stale or is_stale) and not force:
                log_rec = "WAIT"
                log_reason = f"Stale ({freshness_reason})"

            else:
                # --- Reopen cooldown: don't re-enter within 5 min of a full close ---
                # DC breakout entries (price still below/above channel) get immediate re-entry
                # Stoch-based entries wait 5 min to avoid noise
                last_exit_ts = trade_manager.last_exit_times.get(symbol, 0)
                secs_since_close = time.time() - last_exit_ts
                # Check if price is currently outside any DC channel (DC breakout in progress)
                indicators_raw_check = trade_manager.get_indicators(symbol) or {}
                _cp = float(indicators_raw_check.get('current_price') or 0)
                _dcl5 = float(indicators_raw_check.get('dc_low_5m') or 0)
                _dch5 = float(indicators_raw_check.get('dc_high_5m') or 0)
                _is_dc_state = (_cp > 0 and (
                    (_dcl5 > 0 and _cp < _dcl5 * 0.999) or
                    (_dch5 > 0 and _cp > _dch5 * 1.001)
                ))
                # BLOCK entries if close is pending for this symbol (prevents entry/exit race)
                _pending = getattr(trade_manager, '_pending_closes', {})
                _pending_age = time.time() - _pending.get(symbol, 0)
                if _pending_age < 120:  # Block entries for 2 min after close signal
                    log_rec = "CLOSE_PENDING"
                    log_reason = f"Close pending {_pending_age:.0f}s ago"
                    return "CLOSE_PENDING"
                elif symbol in _pending and _pending_age >= 120:
                    del _pending[symbol]  # Clear expired pending
                # User directive 2026-04-10: "reenter ASAP". Zero reopen cooldown —
                # if a fresh signal fires immediately after an exit (e.g. premature stop
                # out), we want to get right back in. The tight trailing stop catches
                # any actual breakdown, so the extra churn on good signals is worth it.
                reopen_wait_s = float(getattr(config, 'TRADIER_REOPEN_WAIT_S', 0.0))
                if last_exit_ts > 0 and reopen_wait_s > 0 and secs_since_close < reopen_wait_s:
                    log_rec = "COOLDOWN"
                    log_reason = f"ReopenCD {(reopen_wait_s - secs_since_close)/60:.0f}m"
                    if force: logger.info(f"[{account_key}] ⏳ {symbol} REOPEN COOLDOWN {log_reason}")
                    return "COOLDOWN"
                # --- 0. PRICE_CROSS_BACK_REENTRY (2026-04-27 fix — was dead code in evaluate_reentry) ---
                # 662 closes/day were tagged _MANDATORY_REENTRY but never executed because the
                # executor was unreachable (positionAmt==0 check inside positionAmt>0 caller path).
                # Now wired here in BRANCH B (positionAmt==0): if a recent close happened within
                # band of current price + within max age, fire immediate reopen. NO_DOUBLE_OPEN
                # guard naturally allows this (positionAmt==0 in this branch).
                action_type = "NO_ACTION"
                reason = "SCANNING"
                qty = 0
                conf = 0
                _delta_entry = False
                _xb_reentry_fired = False
                if bool(getattr(config, 'PRICE_CROSS_BACK_REENTRY_ENABLED', True)):
                    _xb_last_t = trade_manager.last_exit_times.get(symbol, 0)
                    _xb_last_px = trade_manager.last_exit_prices.get(symbol, 0)
                    if _xb_last_t > 0 and _xb_last_px > 0 and current_price > 0:
                        _xb_age_min = (time.time() - _xb_last_t) / 60.0
                        _xb_max_age = float(getattr(config, 'PRICE_CROSS_BACK_MAX_AGE_MIN', 240.0))
                        _xb_band_pct = float(getattr(config, 'PRICE_CROSS_BACK_BAND_PCT', 0.3))
                        _xb_dist_pct = abs(current_price - _xb_last_px) / _xb_last_px * 100.0
                        if _xb_age_min < _xb_max_age and _xb_dist_pct <= _xb_band_pct:
                            # Direction sanity: LONG only buys back when price is at-or-below exit (pullback);
                            # SHORT only sells when price is at-or-above exit (rally back into resistance).
                            _xb_dir_ok = (is_long and current_price <= _xb_last_px * (1 + _xb_band_pct/100)) or \
                                         ((not is_long) and current_price >= _xb_last_px * (1 - _xb_band_pct/100))
                            if _xb_dir_ok:
                                _xb_qty = float(getattr(config, 'START_POSITION_SIZE', 600)) / max(current_price, 1e-9)
                                action_type = "OPEN"
                                qty = max(1, int(_xb_qty))
                                conf = 90.0
                                reason = f"PRICE_CROSS_BACK_REENTRY_exit{_xb_last_px:.4f}_cur{current_price:.4f}_dist{_xb_dist_pct:.2f}%_age{_xb_age_min:.0f}m"
                                _xb_reentry_fired = True
                                logger.warning(f"[{account_key}] 🔁 REENTRY {symbol} {'L' if is_long else 'S'}: {reason}")
                # --- 4. DELTA ENGINE ENTRY (Sharpe 63.44, 85.9% WR) ---
                # tra: long-term account, delta is too fast — block here so the
                # WT/DC scorer (with high threshold) is the only entry path.
                _tra_block_delta = (account_key == 'tra' and getattr(config, 'TRA_DISABLE_DELTA_ENTRY', True))
                if not _xb_reentry_fired and not _tra_block_delta and config.DELTA_ENGINE_ENABLED and config.DELTA_ENTRY_ENABLED and trade_manager.delta_tracker:
                    _d_ind = indicators_raw if indicators_raw else i
                    _d_sig = trade_manager.delta_tracker.update(symbol, _d_ind)
                    if _d_sig and ((is_long and _d_sig.entry_long) or (not is_long and _d_sig.entry_short)):
                        _delta_blocked = False
                        _block_reason = ""
                        _htf_gate = getattr(config, 'DELTA_HTF_GATE', 'none')
                        if _htf_gate != "none":
                            _wt1_4h = float(i.get("wt1_4h", 0))
                            _wt2_4h = float(i.get("wt2_4h", 0))
                            _wt1_D = float(i.get("wt1_D", 0))
                            _wt2_D = float(i.get("wt2_D", 0))
                            _ha_4h = i.get("ha_4h", 0)
                            if _htf_gate in ("4h", "4h_D", "4h_D_strict"):
                                _4h_ok = (_wt1_4h > _wt2_4h) if is_long else (_wt1_4h < _wt2_4h)
                                if not _4h_ok:
                                    _delta_blocked = True
                                    _block_reason = "HTF_4h_against"
                            if _htf_gate in ("4h_D", "4h_D_strict") and not _delta_blocked:
                                _D_ok = (_wt1_D > _wt2_D) if is_long else (_wt1_D < _wt2_D)
                                if not _D_ok:
                                    _delta_blocked = True
                                    _block_reason = "HTF_D_against"
                            if _htf_gate == "4h_D_strict" and not _delta_blocked:
                                _k4h = float(i.get("stoch_k_4h", 50))
                                _kD = float(i.get("stoch_k_D", 50))
                                if is_long and (_k4h > 80 or _kD > 80):
                                    _delta_blocked = True
                                    _block_reason = "HTF_K_overextended"
                                elif not is_long and (_k4h < 20 or _kD < 20):
                                    _delta_blocked = True
                                    _block_reason = "HTF_K_oversold"
                        if not _delta_blocked and getattr(config, 'DELTA_ATR_ENTRY_FILTER', False):
                            _atr_1h = float(i.get("atr_1h", 0))
                            _bar_move = abs(current_price - float(i.get("close_5m_prev", current_price)))
                            if _atr_1h > 0 and _bar_move < _atr_1h * 0.3:
                                _delta_blocked = True
                                _block_reason = f"ATR_noise({_bar_move:.2f}<{_atr_1h*0.3:.2f})"
                        if not _delta_blocked:
                            _base_qty = float(getattr(config, 'START_POSITION_SIZE', 600)) / current_price if current_price > 0 else 1
                            action_type = "OPEN"
                            qty = int(max(1, _base_qty))
                            conf = 100.0
                            _zr = _d_sig.zone_reason or f"bs={_d_sig.bull_speed:.1f}_es={_d_sig.bear_speed:.1f}"
                            reason = f"DELTA_ENTRY_{_d_sig.zone}_{'L' if is_long else 'S'}_{_zr}"
                            _delta_entry = True
                            logger.warning(f"[DELTA_ENTRY] {symbol} {'L' if is_long else 'S'}: zone={_d_sig.zone} {_zr} htf={_htf_gate}")
                        else:
                            logger.info(f"[DELTA_BLOCKED] {symbol} {'L' if is_long else 'S'}: {_block_reason}")
                # Fallback: WT/DC scorer if delta didn't trigger
                if action_type != "OPEN":
                    _entry_ind = indicators_raw if indicators_raw else i
                    _entry_score, _entry_reason = wt_dc_score_entry(_entry_ind, is_long, current_price)
                    # 2026-04-27 — additive entry-engine boost (default OFF; user controls activation).
                    # Engines NEVER block existing entries — only ADD score. Wrapped to never raise.
                    if getattr(config, 'LIVE_ENTRY_ENGINE_ENABLED', False):
                        _ee_score_max = 0.0
                        _ee_reasons = []
                        _ee_side = 'LONG' if is_long else 'SHORT'
                        for _ee_name, _ee_fn, _ee_flag in (
                            ('wt', _ee_should_fire_wt_entry, 'LIVE_ENTRY_ENGINE_WT_ENABLED'),
                            ('stoch', _ee_should_fire_stoch_entry, 'LIVE_ENTRY_ENGINE_STOCH_ENABLED'),
                            ('dc', _ee_should_fire_dc_entry, 'LIVE_ENTRY_ENGINE_DC_ENABLED'),
                            ('htf', _ee_should_fire_htf_entry, 'LIVE_ENTRY_ENGINE_HTF_ENABLED'),
                            ('stdev_macro', _ee_should_fire_stdev_macro_entry, 'LIVE_ENTRY_ENGINE_STDEV_MACRO_ENABLED'),
                        ):
                            if not getattr(config, _ee_flag, False): continue
                            if _ee_fn is None: continue
                            try:
                                _fire, _why, _sc = _ee_fn(symbol, _entry_ind, _ee_side)
                                if _fire and _sc >= float(getattr(config, 'LIVE_ENTRY_ENGINE_MIN_SCORE', 0.6)):
                                    _ee_score_max = max(_ee_score_max, _sc)
                                    _ee_reasons.append(f"{_ee_name}={_sc:.2f}")
                            except Exception:
                                pass  # never let engine error block entry
                        if _ee_score_max > 0:
                            _ee_boost = float(getattr(config, 'LIVE_ENTRY_ENGINE_BOOST_SCORE', 8.0))
                            _entry_score = _entry_score + (_ee_boost * _ee_score_max)
                            _entry_reason = (_entry_reason or '') + f" +ENGINES({','.join(_ee_reasons)})"
                    # tra uses a much higher entry bar so only the strongest HTF
                    # setups fire — long-term hold needs few, very high quality entries.
                    if account_key == 'tra':
                        _entry_threshold = float(getattr(config, 'TRA_WT_DC_ENTRY_THRESHOLD', 85))
                    else:
                        _entry_threshold = getattr(config, 'WT_DC_ENTRY_THRESHOLD', 55)
                    _k5m_now = float((_entry_ind or {}).get('stoch_k_5m', 50) or 50)
                    _k5m_block = (is_long and _k5m_now > float(getattr(config, 'WT_DC_ENTRY_K5M_MAX_LONG', 100))) or ((not is_long) and _k5m_now < float(getattr(config, 'WT_DC_ENTRY_K5M_MIN_SHORT', 0)))
                    # 2026-04-27 — HTF gate on WT_DC_ENTRY path (matches DELTA_HTF_GATE pattern). Closes the
                    # SHORT-into-uptrend gap that allowed PLTR loss. Default 'none' = inert; live config sets '4h'.
                    _wtdc_htf_gate = str(getattr(config, 'WT_DC_HTF_GATE', 'none')).lower()
                    _htf_block = False
                    if _wtdc_htf_gate in ('4h', '4h_d'):
                        _wt1_4h = float((_entry_ind or {}).get('wt1_4h', 0) or 0)
                        _wt2_4h = float((_entry_ind or {}).get('wt2_4h', 0) or 0)
                        _4h_against = (is_long and _wt1_4h < _wt2_4h) or ((not is_long) and _wt1_4h > _wt2_4h)
                        if _4h_against: _htf_block = True
                    if not _htf_block and _wtdc_htf_gate == '4h_d':
                        _wt1_D = float((_entry_ind or {}).get('wt1_D', 0) or 0)
                        _wt2_D = float((_entry_ind or {}).get('wt2_D', 0) or 0)
                        _D_against = (is_long and _wt1_D < _wt2_D) or ((not is_long) and _wt1_D > _wt2_D)
                        if _D_against: _htf_block = True
                    # ═══════════════════════════════════════════════════════════
                    # 🚩 NEW BASELINE 2026-05-12 — 3 additional WT_DC_ENTRY gates
                    # Source: vec_sweep winner dc45_h1_s40_grOFF
                    # ───────────────────────────────────────────────────────────
                    # ROLLBACK: set each config knob to its disabled value:
                    #   HTF_ALIGN_REQUIRED_TRADIER = 0  (currently 1)
                    #   COMBINED_STOCH_GATE_TRADIER = 100  (currently 40)
                    #   GR_HTF_GATE_ENABLED = False  (currently False, no change)
                    # Each gate sets a BLOCK flag that joins the final check below.
                    # ═══════════════════════════════════════════════════════════
                    _htf_align_block = False
                    _stoch_gate_block = False
                    _gr_htf_block = False
                    _htf_align_req = int(getattr(config, 'HTF_ALIGN_REQUIRED_TRADIER', 0))
                    if _htf_align_req > 0:
                        _htf_count = 0
                        for _htf_tf in ('1h', '4h', 'D'):
                            _wt1_h = float((_entry_ind or {}).get(f'wt1_{_htf_tf}', 0) or 0)
                            _wt2_h = float((_entry_ind or {}).get(f'wt2_{_htf_tf}', 0) or 0)
                            if is_long and _wt1_h > _wt2_h:
                                _htf_count += 1
                            elif (not is_long) and _wt1_h < _wt2_h:
                                _htf_count += 1
                        if _htf_count < _htf_align_req:
                            _htf_align_block = True
                    _stoch_gate_thr = float(getattr(config, 'COMBINED_STOCH_GATE_TRADIER', 100.0))
                    if _stoch_gate_thr < 100.0:
                        _k5m_g = float((_entry_ind or {}).get('stoch_k_5m', 50) or 50)
                        if is_long and _k5m_g >= _stoch_gate_thr:
                            _stoch_gate_block = True
                        elif (not is_long) and _k5m_g <= (100.0 - _stoch_gate_thr):
                            _stoch_gate_block = True
                    _gr_htf_enabled = bool(getattr(config, 'GR_HTF_GATE_ENABLED', False))
                    if _gr_htf_enabled:
                        _gr_req_bull = int(getattr(config, 'GR_HTF_REQUIRE_BULL', 1))
                        _gr_req_bear = int(getattr(config, 'GR_HTF_REQUIRE_BEAR', 1))
                        _gr_bull = int((_entry_ind or {}).get('wt_bull_alignment', 0) or 0)
                        _gr_bear = int((_entry_ind or {}).get('wt_bear_alignment', 0) or 0)
                        if is_long and _gr_bull < _gr_req_bull:
                            _gr_htf_block = True
                        elif (not is_long) and _gr_bear < _gr_req_bear:
                            _gr_htf_block = True
                    # ═══ END NEW BASELINE GATES ═══
                    # ═══ 2026-05-16 — BAR_MATURITY guard (DEFAULT OFF behind WT_DC_ENTRY_BAR_MATURITY_BLOCK_ENABLED) ═══
                    # WT_DC_ENTRY_80_D_bear was averaging -7.14% on 4 trades in 30d. Root
                    # cause: WT/DC scorer fires LATE in a daily bar after most of the move
                    # has already happened — entry is into exhaustion, not into a setup.
                    # Block when bar has already consumed >70% of its expected daily ATR
                    # in the SAME direction as the proposed signal. Skips block if open_D
                    # or atr_D are unavailable (fail-open, preserves existing entries on
                    # malformed data). Flag default False: legacy behavior preserved until
                    # sweep-validated.
                    _bar_maturity_block = False
                    if bool(getattr(config, 'WT_DC_ENTRY_BAR_MATURITY_BLOCK_ENABLED', False)):
                        _bm_thr = float(getattr(config, 'WT_DC_ENTRY_BAR_MATURITY_BLOCK', 0.7))
                        if _bm_thr > 0.0 and _bm_thr < 1.0:
                            _bm_open_d = float((_entry_ind or {}).get('open_D', 0) or 0)
                            _bm_atr_d = float((_entry_ind or {}).get('atr_D', 0) or 0)
                            if _bm_open_d > 0 and _bm_atr_d > 0:
                                _bm_pos = abs(current_price - _bm_open_d) / max(_bm_atr_d, 1e-6)
                                _bm_bar_up = (current_price > _bm_open_d)
                                _bm_same_dir = (is_long and _bm_bar_up) or ((not is_long) and (not _bm_bar_up))
                                if _bm_pos > _bm_thr and _bm_same_dir:
                                    _bar_maturity_block = True
                                    logger.info(f"[WT_DC_BAR_MATURITY_BLOCK] {symbol} {'L' if is_long else 'S'}: pos={_bm_pos:.2f}>thr={_bm_thr:.2f} bar_up={_bm_bar_up} px={current_price:.2f} open_D={_bm_open_d:.2f} atr_D={_bm_atr_d:.4f} score={_entry_score:.0f}")
                    if _entry_score >= _entry_threshold and not _k5m_block and not _htf_block and not _htf_align_block and not _stoch_gate_block and not _gr_htf_block and not _bar_maturity_block:
                        _base_qty = float(getattr(config, 'START_POSITION_SIZE', 600)) / current_price if current_price > 0 else 1
                        action_type = "OPEN"
                        qty = int(max(1, _base_qty))
                        conf = _entry_score
                        reason = f"WT_DC_ENTRY_{_entry_score:.0f}_{_entry_reason[:60]}"
                    elif _k5m_block and _entry_score >= _entry_threshold:
                        logger.info(f"[WT_DC_ENTRY_K5M_BLOCK] {symbol} {'L' if is_long else 'S'}: k5m={_k5m_now:.1f} score={_entry_score:.0f}")
                    elif _htf_block and _entry_score >= _entry_threshold:
                        logger.info(f"[WT_DC_HTF_BLOCK] {symbol} {'L' if is_long else 'S'}: HTF against (gate={_wtdc_htf_gate}) score={_entry_score:.0f}")
                # RZ_BREAKOUT: third entry path — fires when bb_pct_b_1h just exited extreme zone.
                # Band approach: LONG fires when bb_pctb is in [rz_bot, rz_bot+band] (just broke up from oversold).
                # SHORT fires when bb_pctb is in [rz_top-band, rz_top] (just broke down from overbought).
                # RZ_BREAKOUT_ENTRY_ENABLED defaults False — enable via config_tradier.py once sweep validates.
                if action_type != "OPEN" and getattr(config, 'RZ_BREAKOUT_ENTRY_ENABLED', False):
                    _rz_ind = indicators_raw if indicators_raw else i
                    _rz_bb_1h = float((_rz_ind or {}).get('bb_pct_b_1h', 0.5) or 0.5)
                    _rz_top_b = float(getattr(config, 'RZ_TOP_BB_THRESHOLD', 0.85))
                    _rz_bot_b = float(getattr(config, 'RZ_BOT_BB_THRESHOLD', 0.15))
                    _rz_band_b = float(getattr(config, 'RZ_BREAKOUT_BAND', 0.05))
                    _rz_fire = (is_long and _rz_bot_b <= _rz_bb_1h <= _rz_bot_b + _rz_band_b) or (not is_long and _rz_top_b - _rz_band_b <= _rz_bb_1h <= _rz_top_b)
                    if _rz_fire:
                        _base_qty = float(getattr(config, 'START_POSITION_SIZE', 600)) / current_price if current_price > 0 else 1
                        action_type = "OPEN"
                        qty = int(max(1, _base_qty))
                        conf = 70.0
                        reason = f"RZ_BREAKOUT_{'L' if is_long else 'S'}_bb={_rz_bb_1h:.2f}"
                        logger.info(f"[RZ_BREAKOUT] {account_key}:{symbol} {'L' if is_long else 'S'}: bb_pctb_1h={_rz_bb_1h:.2f} band=[{_rz_bot_b if is_long else _rz_top_b - _rz_band_b:.2f},{(_rz_bot_b + _rz_band_b) if is_long else _rz_top_b:.2f}]")
                # ═══════════════════════════════════════════════════════════════════════
                # 🚩 GR_HTF_DIRECT_ENTRY — 2026-05-12 USER MANDATE
                # Direct entry signal: Score = n_tfs_aligned × GOLDEN_RULE_MIN_IND.
                # If score >= GR_HTF_DIRECT_ENTRY_SCORE_MIN and no prior path fired → OPEN.
                # If score >= GR_HTF_DIRECT_ENTRY_DOUBLE_SCORE → double size (2×START_POSITION_SIZE).
                # Reason: GR_HTF_DIRECT_ENTRY_{score}_{detail}
                # ROLLBACK: set GR_HTF_DIRECT_ENTRY_ENABLED=False in config_tradier.py.
                # ═══════════════════════════════════════════════════════════════════════
                if action_type != "OPEN" and bool(getattr(config, 'GR_HTF_DIRECT_ENTRY_ENABLED', True)):
                    try:
                        from golden_rule_htf import score_entry_htf as _gr_htf_direct_entry
                        _grde_min_ind = int(getattr(config, 'GOLDEN_RULE_MIN_IND', 1))
                        _grde_pass, _grde_n_tfs, _grde_detail = _gr_htf_direct_entry(
                            indicators_raw if indicators_raw else i, is_long, 'tradier',
                            min_tfs=1, min_ind=_grde_min_ind, current_price=current_price)
                        # Build score as n_tfs × min_ind (the multiplicative score per spec)
                        _grde_score = float(_grde_n_tfs) * float(_grde_min_ind)
                        _grde_score_min = float(getattr(config, 'GR_HTF_DIRECT_ENTRY_SCORE_MIN', 12.0))
                        _grde_double_score = float(getattr(config, 'GR_HTF_DIRECT_ENTRY_DOUBLE_SCORE', 18.0))
                        if _grde_score >= _grde_score_min:
                            _grde_mult = 2.0 if _grde_score >= _grde_double_score else 1.0
                            _grde_sps = float(getattr(config, 'START_POSITION_SIZE', 600))
                            _grde_qty = int(max(1, (_grde_sps * _grde_mult) / current_price)) if current_price > 0 else 1
                            action_type = "OPEN"
                            qty = _grde_qty
                            conf = _grde_score
                            reason = f"GR_HTF_DIRECT_ENTRY_{_grde_score:.0f}_{_grde_detail[:60]}"
                            logger.warning(f"[GR_HTF_DIRECT_ENTRY] {account_key}:{symbol} {'L' if is_long else 'S'}: score={_grde_score:.0f} (tfs={_grde_n_tfs}×ind={_grde_min_ind}) >= {_grde_score_min:.0f} mult={_grde_mult:.0f}x qty={_grde_qty}")
                        else:
                            logger.info(f"[GR_HTF_DIRECT_ENTRY_MISS] {account_key}:{symbol} {'L' if is_long else 'S'}: score={_grde_score:.0f} < {_grde_score_min:.0f} detail={_grde_detail[:60]}")
                    except Exception as _grde_err:
                        logger.warning(f"[GR_HTF_DIRECT_ENTRY_ERR] {account_key}:{symbol}: {_grde_err}")
                # ═══ END GR_HTF_DIRECT_ENTRY ═══
                # VARIANCE_FIX 2026-04-14 — canonical-switch gate application on PRIMARY entry path.
                # The 8 previously-DEAD switches (K_ZONE_LONG/SHORT_THRESHOLD, MI_ENTRY_ENABLED,
                # MI_EXIT_ENABLED, WT_COMPOSITE_SCORING_ENABLED, WT_EXIT_TFS, WT_EXIT_MIN_TFS,
                # ENTRY_SCORE_THRESHOLD) were wired into calculate_signal_score/evaluate_open,
                # but process_position's PRIMARY entry path is wt_dc_score_entry and never called
                # evaluate_open. This block applies them as post-entry veto gates so flipping
                # them in V8 sweeps produces real paired-run variance. Defaults are non-restrictive
                # (preserve current live behaviour); sweeps that flip them tighter will gate trades.
                if action_type == "OPEN" and account_key != 'tra':
                    _veto = None
                    _side_vf = "LONG" if is_long else "SHORT"
                    # ENTRY_SCORE_THRESHOLD: gate the wt_dc _entry_score against the canonical knob.
                    # Default 0 means "no extra gate" — live behaviour unchanged.
                    _min_es = float(_cfg("ENTRY_SCORE_THRESHOLD", 0, account_key, symbol, _side_vf) or 0)
                    if _min_es > 0 and float(_entry_score) < _min_es:
                        _veto = f"ENTRY_SCORE_GATE({_entry_score:.0f}<{_min_es:.0f})"
                    # K_ZONE_LONG/SHORT_THRESHOLD: if enabled, require K_4h in the zone.
                    if _veto is None and getattr(config, 'K_ZONE_VETO_ENABLED_TRADIER', False):
                        _k4h_vf = float(i.get('stoch_k_4h', 50) or 50)
                        if is_long:
                            _kz_l = float(_cfg('K_ZONE_LONG_THRESHOLD_TRADIER', 100, account_key, symbol, _side_vf) or 100)
                            if _k4h_vf >= _kz_l:
                                _veto = f"K_ZONE_L_GATE(k4h={_k4h_vf:.0f}>={_kz_l:.0f})"
                        else:
                            _kz_s = float(_cfg('K_ZONE_SHORT_THRESHOLD_TRADIER', 0, account_key, symbol, _side_vf) or 0)
                            if _k4h_vf <= _kz_s:
                                _veto = f"K_ZONE_S_GATE(k4h={_k4h_vf:.0f}<={_kz_s:.0f})"
                    # MI_ENTRY_ENABLED: when True, require at least one MI confluence signal on 4h.
                    # FIX 2026-04-14 sentinel: was using i.get() which reads parse_market_data() output.
                    # wt_peak_structure_4h/wt_divergence_4h/wt_momentum_state_4h are NOT in that dict →
                    # always returned '' or None → _mi_ok=False → ALL entries vetoed. Use _entry_ind (=indicators_raw).
                    if _veto is None and _cfg('MI_ENTRY_ENABLED_TRADIER', False, account_key, symbol, _side_vf):
                        _wt_peak_4h_vf = _entry_ind.get('wt_peak_structure_4h', '')
                        _wt_trough_4h_vf = _entry_ind.get('wt_trough_structure_4h', '')
                        _wt_mom_4h_vf = _entry_ind.get('wt_momentum_state_4h', '')
                        _wt_div_4h_vf = _entry_ind.get('wt_divergence_4h', None)
                        if is_long:
                            _mi_ok = (_wt_trough_4h_vf == 'HL') or (_wt_mom_4h_vf == 'EXHAUST_DOWN') or (_wt_div_4h_vf == 'BULL')
                        else:
                            _mi_ok = (_wt_peak_4h_vf == 'LH') or (_wt_mom_4h_vf == 'EXHAUST_UP') or (_wt_div_4h_vf == 'BEAR')
                        if not _mi_ok:
                            _veto = f"MI_ENTRY_GATE(no_4h_confluence)"
                    # WT_COMPOSITE_SCORING_ENABLED: when sweep VETO knob is on AND composite scoring is on,
                    # require wt alignment >=2 on favourable side. Gated by WT_COMPOSITE_VETO_ENABLED_TRADIER
                    # so flipping the scoring switch alone in live code doesn't change entries.
                    # FIX 2026-04-14 sentinel ZERO_TRADES: was using i.get() which reads parse_market_data()
                    # output — wt_bull_alignment is NOT in that dict, so i.get() always returned 0 → 0<3 always
                    # True → ALL entries vetoed. Fix: read from _entry_ind (=indicators_raw). Also lowered
                    # threshold from <3 to <2: SATOSHIT fires at WT crosses (alignment 1-2), >=3 vetoed all.
                    if _veto is None and getattr(config, 'WT_COMPOSITE_VETO_ENABLED_TRADIER', False) and _cfg('WT_COMPOSITE_SCORING_ENABLED_TRADIER', False, account_key, symbol, _side_vf):
                        _wt_align_vf = float(_entry_ind.get('wt_bull_alignment' if is_long else 'wt_bear_alignment', 0) or 0)
                        _wt_comp_vf = float(_entry_ind.get('wt_composite_long' if is_long else 'wt_composite_short', 0) or 0)
                        if _wt_align_vf < 2 or _wt_comp_vf < -20.0:
                            _veto = f"WT_COMP_GATE(align={_wt_align_vf:.0f}<2_comp={_wt_comp_vf:.0f})"
                    # DC_POSITION_ENTRY_THRESHOLD: when DC_ENTRY_VETO_ENABLED_TRADIER is on, require price
                    # to be in the DC channel zone. Gated by DC_ENTRY_VETO_ENABLED_TRADIER so live code is
                    # unchanged (T65 removed DC from gate — this is sweep-only, proves threshold gates trades).
                    if _veto is None and getattr(config, 'DC_ENTRY_VETO_ENABLED_TRADIER', False):
                        _dc_th_vf = float(getattr(config, 'DC_POSITION_ENTRY_THRESHOLD', 0.25) or 0.25)
                        _dc_1h_vf = float(_entry_ind.get('dc_position_1h', 0.5) or 0.5)
                        _dc_4h_vf = float(_entry_ind.get('dc_position_4h', 0.5) or 0.5)
                        if is_long and not (_dc_1h_vf < _dc_th_vf or _dc_4h_vf < _dc_th_vf):
                            _veto = f"DC_ENTRY_GATE_L(1h={_dc_1h_vf:.2f}_4h={_dc_4h_vf:.2f}>={_dc_th_vf:.2f})"
                        elif not is_long and not (_dc_1h_vf > (1.0 - _dc_th_vf) or _dc_4h_vf > (1.0 - _dc_th_vf)):
                            _veto = f"DC_ENTRY_GATE_S(1h={_dc_1h_vf:.2f}_4h={_dc_4h_vf:.2f}<={(1.0 - _dc_th_vf):.2f})"
                    if _veto is not None:
                        logger.info(f"[VARIANCE_FIX_VETO] {account_key}:{symbol}_{_side_vf}: {_veto}")
                        action_type = "NO_ACTION"
                        reason = _veto
                if action_type == "OPEN" and trade_manager.strategy.circuit_breaker.is_blocked(reason):
                    logger.critical(f"🛑 [CIRCUIT_BREAKER] {account_key}:{symbol}_{position_side}: BLOCKED entry '{reason}' — path auto-disabled after repeated collapses")
                    return "CIRCUIT_BREAKER_BLOCKED"
                if action_type == "OPEN" and getattr(config, 'TRADIER_LOCAL_EXTREMES_SCORING_ENABLED', False):
                    _le_ind = indicators_raw if indicators_raw else i
                    try:
                        _le_sc, _le_sz, _le_rsn = _le_score_entry(_le_ind, is_long)
                        _le_min_sc = float(getattr(config, 'LOCAL_EXTREMES_MIN_SCORE', 0.0))
                        if _le_sz > 0 and current_price > 0 and _le_sc >= _le_min_sc:
                            qty = int(max(1, _le_sz / current_price))
                            reason = f"LE_{_le_rsn}_{reason}"
                            logger.info(f"[{account_key}] LE_SCORER {symbol} {'L' if is_long else 'S'}: {_le_rsn} score={_le_sc:.0f} qty={qty} (${_le_sz:.0f})")
                        elif _le_sz > 0 and _le_sc < _le_min_sc:
                            action_type = "NO_ACTION"
                            reason = f"LE_MIN_SCORE_SKIP({_le_sc:.0f}<{_le_min_sc:.0f})"
                            logger.debug(f"[{account_key}] LE_SCORER min-score skip {symbol}: score={_le_sc:.0f} < min={_le_min_sc:.0f}")
                        else:
                            action_type = "NO_ACTION"
                            reason = f"LE_SCORE_SKIP({_le_rsn})"
                            logger.debug(f"[{account_key}] LE_SCORER skip {symbol}: {_le_rsn}")
                    except Exception as _le_err:
                        logger.warning(f"[{account_key}] LE_SCORER error {symbol}: {_le_err}")
                if action_type == "OPEN":
                    # Determine intended direction from strategy return
                    signal_is_long = "LONG" in reason.upper()
                    signal_is_short = "SHORT" in reason.upper()
                    
                    # Validate against Position Key (Are we processing the right key for this signal?)
                    # If we are processing AAPL_LONG key, but strategy says SHORT, we ignore.
                    if position_side == "LONG" and signal_is_short:
                        log_rec = "WAIT"
                        log_reason = "Signal Mismatch (Short)"
                    elif position_side == "SHORT" and signal_is_long:
                        log_rec = "WAIT"
                        log_reason = "Signal Mismatch (Long)"
                    else:
                        # --- BALANCER LOGIC START ---
                        global_sentiment = float(i.get('sentiment', 0))
                        trade_manager.update_sentiment_history(global_sentiment)
                        
                        blocked_by_balancer = False
                        block_reason = ""
                        
                        breadth = (trade_manager.calculate_unified_market_ratio() - 0.5) * 2.0  # -1=full bear, +1=full bull
                        current_balance = trade_manager.get_current_portfolio_balance()  # sync, no await
                        current_long_pct = current_balance.get('long_pct', 0.5)
                        target = 0.5 + (breadth * 0.4)

                        _bear_mode = getattr(config, 'BEAR_MARKET_MODE_TRADIER', False)
                        _bal_thr = 0.8 if _bear_mode else 0.4  # In bear mode, only block at extreme levels
                        if position_side == "LONG":
                             if breadth < -_bal_thr and current_long_pct > target and not i.get('0is_top_sentiment'):
                                blocked_by_balancer = True; block_reason = f"Index Bearish ({breadth:.2f})"
                        elif position_side == "SHORT":
                             if breadth > _bal_thr and current_long_pct < target and not i.get('0is_bottom_sentiment'):
                                blocked_by_balancer = True; block_reason = f"Index Bullish ({breadth:.2f})"
                        # --- BALANCER LOGIC END ---

                        if blocked_by_balancer:
                            log_rec = "BLOCKED"
                            log_reason = block_reason
                            logger.info(f"[{account_key}] BLOCKED {symbol}: {block_reason}")
                        else:
                            log_rec = "LONG BUY" if position_side == "LONG" else "SHORT SELL"
                            log_reason = reason
                            logger.info(f"[{account_key}] EXAMINING {position_side} entry {symbol}: {reason} Score={tech_score}")
                            # Heavy artillery: pullback-reexpansion setup boosts qty
                            ha_setup, ha_score, ha_dir, ha_detail = trade_manager.check_pullback_reexpansion(symbol, indicators_raw)
                            if ha_setup and ha_dir == position_side:
                                ha_mult = 3.0 if ha_score == 4 else 2.0
                                qty = int(qty * ha_mult)
                                log_rec = "🔫 HEAVY_ART" if position_side == "LONG" else "🔫 HEAVY_ART_S"
                                logger.info(f"[{account_key}] 🔫 HEAVY ARTILLERY {symbol} ×{ha_mult:.0f} score={ha_score}: {ha_detail}")
                            # 2.5σ STDEV BREAKOUT: boost size when entering during HTF breakout
                            if getattr(config, "STDEV_BREAKOUT_ENABLED", False):
                                _sb_t = detect_stdev_breakout_t(symbol, is_long, i)
                                if _sb_t:
                                    _sb_mult = _sb_t.get('size_mult', 1.0)
                                    if _sb_mult > 1.0:
                                        qty = int(qty * _sb_mult)
                                    reason = f"STDEV_{_sb_t['phase']}_{_sb_t.get('htf', '?')}_pctb={_sb_t.get('pctb', 0):.3f}_{reason}"
                                    logger.warning(f"[STDEV_BREAKOUT] {symbol}: {_sb_t['phase']} {_sb_t['signal']} pctb={_sb_t.get('pctb', 0):.3f} size_mult={_sb_mult:.1f}x")
                                # buffers are always updated inside detect_stdev_breakout_t
                            sw_ok, sw_reason = trade_manager.is_swing_entry_allowed(position_side, float(qty) * current_price)
                            if not sw_ok:
                                log_rec = "BUDGET"; log_reason = sw_reason
                                logger.info(f"[{account_key}] 💰 SWING BUDGET BLOCKED {symbol}: {sw_reason}")
                            elif market_open:
                                # ═══ 2026-05-17 CATALYST_VOLUME_GATE — DEFAULT OFF behind CATALYST_VOLUME_GATE_ENABLED ═══
                                # Per strategy_plan.md §5.6 + audit_relvol_filters.md §3: O'Neil CAN-SLIM "N" +
                                # Bulkowski failure-avoidance filter. Block new OPENs unless today's D-volume
                                # ≥ CATALYST_VOLUME_RATIO × 50d avg AND price breaks D-Donchian-20 in entry direction.
                                # Augments are exempt (we are inside the action_type == "OPEN" branch at ~2446).
                                # Fail-CLOSED on missing data. Reason on block: BLOCKED_CATALYST_VOLUME_NO_BREAKOUT.
                                _cvg_blocked = False
                                if bool(getattr(config, 'CATALYST_VOLUME_GATE_ENABLED', False)):
                                    _cvg_ratio_min = float(getattr(config, 'CATALYST_VOLUME_RATIO', 1.5))
                                    _cvg_vol_today = safe_fetch_float(i.get('volume_D', 0), 0.0)
                                    _cvg_vol_50d_avg = safe_fetch_float(i.get('volume_D_50_sma', 0), 0.0)
                                    _cvg_close_d = safe_fetch_float(i.get('close_D', 0), 0.0)
                                    _cvg_dc_high_d = safe_fetch_float(i.get('dc_high_D', 0), 0.0)
                                    _cvg_dc_low_d = safe_fetch_float(i.get('dc_low_D', 0), 0.0)
                                    _cvg_pass = False
                                    _cvg_detail = ""
                                    if _cvg_vol_50d_avg <= 0 or _cvg_vol_today <= 0:
                                        _cvg_detail = f"NODATA_vol={_cvg_vol_today:.0f}_avg={_cvg_vol_50d_avg:.0f}"
                                    elif _cvg_vol_today < _cvg_ratio_min * _cvg_vol_50d_avg:
                                        _cvg_detail = f"LOW_VOL_ratio={(_cvg_vol_today / _cvg_vol_50d_avg):.2f}x_need={_cvg_ratio_min:.2f}x"
                                    else:
                                        if is_long:
                                            if _cvg_close_d > 0 and _cvg_dc_high_d > 0 and _cvg_close_d >= _cvg_dc_high_d:
                                                _cvg_pass = True
                                            else:
                                                _cvg_detail = f"NO_DC_BREAK_L_close={_cvg_close_d:.2f}_dchigh={_cvg_dc_high_d:.2f}"
                                        else:
                                            if _cvg_close_d > 0 and _cvg_dc_low_d > 0 and _cvg_close_d <= _cvg_dc_low_d:
                                                _cvg_pass = True
                                            else:
                                                _cvg_detail = f"NO_DC_BREAK_S_close={_cvg_close_d:.2f}_dclow={_cvg_dc_low_d:.2f}"
                                    if not _cvg_pass:
                                        _cvg_blocked = True
                                        log_rec = "BLOCKED"
                                        log_reason = f"BLOCKED_CATALYST_VOLUME_NO_BREAKOUT_{_cvg_detail[:80]}"
                                        logger.info(f"[CATALYST_VOLUME_GATE_BLOCK] {account_key}:{symbol} {'L' if is_long else 'S'}: {_cvg_detail}")
                                # ═══ END CATALYST_VOLUME_GATE ═══
                                if not _cvg_blocked:
                                    _pre_r1_stop = safe_fetch_float(i.get('dc_low4_5m' if is_long else 'dc_high4_5m'), 0.0)
                                    if _pre_r1_stop > 0 and position:
                                        _cur_r1_pre = float(getattr(position, 'r1_stop_price', 0.0) or 0.0)
                                        if _cur_r1_pre <= 0 or (is_long and _pre_r1_stop > _cur_r1_pre) or (not is_long and _pre_r1_stop < _cur_r1_pre):
                                            position.r1_stop_price = _pre_r1_stop
                                    # 2026-05-09 USER MANDATE: route reentry-flavored OPENs through guaranteed dispatcher.
                                    # Fresh (non-reentry) opens use the original single-shot dispatch.
                                    if 'REENTRY' in (reason or '').upper():
                                        await _dispatch_reentry_guaranteed_trd(order_queue, trade_manager, position_key, action_type, reason, conf, override_qty=qty)
                                    else:
                                        await queue_trade_action(order_queue, trade_manager, position_key, action_type, reason, conf, override_qty=qty)
                                    trade_manager.strategy.circuit_breaker.record_entry(position_key, current_price, reason, indicators=indicators_raw)
                                    action_taken = True
            
                else:
                    # Strategy returned NO_ACTION
                    log_rec = "WAIT"
                    log_reason = f"Sc:{int(tech_score)} {strategy_reason}"[:20]

        if action_taken:
             _entry = float(getattr(position, 'entry_price', 0.0) or 0.0) if position else 0.0
             _gain = calculate_gain(position_side, current_price, _entry) if _entry > 0 else 0.0
             _qty = locals().get('aug_qty', None) or locals().get('qty', 0) or 0
             _tdetails = {"qty": _qty, "price": current_price, "value": round(float(_qty) * current_price, 2), "entry_price": _entry, "gain_pct": round(_gain, 2), "position_amt": position.positionAmt if position else 0, "action_type": log_rec, "side": "BUY" if is_long else "SELL"}
             await record_decision_context(trade_manager.redis_manager, account_key, position_key, log_rec, log_reason, i, tech_score, market_context, trade_details=_tdetails)

        # 6. UNIFIED VISUAL LOGGING
        await log_stoch_snapshot(trade_manager, account_key, event_type or "DECISION", position_key, current_price, indicators_raw, tech_score, sentiment_rank, log_rec, log_reason, force=(action_taken or force))
        trade_manager.last_monitored_positions[position_key] = time.time()
        return "PROCESSED"

    except Exception as e:
        logger.error(f"[{account_key}] 💥 CRASH in process_position for {symbol}: {e}")
        return "ERROR"


class OrderQueue:
    def __init__(self, trade_manager, max_concurrent_orders=500):
        self.trade_manager = trade_manager
        self.max_concurrent_orders = max_concurrent_orders
        self._orders = asyncio.Queue()
        self.last_executed_time = {}
    
    async def add_order(self, order: dict) -> tuple[bool, str]:
        account_key = order.get('account_key', '')
        current_account.set(account_key)
        position_key = construct_position_key(account_key, order.get('symbol', ''), order.get('position_side', ''))

        now_ts = time.time()
        async with self.trade_manager.dedupe_lock:
            if position_key in self.trade_manager.order_deduplication:
                last_time = float(self.trade_manager.order_deduplication[position_key])
                if (now_ts - last_time) < 90.0:
                    return False, "DUPLICATE"
            self.trade_manager.order_deduplication[position_key] = now_ts
        try:
            await asyncio.wait_for(self._orders.put(order), timeout=2.0)
            return True, "SUCCESS"
        except asyncio.TimeoutError:
            logger.error(f"[add_order] Timeout adding order: {position_key}")
            return False, "TIMEOUT"
        except Exception as e:
            logger.error(f"[add_order] Error: {e}")
            return False, f"EXCEPTION_{str(e)[:50]}"
    
    async def process_orders(self):
        logger.info("[OrderQueue.process_orders]  Task started")
        while True:
            try:
                order = await self._orders.get()
                await self.handle_order(order)
                self._orders.task_done()
            except Exception as e:
                logger.error(f"[process_orders] Error: {e}",  exc_info=True )
                await asyncio.sleep(1)

    async def handle_order(self, order: dict):
        try:
            account_key = order.get('account_key', '')
            current_account.set(account_key)
            symbol = order.get('symbol', '')
            side = order.get('side', '')
            quantity = int(order.get('quantity', 0))
            action = order.get('action', 'OPEN')
            reason = order.get('reason', '')
            price = order.get('current_price', 0)
            order_id = order.get('order_id','None')
            if not is_regular_trading_hours():
                logger.debug(f"[HANDLE_ORDER] Not in trading hours, skipping order: {symbol} {action} {side} qty={quantity}")
                return
            logger.info(f"[ORDER_DEBUG] Processing order: {symbol} {action} {side} qty={quantity}")
            position_key = construct_position_key(account_key, symbol, order.get('position_side', ''))            
            result = await self.trade_manager.execute_trade_action(account_key=account_key,position_key=position_key,symbol=symbol,quantity=quantity,current_price=price,side=side,position_side=order.get('position_side', ''),unique_id=order.get('unique_id', f"{account_key}:{symbol}_{action}_{int(time.time())}"),is_full_close=(action == "CLOSE"),action=action,reason=reason,override_qty=order.get('override_qty')  )
            if result == "SUCCESS":
                status = "SUBMITTED"
                order_id = order.get('order_id', 'None')
                logger.info(f"[ORDER_RESULT] {symbol} {action} {side} qty={quantity} -> {status} (ID: {order_id})")
            else:
                status = str(result) if result else "FAILED"
                order_id = "N/A"

            # Calculate gain% and $ for trade log
            _pos = self.trade_manager.position_manager.get_position(position_key) if hasattr(self, 'trade_manager') else None
            _entry = float(getattr(_pos, 'entry_price', 0)) if _pos else 0.0
            _pos_side = order.get('position_side', '')
            _gain_pct = ((price - _entry) / _entry * 100) if _pos_side == 'LONG' and _entry > 0 and price > 0 else ((_entry - price) / _entry * 100) if _pos_side == 'SHORT' and _entry > 0 and price > 0 else 0.0
            _gain_dollar = quantity * price * _gain_pct / 100 if _gain_pct else 0.0
            _pos_val = float(getattr(_pos, 'positionAmt', 0)) * price if _pos and price > 0 else 0.0
            log_msg = (
                f"[TRADE] {symbol:<5} {action:<7} {side:<4} | "
                f"Qty: {quantity:<4} @ ${price:<7.2f} | "
                f"Status: {status:<10} | order_id: {order_id} | "
                f"Reason: {reason} | Gain: {_gain_pct:+.2f}% ${_gain_dollar:+.1f} | Entry: ${_entry:.2f} PosVal: ${_pos_val:.0f} | "
            )

            if "GHOST_CLEARED_COOLDOWN" in status or "REDIS_EXECUTION_LOCK" in status:
                pass
            elif status == "SUBMITTED":
                logger.info(log_msg)
                tradier_action_logger.info(f"{position_key}: {log_msg}")
                self.last_executed_time[(position_key, side)] = time.time()
            else:
                logger.warning(log_msg)
                async with self.trade_manager.dedupe_lock:
                    self.trade_manager.order_deduplication.pop(position_key, None)

        except Exception as e:
            logger.error(f"[handle_order] Critical Error: {e}",  exc_info=True )


async def _dispatch_reentry_guaranteed_trd(order_queue, trade_manager, position_key, action, reason, conviction, override_qty=None, max_attempts=None, backoff_s=None):
    """USER MANDATE 2026-05-09 (mirror of crypto helper): reentry signals must NEVER be silently dropped on transient queue failure.
    Wraps queue_trade_action with N retries + explicit logging for every attempt.
    Returns the final result (same contract as queue_trade_action: 'SUCCESS' or False)."""
    if not bool(getattr(config, 'REENTRY_NEVER_SKIP_ENABLED', True)):
        return await queue_trade_action(order_queue, trade_manager, position_key, action, reason, conviction, override_qty=override_qty)
    if max_attempts is None: max_attempts = int(getattr(config, 'REENTRY_DISPATCH_MAX_ATTEMPTS', 3))
    if backoff_s is None: backoff_s = float(getattr(config, 'REENTRY_DISPATCH_BACKOFF_S', 0.4))
    last_result = None
    _block_substrings = ('BALANCE_FLOOR_HALT', 'OVERTRADE_GUARD', 'DAILY_LOSS_HALT', 'MAX_POS_BLOCK', 'LS_RATIO_BLOCK', 'RED_ZONE_TRADIER', 'UNMAPPED_ACTION_BLOCK', 'ALLOWLIST_BLOCK', 'OPENING_BUFFER_NO_TRADE')
    for attempt in range(1, max_attempts + 1):
        try:
            last_result = await queue_trade_action(order_queue, trade_manager, position_key, action, reason, conviction, override_qty=override_qty)
        except Exception as _re_e:
            logger.error(f"💥 [REENTRY_DISPATCH_CRASH] {position_key} attempt={attempt}/{max_attempts}: {type(_re_e).__name__}: {_re_e}")
            last_result = f"CRASH:{type(_re_e).__name__}"
        if last_result and (last_result is True or (isinstance(last_result, str) and (last_result.startswith("QUEUED") or last_result.startswith("SUCCESS")))):
            if attempt > 1:
                logger.warning(f"✅ [REENTRY_DISPATCH_RETRY_OK] {position_key}: queued on attempt {attempt}/{max_attempts} (reason={reason[:80]})")
            return last_result
        # If the reason is a known HARD-BLOCK (per pre-queue gates) retrying is pointless.
        if any(_b in (reason or '') for _b in _block_substrings):
            logger.warning(f"⚠️ [REENTRY_DISPATCH_HARD_BLOCK] {position_key}: {reason[:120]} — not retrying (hard gate, not transient)")
            return last_result
        logger.warning(f"⚠️ [REENTRY_DISPATCH_TRANSIENT_FAIL] {position_key} attempt={attempt}/{max_attempts}: result={last_result} reason={reason[:80]} — retry in {backoff_s}s")
        if attempt < max_attempts:
            try: await asyncio.sleep(backoff_s)
            except Exception: pass
    logger.critical(f"☠️ [REENTRY_DISPATCH_FAILED_PERSIST] {position_key}: ALL {max_attempts} attempts failed result={last_result} reason={reason[:120]} qty={override_qty} — reentry signal NOT silently dropped, see this log line")
    try:
        if not hasattr(trade_manager, '_reentry_dispatch_failures'):
            trade_manager._reentry_dispatch_failures = {}
        trade_manager._reentry_dispatch_failures[position_key] = {
            'ts': time.time(), 'reason': reason, 'conviction': conviction,
            'override_qty': override_qty, 'last_result': last_result, 'attempts': max_attempts,
        }
    except Exception: pass
    return last_result


async def queue_trade_action(order_queue: OrderQueue, trade_manager, position_key: str, action: str, reason: str, conviction: float = 50.0, override_qty: float = None):
    try:
        account_key, _, _ = parse_position_key(position_key)
        current_account.set(account_key)
        # ═══════════════════════════════════════════════════════════════════════════
        # 🚨 BALANCE_FLOOR_HALT — NEVER GO BELOW $0 (user 2026-05-01)
        # balance_floor_watchdog writes data/HALT_TRADING_<acct> sentinel when
        # account total_cash drops to/below MIN_FLOOR_USD ($1 default). Refuse
        # OPEN/AUGMENT/ENTRY/BUY when sentinel exists. CLOSE/REDUCE still allowed.
        # ═══════════════════════════════════════════════════════════════════════════
        try:
            _bf_act = (action or '').upper()
            if 'OPEN' in _bf_act or 'AUGMENT' in _bf_act or 'ENTRY' in _bf_act or _bf_act == 'BUY':
                from pathlib import Path as _BFPath
                _bf_base = getattr(config, 'BASE_PATH', None) or '/Users/niels/Documents/binance'
                _bf_halt = _BFPath(_bf_base) / 'data' / f'HALT_TRADING_{account_key}'
                if _bf_halt.exists():
                    logger.critical(f"🚨 [BALANCE_FLOOR_HALT] {position_key}: BLOCKED — {_bf_halt} exists. action={action} reason={(reason or '')[:80]}")
                    return False
        except Exception as _bf_e:
            logger.warning(f"[BALANCE_FLOOR_HALT] check error (fail-open): {_bf_e}")
        # ═══════════════════════════════════════════════════════════════════════════
        # 🚦 OVERTRADE_GUARD — USER 2026-05-09: cap OPEN/AUGMENT to TRADES_PER_SYM_PER_DAY_MAX
        # per pkey per UTC day. CLOSE/REDUCE NOT capped. Emergency exits bypass.
        # Mirror of ez_manage equivalent. Module-level _overtrade_counter keyed by
        # 'YYYYMMDD:pkey'. Trim daily.
        # ═══════════════════════════════════════════════════════════════════════════
        try:
            _ot_act = (action or '').upper()
            if ('OPEN' in _ot_act or 'AUGMENT' in _ot_act or 'ENTRY' in _ot_act) and \
               'CLOSE' not in _ot_act and 'REDUCE' not in _ot_act:
                _ot_max = int(getattr(config, 'TRADES_PER_SYM_PER_DAY_MAX', 8))
                _ot_emerg = ('RIDICULOUS' in str(reason or '').upper() or
                             'BREAK_REVERSE' in str(reason or '').upper() or
                             'ALL_TF_AGAINST' in str(reason or '').upper() or
                             'INTERVENTION' in str(reason or '').upper() or
                             'MANUAL' in str(reason or '').upper())
                if _ot_max > 0 and not _ot_emerg:
                    global _OVERTRADE_COUNTER
                    if '_OVERTRADE_COUNTER' not in globals():
                        _OVERTRADE_COUNTER = {}
                    _ot_today = datetime.now(timezone.utc).strftime('%Y%m%d')
                    _ot_key = f"{_ot_today}:{position_key}"
                    _ot_n = _OVERTRADE_COUNTER.get(_ot_key, 0)
                    if len(_OVERTRADE_COUNTER) > 5000:
                        _OVERTRADE_COUNTER = {k: v for k, v in _OVERTRADE_COUNTER.items() if k.startswith(_ot_today)}
                    if _ot_n >= _ot_max:
                        logger.warning(f"🚦 [OVERTRADE_GUARD] {position_key}: BLOCKED — already {_ot_n} opens today (cap={_ot_max}). action={action} reason={(reason or '')[:80]}")
                        return False
                    _OVERTRADE_COUNTER[_ot_key] = _ot_n + 1
        except Exception as _ot_e:
            logger.warning(f"[OVERTRADE_GUARD] check error (fail-open): {_ot_e}")
        if not is_regular_trading_hours():
            logger.debug(f"[queue_trade_action] not in trading hours")
            return
        # ═══ TRADIER_PHYSICS_OPPOSITE_SIDE_BLOCK (2026-05-11) ═══
        # Tradier cannot hold LONG + SHORT on same equity in same account simultaneously.
        # Refuse OPEN/AUGMENT/ENTRY/REENTRY at queue time when opposite side has amt > 0.
        # This is at the EARLIEST gate so the order never even hits the queue.
        try:
            _ts_act = (action or '').upper()
            if ('OPEN' in _ts_act or 'AUGMENT' in _ts_act or 'ENTRY' in _ts_act or 'REENTER' in _ts_act or _ts_act == 'BUY'):
                _ts_acct, _ts_sym, _ts_side = parse_position_key(position_key)
                if _ts_acct in {'trb', 'trc', 'tra'} and _ts_side in {'LONG', 'SHORT'}:
                    _ts_opp_side = 'SHORT' if _ts_side == 'LONG' else 'LONG'
                    _ts_opp_pk = f"{_ts_acct}:{_ts_sym}_{_ts_opp_side}"
                    _ts_opp_pos = trade_manager.position_manager.get_position(_ts_opp_pk) if hasattr(trade_manager, 'position_manager') and trade_manager.position_manager else None
                    _ts_opp_amt = abs(float(getattr(_ts_opp_pos, 'positionAmt', 0) or 0)) if _ts_opp_pos else 0.0
                    if _ts_opp_amt > 0.0001:
                        logger.critical(f"🛑 [QUEUE_OPPOSITE_SIDE_BLOCK] {position_key}: opposite-side {_ts_opp_pk} has amt={_ts_opp_amt} — Tradier physics prohibit dual-side, REFUSING queue. action={action} reason={(reason or '')[:80]}")
                        return False
        except Exception as _ts_e:
            logger.warning(f"[QUEUE_OPPOSITE_SIDE_BLOCK] check error (fail-open): {_ts_e}")
        # 2026-04-27 user rule: stop "headless-chicken" duplicate fires. Same
        # (position_key, action) within cooldown → refused. Catches MSTR REBALANCE
        # loop and any other path that re-fires after a rejected/invalid order.
        if not hasattr(trade_manager, '_queue_attempt_ts'):
            trade_manager._queue_attempt_ts = {}
        _qa_key = f"{position_key}|{action}"
        _qa_now = time.time()
        _qa_cooldown = float(getattr(config, 'TRADIER_QUEUE_DEDUPE_SEC', 60.0))
        _qa_last = trade_manager._queue_attempt_ts.get(_qa_key, 0.0)
        if _qa_now - _qa_last < _qa_cooldown:
            logger.warning(f"[QUEUE_DEDUPE] {position_key} {action}: queued {_qa_now - _qa_last:.0f}s ago < {_qa_cooldown:.0f}s — refusing duplicate. Reason='{reason[:80]}'")
            return False
        trade_manager._queue_attempt_ts[_qa_key] = _qa_now
        if position_key.count(":") != 1:
            logger.error(f"[queue_trade_action] !! CORRUPTED position_key with {position_key.count(':')} colons: {position_key}")
            from utils import clean_position_key, orjson_default
            position_key = clean_position_key(position_key)
            if position_key.count(":") != 1:
                return False
        position = trade_manager.position_manager.get_position(position_key)
        if position is not None:
            positionAmt = position.positionAmt
            try: positionAmt = abs(float(positionAmt))
            except Exception: positionAmt = 0.0
            if action in ["CLOSE", "REDUCE", "AUGMENT"] and positionAmt <= 0:
                logger.warning(f"[queue_trade_action] !! Blocked {action}: {position_key} has no position in Redis (positionAmt={positionAmt})")
                return False
            # 2026-04-27 USER ABSOLUTE: NO MULTIPLE OPENS. EVER.
            # If position already exists for this key (positionAmt > 0), block any new open of any kind.
            # OPEN/REENTER/REENTRY/HEDGE all forbidden while same-key position is alive.
            # AUGMENT is the ONLY legitimate add path — and it has its own MIN_GAIN gates upstream.
            if action in ("OPEN", "REENTER", "REENTRY", "REENTRY_OPEN", "HEDGE") and positionAmt > 0:
                logger.critical(f"[NO_DOUBLE_OPEN_BLOCK] {position_key}: BLOCKED {action} — positionAmt={positionAmt:.4f} > 0. Only AUGMENT allowed (under MIN_GAIN rules). Reason='{reason[:80]}'")
                return False
            # 2026-04-29 USER RULE: post-close cooldown. After a CLOSE, block re-OPEN of same symbol
            # for TRADIER_POST_CLOSE_COOLDOWN_MIN minutes. Stops the 1-share open→close→reopen flap
            # observed today on NVDA/USO/MSFT/GOOGL (LONG BUY firing every ~30s).
            if action in ("OPEN", "REENTER", "REENTRY", "REENTRY_OPEN", "HEDGE"):
                _pc_cooldown_min = float(getattr(config, 'TRADIER_POST_CLOSE_COOLDOWN_MIN', 15.0))
                if _pc_cooldown_min > 0:
                    _pc_last_red = getattr(position, 'last_reduction_time', None)
                    if _pc_last_red is not None:
                        try:
                            _pc_dt = safe_datetime(_pc_last_red) if not isinstance(_pc_last_red, datetime) else _pc_last_red
                            _pc_age_min = (datetime.now(timezone.utc) - _pc_dt).total_seconds() / 60.0
                            if _pc_age_min < _pc_cooldown_min:
                                logger.warning(f"[POST_CLOSE_COOLDOWN] {position_key}: BLOCKED {action} — last close {_pc_age_min:.1f}m ago < {_pc_cooldown_min:.0f}m cooldown. Reason='{reason[:80]}'")
                                return False
                        except Exception as _pc_e:
                            logger.debug(f"[POST_CLOSE_COOLDOWN_ERR] {position_key}: {_pc_e}")
        parts = position_key.split(':')
        if len(parts) != 2:
            return False
        account_key,symbol,position_side=parse_position_key(position_key)
        current_price = 0.0
        if position:
            current_price = getattr(position, 'current_price', 0) or getattr(position, 'mark_price', 0) or 0
        if current_price <= 0:
            current_price,ts = await trade_manager.get_current_price(symbol)
        if current_price <= 0:
            logger.warning(f"[queue_trade_action] No price for {symbol}")
            return False
        
        if action in ("OPEN", "AUGMENT", "REENTER", "REENTRY"):
            # TRADIER OPPOSING POSITION BLOCK — Tradier cannot hold long+short simultaneously
            if trade_manager.position_manager:
                _opp_side = "SHORT" if position_side == "LONG" else "LONG"
                _opp_key = construct_position_key(account_key, symbol, _opp_side)
                _opp_pos = trade_manager.position_manager.get_position(_opp_key)
                if _opp_pos and abs(float(getattr(_opp_pos, 'positionAmt', 0))) > 0:
                    logger.critical(f"[OPPOSING_BLOCK] {position_key}: BLOCKED {action} — cannot open {position_side} while {_opp_side} position held (Tradier no-hedge). Close {_opp_side} first.")
                    return False
            # === 2026-04-27 RED_ZONE_GATE_TRADIER (options-OI walls as L2 proxy) ===
            # Tradier exposes no L2 depth. Use options-chain max-OI strikes from data/stocks_oi_cache/{sym}.json
            # (populated by tradier_options_oi_fetcher.py READ-ONLY) as proxy walls:
            #   max_call_oi_strike = options-implied resistance ceiling (dealer hedging crushes momentum here)
            #   max_put_oi_strike  = options-implied support floor (dealer absorption / max-pain pin)
            # Block LONG when underlying is within RED_ZONE_TRADIER_MIN_DISTANCE_PCT below max_call_oi_strike.
            # Block SHORT when underlying is within RED_ZONE_TRADIER_MIN_DISTANCE_PCT above max_put_oi_strike.
            if bool(getattr(config, 'RED_ZONE_TRADIER_GATE_ENABLED', False)):
                _rzt_apply = True
                if action == "AUGMENT" and not bool(getattr(config, 'RED_ZONE_TRADIER_AUGMENT_GATE_ENABLED', True)):
                    _rzt_apply = False
                if _rzt_apply:
                    try:
                        _rzt_min_dist = float(getattr(config, 'RED_ZONE_TRADIER_MIN_DISTANCE_PCT', 0.5))
                        _rzt_min_oi = int(getattr(config, 'RED_ZONE_TRADIER_MIN_OI_AT_WALL', 1000))
                        _rzt_stale_h = float(getattr(config, 'RED_ZONE_TRADIER_STALE_MAX_HOURS', 4.0))
                        _rzt_cache_p = Path(config.BASE_PATH) / "data" / "stocks_oi_cache" / f"{symbol}.json"
                        if _rzt_cache_p.exists():
                            with open(_rzt_cache_p) as _rzt_fh: _rzt_doi = json.load(_rzt_fh)
                            _rzt_age_h = (time.time() - (_rzt_doi.get("ts") or 0)) / 3600.0
                            if _rzt_age_h <= _rzt_stale_h:
                                # 2026-04-28 DEEP HEATMAP: check ALL top-N walls within ±50%, not just max.
                                # User: "same utility as oi but over the next 50% up or down".
                                # Falls back to single max strike if top_*_walls fields not yet populated (older cache).
                                _check_long = (position_side == "LONG")
                                _walls = _rzt_doi.get("top_call_walls" if _check_long else "top_put_walls") or []
                                if not _walls:
                                    # backward-compat: synthesize single-wall list from max fields
                                    _ms = _rzt_doi.get("max_call_oi_strike" if _check_long else "max_put_oi_strike")
                                    _mv = _rzt_doi.get("max_call_oi_value" if _check_long else "max_put_oi_value") or 0
                                    if _ms:
                                        _dp = ((_ms - current_price) if _check_long else (current_price - _ms)) / current_price * 100.0
                                        _walls = [{"strike": _ms, "oi": _mv, "dist_pct": _dp}]
                                # Iterate ALL walls; block on FIRST that's too close + size sufficient + correct side
                                _blocked = False
                                for _w in _walls:
                                    try:
                                        _ws = float(_w.get("strike") or 0); _woi = int(_w.get("oi") or 0)
                                        if _ws <= 0 or _woi < _rzt_min_oi:
                                            continue
                                        # LONG: only walls ABOVE price are resistance ceilings; SHORT: only walls BELOW price are support floors
                                        if _check_long and _ws <= current_price: continue
                                        if (not _check_long) and _ws >= current_price: continue
                                        _dist = abs(_ws - current_price) / current_price * 100.0
                                        if _dist < _rzt_min_dist:
                                            _label = "call-OI ceiling" if _check_long else "put-OI floor"
                                            _side = "LONG" if _check_long else "SHORT"
                                            logger.warning(f"🧱 [RED_ZONE_TRADIER] {position_key}: BLOCKED {_side} {action} — {_label} ${_ws:.2f} {_dist:.2f}% away (OI={_woi} ≥ {_rzt_min_oi}, threshold {_rzt_min_dist}%, deep-heatmap top-N). reason={reason[:60]}")
                                            _blocked = True
                                            break
                                    except Exception:
                                        continue
                                if _blocked:
                                    return False
                    except Exception as _rzt_exc:
                        logger.debug(f"[RED_ZONE_TRADIER] {position_key}: exception {type(_rzt_exc).__name__} {_rzt_exc} — skipped (no block)")
        if action == "OPEN":
            # BACKTEST_CHANGE_T37: daily loss circuit breaker
            if _daily_loss_tracker.get("halted", False):
                logger.warning(f"[DAILY_LOSS_HALT] Blocking OPEN {position_key}: daily loss limit breached")
                return False
            # BACKTEST_CHANGE_T35: max concurrent positions
            _max_conc = getattr(config, 'MAX_CONCURRENT_POSITIONS', 999)
            if _max_conc < 999 and trade_manager.position_manager:
                _oc = sum(1 for _pk, _p in trade_manager.position_manager.positions.items() if abs(float(getattr(_p, 'positionAmt', 0))) > 0)
                if _oc >= _max_conc:
                    logger.warning(f"[MAX_POS_BLOCK] Blocking OPEN {position_key}: {_oc} >= {_max_conc} max concurrent")
                    return False
            # L/S ratio enforcement — BY DOLLAR VALUE (not count)
            # 2026-04-09 FIX: count ratio was blind to size (6L tiny vs 2S huge = count 3.0 but value 0.3)
            if getattr(config, 'LS_RATIO_ENFORCE_TRADIER', False) and trade_manager.position_manager:
                _lv2 = 0.0; _sv2 = 0.0
                for _pk, _p in trade_manager.position_manager.positions.items():
                    _amt = abs(float(getattr(_p, 'positionAmt', 0) or getattr(_p, 'quantity', 0)))
                    if _amt <= 0: continue
                    _px = float(getattr(_p, 'mark_price', 0) or getattr(_p, 'entry_price', 0))
                    if _px <= 0: continue
                    _val = _amt * _px
                    if _pk.endswith("_LONG"): _lv2 += _val
                    elif _pk.endswith("_SHORT"): _sv2 += _val
                _adj2 = _ls_ratio_4h_adj_tradier.get("adj", 0.0)
                _ls_min2 = max(0.30, getattr(config, 'LS_RATIO_MIN_TRADIER', 0.50) + _adj2)
                _ls_max2 = min(3.00, getattr(config, 'LS_RATIO_MAX_TRADIER', 2.00) + _adj2)
                _ratio2 = _lv2 / max(_sv2, 1.0)
                if position_side == "LONG" and _ratio2 > _ls_max2:
                    logger.warning(f"[LS_RATIO_BLOCK] {position_key}: L/S VALUE ratio ${_lv2:.0f}/${_sv2:.0f}={_ratio2:.2f} > max {_ls_max2:.2f}, blocking LONG open")
                    return False
                if position_side == "SHORT" and _ratio2 < _ls_min2:
                    logger.warning(f"[LS_RATIO_BLOCK] {position_key}: L/S VALUE ratio ${_lv2:.0f}/${_sv2:.0f}={_ratio2:.2f} < min {_ls_min2:.2f}, blocking SHORT open")
                    return False
            side = "BUY" if position_side == "LONG" else "SELL"
        elif action in ["CLOSE", "REDUCE", "SELL"]:
            side = "SELL" if position_side == "LONG" else "BUY"
        elif action in ["AUGMENT", "REENTER", "REENTRY"]:
            side = "BUY" if position_side == "LONG" else "SELL"
        else:
            logger.critical(f"[UNMAPPED_ACTION_BLOCK] {position_key}: action='{action}' not in any known action list — BLOCKING to prevent wrong-direction order")
            return False

        # logger.info(f"[queue_trade_action] 📋 ORDER LOGIC: action={action} position_side={position_side} -> side={side} symbol={symbol}")
        
        is_recent_reentry = False
        final_override_qty = None
        if action in ("REENTER", "REENTRY"):
            exit_time = None
            if position:
                exit_time = getattr(position, 'last_reduction_time', None)
            if not exit_time:
                last_exit_time = getattr(trade_manager, 'last_exit_times', {}).get(symbol, 0)
                if last_exit_time > 0:
                    exit_time = datetime.fromtimestamp(last_exit_time, tz=timezone.utc)
            if exit_time and isinstance(exit_time, str):
                exit_time = safe_datetime(exit_time)
            if exit_time and isinstance(exit_time, datetime):
                hours_since_exit = (datetime.now(timezone.utc) - exit_time).total_seconds() / 3600.0
                if hours_since_exit < 2.0:
                    is_recent_reentry = True
                    last_reduction_amount = getattr(position, 'last_reduction_amount', None) if position else None
                    if last_reduction_amount and float(last_reduction_amount) > 0:
                        final_override_qty = float(last_reduction_amount)
                        logger.info(f"[queue_trade_action] Recent reentry (<2h): Using position.last_reduction_amount={final_override_qty} as override_qty")
        if action == "OPEN":
            quantity = override_qty if override_qty else await trade_manager.calculate_position_size(symbol, current_price, account_key=account_key)
        elif action in ["REDUCE", "CLOSE"]:
            if position:
                # 2026-04-29 FIX: was `getattr(position, 'quantity', 0)` — TradierPosition has NO 'quantity'
                # attribute (only positionAmt). This silently returned 0, then quantity = 0 * 0.5 = 0
                # → either no order, or order qty came from override_qty. Now reads positionAmt correctly.
                position_qty = abs(getattr(position, 'positionAmt', 0) or 0)
                quantity = override_qty if override_qty else (position_qty * 0.5)
                # 2026-04-29 USER ABSOLUTE: NEVER queue a SELL/CLOSE for more shares than we own.
                # Today user saw "Sell 81" pending order when positionAmt=38 → 43 phantom shares.
                # If override_qty exceeds actual position by >1 share, ABORT (not silent clamp).
                if override_qty and position_qty > 0 and float(override_qty) > position_qty + 1:
                    logger.critical(f"🚫 [QTY_OVERSHOOT_ABORT] {position_key}: override_qty={override_qty} > positionAmt={position_qty}+1 — REFUSING to place {action}. Real bug — find the source. Reason='{reason[:80]}'")
                    return False
                if quantity > 0 and position_qty > 0 and quantity > position_qty + 1:
                    logger.warning(f"⚠️ [QTY_CLAMP_TO_POS] {position_key}: clamping {action} qty {quantity} → {position_qty} (own={position_qty})")
                    quantity = position_qty
            else:
                return False
        elif action in ["AUGMENT", "REENTER", "REENTRY"]:
            if position:
                position_qty = abs(getattr(position, 'positionAmt', 0))
                quantity = override_qty if override_qty else await trade_manager.calculate_position_size(symbol, current_price, account_key=account_key)
            else:
                quantity = override_qty if override_qty else await trade_manager.calculate_position_size(symbol, current_price, account_key=account_key)
        else:
            return False
        if quantity <= 0:
            return False
        if config.SYMBOL_PERF_ENABLED and action in ('OPEN', 'AUGMENT', 'REENTRY', 'QUICK_OPEN', 'QUICK_AUGMENT'):
            try:
                from utils import get_performance_multiplier
                _perf_mult = get_performance_multiplier(symbol, default=1.0)
                quantity = max(1, int(quantity * (0.7 + _perf_mult * 0.3)))
            except Exception:
                pass
        max_order_value = config.MAX_ORDER_VALUE
        order_value = quantity * current_price
        if order_value > max_order_value:
            quantity = int(max_order_value / current_price)
            if config.VERBOSE: logger.debug(f"[queue_trade_action] {symbol} quantity adjusted to {quantity} to respect MAX_ORDER_VALUE={max_order_value}")
        if quantity <= 0:
            return False
        
        final_reason = reason
        if not is_recent_reentry and override_qty and override_qty > 0:
            if "_qty_" not in reason:
                final_reason = f"{reason}_qty_{override_qty}"
        order = {
            'account_key': account_key,
            'symbol': symbol,
            'position_side': position_side,
            'side': side,
            'quantity': quantity,
            'action': action, 
            'reason': final_reason,
            'current_price': current_price,
            'priority': conviction,
            'order_id': f"{position_key}_{action}_{int(time.time())}",
            'override_qty': override_qty   }
        success, msg = await order_queue.add_order(order)
        logger.info(f"[queue_trade_action] Queued {symbol} {action}: success={success}, msg={msg}")
        return "SUCCESS" 
    except Exception as e:
        logger.error(f"[queue_trade_action] Error: {e}",  exc_info=True )
        return False

async def process_symbols_periodically(order_queue: OrderQueue, trade_manager, account_key: str):
    current_account.set(account_key)
    logger.info(f"🚀 [TASK] Trading loop started for {account_key}")
    OPEN_INTERVAL = 30      # open positions re-evaluated every 30s
    CANDIDATE_INTERVAL = 90  # non-open candidates every 90s
    last_candidate_scan = 0.0

    while trade_manager.running:
        try:
            current_account.set(account_key)
            now = time.time()
            await trade_manager.reload_account_symbols()
            longs = getattr(trade_manager, f"symbols_long_{account_key}", [])
            shorts = getattr(trade_manager, f"symbols_short_{account_key}", [])

            # Open positions — always included every 30s
            open_keys = [
                pk for pk, pos in trade_manager.position_manager.positions.items()
                if pk.startswith(f"{account_key}:") and abs(getattr(pos, 'positionAmt', getattr(pos, 'quantity', 0))) > 0
            ]

            # Candidate keys (non-open) — every 90s only
            # tra is a CASH (non-margin) account → never queue SHORT candidates.
            # Without this guard the scan loop spent every cycle generating SHORT
            # entry signals on tra that the Tradier API would reject as
            # "shorting requires margin", churning state and decision logs.
            _tra_long_only = (account_key == 'tra' and getattr(config, 'TRA_LONG_ONLY', True))
            candidate_keys = []
            if now - last_candidate_scan >= CANDIDATE_INTERVAL:
                _short_keys = [] if _tra_long_only else [f"{account_key}:{s}_SHORT" for s in shorts]
                all_candidates = (
                    [f"{account_key}:{s}_LONG" for s in longs] +
                    _short_keys
                )
                candidate_keys = [k for k in all_candidates if k not in open_keys]
                last_candidate_scan = now
                _short_label = "(SUPPRESSED:cash_account)" if _tra_long_only else f"{len(shorts)}S"
                print(f"[SCAN] {account_key}: {len(longs)}L/{_short_label} syms, {len(open_keys)} open, {len(candidate_keys)} candidates, market={'OPEN' if is_regular_trading_hours() else 'CLOSED'}", flush=True)

            all_keys = open_keys + candidate_keys

            if not all_keys:
                if not longs and not shorts:
                    logger.warning(f"[{account_key}] ⚠️ SYMBOL LISTS EMPTY! Check your json lists or data feed.")
                await asyncio.sleep(OPEN_INTERVAL)
                continue

            logger.info(f"[{account_key}] 🔄 SCAN: {len(open_keys)} open (every 30s) + {len(candidate_keys)} candidates (every 90s) = {len(all_keys)} total")
            _results = await asyncio.gather(*[
                process_position(account_key, pk, order_queue, trade_manager, event_type="periodic_loop", force=True)
                for pk in all_keys
            ], return_exceptions=True)
            _errors = [r for r in _results if isinstance(r, Exception)]
            if _errors: print(f"[GATHER_ERRORS] {len(_errors)}/{len(all_keys)} failed: {_errors[0]}", flush=True)

            await asyncio.sleep(OPEN_INTERVAL)

        except Exception as e:
            logger.error(f"[{account_key}] 💥 CRASH in loop: {e}", exc_info=True)
            await asyncio.sleep(10)


async def trc_5m_sweep_loop(order_queue, trade_manager):
    """5-min portfolio sweep for trc. Ranks all trb symbol-list candidates by relative
    momentum (z-score 70% + raw delta vs SPY 30%) × news conviction, then fires
    process_position on the top N each side. Runs in parallel with the existing 30s/90s
    reactive loop which continues to manage open positions independently."""
    SWEEP_INTERVAL = 300
    while trade_manager.running:
        try:
            if not getattr(config, 'TRC_5M_SWEEP_ENABLED', True) or not is_regular_trading_hours():
                await asyncio.sleep(60)
                continue
            current_account.set('trc')
            TOP_N = getattr(config, 'TRC_5M_SWEEP_TOP_N', 8)
            Z_W = getattr(config, 'TRC_5M_SWEEP_Z_WEIGHT', 0.7)
            D_W = getattr(config, 'TRC_5M_SWEEP_DELTA_WEIGHT', 0.3)
            BENCHMARK = getattr(config, 'TRC_5M_SWEEP_BENCHMARK', 'SPY')
            spy_price, _ = await trade_manager.get_current_price(BENCHMARK)
            spy_prev = _trc_5m_price_prev.get(BENCHMARK)
            spy_ret = (spy_price / spy_prev - 1.0) if (spy_prev and spy_prev > 0 and spy_price) else 0.0
            if spy_price:
                _trc_5m_price_prev[BENCHMARK] = spy_price
            news_conv: Dict[str, float] = {}
            try:
                inj_file = config.BASE_PATH / "data" / "news_injections.json"
                if inj_file.exists():
                    with open(inj_file, 'r') as _f:
                        inj_data = json.load(_f)
                    for inj in inj_data.get('active', []):
                        sym = inj.get('symbol', '').upper()
                        side = inj.get('side', '')
                        conv = float(inj.get('conviction', 1))
                        if sym:
                            news_conv[sym] = conv if side == 'LONG' else -conv
            except Exception:
                pass
            longs = list(getattr(trade_manager, 'symbols_long_trb', []))
            shorts = list(getattr(trade_manager, 'symbols_short_trb', []))
            async def _score(symbol: str, is_long: bool) -> float:
                try:
                    price, _ = await trade_manager.get_current_price(symbol)
                    if not price or price <= 0:
                        return -999.0
                    prev = _trc_5m_price_prev.get(symbol)
                    ret = (price / prev - 1.0) if (prev and prev > 0) else 0.0
                    _trc_5m_price_prev[symbol] = price
                    buf = _trc_5m_return_buffer[symbol]
                    buf.append(ret)
                    rel = ret - spy_ret
                    if len(buf) >= 3:
                        arr = np.array(buf)
                        std = float(arr.std())
                        z = float((ret - arr.mean()) / std) if std > 1e-8 else 0.0
                    else:
                        z = 0.0
                    z_norm = float(np.clip(z, -3.0, 3.0)) / 3.0
                    rel_norm = float(np.clip(rel * 100.0, -3.0, 3.0)) / 3.0
                    raw = Z_W * z_norm + D_W * rel_norm
                    nc = news_conv.get(symbol, 0.0)
                    news_mult = 1.0 + (max(0.0, nc) if is_long else max(0.0, -nc)) * 0.2
                    score = raw * news_mult
                    return score if is_long else -score
                except Exception:
                    return -999.0
            scores_long = await asyncio.gather(*[_score(s, True) for s in longs], return_exceptions=True)
            scores_short = await asyncio.gather(*[_score(s, False) for s in shorts], return_exceptions=True)
            ranked_longs = sorted([(s, sc) for s, sc in zip(longs, scores_long) if isinstance(sc, float) and sc > -999], key=lambda x: x[1], reverse=True)
            ranked_shorts = sorted([(s, sc) for s, sc in zip(shorts, scores_short) if isinstance(sc, float) and sc > -999], key=lambda x: x[1], reverse=True)
            top_longs = [s for s, _ in ranked_longs[:TOP_N]]
            top_shorts = [s for s, _ in ranked_shorts[:TOP_N]]
            logger.info(f"[TRC_5M_SWEEP] spy_ret={spy_ret:.4%} | Top longs: {top_longs[:5]} | Top shorts: {top_shorts[:5]}")
            keys = [f"trc:{s}_LONG" for s in top_longs] + [f"trc:{s}_SHORT" for s in top_shorts]
            results = await asyncio.gather(*[
                process_position('trc', pk, order_queue, trade_manager, event_type="trc_5m_sweep", force=True)
                for pk in keys
            ], return_exceptions=True)
            errs = [r for r in results if isinstance(r, Exception)]
            if errs:
                print(f"[TRC_5M_SWEEP] {len(errs)}/{len(keys)} errors: {errs[0]}", flush=True)
            await asyncio.sleep(SWEEP_INTERVAL)
        except Exception as e:
            logger.error(f"[TRC_5M_SWEEP] Crash: {e}", exc_info=True)
            await asyncio.sleep(30)


async def continuous_queue_processor(order_queue: OrderQueue, trade_manager, account_key: str):
    current_account.set(account_key)
    logger.info(f"[continuous_queue_processor] Task started for {account_key}", extra={'account_key': account_key})
    
    while trade_manager.running:
        try:
            current_account.set(account_key)
            await asyncio.sleep(5)
            
            if account_key in trade_manager.account_position_queues:
                queue_data = trade_manager.account_position_queues[account_key]
                if queue_data['queue']:
                    position_keys = list(queue_data['queue'])[:10]
                    if position_keys:
                        await monitor_entries(order_queue, trade_manager, account_key, position_keys, event_type="queue_processor", is_priority_add=False, force=False)
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.error(f"[continuous_queue_processor] Error: {e}",  exc_info=True, extra={'account_key': account_key})
            await asyncio.sleep(5)

async def periodic_override_check(trade_manager, account_key):
    current_account.set(account_key)
    while getattr(trade_manager, "running", False):
        try:
            current_account.set(account_key)
            await asyncio.sleep(120)
            await trade_manager.load_leaderboards()
            positions = trade_manager.position_manager.get_positions_by_account(account_key)
            
            for position_key, position in positions.items():
                try:
                    position_amt = abs(float(getattr(position, 'positionAmt', 0)))
                    if position_amt <= 0: continue
                    
                    symbol = getattr(position, 'symbol', '')
                    if not symbol: continue
                    # Note: blacklisted symbols CAN be reduced (execute_trade_action allows REDUCE/CLOSE through)
                    current_price, ts = await trade_manager.get_current_price(symbol)
                    # if current_price <= 0: continue
                    
                    indicators = trade_manager.get_indicators(symbol)
                    
                    logger.debug(f"{symbol} Indicators Age: {indicators.get('timestamp')}")


                    if not indicators: continue
                    
                    i = trade_manager.strategy.parse_market_data(indicators)
                    position_value = position_amt * current_price
                    gain = calculate_gain(
                        getattr(position, 'position_side', 'LONG'),
                        current_price,
                        getattr(position, 'entry_price', current_price) )
                    market_context = await trade_manager.get_market_context(symbol)
                    market_bias = market_context.get('bias', 0.0)
                    is_long = getattr(position, 'position_side', 'LONG') == 'LONG'
                    
                    # 2026-04-27 user rule: 72h min hold gates the soft REDUCE/take-profit
                    # paths below. Stocks must not be flipped on bias/stoch noise inside the hold window.
                    _ovr_min_hold = float(getattr(config, 'TRADIER_MIN_HOLD_MINUTES', getattr(config, 'MIN_HOLD_MINUTES_TRADIER', 240.0)))
                    _ovr_hold_min: float = 0.0
                    try:
                        _ovr_opened_at = getattr(position, 'opened_at', None)
                        if _ovr_opened_at:
                            _ovr_hold_min = (datetime.now(timezone.utc) - _ovr_opened_at).total_seconds() / 60.0
                    except Exception:
                        _ovr_hold_min = 0.0
                    _ovr_min_hold_ok = _ovr_hold_min >= _ovr_min_hold
                    if (is_long and market_bias < -0.3) or (not is_long and market_bias > 0.3):
                        last_aug_ts = getattr(position, 'last_augmentation_time', None)
                        min_since_aug = (datetime.now(timezone.utc) - last_aug_ts).total_seconds() / 60.0 if last_aug_ts else 9999
                        if gain > 0 and gain < 0.08 and min_since_aug > 15:
                            if not _ovr_min_hold_ok:
                                logger.info(f"[MARKET_BIAS_MIN_HOLD_BLOCK] {position_key}: hold={_ovr_hold_min:.0f}m<{_ovr_min_hold:.0f}m — bias-reduce gated (user rule 2026-04-27)")
                            else:
                                reduce_qty = max(1, int(position_amt * 0.3))
                                reason = f"Market_Against_Position_Bias_{market_bias:.2f}_Loss_{gain:.1f}%_AugAge_{min_since_aug:.0f}m"
                                logger.info(f"[OVERRIDE_CHECK] {position_key}: Market against + loss {gain:.2f}% + {min_since_aug:.0f}m since aug → reducing 30%")
                                await queue_trade_action(trade_manager.order_queue, trade_manager, position_key, "REDUCE", reason, 75.0, override_qty=reduce_qty)

                    if is_long and i.get('stoch_k_5m', 50) > 85 and i.get('rsi_5m', 50) > 75 and i.get('stoch_k_5m', 50) < i.get('stoch_d_5m', 50) :
                        if gain > 2.0:
                            if not _ovr_min_hold_ok:
                                logger.info(f"[OVERBOUGHT_TP_MIN_HOLD_BLOCK] {position_key}: hold={_ovr_hold_min:.0f}m<{_ovr_min_hold:.0f}m — overbought-TP gated")
                            else:
                                reduce_qty = max(1, int(position_amt * 0.25))
                                reason = f"Overbought_Take_Profit_Gain_{gain:.1f}%"
                                logger.info(f"[OVERRIDE_CHECK] {position_key}: Overbought, taking profits")
                                await queue_trade_action(
                                    trade_manager.order_queue, trade_manager, position_key,
                                    "REDUCE", reason, 75.0, override_qty=reduce_qty    )

                    elif not is_long and i.get('stoch_k_5m', 50) < 15 and i.get('rsi_5m', 50) < 25 and i.get('stoch_k_5m', 50) > i.get('stoch_d_5m', 50) :
                        if gain > 2.0 and _ovr_min_hold_ok:
                            reduce_qty = max(1, int(position_amt * 0.25))
                            reason = f"Oversold_Take_Profit_Gain_{gain:.1f}%"
                            logger.info(f"[OVERRIDE_CHECK] {position_key}: Oversold, taking profits")
                            await queue_trade_action(
                                trade_manager.order_queue, trade_manager, position_key,
                                "REDUCE", reason, 75.0, override_qty=reduce_qty  )
                    
                except Exception as e:
                    logger.debug(f"[perio dic_override_check] Error processing {position_key}: {e}")
            
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.debug(f"[period ic_override_check] {e}")
            await asyncio.sleep(10)

async def monitor_stale_augmentations(trade_manager, account_key, order_queue):
    """Monitor positions that were augmented but have become stale (no update for 2 hours)"""
    current_account.set(account_key)
    while getattr(trade_manager, "running", False):
        try:
            current_account.set(account_key)
            await asyncio.sleep(180)

            now = datetime.now(timezone.utc)
            stale_positions = []

            # Check all positions
            positions = trade_manager.get_positions_by_account(account_key)

            for position_key, position in positions.items():               
                try:
                    # Skip if no position quantity
                    position_amt = abs(float(getattr(position, 'positionAmt', 0)))
                    if position_amt <= 0:
                        continue
                    
                    # Check last augmentation time
                    last_aug_time = getattr(position, 'last_augmentation_time', None)
                    if not last_aug_time:
                        continue
                    
                    # Parse timestamp
                    try:
                        if isinstance(last_aug_time, str):
                            last_aug_time = isoparse(last_aug_time)
                        
                        # Check if more than 2 hours since last augmentation
                        if (now - last_aug_time).total_seconds() > 7200:  # 2 hours
                            stale_positions.append(position_key)
                    except Exception:
                        continue
                        
                except Exception:
                    continue
            
            # Process stale positions
            if stale_positions:
                logger.info(f"[STALE_AUG] Found {len(stale_positions)} positions with stale augmentations")
                
                # Re-evaluate these positions
                await monitor_entries( order_queue, trade_manager, 'trb', stale_positions[:25],
                    event_type="stale_aug", is_priority_add=True, force=True )
            
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.debug(f"[moni tor_stale_augmentations] {e}")
            await asyncio.sleep(10)
                    
async def redis_listener_signals(trade_manager):
    while trade_manager.running:
        try:
            await asyncio.sleep(2)
            if not trade_manager.redis_manager: continue
            try:
                data = await trade_manager.redis_manager.get("tradier_signals")
                if data and isinstance(data, dict):
                    account_key = data.get('account_key', 'tra')
                    position_key = data.get('position_key', '')
                    if position_key:
                        from utils import clean_position_key, orjson_default
                        if position_key.count(":") > 1:
                            position_key = clean_position_key(position_key)
                        if account_key in trade_manager.account_position_queues:
                            queue_data = trade_manager.account_position_queues[account_key]
                            _, symbol_clean, position_side = parse_position_key(position_key)
                            if position_side == "SHORT" and symbol_clean.upper() in trade_manager.non_shortable_symbols:
                                continue
                            if position_key not in queue_data['set']:
                                queue_data['queue'].append(position_key)
                                queue_data['set'].add(position_key)
            except Exception:
                pass
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.debug(f"[redis_listener_signals] Error: {e}")
            await asyncio.sleep(5)

class PositionReader:
    def __init__(self, config):
        self.config = config
        self.redis_client = None
        self.positions: Dict[str, TradierPosition] = {} 
        self.ladder_levels = {}


    async def connect(self):
        """Fix: Added strict socket timeouts to prevent startup hang."""
        try:
            self.redis_client = redis.Redis( host=getattr(config, 'REDIS_HOST', 'localhost'),
                port=getattr(config, 'REDIS_PORT', 6379),
                decode_responses=False, socket_connect_timeout=1,  socket_timeout=1     )
            # Ping with timeout to ensure we don't hang
            await asyncio.wait_for(self.redis_client.ping(), timeout=1.0)
            return True
        except Exception:
            self.redis_client = None
            return False

    def _hydrate_position(self, pos_data: dict) -> Any:
        try:
            from dataclasses import fields

            from tradier_positions import TradierPosition
            valid_fields = {f.name for f in fields(TradierPosition)}
            clean_data = {k: v for k, v in pos_data.items() if k in valid_fields}
            if 'entry_price' not in clean_data:
                clean_data['entry_price'] = float(pos_data.get('avg_fill_price', 0.0) or 0.0)
            if 'positionAmt' not in clean_data:
                clean_data['positionAmt'] = float(pos_data.get('quantity', 0.0) or 0.0)
            if 'symbol' not in clean_data: clean_data['symbol'] = 'UNKNOWN'
            if 'position_side' not in clean_data: clean_data['position_side'] = 'LONG'
            if 'entry_price' not in clean_data: clean_data['entry_price'] = 0.0
            if 'positionAmt' not in clean_data: clean_data['positionAmt'] = 0.0
            return TradierPosition(**clean_data)
        except Exception as e:
            logger.error(f"[Reader] Hydration error: {e}")
            return None

    def set_stop_loss(self, symbol, position_side, stop_loss): pass
    async def save_all_positions(self):
        from dataclasses import asdict
        accounts_data: Dict[str, Dict[str, dict]] = {}
        for pk, pos in self.positions.items():
            amt = abs(float(getattr(pos, 'positionAmt', 0) or 0))
            if amt == 0:
                continue
            acc = pk.split(":")[0]
            if acc not in accounts_data:
                accounts_data[acc] = {"long": {}, "short": {}}
            side_key = "short" if pk.endswith("_SHORT") else "long"
            accounts_data[acc][side_key][pk] = pos.to_dict() if hasattr(pos, 'to_dict') else (asdict(pos) if hasattr(pos, '__dataclass_fields__') else pos.__dict__)
        for acc, sides in accounts_data.items():
            acc_conf = config.get_account_config(acc)
            acc_dir = acc_conf['account_dir'] if acc_conf and 'account_dir' in acc_conf else config.BASE_PATH / acc
            if not os.path.exists(acc_dir):
                os.makedirs(acc_dir, exist_ok=True)
            for side_name, positions_dict in sides.items():
                fpath = os.path.join(acc_dir, f"{side_name}_positions.json")
                clean = {}
                for pk_key, pk_data in positions_dict.items():
                    amt = 0
                    if isinstance(pk_data, dict):
                        amt = abs(float(pk_data.get('positionAmt', 0) or 0))
                    elif hasattr(pk_data, 'positionAmt'):
                        amt = abs(float(getattr(pk_data, 'positionAmt', 0) or 0))
                    if amt > 0:
                        clean[pk_key] = pk_data
                try:
                    async with aiofiles.open(fpath, "wb") as f:
                        await f.write(orjson.dumps(clean, option=orjson.OPT_INDENT_2, default=default_json_serializer))
                    logger.info(f"[SAVE] {acc}/{side_name}_positions.json: {len(clean)} active positions (memory=truth, no disk merge)")
                except Exception as e:
                    logger.error(f"[SAVE] Failed to write {fpath}: {e}")
    async def save_all(self): await self.save_all_positions()
    def deteriorate_max_gain(self, account_key, position): return getattr(position, 'max_gain', 0.0)
    def deteriorate_max_quantity(self, account_key, position, current_price): return getattr(position, 'max_quantity', 0.0)

    async def sync(self, account_keys: List[str]):
        if not self.redis_client:
            for acc in account_keys:
                await self._load_from_disk(acc)
            return
        for acc in account_keys:
            data_found = False
            try:
                key = f"tradier:positions:{acc}"
                data = await self.redis_client.get(key)
                if data:
                    wrapper = pickle.loads(data)
                    raw_pos_dict = wrapper.get('positions', {})
                    if raw_pos_dict:
                        data_found = True
                        redis_keys = set()
                        for pk, p_obj in raw_pos_dict.items():
                            if isinstance(p_obj, dict):
                                pos_instance = self._hydrate_position(p_obj)
                                if pos_instance:
                                    self.positions[pk] = pos_instance
                                    redis_keys.add(pk)
                            else:
                                self.positions[pk] = p_obj
                                redis_keys.add(pk)
                        for pk in list(self.positions.keys()):
                            if pk.startswith(f"{acc}:") and pk not in redis_keys:
                                existing = self.positions[pk]
                                amt = abs(float(getattr(existing, 'positionAmt', 0) or 0))
                                if amt > 0:
                                    existing.positionAmt = 0.0
                                    existing.gain = 0.0
                                    # 2026-04-29: reset cycle_peak_gain only (NOT max_gain — that decays separately for reentry logic)
                                    try: existing.cycle_peak_gain = 0.0
                                    except Exception: pass
            except Exception as e:
                logger.warning(f"[Reader] Redis sync failed for {acc}: {e}")
                self.redis_client = None
            if not data_found:
                await self._load_from_disk(acc)

    async def _load_from_disk(self, account_key: str):
        try:
            acc_conf = config.get_account_config(account_key)
            if acc_conf and 'account_dir' in acc_conf:
                acc_dir = acc_conf['account_dir']
            else:
                acc_dir = config.BASE_PATH / account_key

            if not os.path.exists(acc_dir):
                return

            files_to_read = [
                os.path.join(acc_dir, "long_positions.json"),
                os.path.join(acc_dir, "short_positions.json")
            ]

            for fpath in files_to_read:
                if not os.path.exists(fpath):
                    continue
                
                data = await load_json_safe(fpath)
                
                if not isinstance(data, dict):
                    continue

                for symbol, pos_data in data.items():
                    side = pos_data.get('position_side', 'LONG') 
                    if 'short' in fpath and side == 'LONG': side = 'SHORT'
                    
                    symbol_clean = pos_data.get('symbol', symbol).upper()
                    from utils import construct_position_key, orjson_default
                    pk = construct_position_key(account_key, symbol_clean, side)

                    try:
                        if 'symbol' not in pos_data: pos_data['symbol'] = symbol_clean
                        if 'position_side' not in pos_data: pos_data['position_side'] = side
                        
                        pos_instance = self._hydrate_position(pos_data)
                        if pos_instance:
                            self.positions[pk] = pos_instance
                    except Exception as e:
                        logger.error(f"[Reader] Failed to hydrate position {pk} from disk: {e}")

        except Exception as e:
            logger.error(f"[Reader] Disk Load failed for {account_key}: {e}")

    def get_position(self, position_key: str):
        return self.positions.get(position_key)

    def get_positions_by_account(self, account_key: str) -> Dict[str, Any]:
        return {k: v for k, v in self.positions.items() if k.startswith(f"{account_key}:")}

    async def close(self):
        if self.redis_client: await self.redis_client.aclose()

# ═══════════════════════════════════════════════════════════════
# CORRELATION MATRIX (cross-symbol return correlation for hedge selection)
# ═══════════════════════════════════════════════════════════════
class CorrelationMatrix:
    def __init__(self, symbols: List[str], lookback: int = 60):
        self.symbols = symbols
        self.lookback = lookback
        self.returns: Dict[str, List[float]] = {s: [] for s in symbols}
        self.corr_cache: Dict[Tuple[str, str], float] = {}
        self._dirty = True
    def update(self, symbol: str, ret: float):
        if symbol not in self.returns:
            self.returns[symbol] = []
        self.returns[symbol].append(ret)
        if len(self.returns[symbol]) > self.lookback:
            self.returns[symbol] = self.returns[symbol][-self.lookback:]
        self._dirty = True
    def recompute(self):
        if not self._dirty:
            return
        self.corr_cache.clear()
        sym_list = [s for s in self.symbols if len(self.returns.get(s, [])) >= 20]
        for i, s1 in enumerate(sym_list):
            for s2 in sym_list[i + 1:]:
                r1 = np.array(self.returns[s1][-self.lookback:])
                r2 = np.array(self.returns[s2][-self.lookback:])
                min_len = min(len(r1), len(r2))
                if min_len < 20:
                    continue
                r1, r2 = r1[-min_len:], r2[-min_len:]
                std1, std2 = np.std(r1), np.std(r2)
                if std1 < 1e-10 or std2 < 1e-10:
                    continue
                corr = float(np.corrcoef(r1, r2)[0, 1])
                self.corr_cache[(s1, s2)] = corr
                self.corr_cache[(s2, s1)] = corr
        self._dirty = False
    def get_correlation(self, s1: str, s2: str) -> float:
        if s1 == s2:
            return 1.0
        return self.corr_cache.get((s1, s2), 0.0)
    def get_most_negatively_correlated(self, symbol: str, exclude: set = None, top_n: int = 10) -> List[Tuple[str, float]]:
        self.recompute()
        candidates = []
        for s in self.symbols:
            if s == symbol or (exclude and s in exclude):
                continue
            candidates.append((s, self.get_correlation(symbol, s)))
        candidates.sort(key=lambda x: x[1])
        return candidates[:top_n]

# ═══════════════════════════════════════════════════════════════
# TRADIER HEDGE ENGINE (cross-symbol correlated/inverse hedging)
# Stocks CANNOT hold same-symbol opposite-side positions — uses cross-symbol hedges.
# ═══════════════════════════════════════════════════════════════
class TradierHedgeEngine:
    def __init__(self, corr_matrix: CorrelationMatrix, all_symbols: List[str]):
        self.corr_matrix = corr_matrix
        self.all_symbols = set(all_symbols)
        self.hedge_cooldowns: Dict[str, float] = {}
        self.HEDGE_COOLDOWN_SEC = 420  # 7 min
        self.active_hedges: Dict[str, Dict] = {}  # hedge_pk -> {hedge_for: original_pk, ...}
        self._hedge_counter = 0
    def _dynamic_ratio(self, loss_pct: float) -> float:
        loss = abs(loss_pct)
        if loss >= 5.0: return 1.5
        if loss >= 2.0: return 1.2
        if loss >= 1.0: return 1.0
        if loss >= 0.3: return 0.5
        return 0.3
    def find_hedge_candidates(self, losing_symbol: str, losing_side: str, current_prices: Dict[str, float], open_position_symbols_by_side: Dict[str, str], indicators_cache: Dict[str, Dict]) -> List[Tuple[str, float, str]]:
        target_side = "SHORT" if losing_side == "LONG" else "LONG"
        now = time.time()
        for k in list(self.hedge_cooldowns.keys()):
            if now - self.hedge_cooldowns[k] > self.HEDGE_COOLDOWN_SEC:
                del self.hedge_cooldowns[k]
        exclude = {losing_symbol}
        exclude.update(self.hedge_cooldowns.keys())
        for sym, side in open_position_symbols_by_side.items():
            if side == target_side:
                exclude.add(sym)
        candidates = []
        neg_corr = self.corr_matrix.get_most_negatively_correlated(losing_symbol, exclude=exclude, top_n=15)
        for sym, corr in neg_corr:
            if sym in exclude or sym not in current_prices or current_prices[sym] <= 0:
                continue
            neg_strength = max(0, -corr)
            ind = indicators_cache.get(sym, {})
            stoch_k = float(ind.get('stoch_k_1h', 50))
            stoch_score = 0
            if target_side == "LONG":
                if stoch_k < 25: stoch_score += 5
                if stoch_k < 15: stoch_score += 5
            else:
                if stoch_k > 75: stoch_score += 5
                if stoch_k > 85: stoch_score += 5
            combined = neg_strength * 0.4 + min(stoch_score / 10.0, 1.0) * 0.4 + 0.2
            if combined > 0.2:
                candidates.append((sym, combined, target_side))
        sector = get_sector(losing_symbol)
        for fb_sym in SECTOR_HEDGE_FALLBACK.get(sector, ["SPY"]):
            if fb_sym in exclude or fb_sym not in current_prices:
                continue
            candidates.append((fb_sym, 0.3 * 0.8, target_side))
        for broad in ["SPY", "QQQ"]:
            if broad not in exclude and broad in current_prices and not any(c[0] == broad for c in candidates):
                candidates.append((broad, 0.15, target_side))
        candidates.sort(key=lambda x: x[1], reverse=True)
        return candidates[:5]
    def should_trigger_hedge(self, pnl_pct: float, position_side: str, stoch_k_1h: float, stoch_d_1h: float, zone: str = "") -> Tuple[bool, str]:
        # RZ zone awareness: if zone confirms the HEDGE direction, trigger earlier
        _zone_confirms = (position_side == "LONG" and zone == "TOP") or (position_side == "SHORT" and zone == "BOTTOM")
        _loss_threshold = -0.15 if _zone_confirms else -0.3
        if pnl_pct >= _loss_threshold:
            return False, ""
        if position_side == "LONG" and stoch_k_1h < stoch_d_1h and pnl_pct < _loss_threshold:
            return True, f"LOSS_{pnl_pct:.2f}%_STOCH_BEARISH{f'_RZ_{zone}' if zone else ''}"
        if position_side == "SHORT" and stoch_k_1h > stoch_d_1h and pnl_pct < _loss_threshold:
            return True, f"LOSS_{pnl_pct:.2f}%_STOCH_BULLISH{f'_RZ_{zone}' if zone else ''}"
        if pnl_pct < -1.0:
            return True, f"DEEP_LOSS_{pnl_pct:.2f}%_FORCED"
        return False, ""
    def compute_hedge_size(self, losing_value: float, loss_pct: float, max_notional: float = 50000) -> float:
        return min(losing_value * self._dynamic_ratio(loss_pct), max_notional)
    def should_exit_hedge(self, hedge_pnl_pct: float, original_pnl_pct: float, hedge_max_gain: float) -> Tuple[bool, str]:
        if original_pnl_pct >= 0.05:
            return True, f"ORIGINAL_RECOVERED_{original_pnl_pct:.2f}%"
        if hedge_pnl_pct < -0.15:
            return True, f"HEDGE_LIABILITY_KILL_{hedge_pnl_pct:.2f}%"
        mtc = _minutes_to_close()
        if 0 < mtc <= 15:
            return True, f"MARKET_CLOSE_FORCED_{mtc:.0f}min"
        if hedge_max_gain > 0.3 and (hedge_max_gain - hedge_pnl_pct) > 0.2:
            return True, f"PEAK_DECAY_{hedge_max_gain:.2f}%_to_{hedge_pnl_pct:.2f}%"
        return False, ""
    def create_hedge_id(self) -> str:
        self._hedge_counter += 1
        return f"TH_{self._hedge_counter}"

# ═══════════════════════════════════════════════════════════════
# STRICT NO-LOSS EXIT POLICY (NEVER close at a loss — only breakeven or profit)
# ═══════════════════════════════════════════════════════════════
class StrictNoLossPolicy:
    MAX_HOLD_DAYS = 999
    MAX_LOSS_FORCED_EXIT = -99.0
    @staticmethod
    def can_close(pnl_pct: float, hold_days: float = 0) -> Tuple[bool, str]:
        if pnl_pct >= 0.05:
            return True, f"PROFIT_{pnl_pct:.2f}%"
        if hold_days > StrictNoLossPolicy.MAX_HOLD_DAYS and pnl_pct < StrictNoLossPolicy.MAX_LOSS_FORCED_EXIT:
            return True, f"TIME_EXIT_{hold_days:.0f}d_loss_{pnl_pct:.2f}%"
        return False, f"STRICT_NO_LOSS_BLOCK_{pnl_pct:.2f}%"
    @staticmethod
    def should_take_profit(pnl_pct: float, max_gain_pct: float) -> Tuple[bool, str]:
        if max_gain_pct > 1.5 and pnl_pct < max_gain_pct * 0.5:
            return True, f"TRAILING_STOP_{pnl_pct:.2f}%_from_peak_{max_gain_pct:.2f}%"
        if pnl_pct > 3.0:
            return True, f"TAKE_PROFIT_{pnl_pct:.2f}%"
        return False, ""

class EntryCircuitBreaker:
    """Smart entry quality monitor. Records WT+DC state at entry.
    Losing position → check if entry was WRONG (WT/DC against) vs market event (correct entry).
    Bad entry → disable function. Market event → hold until HTF WT exhausts."""
    def __init__(self, grace_minutes=160, collapse_pct=-1.0):
        self.grace_minutes = grace_minutes
        self.collapse_pct = collapse_pct
        self._entries = {}  # {pk: {ts, price, tag, is_long, wt_fav, dc_buy, reason}}
        self._bad_entries = []
        self._disabled_paths = set()
    def record_entry(self, pk, price, reason, indicators=None):
        tag = reason.split('_')[0] if reason else 'UNKNOWN'
        is_long = 'LONG' in pk
        wt_fav = {}; dc_buy = {}
        if indicators:
            for tf in ['5m', '15m', '1h', '4h', 'D']:
                wt1 = float(indicators.get(f'wt1_{tf}', 0) or 0); wt2 = float(indicators.get(f'wt2_{tf}', 0) or 0)
                wt_fav[tf] = (wt1 > wt2) if is_long else (wt1 < wt2)
                dc_pos = float(indicators.get(f'dc_position_{tf}', 0.5) or 0.5)
                dc_buy[tf] = (dc_pos < 0.3) if is_long else (dc_pos > 0.7)
        self._entries[pk] = {'ts': time.time(), 'price': price, 'tag': tag, 'is_long': is_long, 'wt_fav': wt_fav, 'dc_buy': dc_buy, 'reason': reason[:80]}
    def check_positions(self, positions):
        for pk, entry in list(self._entries.items()):
            pos = positions.get(pk)
            if not pos:
                self._entries.pop(pk, None); continue
            gain = float(getattr(pos, 'gain', 0) or 0)
            if gain >= 0:
                continue
            wt_against = sum(1 for v in entry['wt_fav'].values() if not v)
            dc_wrong = sum(1 for v in entry['dc_buy'].values() if not v)
            bad_entry = wt_against >= 3 or (wt_against >= 2 and dc_wrong >= 3)
            if bad_entry:
                tag = entry['tag']; age = (time.time() - entry['ts']) / 60
                self._bad_entries.append((time.time(), tag, pk))
                logger.critical(f"🛑 [CIRCUIT_BREAKER] {pk}: BAD ENTRY g={gain:.2f}% age={age:.0f}m wt_against={wt_against}/5 dc_wrong={dc_wrong}/5 tag={tag}")
                recent_bad = [b for b in self._bad_entries[-10:] if b[1] == tag]
                if len(recent_bad) >= 3 and tag not in self._disabled_paths:
                    self._disabled_paths.add(tag)
                    logger.critical(f"🛑 [CIRCUIT_BREAKER] AUTO-DISABLED '{tag}' — {len(recent_bad)} bad entries in last 10")
                self._entries.pop(pk, None)
    def is_blocked(self, reason):
        tag = reason.split('_')[0] if reason else 'UNKNOWN'
        return tag in self._disabled_paths

class StockStrategy:
    def __init__(self, config, trade_manager=None):
        self.config = config # Max for Re-entry/High Conviction
        self.trade_manager = trade_manager
        self.non_shortable_symbols = config.NON_SHORTABLE
        self.exceptions = self.trade_manager.exceptions
        self.limit_normal_order = self.trade_manager.limit_normal_order
        self.limit_exception_order = self.trade_manager.limit_exception_order
        self.limit_total_pos = self.trade_manager.limit_total_pos
        self._cvar_cache: dict = {}  # symbol -> (cvar_pct, computed_at_ts)
        self.circuit_breaker = EntryCircuitBreaker(grace_minutes=float(getattr(config, 'MIN_HOLD_BARS_TRADIER', 32)) * 5, collapse_pct=-1.0)

    # ------------------------------------------------------------------
    # DC BREAKOUT RETEST SCORE (BACKTEST_CHANGE_MT4)
    # For each TF: was dc_high recently broken (expanded)? Is price retesting the old level?
    # Breakout retest = BUY (support from above). Rejection at dc_high = SELL.
    # Returns (long_score, short_score, detail_str) where score = sum of TF signals.
    # ------------------------------------------------------------------
    def compute_dc_retest_score(self, price: float, ind: dict) -> tuple:
        long_score = 0
        short_score = 0
        details = []
        for tf in ['5m', '15m', '1h', '4h', 'D']:
            dc_hi = float(ind.get(f'dc_high_{tf}', 0) or 0)
            dc_hi_ant = float(ind.get(f'dc_high_{tf}_ant', 0) or 0)
            dc_lo = float(ind.get(f'dc_low_{tf}', 0) or 0)
            dc_lo_ant = float(ind.get(f'dc_low_{tf}_ant', 0) or 0)
            if dc_hi <= 0 or dc_hi_ant <= 0 or dc_lo <= 0 or dc_lo_ant <= 0:
                continue
            dc_range = dc_hi - dc_lo
            if dc_range <= 0:
                continue
            # LONG: dc_high expanded (breakout up) AND price pulling back toward old level
            if dc_hi > dc_hi_ant:
                expansion_pct = (dc_hi - dc_hi_ant) / dc_hi_ant * 100
                if expansion_pct > 0.1:
                    dist_to_retest = (price - dc_hi_ant) / dc_hi_ant * 100
                    if 0 < dist_to_retest < 1.5:
                        long_score += 1
                        details.append(f"{tf}:retest_hi({dist_to_retest:.1f}%)")
                    elif dist_to_retest <= 0:
                        short_score += 1
                        details.append(f"{tf}:failed_hi")
            # SHORT: dc_low expanded down (breakout down) AND price bouncing back toward old level
            if dc_lo < dc_lo_ant:
                expansion_pct = (dc_lo_ant - dc_lo) / dc_lo_ant * 100
                if expansion_pct > 0.1:
                    dist_to_retest = (dc_lo_ant - price) / dc_lo_ant * 100
                    if 0 < dist_to_retest < 1.5:
                        short_score += 1
                        details.append(f"{tf}:retest_lo({dist_to_retest:.1f}%)")
                    elif dist_to_retest <= 0:
                        long_score += 1
                        details.append(f"{tf}:failed_lo")
        return long_score, short_score, "|".join(details) if details else "no_retest"

    # ------------------------------------------------------------------
    # CVaR 5% PRE-ENTRY FILTER (Quantitativo research)
    # Computes Conditional Value at Risk from daily klines.
    # Returns the average of the worst 5% of daily returns (as a %).
    # e.g. -3.9% for AAPL = "on a bad day it loses ~3.9% on average"
    # Filter threshold: -6.0% (skip stocks that are -6% or worse in tail risk)
    # Cache TTL: 4 hours (stale but not wrong — CVaR doesn't change intraday)
    # ------------------------------------------------------------------
    def _compute_cvar_5pct(self, symbol: str) -> float:
        """Read daily klines and return CVaR 5% as a negative percentage, e.g. -3.9."""
        try:
            import json as _json
            import time as _time
            from pathlib import Path as _Path
            klines_path = self.config.KLINES_CACHE_DIR / f"{symbol}_D.json"
            if not klines_path.exists():
                return -999.0  # No data → treat as unsafe (skip)
            data = _json.loads(klines_path.read_text())
            bars = data if isinstance(data, list) else data.get('bars', [])
            closes = []
            for b in bars:
                c = b.get('close') or b.get('c')
                if c: closes.append(float(c))
            if len(closes) < 60:
                return -999.0  # Need at least 60 days of history
            returns = [(closes[i] - closes[i-1]) / closes[i-1] for i in range(1, len(closes))]
            returns.sort()
            n5 = max(1, int(0.05 * len(returns)))
            cvar = sum(returns[:n5]) / n5 * 100.0
            return round(cvar, 2)
        except Exception:
            return -999.0

    def _is_cvar_safe(self, symbol: str, threshold: float = -6.0) -> tuple:
        """
        Returns (is_safe, cvar_value) — safe means CVaR > threshold.
        Caches for 4 hours (TTL=14400s). Missing data treated as unsafe.
        """
        import time as _time
        now = _time.time()
        TTL = 14400.0  # 4 hours
        if symbol in self._cvar_cache:
            cvar, ts = self._cvar_cache[symbol]
            if now - ts < TTL:
                return (cvar > threshold), cvar
        cvar = self._compute_cvar_5pct(symbol)
        self._cvar_cache[symbol] = (cvar, now)
        return (cvar > threshold), cvar

    def _sf(self, val, default=0.0):
        try:
            return float(val) if val is not None else default
        except (ValueError, TypeError):
            return default

    def _minutes_since(self, dt_val):
        if not dt_val: return 9999.0
        try:
            if isinstance(dt_val, str):
                dt_val = isoparse(dt_val)
            if dt_val.tzinfo is None:
                dt_val = dt_val.replace(tzinfo=timezone.utc)
            return (datetime.now(timezone.utc) - dt_val).total_seconds() / 60.0
        except Exception:  return 9999.0

    def is_same_direction(self, side: str, position_side: str) -> bool:
        """Check if order side matches position direction"""
        if position_side == "LONG":
            return side.upper() == "BUY"
        elif position_side == "SHORT":
            return side.upper() in ["SELL", "SELL_SHORT"]
        return False

    def calculate_signal_score(self, account_key: str, symbol: str, is_long: bool, current_price: float, i: dict, position: Any, is_exit: bool = False) -> tuple[float, str, str]:
        now_ts = time.time()
        score = 0.0
        reasons =[]
        longs = getattr(self.trade_manager, f"symbols_long_{account_key}",[])
        shorts = getattr(self.trade_manager, f"symbols_short_{account_key}",[])
        has_position = position is not None and abs(float(getattr(position, 'positionAmt', 0))) > 0
        if not is_exit:
            if not self.trade_manager.is_symbol_tradeable(symbol, account_key, 'LONG' if is_long else 'SHORT'):
                # logger.warning(f"[{account_key}] ⚠️⚠️ SYMBOL {symbol} NOT TRADEABLE (Not in Discovery List)")
                return 0.0, "WAIT", "SYMBOL NOT TRADEABLE"
        
        if not is_regular_trading_hours():
            return 0.0, "WAIT", "Outside Market Hours"
        def g(k, default=0.0): return float(i.get(k, default))
        # === BACKTEST-VALIDATED GATES (121 stocks, 60/40 train/test, STRICT_NO_LOSS) ===
        # These blocks are confirmed negative on BOTH train AND test sets
        if not is_exit and getattr(config, 'BACKTEST_VALIDATED_GATES_TRADIER', True):
            _atr_1h = g('atr_1h', 0); _atr_4h = g('atr_4h', 0)
            _mfi_4h = g('mfi_4h', 50); _wt1_1h = g('wt1_1h', 0); _wt1_4h = g('wt1_4h', 0)
            _k_4h = g('stoch_k_4h', 50)
            _dc_w_1h = g('dc_width_1h', 10); _dc_w_4h = g('dc_width_4h', 10)
            _dc_pos_4h = g('dc_position_4h', 0.5)
            _wt_vel_1h = g('wt_velocity_1h', 0)
            # BLOCK: High ATR = high volatility = losses (TRAIN -0.19%, TEST worst)
            if _atr_4h > 0 and current_price > 0:
                _atr_pct_4h = _atr_4h / current_price * 100
                if _atr_pct_4h > 3.0: return 0.0, "WAIT", f"BV_ATR_HIGH_4h({_atr_pct_4h:.2f}%)"
            if _atr_1h > 0 and current_price > 0:
                _atr_pct_1h = _atr_1h / current_price * 100
                if _atr_pct_1h > 2.5: return 0.0, "WAIT", f"BV_ATR_HIGH_1h({_atr_pct_1h:.2f}%)"
            # BLOCK: MFI > 90 = extreme overbought money flow (TRAIN -0.17%, TEST -0.24%)
            if is_long and _mfi_4h > 85: return 0.0, "WAIT", f"BV_MFI_OB({_mfi_4h:.0f})"
            if not is_long and _mfi_4h < 15: return 0.0, "WAIT", f"BV_MFI_OS({_mfi_4h:.0f})"
            # BLOCK: WT1 extreme (TRAIN -0.15%, TEST -0.24%)
            if is_long and (_wt1_1h > 60 or _wt1_4h > 60): return 0.0, "WAIT", f"BV_WT_OB(1h={_wt1_1h:.0f},4h={_wt1_4h:.0f})"
            if not is_long and (_wt1_1h < -60 or _wt1_4h < -60): return 0.0, "WAIT", f"BV_WT_OS(1h={_wt1_1h:.0f},4h={_wt1_4h:.0f})"
            # BLOCK: Stoch K_4h extreme overbought for longs (TEST -0.25%)
            if is_long and _k_4h > 90: return 0.0, "WAIT", f"BV_K4H_OB({_k_4h:.0f})"
            if not is_long and _k_4h < 10: return 0.0, "WAIT", f"BV_K4H_OS({_k_4h:.0f})"
            # BLOCK: DC width squeeze (TRAIN worst, TEST -0.33%)
            if _dc_w_4h < 1.5 or _dc_w_1h < 1.0: return 0.0, "WAIT", f"BV_DC_SQUEEZE(1h={_dc_w_1h:.1f},4h={_dc_w_4h:.1f})"
            # BLOCK: DC position top (TRAIN -0.16%, TEST worst)
            if is_long and _dc_pos_4h > 0.9: return 0.0, "WAIT", f"BV_DC_TOP({_dc_pos_4h:.2f})"
            if not is_long and _dc_pos_4h < 0.1: return 0.0, "WAIT", f"BV_DC_BOT({_dc_pos_4h:.2f})"
            # BLOCK: WT velocity strongly negative for longs (TEST -0.24%)
            if is_long and _wt_vel_1h < -5: return 0.0, "WAIT", f"BV_WT_VEL_DN({_wt_vel_1h:.1f})"
            if not is_long and _wt_vel_1h > 5: return 0.0, "WAIT", f"BV_WT_VEL_UP({_wt_vel_1h:.1f})"
            # FIX 2026-05-08: wire CT_WT_VELOCITY_GATE_ENABLED (previously only in ez_manage/crypto).
            # Default OFF for tradier (not yet validated for stocks). Sweep vel_gate_on to test.
            if getattr(config, 'CT_WT_VELOCITY_GATE_ENABLED', False):
                _vel_min = float(getattr(config, 'CT_WT_VELOCITY_1H_MIN', 0.0))
                if is_long and _wt_vel_1h < _vel_min: return 0.0, "WAIT", f"CT_VEL_LONG({_wt_vel_1h:.1f}<{_vel_min})"
                if not is_long and _wt_vel_1h > -_vel_min: return 0.0, "WAIT", f"CT_VEL_SHORT({_wt_vel_1h:.1f}>-{_vel_min})"
        k_1m, d_1m, k_1m_prev = g('stoch_k_1m', 50), g('stoch_d_1m', 50), g('stoch_k_1m_prev', 50)
        k_5m, d_5m, k_5m_prev = g('stoch_k_5m', 50), g('stoch_d_5m', 50), g('stoch_k_5m_prev', 50)
        k_15m, d_15m = g('stoch_k_15m', 50), g('stoch_d_15m', 50)
        k_1h, d_1h = g('stoch_k_1h', 50), g('stoch_d_1h', 50)
        k_4h, d_4h = g('stoch_k_4h', 50), g('stoch_d_4h', 50)
        k_D, d_D = g('stoch_k_D', 50), g('stoch_d_D', 50)

        dc_high_15m, dc_low_15m = g('dc_high_15m'), g('dc_low_15m')
        dc_high_1h, dc_low_1h = g('dc_high_1h'), g('dc_low_1h')
        dc_high_4h, dc_low_4h = g('dc_high_4h'), g('dc_low_4h')
        dc_high_D, dc_low_D = g('dc_high_D'), g('dc_low_D')

        dc_high_15m_ant, dc_low_15m_ant = g('dc_high_15m_ant', dc_high_15m), g('dc_low_15m_ant', dc_low_15m)
        dc_high_1h_ant, dc_low_1h_ant = g('dc_high_1h_ant', dc_high_1h), g('dc_low_1h_ant', dc_low_1h)
        dc_high_4h_ant, dc_low_4h_ant = g('dc_high_4h_ant', dc_high_4h), g('dc_low_4h_ant', dc_low_4h)
        dc_high_D_ant, dc_low_D_ant = g('dc_high_D_ant', dc_high_D), g('dc_low_D_ant', dc_low_D)
        m1_ts_raw = i.get('timestamp_1m')
        m1_ts = now_ts
        try:
            if isinstance(m1_ts_raw, str):
                from dateutil.parser import isoparse
                dt = isoparse(m1_ts_raw)
                from datetime import timezone
                if dt.tzinfo is None: dt = dt.replace(tzinfo=timezone.utc)
                m1_ts = dt.timestamp()
            elif hasattr(m1_ts_raw, 'timestamp'):
                m1_ts = m1_ts_raw.timestamp()
            elif m1_ts_raw is not None:
                m1_ts = float(m1_ts_raw)
        except Exception:
            pass
        true_lag = now_ts - m1_ts
        m1_is_fresh = (true_lag < 60.0) 
        if true_lag <= 0: true_lag = 999
        k_1mco = k_1m > d_1m and k_1m_prev <= d_1m and k_1m < 30 and m1_is_fresh
        k_1mcu = k_1m < d_1m and k_1m_prev >= d_1m and k_1m > 70 and m1_is_fresh
        k_5mco = k_5m > d_5m and k_5m_prev <= d_5m and k_5m < 35 and m1_is_fresh
        k_5mcu = k_5m < d_5m and k_5m_prev >= d_5m and k_5m > 65 and m1_is_fresh

        # --- Multi-TF DC Breakout Entry Gate ---
        # Break of DC low → SHORT; break of DC high → LONG
        # Higher TF = bigger size tier. Tight stops because stoch is typically extended.
        # Crossunder/crossover flags fire on the bar where price crosses the channel boundary.
        # Crossunder/crossover: fires only on the exact bar of the break
        dc_low_x_5m  = bool(i.get('dc_low_crossunder_5m',  False))
        dc_low_x_15m = bool(i.get('dc_low_crossunder_15m', False))
        dc_low_x_1h  = bool(i.get('dc_low_crossunder_1h',  False))
        dc_low_x_4h  = bool(i.get('dc_low_crossunder_4h',  False))
        dc_high_x_5m  = bool(i.get('dc_high_crossover_5m',  False))
        dc_high_x_15m = bool(i.get('dc_high_crossover_15m', False))
        dc_high_x_1h  = bool(i.get('dc_high_crossover_1h',  False))
        dc_high_x_4h  = bool(i.get('dc_high_crossover_4h',  False))

        # Current-state: price IS below/above the channel right now (breakout already in progress)
        # Use the tightest (5m) as baseline; higher TFs only if price is clearly outside them
        _dcl5  = g('dc_low_5m');  _dcl15 = g('dc_low_15m');  _dcl1h = g('dc_low_1h');  _dcl4h = g('dc_low_4h')
        _dch5  = g('dc_high_5m'); _dch15 = g('dc_high_15m'); _dch1h = g('dc_high_1h'); _dch4h = g('dc_high_4h')
        # Require at least 0.1% below/above channel to avoid noise at the boundary
        _min_buf = 0.0005  # Marathon winner: buffer=0.0005
        dc_below_5m  = current_price > 0 and _dcl5  > 0 and current_price < _dcl5  * (1 - _min_buf)
        dc_below_15m = current_price > 0 and _dcl15 > 0 and current_price < _dcl15 * (1 - _min_buf)
        dc_below_1h  = current_price > 0 and _dcl1h > 0 and current_price < _dcl1h * (1 - _min_buf)
        dc_below_4h  = current_price > 0 and _dcl4h > 0 and current_price < _dcl4h * (1 - _min_buf)
        dc_above_5m  = current_price > 0 and _dch5  > 0 and current_price > _dch5  * (1 + _min_buf)
        dc_above_15m = current_price > 0 and _dch15 > 0 and current_price > _dch15 * (1 + _min_buf)
        dc_above_1h  = current_price > 0 and _dch1h > 0 and current_price > _dch1h * (1 + _min_buf)
        dc_above_4h  = current_price > 0 and _dch4h > 0 and current_price > _dch4h * (1 + _min_buf)

        # Merge: triggered = crossunder THIS bar OR currently below (already broken)
        dc_low_x_5m  = dc_low_x_5m  or dc_below_5m
        dc_low_x_15m = dc_low_x_15m or dc_below_15m
        dc_low_x_1h  = dc_low_x_1h  or dc_below_1h
        dc_low_x_4h  = dc_low_x_4h  or dc_below_4h
        dc_high_x_5m  = dc_high_x_5m  or dc_above_5m
        dc_high_x_15m = dc_high_x_15m or dc_above_15m
        dc_high_x_1h  = dc_high_x_1h  or dc_above_1h
        dc_high_x_4h  = dc_high_x_4h  or dc_above_4h

        # === MTS GATE — unified multi-TF K+WT scoring (ablation: Sharpe 5.91 with D_dom weights) ===
        _mts = analyze_multi_tf_state_tradier(i, is_long, current_price, self.config)
        _mts_bottom = _mts['bottom_score']; _mts_eq = _mts['entry_quality']; _mts_dir = _mts['direction']; _mts_gp = _mts['gain_potential']; _mts_sz = _mts['size_multiplier']
        # Stocks: WT entry always wins, MTS as score bonus only (real test: MTS gate over-filters stocks)
        # LONG: NEW_WT_NO_MTS = Sharpe 6.12 vs OLD 5.59 (+9%). MTS b10 = 5.08 (hurts).
        # SHORT: NEW_WT_NO_MTS = Sharpe 4.32 vs OLD 3.81 (+13%). MTS gate hurts both sides.
        # Keep MTS for SCORE BONUSES only, not as a hard gate for stocks.
        _mts_gate = getattr(self.config, 'MTS_GATE_ENABLED_TRADIER', False)  # OFF by default for stocks
        if not is_exit and _mts_gate:
            _mts_bmin = getattr(self.config, 'MTS_BOTTOM_MIN_TRADIER', 5.0)
            _mts_eqmin = getattr(self.config, 'MTS_ENTRY_QUALITY_MIN_TRADIER', 0.0)
            if _mts_bottom < _mts_bmin or _mts_eq < _mts_eqmin:
                return 0.0, "WAIT", f"MTS_GATE_T(b={_mts_bottom:.0f}<{_mts_bmin},eq={_mts_eq:.0f}<{_mts_eqmin})"
        if not is_exit:
            if _mts_bottom > 40: score += 8; reasons.append(f"MTS_STRONG({_mts_bottom:.0f},+8)")
            elif _mts_bottom > 25: score += 4; reasons.append(f"MTS_BOTTOM({_mts_bottom:.0f},+4)")
            if _mts_eq > 40: score += 5; reasons.append(f"MTS_EQ({_mts_eq:.0f},+5)")
            elif _mts_eq > 25: score += 2; reasons.append(f"MTS_EQ_OK({_mts_eq:.0f},+2)")
        # Stoch-based entry gate for HODL system (DC breakout moved to DaytradeWing)
        _side = "LONG" if is_long else "SHORT"
        _stoch_zone_long = _cfg('STOCH_ENTRY_LONG_TRADIER', 35, account_key, symbol, _side)
        _stoch_extreme_long = _cfg('STOCH_EXTREME_LONG_TRADIER', 20, account_key, symbol, _side)
        _stoch_zone_short = _cfg('STOCH_ENTRY_SHORT_TRADIER', 65, account_key, symbol, _side)
        _stoch_extreme_short = _cfg('STOCH_EXTREME_SHORT_TRADIER', 80, account_key, symbol, _side)
        stoch_long  = (k_5m < _stoch_zone_long and k_5m > d_5m) or (k_5m < _stoch_extreme_long) or k_5mco
        stoch_short = (k_5m > _stoch_zone_short and k_5m < d_5m) or (k_5m > _stoch_extreme_short) or k_5mcu
        # K-zone entry: K in zone + turning (BACKTEST_CHANGE_T52)
        _kz_en = getattr(self.config, 'K_ZONE_ENTRY_ENABLED_TRADIER', True)
        kzone_long = _kz_en and k_4h < _cfg('K_ZONE_LONG_THRESHOLD_TRADIER', 35, account_key, symbol, _side) and k_4h > d_4h
        kzone_short = _kz_en and k_4h > _cfg('K_ZONE_SHORT_THRESHOLD_TRADIER', 65, account_key, symbol, _side) and k_4h < d_4h
        # MFI mean-reversion entry (BACKTEST_CHANGE_MT2 — MFI ONLY, no RSI fallback. RSI is bullshit.)
        _entry_tf = getattr(self.config, 'ENTRY_PRIMARY_TF', '4h')
        _trigger_tf = getattr(self.config, 'ENTRY_TRIGGER_TF', '15m')  # Trigger TF: referenced for sweep visibility; currently MFI uses primary TF
        _flow_val = g(f'mfi_{_entry_tf}', 50)
        rsi_long = _flow_val < getattr(self.config, 'RSI_ENTRY_LONG_TRADIER', 42)
        rsi_short = _flow_val > getattr(self.config, 'RSI_ENTRY_SHORT_TRADIER', 58)
        # HODL entry: DC position is PRIMARY (Sharpe 18.57 on 61 test stocks) + stoch/K-zone/MFI as confirmation
        # DC position < 0.25 = bottom quarter of channel = mean-reversion LONG setup
        _dc_pos_1h = g('dc_position_1h', 0.5)
        _dc_pos_4h = g('dc_position_4h', 0.5)
        _dc_entry_th = getattr(self.config, 'DC_POSITION_ENTRY_THRESHOLD', 0.25)
        dc_entry_long = _dc_pos_1h < _dc_entry_th or _dc_pos_4h < _dc_entry_th
        dc_entry_short = _dc_pos_1h > (1.0 - _dc_entry_th) or _dc_pos_4h > (1.0 - _dc_entry_th)
        # T65: DC removed from HODL entry gate (28.6% WR, PF 0.65 on stocks). DC stays as scoring BONUS only.
        if is_long:
            good_entry = stoch_long or kzone_long or rsi_long
        else:
            good_entry = stoch_short or kzone_short or rsi_short
        if not good_entry:
            return 0.0, "WAIT", f"NO_{'LONG' if is_long else 'SHORT'}_SETUP"
        if dc_entry_long or dc_entry_short:
            score += 5; reasons.append(f"DC_POS_BONUS({'L' if is_long else 'S'}_1h={_dc_pos_1h:.2f}_4h={_dc_pos_4h:.2f})")
        # MT4: DC breakout-retest bonus (broken level = support/resistance)
        _dc_rt_long, _dc_rt_short, _dc_rt_detail = self.compute_dc_retest_score(current_price, i)
        if is_long and _dc_rt_long >= 2:
            score += 6; reasons.append(f"DC_RETEST_L({_dc_rt_long}TF:{_dc_rt_detail})")
        elif not is_long and _dc_rt_short >= 2:
            score += 6; reasons.append(f"DC_RETEST_S({_dc_rt_short}TF:{_dc_rt_detail})")
        if kzone_long or kzone_short:
            # RECONNECT 2026-04-14 — k_zone: K_ZONE_ENTRY_BONUS_TRADIER score-add when K in zone
            _kz_bonus = _cfg('K_ZONE_ENTRY_BONUS_TRADIER', getattr(self.config, 'TRADIER_K_ZONE_ENTRY_BONUS_TRADIER', 25), account_key, symbol, _side)
            score += float(_kz_bonus)
            reasons.append(f"KZone_4h(+{_kz_bonus})")
        if rsi_long or rsi_short:
            reasons.append(f"MFI_{_flow_val:.0f}")
        # --- WT COMPOSITE SCORING (TRADIER) ---
        _wt_comp_en_t = getattr(self.config, "WT_COMPOSITE_SCORING_ENABLED_TRADIER", False) or getattr(self.config, "WT_COMPOSITE_SCORING_ENABLED", False)
        if _wt_comp_en_t and not is_exit:
            # Composite fields now computed by tradier_indicators._inject_wt_composite — already in dict
            _wt_side_comp = i.get("wt_composite_long", 0) if is_long else i.get("wt_composite_short", 0)
            _wt_side_align = i.get("wt_bull_alignment", 0) if is_long else i.get("wt_bear_alignment", 0)
            _wt_block = -20.0
            if _wt_side_align < 3:
                return 0.0, "WAIT", f"WT_HTF_ALIGN_T({_wt_side_align}/5,comp={'L' if is_long else 'S'}={_wt_side_comp:.0f})"
            if _wt_side_comp < _wt_block:
                return 0.0, "WAIT", f"WT_COMP_BLOCK_T({'L' if is_long else 'S'}={_wt_side_comp:.0f}<{_wt_block:.0f})"
            if _wt_side_comp >= 50: score += 8; reasons.append(f"WT_STRONG({_wt_side_comp:.0f},+8)")
            elif _wt_side_comp >= 30: score += 5; reasons.append(f"WT_GOOD({_wt_side_comp:.0f},+5)")
            elif _wt_side_comp >= 10: score += 2; reasons.append(f"WT_OK({_wt_side_comp:.0f},+2)")
            _wt_os = i.get("wt_oversold_tf_count", 0); _wt_ob = i.get("wt_overbought_tf_count", 0)
            if is_long and _wt_os >= 2: score += 5; reasons.append(f"WT_MULTI_OS({_wt_os}TF,+5)")
            elif not is_long and _wt_ob >= 2: score += 5; reasons.append(f"WT_MULTI_OB({_wt_ob}TF,+5)")
            # BC_154_DIV: Multi-TF divergence confluence. Backtest: 4h div 65-71% WR, 3TF bull = 82.9% WR +10%/wk
            _div_tfs_t = ['5m', '15m', '1h', '4h']
            _div_bull_t = sum(1 for tf in _div_tfs_t if i.get(f'wt_divergence_{tf}') in ('BULL', 'HIDDEN_BULL'))
            _div_bear_t = sum(1 for tf in _div_tfs_t if i.get(f'wt_divergence_{tf}') in ('BEAR', 'HIDDEN_BEAR'))
            if is_long and _div_bull_t >= 3: score += 25; reasons.append(f"DIV_BULL_3TF({_div_bull_t},+25)_bc154div")
            elif is_long and _div_bull_t >= 2: score += 15; reasons.append(f"DIV_BULL_2TF({_div_bull_t},+15)_bc154div")
            elif is_long and _div_bull_t == 1: score += 8; reasons.append(f"WT_BULL_DIV({i.get('wt_strongest_bull_div_tf')},+8)")
            if not is_long and _div_bear_t >= 3: score += 25; reasons.append(f"DIV_BEAR_3TF({_div_bear_t},+25)_bc154div")
            elif not is_long and _div_bear_t >= 2: score += 15; reasons.append(f"DIV_BEAR_2TF({_div_bear_t},+15)_bc154div")
            elif not is_long and _div_bear_t == 1: score += 8; reasons.append(f"WT_BEAR_DIV({i.get('wt_strongest_bear_div_tf')},+8)")
            if is_long and _div_bear_t >= 2: score -= 15; reasons.append(f"DIV_AGAINST_LONG({_div_bear_t}TF,-15)")
            if not is_long and _div_bull_t >= 2: score -= 15; reasons.append(f"DIV_AGAINST_SHORT({_div_bull_t}TF,-15)")

        # --- GOLDEN ANGLES SCORING (backtest-validated: 30 stocks, 69K combos) ---
        # LONG: WT score > 5 + 4h bullish = Sharpe 1.68, WR 65.5%, +0.86% avg
        # SHORT: DC pos > 0.6 + 15m confirms = Sharpe 5.13, WR 89.9%
        if not is_exit:
            _wt1_4h = g('wt1_4h', 0); _wt2_4h = g('wt2_4h', 0)
            _wt1_1h = g('wt1_1h', 0); _wt2_1h = g('wt2_1h', 0)
            _wt1_15m = g('wt1_15m', 0); _wt2_15m = g('wt2_15m', 0)
            _wt_score_4h = _wt1_4h - _wt2_4h
            _wt_score_1h = _wt1_1h - _wt2_1h
            _wt_score_15m = _wt1_15m - _wt2_15m
            _dc_pos_1h = g('dc_position_1h', 0.5)
            _dc_pos_4h = g('dc_position_4h', 0.5)
            _dc_w_1h = g('dc_width_1h', 5)
            _wt_vel_1h = g('wt_velocity_1h', 0)
            _wt_cross_val_1h = g('wt_cross_value_1h', 0)
            _wt_cross_rising_1h = i.get('wt_cross_rising_1h')
            if is_long:
                # LONG golden: 4h WT bullish + WT score > 5 + DC position < 0.4
                if _wt_score_4h > 5: score += 6; reasons.append(f"GA_WT4H_BULL({_wt_score_4h:.0f},+6)")
                elif _wt_score_4h > 0: score += 2; reasons.append(f"GA_WT4H_OK({_wt_score_4h:.0f},+2)")
                if _dc_pos_1h < 0.25: score += 5; reasons.append(f"GA_DC_BOT({_dc_pos_1h:.2f},+5)")
                elif _dc_pos_1h < 0.40: score += 2; reasons.append(f"GA_DC_LOW({_dc_pos_1h:.2f},+2)")
                if _wt_cross_rising_1h is True: score += 3; reasons.append("GA_WT_RISING(+3)")
                if _wt_vel_1h > 2: score += 2; reasons.append(f"GA_VEL_UP({_wt_vel_1h:.1f},+2)")
            else:
                # SHORT golden: DC pos > 0.6 + 15m WT confirms bearish
                if _wt_score_15m < -5: score += 6; reasons.append(f"GA_WT15M_BEAR({_wt_score_15m:.0f},+6)")
                elif _wt_score_15m < 0: score += 2; reasons.append(f"GA_WT15M_OK({_wt_score_15m:.0f},+2)")
                if _dc_pos_1h > 0.75: score += 5; reasons.append(f"GA_DC_TOP({_dc_pos_1h:.2f},+5)")
                elif _dc_pos_1h > 0.60: score += 2; reasons.append(f"GA_DC_HIGH({_dc_pos_1h:.2f},+2)")
                if _wt_cross_rising_1h is False: score += 3; reasons.append("GA_WT_FALLING(+3)")
                if _wt_vel_1h < -2: score += 2; reasons.append(f"GA_VEL_DN({_wt_vel_1h:.1f},+2)")

        sent_rank = g('0sentiment_rank', 999)
        sent_score = g('0market_sentiment_score', 0)
        sent_local = g('0market_sentiment_local', 0)
        sent_class = i.get('0sentiment_classification', 'NEUTRAL')
        sent_points = 0
        if is_long:
            if sent_rank <= 20: sent_points += 4; reasons.append("Rank_Top20")
            elif sent_rank <= 50: sent_points += 2
            if sent_local > 20: sent_points += 2
            if sent_local > sent_score + 10: sent_points += 2; reasons.append("Rel_Strength")
            if "BULLISH" in sent_class: sent_points += 2
        else:
            if sent_rank >= 180: sent_points += 4; reasons.append("Rank_Bot20")
            elif sent_rank >= 150: sent_points += 2
            if sent_local < -20: sent_points += 2
            if sent_local < sent_score - 10: sent_points += 2; reasons.append("Rel_Weakness")
            if "BEARISH" in sent_class: sent_points += 2
        score += sent_points
        trend_points = 0

        if is_long:
            if dc_high_D >= dc_high_D_ant: trend_points += 2
            if dc_low_D > dc_low_D_ant: trend_points += 3; reasons.append("D_Trend_Up")

            # 4H: Immediate Trend Slope
            if dc_high_4h > dc_high_4h_ant: trend_points += 3; reasons.append("4h_Expanding")
            elif dc_low_4h > dc_low_4h_ant: trend_points += 2

            # 1H: Local Trend Support
            if dc_low_1h >= dc_low_1h_ant: trend_points += 2
            if current_price > g('ema_200_1h', 0): trend_points += 2

        else: # SHORT
            # Daily: Lower Lows or Lower Highs
            if dc_low_D <= dc_low_D_ant: trend_points += 2
            if dc_high_D < dc_high_D_ant: trend_points += 3; reasons.append("D_Trend_Down")

            # 4H: Immediate Trend Slope
            if dc_low_4h < dc_low_4h_ant: trend_points += 3; reasons.append("4h_Expanding_Down")
            elif dc_high_4h < dc_high_4h_ant: trend_points += 2

            # 1H: Local Trend Resistance
            if dc_high_1h <= dc_high_1h_ant: trend_points += 2
            if current_price < g('ema_200_1h', 999999): trend_points += 2

        score += trend_points

        # --- 3. PULLBACK/SETUP LAYER (The Wave) ---
        # "Buy Low, Sell High" within the trend.
        setup_points = 0
        
        if not is_exit:
            dc_low_15m = g('dc_low_15m')
            dc_high_15m = g('dc_high_15m')
            dc_low_5m = g('dc_low_5m')
            dc_high_5m = g('dc_high_5m')

            if is_long:
                if dc_low_15m > 0 and (current_price - dc_low_15m) / dc_low_15m < 0.015:
                    setup_points += 5; reasons.append("Bounce_15m_Low")
                elif dc_low_5m > 0 and (current_price - dc_low_5m) / dc_low_5m < 0.008:
                    setup_points += 4; reasons.append("Bounce_5m_Low")

                # 4H Stoch: If < 50, we have room. If > 80, we are chasing.
                if k_D > 95: setup_points -= 5
                elif k_D < 50 and k_D > d_D: setup_points += 5  

                if k_4h < 50: 
                    setup_points += 5; reasons.append("4h_Deep_Value")
                elif k_4h < 80: 
                    setup_points += 1
                if k_4h < 20 and k_4h > d_4h: 
                    setup_points += 4 
                elif k_4h < 50 and k_4h > d_4h: 
                    setup_points += 2  
                                     
                else: 
                    setup_points -= 3 # Chasing top
                
                # 1H Stoch: Ideally turning up
                if k_1h < 40 and k_1h > g('stoch_k_1h_prev'): 
                    setup_points += 3; reasons.append("1h_Turn_Up")
                elif k_1h < 80 and k_1h > d_1h:
                    setup_points += 2
                    
        
            else: # SHORT
                if dc_high_15m > 0 and (dc_high_15m - current_price) / dc_high_15m < 0.015:
                    setup_points += 5; reasons.append("Bounce_15m_High")
                elif dc_high_5m > 0 and (dc_high_5m - current_price) / dc_high_5m < 0.008:
                    setup_points += 4; reasons.append("Bounce_5m_High")

                if k_D < 5: setup_points -= 5
                if k_D < 50 and k_D > d_D: setup_points += 5  
                if k_4h > 50: 
                    setup_points += 5; reasons.append("4h_Premium_Short")
                elif k_4h > 20: 
                    setup_points += 2
                if k_4h > 80 and k_4h < d_4h: 
                    setup_points += 4 
                elif k_4h > 50 and k_4h < d_4h: 
                    setup_points += 2  
                else:
                    setup_points -= 3 # Chasing bottom
                if k_1h > 60 and k_1h < g('stoch_k_1h_prev'): 
                    setup_points += 3; reasons.append("1h_Turn_Down")
                elif k_1h > 20 and k_1h < d_1h:
                    setup_points += 2         
   
        score += setup_points

        trig_points = 0
        wt1, wt2 = g('wt1_15m'), g('wt2_15m')
        rel_vol = g('rel_vol_15m', 1.0)
        if is_long:
            if k_15m > d_15m: trig_points += 2
            if k_5m > d_5m: trig_points += 2
            if wt1 > wt2: trig_points += 2
            if k_5m < 30 and k_5m > d_5m: 
                trig_points += 3; reasons.append("5m_Sniper")
            
            if m1_is_fresh:
                # Crossover logic
                if k_1m > d_1m and k_1m_prev <= d_1m and k_1m < 30:
                    trig_points += 4; reasons.append("1m_Cross_Up")
                # Falling Knife penalty
                if k_1m < d_1m and k_5m < d_5m and score < 15:
                    trig_points -= 3
            else:
                reasons.append("1m_Stale_Ignored")         
                
        else: # SHORT
            if k_15m < d_15m: trig_points += 2
            if k_5m < d_5m: trig_points += 2
            if wt1 < wt2: trig_points += 2
            if k_5m > 70 and k_5m < d_5m: 
                trig_points += 3; reasons.append("5m_Sniper")

            # --- 1M CONDITIONAL BLOCK ---
            if m1_is_fresh:
                if k_1m < d_1m and k_1m_prev >= d_1m and k_1m > 70:
                    trig_points += 4; reasons.append("1m_Cross_Down")
                # Rocket Ship penalty
                if k_1m > d_1m and k_5m > d_5m and score < 15:
                    trig_points -= 3
            else:
                reasons.append("1m_Stale_Ignored")
        if rel_vol > 1.2: trig_points += 2; reasons.append("High_Vol")
        # BACKTEST_CHANGE_T55b: MOMENTUM FADE — big candle + high volume = fade the move (mean reversion)
        _mf_en = getattr(self.config, 'MOMENTUM_FADE_ENABLED_TRADIER', True)
        if _mf_en and not is_exit:
            _mf_body_min = getattr(self.config, 'MOMENTUM_FADE_BODY_ATR_MIN_TRADIER', 2.0)
            _mf_vol_min = getattr(self.config, 'MOMENTUM_FADE_VOL_MIN_TRADIER', 2.0)
            _mf_kz = getattr(self.config, 'MOMENTUM_FADE_K_ZONE_TRADIER', True)
            _mf_bonus = getattr(self.config, 'MOMENTUM_FADE_SCORE_BONUS_TRADIER', 5)
            _mf_atr = g('atr_15m'); _mf_hi = g('high_15m'); _mf_lo = g('low_15m')
            _mf_range = _mf_hi - _mf_lo if _mf_hi > 0 and _mf_lo > 0 else 0
            _mf_body_vs_atr = (_mf_range / _mf_atr) if _mf_atr > 0 else 0
            if _mf_body_vs_atr >= _mf_body_min and rel_vol >= _mf_vol_min:
                _mf_k_ok = True
                if _mf_kz: _mf_k_ok = (is_long and k_15m < 40) or (not is_long and k_15m > 60)
                if _mf_k_ok: trig_points += _mf_bonus; reasons.append(f"MOM_FADE({'L' if is_long else 'S'},rng={_mf_body_vs_atr:.1f}xATR,rv={rel_vol:.1f}x,k={k_15m:.0f},+{_mf_bonus})_bcT55b")
        score += trig_points
        bar_points = 0
        for _bp_tf, _bp_w in [("1h", 1), ("4h", 2), ("D", 3)]:
            _bp_dir = float(i.get(f"bar_direction_{_bp_tf}", 0) or 0)
            _bp_str = float(i.get(f"bar_strength_{_bp_tf}", 0) or 0)
            _bp_vc = bool(i.get(f"bar_vol_confirm_{_bp_tf}", False))
            _bp_pat = str(i.get(f"bar_pattern_{_bp_tf}", "none"))
            if _bp_dir != 0 and _bp_str >= 0.3:
                _pts = _bp_w * (1 + int(_bp_vc)) * (1 if _bp_str >= 0.6 else 0.5)
                if (is_long and _bp_dir > 0) or (not is_long and _bp_dir < 0): bar_points += _pts; reasons.append(f"{_bp_pat}_{_bp_tf}")
                elif (is_long and _bp_dir < 0) or (not is_long and _bp_dir > 0): bar_points -= _pts
            if i.get(f"bar_swing_bull_{_bp_tf}") and is_long: bar_points += _bp_w; reasons.append(f"Swing_Bull_{_bp_tf}")
            elif i.get(f"bar_swing_bear_{_bp_tf}") and not is_long: bar_points += _bp_w; reasons.append(f"Swing_Bear_{_bp_tf}")
            elif i.get(f"bar_swing_bull_{_bp_tf}") and not is_long: bar_points -= _bp_w
            elif i.get(f"bar_swing_bear_{_bp_tf}") and is_long: bar_points -= _bp_w
            _streak = int(i.get(f"bar_streak_{_bp_tf}", 0) or 0)
            if is_long and _streak >= 3: bar_points += _bp_w
            elif not is_long and _streak <= -3: bar_points += _bp_w
            elif is_long and _streak <= -3: bar_points -= _bp_w
            elif not is_long and _streak >= 3: bar_points -= _bp_w
            if i.get(f"bar_compression_{_bp_tf}"): bar_points += 1; reasons.append(f"Squeeze_{_bp_tf}")
            if i.get(f"bar_vol_spike_{_bp_tf}") and _bp_dir != 0:
                if (is_long and _bp_dir > 0) or (not is_long and _bp_dir < 0): bar_points += _bp_w
        score += bar_points

        if is_exit and position:
            # For exits, we want to know if the trend is BROKEN.
            # A low score means "Sell Now". A high score means "Hold".
            
            gain = getattr(position, 'gain', 0)
            
            if is_long:
                # Structure Break
                if current_price <= dc_low_1h: 
                    score -= 15; reasons.append("Broken_1h_Low")
                elif current_price <= dc_low_15m: 
                    score -= 10; reasons.append("Broken_1h_Low") 
                # Momentum Roll
                if k_4h < d_4h and k_4h > 60:
                    score -= 5; reasons.append("4h_Peak")

                # Profit Take
                if gain > 5.0 and k_15m < d_15m:
                    score -= 5; reasons.append("Take_Profit")

            else: # SHORT
                if current_price >= dc_high_1h:
                    score -= 15; reasons.append("Broken_1h_High")
                elif current_price >= dc_high_15m:
                    score -= 10; reasons.append("Broken_1h_High")
                if k_4h > d_4h and k_4h < 20:
                    score -= 5; reasons.append("4h_Bottom")
                if gain > 5.0 and k_15m > d_15m:
                    score -= 5; reasons.append("Take_Profit")

        # URGENT_FIX: Bear market bias — penalize longs, favor shorts
        if getattr(config, 'BEAR_MARKET_MODE_TRADIER', False):
            if is_long:
                score -= 20; reasons.append("BEAR_PENALTY")
            else:
                score += 15; reasons.append("BEAR_BONUS")
        # --- MARKET QUALITY SCORING (BACKTEST_CHANGE_146) --- 11K-trade validated across Finandy/Bitget/OKX
        if getattr(config, 'MARKET_QUALITY_SCORE_ENABLED_TRADIER', False) and not is_exit:
            _mq_bb_1h = g('bb_width_1h', 10.0); _mq_bb_4h = g('bb_width_4h', 10.0)
            _mq_dc_1h = g('dc_width_1h', 8.0); _mq_dc_4h = g('dc_width_4h', 12.0)
            _mq_adx_1h = g('adx_1h', 30); _mq_adx_4h = g('adx_4h', 30)
            _mq_mfi_1h = g('mfi_1h', 50); _mq_mfi_4h = g('mfi_4h', 50); _mq_mfi_15m = g('mfi_15m', 50)
            _mq_rsi_1h = g('rsi_1h', 50); _mq_rsi_4h = g('rsi_4h', 50); _mq_rsi_15m = g('rsi_15m', 50)
            _mq_rvol_1h = g('relative_volume_1h', 1.0); _mq_rvol_15m = g('relative_volume_15m', 1.0)
            _mq_quality = 0.0; _mq_reasons = []
            # BB tight = ranging (Bitget d=0.34)
            if _mq_bb_1h < 6.5 and _mq_bb_4h < 10.0: _mq_quality += 1.5; _mq_reasons.append(f"BB_TIGHT")
            elif _mq_bb_1h < 9.0 and _mq_bb_4h < 13.0: _mq_quality += 0.5
            # ADX low = mean-reversion works (Bitget d=0.20)
            if _mq_adx_1h < 20 and _mq_adx_4h < 25: _mq_quality += 1.0; _mq_reasons.append(f"ADX_LOW({_mq_adx_1h:.0f})")
            elif _mq_adx_1h < 25: _mq_quality += 0.5
            # Indicator-by-direction (2026-04-17 fix):
            #   LONG  uses MFI (RSI_up + volume → MFI rises, captures long confirmation correctly)
            #   SHORT uses RSI + rel_vol (RSI falling + volume gives MISLEADING higher MFI, not lower — user-confirmed)
            _short_rvol_gate = getattr(config, 'TRADIER_RSI_SHORT_RVOL_1H', 1.0)
            if is_long:
                if _mq_mfi_1h > 50 and _mq_mfi_4h > 45:
                    _mq_quality += 1.0; _mq_reasons.append("MFI_STRONG_L")
                elif _mq_mfi_1h > 40 and _mq_mfi_4h > 40:
                    _mq_quality += 0.5
            else:
                # SHORT: RSI elevated (overbought on HTF) + rel_vol confirming
                if _mq_rsi_1h > 55 and _mq_rsi_4h > 50 and _mq_rvol_1h >= _short_rvol_gate:
                    _mq_quality += 1.0; _mq_reasons.append(f"RSI_STRONG_S(r1h={_mq_rsi_1h:.0f},rv={_mq_rvol_1h:.2f})")
                elif _mq_rsi_1h > 50 and _mq_rsi_4h > 45 and _mq_rvol_1h >= _short_rvol_gate:
                    _mq_quality += 0.5
            # Low volume = quiet market (applies to LONG — LONG uses MFI). For SHORT, rel_vol was already gated above.
            if is_long and _mq_rvol_1h < 0.95: _mq_quality += 0.5; _mq_reasons.append("LOW_VOL_L")
            # Extreme-reversal scoring — per-direction indicator
            _mq_rev = 0.0
            if is_long:
                if _mq_mfi_15m < 30 and _mq_mfi_1h < 35:
                    _mq_rev += 1.0; _mq_reasons.append("MFI_EXTREME_L")
                elif _mq_mfi_15m > _mq_mfi_1h and _mq_mfi_1h < 40:
                    _mq_rev += 0.5; _mq_reasons.append("MFI_TURN_L")
            else:
                # SHORT extreme: per-TF RSI thresholds + rel_vol gate (MFI would give false signal here)
                _rsi_s15 = getattr(config, 'TRADIER_RSI_SHORT_15M', 65.0)
                _rsi_s1h = getattr(config, 'TRADIER_RSI_SHORT_1H', 65.0)
                _rvol_s15 = getattr(config, 'TRADIER_RSI_SHORT_RVOL_15M', 1.0)
                if _mq_rsi_15m > _rsi_s15 and _mq_rsi_1h > _rsi_s1h and _mq_rvol_15m >= _rvol_s15:
                    _mq_rev += 1.0; _mq_reasons.append(f"RSI_EXTREME_S(r15={_mq_rsi_15m:.0f},r1h={_mq_rsi_1h:.0f},rv={_mq_rvol_15m:.2f})")
                elif _mq_rsi_15m < _mq_rsi_1h and _mq_rsi_1h > (_rsi_s1h - 5):
                    _mq_rev += 0.5; _mq_reasons.append(f"RSI_TURN_S(r15<r1h={_mq_rsi_1h:.0f})")
            _mq_total = _mq_quality + _mq_rev
            # Bonus only — no filtering/penalty (entries already have enough gates)
            if _mq_total >= 4.0: score += 4; reasons.append(f"MQ_STRONG({_mq_total:.1f})|{'|'.join(_mq_reasons)}")
            elif _mq_total >= 2.5: score += 2; reasons.append(f"MQ_GOOD({_mq_total:.1f})")
        # --- EOD RATIO ENFORCEMENT (BACKTEST_CHANGE_147) --- scale down 30min before close
        if getattr(config, 'EOD_RATIO_ENFORCE_TRADIER', False) and not is_exit:
            try:
                from zoneinfo import ZoneInfo
                _now_et = datetime.now(ZoneInfo("America/New_York"))
                _mins_to_close = (16 * 60) - (_now_et.hour * 60 + _now_et.minute)
                if 0 < _mins_to_close <= 30:
                    score -= int(10 * (1 - _mins_to_close / 30)); reasons.append(f"EOD_FADE({_mins_to_close}min)")
                if _mins_to_close <= 5:
                    return 0.0, "WAIT", f"EOD_BLOCK({_mins_to_close}min_to_close)"
            except Exception: pass
        # --- FINAL CALCULATION ---
        final_score = float(min(30, int(score))) if is_exit else float(max(0, min(30, int(score))))
        rec = "WAIT"
        if is_exit:
            # Exit Logic: Low score = GET OUT
            if final_score < 0: rec = "STRONG_REDUCE"
            elif final_score < 10: rec = "REDUCE"
            else: rec = "HOLD"
        else:
            # Entry Logic: High score = GET IN
            if final_score >= 28: rec = "STRONG_BUY" if is_long else "STRONG_SELL"
            elif final_score >= 20: rec = "GOOD_BUY" if is_long else "GOOD_SELL"
            elif final_score >= 15: rec = "WEAK_BUY" if is_long else "WEAK_SELL"
        return final_score, rec, ",".join(reasons)

    def compute_velocity_multiplier(self, i, current_price, position_side):
        try:
            g = lambda k: float(i.get(k, 0.0) or 0.0)
            pct = lambda a, b: 0.0 if abs(b) < 1e-9 else (float(a) - float(b)) / abs(float(b))
            ema20_4h, ema50_4h, ema200_4h = g('ema_20_4h'), g('ema_50_4h'), g('ema_200_4h')
            ema20_1h, ema50_1h, ema200_1h = g('ema_20_1h'), g('ema_50_1h'), g('ema_200_1h')
            ema20_15m, ema50_15m = g('ema_20_15m'), g('ema_50_15m')
            trend_4h = pct(ema20_4h, ema50_4h) + pct(ema50_4h, ema200_4h)
            trend_1h = pct(ema20_1h, ema50_1h) + pct(ema50_1h, ema200_1h)
            trend_15m = pct(ema20_15m, ema50_15m)
            total_trend_score = (trend_4h * 0.5) + (trend_1h * 0.3) + (trend_15m * 0.2)
            direction = 1.0 if position_side == "LONG" else -1.0
            multiplier = 1.0 + (total_trend_score * direction * 20.0)
            wt1_1h, wt2_1h = g('wt1_1h'), g('wt2_1h')
            wt1_4h, wt2_4h = g('wt1_4h'), g('wt2_4h')
            if position_side == "LONG":
                if wt1_4h > wt2_4h: multiplier += 0.3 # Major Momentum
                if wt1_1h > wt2_1h: multiplier += 0.2 # Minor Momentum
            else:
                if wt1_4h < wt2_4h: multiplier += 0.3
                if wt1_1h < wt2_1h: multiplier += 0.2
            return max(0.5, min(3.0, multiplier))
        except Exception:
            return 1.0


    async def calculate_quantity_complex(self, symbol: str, action: str, position_side: str, base_quantity: float, indicators: dict, position: Any, market_context: dict = None) -> float:
        i = self.parse_market_data(indicators)
        return await self.calculate_quantity_complex_extended(symbol, action, position_side, base_quantity, i, position, market_context)

 
# class StockStrategy:
#     def __init__(self, config):
#         self.config = config
#         self.non_shortable_symbols = {"ETHE", "TCEHY", "XIACF", "IBIT", "BITO", "GBTC", "MSTR", "MARA", "RIOT", "CLSK", "HIVE", "CAN", "BTBT"}

    def _get_val(self, indicators, key, default=0.0):
        try:
            val = indicators.get(key, default)
            if isinstance(val, str):
                val = val.strip()
                if val.lower() == 'nan': return default
                return float(val)
            return float(val) if val is not None else default
        except (ValueError, TypeError):
            return default

    def _get_str(self, indicators, key, default="neutral"):
        val = indicators.get(key, default)
        return str(val).lower().strip() if val else default

    def _get_bool(self, indicators, key, default=False):
        val = indicators.get(key, default)
        if isinstance(val, str): return val.lower() == 'true'
        return bool(val)

    def parse_market_data(self, indicators: dict):
        i = indicators or {}
        d = {}
        
        # --- Add this so timestamps survive the parsing ---
        d['timestamp_1m'] = i.get('timestamp_1m', i.get('timestamp', 0))
        d['timestamp'] = i.get('timestamp', 0)
        
        # --- 0. Core Data ---
        d['current_price'] = self._get_val(i, 'current_price') or self._get_val(i, 'current_price') or self._get_val(i, 'last')
        
        # --- 0. Core Data ---
        d['score'] = self._get_val(i, 'score', 0)
        d['symbol'] = str(i.get('symbol') or i.get('ticker') or 'UNKNOWN').upper()
        
        # --- 1. Sentiment Data (CRITICAL FOR "WHO IS WINNING") ---
        d['sentiment'] = self._get_val(i, '0market_sentiment_score', 0) # Global Market
        d['sentiment_local'] = self._get_val(i, '0market_sentiment_local', 0) # This Stock
        d['sentiment_rank'] = int(self._get_val(i, '0sentiment_rank', 999))
        d['sentiment_strength'] = self._get_val(i, '0sentiment_strength', 50)
        
        # Initialize all timeframe fields
        timeframes = ['1m', '5m', '15m', '1h', '4h', 'D']
        
        for tf in timeframes:
            # Stochastics & RSI
            d[f'stoch_k_{tf}'] = self._get_val(i, f'stoch_k_{tf}', 50); d[f'stoch_d_{tf}'] = self._get_val(i, f'stoch_d_{tf}', 50)
            d[f'stoch_k_{tf}_prev'] = self._get_val(i, f'stoch_k_{tf}_prev', 50)
            d[f'rsi_{tf}'] = self._get_val(i, f'rsi_{tf}', 50)
            
            # WaveTrend & HA
            d[f'wt1_{tf}'] = self._get_val(i, f'wt1_{tf}', 0); d[f'wt2_{tf}'] = self._get_val(i, f'wt2_{tf}', 0)
            d[f'ha_{tf}'] = self._get_str(i, f'ha_{tf}', 'neutral')
            
            # MA & DC
            d[f'sma_200_{tf}'] = self._get_val(i, f'sma_200_{tf}', 0)
            d[f'ema_20_{tf}'] = self._get_val(i, f'ema_20_{tf}', 0)
            d[f'ema_50_{tf}'] = self._get_val(i, f'ema_50_{tf}', 0)
            d[f'ema_200_{tf}'] = self._get_val(i, f'ema_200_{tf}', 0) # FIX: Added extraction of ema_200
            d[f'dc_high_{tf}'] = self._get_val(i, f'dc_high_{tf}', 0); d[f'dc_low_{tf}'] = self._get_val(i, f'dc_low_{tf}', 0)
            d[f'dc_basis_{tf}'] = self._get_val(i, f'dc_basis_{tf}', 0)
            
            # ATR & Vol & MFI
            d[f'atr_{tf}'] = self._get_val(i, f'atr_{tf}', 0)
            d[f'rel_vol_{tf}'] = self._get_val(i, f'relative_volume_{tf}', 1.0)
            d[f'lr_trend_{tf}'] = self._get_val(i, f'lr_trend_{tf}', 0.0)
            d[f'mfi_{tf}'] = self._get_val(i, f'mfi_{tf}', 50)
            
            # OHLC current & prev (needed for IBS, K5M bounce logic, structural checks)
            d[f'high_{tf}'] = self._get_val(i, f'high_{tf}', 0)
            d[f'low_{tf}'] = self._get_val(i, f'low_{tf}', 0)
            d[f'high_{tf}_prev'] = self._get_val(i, f'high_{tf}_prev', 0)
            d[f'low_{tf}_prev'] = self._get_val(i, f'low_{tf}_prev', 0)
            d[f'close_{tf}_prev'] = self._get_val(i, f'close_{tf}_prev', 0)

        # Special 1H
        d['slope_close_1h'] = self._get_val(i, 'slope_close_1h', 0.0)
        
        # Mappings
        if d.get('rsi_5m', 50) == 50 and ('rsi_5m' not in (i or {})): d['rsi_5m'] = self._get_val(i, 'rsi_5m', 50)

        return d

    # def compute_velocity_multiplier(self, i: dict, is_long: bool) -> float:
    #     # Keep your existing logic here, it was good for momentum detection
    #     pct = lambda a, b: 0.0 if abs(b) < 1e-9 else (float(a) - float(b)) / abs(float(b))
    #     long_speed = pct(i['ema_20_4h'], i['ema_50_4h']) + pct(i['ema_50_4h'], i['ema_200_4h']) + pct(i['ema_20_1h'], i['ema_50_1h'])
    #     short_speed = pct(i['ema_20_15m'], i['ema_50_15m']) + pct(i['ema_50_15m'], i['ema_200_15m'])
    #     direction = 1.0 if is_long else -1.0
    #     score = direction * (long_speed * 1.1 + short_speed * 0.9)
    #     return max(0.5, min(2.5, 1.0 + score))

    def _aggregate_numeric(self, data: dict) -> dict:
        nums = [float(v) for v in data.values() if isinstance(v, (int, float))]
        if not nums: return {'mean':0.0,'max':0.0,'min':0.0}
        return {'mean':sum(nums)/len(nums),'max':max(nums),'min':min(nums)}



    async def calculate_quantity_complex_extended(self, symbol: str, action: str, position_side: str, base_quantity: float,   i: dict, position: Any, market_context: dict = None,   account_key: str = None, reason: str = "") -> float:
        qty = float(base_quantity)
        is_long = position_side == 'LONG'
        current_price,_ = await self.trade_manager.get_current_price(symbol)
        if not current_price or current_price <= 0:
            current_price = float(i.get('current_price', 0) or 0)
        if current_price <= 0:
            return 0.0
        SP = config.START_POSITION_SIZE / current_price
        velocity_mult = 1.0                 
        log_parts = [f"[SIZE_CALC] {symbol} {position_side} Base: {qty:.2f}"]
        knife_penalty = 1.0
        ha_5m = i.get('ha_5m', 'neutral')
        if is_long:
            if ha_5m == 'red': 
                # Strict: If 5m is still red, we are likely early.
                knife_penalty *= 0.3 
                log_parts.append("Knife(HA_Red)")
        else: # SHORT
            if ha_5m == 'green': 
                knife_penalty *= 0.3
                log_parts.append("Knife(HA_Grn)")


        # Apply Knife Penalty immediately
        qty *= knife_penalty
        if qty <= 0: return 0.0
        # ============================================
        # 1. TIME-BASED STRATEGY SELECTION
        # ============================================
        now_est = datetime.now(timezone.utc).astimezone(ZoneInfo("America/New_York"))
        current_time_est = now_est.time()
        market_open = dt_time(9, 30)
        market_close = dt_time(16, 0)
        
        # Determine if we should focus on long-term (1h/4h/D) or short-term (1m/5m/15m)
        is_long_term_focus = False
        is_short_term_focus = False
        
        # Before 11 AM or after 2 PM: Focus on longer timeframes
        if current_time_est < dt_time(11, 0) or current_time_est > dt_time(14, 0):
            is_long_term_focus = True
        # 11 AM - 2 PM: Focus on short-term intraday
        else:
            is_short_term_focus = True
        
        # After 3:30 PM, reduce all positions for market close
        is_near_close = current_time_est > dt_time(15, 30)
        
        # ============================================
        # 2. INITIAL REDUCTION FACTORS
        # ============================================
        reduction_factor = 1.0
        
        # Check if K aligns with D on 15m (basic alignment)
        k_15m = i.get('stoch_k_15m', 50)
        d_15m = i.get('stoch_d_15m', 50)
        k_ok = (k_15m >= d_15m) if is_long else (k_15m <= d_15m)
        if not k_ok:
            reduction_factor *= 0.3

        k_5m = i.get('stoch_k_5m', 50)
        k_1h = i.get('stoch_k_1h', 50)

        if is_long_term_focus:
            if is_long and k_1h > 80: reduction_factor *= 0.7
            elif not is_long and k_1h < 20: reduction_factor *= 0.7
        
        # Short-term overextension reduction
        if is_short_term_focus:
            if is_long and k_5m > 85: reduction_factor *= 0.6
            elif not is_long and k_5m < 15: reduction_factor *= 0.6
        
        if is_near_close: reduction_factor *= 0.5
        
        qty = qty * reduction_factor
        
        # ============================================
        # 2b. GOLDEN ANGLES DYNAMIC SIZING (backtest-validated)
        # Scale size by: DC position depth, WT score strength, DC width (volatility), WT velocity
        # ============================================
        _ga_mult = 1.0
        _ga_parts = []
        _dc_pos = float(i.get('dc_position_1h', 0.5) or 0.5)
        _dc_width = float(i.get('dc_width_1h', 5) or 4)
        _wt_score = float(i.get('wt1_4h', 0) or 0) - float(i.get('wt2_4h', 0) or 0)
        _wt_vel = float(i.get('wt_velocity_1h', 0) or 0)
        # DC position depth: deeper in the channel = larger size (more mean-reversion potential)
        if is_long:
            if _dc_pos < 0.10: _ga_mult *= 2.0; _ga_parts.append(f"DC_DEEP({_dc_pos:.2f},2x)")
            elif _dc_pos < 0.20: _ga_mult *= 1.5; _ga_parts.append(f"DC_LOW({_dc_pos:.2f},1.5x)")
            elif _dc_pos < 0.30: _ga_mult *= 1.2; _ga_parts.append(f"DC_OK({_dc_pos:.2f},1.2x)")
            elif _dc_pos > 0.70: _ga_mult *= 0.5; _ga_parts.append(f"DC_HIGH({_dc_pos:.2f},0.5x)")
        else:
            if _dc_pos > 0.90: _ga_mult *= 2.0; _ga_parts.append(f"DC_DEEP({_dc_pos:.2f},2x)")
            elif _dc_pos > 0.80: _ga_mult *= 1.5; _ga_parts.append(f"DC_HIGH({_dc_pos:.2f},1.5x)")
            elif _dc_pos > 0.70: _ga_mult *= 1.2; _ga_parts.append(f"DC_OK({_dc_pos:.2f},1.2x)")
            elif _dc_pos < 0.30: _ga_mult *= 0.5; _ga_parts.append(f"DC_LOW({_dc_pos:.2f},0.5x)")
        # WT score strength: stronger momentum = larger size
        if (is_long and _wt_score > 10) or (not is_long and _wt_score < -10): _ga_mult *= 1.3; _ga_parts.append(f"WT_STRONG({_wt_score:.0f},1.3x)")
        elif (is_long and _wt_score > 5) or (not is_long and _wt_score < -5): _ga_mult *= 1.1; _ga_parts.append(f"WT_OK({_wt_score:.0f},1.1x)")
        elif (is_long and _wt_score < -5) or (not is_long and _wt_score > 5): _ga_mult *= 0.5; _ga_parts.append(f"WT_AGAINST({_wt_score:.0f},0.5x)")
        # DC width: wider channel = more volatile = smaller size for safety
        if _dc_width > 8: _ga_mult *= 0.7; _ga_parts.append(f"DCW_WIDE({_dc_width:.1f},0.7x)")
        elif _dc_width < 3: _ga_mult *= 1.3; _ga_parts.append(f"DCW_TIGHT({_dc_width:.1f},1.3x)")
        # WT velocity: strong momentum in right direction = larger
        if (is_long and _wt_vel > 3) or (not is_long and _wt_vel < -3): _ga_mult *= 1.2; _ga_parts.append(f"VEL_STRONG({_wt_vel:.1f},1.2x)")
        elif (is_long and _wt_vel < -2) or (not is_long and _wt_vel > 2): _ga_mult *= 0.6; _ga_parts.append(f"VEL_AGAINST({_wt_vel:.1f},0.6x)")
        # Clamp total multiplier
        _ga_mult = max(0.3, min(3.0, _ga_mult))
        qty *= _ga_mult
        if _ga_parts: log_parts.append(f"GA({_ga_mult:.1f}x={'|'.join(_ga_parts)})")
        # ============================================
        # 3. SENTIMENT & MARKET STRUCTURE SCALING
        # ============================================
        
        # Extract sentiment data
        sent_global = i.get('0market_sentiment_score', 0)
        sent_local = i.get('0market_sentiment_local', 0)
        sent_strength = i.get('0sentiment_strength', 50)
        sentiment_rank = i.get('0sentiment_rank', 999)
        
        # Sentiment Multiplier (0.3x to 2.0x)
        sentiment_mult = 1.0
        
        if is_long:
            if sent_global > 0:
                if sent_local > sent_global + 40: sentiment_mult = 1.5
                elif sent_local > sent_global + 20: sentiment_mult = 1.2
                elif sent_local < -20: sentiment_mult = 0.3
            else: # Bear Market
                if sent_local > 60: sentiment_mult = 1.3
                elif sent_local < sent_global: sentiment_mult = 0.1
        else: # Short
            if sent_global < 0:
                if sent_local < sent_global - 40: sentiment_mult = 1.5
                elif sent_local < sent_global: sentiment_mult = 1.2
                elif sent_local > 20: sentiment_mult = 0.3
            else: # Bull Market
                if sent_local < -80: sentiment_mult = 1.3
                elif sent_local > sent_global: sentiment_mult = 0.1
        
        strength_boost = 1.0 + (sent_strength / 200.0)
        qty = qty * sentiment_mult * strength_boost
        log_parts.append(f"Sent({sentiment_mult:.1f}x)")

        # ============================================
        # 5. TECHNICAL STRUCTURE & POSITIONING
        # ============================================
        
        # Donchian Channel positioning (0.0 = bottom, 1.0 = top)
        def get_dc_position(price, dc_low, dc_high):
            if dc_high <= dc_low: return 0.5
            return (price - dc_low) / (dc_high - dc_low)
        
        # Multiple timeframe positioning
        pos_1m = get_dc_position(current_price, i.get('dc_low_1m', current_price), i.get('dc_high_1m', current_price))
        pos_5m = get_dc_position(current_price, i.get('dc_low_5m', current_price), i.get('dc_high_5m', current_price))
        pos_15m = get_dc_position(current_price, i.get('dc_low_15m', current_price), i.get('dc_high_15m', current_price))
        pos_1h = get_dc_position(current_price, i.get('dc_low_1h', current_price), i.get('dc_high_1h', current_price))
        pos_4h = get_dc_position(current_price, i.get('dc_low_4h', current_price), i.get('dc_high_4h', current_price))
        pos_D = get_dc_position(current_price, i.get('dc_low_D', current_price), i.get('dc_high_D', current_price))
        
        positioning_mult = 1.0
        
        # LONG-TERM FOCUS: Value near 4h/D lows
        if is_long_term_focus:
            if is_long:
                if pos_4h < 0.2:
                    positioning_mult = 1.8  # Deep value on 4h
                elif pos_4h < 0.4:
                    positioning_mult = 1.4
                elif pos_4h > 0.8:
                    positioning_mult = 0.6  # Chasing top
                
                if pos_D < 0.3:
                    positioning_mult *= 1.3  # Weekly deep value
            else:
                if pos_4h > 0.8:
                    positioning_mult = 1.8  # Top of 4h range
                elif pos_4h > 0.6:
                    positioning_mult = 1.4
                elif pos_4h < 0.2:
                    positioning_mult = 0.6  # Chasing bottom
        
        # SHORT-TERM FOCUS: Precision on 5m/15m
        elif is_short_term_focus:
            if is_long:
                if pos_5m < 0.3 and pos_15m < 0.4:
                    positioning_mult = 1.6  # Good intraday pullback
                elif pos_5m > 0.7:
                    positioning_mult = 0.7  # Extended on 5m
            else:
                if pos_5m > 0.7 and pos_15m > 0.6:
                    positioning_mult = 1.6  # Good intraday bounce
                elif pos_5m < 0.3:
                    positioning_mult = 0.7  # Oversold bounce
        
        log_parts.append(f"Velo({velocity_mult:.2f}x)")

        # Avoid extreme positioning
        if pos_1m > 0.95 or pos_1m < 0.05:
            positioning_mult *= 0.8  # Extreme on 1m
        
        qty = qty * positioning_mult
        
        # ============================================
        # 5. VOLUME & LIQUIDITY CONSIDERATIONS
        # ============================================
        
        # Relative volume multipliers
        rel_vol_5m = i.get('rel_vol_5m', 1.0)
        rel_vol_15m = i.get('rel_vol_15m', 1.0)
        rel_vol_1h = i.get('rel_vol_1h', 1.0)
        
        volume_mult = 1.0
        
        # High volume confirms moves
        if rel_vol_5m > 2.0:
            volume_mult = 1.3
        elif rel_vol_5m > 1.5:
            volume_mult = 1.15
        elif rel_vol_5m < 0.7:
            volume_mult = 0.8  # Low volume = weak move
        
        # Sustained volume on higher timeframes
        if rel_vol_1h > 1.8:
            volume_mult *= 1.2
        
        qty = qty * volume_mult
        
        # ============================================
        # 5b. MFI SIZING BOOST (BC_154_T: stock ablation — MFI_only Sharpe 1.357 vs default -0.177)
        # MFI is the #1 stock sizing signal. Extreme MFI = high conviction.
        # ============================================
        _mfi_1h_sz = float(i.get('mfi_1h', 50) or 50)
        _mfi_15m_sz = float(i.get('mfi_15m', 50) or 50)
        _mfi_sz_mult = 1.0
        if is_long:
            if _mfi_1h_sz < 30: _mfi_sz_mult = 1.8
            elif _mfi_1h_sz < 40: _mfi_sz_mult = 1.4
            elif _mfi_1h_sz < 50: _mfi_sz_mult = 1.1
            elif _mfi_1h_sz > 70: _mfi_sz_mult = 0.5
            if _mfi_15m_sz < 25: _mfi_sz_mult *= 1.3
        else:
            if _mfi_1h_sz > 70: _mfi_sz_mult = 1.8
            elif _mfi_1h_sz > 60: _mfi_sz_mult = 1.4
            elif _mfi_1h_sz > 50: _mfi_sz_mult = 1.1
            elif _mfi_1h_sz < 30: _mfi_sz_mult = 0.5
            if _mfi_15m_sz > 75: _mfi_sz_mult *= 1.3
        _mfi_sz_mult = max(0.3, min(2.5, _mfi_sz_mult))
        qty *= _mfi_sz_mult
        if _mfi_sz_mult != 1.0: log_parts.append(f"MFI_SZ({_mfi_sz_mult:.1f}x,1h={_mfi_1h_sz:.0f},15m={_mfi_15m_sz:.0f})")
        # ============================================
        # 5c. DIVERGENCE CONFLUENCE SIZING (BC_154_DIV: 4h div 65-71% WR, 3TF = 82.9% WR +10%/wk)
        # ============================================
        _div_tfs_sz = ['5m', '15m', '1h', '4h']
        _div_bull_sz = sum(1 for tf in _div_tfs_sz if i.get(f'wt_divergence_{tf}') in ('BULL', 'HIDDEN_BULL'))
        _div_bear_sz = sum(1 for tf in _div_tfs_sz if i.get(f'wt_divergence_{tf}') in ('BEAR', 'HIDDEN_BEAR'))
        _div_match_sz = _div_bull_sz if is_long else _div_bear_sz
        _div_against_sz = _div_bear_sz if is_long else _div_bull_sz
        _div_sz_mult = 1.0
        if _div_match_sz >= 3: _div_sz_mult = 2.0
        elif _div_match_sz >= 2: _div_sz_mult = 1.5
        elif _div_match_sz == 1: _div_sz_mult = 1.2
        if _div_against_sz >= 2: _div_sz_mult *= 0.5
        _div_sz_mult = max(0.3, min(2.5, _div_sz_mult))
        qty *= _div_sz_mult
        if _div_sz_mult != 1.0: log_parts.append(f"DIV_SZ({_div_sz_mult:.1f}x,match={_div_match_sz},against={_div_against_sz})_bc154div")
        # ============================================
        # 6. TREND VELOCITY & MOMENTUM (from crypto)
        # ============================================

        if action in ['OPEN', 'AUGMENT', 'REENTER']:
            velocity_mult = self.compute_velocity_multiplier(i, current_price, position_side)
            qty = qty * velocity_mult
        
        # ============================================
        # 7. EXTENSIVE TECHNICAL BONUSES (CRYPTO-STYLE)
        # ============================================
        # WaveTrend alignment
        wt1_5m, wt2_5m = i.get('wt1_5m',0), i.get('wt2_5m',0)
        wt1_1h, wt2_1h = i.get('wt1_1h',0), i.get('wt2_1h',0)

        # WaveTrend alignment bonuses (basic)
        if is_long:
            if wt1_5m > wt2_5m and k_15m < 50: qty += 0.3 * SP
            if wt1_1h > wt2_1h: qty += 0.5 * SP
        else:
            if i.get('wt1_5m', 0) < i.get('wt2_5m', 0) and i.get('stoch_k_15m', 50) > 50:
                qty += 0.3 * SP
            if i.get('wt1_1h', 0) < i.get('wt2_1h', 0) and i.get('wt1_4h', 0) < i.get('wt2_4h', 0):
                qty += 0.5 * SP
        # WT Intelligence bonuses (structural analysis)
        _wt_div_1h = i.get('wt_divergence_1h', None)
        _wt_mom_1h = i.get('wt_momentum_state_1h', '')
        _wt_cross_1h = i.get('wt_cross_1h', None)
        _wt_cross_rising = i.get('wt_cross_rising_1h', None)
        _wt_pctile_1h = float(i.get('wt_percentile_1h', 50) or 50)
        _wt_trough_s = i.get('wt_trough_structure_1h', '')
        _wt_peak_s = i.get('wt_peak_structure_1h', '')
        if is_long and _wt_div_1h == 'BULL': qty += 1.0 * SP
        elif not is_long and _wt_div_1h == 'BEAR': qty += 1.0 * SP
        if is_long and _wt_mom_1h == 'EXHAUST_DOWN': qty += 0.8 * SP
        elif not is_long and _wt_mom_1h == 'EXHAUST_UP': qty += 0.8 * SP
        if is_long and _wt_mom_1h == 'IMPULSE_DOWN': qty -= 0.5 * SP
        elif not is_long and _wt_mom_1h == 'IMPULSE_UP': qty -= 0.5 * SP
        if _wt_cross_1h == 'BULL' and is_long: qty += 0.6 * SP
        elif _wt_cross_1h == 'BEAR' and not is_long: qty += 0.6 * SP
        if is_long and _wt_pctile_1h < 10: qty += 0.5 * SP
        elif not is_long and _wt_pctile_1h > 90: qty += 0.5 * SP
        if is_long and _wt_trough_s == 'HL': qty += 0.4 * SP
        elif not is_long and _wt_peak_s == 'LH': qty += 0.4 * SP
        # MOMENTUM INTERCEPTION ENTRY — bonus for favorable opposing-side exhaustion + structure
        _side = position_side
        if _cfg('MI_ENTRY_ENABLED_TRADIER', False, account_key, symbol, _side):
            _mi_eb = 0
            _mi_struct_b = getattr(config, 'MI_ENTRY_STRUCT_BONUS_TRADIER', 10) * 0.01 * SP
            _mi_exh_b = getattr(config, 'MI_ENTRY_EXHAUST_BONUS_TRADIER', 8) * 0.01 * SP
            _wt_peak_s_4h_e = i.get('wt_peak_structure_4h', '')
            _wt_trough_s_4h_e = i.get('wt_trough_structure_4h', '')
            _wt_mom_4h_e = i.get('wt_momentum_state_4h', '')
            _wt_div_4h_e = i.get('wt_divergence_4h', None)
            if is_long:
                if _wt_trough_s_4h_e == 'HL': _mi_eb += _mi_struct_b
                if _wt_mom_4h_e == 'EXHAUST_DOWN': _mi_eb += _mi_exh_b
                if _wt_div_4h_e == 'BULL': _mi_eb += _mi_exh_b
            else:
                if _wt_peak_s_4h_e == 'LH': _mi_eb += _mi_struct_b
                if _wt_mom_4h_e == 'EXHAUST_UP': _mi_eb += _mi_exh_b
                if _wt_div_4h_e == 'BEAR': _mi_eb += _mi_exh_b
            qty += _mi_eb
        # Donchian Breakout bonuses
        if is_long:
            if current_price > i.get('dc_high_15m', 999999) and i.get('stoch_k_15m', 50) < 70:
                qty += 0.5 * SP  # Breakout without being overbought

            if current_price > i.get('dc_high_1h', 999999) and i.get('ha_1h', 'neutral') == 'green':
                qty += 0.8 * SP  # Strong 1h breakout

        else:
            if current_price < i.get('dc_low_15m', 0) and i.get('stoch_k_15m', 50) > 30:
                qty += 0.5 * SP

            if current_price < i.get('dc_low_1h', 0) and i.get('ha_1h', 'neutral') == 'red':
                qty += 0.8 * SP

        # RSI extremes with volume
        if is_long:
            if i.get('rsi_5m', 50) < 30 and rel_vol_5m > 1.2:
                qty += 0.4 * SP
            if i.get('rsi_1h', 50) < 35 and rel_vol_1h > 1.5:
                qty += 0.6 * SP  # Hourly oversold with volume

        else:
            if i.get('rsi_5m', 50) > 70 and rel_vol_5m > 1.2:
                qty += 0.4 * SP
            if i.get('rsi_1h', 50) > 65 and rel_vol_1h > 1.5:
                qty += 0.6 * SP

        # Heikin Ashi alignment bonuses
        ha_alignment = 0
        if is_long:
            if i.get('ha_5m', 'neutral') == 'green': ha_alignment += 1
            if i.get('ha_15m', 'neutral') == 'green': ha_alignment += 1
            if i.get('ha_1h', 'neutral') == 'green': ha_alignment += 2
            if i.get('ha_4h', 'neutral') == 'green': ha_alignment += 3
        else:
            if i.get('ha_5m', 'neutral') == 'red': ha_alignment += 1
            if i.get('ha_15m', 'neutral') == 'red': ha_alignment += 1
            if i.get('ha_1h', 'neutral') == 'red': ha_alignment += 2
            if i.get('ha_4h', 'neutral') == 'red': ha_alignment += 3

        if ha_alignment >= 3:
            qty += (ha_alignment * 0.1) * SP

        # ============================================
        # 8. MARKET CONTEXT & BREADTH
        # ============================================

        # Market bias from context
        market_bias = market_context.get('bias', 0.0) if market_context else 0.0
        
        if is_long:
            if market_bias > 0.3:  # Strong bullish market
                qty *= 1.3
            elif market_bias < -0.3:  # Strong bearish market
                qty *= 0.6
            elif -0.1 < market_bias < 0.1:  # Neutral market
                qty *= 0.9  # Reduce size in uncertain markets
        else:
            if market_bias < -0.3:  # Strong bearish market
                qty *= 1.3
            elif market_bias > 0.3:  # Strong bullish market
                qty *= 0.6
            elif -0.1 < market_bias < 0.1:  # Neutral market
                qty *= 0.9

        # ============================================
        # 9. POSITION-SPECIFIC ADJUSTMENTS
        # ============================================

        if position:
            # Existing position adjustments
            position_amt = abs(float(getattr(position, 'positionAmt', 0)))
            position_gain = float(getattr(position, 'gain', 0))

            # For AUGMENT actions
            if action == 'AUGMENT':
                if position_gain < -2.0:
                    qty = 0.0  # Don't add to big losers
                elif position_gain < 0:
                    qty *= 0.3  # Small add to recover
                elif position_gain < 1.0:
                    qty *= 0.7  # Conservative add to small winners
                elif position_gain < 3.0:
                    qty *= 1.1  # Good momentum
                elif position_gain < 5.0:
                    qty *= 1.3  # Strong momentum
                else:
                    qty *= 1.5  # Big winner - pyramid

            # For REENTRY after reduction
            if action == 'REENTER':
                last_red_amt = float(getattr(position, 'last_reduction_amount', 0))
                if last_red_amt > 0:
                    # Try to reclaim at least the reduced amount
                    qty = max(qty, last_red_amt * 1.2)

        # ============================================
        # 10. SENTIMENT RANK BONUSES (TOP/BOTTOM 20)
        # ============================================CRAP

        if sentiment_rank >= 105:  # Top 5 bullish
            if is_long:
                qty += 2.0 * SP
            else:
                qty *= 0.3  # Hard to short top bullish

        elif sentiment_rank >= 195:  # Bottom 5 bearish
            if not is_long:
                qty += 2.0 * SP
            else:
                qty *= 0.3  # Hard to long bottom bearish

        elif 6 <= sentiment_rank <= 20:  # Top 6-20
            if is_long:
                qty += 1.0 * SP

        elif 180 <= sentiment_rank <= 194:  # Bottom 6-20
            if not is_long:
                qty += 1.0 * SP

        # ============================================
        # 11. FINAL SAFETY CHECKS & CAPS
        # ============================================
        
        is_exception = False
        
        # Check if "Exception" applies (High Conviction or Recovery)
        # REENTER: allow larger single order but NEVER exceed per-position total cap
        if action == "REENTER" or symbol in self.exceptions: 
            is_exception = True
            qty *= 3.0
            log_parts.append("🚀EXCEPTION_BOOST(3x)")
            
        if "RECOVERY" in reason.upper(): is_exception = True
        max_usd = self.limit_exception_order if is_exception else self.limit_normal_order
        
        # B. Calculate proposed USD value
        proposed_usd = qty * current_price
        
        # C. Clamp Single Order
        if proposed_usd > max_usd:
            old_qty = qty
            qty = max_usd / current_price
            log_parts.append(f"⚠️CAPPED(OrderLim ${max_usd:.0f})")
        
        # D. Clamp Total Position
        now_est = datetime.now(timezone.utc).astimezone(ZoneInfo("America/New_York"))
        current_time_est = now_est.time()

        # --- 1. THE OPENING BELL BOOST (9:30 AM - 10:30 AM) ---
        is_opening_hour = dt_time(9, 30) <= current_time_est <= dt_time(10, 30)
        if is_opening_hour and action in ["OPEN", "REENTER"]:
            # Boost size by 1.5x - 2.0x during the initial session momentum
            opening_multiplier = 1.75 
            qty *= opening_multiplier
            log_parts.append(f"🚀OPEN_BOOST({opening_multiplier}x)")

        # --- 2. THE CLOSING BALANCER REDUCTION (3:00 PM - 4:00 PM) ---
        is_last_hour = dt_time(15, 0) <= current_time_est <= dt_time(16, 0)
        if is_last_hour:
            # Reduce new entry sizes significantly as we approach close to avoid overnight gap risk
            qty *= 0.5
            log_parts.append("⚖️EOD_CONSERVATIVE")
        positionAmt = abs(float(getattr(position, 'positionAmt', 0)))
        current_val = positionAmt * current_price
        max_Total = self.limit_total_pos  # ALWAYS cap total position — REENTER can boost single order but never exceed per-position max
        room_usd = max_Total - current_val
        if room_usd <= 0:
            qty = 0.0
            log_parts.append(f"⚠️BLOCKED(MaxPos ${self.limit_total_pos:.0f})")
        elif (qty * current_price) > room_usd:
            qty = room_usd / current_price
            log_parts.append(f"⚠️CAPPED(MaxPosRoom ${room_usd:.0f})")
        if qty * current_price < 50.0:
            if (positionAmt * current_price) + 50.0 <= self.limit_total_pos:
                qty = 50.0 / current_price
            else:
                qty = 0.0 #
        qty = max(0.0, float(int(qty)))
        if qty * current_price < 25.0: return 0.0
        return max(0.0, float(int(qty)))

    @staticmethod
    def evaluate_multi_tf_exit(i: dict, is_long: bool, gain: float, hold_time_min: float, current_price: float) -> tuple:
        """Multi-TF technical exit scorer. Returns (should_exit, reason, exit_score 0-100)."""
        g = lambda k, d=0: float(i.get(k, d) or d)
        score = 0.0
        parts = []
        tfs = ('5m', '15m', '1h', '4h', 'D')
        # --- 1. WT DIVERGENCE: price vs WT peak/trough ---
        for tf in ('1h', '4h', 'D'):
            wt1 = g(f'wt1_{tf}'); pk_val = g(f'wt_peak_value_{tf}'); tr_val = g(f'wt_trough_value_{tf}')
            close_now = g(f'close_{tf}', current_price); close_prev = g(f'close_{tf}_prev')
            if is_long and pk_val != 0 and close_prev > 0:
                if wt1 < pk_val * 0.85 and close_now >= close_prev:
                    w = {'1h': 6, '4h': 10, 'D': 14}.get(tf, 6)
                    score += w; parts.append(f"DIV_LH_{tf}({wt1:.0f}<pk{pk_val:.0f})")
            elif not is_long and tr_val != 0 and close_prev > 0:
                if wt1 > tr_val * 0.85 and close_now <= close_prev:
                    w = {'1h': 6, '4h': 10, 'D': 14}.get(tf, 6)
                    score += w; parts.append(f"DIV_HL_{tf}({wt1:.0f}>tr{tr_val:.0f})")
        # --- 2. DC POSITION FADE (breakout failure) ---
        for tf in ('15m', '1h', '4h'):
            dc_pos = g(f'dc_position_{tf}', 0.5)
            w = {'15m': 4, '1h': 7, '4h': 10}.get(tf, 4)
            if is_long and dc_pos < 0.35:
                score += w; parts.append(f"DC_FADE_{tf}({dc_pos:.2f})")
            elif not is_long and dc_pos > 0.65:
                score += w; parts.append(f"DC_FADE_{tf}({dc_pos:.2f})")
        # --- 3. WT VELOCITY DECELERATION (trend dying before cross) ---
        vel_against = 0; vel_details = []
        for tf in ('5m', '15m', '1h', '4h'):
            vel = g(f'wt_velocity_{tf}')
            thresh = {'5m': 1.5, '15m': 1.0, '1h': 0.5, '4h': 0.3}.get(tf, 1.0)
            if is_long and vel < -thresh:
                vel_against += 1; vel_details.append(f"{tf}:{vel:.1f}")
            elif not is_long and vel > thresh:
                vel_against += 1; vel_details.append(f"{tf}:{vel:.1f}")
        if vel_against >= 2:
            score += vel_against * 5; parts.append(f"VEL_DECEL_{vel_against}tf({','.join(vel_details)})")
        # --- 4. MULTI-TF STRUCTURE BREAK SCORE ---
        struct_total = 0
        for tf in tfs:
            tf_score_local = 0
            wt1 = g(f'wt1_{tf}'); wt2 = g(f'wt2_{tf}')
            if is_long and wt1 < wt2: tf_score_local += 1
            elif not is_long and wt1 > wt2: tf_score_local += 1
            k = g(f'stoch_k_{tf}', 50); d = g(f'stoch_d_{tf}', 50)
            if is_long and k < d: tf_score_local += 1
            elif not is_long and k > d: tf_score_local += 1
            dc_pos = g(f'dc_position_{tf}', 0.5)
            if is_long and dc_pos < 0.3: tf_score_local += 1
            elif not is_long and dc_pos > 0.7: tf_score_local += 1
            ha = g(f'ha_green_{tf}', 0.5)
            if is_long and ha < 0.5: tf_score_local += 1
            elif not is_long and ha > 0.5: tf_score_local += 1
            struct_total += tf_score_local
        if struct_total >= 12:
            score += 20; parts.append(f"STRUCT_BREAK_{struct_total}/20")
        elif struct_total >= 9:
            score += 10; parts.append(f"STRUCT_WEAK_{struct_total}/20")
        # --- 5. BB BAND REJECTION (breakout failed) ---
        for tf in ('15m', '1h', '4h'):
            bb = g(f'bb_pct_b_{tf}', 0.5)
            w = {'15m': 4, '1h': 6, '4h': 8}.get(tf, 4)
            if is_long and bb < 0.3:
                score += w; parts.append(f"BB_REJ_{tf}({bb:.2f})")
            elif not is_long and bb > 0.7:
                score += w; parts.append(f"BB_REJ_{tf}({bb:.2f})")
        # --- 6. STOCH CROSS AGAINST (confirmation) ---
        stoch_against = 0
        for tf in ('5m', '15m', '1h'):
            k = g(f'stoch_k_{tf}', 50); kp = g(f'stoch_k_{tf}_prev', 50)
            d = g(f'stoch_d_{tf}', 50); dp = g(f'stoch_d_{tf}_prev', 50)
            if is_long and kp >= dp and k < d: stoch_against += 1
            elif not is_long and kp <= dp and k > d: stoch_against += 1
        if stoch_against >= 2:
            score += stoch_against * 4; parts.append(f"STOCH_CROSS_{stoch_against}tf")
        # --- 7. MFI EXHAUSTION (flow drying up) ---
        mfi_1h = g('mfi_1h', 50); mfi_4h = g('mfi_4h', 50)
        if is_long and mfi_1h > 80 and mfi_4h > 70:
            score += 6; parts.append(f"MFI_EXHST_L({mfi_1h:.0f},{mfi_4h:.0f})")
        elif not is_long and mfi_1h < 20 and mfi_4h < 30:
            score += 6; parts.append(f"MFI_EXHST_S({mfi_1h:.0f},{mfi_4h:.0f})")
        # --- GAIN-AWARE URGENCY BOOST ---
        if gain > 3.0 and score >= 20: score *= 1.3
        elif gain > 1.0 and score >= 25: score *= 1.2
        score = min(score, 100.0)
        # --- THRESHOLD: exit when score high enough ---
        threshold = 35 if gain > 1.0 else 45 if gain > 0.3 else 55
        should_exit = score >= threshold
        reason = f"MULTI_TF_EXIT(s={score:.0f}/{threshold},g={gain:.1f}%,{'+'.join(parts[:6])})" if parts else ""
        # RECONNECT 2026-04-14 — wt_exit: WT_EXIT_TFS_TRADIER + WT_EXIT_MIN_TFS_TRADIER
        # WT cross-vote across configured TFs — if min TFs flipped against position, force exit.
        try:
            _wt_tfs_str = getattr(config, 'TRADIER_WT_EXIT_TFS_TRADIER', getattr(config, 'WT_EXIT_TFS_TRADIER', '5m+15m+1h+4h+D'))
            _wt_min_tfs = int(getattr(config, 'TRADIER_WT_EXIT_MIN_TFS_TRADIER', getattr(config, 'WT_EXIT_MIN_TFS_TRADIER', 4)))
            _wt_tfs = [t.strip() for t in _wt_tfs_str.replace('+', ',').split(',') if t.strip()]
            _wt_against = 0
            _wt_against_tfs = []
            for _tf in _wt_tfs:
                _wt1 = g(f'wt1_{_tf}'); _wt2 = g(f'wt2_{_tf}')
                if is_long and _wt1 < _wt2:
                    _wt_against += 1; _wt_against_tfs.append(_tf)
                elif not is_long and _wt1 > _wt2:
                    _wt_against += 1; _wt_against_tfs.append(_tf)
            if _wt_min_tfs > 0 and _wt_against >= _wt_min_tfs and len(_wt_tfs) > 0:
                should_exit = True
                reason = f"WT_EXIT_TFS({_wt_against}/{len(_wt_tfs)}>={_wt_min_tfs}:{'+'.join(_wt_against_tfs)})|" + reason
        except Exception:
            pass
        return (should_exit, reason, score)

    def evaluate_price_only_exit(self, position: Any, current_price: float) -> tuple[bool, str]:
        """
        Emergency logic used when indicators are stale (>900s).
        Decides to exit based strictly on PnL erosion and hard stops.
        """
        is_long = getattr(position, 'position_side', 'LONG') == 'LONG'
        entry = float(getattr(position, 'entry_price', 0))
        max_gain = float(getattr(position, 'max_gain', 0))
        
        if entry <= 0: return False, ""
        
        # Calculate current gain strictly from the fresh price feed
        gain = ((current_price - entry) / entry * 100) if is_long else ((entry - current_price) / entry * 100)

        # 1. HARD STOP — DISABLED. STRICT_NO_LOSS. NEVER close at a loss.
        # 2. GAIN EROSION — DISABLED. Do NOT close winning positions going in the right direction.
        pass
        # STALE_DATA_PROFIT_SHIELD — DISABLED. Stale data must NEVER trigger exits.
        # Only entries should be blocked on stale data. Exiting on stale data = bad decisions.
        # Was killing manually opened positions (USO bought at $113.08, killed at $113.03 within seconds).
        # Exit decisions require: gain erosion from prev_gain check (every 5min) AND price below DC_low4_5m.
        pass
        return False, ""

    async def evaluate_stop(self, symbol: str, position: Any, indicators: dict, market_context: dict = None, in_grace_period: bool = False) -> tuple[bool, str, float]:
        # OPENING_BUFFER_NO_TRADE (2026-04-26 owner directive): block ALL trade
        # actions during first N min after open. Wait for first-30m data to be
        # meaningful (VWAP, range, trend) before deciding anything. Default 30m.
        _in_buffer, _mins_open = in_opening_buffer()
        if _in_buffer:
            return False, f"OPENING_BUFFER_NO_TRADE({_mins_open:.0f}m<30m_no_data)", 0
        # 🤖 TRC_AGENT advisory short-circuit (paper account, supervisor routine override)
        try:
            _trc_acct = current_account.get("")
            if _trc_acct == "trc":
                import trc_advisory_consumer as _trc_adv
                _trc_pside = getattr(position, 'position_side', 'LONG')
                _trc_adv_obj = _trc_adv.check(_trc_acct, symbol, _trc_pside, "stop")
                if _trc_adv_obj:
                    _trc_action = _trc_adv_obj.get("action")
                    _trc_reason = _trc_adv_obj.get("reason", "")[:60]
                    if _trc_action == "force_close":
                        _trc_adv.log_application(_trc_acct, symbol, _trc_pside, "stop", "force_close", _trc_adv_obj)
                        logger.info(f"🤖 TRC_AGENT_FORCE_CLOSE {symbol}_{_trc_pside}: {_trc_reason}")
                        return True, f"TRC_AGENT_FORCE_CLOSE({_trc_reason})", 1.0
                    if _trc_action == "hold":
                        _trc_adv.log_application(_trc_acct, symbol, _trc_pside, "stop", "hold", _trc_adv_obj)
                        logger.info(f"🤖 TRC_AGENT_HOLD {symbol}_{_trc_pside}: {_trc_reason}")
                        return False, f"TRC_AGENT_HOLD({_trc_reason})", 0
        except Exception as _trc_err:
            logger.warning(f"[TRC_AGENT] evaluate_stop advisory check failed: {_trc_err}")
        i = self.parse_market_data(indicators)
        is_long = getattr(position, 'position_side', 'LONG') == 'LONG'
        qty = abs(float(getattr(position, 'positionAmt', 0)))
        gain = float(getattr(position, 'gain', 0))
        current_price, ts = await self.trade_manager.get_current_price(symbol)
        current_price = float(current_price)
        # ═══ 2026-04-30 HTF PORT — F3. HTF_W_REVERSAL_EXIT (default OFF, sweep-gated) ═══
        # Crypto baseline uses ALL_TF_BRAKE counting W+M; tradier baseline never reads W or M
        # for exits. F3 fires when W WaveTrend turns against position AND (optional) D agrees.
        # Only fires in profit (gain > 0) — STRICT_NO_LOSS rule still applies.
        _gr_exit_min_tfs = int(getattr(config, 'GOLDEN_RULE_EXIT_MIN_TFS', 0))
        if _gr_exit_min_tfs > 0:
            try:
                from golden_rule_htf import score_exit_htf as _gr_score_exit
                _gr_exit_min_ind = int(getattr(config, 'GOLDEN_RULE_EXIT_MIN_IND', 2))
                _gr_ex_ok, _gr_ex_ntfs, _gr_ex_detail = _gr_score_exit(i, is_long, "tradier", _gr_exit_min_tfs, _gr_exit_min_ind, current_price)
                if not _gr_ex_ok:
                    return False, f"GR_HTF_EXIT_HOLD {_gr_ex_ntfs}of{_gr_exit_min_tfs}bearish_tfs_req", 0
            except Exception as _gree:
                logger.warning(f"[GR_HTF_EXIT] {symbol}: {_gree}")
        if getattr(config, 'HTF_W_REVERSAL_EXIT_TRADIER_ENABLED', False) and gain > 0:
            try:
                _w1_W = float(i.get('wt1_W', 0) or 0)
                _w2_W = float(i.get('wt2_W', 0) or 0)
                _w1_D = float(i.get('wt1_D', 0) or 0)
                _w2_D = float(i.get('wt2_D', 0) or 0)
                _w_has = (_w1_W != 0 or _w2_W != 0)
                _d_has = (_w1_D != 0 or _w2_D != 0)
                if _w_has:
                    if is_long:
                        _w_against = _w1_W < _w2_W
                        _d_against = _w1_D < _w2_D and _d_has
                    else:
                        _w_against = _w1_W > _w2_W
                        _d_against = _w1_D > _w2_D and _d_has
                    _need_d = bool(getattr(config, 'HTF_W_REVERSAL_EXIT_TRADIER_REQUIRE_D', True))
                    if _w_against and (_d_against or not _need_d):
                        logger.warning(f"[HTF_W_REVERSAL_EXIT] {symbol}_{('L' if is_long else 'S')} g={gain:.2f}% wt_W={_w1_W:.1f}/{_w2_W:.1f} wt_D={_w1_D:.1f}/{_w2_D:.1f} need_D={_need_d}")
                        return True, f"HTF_W_REVERSAL_EXIT(W={_w1_W:.1f}/{_w2_W:.1f}_D={_w1_D:.1f}/{_w2_D:.1f}_g={gain:.2f}%)", qty
            except Exception as _htfwre:
                logger.debug(f"[HTF_W_REVERSAL_EXIT] {symbol}: {type(_htfwre).__name__} {_htfwre} — skipped")

        opened_at = getattr(position, 'opened_at', None) or getattr(position, 'entry_time', None)
        hold_time_min = 0.0
        if opened_at:
            dt_opened = safe_datetime(opened_at)
            if dt_opened:
                hold_time_min = (datetime.now(timezone.utc) - dt_opened).total_seconds() / 60.0
        else:
            if in_grace_period:
                hold_time_min = 5.0 # fallback

        # ── OPTIONS EQUITY HEDGE GUARD ─────────────────────────────────────────
        # If this equity position is hedging a losing option, block the close.
        # Guard file is written every cycle by tradier_options_analyzer watch loop.
        # Stale > 10 minutes = ignored (watchdog probably stopped).
        try:
            _eq_guard_path = Path(str(config.DATA_DIR)) / "options_equity_hedge_guard.json"
            if _eq_guard_path.exists() and time.time() - _eq_guard_path.stat().st_mtime < 600:
                with open(_eq_guard_path) as _egf:
                    _eg = json.load(_egf)
                if _eg.get("account", "trb") == current_account.get(""):
                    _eg_ps = _eg.get("protect_short", {})
                    _eg_pl = _eg.get("protect_long", {})
                    _eg_xs = _eg.get("cross_protect_short", {})
                    _eg_xl = _eg.get("cross_protect_long", {})
                    if not is_long and (symbol in _eg_ps or symbol in _eg_xs):
                        _eg_why = (_eg_ps.get(symbol) or _eg_xs.get(symbol, symbol))[:80]
                        return False, f"OPT_HEDGE_GUARD_SHORT({_eg_why})", 0
                    if is_long and (symbol in _eg_pl or symbol in _eg_xl):
                        _eg_why = (_eg_pl.get(symbol) or _eg_xl.get(symbol, symbol))[:80]
                        return False, f"OPT_HEDGE_GUARD_LONG({_eg_why})", 0
        except Exception as _ege:
            logger.warning(f"[OPT_HEDGE_GUARD] read error: {_ege}")
        # ==================================================================
        # tra LONG-TERM HOLD GATES (rewritten 2026-04-09)
        #
        # tra is a long-term-bias cash account that was bleeding from constant
        # losing exits. Three gates apply BEFORE any other exit logic:
        #   1. Never close at a loss (TRA_NO_LOSS_EXIT)
        #   2. Long minimum hold (TRA_MIN_HOLD_MINUTES, default 24h)
        #   3. Only the strict 5-of-5 multi-TF gate counts as an exit signal
        #      (TRA_STRICT_EXIT_ONLY) — score must be 100, not the 20 that
        #      lets 5m/15m noise fire exits.
        # ==================================================================
        _exit_acct_top = current_account.get('')
        if _exit_acct_top == 'tra':
            if getattr(config, 'TRA_NO_LOSS_EXIT', True) and gain < 0:
                return False, f"TRA_NO_LOSS_HOLD(g={gain:.2f}%<0)", 0
            _tra_min_hold = float(getattr(config, 'TRA_MIN_HOLD_MINUTES', 1440.0))
            if hold_time_min < _tra_min_hold:
                return False, f"TRA_MIN_HOLD({hold_time_min:.0f}m<{_tra_min_hold:.0f}m)", 0
            if getattr(config, 'TRA_STRICT_EXIT_ONLY', True):
                _tra_ind = indicators if indicators else i
                _tra_score, _tra_reason = wt_dc_score_exit(_tra_ind, is_long, current_price)
                # The strict 5-of-5 gate inside the scorer returns score == 100.
                # PARTIAL (4-of-5) returns 40 — we treat that as HOLD for tra.
                # The legacy fallback tops out at ~99 but rarely matches strict 100.
                if _tra_score < 100:
                    return False, f"TRA_HOLD_no_strict_exit(score={_tra_score:.0f}<100,g={gain:.2f}%)", 0
                logger.warning(f"[TRA_STRICT_EXIT] {symbol} L: STRICT 5-of-5 gate fired score={_tra_score:.0f} g={gain:.2f}% — exiting in profit only")
                return True, f"TRA_STRICT_EXIT_g={gain:.2f}%_{_tra_reason[:60]}", qty

        # ═══ PEAK_GIVEBACK_PROTECTION + DC_LOW4_5M (2026-04-20) ═══
        # "Positions that were in gain and fall back below 0 must close."
        # 2026-04-27 user rule: NOW respects STOCK_MIN_HOLD (was bypassing). Stocks are
        # NOT scalps — peak-to-now noise must not flip a 72h-hold position.
        # DC_LOW4_5M structural stop still fires any time the position was ever profitable.
        _is_opts_check = hasattr(position, 'option_type') and getattr(position, 'option_type', None)
        _peak_min_hold = float(getattr(config, 'TRADIER_MIN_HOLD_MINUTES', getattr(config, 'MIN_HOLD_MINUTES_TRADIER', 240.0)))
        if not _is_opts_check and bool(getattr(config, 'PEAK_GIVEBACK_PROTECTION_ENABLED', True)) and hold_time_min >= _peak_min_hold:
            # 2026-04-29 USER RULE: PEAK_GIVEBACK uses cycle_peak_gain (resets on close), NOT max_gain
            # (which has its own decay and is for reentry). Stops "stale 12% peak from prior cycle kills
            # fresh manual buy" bug. Maintain cycle_peak_gain inline so it tracks live.
            _cur_g_pk = float(getattr(position, 'gain', 0) or 0)
            _cycle_peak_old = float(getattr(position, 'cycle_peak_gain', 0) or 0)
            if _cur_g_pk > _cycle_peak_old:
                try: position.cycle_peak_gain = _cur_g_pk
                except Exception: pass
            _pgp_max_g = float(getattr(position, 'cycle_peak_gain', 0) or 0)  # was max_gain
            _pgp_min_peak = float(getattr(config, 'PEAK_GIVEBACK_MIN_PEAK_PCT', 0.3))
            _be_grace = float(getattr(config, 'BREAKEVEN_GRACE_MINUTES', 15.0))
            _pgp_drop = float(getattr(config, 'PEAK_GIVEBACK_DROP_PCT', 2.0))
            _pgp_hard_zero = bool(getattr(config, 'PEAK_GIVEBACK_HARD_ZERO_ENABLED', True))
            _pgp_ind = indicators if indicators else i
            if _pgp_max_g >= _pgp_min_peak and hold_time_min >= _be_grace:
                # 2026-04-29 USER RULE: PEAK_GIVEBACK must NOT close manually-bought positions that
                # peaked then pulled back to flat. Today closed user's GOOGL (peak 12.37% → cur 0.00%)
                # and MSFT (peak 8.69% → 0.00%) for "no apparent reason". A 5pp drawdown from a 12% peak
                # is normal swing pullback, not a reversal. Require the position to actually be at a
                # net loss after fees before this fires.
                _pgp_require_neg = bool(getattr(config, 'PEAK_GIVEBACK_REQUIRE_NEGATIVE_GAIN', True))
                _pgp_neg_floor = float(getattr(config, 'PEAK_GIVEBACK_NEGATIVE_GAIN_FLOOR_PCT', -0.5))  # require gain <= -0.5% (real loss after fees)
                if _pgp_require_neg and gain > _pgp_neg_floor:
                    logger.info(f"🛡️[PEAK_GIVEBACK_BLOCKED_PROFIT] {symbol} {'L' if is_long else 'S'}: peak={_pgp_max_g:.2f}% cur={gain:.2f}% > floor={_pgp_neg_floor:.2f}% — holding (still in profit zone, manual-buy protection)")
                else:
                    if _pgp_hard_zero and gain < 0.08:
                        logger.critical(f"🔥[PEAK_GIVEBACK] {symbol} {'L' if is_long else 'S'}: peak={_pgp_max_g:.2f}% near-zero gain={gain:.2f}% age={hold_time_min:.0f}m — EXITING ⚠️ DO NOT DISABLE")
                        return True, f"PEAK_GIVEBACK_GAIN_EROSION_STOP_peak{_pgp_max_g:.2f}%_cur{gain:.2f}%", qty
                    if gain < _pgp_max_g - _pgp_drop:
                        logger.critical(f"🔥[PEAK_GIVEBACK] {symbol} {'L' if is_long else 'S'}: peak={_pgp_max_g:.2f}% gave back >{_pgp_drop:.1f}% cur={gain:.2f}% age={hold_time_min:.0f}m — EXITING ⚠️ DO NOT DISABLE")
                        return True, f"PEAK_GIVEBACK_GAIN_EROSION_STOP_peak{_pgp_max_g:.2f}%_drop{_pgp_drop:.1f}%_cur{gain:.2f}%", qty
        if not _is_opts_check and bool(getattr(config, 'PEAK_GIVEBACK_PROTECTION_ENABLED', True)) and hold_time_min < _peak_min_hold:
            logger.info(f"[PEAK_GIVEBACK_MIN_HOLD_BLOCK] {symbol} {'L' if is_long else 'S'}: hold={hold_time_min:.0f}m<{_peak_min_hold:.0f}m — peak-giveback gated (user rule 2026-04-27)")
            # 2026-05-10: hoist locals from sister branch (>= _peak_min_hold) — they were defined only there,
            # crashing here for any trb position younger than 240min (e.g. GOOGL, WDAY 2026-05-09 23:50+).
            _pgp_max_g = float(getattr(position, 'cycle_peak_gain', 0) or 0)
            _pgp_ind = indicators if indicators else i
            if _pgp_max_g > 0 and getattr(config, 'BREAKEVEN_DC_LOW4_ENABLED', True):
                _be_dc_low4 = float((_pgp_ind).get('dc_low4_5m', 0) or 0)
                _be_dc_high4 = float((_pgp_ind).get('dc_high4_5m', 0) or 0)
                if is_long and _be_dc_low4 > 0 and current_price > 0 and current_price < _be_dc_low4:
                    logger.critical(f"🚫[DC_LOW4_5M_BREAK] {symbol} L: price {current_price:.4f} < dc_low4_5m {_be_dc_low4:.4f} was_profitable={_pgp_max_g:.2f}% — structural stop")
                    return True, f"DC_LOW4_5M_GAIN_EROSION_STOP_p{current_price:.4f}<dc4{_be_dc_low4:.4f}_g{gain:.2f}%", qty
                if not is_long and _be_dc_high4 > 0 and current_price > 0 and current_price > _be_dc_high4:
                    logger.critical(f"🚫[DC_HIGH4_5M_BREAK] {symbol} S: price {current_price:.4f} > dc_high4_5m {_be_dc_high4:.4f} was_profitable={_pgp_max_g:.2f}% — structural stop")
                    return True, f"DC_HIGH4_5M_GAIN_EROSION_STOP_p{current_price:.4f}>dc4{_be_dc_high4:.4f}_g{gain:.2f}%", qty
        # ═══ NOLOSS_BB1H_GATE (stocks structural break — 2026-04-20) ═══
        # Stock equivalent of crypto NOLOSS_DC4H_GATE. When price breaks OUTSIDE the 1h
        # Bollinger Band in the wrong direction while at a loss, the original thesis is
        # structurally invalidated — override NO_LOSS and close. If still inside the band,
        # it is a pullback within structure — hold (STOCK_MIN_HOLD + UNIVERSAL_NOLOSS_GATE handle it).
        # Bypasses STOCK_MIN_HOLD and UNIVERSAL_NOLOSS_GATE intentionally.
        if not _is_opts_check and gain < 0 and bool(getattr(config, 'NOLOSS_BB1H_GATE_ENABLED', True)):
            _bb1h_ind = indicators if indicators else i
            _bb1h_upper = float((_bb1h_ind).get('bb_upper_1h', 0) or 0)
            _bb1h_lower = float((_bb1h_ind).get('bb_lower_1h', 0) or 0)
            if _bb1h_upper > 0 and _bb1h_lower > 0 and current_price > 0:
                if is_long and current_price < _bb1h_lower:
                    logger.critical(f"🏗️[NOLOSS_BB1H_GATE] {symbol} L: price {current_price:.4f} < bb_lower_1h {_bb1h_lower:.4f} — structural breakdown, NO_LOSS overridden, gain={gain:.2f}%")
                    return True, f"NOLOSS_BB1H_BREAKDOWN_LONG_px{current_price:.4f}<bb_low{_bb1h_lower:.4f}_g{gain:.2f}%", qty
                elif not is_long and current_price > _bb1h_upper:
                    logger.critical(f"🏗️[NOLOSS_BB1H_GATE] {symbol} S: price {current_price:.4f} > bb_upper_1h {_bb1h_upper:.4f} — structural breakout against, NO_LOSS overridden, gain={gain:.2f}%")
                    return True, f"NOLOSS_BB1H_BREAKDOWN_SHORT_px{current_price:.4f}>bb_up{_bb1h_upper:.4f}_g{gain:.2f}%", qty
        # ═══════════════════════════════════════════════════════════════════════
        # 🚩 GR_HTF_DIRECT_EXIT — 2026-05-12 USER MANDATE
        # Direct exit signal: if opposite-direction GR_HTF score >= GR_HTF_DIRECT_EXIT_SCORE → CLOSE.
        # Bypass UNIVERSAL_NOLOSS_GATE: reason contains 'GR_HTF_DIRECT_EXIT' (per config.py bypass list).
        # Fires BEFORE STOCK_MIN_HOLD and NOLOSS gate — this is a sanctioned technical loss-exit path.
        # ROLLBACK: set GR_HTF_DIRECT_EXIT_ENABLED=False in config_tradier.py.
        # ═══════════════════════════════════════════════════════════════════════
        if not _is_opts_check and bool(getattr(config, 'GR_HTF_DIRECT_EXIT_ENABLED', True)):
            try:
                from golden_rule_htf import score_entry_htf as _gr_htf_direct_exit_fn
                _grde_exit_min_ind = int(getattr(config, 'GOLDEN_RULE_MIN_IND', 1))
                _grde_exit_ind = indicators if indicators else i
                # Score opposite-direction alignment (is_long=False for LONG pos means we look for bearish)
                _grde_exit_pass, _grde_exit_n_tfs, _grde_exit_detail = _gr_htf_direct_exit_fn(
                    _grde_exit_ind, not is_long, 'tradier',
                    min_tfs=1, min_ind=_grde_exit_min_ind, current_price=current_price)
                _grde_exit_score = float(_grde_exit_n_tfs) * float(_grde_exit_min_ind)
                _grde_exit_score_min = float(getattr(config, 'GR_HTF_DIRECT_EXIT_SCORE', 18.0))
                if _grde_exit_score >= _grde_exit_score_min:
                    logger.critical(f"⛔ [GR_HTF_DIRECT_EXIT] {symbol} {'L' if is_long else 'S'}: opp_score={_grde_exit_score:.0f} (tfs={_grde_exit_n_tfs}×ind={_grde_exit_min_ind}) >= {_grde_exit_score_min:.0f} gain={gain:.2f}% — CLOSE (bypasses NOLOSS)")
                    return True, f"GR_HTF_DIRECT_EXIT_{_grde_exit_score:.0f}_g{gain:.2f}%_{_grde_exit_detail[:60]}", qty
                else:
                    logger.debug(f"[GR_HTF_DIRECT_EXIT_MISS] {symbol} {'L' if is_long else 'S'}: opp_score={_grde_exit_score:.0f} < {_grde_exit_score_min:.0f}")
            except Exception as _grde_exit_err:
                logger.warning(f"[GR_HTF_DIRECT_EXIT_ERR] {symbol}: {_grde_exit_err}")
        # ═══ END GR_HTF_DIRECT_EXIT ═══
        # ═══ P2-D: REENTRY_BREAKOUT structural stop (default OFF) ═══
        # If a reentry was tagged with _reentry_breakout_level (the DC level it broke through),
        # close immediately if price crosses back through that level (position is invalidated).
        # Bypasses UNIVERSAL_NOLOSS_GATE via reason string 'REENTRY_BREAKOUT' (added to SRS bypass check).
        if not _is_opts_check and getattr(config, 'REENTRY_BREAKOUT_ENABLED', False):
            try:
                _rb_level = getattr(position, '_reentry_breakout_level', None)
                if _rb_level is not None:
                    _rb_level = float(_rb_level)
                    if _rb_level > 0:
                        if is_long and current_price < _rb_level * (1 - 0.001):
                            logger.warning(f"[REENTRY_BREAKOUT_STOP] {symbol} L: price {current_price:.4f} < level {_rb_level:.4f} × 0.999 — structural stop (breakout invalidated)")
                            return True, f"REENTRY_BREAKOUT_FALLBACK_TO_LEVEL_px{current_price:.4f}<lvl{_rb_level:.4f}_g{gain:.2f}%", qty
                        elif not is_long and current_price > _rb_level * (1 + 0.001):
                            logger.warning(f"[REENTRY_BREAKOUT_STOP] {symbol} S: price {current_price:.4f} > level {_rb_level:.4f} × 1.001 — structural stop (breakout invalidated)")
                            return True, f"REENTRY_BREAKOUT_FALLBACK_TO_LEVEL_px{current_price:.4f}>lvl{_rb_level:.4f}_g{gain:.2f}%", qty
            except Exception as _rb_err:
                logger.debug(f"[REENTRY_BREAKOUT_STOP] {symbol}: {_rb_err}")
        # ═══ END P2-D REENTRY_BREAKOUT structural stop ═══
        # ==================================================================
        # #1 RULE: DELTA ENGINE EXIT (Sharpe 63.44) + WT/DC SCORER FALLBACK (Sharpe 11.46)
        # DEPLOYED 2026-04-08. 48h monitoring. ROLLBACK: backups/before_scorer_wire_202604080100.py
        # ==================================================================
        # User directive 2026-04-10: STOCKS must be held at least 4h. Stocks are NOT
        # crypto — they are swing/position trades, not scalps. The 1h/4h/D slowdown
        # is what signals a reversal; LTF noise must not close a position.
        _stock_min_hold = float(getattr(config, 'TRADIER_MIN_HOLD_MINUTES', getattr(config, 'MIN_HOLD_MINUTES_TRADIER', 240.0)))
        if not _is_opts_check and hold_time_min < _stock_min_hold:
            return False, f"STOCK_MIN_HOLD({hold_time_min:.0f}m<{_stock_min_hold:.0f}m)", 0
        # ═══ DYNAMIC_SCORE_COUNTER_EXIT (2026-04-20) — le_dynamic winner ═══
        # Exit when opposite-direction LE score >= threshold — the entry setup has reversed.
        # Fires after STOCK_MIN_HOLD, before NOLOSS gate (technical exit like SRS).
        # Validated: le_dynamic_tradier_v2, 262sym Sharpe 3.5479, threshold=55.
        if not _is_opts_check and bool(getattr(config, 'DYNAMIC_SCORE_COUNTER_EXIT_ENABLED', False)):
            _ce_threshold = float(getattr(config, 'DYNAMIC_SCORE_COUNTER_EXIT_THRESHOLD', 55.0))
            try:
                _ce_ind = indicators if indicators else i
                _ce_sc, _ce_sz, _ce_rsn = _le_score_entry(_ce_ind, not is_long)
                if _ce_sc >= _ce_threshold:
                    logger.critical(f"📊[DYNAMIC_COUNTER_EXIT] {symbol} {'L' if is_long else 'S'}: counter_score={_ce_sc:.0f}>={_ce_threshold:.0f} ({_ce_rsn}) gain={gain:.2f}% hold={hold_time_min:.0f}m")
                    return True, f"DYNAMIC_SCORE_COUNTER_EXIT_score{_ce_sc:.0f}>={_ce_threshold:.0f}_g{gain:.2f}%", qty
            except Exception as _ce_err:
                logger.warning(f"[DYNAMIC_COUNTER_EXIT] {symbol}: error {_ce_err}")
        # ═══ RANGE TOP/BOTTOM EXIT (stocks) — USER 2026-04-14 ═══
        # LONG: entry_price > bb_upper_1h (entered above the range). When price comes back to the top of
        #   the range AND k_1h > 80 AND k_15m > 80 AND k_5m turning down → exit (sells into the retest).
        # SHORT: entry_price < bb_lower_1h (entered below the range). When price comes back to the bottom
        #   of the range AND k_1h < 20 AND k_15m < 20 AND k_5m turning up → cover (buys into the retest).
        # SRS reason bypasses STRICT_NO_LOSS in execute_now.
        if not _is_opts_check and getattr(config, 'STRUCTURAL_RANGE_SHIFT_EXIT', False):
            _srs_entry = float(getattr(position, 'entry_price', 0) or 0)
            _srs_tf = getattr(config, 'STRUCTURAL_RANGE_SHIFT_TF', 'bb_1h')
            _srs_field_map = {
                'dc_1h': ('dc_high_1h', 'dc_low_1h'), 'dc_4h': ('dc_high_4h', 'dc_low_4h'),
                'dc_D': ('dc_high_D', 'dc_low_D'), 'bb_1h': ('bb_upper_1h', 'bb_lower_1h'),
                'bb_4h': ('bb_upper_4h', 'bb_lower_4h'), 'bb_D': ('bb_upper_D', 'bb_lower_D'),
            }
            _srs_high_key, _srs_low_key = _srs_field_map.get(_srs_tf, ('bb_upper_1h', 'bb_lower_1h'))
            _srs_ind = indicators or i
            _srs_high = float((_srs_ind).get(_srs_high_key, 0) or 0)
            _srs_low = float((_srs_ind).get(_srs_low_key, 0) or 0)
            _srs_prox_bps = float(getattr(config, 'STRUCTURAL_RANGE_SHIFT_PROXIMITY_BPS', 100.0))
            _srs_band = _srs_prox_bps / 10000.0
            _srs_k_high = float(getattr(config, 'STRUCTURAL_RANGE_SHIFT_K_HIGH', 80.0))
            _srs_k_low = float(getattr(config, 'STRUCTURAL_RANGE_SHIFT_K_LOW', 20.0))
            _srs_k1h = float((_srs_ind).get('stoch_k_1h', 50) or 50)
            _srs_k15m = float((_srs_ind).get('stoch_k_15m', 50) or 50)
            _srs_k5m = float((_srs_ind).get('stoch_k_5m', 50) or 50)
            _srs_k5m_prev = float((_srs_ind).get('stoch_k_5m_prev', 50) or 50)
            if _srs_entry > 0 and _srs_high > 0 and _srs_low > 0:
                if is_long and _srs_entry > _srs_high:
                    _srs_prox = abs(current_price - _srs_high) / _srs_high <= _srs_band
                    _srs_1h = _srs_k1h > _srs_k_high
                    _srs_15m = _srs_k15m > _srs_k_high
                    _srs_5m_down = _srs_k5m < _srs_k5m_prev
                    if _srs_prox and _srs_1h and _srs_15m and _srs_5m_down:
                        logger.critical(f"🏗️ [RANGE_TOP_EXIT] {symbol} LONG {_srs_tf}: entry={_srs_entry:.4f}>{_srs_high_key}={_srs_high:.4f} price={current_price:.4f} k1h={_srs_k1h:.0f} k15m={_srs_k15m:.0f} k5m={_srs_k5m:.0f}↓{_srs_k5m_prev:.0f}")
                        return True, f"STRUCTURAL_RANGE_SHIFT_LONG_{_srs_tf}_top={_srs_high:.4f}_k1h={_srs_k1h:.0f}_k15m={_srs_k15m:.0f}_k5m={_srs_k5m:.0f}", qty
                elif not is_long and _srs_entry < _srs_low:
                    _srs_prox = abs(current_price - _srs_low) / _srs_low <= _srs_band
                    _srs_1h = _srs_k1h < _srs_k_low
                    _srs_15m = _srs_k15m < _srs_k_low
                    _srs_5m_up = _srs_k5m > _srs_k5m_prev
                    if _srs_prox and _srs_1h and _srs_15m and _srs_5m_up:
                        logger.critical(f"🏗️ [RANGE_BOTTOM_EXIT] {symbol} SHORT {_srs_tf}: entry={_srs_entry:.4f}<{_srs_low_key}={_srs_low:.4f} price={current_price:.4f} k1h={_srs_k1h:.0f} k15m={_srs_k15m:.0f} k5m={_srs_k5m:.0f}↑{_srs_k5m_prev:.0f}")
                        return True, f"STRUCTURAL_RANGE_SHIFT_SHORT_{_srs_tf}_bot={_srs_low:.4f}_k1h={_srs_k1h:.0f}_k15m={_srs_k15m:.0f}_k5m={_srs_k5m:.0f}", qty
        # === PARTIAL_PROFIT_LOCK v2 (2026-04-21) — mirror of crypto PPL v2.
        # Fires BEFORE UNIVERSAL_NOLOSS_GATE — PPL is staged partial+trail, not a NOLOSS trigger.
        # Step 1 (+0.5%): TP fires 50% via execute_trade_action(REDUCE); stop_level = entry × (1 ± BE_buffer%).
        # Step 2 (+0.75%): upgrade stop_level → first_exit_price (locks +0.5% scalp on remainder).
        # Step 3 (price ≤/≥ stop_level): close remainder (return True → caller fires full close).
        # 2026-05-18 per-sym overlay
        _ppl_side_t = 'LONG' if is_long else 'SHORT'
        _ppl_acct_list_t = getattr(config, 'PARTIAL_PROFIT_LOCK_ACCOUNTS_TRADIER', ['trb', 'trc'])
        if (_cfg('PARTIAL_PROFIT_LOCK_ENABLED', False, _exit_acct_top, symbol, _ppl_side_t)
            and _exit_acct_top in _ppl_acct_list_t):
            _ppl_min_gain_t = float(_cfg('PARTIAL_PROFIT_LOCK_GAIN_PCT_TRADIER', 0.5, _exit_acct_top, symbol, _ppl_side_t))
            _ppl_arm_gain_t = float(_cfg('PARTIAL_PROFIT_LOCK_ARM_GAIN_PCT_TRADIER', 0.75, _exit_acct_top, symbol, _ppl_side_t))
            _ppl_be_buffer_t = float(_cfg('PARTIAL_PROFIT_LOCK_BE_BUFFER_PCT_TRADIER', 0.02, _exit_acct_top, symbol, _ppl_side_t))
            _ppl_frac_t = float(_cfg('PARTIAL_PROFIT_LOCK_FRAC_TRADIER', 0.5, _exit_acct_top, symbol, _ppl_side_t))
            _ppl_pk_t = f"{_exit_acct_top}:{symbol}_{'LONG' if is_long else 'SHORT'}"
            _tm_t = self.trade_manager if hasattr(self, 'trade_manager') else self
            if not hasattr(_tm_t, 'partial_profit_lock_state'):
                _tm_t.partial_profit_lock_state = {}
            _ppl_state_t = _tm_t.partial_profit_lock_state.get(_ppl_pk_t, {})
            _ppl_fired_t = _ppl_state_t.get('fired', False)
            _ppl_first_exit_px_t = float(_ppl_state_t.get('first_exit_price', 0.0))
            _ppl_stop_level_t = float(_ppl_state_t.get('stop_level', 0.0))
            _ppl_stop_upgraded_t = _ppl_state_t.get('stop_upgraded', False)
            _ppl_entry_px_t = float(getattr(position, 'entry_price', 0.0) or 0.0)
            _ppl_min_qty_t = self.min_qty.get(symbol, 1.0) if hasattr(self, 'min_qty') else 1.0
            _ppl_close_side_t = "SELL" if is_long else "BUY"
            _ppl_pos_side_t = "LONG" if is_long else "SHORT"
            if not _ppl_fired_t and gain >= _ppl_min_gain_t and qty > _ppl_min_qty_t and _ppl_entry_px_t > 0:
                _ppl_reduce_qty_t = qty * _ppl_frac_t
                _ppl_keep_qty_t = qty - _ppl_reduce_qty_t
                if _ppl_reduce_qty_t >= _ppl_min_qty_t and _ppl_keep_qty_t >= _ppl_min_qty_t:
                    _ppl_be_stop_t = _ppl_entry_px_t * (1.0 + _ppl_be_buffer_t / 100.0) if is_long else _ppl_entry_px_t * (1.0 - _ppl_be_buffer_t / 100.0)
                    _ppl_uid_t = f"PPL_TP_{_ppl_pk_t}_{int(time.time())}"
                    _ppl_reason_t = f"PPL_TP_gain{gain:.2f}_50pct"
                    logger.warning(f"[PARTIAL_PROFIT_LOCK_TRADIER_TP] {_ppl_pk_t}: gain={gain:.2f}% ≥ {_ppl_min_gain_t}% — closing 50% ({_ppl_reduce_qty_t:.4f}/{qty:.4f}) + BE stop @ {_ppl_be_stop_t:.4f}")
                    try:
                        _ppl_res_t = await self.execute_trade_action(account_key=_exit_acct_top, position_key=_ppl_pk_t, symbol=symbol, quantity=_ppl_reduce_qty_t, current_price=current_price, side=_ppl_close_side_t, position_side=_ppl_pos_side_t, unique_id=_ppl_uid_t, is_full_close=False, action='REDUCE', reason=_ppl_reason_t, override_qty=_ppl_reduce_qty_t)
                        _ppl_ok_t = _ppl_res_t and "BLOCK" not in str(_ppl_res_t).upper() and "FAIL" not in str(_ppl_res_t).upper() and "MARKET_CLOSED" not in str(_ppl_res_t).upper()
                    except Exception as _ppl_e_t:
                        logger.error(f"[PPL_TRADIER_TP_ERR] {_ppl_pk_t}: {type(_ppl_e_t).__name__}: {_ppl_e_t}")
                        _ppl_ok_t = False
                    if _ppl_ok_t:
                        _tm_t.partial_profit_lock_state[_ppl_pk_t] = {'fired': True, 'first_exit_price': current_price, 'stop_level': _ppl_be_stop_t, 'stop_upgraded': False}
                        return False, f"PPL_TP_gain{gain:.2f}_BEstop{_ppl_be_stop_t:.4f}", 0
            if _ppl_fired_t and not _ppl_stop_upgraded_t and gain >= _ppl_arm_gain_t and _ppl_first_exit_px_t > 0:
                _tm_t.partial_profit_lock_state[_ppl_pk_t] = {**_ppl_state_t, 'stop_level': _ppl_first_exit_px_t, 'stop_upgraded': True}
                logger.warning(f"[PPL_TRADIER_STOP_UPGRADED] {_ppl_pk_t}: gain={gain:.2f}% ≥ {_ppl_arm_gain_t}% — stop BE→first_exit {_ppl_first_exit_px_t:.4f} (locks +0.5% scalp)")
            if _ppl_fired_t and _ppl_stop_level_t > 0 and qty > _ppl_min_qty_t:
                _sl_hit_t = (is_long and current_price <= _ppl_stop_level_t) or (not is_long and current_price >= _ppl_stop_level_t)
                if _sl_hit_t:
                    _tm_t.partial_profit_lock_state.pop(_ppl_pk_t, None)
                    _ppl_sl_label_t = 'upgraded-0.5pct' if _ppl_stop_upgraded_t else 'BE+buffer'
                    logger.warning(f"[PARTIAL_PROFIT_LOCK_TRADIER_SL_HIT] {_ppl_pk_t}: price={current_price:.4f} hit stop={_ppl_stop_level_t:.4f} ({_ppl_sl_label_t}) — closing remaining {qty:.4f}")
                    return True, f"PPL_SL_CLOSE_{_ppl_sl_label_t}_px{current_price:.4f}_stop{_ppl_stop_level_t:.4f}", qty
        # ═══ UNIVERSAL_NOLOSS_GATE (2026-04-17) — AFTER SRS so SRS can exit at a loss ═══
        # trb/trc were bleeding because DELTA_EXIT/RZ_EXIT/WT_DC_EXIT/MI_EXIT
        # paths fired on technicals REGARDLESS of gain — closing GLD at -0.09%,
        # COPX at -1.09%, XOM at -3.34%, TTD at -9.96%. L/S ratio is the hedge;
        # realized losses are the enemy. This gate stops ALL SUBSEQUENT exit
        # paths when gain < NOLOSS_MIN_PROFIT_PCT_TRADIER (3.0% = Mar-30 baseline).
        # NOTE: SRS runs ABOVE this gate — SRS is the one path allowed to exit at a loss
        # (proven-profitable backtest: entry outside BB_1h band → wait for retest → sell near band).
        # Intended scope: live strict-no-loss accounts (trb, trc).
        _noloss_min_ts = float(getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0))
        _strict_acct_list = set(getattr(config, 'STRICT_NO_LOSS_ACCOUNTS_TRADIER', ('trb', 'trc')))
        if _exit_acct_top in _strict_acct_list and _noloss_min_ts > 0 and gain < _noloss_min_ts:
            # NOLOSS_BYPASS_WT_5OF5 (2026-04-21): 5/5 WT TFs against → allow close at loss.
            # Stocks: 5m/15m/1h/4h/D TFs. Default OFF, sweep-only.
            # 2026-05-18 per-sym overlay
            _nlb_side_t = 'LONG' if is_long else 'SHORT'
            _nlb_on = bool(_cfg('NOLOSS_BYPASS_WT_5OF5_ENABLED', False, _exit_acct_top, symbol, _nlb_side_t))
            _nlb_pass = False
            if _nlb_on:
                _nlb_min = int(_cfg('NOLOSS_BYPASS_WT_5OF5_MIN_TFS', 5, _exit_acct_top, symbol, _nlb_side_t))
                _nlb_src = indicators or i
                _nlb_against = 0
                for _tf in ('5m', '15m', '1h', '4h', 'D'):
                    _w1 = float(_nlb_src.get(f'wt1_{_tf}', 0) or 0)
                    _w2 = float(_nlb_src.get(f'wt2_{_tf}', 0) or 0)
                    if (is_long and _w1 < _w2) or (not is_long and _w1 > _w2):
                        _nlb_against += 1
                if _nlb_against >= _nlb_min:
                    _nlb_pass = True
                    logger.warning(f"[NOLOSS_BYPASS_WT_5OF5] {symbol} {'L' if is_long else 'S'}: {_nlb_against}/5 WT TFs against + gain={gain:.2f}% — BYPASSING NOLOSS, allowing subsequent exit paths")
            if not _nlb_pass:
                if gain < 0 or gain % 1 < 0.05:  # only log occasionally to avoid spam
                    logger.info(f"[NOLOSS_HOLD_{_exit_acct_top}] {symbol} {'L' if is_long else 'S'}: g={gain:.2f}% < noloss={_noloss_min_ts:.2f}% — holding (ratio is the hedge; SRS would have fired above)")
                return False, f"NOLOSS_HOLD_{_exit_acct_top}(g={gain:.2f}%<{_noloss_min_ts:.1f}%)", 0
        # NO PERCENTAGE GATES. Technicals decide exits. Period.
        # User directive 2026-04-10: NO trailing stops. Stocks exit ONLY when 1h/4h/D
        # delta slows down. Configure DeltaTracker tf_weights so LTF (5m/15m) don't
        # dominate the exit computation — handled by `DELTA_EXIT_TF_WEIGHTS_STOCK` cfg.
        # WT crossunder on 1h/4h/D acts as final resort if delta missed the slowdown.
        # Delta engine exit — skipped for options (handled in dedicated options block below)
        _delta_exit_gate = config.DELTA_ENGINE_ENABLED and config.DELTA_EXIT_ENABLED
        if not _is_opts_check and _delta_exit_gate and self.trade_manager.delta_tracker:
            _d_ind = indicators if indicators else i
            _d_pos_state = {"side": "LONG" if is_long else "SHORT"} if qty > 0 else None
            _d_sig = self.trade_manager.delta_tracker.update(symbol, _d_ind, _d_pos_state)
            if _d_sig and ((is_long and _d_sig.exit_long) or (not is_long and _d_sig.exit_short)):
                # STRICT_NO_LOSS gate (2026-04-17): delta can signal exit but we REFUSE to close at a loss.
                # System was closing GLD -0.09%, COPX -1.09%, XOM -3.34%, TTD -9.96% on "delta exit" signals.
                # L/S ratio is the hedge — don't realize losses. Wait for gain >= NOLOSS floor.
                _noloss_min = float(getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0))
                if _noloss_min > 0 and gain < _noloss_min:
                    logger.info(f"[DELTA_EXIT_BLOCKED_NOLOSS] {symbol} {'L' if is_long else 'S'}: zone={_d_sig.zone} gain={gain:.2f}% < NOLOSS={_noloss_min:.2f}% — HOLDING (ratio hedges, no realized loss)")
                else:
                    _zr = _d_sig.zone_reason or f"bs={_d_sig.bull_speed:.1f}_es={_d_sig.bear_speed:.1f}_btf={_d_sig.bull_tf_count}_etf={_d_sig.bear_tf_count}"
                    _reason = f"DELTA_EXIT_{_d_sig.zone}_{_zr}"
                    logger.warning(f"[DELTA_EXIT] {symbol} {'L' if is_long else 'S'}: zone={_d_sig.zone} {_zr} gain={gain:.2f}% hold={hold_time_min:.0f}m")
                    return True, f"{_reason}_g={gain:.2f}%_hold{hold_time_min:.0f}m_MANDATORY_REENTRY", qty
            # ═══ WT CROSSUNDER FINAL RESORT ═══ (user: "if delta fucked up")
            # RULE: LTF (5m for stocks / 3m for crypto) must be going down
            #   PLUS 15m must be going down OR be above 95 (overbought)
            #   PLUS at least 1 of (1h, 4h, D) must be negative
            # This prevents false exits from HTF noise on forward-filled data.
            _wt1_5m = float((_d_ind or {}).get('wt1_5m', (_d_ind or {}).get('wt1_3m', 0)) or 0)
            _wt2_5m = float((_d_ind or {}).get('wt2_5m', (_d_ind or {}).get('wt2_3m', 0)) or 0)
            _wt1_15m = float((_d_ind or {}).get('wt1_15m', 0) or 0)
            _wt2_15m = float((_d_ind or {}).get('wt2_15m', 0) or 0)
            _wt1_1h = float((_d_ind or {}).get('wt1_1h', 0) or 0)
            _wt2_1h = float((_d_ind or {}).get('wt2_1h', 0) or 0)
            _wt1_4h = float((_d_ind or {}).get('wt1_4h', 0) or 0)
            _wt2_4h = float((_d_ind or {}).get('wt2_4h', 0) or 0)
            _wt1_D = float((_d_ind or {}).get('wt1_D', 0) or 0)
            _wt2_D = float((_d_ind or {}).get('wt2_D', 0) or 0)
            if is_long:
                # LTF: 5m/3m wt1 crossed under wt2 (going down)
                _ltf_down = _wt1_5m < _wt2_5m
                # 15m: going down OR overbought (wt1 > 95)
                _15m_confirm = (_wt1_15m < _wt2_15m) or (_wt1_15m > 95)
                # HTF: at least 1 of 1h/4h/D against
                _htf_against = (_wt1_1h < _wt2_1h) or (_wt1_4h < _wt2_4h) or (_wt1_D < _wt2_D)
                if _ltf_down and _15m_confirm and _htf_against:
                    _pp_up_d, _pp_dn_d, _, _ = _parabolic_state(_d_ind or {}, config)
                    if _pp_up_d:
                        logger.warning(f"🌟 [WT_CROSSUNDER_FINAL_PARABOLIC_BYPASS] {symbol} L: parabolic uptrend → skip close, let LONG ride")
                    else:
                        _xu_1h = _wt1_1h < _wt2_1h
                        _xu_4h = _wt1_4h < _wt2_4h
                        _xu_D = _wt1_D < _wt2_D
                        logger.warning(f"[WT_CROSSUNDER_FINAL] {symbol} L: 5m={_wt1_5m<_wt2_5m} 15m={_wt1_15m<_wt2_15m}(wt1={_wt1_15m:.0f}) 1h={_xu_1h} 4h={_xu_4h} D={_xu_D} — gain={gain:.2f}% hold={hold_time_min:.0f}m")
                        return True, f"WT_CROSSUNDER_FINAL_5m_15m_1h{_xu_1h}_4h{_xu_4h}_D{_xu_D}_g{gain:.2f}%_hold{hold_time_min:.0f}m_MANDATORY_REENTRY", qty
            else:
                # SHORT exit: LTF going up (against short)
                _ltf_up = _wt1_5m > _wt2_5m
                # 15m: going up OR oversold (wt1 < -95)
                _15m_confirm = (_wt1_15m > _wt2_15m) or (_wt1_15m < -95)
                # HTF: at least 1 of 1h/4h/D against short
                _htf_against = (_wt1_1h > _wt2_1h) or (_wt1_4h > _wt2_4h) or (_wt1_D > _wt2_D)
                if _ltf_up and _15m_confirm and _htf_against:
                    _pp_up_d, _pp_dn_d, _, _ = _parabolic_state(_d_ind or {}, config)
                    if _pp_dn_d:
                        logger.warning(f"🌟 [WT_CROSSOVER_FINAL_PARABOLIC_BYPASS] {symbol} S: parabolic downtrend → skip close, let SHORT ride")
                    else:
                        _xo_1h = _wt1_1h > _wt2_1h
                        _xo_4h = _wt1_4h > _wt2_4h
                        _xo_D = _wt1_D > _wt2_D
                        logger.warning(f"[WT_CROSSOVER_FINAL] {symbol} S: 5m={_wt1_5m>_wt2_5m} 15m={_wt1_15m>_wt2_15m}(wt1={_wt1_15m:.0f}) 1h={_xo_1h} 4h={_xo_4h} D={_xo_D} — gain={gain:.2f}% hold={hold_time_min:.0f}m")
                        return True, f"WT_CROSSOVER_FINAL_5m_15m_1h{_xo_1h}_4h{_xo_4h}_D{_xo_D}_g{gain:.2f}%_hold{hold_time_min:.0f}m_MANDATORY_REENTRY", qty
        # RZ EXIT standalone — fires independently when DELTA_ENGINE is OFF but RZ zones are enabled
        if not _is_opts_check and not _delta_exit_gate and getattr(config, 'RZ_EXIT_ENABLED', True) and self.trade_manager.delta_tracker:
            _rz_ind = indicators if indicators else i
            _rz_pos_state = {"side": "LONG" if is_long else "SHORT"} if qty > 0 else None
            _rz_sig = self.trade_manager.delta_tracker.update(symbol, _rz_ind, _rz_pos_state)
            if _rz_sig and ((is_long and _rz_sig.exit_long) or (not is_long and _rz_sig.exit_short)):
                _rz_zone = getattr(_rz_sig, 'zone', '') or ''
                _rz_zr = _rz_sig.zone_reason or f"zone={_rz_zone}"
                logger.warning(f"[RZ_EXIT_STANDALONE] {symbol} {'L' if is_long else 'S'}: zone={_rz_zone} {_rz_zr} gain={gain:.2f}% hold={hold_time_min:.0f}m")
                return True, f"RZ_EXIT_{_rz_zone}_{_rz_zr}_g={gain:.2f}%_hold{hold_time_min:.0f}m", qty
        # WT CROSSUNDER FINAL standalone — fires independently when DELTA_ENGINE is OFF
        if not _is_opts_check and not _delta_exit_gate:
            _xu_ind = indicators if indicators else i
            _wt1_5m = float((_xu_ind or {}).get('wt1_5m', (_xu_ind or {}).get('wt1_3m', 0)) or 0)
            _wt2_5m = float((_xu_ind or {}).get('wt2_5m', (_xu_ind or {}).get('wt2_3m', 0)) or 0)
            _wt1_15m = float((_xu_ind or {}).get('wt1_15m', 0) or 0)
            _wt2_15m = float((_xu_ind or {}).get('wt2_15m', 0) or 0)
            _wt1_1h = float((_xu_ind or {}).get('wt1_1h', 0) or 0)
            _wt2_1h = float((_xu_ind or {}).get('wt2_1h', 0) or 0)
            _wt1_4h = float((_xu_ind or {}).get('wt1_4h', 0) or 0)
            _wt2_4h = float((_xu_ind or {}).get('wt2_4h', 0) or 0)
            _wt1_D = float((_xu_ind or {}).get('wt1_D', 0) or 0)
            _wt2_D = float((_xu_ind or {}).get('wt2_D', 0) or 0)
            if is_long:
                _ltf_down = _wt1_5m < _wt2_5m
                _15m_confirm = (_wt1_15m < _wt2_15m) or (_wt1_15m > 95)
                _htf_against = (_wt1_1h < _wt2_1h) or (_wt1_4h < _wt2_4h) or (_wt1_D < _wt2_D)
                if _ltf_down and _15m_confirm and _htf_against:
                    _pp_up_x, _pp_dn_x, _, _ = _parabolic_state(_xu_ind or {}, config)
                    if _pp_up_x:
                        logger.warning(f"🌟 [WT_CROSSUNDER_FINAL_PARABOLIC_BYPASS] {symbol} L (standalone): parabolic uptrend → skip close")
                    else:
                        _xu_1h = _wt1_1h < _wt2_1h
                        _xu_4h = _wt1_4h < _wt2_4h
                        _xu_D = _wt1_D < _wt2_D
                        logger.warning(f"[WT_CROSSUNDER_FINAL] {symbol} L: 5m={_wt1_5m<_wt2_5m} 15m={_wt1_15m<_wt2_15m}(wt1={_wt1_15m:.0f}) 1h={_xu_1h} 4h={_xu_4h} D={_xu_D} — gain={gain:.2f}% hold={hold_time_min:.0f}m (standalone)")
                        return True, f"WT_CROSSUNDER_FINAL_5m_15m_1h{_xu_1h}_4h{_xu_4h}_D{_xu_D}_g{gain:.2f}%_hold{hold_time_min:.0f}m_MANDATORY_REENTRY", qty
            else:
                _ltf_up = _wt1_5m > _wt2_5m
                _15m_confirm = (_wt1_15m > _wt2_15m) or (_wt1_15m < -95)
                _htf_against = (_wt1_1h > _wt2_1h) or (_wt1_4h > _wt2_4h) or (_wt1_D > _wt2_D)
                if _ltf_up and _15m_confirm and _htf_against:
                    _pp_up_x, _pp_dn_x, _, _ = _parabolic_state(_xu_ind or {}, config)
                    if _pp_dn_x:
                        logger.warning(f"🌟 [WT_CROSSOVER_FINAL_PARABOLIC_BYPASS] {symbol} S (standalone): parabolic downtrend → skip close")
                    else:
                        _xo_1h = _wt1_1h > _wt2_1h
                        _xo_4h = _wt1_4h > _wt2_4h
                        _xo_D = _wt1_D > _wt2_D
                        logger.warning(f"[WT_CROSSOVER_FINAL] {symbol} S: 5m={_wt1_5m>_wt2_5m} 15m={_wt1_15m>_wt2_15m}(wt1={_wt1_15m:.0f}) 1h={_xo_1h} 4h={_xo_4h} D={_xo_D} — gain={gain:.2f}% hold={hold_time_min:.0f}m (standalone)")
                        return True, f"WT_CROSSOVER_FINAL_5m_15m_1h{_xo_1h}_4h{_xo_4h}_D{_xo_D}_g{gain:.2f}%_hold{hold_time_min:.0f}m_MANDATORY_REENTRY", qty
        # OPTIONS: exit ONLY on Daily reversal (CLAUDE.md rule, not stock ST scalping)
        _is_options_pos = hasattr(position, 'option_type') and getattr(position, 'option_type', None)
        if _is_options_pos:
            _o_ind = indicators if indicators else i
            _wt1_D = float(_o_ind.get('wt1_D', 0) or 0)
            _wt2_D = float(_o_ind.get('wt2_D', 0) or 0)
            _wt_cross_D = str(_o_ind.get('wt_cross_D', ''))
            _ha_D = str(_o_ind.get('ha_D', '')).lower()
            # LONG option exit: D WT cross bear + HA red on D
            # SHORT option exit: D WT cross bull + HA green on D
            _d_reversal_long = is_long and _wt_cross_D == "BEAR" and _wt1_D < _wt2_D and _ha_D == "red"
            _d_reversal_short = (not is_long) and _wt_cross_D == "BULL" and _wt1_D > _wt2_D and _ha_D == "green"
            if _d_reversal_long or _d_reversal_short:
                _reason = f"OPTIONS_D_REVERSAL_wt1D={_wt1_D:.1f}_wt2D={_wt2_D:.1f}_cross={_wt_cross_D}_ha={_ha_D}_g={gain:.2f}%"
                logger.warning(f"[OPTIONS_EXIT] {symbol} {'L' if is_long else 'S'}: {_reason}")
                return True, _reason, qty
            return False, f"OPTIONS_HOLD_D_intact_cross={_wt_cross_D}_ha={_ha_D}_g={gain:.2f}%", 0
        _exit_ind = indicators if indicators else i
        _exit_score, _exit_reason = wt_dc_score_exit(_exit_ind, is_long, current_price, cfg=config)
        _exit_threshold = getattr(config, 'WT_DC_EXIT_THRESHOLD', 20)
        # STALE DATA GUARD: don't fire exits on indicators > 10min stale (prevents phantom exits)
        _ind_age = float(_exit_ind.get('age_5m', 0) or 0)
        _stale_max = getattr(config, 'WT_DC_EXIT_STALE_MAX_S', 600)
        if _ind_age > _stale_max:
            return False, f"STALE_DATA_age={_ind_age:.0f}s>{_stale_max}s", 0
        if _exit_score >= _exit_threshold:
            _pp_up_e, _pp_dn_e, _, _ = _parabolic_state(_exit_ind or {}, config)
            if (is_long and _pp_up_e) or ((not is_long) and _pp_dn_e):
                logger.warning(f"🌟 [WT_DC_EXIT_PARABOLIC_BYPASS] {symbol} {'L' if is_long else 'S'}: score={_exit_score:.0f} gain={gain:.2f}% — parabolic {'UP' if is_long else 'DOWN'}trend, skipping close, let trend run")
            else:
                logger.warning(f"[WT_DC_EXIT] {symbol} {'L' if is_long else 'S'}: score={_exit_score:.0f} gain={gain:.2f}% {_exit_reason[:80]} → EXIT_MANDATORY_REENTRY (REENTRY_MONITOR will reopen when ALL exit conditions clear)")
                return True, f"WT_DC_EXIT_{_exit_score:.0f}_g={gain:.2f}%_{_exit_reason[:60]}_MANDATORY_REENTRY", qty

        # --------------------------------------------------
        # 2026-04-08: WT/DC SCORER IS THE ONLY EXIT AUTHORITY
        # If scorer said hold (score < threshold), NOTHING below should override.
        # All legacy exits (K5M, ALGO, STRUCT_BREAK, IBS, etc.) are SUBORDINATE.
        # Only exception: position max hold timeout (8 hours).
        # --------------------------------------------------
        if _exit_score < _exit_threshold:
            # VARIANCE_FIX 2026-04-14 — canonical-switch exit gates run BEFORE SCORER_HOLD early return.
            # WT_EXIT_TFS/MIN_TFS/MI_EXIT_ENABLED were wired into evaluate_multi_tf_exit, but that code
            # is never reached because the wt_dc_score_exit early-returns above. Apply them here so the
            # switches actually gate exits. All gates are off/inert by default — only toggling them on
            # changes behaviour.
            _vf_exit_side = "LONG" if is_long else "SHORT"
            _vf_acct = current_account.get('')
            # WT_EXIT_TFS / WT_EXIT_MIN_TFS — only fires if WT_EXIT_VETO_ENABLED_TRADIER is on.
            if getattr(config, 'WT_EXIT_VETO_ENABLED_TRADIER', False):
                try:
                    _vf_wt_tfs_str = _cfg('WT_EXIT_TFS_TRADIER', '5m+15m+1h+4h+D', _vf_acct, symbol, _vf_exit_side)
                    _vf_wt_min_tfs = int(_cfg('WT_EXIT_MIN_TFS_TRADIER', 4, _vf_acct, symbol, _vf_exit_side) or 4)
                    _vf_wt_tfs = [t.strip() for t in str(_vf_wt_tfs_str).replace('+', ',').split(',') if t.strip()]
                    _vf_wt_against = 0
                    _vf_against_tfs = []
                    for _tf in _vf_wt_tfs:
                        _w1 = float(i.get(f'wt1_{_tf}', 0) or 0)
                        _w2 = float(i.get(f'wt2_{_tf}', 0) or 0)
                        if is_long and _w1 < _w2:
                            _vf_wt_against += 1; _vf_against_tfs.append(_tf)
                        elif not is_long and _w1 > _w2:
                            _vf_wt_against += 1; _vf_against_tfs.append(_tf)
                    if _vf_wt_min_tfs > 0 and _vf_wt_against >= _vf_wt_min_tfs and len(_vf_wt_tfs) > 0:
                        logger.warning(f"[VARIANCE_FIX_WT_EXIT] {symbol} {_vf_exit_side}: {_vf_wt_against}/{len(_vf_wt_tfs)}>={_vf_wt_min_tfs} ({'+'.join(_vf_against_tfs)}) g={gain:.2f}%")
                        return True, f"WT_EXIT_TFS_VARFIX({_vf_wt_against}/{len(_vf_wt_tfs)}>={_vf_wt_min_tfs}:{'+'.join(_vf_against_tfs)})_g={gain:.2f}%", qty
                except Exception:
                    pass
            # MI_EXIT_ENABLED — only fires if MI_EXIT_VETO_ENABLED_TRADIER is on (uses same 5-signal logic as evaluate_multi_tf_exit).
            if getattr(config, 'MI_EXIT_VETO_ENABLED_TRADIER', False) and _cfg('MI_EXIT_ENABLED_TRADIER', False, _vf_acct, symbol, _vf_exit_side):
                _vf_mi_min_gain = float(getattr(config, 'MI_MIN_GAIN_EXIT_TRADIER', 0.0) or 0.0)
                if gain >= _vf_mi_min_gain:
                    _vf_mi_signals = 0
                    _vf_mi_reasons = []
                    if is_long:
                        if i.get('wt_peak_structure_1h', '') == 'LH': _vf_mi_signals += 1; _vf_mi_reasons.append("LH_1h")
                        if i.get('wt_peak_structure_4h', '') == 'LH': _vf_mi_signals += 1; _vf_mi_reasons.append("LH_4h")
                        if i.get('wt_momentum_state_1h', '') == 'EXHAUST_UP': _vf_mi_signals += 1; _vf_mi_reasons.append("EXH_1h")
                        if i.get('wt_momentum_state_4h', '') == 'EXHAUST_UP': _vf_mi_signals += 1; _vf_mi_reasons.append("EXH_4h")
                        if i.get('wt_divergence_1h', None) == 'BEAR': _vf_mi_signals += 1; _vf_mi_reasons.append("DIV_BEAR_1h")
                        if i.get('wt_divergence_4h', None) == 'BEAR': _vf_mi_signals += 1; _vf_mi_reasons.append("DIV_BEAR_4h")
                    else:
                        if i.get('wt_trough_structure_1h', '') == 'HL': _vf_mi_signals += 1; _vf_mi_reasons.append("HL_1h")
                        if i.get('wt_trough_structure_4h', '') == 'HL': _vf_mi_signals += 1; _vf_mi_reasons.append("HL_4h")
                        if i.get('wt_momentum_state_1h', '') == 'EXHAUST_DOWN': _vf_mi_signals += 1; _vf_mi_reasons.append("EXH_1h")
                        if i.get('wt_momentum_state_4h', '') == 'EXHAUST_DOWN': _vf_mi_signals += 1; _vf_mi_reasons.append("EXH_4h")
                        if i.get('wt_divergence_1h', None) == 'BULL': _vf_mi_signals += 1; _vf_mi_reasons.append("DIV_BULL_1h")
                        if i.get('wt_divergence_4h', None) == 'BULL': _vf_mi_signals += 1; _vf_mi_reasons.append("DIV_BULL_4h")
                    _vf_mi_min = int(getattr(config, 'MI_TF_AGREE_MIN_TRADIER', 3) or 3)
                    if _vf_mi_signals >= _vf_mi_min:
                        logger.warning(f"[VARIANCE_FIX_MI_EXIT] {symbol} {_vf_exit_side}: {_vf_mi_signals}/{_vf_mi_min} ({'+'.join(_vf_mi_reasons)}) g={gain:.2f}%")
                        return True, f"MI_EXIT_VARFIX({_vf_mi_signals}/{_vf_mi_min},g={gain:.2f}%,{'+'.join(_vf_mi_reasons)})", qty
            # STDEV_BB_RZ_EXIT: exit when pctb crosses FROM ≥ 1.0 back to < 1.0 (failed breakout rejection)
            if getattr(config, 'STDEV_BB_RZ_EXIT_ENABLED', False):
                _brz_tf = str(getattr(config, 'STDEV_BB_RZ_EXIT_TF', 'D'))
                _brz_pctb_now = float(i.get(f'bb_pct_b_{_brz_tf}', 0.5) or 0.5)
                _brz_pctb_prev = float(i.get(f'bb_pct_b_{_brz_tf}_prev', _brz_pctb_now) or _brz_pctb_now)
                _brz_vel = float(i.get('wt_velocity_1h', 0) or 0)
                if is_long and _brz_pctb_prev >= 1.0 and _brz_pctb_now < 1.0 and _brz_vel < 0:
                    logger.warning(f"[STDEV_BB_RZ_EXIT] {symbol} LONG: pctb {_brz_pctb_prev:.3f}→{_brz_pctb_now:.3f} vel={_brz_vel:.1f} g={gain:.2f}%")
                    return True, f"STDEV_BB_RZ_EXIT_{_brz_tf}_pctb={_brz_pctb_now:.3f}_g={gain:.2f}%", qty
                if not is_long and _brz_pctb_prev <= 0.0 and _brz_pctb_now > 0.0 and _brz_vel > 0:
                    logger.warning(f"[STDEV_BB_RZ_EXIT] {symbol} SHORT: pctb {_brz_pctb_prev:.3f}→{_brz_pctb_now:.3f} vel={_brz_vel:.1f} g={gain:.2f}%")
                    return True, f"STDEV_BB_RZ_EXIT_{_brz_tf}_pctb={_brz_pctb_now:.3f}_g={gain:.2f}%", qty
            # STDEV_REJECT_EXIT: exit when price approached band but failed to hold (pre-breakout rejection)
            if getattr(config, 'STDEV_REJECT_EXIT_ENABLED', False):
                _sre_tf = str(getattr(config, 'STDEV_REJECT_EXIT_TF', 'D'))
                _sre_zone = float(getattr(config, 'STDEV_REJECT_EXIT_ZONE', 0.80))
                _sre_ret = float(getattr(config, 'STDEV_REJECT_EXIT_RETURN', 0.65))
                _sre_pctb_now = float(i.get(f'bb_pct_b_{_sre_tf}', 0.5) or 0.5)
                _sre_pctb_prev = float(i.get(f'bb_pct_b_{_sre_tf}_prev', _sre_pctb_now) or _sre_pctb_now)
                _sre_vel = float(i.get('wt_velocity_1h', 0) or 0)
                if is_long and _sre_pctb_prev >= _sre_zone and _sre_pctb_now < _sre_ret and _sre_vel < 0:
                    logger.warning(f"[STDEV_REJECT_EXIT] {symbol} LONG: pctb {_sre_pctb_prev:.3f}→{_sre_pctb_now:.3f} zone={_sre_zone} vel={_sre_vel:.1f} g={gain:.2f}%")
                    return True, f"STDEV_REJECT_EXIT_{_sre_tf}_pctb={_sre_pctb_now:.3f}_g={gain:.2f}%", qty
                if not is_long and _sre_pctb_prev <= (1.0 - _sre_zone) and _sre_pctb_now > (1.0 - _sre_ret) and _sre_vel > 0:
                    logger.warning(f"[STDEV_REJECT_EXIT] {symbol} SHORT: pctb {_sre_pctb_prev:.3f}→{_sre_pctb_now:.3f} zone={1.0-_sre_zone:.2f} vel={_sre_vel:.1f} g={gain:.2f}%")
                    return True, f"STDEV_REJECT_EXIT_{_sre_tf}_pctb={_sre_pctb_now:.3f}_g={gain:.2f}%", qty
            # Scorer says HOLD — check max hold timeout if enabled
            if getattr(config, 'EXIT_MAX_HOLD_ENABLED', False) and hold_time_min > getattr(config, 'EXIT_MAX_HOLD_MINUTES', 99999):
                return True, f"MAX_HOLD_TIMEOUT_{hold_time_min:.0f}min_scorer={_exit_score:.0f}", qty
            return False, f"SCORER_HOLD(s={_exit_score:.0f}<{_exit_threshold})", 0
        # --------------------------------------------------
        # Below: legacy exit paths, each gated by config flag.
        # Scorer already said EXIT above (score >= threshold) so these are redundant.
        # Kept for easy re-enable via config if needed for testing.
        # ════════════════════════════════════════════════════════════════
        # LEGACY EXIT PATHS — all gated by config flags (config_tradier.py)
        # These only fire if scorer ALREADY said EXIT (score >= threshold).
        # If scorer says HOLD, we return False above and NONE of this runs.
        #
        # EXIT_CONV_FAIL_ENABLED      = False  — convergence failure early exit
        # EXIT_HTF_QUICK_TP_ENABLED   = True   — 1h exhausted + LTF turning (PROVEN)
        # EXIT_MI_ENABLED             = False  — momentum interception sub-signals
        # EXIT_BOUNCE_TOP_ENABLED     = False  — bounce-top loss exit (% stop in disguise)
        # EXIT_STRUCT_DC_BREAK_ENABLED = True  — DC structural break (PROVEN)
        # EXIT_K5M_BOUNCE_ENABLED     = False  — K5M stoch bounce + low break
        # EXIT_HARD_DROP_5M_ENABLED   = False  — price < prev 5m low
        # EXIT_ALGO_SCORE_ENABLED     = False  — old calculate_signal_score exit
        # EXIT_STRUCT_BREAK_5M_ENABLED = False — 5m LH/HL structure
        # EXIT_IBS_EXHAUSTION_ENABLED = False  — internal bar strength extreme
        # EXIT_SENTIMENT_ENABLED      = False  — sentiment collapse
        # EXIT_MAX_HOLD_ENABLED       = False  — forced close after N minutes
        # ════════════════════════════════════════════════════════════════
        _noloss_min_t = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 1.0)

        # CONVERGENCE FAILURE FAST EXIT — exit at breakeven when BTB thesis breaks
        # If momentum reverses against us early → exit at tiny profit, don't let it become a big loser
        # Frees capital for the opposite-side trade (breakdown/breakout)
        _wt_vel_1h = float(i.get('wt_velocity_1h', 0) or 0)
        _wt_bull_1h = float(i.get('wt1_1h', 0) or 0) > float(i.get('wt2_1h', 0) or 0)
        _k_1h = float(i.get('stoch_k_1h', 50) or 50); _d_1h = float(i.get('stoch_d_1h', 50) or 50)
        if False and gain > 0.05 and gain < getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0):  # DISABLED: was closing at 0.05-3% gain (below noloss_min). STRICT_NO_LOSS means hold until 3%+.
            _k_1h = float(i.get('stoch_k_1h', 50) or 50); _d_1h = float(i.get('stoch_d_1h', 50) or 50)
            _wt_against = (is_long and _wt_vel_1h < -3.0 and not _wt_bull_1h) or (not is_long and _wt_vel_1h > 3.0 and _wt_bull_1h)
            _k_against = (is_long and _k_1h < _d_1h) or (not is_long and _k_1h > _d_1h)
            if _wt_against and _k_against:
                # Check if opposite side is building conviction (convergence forming for the other side)
                _opp = analyze_multi_tf_state_tradier(indicators, not is_long, current_price, self.config)
                _opp_conv = _opp.get('convergence_count', 0); _opp_b = _opp.get('bottom_score', 0)
                if _opp_conv >= 2 or _opp_b > 20:
                    return True, f"CONV_FAIL_EXIT(g={gain:.2f}%,wt_vel={_wt_vel_1h:.1f},opp_conv={_opp_conv},opp_b={_opp_b:.0f})", qty
                elif gain > 0.5:
                    return True, f"THESIS_BREAK_EXIT(g={gain:.2f}%,wt_vel={_wt_vel_1h:.1f},k={_k_1h:.0f}<d={_d_1h:.0f})", qty
        # ═══════════════════════════════════════════════════════════════
        # MOMENTUM INTERCEPTION EXIT — 5 sub-signals vote before D/W flip
        # ═══════════════════════════════════════════════════════════════
        _exit_side = "LONG" if is_long else "SHORT"
        _exit_acct = current_account.get('')
        _mi_enabled_t = _cfg('MI_EXIT_ENABLED_TRADIER', False, _exit_acct, symbol, _exit_side)
        _mi_min_gain_t = getattr(config, 'MI_MIN_GAIN_EXIT_TRADIER', 0.50)
        if _mi_enabled_t and gain >= _mi_min_gain_t:
            _mi_signals_t = 0
            _mi_reasons_t = []
            _wt_peak_s_1h = i.get('wt_peak_structure_1h', '')
            _wt_trough_s_1h = i.get('wt_trough_structure_1h', '')
            _wt_peak_s_4h = i.get('wt_peak_structure_4h', '')
            _wt_trough_s_4h = i.get('wt_trough_structure_4h', '')
            _wt_mom_1h_t = i.get('wt_momentum_state_1h', '')
            _wt_mom_4h_t = i.get('wt_momentum_state_4h', '')
            _wt_div_1h_t = i.get('wt_divergence_1h', None)
            _wt_div_4h_t = i.get('wt_divergence_4h', None)
            _wt_wave_1h_t = i.get('wt_wave_phase_1h', '')
            if getattr(config, 'MI_STRUCT_EXIT_ENABLED_TRADIER', True):
                if is_long:
                    if _wt_peak_s_1h == 'LH': _mi_signals_t += 1; _mi_reasons_t.append("LH_1h")
                    if _wt_peak_s_4h == 'LH': _mi_signals_t += 1; _mi_reasons_t.append("LH_4h")
                else:
                    if _wt_trough_s_1h == 'HL': _mi_signals_t += 1; _mi_reasons_t.append("HL_1h")
                    if _wt_trough_s_4h == 'HL': _mi_signals_t += 1; _mi_reasons_t.append("HL_4h")
            if getattr(config, 'MI_EXHAUST_EXIT_ENABLED_TRADIER', True):
                _mi_exh_t = 'EXHAUST_UP' if is_long else 'EXHAUST_DOWN'
                if _wt_mom_1h_t == _mi_exh_t: _mi_signals_t += 1; _mi_reasons_t.append("EXH_1h")
                if _wt_mom_4h_t == _mi_exh_t: _mi_signals_t += 1; _mi_reasons_t.append("EXH_4h")
            if getattr(config, 'MI_DIV_EXIT_ENABLED_TRADIER', True):
                _mi_div_t = 'BEAR' if is_long else 'BULL'
                if _wt_div_1h_t == _mi_div_t: _mi_signals_t += 1; _mi_reasons_t.append(f"DIV_{_mi_div_t}_1h")
                if _wt_div_4h_t == _mi_div_t: _mi_signals_t += 1; _mi_reasons_t.append(f"DIV_{_mi_div_t}_4h")
            if getattr(config, 'MI_VELOCITY_EXIT_ENABLED_TRADIER', True):
                _mi_vel_ag_t = 0
                _wt_vel_5m_mi = float(i.get('wt_velocity_5m', 0) or 0)
                _wt_vel_15m_mi = float(i.get('wt_velocity_15m', 0) or 0)
                _wt_vel_1h_mi = float(i.get('wt_velocity_1h', 0) or 0)
                if is_long:
                    if _wt_vel_5m_mi < -1.0: _mi_vel_ag_t += 1
                    if _wt_vel_15m_mi < -0.5: _mi_vel_ag_t += 1
                    if _wt_vel_1h_mi < -1.0: _mi_vel_ag_t += 1
                else:
                    if _wt_vel_5m_mi > 1.0: _mi_vel_ag_t += 1
                    if _wt_vel_15m_mi > 0.5: _mi_vel_ag_t += 1
                    if _wt_vel_1h_mi > 1.0: _mi_vel_ag_t += 1
                if _mi_vel_ag_t >= 2: _mi_signals_t += 1; _mi_reasons_t.append(f"VEL_DECEL_{_mi_vel_ag_t}tf")
            if getattr(config, 'MI_WAVE_EXIT_ENABLED_TRADIER', True):
                if _wt_wave_1h_t == 'CONTRACTING': _mi_signals_t += 1; _mi_reasons_t.append("WAVE_CONTRACT")
            _mi_min_t = getattr(config, 'MI_TF_AGREE_MIN_TRADIER', 3)
            if _mi_signals_t >= _mi_min_t:
                logger.warning(f"[MI_EXIT] {symbol} {'L' if is_long else 'S'}: {_mi_signals_t}/{_mi_min_t} signals ({'+'.join(_mi_reasons_t)}) g={gain:.2f}% → EXIT")
                return True, f"MI_EXIT({_mi_signals_t}/{_mi_min_t},g={gain:.2f}%,{'+'.join(_mi_reasons_t)})", qty
        # HTF QUICK TP — 1h exhausted but 4h trend intact → take profit, reenter bigger on resume
        # Stocks: 1h K>90 + short-TFs turning down + 4h K<60 still up → exit for quick TP
        if gain >= _noloss_min_t:
            _k4h_s = float(i.get('stoch_k_4h', 50) or 50); _d4h_s = float(i.get('stoch_d_4h', 50) or 50)
            _k4h_prev_s = float(i.get('stoch_k_4h_prev', _k4h_s) or _k4h_s)
            _wt_bull_4h_s = float(i.get('wt1_4h', 0) or 0) > float(i.get('wt2_4h', 0) or 0)
            _k5m = float(i.get('stoch_k_5m', 50) or 50); _k5m_prev = float(i.get('stoch_k_5m_prev', 50) or 50)
            _k15m_s = float(i.get('stoch_k_15m', 50) or 50); _k15m_prev_s = float(i.get('stoch_k_15m_prev', 50) or 50)
            _ltf_down = _k5m < _k5m_prev and _k15m_s < _k15m_prev_s
            _ltf_up = _k5m > _k5m_prev and _k15m_s > _k15m_prev_s
            if is_long and _k_1h > 90 and _ltf_down and (_wt_bull_4h_s or _k4h_s > _d4h_s) and _k4h_s < 60:
                return True, f"HTF_QUICK_TP_L(g={gain:.2f}%,k1h={_k_1h:.0f}>90,ltf↓,k4h={_k4h_s:.0f}<60↑)_REENTER_1.5x", qty
            elif not is_long and _k_1h < 10 and _ltf_up and (not _wt_bull_4h_s or _k4h_s < _d4h_s) and _k4h_s > 40:
                return True, f"HTF_QUICK_TP_S(g={gain:.2f}%,k1h={_k_1h:.0f}<10,ltf↑,k4h={_k4h_s:.0f}>40↓)_REENTER_1.5x", qty
        # BACKTEST_CHANGE_T51: NO-LOSS NATURAL EXIT — block all exits below min profit %
        # Tradier backtest: HODL LONG D at 2% TP → 95.7% WR, +47.85% avg return
        _noloss_min_t = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 1.0)
        # BOUNCE-TOP EXIT — V4 backtest proven: Sharpe -0.5 → +0.42 on 121 stocks 2yr
        # Instead of holding losers forever (capital trap), wait for price to bounce toward entry,
        # then exit at the TOP of the bounce (smallest possible loss).
        # Mandatory reentry plan follows: 150% at pullback, 200% at rising WT cross.
        # This is NOT a stop loss (exits at bottom). This exits at the BEST moment within a loss.
        _bt_enabled = False  # KILLED 2026-03-30: NO percentage-based exits EVER. Only technical exits (WT turn, volume die, DC reversal). This was a stop loss in disguise.
        _bt_min_hold = getattr(config, 'BOUNCE_TOP_MIN_HOLD_MINUTES', 1440.0)
        _bt_min_loss = getattr(config, 'BOUNCE_TOP_MIN_LOSS_PCT', -3.0)
        _bt_max_loss = getattr(config, 'BOUNCE_TOP_MAX_LOSS_PCT', -50.0)
        if _bt_enabled and gain < _bt_min_loss and gain > _bt_max_loss and hold_time_min > _bt_min_hold:
            _k5m_bt = float(i.get('stoch_k_5m', 50) or 50)
            _k15m_bt = float(i.get('stoch_k_15m', 50) or 50)
            _wt_vel_5m_bt = float(i.get('wt_velocity_5m', 0) or 0)
            _wt_vel_15m_bt = float(i.get('wt_velocity_15m', 0) or 0)
            _wt_bull_5m_bt = float(i.get('wt1_5m', 0) or 0) > float(i.get('wt2_5m', 0) or 0)
            _wt_bull_15m_bt = float(i.get('wt1_15m', 0) or 0) > float(i.get('wt2_15m', 0) or 0)
            _max_gain_bt = float(getattr(position, 'max_gain', 0) or 0)
            _min_gain_est = min(gain, _max_gain_bt) if _max_gain_bt < 0 else gain
            if is_long:
                _bouncing = (_wt_bull_5m_bt and _wt_vel_5m_bt > 1.0) or _k5m_bt > 40
                _topped = (not _wt_bull_5m_bt and _wt_vel_5m_bt < -1.0) or (not _wt_bull_15m_bt and _wt_vel_15m_bt < -1.0)
                _k_turned = _k5m_bt < 60 and _k15m_bt < 70
            else:
                _bouncing = ((not _wt_bull_5m_bt) and _wt_vel_5m_bt < -1.0) or _k5m_bt < 60
                _topped = (_wt_bull_5m_bt and _wt_vel_5m_bt > 1.0) or (_wt_bull_15m_bt and _wt_vel_15m_bt > 1.0)
                _k_turned = _k5m_bt > 40 and _k15m_bt > 30
            if _topped and _k_turned:
                return True, f"BOUNCE_TOP_EXIT(g={gain:.2f}%,held={hold_time_min:.0f}m,k5={_k5m_bt:.0f},wv5={_wt_vel_5m_bt:.1f})_REENTRY_PLAN_ACTIVE", qty
        if _noloss_min_t > 0 and gain < _noloss_min_t and gain > -0.01:
            return False, f"NOLOSS_HOLD({gain:.2f}%<{_noloss_min_t}%)_bcT51", 0
        # A. HARD PERCENTAGE STOP — DISABLED. STRICT_NO_LOSS: L/S ratio IS the hedge. NEVER close at a loss.
        # Was: if gain < -1.5: return True — THIS CAUSED ASTS -6%, SNDK -5%, STZ -2% LOSSES ON 2026-03-24
        # if gain < -1.5:
        #     return True, f"HARD_STOP_LOSS_MAX_PAIN_(-1.5%)_Actual:{gain:.2f}%_qty_{qty}", qty
        pass

        # B. TIME-BASED STRUCTURAL STOPS (cascading: tight→medium→wide)
        # 0-20 min : dc_low4_5m  (4-period tight) — wide buffer (0.4%) to hold firm
        # 20-60 min: dc_low_5m   (20-period 5m)   — standard buffer (0.15%)
        # 60+ min  : dc_low_1h   (20-period 1h)   — standard buffer (0.1%)
        # Early stop also requires the PREVIOUS 5m candle to have closed below (confirmation).
        close_5m_prev = float(i.get('close_5m_prev', current_price) or current_price)

        if is_long:
            if hold_time_min <= 20.0:
                dc_stop = i.get('dc_low4_5m', 0)
                level_name = "DC4_5m"
                buf = 0.004  # 0.4% — wide buffer, hold firm vs noise
                # Also require prev 5m candle to confirm break
                confirmed = close_5m_prev < dc_stop if dc_stop > 0 else False
            elif hold_time_min <= 60.0:
                dc_stop = i.get('dc_low_5m', 0)
                level_name = "DC_5m"
                buf = 0.0015
                confirmed = True
            else:
                dc_stop = i.get('dc_low_1h', 0)
                level_name = "DC_1h"
                buf = 0.001
                confirmed = True

            if dc_stop > 0 and current_price < dc_stop * (1 - buf) and confirmed and gain >= getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0):
                return True, f"STRUCT_BREAK_{level_name}_LOW_({dc_stop:.2f})_gain:{gain:.2f}%", qty
        else:
            if hold_time_min <= 20.0:
                dc_stop = i.get('dc_high4_5m', 999999)
                level_name = "DC4_5m"
                buf = 0.004
                close_5m_prev_h = float(i.get('close_5m_prev', current_price) or current_price)
                confirmed = close_5m_prev_h > dc_stop if dc_stop < 999999 else False
            elif hold_time_min <= 60.0:
                dc_stop = i.get('dc_high_5m', 999999)
                level_name = "DC_5m"
                buf = 0.0015
                confirmed = True
            else:
                dc_stop = i.get('dc_high_1h', 999999)
                level_name = "DC_1h"
                buf = 0.001
                confirmed = True

            if dc_stop < 999999 and current_price > dc_stop * (1 + buf) and confirmed and gain >= getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0):
                return True, f"STRUCT_BREAK_{level_name}_HIGH_({dc_stop:.2f})_gain:{gain:.2f}%", qty

        # --------------------------------------------------
        # 2. EMERGENCY SIZE KILL (The Fix for Huge Pos)
        # --------------------------------------------------
        pos_value = qty * current_price
        is_huge_position = pos_value > 20000.0

        # DISABLED: EMERGENCY_KILL_SIZE was closing at -0.5% loss — STRICT_NO_LOSS violation
        # if is_huge_position and gain < -0.5: pass

        # --------------------------------------------------
        # 3. SHIELD 
        # --------------------------------------------------
        # Shield weak technical exits: always under 20m; deadzone only up to 40m.
        is_in_deadzone = -0.3 < gain < 0.3
        if hold_time_min < 20.0 or (is_in_deadzone and hold_time_min < 40.0):
            return False, "", 0.0

        # --------------------------------------------------
        # 3.1. MULTI-TF EXIT SCORER — comprehensive technical exit
        # Checks divergences, velocity decel, DC fade, BB rejection, structure breaks
        # --------------------------------------------------
        _mt_exit, _mt_reason, _mt_score = self.evaluate_multi_tf_exit(i, is_long, gain, hold_time_min, current_price)
        _noloss_wt_mt = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 0.3)
        if _mt_exit and gain >= _noloss_wt_mt:
            logger.warning(f"[MULTI_TF_EXIT] {symbol} {'L' if is_long else 'S'}: {_mt_reason}")
            return True, _mt_reason, qty

        # --------------------------------------------------
        # 3.45. MULTI-TF WT EXIT CONFIRMATION GATE — V4 backtest proven
        # Entry requires multi-TF WT alignment. Exit must mirror this.
        # Count how many TFs have WT AGAINST the position direction.
        # If fewer than 2 TFs confirm exit → BLOCK the exit, let position mature.
        # This prevents 5m noise from killing positions that HTFs still support.
        # --------------------------------------------------
        _wt_bull_5m_ex = float(i.get('wt1_5m', 0) or 0) > float(i.get('wt2_5m', 0) or 0)
        _wt_bull_15m_ex = float(i.get('wt1_15m', 0) or 0) > float(i.get('wt2_15m', 0) or 0)
        _wt_bull_1h_ex = float(i.get('wt1_1h', 0) or 0) > float(i.get('wt2_1h', 0) or 0)
        _wt_bull_4h_ex = float(i.get('wt1_4h', 0) or 0) > float(i.get('wt2_4h', 0) or 0)
        if is_long:
            _exit_tf_against = sum([1 for b in [_wt_bull_5m_ex, _wt_bull_15m_ex, _wt_bull_1h_ex, _wt_bull_4h_ex] if not b])
        else:
            _exit_tf_against = sum([1 for b in [_wt_bull_5m_ex, _wt_bull_15m_ex, _wt_bull_1h_ex, _wt_bull_4h_ex] if b])
        _min_exit_tf = _cfg('MIN_EXIT_TF_AGAINST_TRADIER', 2, _exit_acct, symbol, _exit_side)
        _exit_confirmed = _exit_tf_against >= _min_exit_tf

        # --------------------------------------------------
        # 3.5. K5M MOMENTUM EXIT (BOUNCE-TURN / REAL DROP)
        # Now gated by multi-TF WT confirmation
        # --------------------------------------------------
        k_5m = float(i.get('stoch_k_5m', 50)); k_5m_prev = float(i.get('stoch_k_5m_prev', 50)); k_15m = float(i.get('stoch_k_15m', 50)); k_1h = float(i.get('stoch_k_1h', 50))
        low_5m = float(i.get('low_5m', 0)); low_5m_prev = float(i.get('low_5m_prev', 0)); high_5m = float(i.get('high_5m', 0)); high_5m_prev = float(i.get('high_5m_prev', 0))
        dc_low_5m = float(i.get('dc_low_5m', 0)); dc_high_5m = float(i.get('dc_high_5m', 0)); close_5m_prev = float(i.get('close_5m_prev', current_price) or current_price)
        # K5M exits: raised from gain>0.5 to gain>=noloss_min — was losing -$1,804 total on stocks
        # 2026-04-08: DISABLED K5M/HARD_DROP exits — these bypass the WT/DC scorer
        # and close on ANY 5m candle dip. Killed PLTR option at a loss on a minor pullback.
        # The scorer (threshold 30) is the ONLY exit authority now.
        # if gain >= _noloss_min_t and _exit_confirmed:
        #     K5M_BOUNCE_TURN / HARD_DROP_BELOW_PREV_LOW — ALL DISABLED
        pass
        _noloss_min_t = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0)
        if is_long and dc_low_5m > 0 and current_price < dc_low_5m and k_5m < 30 and gain >= _noloss_min_t:
            return True, f"K5M_REAL_DROP_DC_k5:{k_5m:.1f}<dc5:{dc_low_5m:.2f}_gain:{gain:.2f}%", qty
        if not is_long and dc_high_5m > 0 and current_price > dc_high_5m and k_5m > 70 and gain >= _noloss_min_t:
            return True, f"K5M_REAL_RISE_DC_k5:{k_5m:.1f}>dc5:{dc_high_5m:.2f}_gain:{gain:.2f}%", qty
        # BACKTEST_CHANGE_100: 5m candle structure break exit (Tradier: PF 2.53, Sharpe 4.45 at 15m+1h)
        # lower_high on 5m for LONG (structure breaking down) + k_5m falling = exit
        # higher_low on 5m for SHORT (structure turning up) + k_5m rising = exit
        _higher_low_5m = low_5m > 0 and low_5m_prev > 0 and low_5m > low_5m_prev
        _lower_high_5m = high_5m > 0 and high_5m_prev > 0 and high_5m < high_5m_prev
        if gain >= _noloss_min_t and _exit_confirmed:
            if is_long and _lower_high_5m and k_5m < k_5m_prev and hold_time_min > 15:
                return True, f"STRUCT_LH5M_EXIT_bc100_k5:{k_5m:.1f}<prev:{k_5m_prev:.1f}_hi5:{high_5m:.2f}<prev:{high_5m_prev:.2f}_gain:{gain:.2f}%_WT{_exit_tf_against}TF", qty
            elif not is_long and _higher_low_5m and k_5m > k_5m_prev and hold_time_min > 15:
                return True, f"STRUCT_HL5M_EXIT_bc100_k5:{k_5m:.1f}>prev:{k_5m_prev:.1f}_lo5:{low_5m:.2f}>prev:{low_5m_prev:.2f}_gain:{gain:.2f}%_WT{_exit_tf_against}TF", qty

        # --------------------------------------------------
        # 3.6. IBS EXIT (Internal Bar Strength)
        # Quantitativo research: IBS > 0.9 = close in top 10% of range → exhaustion for longs
        #                        IBS < 0.1 = close in bottom 10% of range → exhaustion for shorts
        # Uses previous COMPLETED 5m bar for clean signal.
        # Minimum gain guard (>1.0%) to avoid premature exits on flat/losing trades.
        # --------------------------------------------------
        if gain > 1.0:
            h5p = float(i.get('high_5m_prev', 0)); l5p = float(i.get('low_5m_prev', 0)); c5p = float(i.get('close_5m_prev', 0) or 0)
            bar_range_5m = h5p - l5p
            if bar_range_5m > 0 and c5p > 0:
                ibs_5m = (c5p - l5p) / bar_range_5m
                if is_long and ibs_5m > 0.9:
                    return True, f"IBS_EXHAUSTION_LONG_ibs:{ibs_5m:.2f}_gain:{gain:.1f}%", qty
                elif not is_long and ibs_5m < 0.1:
                    return True, f"IBS_EXHAUSTION_SHORT_ibs:{ibs_5m:.2f}_gain:{gain:.1f}%", qty
            # Also check 15m bar for stronger confirmation
            h15p = float(i.get('high_15m_prev', 0)); l15p = float(i.get('low_15m_prev', 0)); c15p = float(i.get('close_15m_prev', 0) or 0)
            bar_range_15m = h15p - l15p
            if bar_range_15m > 0 and c15p > 0 and gain > 1.5:
                ibs_15m = (c15p - l15p) / bar_range_15m
                if is_long and ibs_15m > 0.9:
                    return True, f"IBS_15m_EXHAUSTION_LONG_ibs:{ibs_15m:.2f}_gain:{gain:.1f}%", qty
                elif not is_long and ibs_15m < 0.1:
                    return True, f"IBS_15m_EXHAUSTION_SHORT_ibs:{ibs_15m:.2f}_gain:{gain:.1f}%", qty

        # --------------------------------------------------
        # 4. NORMAL TECHNICAL EXITS — DISABLED 2026-04-08
        # The old calculate_signal_score exit bypassed the WT/DC scorer.
        # Caused PLTR option to be sold at a loss on a minor pullback.
        # The WT/DC scorer (threshold 30) is now the ONLY exit authority.
        # --------------------------------------------------
        # score_exit, rec_exit, reason_exit = self.calculate_signal_score(...)
        # ALGO_EXIT / ALGO_WEAK_EXIT — ALL DISABLED
        pass

        # Sentiment Collapse — close while still above commission cost, before position goes negative
        # Must hold >3 min (no instant dumps) AND gain must cover commissions (>0.12%)
        s_local = i.get('sentiment_local', 0)
        s_global = i.get('sentiment', 0)
        _hold_min = (datetime.now(timezone.utc) - position.opened_at).total_seconds() / 60 if position and hasattr(position, 'opened_at') and position.opened_at else 999
        if gain > 0.5 and _hold_min > 3:
            if is_long:
                if s_local < -30 and s_local < (s_global - 25):
                    return True, f"Sentiment_Collapsed_Below_Global_gain:{gain:.2f}%", qty
            else:
                if s_local > 30 and s_local > (s_global + 25):
                    return True, f"Sentiment_Spiked_Above_Global_gain:{gain:.2f}%", qty

        # Standard Technical Breakdowns (Profit Taking / Extended Trends)
        # STRICT_NO_LOSS: ALL exits require gain >= noloss_min (3% trb, 0.5% trc). 0.05% was losing -$598.
        if is_long:
            if gain >= _noloss_min_t and current_price < i.get('dc_basis_1h', 0) and i.get('lr_trend_15m', 0) < 0:
                return True, f"Structure_Break_1H_Basis_gain:{gain:.2f}%", qty
            if gain > 1.5 and current_price > i.get('dc_high_1h', 999999) and i.get('rsi_15m', 50) > 80:
                return True, f"Extreme_Overbought_TP_gain:{gain:.2f}%", qty
            if gain >= _noloss_min_t and i.get('rsi_15m', 50) < 40 and i.get('stoch_k_15m', 50) < i.get('stoch_d_15m', 50) and i.get('ha_15m', 'neutral') == 'red':
                 return True, f"Technical_Breakdown_15m_gain:{gain:.2f}%", qty
        else:
            if gain >= _noloss_min_t and current_price > i.get('dc_basis_1h', 999999) and i.get('lr_trend_15m', 0) > 0:
                return True, f"Structure_Break_1H_Basis_gain:{gain:.2f}%", qty
            if gain > 1.5 and current_price < i.get('dc_low_1h', 0) and i.get('rsi_15m', 50) < 20:
                return True, f"Extreme_Oversold_TP_gain:{gain:.2f}%", qty
            if gain >= _noloss_min_t and i.get('rsi_15m', 50) > 60 and i.get('stoch_k_15m', 50) > i.get('stoch_d_15m', 50) and i.get('ha_15m', 'neutral') == 'green':
                 return True, f"Technical_Breakdown_15m_gain:{gain:.2f}%", qty
                 
        return False, "", 0.0
    
    async def evaluate_open(self, account_key, symbol: str, position_side: str, indicators: dict, market_context: dict = None) -> tuple[str, str, float, float]:
        # OPENING_BUFFER_NO_TRADE: no new opens in first 30m after open
        _in_buf, _mins = in_opening_buffer()
        if _in_buf:
            return "NO_ACTION", f"OPENING_BUFFER_NO_TRADE({_mins:.0f}m<30m)", 0.0, 0.0
        # 🤖 TRC_AGENT advisory short-circuit (paper, supervisor routine override)
        try:
            if account_key == "trc":
                import trc_advisory_consumer as _trc_adv
                _trc_adv_obj = _trc_adv.check(account_key, symbol.upper(), position_side, "open")
                if _trc_adv_obj:
                    _trc_action = _trc_adv_obj.get("action")
                    _trc_reason = _trc_adv_obj.get("reason", "")[:60]
                    if _trc_action == "block_entry":
                        _trc_adv.log_application(account_key, symbol.upper(), position_side, "open", "block_entry", _trc_adv_obj)
                        logger.info(f"🤖 TRC_AGENT_BLOCK_ENTRY {symbol}_{position_side}: {_trc_reason}")
                        return "NO_ACTION", f"TRC_AGENT_BLOCK_ENTRY({_trc_reason})", 0.0, 0.0
                    if _trc_action == "force_open":
                        _trc_size = float(_trc_adv_obj.get("size_override_usd") or 0)
                        _trc_price, _ = await self.trade_manager.get_current_price(symbol.upper())
                        _trc_price = float(_trc_price) if _trc_price else 0.0
                        if _trc_size > 0 and _trc_price > 0:
                            _trc_qty = max(1.0, _trc_size / _trc_price)
                            _trc_act = "OPEN_LONG" if position_side == "LONG" else "OPEN_SHORT"
                            _trc_adv.log_application(account_key, symbol.upper(), position_side, "open", "force_open", _trc_adv_obj)
                            logger.info(f"🤖 TRC_AGENT_FORCE_OPEN {symbol}_{position_side} qty={_trc_qty:.2f}: {_trc_reason}")
                            return _trc_act, f"TRC_AGENT_FORCE_OPEN({_trc_reason})", _trc_price, _trc_qty
        except Exception as _trc_err:
            logger.warning(f"[TRC_AGENT] evaluate_open advisory check failed: {_trc_err}")
        symbol = symbol.upper()
        i = self.parse_market_data(indicators)
        final_qty = 0.0
        longs = getattr(self.trade_manager, f"symbols_long_{account_key}",[])
        shorts = getattr(self.trade_manager, f"symbols_short_{account_key}",[])
        current_price,ts = await self.trade_manager.get_current_price(symbol)
        current_price = float(current_price) if current_price else 0.0
        if not current_price or current_price <= 0:
            logger.warning(f"Skipping {symbol}: Price is zero or unavailable.")
            return "NO_ACTION", "No Price", 0.0, 0.0
            
        can_short = symbol not in config.NON_SHORTABLE
        is_long = (position_side == "LONG")
        # 0. FIRST-HOUR MOMENTUM (BACKTEST_CHANGE_MT3) — fires FIRST, before all other entries
        # Research: 30min after open, if move > 0.5% → 82% day follows direction
        _fh_enabled = _cfg('FH_MOMENTUM_ENABLED', False, account_key, symbol, position_side)
        if _fh_enabled:
            _open_d = float(indicators.get('open_D', 0) or i.get('open_D', 0) or 0)
            if _open_d > 0:
                    _fh_move = (current_price - _open_d) / _open_d * 100.0
                    _fh_min = getattr(config, 'FH_MOMENTUM_MIN_MOVE_PCT', 0.5)
                    if abs(_fh_move) >= _fh_min:
                        _fh_mfi_d = float(indicators.get('mfi_D', indicators.get('mfi_4h', i.get('mfi_D', i.get('mfi_4h', 50)))) or 50)
                        _fh_dc_pos = float(indicators.get('dc_position_D', indicators.get('dc_position_4h', i.get('dc_position_D', i.get('dc_position_4h', 0.5)))) or 0.5)
                        _fh_ok = True
                        if getattr(config, 'FH_MOMENTUM_MFI_CONFIRM', True):
                            if _fh_move > 0 and _fh_mfi_d < 50:
                                _fh_ok = False
                            if _fh_move < 0 and _fh_mfi_d > 50:
                                _fh_ok = False
                        # DC breakout-retest scoring (MT4): broken level = support/resistance
                        _dc_long_rt, _dc_short_rt, _dc_detail = self.compute_dc_retest_score(current_price, indicators)
                        _fh_dc_max = getattr(config, 'FH_MOMENTUM_DC_MAX_LONG', 0.5)
                        if _fh_ok and getattr(config, 'FH_MOMENTUM_DC_CONFIRM', True):
                            if _fh_move > 0 and _fh_dc_pos > _fh_dc_max and _dc_long_rt == 0:
                                _fh_ok = False
                            if _fh_move < 0 and _fh_dc_pos < (1.0 - _fh_dc_max) and _dc_short_rt == 0:
                                _fh_ok = False
                        # Block if retest score favors OPPOSITE direction
                        if _fh_ok and _fh_move > 0 and _dc_short_rt >= 2 and _dc_long_rt == 0:
                            _fh_ok = False
                        if _fh_ok and _fh_move < 0 and _dc_long_rt >= 2 and _dc_short_rt == 0:
                            _fh_ok = False
                        if _fh_ok:
                            _fh_size = getattr(config, 'FH_MOMENTUM_POSITION_SIZE', 600.0)
                            _fh_size_mult = 1.0
                            if _fh_move > 0 and _dc_long_rt >= 2:
                                _fh_size_mult = 1.5
                            elif _fh_move < 0 and _dc_short_rt >= 2:
                                _fh_size_mult = 1.5
                            _fh_qty = max(1, int(_fh_size * _fh_size_mult / max(current_price, 1e-9)))
                            if _fh_move > 0 and is_long and symbol in longs:
                                return "OPEN", f"FH_MOMENTUM_L move={_fh_move:+.2f}%_mfi={_fh_mfi_d:.0f}_dc={_fh_dc_pos:.2f}_rt={_dc_long_rt}({_dc_detail})", 85.0, _fh_qty
                            elif _fh_move < 0 and not is_long and symbol in shorts and can_short:
                                return "OPEN", f"FH_MOMENTUM_S move={_fh_move:+.2f}%_mfi={_fh_mfi_d:.0f}_dc={_fh_dc_pos:.2f}_rt={_dc_short_rt}({_dc_detail})", 85.0, _fh_qty
        # 2026-03-31: DC_BREAK disabled for live — enters at channel HIGHS (worst entry price).
        # Contradicts dip-buying philosophy. Keep Invest_Pullback + Fast_Bounce + extra strategies.
        # Backtest grid running to validate. Re-enable only with backtest proof.
        _DC_BREAK_DISABLED = getattr(config, 'DC_BREAK_ENTRY_DISABLED', True)

        # ----------------------------------------------------------
        # RATIO GATE: Custom Universe Index + QQQ momentum
        # Uses our OWN traded symbols as market proxy (not generic SPY/QQQ)
        # PLTR and oil exempt from ratio blocking
        # ----------------------------------------------------------
        _ratio_exempt = symbol.upper() in ('PLTR', 'XOM', 'CVX', 'OXY', 'COP', 'SLB', 'HAL', 'USO')
        if not _ratio_exempt:
            try:
                _bal = self.trade_manager.get_current_portfolio_balance()
                _long_pct = _bal.get('long_pct', 0.5) * 100
                _short_pct = _bal.get('short_pct', 0.5) * 100
                _custom_idx = self.trade_manager.get_custom_universe_index()
                _universe_score = _custom_idx.get('composite', 0.0)
                _qqq = self.trade_manager.market_snapshot.get('QQQ', indicators)
                _k1h = float(_qqq.get('stoch_k_1h', 50) or 50)
                _d1h = float(_qqq.get('stoch_d_1h', 50) or 50)
                _qqq_momentum = 30.0 if (_k1h > _d1h and _k1h > 50) else (-30.0 if (_k1h < _d1h and _k1h < 50) else 0.0)
                # Active book breadth (our actual traded symbols)
                _active_score = 0.0
                try:
                    _active_syms = set(getattr(self.trade_manager, f"symbols_long_{account_key}", []) +
                                       getattr(self.trade_manager, f"symbols_short_{account_key}", []))
                    _snap = self.trade_manager.market_snapshot
                    _a_up, _a_dn = 0, 0
                    for _s in _active_syms:
                        _d = _snap.get(_s, {})
                        _c = float(_d.get('current_price', 0) or 0)
                        _p = float(_d.get('close_1h_prev', 0) or 0)
                        if _c > 0 and _p > 0:
                            if _c > _p * 1.001: _a_up += 1
                            elif _c < _p * 0.999: _a_dn += 1
                    _a_total = _a_up + _a_dn
                    if _a_total > 0:
                        _active_score = ((_a_up - _a_dn) / _a_total) * 50.0
                except Exception:
                    pass
                # Composite: 50% universe + 30% active book + 20% QQQ momentum
                _composite = _universe_score * 0.5 + _active_score * 0.3 + _qqq_momentum * 0.2 * 5
                _target_long_pct = max(10.0, min(90.0, 50.0 + _composite * 0.9))
                _ad = _custom_idx.get('ad_ratio', 1.0)
                if is_long and _long_pct > _target_long_pct:
                    logger.warning(f"[RATIO_GATE] {account_key}:{symbol}_{position_side}: BLOCK LONG — universe={_universe_score:.0f} qqq_m={_qqq_momentum:.0f} composite={_composite:.0f} A/D={_ad:.2f} long={_long_pct:.0f}%>target={_target_long_pct:.0f}%")
                    return "NO_ACTION", f"RATIO_GATE(L{_long_pct:.0f}>T{_target_long_pct:.0f})", 0.0, 0.0
                if not is_long and _short_pct > (100 - _target_long_pct):
                    logger.warning(f"[RATIO_GATE] {account_key}:{symbol}_{position_side}: BLOCK SHORT — composite={_composite:.0f} short={_short_pct:.0f}%>target={100-_target_long_pct:.0f}%")
                    return "NO_ACTION", f"RATIO_GATE(S{_short_pct:.0f}>T{100-_target_long_pct:.0f})", 0.0, 0.0
            except Exception as e:
                logger.warning(f"[RATIO_GATE] Error {symbol}: {e} — blocking as safety")
                return "NO_ACTION", "RATIO_GATE_ERROR", 0.0, 0.0

        # ----------------------------------------------------------
        # CVaR 5% PRE-ENTRY GATE (Quantitativo research)
        # Skip stocks where tail risk > -6% (avg of worst 5% of days)
        # ----------------------------------------------------------
        cvar_safe, cvar_val = self._is_cvar_safe(symbol, threshold=-6.0)
        if not cvar_safe and cvar_val != -999.0:
            # Has data but fails CVaR: log and block
            logger.debug(f"[CVaR_BLOCK] {symbol}: CVaR={cvar_val:.1f}% < -6.0% threshold, skipping entry")
            return "NO_ACTION", f"CVaR_Block({cvar_val:.1f}%<-6.0%)", 0.0, 0.0
        # If cvar_val == -999 (no data), allow trade but log warning
        if cvar_val == -999.0:
            logger.debug(f"[CVaR_WARN] {symbol}: No daily klines for CVaR check, proceeding without filter")

        # ----------------------------------------------------------
        # WT_D EXHAUSTION GATE — don't enter when daily WT is already extended
        # LONG: if wt1_D > 60 = overbought, wave near top → SKIP or tight stop
        # SHORT: if wt1_D < -60 = oversold, wave near bottom → SKIP or tight stop
        # Sweep: ride the wave from the START, not the top
        # ----------------------------------------------------------
        _wt1_D = float(i.get('wt1_D', 0) or 0)
        _wt2_D = float(i.get('wt2_D', 0) or 0)
        _wt1_4h = float(i.get('wt1_4h', 0) or 0)
        _wt_d_exhausted = (is_long and _wt1_D > 60) or (not is_long and _wt1_D < -60)
        _wt_4h_exhausted = (is_long and _wt1_4h > 60) or (not is_long and _wt1_4h < -60)
        if _wt_d_exhausted and _wt_4h_exhausted:
            logger.info(f"[WT_D_EXHAUST_BLOCK] {symbol} {'L' if is_long else 'S'}: wt1_D={_wt1_D:.0f} wt1_4h={_wt1_4h:.0f} — wave exhausted, skipping entry")
            return "NO_ACTION", f"WT_D_EXHAUST(D={_wt1_D:.0f},4h={_wt1_4h:.0f})", 0.0, 0.0
        # Circuit breaker: check & update
        self.circuit_breaker.check_positions(self.trade_manager.positions if self.trade_manager else {})
        score, rec, reason = 0.0, "WAIT", "uncalculated"
        k5 = i.get('stoch_k_5m', 50)
        d5 = i.get('stoch_d_5m', 50)
        k5_prev = i.get('stoch_k_5m_prev', 50)
        
        # Entry requires price to be on a genuine pullback — at or near DC support/resistance.
        # This prevents chasing mid-range after a close.
        dc_low4 = i.get('dc_low4_5m', 0)
        dc_low5 = i.get('dc_low_5m', 0)
        dc_high4 = i.get('dc_high4_5m', 999999)
        dc_high5 = i.get('dc_high_5m', 999999)
        dc_basis1h = i.get('dc_basis_1h', 0)
        # Stoch direction vars — used by both LONG and SHORT DC paths below
        _k5e = float(i.get('stoch_k_5m', 50) or 50)
        _d5e = float(i.get('stoch_d_5m', 50) or 50)
        _k15e = float(i.get('stoch_k_15m', 50) or 50)
        _d15e = float(i.get('stoch_d_15m', 50) or 50)

        if is_long:
            # DC LONG fast-path: check BEFORE stoch hook guard
            _dch5e_l  = float(i.get('dc_high_5m', 0) or 0)
            _dch15e_l = float(i.get('dc_high_15m', 0) or 0)
            _dch1he_l = float(i.get('dc_high_1h', 0) or 0)
            _dch4he_l = float(i.get('dc_high_4h', 0) or 0)
            _buf_l = 0.001
            _dc_long_mult_l, _dc_long_stop_l, _dc_long_tf_l = 1.0, 0.0, None
            if _dch4he_l > 0 and current_price > _dch4he_l * (1 + _buf_l):
                _dc_long_mult_l, _dc_long_stop_l, _dc_long_tf_l = 3.0, float(i.get('dc_basis_4h', 0) or 0), "DC4H"
            elif _dch1he_l > 0 and current_price > _dch1he_l * (1 + _buf_l):
                _dc_long_mult_l, _dc_long_stop_l, _dc_long_tf_l = 2.0, float(i.get('dc_basis_1h', 0) or 0), "DC1H"
            elif _dch15e_l > 0 and current_price > _dch15e_l * (1 + _buf_l):
                _dc_long_mult_l, _dc_long_stop_l, _dc_long_tf_l = 1.5, float(i.get('dc_basis_15m', 0) or 0), "DC15M"
            elif _dch5e_l > 0 and current_price > _dch5e_l * (1 + _buf_l):
                _dc_long_mult_l, _dc_long_stop_l, _dc_long_tf_l = 1.0, float(i.get('dc_basis_5m', 0) or 0), "DC5M"

            _long_stoch_ok = (_k5e > _d5e) and (_k15e > _d15e)
            if _dc_long_tf_l and symbol in longs and _long_stoch_ok and not _DC_BREAK_DISABLED:
                # BC_156: DC5M = 5m TRIGGER only. 1H DECIDES direction. No 1H agreement = no trade.
                # Backtest: DC5M alone = 42% WR (noise). DC1H = 84% WR. 5m triggers INTO 1h direction.
                if _dc_long_tf_l == "DC5M":
                    _wt1_1h_l = float(i.get('wt1_1h', 0) or 0)
                    _wt2_1h_l = float(i.get('wt2_1h', 0) or 0)
                    _k1h_l = float(i.get('stoch_k_1h', 50) or 50)
                    _d1h_l = float(i.get('stoch_d_1h', 50) or 50)
                    _dch1h_l = float(i.get('dc_high_1h', 0) or 0)
                    _dcb1h_l = float(i.get('dc_basis_1h', 0) or 0)
                    # 1H must AGREE with LONG: WT bullish + stoch bullish + price above dc_basis_1h
                    _1h_wt_bull = _wt1_1h_l > _wt2_1h_l
                    _1h_stoch_bull = _k1h_l > _d1h_l
                    _1h_above_basis = _dcb1h_l > 0 and current_price > _dcb1h_l
                    if not (_1h_wt_bull and _1h_stoch_bull and _1h_above_basis):
                        pass  # 1H disagrees — 5m break is noise, skip
                    else:
                        _tier_mults_l = {"DC5M": 1.0, "DC15M": 2.0, "DC1H": 3.0, "DC4H": 5.0}
                        _dc_long_mult_l = _tier_mults_l.get(_dc_long_tf_l, 1.0)
                        base_qty = config.START_POSITION_SIZE * _dc_long_mult_l / max(current_price, 1e-9)
                        final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "LONG", base_qty, indicators, None, market_context)
                        if final_qty > 0:
                            stop_tag = f"_stop:{_dc_long_stop_l:.2f}" if _dc_long_stop_l > 0 else ""
                            conviction = min(90.0, 60.0 + _dc_long_mult_l * 6)
                            logger.info(f"[{account_key}] 📈 DC5M_TRIGGER LONG {symbol} @ {current_price:.2f} | 1H: wt={'B' if _1h_wt_bull else 'S'} k={_k1h_l:.0f} basis={_dcb1h_l:.2f}")
                            return "OPEN", f"DC_BREAK_{_dc_long_tf_l}_LONG{stop_tag}", conviction, final_qty
                else:
                    _tier_mults_l = {"DC5M": 1.0, "DC15M": 2.0, "DC1H": 3.0, "DC4H": 5.0}
                    _dc_long_mult_l = _tier_mults_l.get(_dc_long_tf_l, 1.0)
                    base_qty = config.START_POSITION_SIZE * _dc_long_mult_l / max(current_price, 1e-9)
                    final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "LONG", base_qty, indicators, None, market_context)
                    if final_qty > 0:
                        stop_tag = f"_stop:{_dc_long_stop_l:.2f}" if _dc_long_stop_l > 0 else ""
                        conviction = min(90.0, 60.0 + _dc_long_mult_l * 6)
                        logger.info(f"[{account_key}] 📈 DC_BREAK {_dc_long_tf_l} LONG {symbol} @ {current_price:.2f} k5={_k5e:.0f}>d5={_d5e:.0f} | ×{_dc_long_mult_l} stop={_dc_long_stop_l:.2f}")
                        return "OPEN", f"DC_BREAK_{_dc_long_tf_l}_LONG{stop_tag}", conviction, final_qty

            # === OVERSOLD_TURN entry (Grid test winner: 87% WR, PF 22.4) ===
            # k_15m < 30 (oversold on higher TF) AND k_5m rising (turn confirmed)
            _k15_os = float(i.get('stoch_k_15m', 50) or 50)
            _k1h_l = float(i.get('stoch_k_1h', 50) or 50)
            _d1h_l = float(i.get('stoch_d_1h', 50) or 50)
            # NEVER open LONG below dc_low_5m — wrong side of the channel
            if dc_low5 > 0 and current_price < dc_low5:
                return "NO_ACTION", f"Below_DC_Low5m_no_long({dc_low5:.2f})", 0.0, 0.0
            is_oversold_turn = (_k15e < 30) and (k5 > k5_prev) and (k5 > d5)
            # Quality gate: 2/3 stoch TFs aligned for LONG
            _sq_votes_l = int(_k5e > _d5e) + int(_k15e > _d15e) + int(_k1h_l > _d1h_l)
            if not is_oversold_turn or _sq_votes_l < 2:
                return "NO_ACTION", f"Wait_Long_OversoldTurn_k15:{_k15e:.0f}_k5:{k5:.0f}_votes:{_sq_votes_l}", 0.0, 0.0
            if dc_basis1h > 0 and current_price > dc_basis1h * 1.008:
                return "NO_ACTION", f"Extended_Above_Basis1h({dc_basis1h:.2f})", 0.0, 0.0
        else:
            # --- DC current-state check for SHORT: price IS below a DC channel ---
            # Runs BEFORE stoch hook check — DC breakouts are valid regardless of stoch level
            _dcl5e  = float(i.get('dc_low_5m', 0) or 0)
            _dcl15e = float(i.get('dc_low_15m', 0) or 0)
            _dcl1he = float(i.get('dc_low_1h', 0) or 0)
            _dcl4he = float(i.get('dc_low_4h', 0) or 0)
            _buf = 0.0005  # Marathon winner: buffer=0.0005
            _dc_short_mult, _dc_short_stop, _dc_short_tf = 1.0, 0.0, None
            if _dcl4he > 0 and current_price < _dcl4he * (1 - _buf):
                _dc_short_mult, _dc_short_stop, _dc_short_tf = 3.0, float(i.get('dc_basis_4h', 0) or 0), "DC4H"
            elif _dcl1he > 0 and current_price < _dcl1he * (1 - _buf):
                _dc_short_mult, _dc_short_stop, _dc_short_tf = 2.0, float(i.get('dc_basis_1h', 0) or 0), "DC1H"
            elif _dcl15e > 0 and current_price < _dcl15e * (1 - _buf):
                _dc_short_mult, _dc_short_stop, _dc_short_tf = 1.5, float(i.get('dc_basis_15m', 0) or 0), "DC15M"
            elif _dcl5e > 0 and current_price < _dcl5e * (1 - _buf):
                _dc_short_mult, _dc_short_stop, _dc_short_tf = 1.0, float(i.get('dc_basis_5m', 0) or 0), "DC5M"

            # Direction confirmation: stoch must confirm active SHORT momentum
            _short_stoch_confirms = (_k5e < _d5e) and (_k15e < _d15e)

            if _dc_short_tf and symbol in shorts and can_short and _short_stoch_confirms and not _DC_BREAK_DISABLED:
                # BC_156: DC5M = 5m TRIGGER only. 1H DECIDES direction. No 1H agreement = no trade.
                if _dc_short_tf == "DC5M":
                    _wt1_1h_s = float(i.get('wt1_1h', 0) or 0)
                    _wt2_1h_s = float(i.get('wt2_1h', 0) or 0)
                    _k1h_s = float(i.get('stoch_k_1h', 50) or 50)
                    _d1h_s2 = float(i.get('stoch_d_1h', 50) or 50)
                    _dcb1h_s = float(i.get('dc_basis_1h', 0) or 0)
                    # 1H must AGREE with SHORT: WT bearish + stoch bearish + price below dc_basis_1h
                    _1h_wt_bear = _wt1_1h_s < _wt2_1h_s
                    _1h_stoch_bear = _k1h_s < _d1h_s2
                    _1h_below_basis = _dcb1h_s > 0 and current_price < _dcb1h_s
                    if not (_1h_wt_bear and _1h_stoch_bear and _1h_below_basis):
                        pass  # 1H disagrees — 5m break is noise, skip
                    else:
                        _tier_mults = {"DC5M": 1.0, "DC15M": 2.0, "DC1H": 3.0, "DC4H": 5.0}
                        _dc_short_mult = _tier_mults.get(_dc_short_tf, 1.0)
                        base_qty = config.START_POSITION_SIZE * _dc_short_mult / max(current_price, 1e-9)
                        final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "SHORT", base_qty, indicators, None, market_context)
                        if final_qty > 0:
                            stop_tag = f"_stop:{_dc_short_stop:.2f}" if _dc_short_stop > 0 else ""
                            conviction = min(90.0, 60.0 + _dc_short_mult * 6)
                            logger.info(f"[{account_key}] 📉 DC5M_TRIGGER SHORT {symbol} @ {current_price:.2f} | 1H: wt={'B' if not _1h_wt_bear else 'S'} k={_k1h_s:.0f} basis={_dcb1h_s:.2f}")
                            return "OPEN", f"DC_BREAK_{_dc_short_tf}_SHORT{stop_tag}", conviction, final_qty
                else:
                    _tier_mults = {"DC5M": 1.0, "DC15M": 2.0, "DC1H": 3.0, "DC4H": 5.0}
                    _dc_short_mult = _tier_mults.get(_dc_short_tf, 1.0)
                    base_qty = config.START_POSITION_SIZE * _dc_short_mult / max(current_price, 1e-9)
                    final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "SHORT", base_qty, indicators, None, market_context)
                    if final_qty > 0:
                        stop_tag = f"_stop:{_dc_short_stop:.2f}" if _dc_short_stop > 0 else ""
                        conviction = min(90.0, 60.0 + _dc_short_mult * 6)
                        logger.info(f"[{account_key}] 📉 DC_BREAK {_dc_short_tf} SHORT {symbol} @ {current_price:.2f} k5={_k5e:.0f}<d5={_d5e:.0f} | ×{_dc_short_mult} stop={_dc_short_stop:.2f}")
                        return "OPEN", f"DC_BREAK_{_dc_short_tf}_SHORT{stop_tag}", conviction, final_qty

            # === OVERBOUGHT_SHORT entry (Grid test winner: 89% WR, PF 40.2) ===
            # k_15m > 70 (overbought on higher TF) AND k_5m falling (turn confirmed)
            _k1h_s = float(i.get('stoch_k_1h', 50) or 50)
            _d1h_s = float(i.get('stoch_d_1h', 50) or 50)
            # NEVER open SHORT above dc_high_5m — wrong side of the channel
            if dc_high5 < 999999 and dc_high5 > 0 and current_price > dc_high5:
                return "NO_ACTION", f"Above_DC_High5m_no_short({dc_high5:.2f})", 0.0, 0.0
            is_overbought_turn = (_k15e > 70) and (k5 < k5_prev) and (k5 < d5)
            # Quality gate: 2/3 stoch TFs aligned for SHORT
            _sq_votes_s = int(_k5e < _d5e) + int(_k15e < _d15e) + int(_k1h_s < _d1h_s)
            if not is_overbought_turn or _sq_votes_s < 2:
                return "NO_ACTION", f"Wait_Short_OverboughtTurn_k15:{_k15e:.0f}_k5:{k5:.0f}_votes:{_sq_votes_s}", 0.0, 0.0
            if dc_basis1h > 0 and current_price < dc_basis1h * 0.992:
                return "NO_ACTION", f"Extended_Below_Basis1h({dc_basis1h:.2f})", 0.0, 0.0

        if is_long and symbol in longs:
            score, rec, reason = self.calculate_signal_score(account_key, symbol, True, current_price, i, None, is_exit=False)
            score = safe_float(score)
            _min_score = _cfg("ENTRY_SCORE_THRESHOLD", 12, account_key, symbol, position_side)
            if score >= _min_score:
                base_qty = (config.START_POSITION_SIZE / max(current_price, 1e-9))
                final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "LONG", base_qty, indicators, None, market_context)
                if final_qty > 0:
                    conviction = 50.0  # BACKTEST_CHANGE_T40: conviction only works for SHORT
                    return "OPEN", f"LONG_{rec}: {reason}", conviction, final_qty
            if score >= 6 and "Sniper" in reason:
                 base_qty = (config.START_POSITION_SIZE * 0.5 / max(current_price, 1e-9))
                 return "OPEN", f"SNIPER_LONG_{rec}", 60.0, base_qty

        elif not is_long and symbol in shorts and can_short:
            score, rec, reason = self.calculate_signal_score(account_key, symbol, False, current_price, i, None, is_exit=False)
            score = safe_float(score)
            _min_score = _cfg("ENTRY_SCORE_THRESHOLD", 12, account_key, symbol, position_side)
            if score >= _min_score:
                base_qty = (config.START_POSITION_SIZE / max(current_price, 1e-9))
                final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "SHORT", base_qty, indicators, None, market_context)
                if final_qty > 0:
                    conviction = min(95.0, score * 3.5)
                    return "OPEN", f"SHORT_{rec}: {reason}", conviction, final_qty
            if score >= 6 and "Sniper" in reason:
                 base_qty = (config.START_POSITION_SIZE * 0.5 / max(current_price, 1e-9))
                 return "OPEN", f"SNIPER_SHORT_{rec}", 60.0, base_qty

        # 2. Strategy Variables
        uptrend_1h = i.get('lr_trend_1h', 0) > 0 and current_price > i.get('ema_200_1h', 0)
        downtrend_1h = i.get('lr_trend_1h', 0) < 0 and current_price < i.get('ema_200_1h', 999999)

        dist_to_low = (current_price - i.get('dc_low_15m', 0)) / max(current_price, 1e-9)
        dist_to_high = (i.get('dc_high_15m', 999999) - current_price) / max(current_price, 1e-9)

        pullback_long = dist_to_low < 0.01
        pullback_short = dist_to_high < 0.01

        trigger_long = k5 > d5 and k5 < 35 and k5 > k5_prev   # Was oversold (<35) and turning up
        trigger_short = k5 < d5 and k5 > 65 and k5 < k5_prev  # Was overbought (>65) and turning down
        # BACKTEST_CHANGE_100: 5m candle structure entry (higher_low for LONG, lower_high for SHORT)
        # Backtest: structure entry + 15m confirm = PF 2.53, Sharpe 4.45 on stocks
        _low5 = float(i.get('low_5m', 0) or 0); _low5p = float(i.get('low_5m_prev', 0) or 0)
        _high5 = float(i.get('high_5m', 0) or 0); _high5p = float(i.get('high_5m_prev', 0) or 0)
        _struct_hl5 = _low5 > 0 and _low5p > 0 and _low5 > _low5p  # higher_low on 5m
        _struct_lh5 = _high5 > 0 and _high5p > 0 and _high5 < _high5p  # lower_high on 5m
        if is_long and _struct_hl5 and k5 > d5 and k5 < 60 and _k15e > _d15e:
            trigger_long = True  # Structure confirms long entry
        elif not is_long and _struct_lh5 and k5 < d5 and k5 > 40 and _k15e < _d15e:
            trigger_short = True  # Structure confirms short entry

        sent_long_ok = (i.get('sentiment_local', 0) > i.get('sentiment', 0)) or (i.get('sentiment_local', 0) > 10)
        sent_short_ok = (i.get('sentiment_local', 0) < i.get('sentiment', 0)) or (i.get('sentiment_local', 0) < -10)
        
        if is_long and symbol in longs:
            if uptrend_1h and pullback_long and trigger_long and sent_long_ok:
                base_qty = (config.START_POSITION_SIZE / max(current_price, 1e-9))
                final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "LONG", base_qty, indicators, None, market_context)
                if final_qty > 0:
                    return "OPEN", "Invest_Long_Pullback", 85.0, final_qty
                    
            # Fast bounce off 5m low
            dist_5m_low = (current_price - i.get('dc_low_5m', 0)) / max(current_price, 1e-9)
            bounce_1m_long = i.get('stoch_k_1m', 50) > i.get('stoch_d_1m', 50) and i.get('stoch_k_1m', 50) < 30
            if dist_5m_low < 0.008 and bounce_1m_long:
                base_qty = (config.START_POSITION_SIZE / max(current_price, 1e-9))
                final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "LONG", base_qty, indicators, None, market_context)
                if final_qty > 0:
                    return "OPEN", "Fast_Bounce_Long", 80.0, final_qty

        elif not is_long and symbol in shorts and can_short:
            if downtrend_1h and pullback_short and trigger_short and sent_short_ok:
                base_qty = (config.START_POSITION_SIZE / max(current_price, 1e-9))
                final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "SHORT", base_qty, indicators, None, market_context)
                if final_qty > 0:
                    return "OPEN", "Invest_Short_Rip", 85.0, final_qty
                    
            # Fast bounce off 5m high
            dist_5m_high = (i.get('dc_high_5m', 999999) - current_price) / max(current_price, 1e-9)
            bounce_1m_short = i.get('stoch_k_1m', 50) < i.get('stoch_d_1m', 50) and i.get('stoch_k_1m', 50) > 70
            if dist_5m_high < 0.008 and bounce_1m_short:
                base_qty = (config.START_POSITION_SIZE / max(current_price, 1e-9))
                final_qty = await self.calculate_quantity_complex(symbol, "OPEN", "SHORT", base_qty, indicators, None, market_context)
                if final_qty > 0:
                    return "OPEN", "Fast_Bounce_Short", 80.0, final_qty


        # --- LOGGING FOR DEBUGGING (Why didn't we trade?) ---
        if is_long and (i.get('stoch_k_5m', 50) > i.get('stoch_d_5m', 50)):
            if not uptrend_1h: return "NO_ACTION", f"Wait_Trend_1H (Price < EMA200)", 0.0, 0.0
            if not pullback_long: return "NO_ACTION", f"Wait_Pullback (DistToLow {dist_to_low:.2%})", 0.0, 0.0
            if not trigger_long: return "NO_ACTION", f"Stoch_High ({i.get('stoch_k_5m', 50):.0f} > 50)", 0.0, 0.0
        return "NO_ACTION", "", 0.0, 0.0


    async def evaluate_augment(self, symbol, position, indicators, market_context=None):
        """
        DC Tier Augment: position sizing per DC tier.
        Tiers: 5m=1×, 15m=2×, 1h=3×, 4h=5× of START_POSITION_SIZE.
        REQUIRES gain > 0.15% before augmenting — never add to a losing position.
        """
        # OPENING_BUFFER_NO_TRADE: no augments in first 30m after open
        _in_buf, _mins = in_opening_buffer()
        if _in_buf:
            return False, f"OPENING_BUFFER_NO_TRADE({_mins:.0f}m<30m)", 0.0, 0.0
        # 🤖 TRC_AGENT advisory short-circuit (paper, supervisor routine override)
        try:
            _trc_acct = current_account.get("")
            if _trc_acct == "trc":
                import trc_advisory_consumer as _trc_adv
                _trc_pside = getattr(position, 'position_side', 'LONG')
                _trc_adv_obj = _trc_adv.check(_trc_acct, symbol, _trc_pside, "augment")
                if _trc_adv_obj:
                    _trc_action = _trc_adv_obj.get("action")
                    _trc_reason = _trc_adv_obj.get("reason", "")[:60]
                    if _trc_action == "block_augment":
                        _trc_adv.log_application(_trc_acct, symbol, _trc_pside, "augment", "block_augment", _trc_adv_obj)
                        logger.info(f"🤖 TRC_AGENT_BLOCK_AUGMENT {symbol}_{_trc_pside}: {_trc_reason}")
                        return False, f"TRC_AGENT_BLOCK_AUGMENT({_trc_reason})", 0.0, 0.0
                    if _trc_action == "force_augment":
                        _trc_size = float(_trc_adv_obj.get("size_override_usd") or 0)
                        _trc_price, _ = await self.trade_manager.get_current_price(symbol)
                        _trc_price = float(_trc_price) if _trc_price else 0.0
                        if _trc_size > 0 and _trc_price > 0:
                            _trc_qty = max(1.0, _trc_size / _trc_price)
                            _trc_adv.log_application(_trc_acct, symbol, _trc_pside, "augment", "force_augment", _trc_adv_obj)
                            logger.info(f"🤖 TRC_AGENT_FORCE_AUGMENT {symbol}_{_trc_pside} qty={_trc_qty:.2f}: {_trc_reason}")
                            return True, f"TRC_AGENT_FORCE_AUGMENT({_trc_reason})", _trc_price, _trc_qty
        except Exception as _trc_err:
            logger.warning(f"[TRC_AGENT] evaluate_augment advisory check failed: {_trc_err}")
        try:
            i = self.parse_market_data(indicators)
            current_price = float(i.get('current_price', 0) or 0)
            if current_price <= 0: return False, "", 0.0, 0.0

            is_long = getattr(position, 'position_side', 'LONG') == 'LONG'
            current_qty = abs(float(getattr(position, 'quantity', 0) or getattr(position, 'positionAmt', 0) or 0))
            current_value = current_qty * current_price
            entry_price = float(getattr(position, 'entry_price', 0) or 0)
            if entry_price > 0:
                gain = (current_price - entry_price) / entry_price * 100 if is_long else (entry_price - current_price) / entry_price * 100
            else:
                gain = 0.0

            # wt_D bounce augment — fires BEFORE the gain gate, explicitly bypasses it for this signal
            if getattr(config, 'WT_D_BOUNCE_AUG_ENABLED', False) and gain < 0:
                wt1_d = float(i.get('wt1_D', 0) or 0)
                if not hasattr(self, '_wt_d_aug_state'):
                    self._wt_d_aug_state = {}
                _wds = self._wt_d_aug_state.get(symbol, {})
                prev_wt1_d = _wds.get('prev_wt1_d', wt1_d)
                last_aug_wt1_d = _wds.get('last_aug_wt1_d', (-999.0 if is_long else 999.0))
                last_aug_price = _wds.get('last_aug_price', (0.0 if is_long else float('inf')))
                last_aug_ts = _wds.get('last_aug_ts', 0.0)
                self._wt_d_aug_state[symbol] = {**_wds, 'prev_wt1_d': wt1_d}
                bounce = (wt1_d > prev_wt1_d) if is_long else (wt1_d < prev_wt1_d)
                req_hwt = getattr(config, 'WT_D_BOUNCE_AUG_REQUIRE_HIGHER_WT', True)
                req_hpx = getattr(config, 'WT_D_BOUNCE_AUG_REQUIRE_HIGHER_PRICE', True)
                higher_wt_ok = (not req_hwt) or ((wt1_d > last_aug_wt1_d) if is_long else (wt1_d < last_aug_wt1_d))
                higher_px_ok = (not req_hpx) or ((current_price > last_aug_price) if is_long else (current_price < last_aug_price))
                cooldown_ok = (time.time() - last_aug_ts) >= float(getattr(config, 'WT_D_BOUNCE_AUG_COOLDOWN_HOURS', 1.0)) * 3600
                if bounce and higher_wt_ok and higher_px_ok and cooldown_ok:
                    mult = float(getattr(config, 'WT_D_BOUNCE_AUG_MULTIPLIER', 2.0))
                    aug_qty_raw = current_qty * (mult - 1.0)
                    pk = getattr(position, 'position_key', '') or f"{getattr(position, 'account_key', 'trb')}:{symbol}_{'LONG' if is_long else 'SHORT'}"
                    _acct_key = pk.split(':')[0].upper() if ':' in pk else 'TRB'
                    _max_val = getattr(config, f'{_acct_key}_MAX_SYMBOL_VALUE', getattr(config, 'MAX_SYMBOL_VALUE_TRADIER', 15000))
                    if current_value < _max_val:
                        aug_qty_raw = min(aug_qty_raw, (_max_val - current_value) / max(current_price, 0.01))
                        if aug_qty_raw >= 0.5:
                            direction = "LONG" if is_long else "SHORT"
                            qty = await self.calculate_quantity_complex(symbol, "AUGMENT", direction, aug_qty_raw, indicators, position, market_context)
                            if qty > 0:
                                self._wt_d_aug_state[symbol] = {**self._wt_d_aug_state.get(symbol, {}), 'prev_wt1_d': wt1_d, 'last_aug_wt1_d': wt1_d, 'last_aug_price': current_price, 'last_aug_ts': time.time(), 'dd_qty': qty}
                                return True, f"WT_D_BOUNCE_AUG wt1_D={wt1_d:.2f}>prev={prev_wt1_d:.2f} px={current_price:.2f}>prev_aug={last_aug_price:.2f} gain={gain:.2f}% mult={mult:.1f}x", 75.0, qty
            # 2026-04-28 PATH B (B4): TRAILING_AUG — compound winners with stepped augments at small gains.
            # Investigation showed evaluate_augment had 0 fires on tradier vs 17,449 on crypto because
            # MIN_GAIN_TO_BUY_AGGRESSIVELY=3% was too high for typical stock moves. New path fires every
            # +0.5% gain (configurable), capped at N augments per position. Compounds without breakout.
            if getattr(config, 'TRAILING_AUG_ENABLED_TRADIER', False) and gain > 0:
                _ta_step_pct = float(getattr(config, 'TRAILING_AUG_GAIN_STEP_PCT', 0.5))
                _ta_max_augs = int(getattr(config, 'TRAILING_AUG_MAX_PER_POSITION', 3))
                _ta_min_step = float(getattr(config, 'TRAILING_AUG_MIN_GAIN_PCT', 0.5))
                if not hasattr(self, '_trailing_aug_state'): self._trailing_aug_state = {}
                _ta_pk = f"{getattr(self, 'account_key', symbol)}:{symbol}_{'LONG' if is_long else 'SHORT'}"
                _ta_state = self._trailing_aug_state.get(_ta_pk, {'last_threshold': 0.0, 'aug_count': 0})
                _ta_next_threshold = max(_ta_min_step, _ta_state['last_threshold'] + _ta_step_pct)
                if gain >= _ta_next_threshold and _ta_state['aug_count'] < _ta_max_augs:
                    _ta_max_val = getattr(config, 'MAX_SYMBOL_VALUE_TRADIER', 15000)
                    if current_value < _ta_max_val * 0.95:
                        # 2026-04-28: round up to 1 share min — START_POSITION_SIZE/price often <1 for high-priced stocks
                        _ta_aug_qty_raw = max(1.0, round(float(getattr(config, 'START_POSITION_SIZE', 100)) / max(current_price, 0.01)))
                        if _ta_aug_qty_raw >= 1.0:
                            direction = "LONG" if is_long else "SHORT"
                            qty = await self.calculate_quantity_complex(symbol, "AUGMENT", direction, _ta_aug_qty_raw, indicators, position, market_context)
                            if qty > 0:
                                self._trailing_aug_state[_ta_pk] = {'last_threshold': _ta_next_threshold, 'aug_count': _ta_state['aug_count'] + 1}
                                return True, f"TRAILING_AUG_step{_ta_next_threshold:.2f}%_g{gain:.2f}%_count{_ta_state['aug_count']+1}/{_ta_max_augs}", 80.0, qty
            # MUST be in profit before augmenting — 3% gate matches crypto (MIN_GAIN)
            # 2026-04-28: if PPL has fired, effective gain is doubled.
            try:
                from ez_reentry import effective_gain_pct as _eff_gain_t
                _eff_gain_val = _eff_gain_t(f"{getattr(self, 'account_key', '')}:{symbol}_{position_side}", gain, self, config)
            except Exception:
                _eff_gain_val = gain
            if _eff_gain_val < getattr(config, 'MIN_GAIN_TO_BUY_AGGRESSIVELY', 3.0):
                return False, "", 0.0, 0.0

            # Cooldown: don't augment if we augmented in the last 5 min
            last_aug_time = getattr(position, 'last_augmentation_time', None)
            if last_aug_time:
                try:
                    from datetime import datetime, timezone
                    if isinstance(last_aug_time, str):
                        from dateutil.parser import isoparse
                        lat = isoparse(last_aug_time)
                        if lat.tzinfo is None: lat = lat.replace(tzinfo=timezone.utc)
                        secs_since_aug = (datetime.now(timezone.utc) - lat).total_seconds()
                    else:
                        secs_since_aug = time.time() - float(last_aug_time)
                    if secs_since_aug < 300:
                        return False, "", 0.0, 0.0
                except Exception:
                    pass

            buf = 0.001
            tier_mults = {1: 1.0, 2: 2.0, 3: 3.0, 4: 5.0}
            active_tier = 0

            if is_long:
                dch5  = float(i.get('dc_high_5m', 0) or 0)
                dch15 = float(i.get('dc_high_15m', 0) or 0)
                dch1h = float(i.get('dc_high_1h', 0) or 0)
                dch4h = float(i.get('dc_high_4h', 0) or 0)
                if dch5  > 0 and current_price > dch5  * (1 + buf): active_tier = 1
                if dch15 > 0 and current_price > dch15 * (1 + buf): active_tier = 2
                if dch1h > 0 and current_price > dch1h * (1 + buf): active_tier = 3
                if dch4h > 0 and current_price > dch4h * (1 + buf): active_tier = 4
            else:
                dcl5  = float(i.get('dc_low_5m', 0) or 0)
                dcl15 = float(i.get('dc_low_15m', 0) or 0)
                dcl1h = float(i.get('dc_low_1h', 0) or 0)
                dcl4h = float(i.get('dc_low_4h', 0) or 0)
                if dcl5  > 0 and current_price < dcl5  * (1 - buf): active_tier = 1
                if dcl15 > 0 and current_price < dcl15 * (1 - buf): active_tier = 2
                if dcl1h > 0 and current_price < dcl1h * (1 - buf): active_tier = 3
                if dcl4h > 0 and current_price < dcl4h * (1 - buf): active_tier = 4

            if active_tier > 0:
                # 2026-05-16 — DC_TIER4 LATE-ENTRY GUARD (DEFAULT OFF behind DC_TIER4_BAR_MATURITY_BLOCK_ENABLED)
                # Live evidence (data/tradier/history/trc): 6 DC_TIER4_DC4H_AUG events in last
                # 30d averaged -2.25%. Root cause is NOT a DUP_GUARD bypass — the existing
                # MIN_GAIN_TO_BUY_AGGRESSIVELY=3.0% gate above (line ~6658) is stricter than
                # DUP_GUARD's 1.5% floor. Real cause: augmenting AT the top of the 4h breakout
                # bar where most of the ATR has already been consumed. Position bleeds when
                # the bar retraces from the breakout high.
                # Guard: when active_tier==4 (DC4H), refuse the augment if current bar has
                # consumed >70% of daily ATR in the direction of the augment. Tier 1/2/3
                # untouched (lower-TF breakouts retest quickly). Mirrors Patch 2's
                # bar-maturity logic for the entry-side equivalent. Fail-open on missing
                # open_D / atr_D so we never silently kill all aug fires. Flag default False:
                # legacy behavior preserved until sweep-validated.
                if active_tier == 4 and bool(getattr(config, 'DC_TIER4_BAR_MATURITY_BLOCK_ENABLED', False)):
                    _tier4_bm_thr = float(getattr(config, 'DC_TIER4_BAR_MATURITY_BLOCK', 0.7))
                    if _tier4_bm_thr > 0.0 and _tier4_bm_thr < 1.0:
                        _tier4_open_d = float(i.get('open_D', 0) or 0)
                        _tier4_atr_d = float(i.get('atr_D', 0) or 0)
                        if _tier4_open_d > 0 and _tier4_atr_d > 0:
                            _tier4_pos = abs(current_price - _tier4_open_d) / max(_tier4_atr_d, 1e-6)
                            _tier4_bar_up = (current_price > _tier4_open_d)
                            _tier4_same_dir = (is_long and _tier4_bar_up) or ((not is_long) and (not _tier4_bar_up))
                            if _tier4_pos > _tier4_bm_thr and _tier4_same_dir:
                                logger.info(f"[DC_TIER4_BAR_MATURITY_BLOCK] {symbol} {'L' if is_long else 'S'}: pos={_tier4_pos:.2f}>thr={_tier4_bm_thr:.2f} bar_up={_tier4_bar_up} px={current_price:.2f} open_D={_tier4_open_d:.2f} atr_D={_tier4_atr_d:.4f} gain={gain:.2f}% — AUG REFUSED (late-entry)")
                                return False, "", 0.0, 0.0
                # CONCENTRATION CAP: Never let a single symbol exceed $15,000 — USO grew to $352K, IBIT to $119K
                pk = getattr(position, 'position_key', '') or f"{getattr(position, 'account_key', 'trb')}:{symbol}_{'LONG' if is_long else 'SHORT'}"
                _acct_key = pk.split(':')[0].upper() if ':' in pk else 'TRB'
                _max_symbol_value = getattr(config, f'{_acct_key}_MAX_SYMBOL_VALUE', getattr(config, 'MAX_SYMBOL_VALUE_TRADIER', 15000))
                if current_value >= _max_symbol_value:
                    logger.info(f"[CONC_CAP] {symbol} {('LONG' if is_long else 'SHORT')}: value=${current_value:.0f} >= cap ${_max_symbol_value} — NO AUGMENT")
                    return False, "", 0.0, 0.0
                target_value = config.START_POSITION_SIZE * tier_mults[active_tier]
                target_value = min(target_value, _max_symbol_value - current_value)  # Don't exceed cap
                if current_value < target_value * 0.75:
                    aug_value = target_value - current_value
                    aug_qty = aug_value / current_price
                    if aug_qty >= 0.5:
                        direction = "LONG" if is_long else "SHORT"
                        qty = await self.calculate_quantity_complex(symbol, "AUGMENT", direction, aug_qty, indicators, position, market_context)
                        if qty > 0:
                            tf_name = {1:"DC5M",2:"DC15M",3:"DC1H",4:"DC4H"}[active_tier]
                            return True, f"DC_TIER{active_tier}_{tf_name}_AUG target={target_value:.0f} cur={current_value:.0f}", 80.0, qty

            return False, "", 0.0, 0.0
        except Exception as e:
            logger.error(f"[evaluate_augment] {symbol}: {e}")
            return False, f"Error: {e}", 0.0, 0.0

    async def evaluate_reentry(self, symbol: str, position: Any, indicators: dict) -> tuple[str, str, float, float]:
        """
        STOCH_TURN re-entry (Grid test winner: Sharpe 27.7 for stocks).
        Simple and fast: k_5m crosses d_5m + k_15m confirms direction.
        30-bar cooldown after exit.
        """
        # OPENING_BUFFER_NO_TRADE: no reentries in first 30m after open
        _in_buf, _mins = in_opening_buffer()
        if _in_buf:
            return "NO_ACTION", f"OPENING_BUFFER_NO_TRADE({_mins:.0f}m<30m)", 0.0, 0.0
        i = self.parse_market_data(indicators)
        current_price, _ = await self.trade_manager.get_current_price(symbol)
        if current_price is None:
            return "NO_ACTION", "No Price", 0.0, 0.0
        current_price = float(current_price)
        is_long = getattr(position, 'position_side', 'LONG') == 'LONG'
        positionAmt = abs(float(getattr(position, 'positionAmt', 0) or 0))
        # ── ALLOWLIST GATE (user rule 2026-04-22) ──
        # Only reenter symbols still in symbols_trb_long (for LONG) / symbols_trb_short (for SHORT).
        # If the user removed the symbol from the hand-picked list, don't chase it back in.
        try:
            _base_path = Path(getattr(config, 'BASE_PATH', '/Users/niels/Documents/binance'))
            _allow_file = _base_path / ("symbols_trb_long.json" if is_long else "symbols_trb_short.json")
            if _allow_file.exists():
                with open(_allow_file) as _f:
                    _allow_data = json.load(_f)
                _allow_set = set(_allow_data) if isinstance(_allow_data, list) else set(_allow_data.keys())
                if symbol not in _allow_set:
                    return "NO_ACTION", f"ALLOWLIST_BLOCK_{symbol}_not_in_{'trb_long' if is_long else 'trb_short'}", 0.0, 0.0
        except Exception as _allow_err:
            logger.warning(f"[REENTRY_ALLOWLIST] {symbol}: allowlist check failed ({_allow_err}) — proceeding")
        logger.info(f"[REENTRY_EVAL] {symbol}: positionAmt={positionAmt} gain={getattr(position, 'gain', 'N/A')} last_red_price={getattr(position, 'last_reduction_price', 'N/A')} last_red_time={getattr(position, 'last_reduction_time', 'N/A')}")
        # ═══ PRICE_CROSS_BACK_REENTRY (2026-04-27 user rule) ═══════════════
        # "IT NEEDS TO IMMEDIATELY BUY AGAIN IF EXIT PRICE IS CROSSED." When
        # a position is flat and price returns to within a tight band of the
        # last reduction price, reopen — bypasses ANTI_CHURN, RZ_BLOCK,
        # hardcool, stoch/WT/HTF/rally gates. The user explicitly demands it.
        if positionAmt == 0 and bool(getattr(config, 'PRICE_CROSS_BACK_REENTRY_ENABLED', True)):
            _xb_last_px = float(getattr(position, 'last_reduction_price', 0) or 0)
            _xb_last_t = getattr(position, 'last_reduction_time', None)
            _xb_band_pct = float(getattr(config, 'PRICE_CROSS_BACK_BAND_PCT', 0.3))
            _xb_max_age_min = float(getattr(config, 'PRICE_CROSS_BACK_MAX_AGE_MIN', 240.0))
            _xb_age_min = 9999.0
            if _xb_last_t:
                try:
                    _xb_lt = _xb_last_t if not isinstance(_xb_last_t, str) else datetime.fromisoformat(str(_xb_last_t).replace("Z", "+00:00"))
                    if _xb_lt.tzinfo is None: _xb_lt = _xb_lt.replace(tzinfo=timezone.utc)
                    _xb_age_min = (datetime.now(timezone.utc) - _xb_lt).total_seconds() / 60.0
                except Exception: pass
            if _xb_last_px > 0 and _xb_age_min < _xb_max_age_min and current_price > 0:
                _xb_dist_pct = abs(current_price - _xb_last_px) / _xb_last_px * 100.0
                if _xb_dist_pct <= _xb_band_pct:
                    _xb_qty = config.START_POSITION_SIZE / max(current_price, 1e-9)
                    logger.warning(f"[PRICE_CROSS_BACK_REENTRY] {symbol} {'L' if is_long else 'S'}: cur={current_price:.4f} ≈ exit={_xb_last_px:.4f} ({_xb_dist_pct:.2f}% within {_xb_band_pct:.2f}%) age={_xb_age_min:.0f}m — REOPEN")
                    return "REENTRY_OPEN", f"PRICE_CROSS_BACK_exit{_xb_last_px:.4f}_cur{current_price:.4f}_dist{_xb_dist_pct:.2f}%_age{_xb_age_min:.0f}m", 90.0, _xb_qty
        entry_price = float(getattr(position, 'entry_price', current_price) or current_price)
        max_q = float(getattr(position, 'max_positionSize', 0) or positionAmt)
        last_red_time = getattr(position, 'last_reduction_time', None)
        gain = (current_price - entry_price) / entry_price * 100 if is_long else (entry_price - current_price) / entry_price * 100
        max_gain = max(gain, getattr(position, 'max_gain', 0))
        position.max_gain = max_gain
        position.gain = gain
        now = datetime.now(timezone.utc)

        # --- At max position? (skip if positionAmt=0 — that's a full close needing reentry) ---
        if max_q > 0 and positionAmt > 0 and positionAmt >= max_q:
            return "NO_ACTION", "At_max_qty", 0.0, 0.0

        # --- FIX 2026-04-08: ANTI-CHURN GATE — do NOT reenter if exit score is still above threshold ---
        # 2026-05-10 USER MANDATE: gated behind TRADIER_REENTRY_ANTI_CHURN_ENABLED (default False).
        if bool(getattr(config, 'TRADIER_REENTRY_ANTI_CHURN_ENABLED', False)):
            _re_exit_score, _re_exit_reason = wt_dc_score_exit(i, is_long, current_price)
            _re_exit_threshold = getattr(config, 'WT_DC_EXIT_THRESHOLD', 30)
            if _re_exit_score >= _re_exit_threshold:
                return "NO_ACTION", f"ANTI_CHURN_exit_score={_re_exit_score:.0f}>={_re_exit_threshold}_still_active", 0.0, 0.0
        # --- RED ZONE REENTRY GATE: 2026-05-10 USER MANDATE — gated behind TRADIER_REENTRY_RZ_BLOCK_ENABLED (default False).
        if bool(getattr(config, 'TRADIER_REENTRY_RZ_BLOCK_ENABLED', False)) and config.DELTA_ENGINE_ENABLED and hasattr(self.trade_manager, 'delta_tracker') and self.trade_manager.delta_tracker:
            _re_dsig = self.trade_manager.delta_tracker.update(symbol, i)
            if _re_dsig:
                if is_long and _re_dsig.zone == "TOP" and _re_dsig.exit_long:
                    return "NO_ACTION", f"RZ_BLOCK_REENTRY_LONG_AT_TOP_{_re_dsig.zone_reason}", 0.0, 0.0
                if not is_long and _re_dsig.zone == "BOTTOM" and _re_dsig.exit_short:
                    return "NO_ACTION", f"RZ_BLOCK_REENTRY_SHORT_AT_BOTTOM_{_re_dsig.zone_reason}", 0.0, 0.0
        # --- Reentry hardcool (was hardcoded 30 min — caused 3.7% reentry rate per 2026-04-25 audit) ---
        # Now configurable via TRADIER_REENTRY_HARDCOOL_MIN (default 30 preserves prior behavior).
        last_red_age_min = 999.0
        if last_red_time:
            try:
                lt = last_red_time if not isinstance(last_red_time, str) else datetime.fromisoformat(str(last_red_time).replace("Z", "+00:00"))
                if lt.tzinfo is None: lt = lt.replace(tzinfo=timezone.utc)
                last_red_age_min = (now - lt).total_seconds() / 60.0
            except Exception: pass
        _hardcool_min = float(getattr(config, 'TRADIER_REENTRY_HARDCOOL_MIN', 30.0))
        if last_red_age_min < _hardcool_min:
            return "NO_ACTION", f"Reentry_cooldown_{_hardcool_min-last_red_age_min:.0f}m_of_{_hardcool_min:.0f}m", 0.0, 0.0
        # === BC_155: TIER 2 CHASE REENTRY — trend continued past exit without pullback ===
        _last_red_px = float(getattr(position, 'last_reduction_price', 0) or 0)
        _t2_pct_t = getattr(config, 'REENTRY_TIER2_PRICE_PCT_TRADIER', 0.003)
        _t2_min_t = getattr(config, 'REENTRY_TIER2_MIN_MINUTES_TRADIER', 10.0)
        _t2_max_t = getattr(config, 'REENTRY_TIER2_MAX_MINUTES_TRADIER', 120.0)
        _t2_size_t = getattr(config, 'REENTRY_TIER2_SIZE_MULT_TRADIER', 0.8)
        k_5m_t2 = float(i.get('stoch_k_5m', 50) or 50); k_5m_prev_t2 = float(i.get('stoch_k_5m_prev', 50) or 50); k_15m_t2 = float(i.get('stoch_k_15m', 50) or 50)
        if _last_red_px > 0 and last_red_age_min >= _t2_min_t and last_red_age_min < 1200:
            _trend_past_t = (is_long and current_price > _last_red_px * (1.0 + _t2_pct_t)) or (not is_long and current_price < _last_red_px * (1.0 - _t2_pct_t))
            _mom_ok_t = (is_long and k_5m_t2 > k_5m_prev_t2) or (not is_long and k_5m_t2 < k_5m_prev_t2)
            _not_exh_t = not ((is_long and k_5m_t2 > 95) or (not is_long and k_5m_t2 < 5))
            if _trend_past_t and _mom_ok_t and _not_exh_t:
                _t2_qty_t = config.START_POSITION_SIZE / max(current_price, 1e-9) * _t2_size_t
                logger.warning(f"[TIER2_CHASE] {'LONG' if is_long else 'SHORT'} {symbol}: Trend past exit {_last_red_px:.2f}→{current_price:.2f} ({last_red_age_min:.0f}min) — chase at {_t2_size_t*100:.0f}% qty={_t2_qty_t:.2f}")
                return "REENTRY_OPEN", f"TIER2_CHASE_exit{_last_red_px:.2f}_cur{current_price:.2f}_t{last_red_age_min:.0f}m", 75.0, _t2_qty_t
            if last_red_age_min >= _t2_max_t and _not_exh_t:
                _forced_qty_t = config.START_POSITION_SIZE / max(current_price, 1e-9) * 0.5
                logger.warning(f"[TIER2_FORCED] {'LONG' if is_long else 'SHORT'} {symbol}: {last_red_age_min:.0f}min overdue — forced min reentry qty={_forced_qty_t:.2f}")
                return "REENTRY_OPEN", f"TIER2_FORCED_{last_red_age_min:.0f}min_exit{_last_red_px:.2f}", 60.0, _forced_qty_t
            if last_red_age_min >= 60.0:
                logger.warning(f"⚠️ [REENTRY_OVERDUE] {symbol}: {last_red_age_min:.0f}min since exit at {_last_red_px:.2f}, waiting! k5m={k_5m_t2:.0f}")
            elif last_red_age_min >= 30.0:
                logger.info(f"[REENTRY_PENDING] {symbol}: {last_red_age_min:.0f}min since exit at {_last_red_px:.2f}, waiting. k5m={k_5m_t2:.0f}")
        # === EMA200_1H_BOUNCE REENTRY (user rule 2026-04-22) ===
        # Reenter when price bounces above ema_200_1h (longs) or below (shorts).
        # This is the "macro structure intact" trigger — if we exited mid-trend and
        # the 1h 200-EMA was never lost, a recovery back through it is a valid re-entry
        # without needing micro-stoch confirmation.
        _ema200_1h = float(i.get('ema_200_1h', 0) or 0)
        _ema200_1h_prev = float(i.get('ema_200_1h_prev', _ema200_1h) or _ema200_1h)
        _last_price_prev = float(i.get('current_price_prev', 0) or 0)
        if _ema200_1h > 0 and current_price > 0:
            # LONG: price was ≤ ema_200_1h last bar, now above → bounce back over the line
            if is_long:
                _below_prev = (_last_price_prev > 0 and _last_price_prev <= _ema200_1h_prev) or current_price <= _ema200_1h * 1.002  # small tolerance
                _above_now = current_price > _ema200_1h
                if _above_now and _below_prev:
                    _emaq = config.START_POSITION_SIZE / max(current_price, 1e-9) * 0.8
                    logger.warning(f"[EMA200_1H_BOUNCE] LONG {symbol}: price {current_price:.2f} crossed back above ema_200_1h {_ema200_1h:.2f} — REENTER qty={_emaq:.2f}")
                    return "REENTRY_OPEN", f"EMA200_1H_BOUNCE_LONG_px{current_price:.2f}>ema{_ema200_1h:.2f}", 80.0, _emaq
            # SHORT: mirror — price was ≥ ema_200_1h, now below
            else:
                _above_prev = (_last_price_prev > 0 and _last_price_prev >= _ema200_1h_prev) or current_price >= _ema200_1h * 0.998
                _below_now = current_price < _ema200_1h
                if _below_now and _above_prev:
                    _emaq = config.START_POSITION_SIZE / max(current_price, 1e-9) * 0.8
                    logger.warning(f"[EMA200_1H_BOUNCE] SHORT {symbol}: price {current_price:.2f} crossed back below ema_200_1h {_ema200_1h:.2f} — REENTER qty={_emaq:.2f}")
                    return "REENTRY_OPEN", f"EMA200_1H_BOUNCE_SHORT_px{current_price:.2f}<ema{_ema200_1h:.2f}", 80.0, _emaq
        # === BOUNCE_REENTRY_K_RESET (2026-04-26) — wires DEAD switches BOUNCE_REENTRY_K_RESET_{LONG,SHORT}_TRADIER ===
        # K-reset bounce: previous bar's K was at/below the LONG threshold (default 35) and current K bounced
        # back above it AND is rising. Mirror for SHORT at the SHORT threshold (default 65).
        # Only fires when BOUNCE_REENTRY_ENABLED_TRADIER (default True). Conviction 75 (between EMA bounce 80 and WT 85).
        if bool(getattr(config, 'BOUNCE_REENTRY_ENABLED_TRADIER', True)):
            _kr_long = int(getattr(config, 'BOUNCE_REENTRY_K_RESET_LONG_TRADIER', 35))
            _kr_short = int(getattr(config, 'BOUNCE_REENTRY_K_RESET_SHORT_TRADIER', 65))
            if is_long and k_5m_prev_t2 <= _kr_long and k_5m_t2 > _kr_long and k_5m_t2 > k_5m_prev_t2:
                _kr_qty = config.START_POSITION_SIZE / max(current_price, 1e-9)
                logger.warning(f"[BOUNCE_REENTRY_K_RESET] LONG {symbol}: k_5m {k_5m_prev_t2:.0f}→{k_5m_t2:.0f} bounced above reset_long={_kr_long} — REENTER qty={_kr_qty:.2f}")
                return "REENTRY_OPEN", f"BOUNCE_REENTRY_K_RESET_LONG_k{k_5m_t2:.0f}_from{k_5m_prev_t2:.0f}_thresh{_kr_long}", 75.0, _kr_qty
            if (not is_long) and k_5m_prev_t2 >= _kr_short and k_5m_t2 < _kr_short and k_5m_t2 < k_5m_prev_t2:
                _kr_qty = config.START_POSITION_SIZE / max(current_price, 1e-9)
                logger.warning(f"[BOUNCE_REENTRY_K_RESET] SHORT {symbol}: k_5m {k_5m_prev_t2:.0f}→{k_5m_t2:.0f} bounced below reset_short={_kr_short} — REENTER qty={_kr_qty:.2f}")
                return "REENTRY_OPEN", f"BOUNCE_REENTRY_K_RESET_SHORT_k{k_5m_t2:.0f}_from{k_5m_prev_t2:.0f}_thresh{_kr_short}", 75.0, _kr_qty
        # === 2/3 WT IN FAVOR = REENTER NOW (matches exit signal mirror) ===
        # Backtest: 198,264/198,321 reentries (99.97%), median 55min wait
        _wt1_5m = float(i.get('wt1_5m', 0) or 0); _wt2_5m = float(i.get('wt2_5m', 0) or 0)
        _wt1_15m = float(i.get('wt1_15m', 0) or 0); _wt2_15m = float(i.get('wt2_15m', 0) or 0)
        _wt1_1h = float(i.get('wt1_1h', 0) or 0); _wt2_1h = float(i.get('wt2_1h', 0) or 0)
        _wt1_4h = float(i.get('wt1_4h', 0) or 0); _wt2_4h = float(i.get('wt2_4h', 0) or 0); _wt1_D_re = float(i.get('wt1_D', 0) or 0); _wt2_D_re = float(i.get('wt2_D', 0) or 0)
        _wt_fav = 0
        if is_long:
            if _wt1_5m > _wt2_5m: _wt_fav += 1
            if _wt1_15m > _wt2_15m: _wt_fav += 1
            if _wt1_1h > _wt2_1h: _wt_fav += 1
            if _wt1_4h > _wt2_4h: _wt_fav += 1
        else:
            if _wt1_5m < _wt2_5m: _wt_fav += 1
            if _wt1_15m < _wt2_15m: _wt_fav += 1
            if _wt1_1h < _wt2_1h: _wt_fav += 1
            if _wt1_4h < _wt2_4h: _wt_fav += 1
        logger.info(f"[REENTRY_GATE] {symbol} {'L' if is_long else 'S'}: wt_fav={_wt_fav}/4 5m={_wt1_5m:.0f}/{_wt2_5m:.0f} 15m={_wt1_15m:.0f}/{_wt2_15m:.0f} 1h={_wt1_1h:.0f}/{_wt2_1h:.0f} 4h={_wt1_4h:.0f}/{_wt2_4h:.0f} k5m={k_5m_t2:.0f}/prev={k_5m_prev_t2:.0f}")
        if _wt_fav >= 2:
            # STOCH GATE: k_5m < 50 AND rising (LONG) or > 50 AND falling (SHORT)
            # Bypass if position has been waiting >2h (k_5m may be stuck at a low/high during flat market)
            _stoch_overdue = last_red_age_min >= 120.0
            if is_long and not (k_5m_t2 < 50.0 and (k_5m_t2 > k_5m_prev_t2 or _stoch_overdue)):
                logger.info(f"[REENTRY_GATE] {symbol}: STOCH_GATE_FAIL k5m={k_5m_t2:.0f} prev={k_5m_prev_t2:.0f} overdue={_stoch_overdue}")
                return "NO_ACTION", f"STOCH_GATE_k5m={k_5m_t2:.0f}_prev={k_5m_prev_t2:.0f}_need_lt50_rising", 0.0, 0.0
            if not is_long and not (k_5m_t2 > 50.0 and (k_5m_t2 < k_5m_prev_t2 or _stoch_overdue)):
                logger.info(f"[REENTRY_GATE] {symbol}: STOCH_GATE_FAIL k5m={k_5m_t2:.0f} prev={k_5m_prev_t2:.0f} overdue={_stoch_overdue}")
                return "NO_ACTION", f"STOCH_GATE_k5m={k_5m_t2:.0f}_prev={k_5m_prev_t2:.0f}_need_gt50_falling", 0.0, 0.0
            # RALLY GATE: optional k15m level cap + HTF WT count (sweep knobs)
            _rally_k15m_max = float(getattr(config, 'REENTRY_RALLY_K15M_MAX', 100.0))
            _rally_htf_min = int(getattr(config, 'REENTRY_RALLY_HTF_MIN', 1))
            if _rally_k15m_max < 100.0:
                _k15m_ok = (k_15m_t2 < _rally_k15m_max) if is_long else (k_15m_t2 > (100.0 - _rally_k15m_max))
                if not _k15m_ok:
                    return "NO_ACTION", f"RALLY_K15M_GATE_k15m={k_15m_t2:.0f}_max={_rally_k15m_max:.0f}_side={'L' if is_long else 'S'}", 0.0, 0.0
            _htf_fav_re = sum(1 for w1, w2 in [(_wt1_1h, _wt2_1h), (_wt1_4h, _wt2_4h), (_wt1_D_re, _wt2_D_re)] if (w1 > w2 if is_long else w1 < w2))
            if _htf_fav_re < _rally_htf_min:
                return "NO_ACTION", f"RALLY_HTF_GATE_htf={_htf_fav_re}<{_rally_htf_min}_1h={_wt1_1h:.0f}/{_wt2_1h:.0f}_4h={_wt1_4h:.0f}/{_wt2_4h:.0f}_D={_wt1_D_re:.0f}/{_wt2_D_re:.0f}", 0.0, 0.0
            _re_qty = config.START_POSITION_SIZE / max(current_price, 1e-9)
            logger.warning(f"[WT_2of3_REENTRY] {'L' if is_long else 'S'} {symbol}: {_wt_fav}/3 WT favor k5m={k_5m_t2:.0f} k15m={k_15m_t2:.0f} htf={_htf_fav_re}/3 → REENTER qty={_re_qty:.2f}")
            return "REENTRY_OPEN", f"WT_2of3_REENTRY_{_wt_fav}of3_favor_htf{_htf_fav_re}", 85.0, _re_qty
        _wt_cross_bull_5m = i.get('wt_cross_bull_5m', i.get('wt_cross_5m') == "BULL")
        _wt_cross_bear_5m = i.get('wt_cross_bear_5m', i.get('wt_cross_5m') == "BEAR")
        _dc_pos = float(i.get('dc_position_1h', 0.5) or 0.5)
        # Fallback to stoch for symbols without WT data
        k_5m = float(i.get('stoch_k_5m', 50)); d_5m = float(i.get('stoch_d_5m', 50))
        k_15m = float(i.get('stoch_k_15m', 50)); d_15m = float(i.get('stoch_d_15m', 50))
        # WT cross value analysis — is this cross HIGHER than previous? (strengthening trend)
        _wt_cross_val = float(i.get('wt_cross_value_5m') or i.get('wt_cross_value_15m') or 0)
        _wt_cross_prev = float(i.get('wt_cross_prev_value_5m') or i.get('wt_cross_prev_value_15m') or 0)
        _wt_cross_rising = i.get('wt_cross_rising_5m') or i.get('wt_cross_rising_15m')
        _wt_cross_rising_1h = i.get('wt_cross_rising_1h')
        # Divergence detection (any TF)
        _bull_div = any(i.get(f'wt_divergence_{tf}') in ('BULL', 'HIDDEN_BULL') for tf in ['5m', '15m', '1h', '4h'] if i.get(f'wt_divergence_{tf}'))
        _bear_div = any(i.get(f'wt_divergence_{tf}') in ('BEAR', 'HIDDEN_BEAR') for tf in ['5m', '15m', '1h', '4h'] if i.get(f'wt_divergence_{tf}'))
        # Size multiplier based on cross quality
        _reentry_size_mult = 1.0
        if is_long:
            # Reentry LONG: WT cross bull on 5m + 15m/1h WT bullish + DC position pullback
            wt_entry = _wt_cross_bull_5m or (_wt1_5m > _wt2_5m and _wt1_5m > float(i.get('wt1_5m_prev', _wt1_5m) or _wt1_5m))
            htf_confirm = (_wt1_15m > _wt2_15m) or (_wt1_1h > _wt2_1h)
            dc_pullback = _dc_pos < 0.40
            stoch_fallback = (k_5m > d_5m) and (k_15m > d_15m or k_15m < 30)
            if not ((wt_entry and htf_confirm) or (stoch_fallback and dc_pullback)):
                # Check for bullish divergence — override even without cross
                if _bull_div and dc_pullback:
                    _reentry_size_mult = float(getattr(config, 'BOUNCE_TOP_REENTRY_MULT', 1.5))
                    logger.info(f"[REENTRY_DIV] LONG {symbol}: Bullish divergence + DC pullback({_dc_pos:.2f}) — conviction reentry {_reentry_size_mult:.2f}x (BOUNCE_TOP_REENTRY_MULT)")
                else:
                    return "NO_ACTION", f"Wait_WT_Reentry_L(wt5={_wt1_5m:.0f},htf={'Y' if htf_confirm else 'N'},dc={_dc_pos:.2f})", 0.0, 0.0
            # Rising cross = strengthening trend
            if _wt_cross_rising is True or _wt_cross_rising_1h is True:
                _reentry_size_mult = float(getattr(config, 'BOUNCE_TOP_RISING_CROSS_MULT', 2.0))
                logger.info(f"[REENTRY_RISING] LONG {symbol}: WT cross at {_wt_cross_val:.0f} > prev {_wt_cross_prev:.0f} — trend strengthening, {_reentry_size_mult:.2f}x size (BOUNCE_TOP_RISING_CROSS_MULT)")
            # Divergence bonus stacks
            if _bull_div:
                _reentry_size_mult = min(2.0, _reentry_size_mult * 1.3)
                logger.info(f"[REENTRY_BULL_DIV] LONG {symbol}: Bullish divergence detected — {_reentry_size_mult:.1f}x size")
        else:
            wt_entry = _wt_cross_bear_5m or (_wt1_5m < _wt2_5m and _wt1_5m < float(i.get('wt1_5m_prev', _wt1_5m) or _wt1_5m))
            htf_confirm = (_wt1_15m < _wt2_15m) or (_wt1_1h < _wt2_1h)
            dc_pullback = _dc_pos > 0.60
            stoch_fallback = (k_5m < d_5m) and (k_15m < d_15m or k_15m > 70)
            if not ((wt_entry and htf_confirm) or (stoch_fallback and dc_pullback)):
                if _bear_div and dc_pullback:
                    _reentry_size_mult = float(getattr(config, 'BOUNCE_TOP_REENTRY_MULT', 1.5))
                    logger.info(f"[REENTRY_DIV] SHORT {symbol}: Bearish divergence + DC pullback({_dc_pos:.2f}) — conviction reentry {_reentry_size_mult:.2f}x (BOUNCE_TOP_REENTRY_MULT)")
                else:
                    return "NO_ACTION", f"Wait_WT_Reentry_S(wt5={_wt1_5m:.0f},htf={'Y' if htf_confirm else 'N'},dc={_dc_pos:.2f})", 0.0, 0.0
            # Falling cross = strengthening downtrend
            if _wt_cross_rising is False or _wt_cross_rising_1h is False:
                _reentry_size_mult = float(getattr(config, 'BOUNCE_TOP_RISING_CROSS_MULT', 2.0))
                logger.info(f"[REENTRY_FALLING] SHORT {symbol}: WT cross at {_wt_cross_val:.0f} < prev {_wt_cross_prev:.0f} — downtrend strengthening, {_reentry_size_mult:.2f}x size (BOUNCE_TOP_RISING_CROSS_MULT)")
            if _bear_div:
                _reentry_size_mult = min(2.0, _reentry_size_mult * 1.3)
                logger.info(f"[REENTRY_BEAR_DIV] SHORT {symbol}: Bearish divergence detected — {_reentry_size_mult:.1f}x size")

        # ── ALL GATES PASSED — Size based on LAST REDUCTION (not start size) ──
        _last_red_amt = abs(float(getattr(position, 'last_reduction_amount', 0) or 0))
        _start_qty = config.START_POSITION_SIZE / max(current_price, 1e-9)
        base_qty = max(_last_red_amt, _start_qty) if _last_red_amt > 0 else _start_qty  # Never less than what we just sold
        # 2026-04-26 — wires DEAD switch REENTRY_TIER1_SIZE_MULT_TRADIER. Default 1.5 preserves prior 1.5× cap behavior.
        _tier1_mult = float(getattr(config, 'REENTRY_TIER1_SIZE_MULT_TRADIER', 1.5))
        if max_q: base_qty = min(base_qty * _tier1_mult, max(1.0, max_q - positionAmt))

        dc_high_15m_val = float(i.get('dc_high_15m', 0))
        dc_low_15m_val = float(i.get('dc_low_15m', 0))
        dc_high_1h = float(i.get('dc_high_1h', 0))
        dc_low_1h = float(i.get('dc_low_1h', 0))
        dc_high_1h_ant = float(i.get('dc_high_1h_ant', dc_high_1h))
        dc_low_1h_ant = float(i.get('dc_low_1h_ant', dc_low_1h))
        dc_basis_1h_val = float(i.get('dc_basis_1h', (dc_high_1h + dc_low_1h) / 2 if dc_high_1h and dc_low_1h else 0))
        bb_upper_1h = float(i.get('bb_upper_1h', 0))
        bb_lower_1h = float(i.get('bb_lower_1h', 0))
        dc_high_4h = float(i.get('dc_high_4h', 0))
        dc_low_4h = float(i.get('dc_low_4h', 0))
        # GOLDEN_RULE: When ON, block entries that are too extended (above DC high / BB upper)
        # and load up on retests at basis after a confirmed breakout expansion.
        # When OFF, no DC-position filter — all entries at uniform 1x size (baseline behavior).
        # FIX 2026-05-08: originally only changed SIZE not ENTRY decision → pool_sharpe unchanged
        # (pnl_pct is price-based, size-independent). Real fix: block breakout entries so trade
        # COUNT differs, which changes pool_sharpe distribution.
        _m_ret = float(getattr(config, 'GOLDEN_RULE_MULT_RETEST', 5.0))
        size_mult = 1.0
        dc_info = "above_basis"
        if getattr(config, 'GOLDEN_RULE_ENABLED', True):
            if is_long:
                _above_dch = dc_high_1h > 0 and current_price > dc_high_1h
                _above_bbu = bb_upper_1h > 0 and current_price > bb_upper_1h
                _breakout = _above_dch or _above_bbu
                if _breakout:
                    return "NO_ACTION", f"GOLDEN_RULE_BLOCK price={current_price:.2f}>DC1h={dc_high_1h:.2f}/BB1h={bb_upper_1h:.2f}", 0.0, 0.0
                _retest = (dc_basis_1h_val > 0 and current_price >= dc_basis_1h_val
                           and dc_high_1h_ant > 0 and dc_high_1h > dc_high_1h_ant * 1.005)
                if _retest:
                    size_mult = _m_ret; dc_info = "RETEST_basis_1h_3x"
                elif dc_high_15m_val > 0 and current_price > dc_high_15m_val:
                    size_mult = 2.0; dc_info = "above_DC15m"
                elif dc_high_4h > 0 and current_price > dc_high_4h:
                    size_mult = 3.0; dc_info = "above_DC4h"
            else:
                _below_dcl = dc_low_1h > 0 and current_price < dc_low_1h
                _below_bbl = bb_lower_1h > 0 and current_price < bb_lower_1h
                _breakout = _below_dcl or _below_bbl
                if _breakout:
                    return "NO_ACTION", f"GOLDEN_RULE_BLOCK price={current_price:.2f}<DC1h={dc_low_1h:.2f}/BB1h={bb_lower_1h:.2f}", 0.0, 0.0
                _retest = (dc_basis_1h_val > 0 and current_price <= dc_basis_1h_val
                           and dc_low_1h_ant > 0 and dc_low_1h < dc_low_1h_ant * 0.995)
                if _retest:
                    size_mult = _m_ret; dc_info = "RETEST_basis_1h_3x"
                elif dc_low_15m_val > 0 and current_price < dc_low_15m_val:
                    size_mult = 2.0; dc_info = "below_DC15m"
                elif dc_low_4h > 0 and current_price < dc_low_4h:
                    size_mult = 3.0; dc_info = "below_DC4h"
        _gr_htf_min_tfs = int(getattr(config, 'GOLDEN_RULE_HTF_MIN_TFS', 0))
        if _gr_htf_min_tfs > 0:
            try:
                from golden_rule_htf import score_entry_htf as _gr_score_entry
                _gr_min_ind = int(getattr(config, 'GOLDEN_RULE_MIN_IND', 2))
                _gr_ok, _gr_ntfs, _gr_detail = _gr_score_entry(i, is_long, "tradier", _gr_htf_min_tfs, _gr_min_ind, current_price)
                if not _gr_ok:
                    return "NO_ACTION", f"GR_HTF_BLOCK {_gr_ntfs}of{_gr_htf_min_tfs}tfs {_gr_detail}", 0.0, 0.0
            except Exception as _gre:
                logger.warning(f"[GR_HTF] {symbol}: {_gre}")
        wt_count = sum([_wt1_5m > _wt2_5m, _wt1_15m > _wt2_15m, _wt1_1h > _wt2_1h]) if is_long else sum([_wt1_5m < _wt2_5m, _wt1_15m < _wt2_15m, _wt1_1h < _wt2_1h])
        qty = base_qty * size_mult * _reentry_size_mult
        confidence = 90.0 if (wt_count >= 2 and _reentry_size_mult >= 1.5) else (85.0 if wt_count >= 2 else 75.0)
        _rising_tag = "RISING_CROSS" if _wt_cross_rising is True else ("FALLING_CROSS" if _wt_cross_rising is False else "")
        _div_tag = "BULL_DIV" if (is_long and _bull_div) else ("BEAR_DIV" if (not is_long and _bear_div) else "")
        reason = (f"REENTRY_WT wt={wt_count}/3 dc={dc_info} dc_pos={_dc_pos:.2f} "
                  f"wt5={_wt1_5m:.0f} wt15={_wt1_15m:.0f} wt1h={_wt1_1h:.0f} "
                  f"cross_val={_wt_cross_val:.0f} mult={size_mult*_reentry_size_mult:.1f}x "
                  f"{_rising_tag} {_div_tag}".strip())

        return "REENTRY_OPEN", reason, confidence, qty

class DailyHistoryManager:
    def __init__(self, config):
        self.config = config
        self.file_path = config.DATA_DIR / "daily_klines.json"
        self.cache = {} # { "SYMBOL": { "2024-01-01": {open, high...}, ... } }
        self.lock = asyncio.Lock()
        self.dirty = False

    async def load(self):
        """Load accumulated history from disk"""
        try:
            if self.file_path.exists():
                async with aiofiles.open(self.file_path, 'r') as f:
                    content = await f.read()
                    if content.strip():
                        self.cache = json.loads(content)
                        logger.info(f"[History] Loaded daily data for {len(self.cache)} symbols")
        except Exception as e:
            logger.error(f"[History] Load error: {e}")

    async def save(self):
        """Save accumulated history to disk"""
        if not self.dirty: return
        try:
            async with self.lock:
                async with aiofiles.open(self.file_path, 'w') as f:
                    await f.write(json.dumps(self.cache, sort_keys=True))
                self.dirty = False
                logger.info("[History] Saved daily klines to disk")
        except Exception as e:
            logger.error(f"[History] Save error: {e}")

    async def update_symbol(self, symbol: str, api_history: List[Dict]):
        """Merge new API data with existing cache (deduplicate by date)"""
        if not api_history: return

        symbol = symbol.upper()
        if symbol not in self.cache: self.cache[symbol] = {}

        changed = False
        for candle in api_history:
            # Tradier history keys: date, open, high, low, close, volume
            date = candle.get('date')
            if not date: continue

            # Only update if missing or if data looks more complete
            if date not in self.cache[symbol]:
                self.cache[symbol][date] = candle
                changed = True

        if changed:
            self.dirty = True

    def calculate_daily_stats(self, symbol: str) -> Dict[str, float]:
        """Calculate SMA200, EMA20, ATR14 from cached history"""
        symbol = symbol.upper()
        if symbol not in self.cache: return {}

        # 1. Sort candles by date
        candles = sorted(self.cache[symbol].values(), key=lambda x: x['date'])
        if len(candles) < 20: return {} # Not enough data
        closes = [float(c['close']) for c in candles]
        highs = [float(c['high']) for c in candles]
        lows = [float(c['low']) for c in candles]
        stats = {}
        def calc_sma(period):
            if len(closes) < period: return 0.0
            return sum(closes[-period:]) / period

        # Helper: EMA
        def calc_ema(period):
            if len(closes) < period: return 0.0
            k = 2 / (period + 1)
            ema = sum(closes[:period]) / period # Start with SMA
            for price in closes[period:]:
                ema = (price * k) + (ema * (1 - k))
            return ema

        # Helper: ATR (Simplified WMA)
        def calc_atr(period=14):
            if len(closes) < period + 1: return 0.0
            tr_list = []
            for i in range(1, len(closes)):
                h, l, pc = highs[i], lows[i], closes[i-1]
                tr = max(h - l, abs(h - pc), abs(l - pc))
                tr_list.append(tr)

            # Simple average for initial, smoothed for rest (optional, keeping it simple SMA for robustness here)
            return sum(tr_list[-period:]) / period

        # Calculate Indicators
        stats['sma_200_D'] = calc_sma(200)
        stats['ema_20_D'] = calc_ema(20)
        stats['ema_50_D'] = calc_ema(50)
        stats['atr_D'] = calc_atr(14)

        # High/Low D (Last candle)
        stats['high_D'] = highs[-1]
        stats['low_D'] = lows[-1]

        # Donchian Daily (20 day)
        if len(highs) >= 20:
            stats['dc_high_D'] = max(highs[-20:])
            stats['dc_low_D'] = min(lows[-20:])

        return stats



def _apply_trc_overrides(cfg):
    """If running trc only, override config with aggressive TRC_ values for paper sandbox."""
    mapping = {"TRC_START_POSITION_SIZE": "START_POSITION_SIZE", "TRC_MAX_ORDER_VALUE": "MAX_ORDER_VALUE", "TRC_MAX_POSITION_SIZE": "MAX_POSITION_SIZE", "TRC_SCALP_START_SIZE": "SCALP_START_SIZE", "TRC_SCALP_MAX_POSITIONS_PER_SIDE": "SCALP_MAX_POSITIONS_PER_SIDE", "TRC_MAX_CONCURRENT_POSITIONS": "MAX_CONCURRENT_POSITIONS", "TRC_ROTATION_POSITION_SIZE": "ROTATION_POSITION_SIZE", "TRC_RSI2_POSITION_SIZE": "RSI2_POSITION_SIZE", "TRC_GAP_FILL_POSITION_SIZE": "GAP_FILL_POSITION_SIZE", "TRC_DC_DAYTRADE_START_SIZE": "DC_DAYTRADE_START_SIZE", "TRC_DC_DAYTRADE_LONG_BUDGET": "DC_DAYTRADE_LONG_BUDGET", "TRC_DC_DAYTRADE_SHORT_BUDGET": "DC_DAYTRADE_SHORT_BUDGET", "TRC_SWING_LONG_BUDGET": "SWING_LONG_BUDGET", "TRC_SWING_SHORT_BUDGET": "SWING_SHORT_BUDGET", "TRC_SCALP_LONG_BUDGET": "SCALP_LONG_BUDGET", "TRC_SCALP_SHORT_BUDGET": "SCALP_SHORT_BUDGET", "TRC_ENTRY_ZONE_LONG": "ENTRY_ZONE_LONG", "TRC_ENTRY_ZONE_SHORT": "ENTRY_ZONE_SHORT", "TRC_ENTRY_MIN_ALIGNMENT": "ENTRY_MIN_ALIGNMENT", "TRC_LS_RATIO_MIN": "LS_RATIO_MIN_TRADIER", "TRC_LS_RATIO_MAX": "LS_RATIO_MAX_TRADIER", "TRC_MAX_DAILY_LOSS_PCT": "MAX_DAILY_LOSS_PCT", "TRC_SCALP_TARGET_PCT": "SCALP_TARGET_PCT", "TRC_NOLOSS_MIN_PROFIT_PCT": "NOLOSS_MIN_PROFIT_PCT_TRADIER"}
    applied = []
    for src, dst in mapping.items():
        val = getattr(cfg, src, None)
        if val is not None:
            setattr(cfg, dst, val)
            applied.append(f"{dst}={val}")
    if hasattr(cfg, 'TRC_BEAR_MARKET_MODE'):
        cfg.BEAR_MARKET_MODE_TRADIER = cfg.TRC_BEAR_MARKET_MODE
        applied.append(f"BEAR_MARKET_MODE_TRADIER={cfg.TRC_BEAR_MARKET_MODE}")
    # YouTube Consensus experimental strategy overrides for trc (paper)
    _exp_map = {"TRC_ORB_ENABLED": "ORB_ENABLED", "TRC_EPISODIC_PIVOT_ENABLED": "EPISODIC_PIVOT_ENABLED", "TRC_SQUEEZE_ENABLED": "SQUEEZE_ENABLED", "TRC_MOMENTUM_FADE_ENABLED": "MOMENTUM_FADE_ENABLED_TRADIER", "TRC_CLENOW_ENABLED": "CLENOW_ENABLED", "TRC_SMFI_ENABLED": "SMFI_ENABLED", "TRC_MINERVINI_ENABLED": "MINERVINI_ENABLED", "TRC_CONNORS_RSI_ENABLED": "CONNORS_RSI_ENABLED", "TRC_ORB_POSITION_SIZE": "ORB_POSITION_SIZE", "TRC_EP_POSITION_SIZE": "EP_POSITION_SIZE", "TRC_ORB_LONG_BUDGET": "ORB_LONG_BUDGET", "TRC_ORB_SHORT_BUDGET": "ORB_SHORT_BUDGET", "TRC_CLENOW_POSITION_SIZE": "CLENOW_POSITION_SIZE", "TRC_SMFI_POSITION_SIZE": "SMFI_POSITION_SIZE", "TRC_MINERVINI_POSITION_SIZE": "MINERVINI_POSITION_SIZE", "TRC_CONNORS_RSI_POSITION_SIZE": "CONNORS_RSI_POSITION_SIZE", "TRC_SMFI_LONG_BUDGET": "SMFI_LONG_BUDGET", "TRC_SMFI_SHORT_BUDGET": "SMFI_SHORT_BUDGET", "TRC_MINERVINI_LONG_BUDGET": "MINERVINI_LONG_BUDGET"}
    for src, dst in _exp_map.items():
        val = getattr(cfg, src, None)
        if val is not None:
            setattr(cfg, dst, val)
            applied.append(f"{dst}={val}")
    logger.info(f"[TRC_SANDBOX] Applied {len(applied)} aggressive overrides: {', '.join(applied[:5])}... (+{max(0, len(applied)-5)} more)")

# ═══════════════════════════════════════════════════════════════
# GFV (Good Faith Violation) PROTECTION — T+1 Settlement Tracker
# ═══════════════════════════════════════════════════════════════
# Cash accounts: if you sell Stock A → buy Stock B with unsettled proceeds → sell Stock B before A settles = GFV.
# 4th GFV = account restricted 90 days. 5th = account closed.
# This tracker: records sales (unsettled proceeds), marks buys that use unsettled funds, blocks premature sells.
class GFVTracker:
    def __init__(self, data_dir):
        self.file_path = Path(data_dir) / "gfv_tracker.json"
        self.unsettled_sales = []  # [{account, symbol, amount, sold_at, settles_at}]
        self.restricted_positions = {}  # position_key -> {restricted_until, bought_at}
        self._load()
    @staticmethod
    def _next_business_day(from_date=None):
        d = from_date or datetime.now(ZoneInfo("US/Eastern"))
        if isinstance(d, datetime): d = d.date()
        d += timedelta(days=1)
        while d.weekday() >= 5:
            d += timedelta(days=1)
        return d
    def _load(self):
        try:
            if self.file_path.exists():
                with open(self.file_path, 'r') as f:
                    data = json.load(f)
                self.unsettled_sales = data.get('unsettled_sales', [])
                self.restricted_positions = data.get('restricted_positions', {})
                self._cleanup()
        except Exception as e:
            logger.error(f"[GFV] Load error: {e}")
    def _save(self):
        try:
            with open(self.file_path, 'w') as f:
                json.dump({'unsettled_sales': self.unsettled_sales, 'restricted_positions': self.restricted_positions}, f, indent=2)
        except Exception as e:
            logger.error(f"[GFV] Save error: {e}")
    def _cleanup(self):
        today_et = datetime.now(ZoneInfo("US/Eastern")).date()
        self.unsettled_sales = [s for s in self.unsettled_sales if datetime.fromisoformat(s['settles_at']).date() > today_et]
        expired = [k for k, v in self.restricted_positions.items() if datetime.fromisoformat(v['restricted_until']).date() <= today_et]
        for k in expired:
            del self.restricted_positions[k]
        if expired:
            logger.info(f"[GFV] Cleaned up {len(expired)} settled restrictions: {expired}")
    def get_unsettled_total(self, account_key):
        today_et = datetime.now(ZoneInfo("US/Eastern")).date()
        return sum(s['amount'] for s in self.unsettled_sales if s['account'] == account_key and datetime.fromisoformat(s['settles_at']).date() > today_et)
    def record_sale(self, account_key, symbol, amount):
        settles_at = self._next_business_day()
        self.unsettled_sales.append({'account': account_key, 'symbol': symbol, 'amount': round(amount, 2), 'sold_at': datetime.now(timezone.utc).isoformat(), 'settles_at': settles_at.isoformat()})
        self._save()
        logger.info(f"[GFV] Recorded sale: {account_key}:{symbol} ${amount:.2f} — settles {settles_at}")
    def record_buy_with_unsettled(self, position_key, settle_date):
        self.restricted_positions[position_key] = {'restricted_until': settle_date.isoformat(), 'bought_at': datetime.now(timezone.utc).isoformat()}
        self._save()
        logger.warning(f"[GFV] ⚠️ {position_key} bought with unsettled funds — CANNOT SELL until {settle_date}")
    def can_sell(self, position_key):
        if position_key not in self.restricted_positions:
            return True, ""
        today_et = datetime.now(ZoneInfo("US/Eastern")).date()
        restricted_until = datetime.fromisoformat(self.restricted_positions[position_key]['restricted_until']).date()
        if today_et >= restricted_until:
            del self.restricted_positions[position_key]
            self._save()
            return True, ""
        return False, f"GFV_BLOCK: bought with unsettled funds, hold until {restricted_until}"
    def get_earliest_settlement(self, account_key):
        today_et = datetime.now(ZoneInfo("US/Eastern")).date()
        dates = [datetime.fromisoformat(s['settles_at']).date() for s in self.unsettled_sales if s['account'] == account_key and datetime.fromisoformat(s['settles_at']).date() > today_et]
        return max(dates) if dates else None
    def status_summary(self):
        self._cleanup()
        lines = [f"Unsettled sales: {len(self.unsettled_sales)}", f"Restricted positions: {len(self.restricted_positions)}"]
        for pk, v in self.restricted_positions.items():
            lines.append(f"  {pk} → hold until {v['restricted_until']}")
        return "\n".join(lines)

class TradierTradeManager:
    def __init__(self, account_list: List[str] = None):
        self.config = config
        self.target_accounts = account_list or ['trc','trb']
        if self.target_accounts == ['trc'] or (len(self.target_accounts) == 1 and self.target_accounts[0] == 'trc'):
            _apply_trc_overrides(config)
        self.last_symbol_refresh = 0
        self.refresh_interval = 60  
        for acc in self.target_accounts:
            setattr(self, f"symbols_long_{acc}", [])
            setattr(self, f"symbols_short_{acc}", [])
        self.order_queue = OrderQueue(self)
        self.reader = PositionReader(config)
        self.position_manager = self.reader
        self.logger = logger
        self.api_client: Optional[TradierAPIClient] = None      
        self.non_shortable_symbols = config.NON_SHORTABLE
        self.always_tradeable_trb = config.ALWAYS_TRADEABLE
        self.exceptions = config.EXCEPTIONS
        self.blacklist = getattr(config, 'BLACKLIST', set())
        self.limit_normal_order = getattr(config, 'MAX_ORDER_VALUE', 2000.0) 
        self.limit_exception_order = self.limit_normal_order * 4.0  
        self.limit_total_pos = getattr(config, 'MAX_POSITION_SIZE', 5000.0)
        self.limit_exception_total_pos =  getattr(config, 'MAX_POSITION_SIZE', 5000.0) * 4.0  
        self.strategy = StockStrategy(config, trade_manager=self)
        # Delta Engine — WHALE_LTF: Sharpe 63.44, 85.9% WR
        # Stock-specific RZ overrides: 5m base TF (not 3m), looser baseline tolerance,
        # stock-friendly k_entry/k_exit. All RZ_* fields read from config_tradier so
        # the perpetual sweep can override them via V8_OVERRIDE_FILE.
        # User directive 2026-04-10: stocks exit ONLY on 1h/4h/D slowdown. Override
        # DELTA_TF_WEIGHTS to HTF-only so LTF noise can't fire delta exits. This also
        # makes wt_dc_delta._tfs iterate only HTFs for exit computation.
        _stock_tf_weights = getattr(config, 'DELTA_TF_WEIGHTS_STOCK', None) or {"1h": 2.0, "4h": 3.0, "D": 2.0}
        self.delta_tracker = DeltaTracker(cfg={
            "speed_smooth": config.DELTA_SPEED_SMOOTH, "accel_lookback": config.DELTA_ACCEL_LOOKBACK,
            "tf_weights": _stock_tf_weights, "tf_z_threshold": config.DELTA_TF_Z_THRESHOLD,
            "entry_z_threshold": config.DELTA_ENTRY_Z_THRESHOLD, "entry_accel_threshold": config.DELTA_ENTRY_ACCEL_THRESHOLD,
            "entry_min_tf": config.DELTA_ENTRY_MIN_TF, "exit_speed_decay_ratio": config.DELTA_EXIT_DECAY_RATIO,
            "exit_accel_threshold": config.DELTA_EXIT_ACCEL_THRESHOLD, "exit_opposing_ratio": config.DELTA_EXIT_OPPOSING_RATIO,
            "exit_min_tf_lost": config.DELTA_EXIT_MIN_TF_LOST, "exit_min_hold": config.DELTA_EXIT_MIN_HOLD,
            "pyramid_accel_threshold": config.DELTA_PYRAMID_ACCEL_THRESHOLD, "pyramid_min_bars": config.DELTA_PYRAMID_MIN_BARS,
            "pyramid_price_tolerance": config.DELTA_PYRAMID_PRICE_TOL, "pyramid_qty_mult": config.DELTA_PYRAMID_QTY_MULT,
            "pyramid_max": config.DELTA_PYRAMID_MAX, "z_window": config.DELTA_Z_WINDOW,
            # RZ params — stock-tuned defaults (looser than crypto)
            "rz_ltf_micro": getattr(config, "RZ_LTF_MICRO", "5m"),
            "rz_baseline_tol": getattr(config, "RZ_BASELINE_TOL", 0.05),
            "rz_top_bb": getattr(config, "RZ_TOP_BB_THRESHOLD", 0.85),
            "rz_bot_bb": getattr(config, "RZ_BOT_BB_THRESHOLD", 0.15),
            "rz_legs_min": getattr(config, "RZ_LEGS_MIN", 10.0),
            "rz_require_struct": getattr(config, "RZ_REQUIRE_STRUCT", False),
            "rz_k_exit": getattr(config, "RZ_K_EXIT", 80.0),
            "rz_k_entry_max": getattr(config, "RZ_K_ENTRY_MAX", 50.0),
            "rz_mfi_exit": getattr(config, "RZ_MFI_EXIT", 85.0),
            "rz_entry_enabled": getattr(config, "RZ_ENTRY_ENABLED", True),
            "rz_exit_enabled": getattr(config, "RZ_EXIT_ENABLED", True),
            # NEW 2026-04-11: feature toggles for ablation testing
            "rz_zscore_zone_enabled": getattr(config, "RZ_ZSCORE_ZONE_ENABLED", True),
            "rz_div_block_min": getattr(config, "RZ_DIV_BLOCK_MIN", 2),
            "rz_two_phase_exit_enabled": getattr(config, "RZ_TWO_PHASE_EXIT_ENABLED", True),
            "rz_div_exit_enabled": getattr(config, "RZ_DIV_EXIT_ENABLED", True),
            "rz_zscore_exit_enabled": getattr(config, "RZ_ZSCORE_EXIT_ENABLED", True),
        }) if config.DELTA_ENGINE_ENABLED else None
        self.running = False
        self.background_tasks = []
        self.partial_profit_lock_state = {}
        self.account_position_queues = defaultdict(lambda: {'queue': deque(), 'set': set()})
        self.recently_processed_signals: Dict[str, float] = {}
        self.order_deduplication: Dict[str, float] = {}
        self.dedupe_lock = asyncio.Lock()
        self.active_order_locks = {}
        self.price_cache = {}
        self.price_update_time = {}
        self.managers: Dict[str, Any] = {} 
        self.symbols: List[str] = []
        self.last_exit_prices = {}
        self.last_exit_times = {}  # Track when positions were exited (symbol -> timestamp)
        # self.position_manager: Optional[TradierPositionManager] = None
        # Indicators cache (from ez_indicators)
        self.redis_manager = None
        self.indicators_cache: Dict[str, Dict] = {}
        self.last_indicators_update = 0.0
        self.redis_indicators_full_cache: Dict[str, Any] = {}
        self.redis_indicators_cache_time = 0.0
        self.last_breadth_calc_time = 0
        self.cached_breadth_score = 0.0
        self.rankings_cache: Dict[str, Any] = {}
        self.sentiment_history = deque(maxlen=40) # Keep last 10 readings
        self.last_sentiment_update = 0
        self.redis_client = None
        self.symbols_long_tra: List[str] = []
        self.symbols_short_tra: List[str] = []
        self.symbols_long_trb: List[str] = []
        self.symbols_short_trb: List[str] = []
        self.symbols_long_trc: List[str] = []
        self.symbols_short_trc: List[str] = []
        self.min_qty: Dict[str, float] = {}  # symbol -> min_qty (in shares) - set to 1 for now
        self.positions: Dict[str, Any] = {}  # position_key -> position
        self.positions_by_account: Dict[str, Dict[str, Any]] = defaultdict(dict)  # account_key -> {position_key -> position}
        self.processing_keys: TrackingSet = TrackingSet()
        self.position_last_processed: Dict[str, float] = {}
        self._gap_fill_positions: Dict[str, Dict] = {}
        self._gap_fill_ran_today: str = ''
        # YouTube Consensus strategy state
        self._orb_levels: Dict[str, Dict] = {}  # symbol -> {orb_high, orb_low, orb_range, orb_midpoint}
        self._orb_positions: Dict[str, Dict] = {}  # pk -> {entry, stop, target, direction}
        self._orb_ran_today: str = ''
        self._orb_entries_today: Dict[str, int] = {}  # account_key -> count
        self._extra_indicators_cache: Dict[str, Dict] = {}  # symbol -> extra indicators
        self._extra_indicators_cache_time: Dict[str, float] = {}  # symbol -> last compute time
        self.recently_queued_signals: Dict[str, float] = {}
        self.indicators_snapshot: Dict[str, Dict] = {}
        self.active_maker_orders: Dict[str, Dict] = {}
        self.orders_in_limbo: Dict[str, Dict[str, float]] = {}
        self.augmentation_cooldown_map: Dict[str, Any] = {}
        self.reduction_cooldown_map: Dict[str, Any] = {}
        self.augmented_positions: set = set()
        self.positions_to_gracefully_exit: set = set()
        self.tradeable_keys: set = set()
        self._last_tradeable_update: float = 0.0
        self.tradeable_lock = asyncio.Lock()
        self.stop_levels: Dict[str, List] = {}
        self.stop_breach_tracker: Dict[str, float] = {}
        self.accounts: Dict[str, Any] = {'tra': SimpleNamespace(account_key='tra')}
        self.active_order_locks: Dict[str, Dict[str, Any]] = {}
        self._local_locks: Dict[str, Dict[str, float]] = {}
        self.last_symbol_monitored: Dict[str, float] = defaultdict(float)
        self.last_monitored_positions: Dict[str, float] = defaultdict(float)
        self.price_cache_2: Dict[str, Dict[str, Any]] = {}
        self.price_cache_3: Dict[str, Dict[str, Any]] = {}
        self.queue_metrics: Dict[str, Dict[str, float]] = defaultdict(dict)
        self.concurrency_health: Dict[str, Dict[str, float]] = defaultdict(dict)
        self._position_update_timestamps: Dict[str, datetime] = {}
        self._last_stale_reload_ts = 0.0
        self._metrics_runner = None
        self._metrics_site = None
        self.reversed_positions: Dict[str, float] = {}
        self.direct_high_gain: Dict[str, Any] = {}
        self.reenter_data: Dict[str, Any] = {}
        self.reentry_plans: Dict[str, Any] = {}
        self._stable_valid_keys_cache: Dict[str, set] = {}
        self.reduced_positions = {}
        self.position_validation_task = None
        self.rejection_cooldowns = {}
        self.corr_matrix: Optional[CorrelationMatrix] = None
        self.hedge_engine: Optional[TradierHedgeEngine] = None
        self.no_loss_policy = StrictNoLossPolicy()
        if not hasattr(config, 'ACCOUNT_KEYS'):
            config.ACCOUNT_KEYS = ['trc','trb']
        if not hasattr(config, 'SLEEP_TIME_PER_TASKS'):
            config.SLEEP_TIME_PER_TASKS = 15
        self.market_snapshot = {}
        self._dc_range_history: Dict[str, deque] = defaultdict(lambda: deque(maxlen=8))
        self.last_market_data_ts = None  # To track the "version" of data
        self.last_json_mtime = 0   
        self._redis_circuit_broken = False
        self.clock_offset = 0.0
        self.ready_to_trade = True
        self.gfv_tracker = GFVTracker(config.DATA_DIR)

    async def initialize_all_position_fields(self):
        """Ensure ALL position fields are initialized"""
        if not self.position_manager:
            return
        for position_key, position in self.position_manager.positions.items():
            await self.validate_and_complete_position(position_key, position)

    def get_position(self, position_key: str):
        return self.reader.positions.get(position_key)
    # async def get_position(self, position_key: str) -> Optional[Any]:
    #     """Get position by position_key"""
    #     if not self.position_manager:
    #         return None
    #     # FIXED: Direct access to flat dictionary
    #     return self.position_manager.positions.get(position_key)
    #     try:
    #         if position_key.count(":") != 1:
    #             logger.error(f"[get_position] !! CORRUPTED position_key with {position_key.count(':')} colons: {position_key}")
    #             from utils import clean_position_key, orjson_default
    #             position_key = clean_position_key(position_key)
    #             if position_key.count(":") != 1:
    #                 return None
    #         parts = position_key.split(':', 1)
    #         if len(parts) != 2:
    #             return None
    #         account_key, symbol_side = parts
    #         symbol_side_parts = symbol_side.split('_', 1)
    #         if len(symbol_side_parts) != 2:
    #             return None
    #         symbol, position_side = symbol_side_parts
    #         return self.position_manager.positions.get(position_side, {}).get(symbol)
    #     except Exception:
    #         return None


    async def load_account_symbols(self, force=False):
        """
        Periodically syncs disk JSONs to memory.
        Trading loops should read self.symbols_long_tra, etc.
        """
        now = time.time()
        # Only touch the disk if forced or if interval has passed
        if not force and (now - self.last_symbol_refresh < self.refresh_interval):
            return

        try:
            base = Path(self.config.BASE_PATH)

            for acc in self.target_accounts:
                # tra is satoshit-only — must read symbols_tra_satoshit_*.json,
                # not the generic symbols_tra_*.json (which is left as a legacy
                # fallback only). Also, tra is long-only (cash account) so the
                # short list is force-emptied here regardless of disk state.
                _tra_sat = (acc == 'tra' and getattr(self.config, 'TRA_SATOSHIT_ONLY', False))
                _tra_long_only = (acc == 'tra' and getattr(self.config, 'TRA_LONG_ONLY', True))
                for side in ["long", "short"]:
                    if _tra_long_only and side == "short":
                        setattr(self, f"symbols_{side}_{acc}", [])
                        continue
                    if _tra_sat:
                        file_path = base / f"symbols_{acc}_satoshit_{side}.json"
                        if not file_path.exists():
                            file_path = base / f"symbols_{acc}_{side}.json"
                    else:
                        file_path = base / f"symbols_{acc}_{side}.json"

                    if file_path.exists():
                        # Non-blocking read
                        def read_file():
                            with open(file_path, 'r') as f:
                                return json.load(f)

                        raw_data = await asyncio.to_thread(read_file)

                        # Process data into a clean list
                        if isinstance(raw_data, list):
                            processed = [str(s).strip().upper() for s in raw_data if s]
                        elif isinstance(raw_data, dict):
                            processed = [str(k).strip().upper() for k in raw_data.keys() if k]
                        else:
                            processed = []

                        # Update the MEMORY variable (Atomic pointer swap)
                        setattr(self, f"symbols_{side}_{acc}", sorted(list(set(processed))))
            self.last_symbol_refresh = now
            logger.info(f"💾 [DISK_SYNC] Symbols reloaded into memory.")

        except Exception as e:
            logger.error(f"!! [SYMBOL_SYNC_FAIL] {e}")


    def update_sentiment_history(self, current_score: float):
        """Updates the rolling history of market sentiment."""
        # Only update if enough time has passed (e.g., 5 mins) to capture meaningful trends
        if time.time() - self.last_sentiment_update > 300:
            self.sentiment_history.append(current_score)
            self.last_sentiment_update = time.time()

    def get_sentiment_direction(self) -> str:
        """Returns 'IMPROVING', 'DEGRADING', or 'FLAT'"""
        if len(self.sentiment_history) < 3: return "FLAT"

        # Simple average of recent vs older to determine slope
        recent = sum(list(self.sentiment_history)[-3:]) / 3
        older = sum(list(self.sentiment_history)[:3]) / 3

        if recent > older + 5: return "IMPROVING"
        if recent < older - 5: return "DEGRADING"
        return "FLAT"

    def is_symbol_allowed(self, account_key: str, symbol: str, position_side: str) -> bool:
        symbol = symbol.upper().strip()

        # 1. ALWAYS ALLOW EXISTING POSITIONS (Management/Exit)
        pk = f"{account_key}:{symbol}_{position_side}"
        pos = self.position_manager.positions.get(pk)
        if pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0:
            return True

        # 2. ACCOUNT SPECIFIC LOGIC

        # --- TRB (Leaderboards + Whitelist) ---
        if account_key == 'trb':
            # A. Check Whitelist
            if symbol in [s.upper() for s in self.always_tradeable_trb]:
                # Intersection Logic:
                in_winners = symbol in [s.upper() for s in self.symbols_long_trb]
                in_losers = symbol in [s.upper() for s in self.symbols_short_trb]

                # If "Nowhere" (not in either leaderboard) -> Allow BOTH sides
                if not in_winners and not in_losers:
                    return True

                # If it IS in a leaderboard, it must match that list's side
                if position_side == 'LONG' and in_winners: return True
                if position_side == 'SHORT' and in_losers: return True

                # If it's in Winners but we want Short -> Block
                return False

            # B. Check Standard Leaderboards for TRB
            if position_side == 'LONG':
                return symbol in [s.upper() for s in self.symbols_long_trb]
            elif position_side == 'SHORT':
                return symbol in [s.upper() for s in self.symbols_short_trb]

        # --- TRA (Strict Leaderboards) ---
        elif account_key == 'tra':
            if position_side == 'LONG':
                return symbol in [s.upper() for s in self.symbols_long_tra]
            elif position_side == 'SHORT':
                return symbol in [s.upper() for s in self.symbols_short_tra]

        # --- TRC (Strict Leaderboards) ---
        elif account_key == 'trc':
            if position_side == 'LONG':
                return symbol in [s.upper() for s in self.symbols_long_trc]
            elif position_side == 'SHORT':
                return symbol in [s.upper() for s in self.symbols_short_trc]
        return False

    def allows_side(self, account_key: str, position_side: str, symbol: str = None) -> bool:
        """Check if account allows trading this side - compatibility method for ez_manage"""
        # For Tradier, both LONG and SHORT are allowed
        return True

    def get_positions_by_account(self, account_key: str) -> Dict[str, Any]:
            """Get all positions for an account using flat dict"""
            if not self.position_manager:
                return {}

            result = {}
            # FIXED: Iterate the flat dictionary directly
            for pk, position in self.position_manager.positions.items():
                # Basic filter: check if key starts with account or assumes 'tra'
                if pk.startswith(f"{account_key}:") and position:
                    # FIXED: use positionAmt
                    if abs(float(getattr(position, 'positionAmt', 0))) >= 0:
                        result[pk] = position
            return result

    async def is_data_fresh(self, symbol: str, position_key: str = None) -> tuple[bool, str, bool, Dict[str, Any]]:
        now = datetime.now(timezone.utc)
        indicators = self.get_indicators(symbol)
        ts_obj = indicators.get('timestamp_1m') or indicators.get('timestamp')
        if not isinstance(ts_obj, datetime):
            ts_obj = safe_datetime(ts_obj)
        if not ts_obj:
            return False, "No Timestamp", False, indicators
        age = (now - ts_obj).total_seconds()
        if age < -1.0: age = 0.1 
        m1_fresh = (age < 300.0)  # Was 90s — too tight for stocks. Low-volume symbols don't tick every minute.
        macro_fresh = (age < 1200.0)  # 20 min — indicators cycle is ~2-5 min
        return macro_fresh, f"Age:{age:.0f}s", m1_fresh, indicators

    def load_indicators_from_json(self, symbol: str) -> Dict[str, Any]:
        global _GLOBAL_JSON_CACHE, _GLOBAL_JSON_TS, _GLOBAL_JSON_LOCK
        try:
            symbol = symbol.strip().upper()
            now = time.time()
            
            with _GLOBAL_JSON_LOCK:
                if now - _GLOBAL_JSON_TS < 3.0 and _GLOBAL_JSON_CACHE:
                    return _GLOBAL_JSON_CACHE.get(symbol, {})

                search_dirs = [config.DATA_DIR, config.DATA_DIR / "tradier"]
                for d in search_dirs:
                    # Use newest TIMESTAMPED file, fallback to latest
                    ts_files = sorted(d.glob("tradier_indicators_[0-9]*.json"), key=lambda f: f.stat().st_mtime, reverse=True)
                    target = ts_files[0] if ts_files else d / "tradier_indicators_latest.json"
                    if target.exists():
                        try:
                            with open(target, 'r') as f:
                                data = json.loads(f.read())
                                if isinstance(data, dict) and data:
                                    _GLOBAL_JSON_CACHE = data
                                    _GLOBAL_JSON_TS = now
                                    return data.get(symbol, {})
                        except Exception: pass
                candidates =[]
                import re
                pattern = re.compile(r"tradier_indicators_\d{8}_\d{6}\.json")
                for d in search_dirs:
                    if not d.exists(): continue
                    for f in d.iterdir():
                        if f.is_file() and pattern.match(f.name):
                            candidates.append(f)
                if not candidates: return {}
                candidates.sort(key=lambda p: p.name, reverse=True)
                for file_path in candidates[:3]:
                    try:
                        with open(file_path, 'rb') as f:
                            content = f.read()
                            if not content.strip(): continue
                            data = safe_json_loads(content)
                            if isinstance(data, dict):
                                _GLOBAL_JSON_CACHE = data
                                _GLOBAL_JSON_TS = now
                                return data.get(symbol, {})
                    except (JSONDecodeError, OSError):
                        continue 
            return {}
        except Exception as e:
            logger.error(f"[JSON_LOAD] Error loading for {symbol}: {e}")
            return {}

    async def ensure_position_present(self, account_key: str, symbol: str, position_side: str, position_key: str) -> Optional[Any]:
        """Return existing position from memory. NEVER fabricate empty positions — positions are created once when symbol is added."""
        if not self.position_manager:
            return None
        if position_key in self._phantom_killed_keys:
            return self.position_manager.positions.get(position_key)
        position = self.position_manager.positions.get(position_key)
        if not position:
            logger.debug(f"[ensure_position_present] {position_key} not in memory — searching disk/backups")
            try:
                side_file = f"{position_side.lower()}_positions.json"
                file_path = Path(config.BASE_PATH) / account_key / side_file
                if file_path.exists():
                    import json as _json
                    with open(file_path) as f:
                        data = _json.load(f)
                    if isinstance(data, dict) and 'positions' in data: data = data['positions']
                    pos_data = data.get(position_key)
                    if pos_data and isinstance(pos_data, dict):
                        from tradier_positions import TradierPosition
                        position = TradierPosition.from_dict(pos_data) if hasattr(TradierPosition, 'from_dict') else TradierPosition(**{k: v for k, v in pos_data.items() if k in TradierPosition.__dataclass_fields__}) if hasattr(TradierPosition, '__dataclass_fields__') else None
                        if position:
                            self.position_manager.positions[position_key] = position
                            logger.debug(f"[ensure_position_present] Loaded {position_key} from disk")
            except Exception as e:
                logger.debug(f"[ensure_position_present] Disk load failed for {position_key}: {e}")
        return position

    async def periodic_sentiment_rebalancing(self):
        """
        Scans all positions. Calculates ideal size based on CURRENT sentiment.
        Reduces size if sentiment degrades. Augments if sentiment improves.
        """
        logger.info("[sentiment_rebalancer] Task started")
        while self.running:
            try:
                # Run every 60 seconds (or 2m) to avoid over-trading
                await asyncio.sleep(60)

                if not self.position_manager: continue

                # Iterate a COPY of the items to avoid modification issues
                current_positions = list(self.position_manager.positions.items())

                for pk, pos in current_positions:
                    try:
                        # 1. Basic Parse
                        if not pos or abs(getattr(pos, 'positionAmt', 0)) == 0: continue

                        symbol = getattr(pos, 'symbol', '')
                        side = getattr(pos, 'position_side', 'LONG')
                        current_qty = abs(float(pos.positionAmt))

                        current_price = float(getattr(pos, 'mark_price', 0))

                        if current_price <= 0: current_price,ts=await self.get_current_price(symbol)
                        current_price = float(current_price)

                        # Cooldown: skip if reduced or augmented within last 30 min
                        _rebal_cooldown_s = 30 * 60
                        _last_red = getattr(pos, 'last_reduction_time', None)
                        _last_aug = getattr(pos, 'last_augmentation_time', None)
                        _now_ts = time.time()
                        def _age_s(dt):
                            if not dt: return 9999
                            try:
                                ts_ = dt.timestamp() if hasattr(dt, 'timestamp') else float(dt)
                                return _now_ts - ts_
                            except Exception: return 9999
                        if _age_s(_last_red) < _rebal_cooldown_s or _age_s(_last_aug) < _rebal_cooldown_s:
                            continue
                        # 2026-04-27 user rule: stop the headless-chicken loop. last_reduction_time
                        # is only set on SUCCESS — failed/rejected orders never updated it, so the
                        # rebalance retried every 60s. Track ATTEMPT timestamps separately and
                        # refuse to re-attempt within 5 min, success or fail.
                        if not hasattr(self, '_rebal_attempt_ts'):
                            self._rebal_attempt_ts = {}
                        _attempt_age = _now_ts - self._rebal_attempt_ts.get(pk, 0.0)
                        _attempt_cooldown_s = float(getattr(config, 'REBAL_ATTEMPT_COOLDOWN_SEC', 300.0))
                        if _attempt_age < _attempt_cooldown_s:
                            logger.info(f"[REBAL_ATTEMPT_COOLDOWN] {pk}: last attempt {_attempt_age:.0f}s ago < {_attempt_cooldown_s:.0f}s — skip re-fire")
                            continue
                        # 2026-04-27 user rule: 72h min hold also gates SENTIMENT_FADE rebalance.
                        # Stocks must not be flipped on bias inside the hold window.
                        _rebal_min_hold = float(getattr(config, 'TRADIER_MIN_HOLD_MINUTES', getattr(config, 'MIN_HOLD_MINUTES_TRADIER', 240.0)))
                        try:
                            _opened_at = getattr(pos, 'opened_at', None)
                            _hold_min = (datetime.now(timezone.utc) - _opened_at).total_seconds() / 60.0 if _opened_at else 0.0
                        except Exception:
                            _hold_min = 0.0
                        if _hold_min < _rebal_min_hold:
                            logger.info(f"[REBAL_MIN_HOLD_BLOCK] {pk}: hold={_hold_min:.0f}m<{_rebal_min_hold:.0f}m — SENTIMENT_FADE rebalance gated")
                            continue

                        # 2. Get Fresh Indicators
                        i = self.get_indicators(symbol)
                        if not i: continue
                        base_qty = config.START_POSITION_SIZE / current_price
                        ideal_qty = await self.strategy.calculate_quantity_complex(
                            symbol, "REBALANCE", side, base_qty, i, pos)
                        if current_qty == 0: continue
                        deviation_pct = (ideal_qty - current_qty) / current_qty
                        if deviation_pct < -0.20:
                            qty_to_reduce = current_qty - ideal_qty
                            if qty_to_reduce * current_price > 100: # Only if moving > $100
                                # STRICT_NO_LOSS: Never reduce at a loss — IBIT disaster -$3,148 on Mar 5-11
                                _gain_pct = getattr(pos, 'gain', 0) or 0
                                if _gain_pct < 0.3:
                                    logger.info(f"[REBAL_NOLOSS_BLOCK] {symbol} {side}: sentiment wants reduce but gain={_gain_pct:+.2f}% < 0.3% — HOLDING (STRICT_NO_LOSS)")
                                    continue
                                # V4: Multi-TF WT exit confirmation — don't rebalance if HTFs still support
                                _wt_b5 = float(i.get('wt1_5m', 0) or 0) > float(i.get('wt2_5m', 0) or 0)
                                _wt_b15 = float(i.get('wt1_15m', 0) or 0) > float(i.get('wt2_15m', 0) or 0)
                                _wt_b1h = float(i.get('wt1_1h', 0) or 0) > float(i.get('wt2_1h', 0) or 0)
                                _wt_b4h = float(i.get('wt1_4h', 0) or 0) > float(i.get('wt2_4h', 0) or 0)
                                if side == "LONG":
                                    _rebal_tf_against = sum([1 for b in [_wt_b5, _wt_b15, _wt_b1h, _wt_b4h] if not b])
                                else:
                                    _rebal_tf_against = sum([1 for b in [_wt_b5, _wt_b15, _wt_b1h, _wt_b4h] if b])
                                if _rebal_tf_against < 2:
                                    logger.info(f"[REBAL_WT_BLOCK] {symbol} {side}: sentiment wants reduce but only {_rebal_tf_against}/4 TFs against — HOLDING")
                                    continue
                                reason = f"SENTIMENT_FADE ideal={int(ideal_qty)} cur={int(current_qty)} loc={i.get('0market_sentiment_local',0):.1f} WT{_rebal_tf_against}TF"
                                logger.info(f"📉 {symbol} REBALANCE REDUCE: {reason}")
                                _acct = pk.split(':')[0] if ':' in pk else 'trb'
                                # Stamp attempt BEFORE firing so a failed order still blocks re-fire for cooldown window.
                                self._rebal_attempt_ts[pk] = _now_ts
                                await self.execute_trade_action(
                                    _acct, pk, symbol, qty_to_reduce, current_price,
                                    "SELL" if side == "LONG" else "BUY",
                                    side, f"rebal_{int(time.time())}",
                                    action="REDUCE", reason=reason, override_qty=qty_to_reduce)
                        elif deviation_pct > 0.25:
                            # Use higher threshold for adding to be conservative
                            qty_to_add = ideal_qty - current_qty

                            # Check Profitability Gate (don't add to losers usually, unless deep value strategy)
                            # Assuming trend following: only add to winners
                            pnl_pct = getattr(pos, 'gain', 0)
                            if pnl_pct > 0.5: # Only add if slightly green
                                if qty_to_add * current_price > 100:
                                    reason = f"SENTIMENT_BOOST ideal={int(ideal_qty)} cur={int(current_qty)} loc={i.get('0market_sentiment_local',0):.1f}"
                                    logger.info(f"📈 {symbol} REBALANCE AUGMENT: {reason}")
                                    await self.execute_trade_action(
                                        'tra', pk, symbol, qty_to_add, current_price,
                                        "BUY" if side == "LONG" else "SELL",
                                        side, f"rebal_{int(time.time())}",
                                        action="AUGMENT", reason=reason, override_qty=qty_to_add  )

                    except Exception as inner_e:
                        logger.error(f"[rebalancer] Error processing {pk}: {inner_e}")
                        continue

            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[rebalancer] Global Error: {e}",  exc_info=True )
                await asyncio.sleep(10)

    async def get_market_context(self, symbol: str) -> dict:
        """
        Determines market bias based on SPY and QQQ.
        Returns a dict: {'bias': float (-1.0 to 1.0), 'is_tech': bool}
        """
        # 1. Determine if target is likely Tech (Heavy QQQ correlation)
        # Simple list for example - ideally load from a config/json
        tech_tickers = {'NVDA', 'AMD', 'TSLA', 'AAPL', 'MSFT', 'GOOG', 'META', 'AMZN', 'NFLX', 'INTC', 'QCOM', 'MU'}
        is_tech = symbol.upper() in tech_tickers

        # 2. Fetch Index Data
        spy_ind = self.get_indicators("SPY")
        qqq_ind = self.get_indicators("QQQ")

        bias_score = 0.0

        # Helper to calculate score for an index
        def calc_index_score(ind):
            if not ind: return 0.0
            score = 0.0
            # 15m Trend (Primary)
            price = float(ind.get('current_price', 0) or ind.get('close_15m', 0))
            ema20 = float(ind.get('ema_20_15m', 0))
            if price > ema20: score += 0.5
            else: score -= 0.5

            # Momentum (Secondary)
            ha = ind.get('ha_15m', 'neutral')
            if ha == 'green': score += 0.5
            elif ha == 'red': score -= 0.5

            return score

        # 3. Weighting
        spy_score = calc_index_score(spy_ind)
        qqq_score = calc_index_score(qqq_ind)

        if is_tech:
            # If Tech, weight QQQ 70%, SPY 30%
            bias_score = (qqq_score * 0.7) + (spy_score * 0.3)
        else:
            # Otherwise, weight SPY 70%, QQQ 30%
            bias_score = (spy_score * 0.7) + (qqq_score * 0.3)

        return {'bias': bias_score, 'is_tech': is_tech}

    # --------------------------------------------------------------------------
    # PORTFOLIO BALANCING LOGIC
    # --------------------------------------------------------------------------
    def get_market_movement_intensity(self) -> float:
        snap = self.market_snapshot
        if not snap: return 50.0
        
        movement_components = []
        for symbol, data in snap.items():
            symbol_score = 0
            count = 0
            price = float(data.get('current_price', 0))
            if price <= 0: continue

            # 1. Volatility (ATR)
            atr15, atr5 = float(data.get('atr_15m', 0)), float(data.get('atr_5m', 0))
            if atr15 > 0:
                symbol_score += min((atr15 / price) * 1000, 50); count += 1
            if atr5 > 0:
                symbol_score += min((atr5 / price) * 1000, 30); count += 1

            # 2. Relative Volume
            rv15, rv5 = float(data.get('rel_vol_15m', 1.0)), float(data.get('rel_vol_5m', 1.0))
            if rv15 > 1.0: symbol_score += (rv15 - 1.0) * 20; count += 1
            if rv5 > 1.0: symbol_score += (rv5 - 1.0) * 10; count += 1

            # 3. RSI Momentum
            rsi15, rsi5 = float(data.get('rsi_15m', 50)), float(data.get('rsi_5m', 50))
            symbol_score += (abs(rsi15 - 50) / 50.0) * 10; count += 1
            symbol_score += (abs(rsi5 - 50) / 50.0) * 5; count += 1

            if count > 0:
                movement_components.append(min(symbol_score / count, 100))

        return sum(movement_components) / len(movement_components) if movement_components else 50.0

    def calculate_unified_market_ratio(self) -> float:
        snap = self.market_snapshot
        if not snap: return 0.5
        bullish_count, total_valid = 0, 0
        global_sent_total, sent_count = 0.0, 0
        
        for sym, data in snap.items():
            price = float(data.get('current_price', 0))
            ema15 = float(data.get('ema_20_15m', 0)) # Intraday anchor
            sent = float(data.get('0market_sentiment_score', 0))
            
            if price > 0 and ema15 > 0:
                total_valid += 1
                if price > ema15: bullish_count += 1
            
            if abs(sent) > 0:
                global_sent_total += sent
                sent_count += 1

        # Calculate Components
        breadth_bias = (bullish_count / total_valid) if total_valid > 0 else 0.5
        
        # B. Scraper Sentiment (-100 to 100 mapped to 0 to 1.0)
        avg_global_sent = (global_sent_total / sent_count) if sent_count > 0 else 0.0
        sentiment_ratio = (avg_global_sent + 100) / 200.0
        
        # C. Intensity Scaling
        # If intensity is high, we lean harder into the bias. 
        # If intensity is low, we stay near 0.5 (Neutral).
        intensity = self.get_market_movement_intensity() / 100.0 # 0.0 to 1.0
        
        # Weighted Fusion
        # 40% Breadth, 40% Scraper Sentiment, 20% Intensity
        base_ratio = (breadth_bias * 0.5) + (sentiment_ratio * 0.5)
        
        # Exaggerate: amplify the ratio signal 2x, then dampen slightly by intensity
        # Old: pulled hard toward 0.5 → killed the signal
        _ratio_mult = getattr(config, 'RATIO_MULTIPLIER_TRADIER', 3.5)  # BACKTEST_CHANGE_T61: was 2.0. 3.5x = best Sharpe in 65-config sweep
        amplified = 0.5 + (base_ratio - 0.5) * _ratio_mult
        final_ratio = 0.5 + (amplified - 0.5) * max(intensity, 0.5)

        return max(0.05, min(0.95, final_ratio))

    def get_custom_universe_index(self) -> dict:
        """Build a custom market index from ALL our traded symbols.
        Returns breadth (A/D ratio), avg change, and a composite score
        that's a better market proxy than SPY/QQQ for our specific universe."""
        try:
            snap = self.market_snapshot
            if not snap:
                return {'breadth': 0.0, 'avg_change_1h': 0.0, 'up': 0, 'down': 0, 'composite': 0.0}
            up, down, flat = 0, 0, 0
            changes_1h = []
            for sym, d in snap.items():
                cur = float(d.get('current_price', 0) or 0)
                prev = float(d.get('close_1h_prev', 0) or 0)
                if cur > 0 and prev > 0:
                    pct = (cur - prev) / prev * 100
                    changes_1h.append(pct)
                    if pct > 0.1: up += 1
                    elif pct < -0.1: down += 1
                    else: flat += 1
            total = up + down + flat
            if total == 0:
                return {'breadth': 0.0, 'avg_change_1h': 0.0, 'up': 0, 'down': 0, 'composite': 0.0}
            breadth = (up - down) / total  # -1 to +1
            avg_change = sum(changes_1h) / len(changes_1h) if changes_1h else 0.0
            # Composite: breadth * 50 + avg_change * 20, clamped to -100..+100
            composite = max(-100.0, min(100.0, breadth * 50 + avg_change * 20))
            return {
                'breadth': breadth,
                'avg_change_1h': avg_change,
                'up': up, 'down': down, 'flat': flat,
                'total': total,
                'ad_ratio': up / max(down, 1),
                'composite': composite
            }
        except Exception as e:
            logger.debug(f"[custom_universe_index] Error: {e}")
            return {'breadth': 0.0, 'avg_change_1h': 0.0, 'up': 0, 'down': 0, 'composite': 0.0}

    def get_current_portfolio_balance(self) -> dict:
        """Calculates current Long/Short dollar exposure percentage."""
        long_val, short_val = 0.0, 0.0
        
        for pk, pos in self.position_manager.positions.items():
            qty = abs(float(getattr(pos, 'positionAmt', 0)))
            if qty <= 0: continue
            
            # Use memory price
            price_data = self.get_indicators(pos.symbol)
            price = float(price_data.get('current_price', 0))
            if price <= 0: continue
            
            val = qty * price
            if pk.endswith("_LONG"): long_val += val
            else: short_val += val
            
        total = long_val + short_val
        if total <= 0: return {'long_pct': 0.5, 'short_pct': 0.5, 'total': 0.0}
        return {
            'long_pct': long_val / total,
            'short_pct': short_val / total,
            'total': total  }
  
    async def get_portfolio_exposure_ratio(self) -> dict:
        """
        Calculates current Long vs Short exposure in dollars.
        Returns dict with percentages.
        """
        total_long_value = 0.0
        total_short_value = 0.0

        if self.position_manager:
            for pk, pos in self.position_manager.positions.items():
                qty = abs(float(getattr(pos, 'positionAmt', 0) or 0))
                current_price,_ = await self.get_current_price(pos.symbol)
                current_price = float(current_price)

                if qty > 0 and current_price > 0:
                    val = qty * current_price
                    # Parse side from key (tra:AAPL_LONG)
                    if "_LONG" in pk:
                        total_long_value += val
                    elif "_SHORT" in pk:
                        total_short_value += val

        total_exposure = total_long_value + total_short_value

        if total_exposure <= 0:
            return {'long_pct': 0.0, 'short_pct': 0.0, 'total_exposure': 0.0}

        return {
            'long_pct': (total_long_value / total_exposure),
            'short_pct': (total_short_value / total_exposure),
            'total_exposure': total_exposure  }

    def get_wing_exposure(self, wing: str, side: str) -> float:
        """Return current dollar exposure for a given trade wing + side."""
        total = 0.0
        if not self.position_manager: return total
        for pk, pos in self.position_manager.positions.items():
            if getattr(pos, 'trade_wing', 'swing') != wing: continue
            if side.upper() not in pk.upper(): continue
            qty = abs(float(getattr(pos, 'positionAmt', 0)))
            if qty <= 0: continue
            price_data = self.get_indicators(pos.symbol)
            price = float(price_data.get('current_price', 0))
            if price > 0: total += qty * price
        return total

    def is_swing_entry_allowed(self, side: str, order_value: float) -> tuple:
        """
        Check if a swing entry fits within the ratio-adjusted budget.
        ratio=0.7 → long cap ×140%, short cap ×60%. Clamped to ±50% of base.
        Returns (allowed: bool, reason: str).
        """
        ratio = self.calculate_unified_market_ratio()
        if side == "LONG":
            base = getattr(config, 'SWING_LONG_BUDGET', 20000.0)
            effective = min(base * 1.5, base * (ratio / 0.5))
        else:
            base = getattr(config, 'SWING_SHORT_BUDGET', 20000.0)
            effective = min(base * 1.5, base * ((1.0 - ratio) / 0.5))
        current = self.get_wing_exposure('swing', side)
        if current + order_value > effective:
            return False, f"SwingBudget_{side} {current:.0f}+{order_value:.0f}>{effective:.0f}"
        return True, ""

    def check_pullback_reexpansion(self, symbol: str, i_raw: dict) -> tuple:
        """
        Detects the pullback-reexpansion setup across all symbols:
          1. HTF range peaked: 15m (or 1h) DC range was expanding, now contracting.
          2. Price at key level: dc_basis_15m ±0.5% OR sma_200_1m ±0.3%.
          3. STF contracted: 5m and 1m DC ranges have tightened vs 2 readings ago.
          4. Reigniting: 1m DC range just started expanding again (coil breaking).

        Score 4 = perfect; 3 = strong. Returns (is_setup, score, direction, detail).
        direction: "LONG" if price >= sma_200_1m, "SHORT" otherwise.
        Updates self._dc_range_history[symbol] (maxlen=8, one entry per ~10s cycle).
        """
        def _r(hk, lk):
            h = safe_fetch_float(i_raw.get(hk, 0))
            l = safe_fetch_float(i_raw.get(lk, 0))
            return (h - l) / l if h > 0 and l > 0.001 else 0.0
        ratios = {
            '1m':  _r('dc_high_1m',  'dc_low_1m'),
            '5m':  _r('dc_high_5m',  'dc_low_5m'),
            '1h':  _r('dc_high_1h',  'dc_low_1h'),
        }
        hist = self._dc_range_history[symbol]
        hist.append({'ts': time.time(), **ratios})
        if len(hist) < 3:
            return False, 0, "", "insufficient_history"
        prev = hist[-2]
        prev2 = hist[-3]
        price = safe_fetch_float(i_raw.get('current_price', 0))
        basis_1h = safe_fetch_float(i_raw.get('dc_basis_1h', 0))
        sma200 = safe_fetch_float(i_raw.get('sma_200_1m', 0))
        # Condition 1 – HTF range peaked (was rising, now falling)
        peaked_5m = prev2['5m'] < prev['5m'] > ratios['5m'] and prev['5m'] > 0.003
        peaked_1h = len(hist) >= 4 and hist[-4]['1h'] < prev2['1h'] < prev['1h'] > ratios['1h']
        htf_peaked = peaked_5m or peaked_1h
        # Condition 2 – Price at key support/resistance level
        at_basis = basis_1h > 0 and abs(price - basis_1h) / basis_1h < 0.008
        at_sma200 = sma200 > 0 and abs(price - sma200) / sma200 < 0.003
        at_key_level = at_basis or at_sma200
        # Condition 3 – Short-term contracted vs 2 cycles ago
        tight_5m = ratios['5m'] < prev2['5m'] * 0.85 and prev2['5m'] > 0.001
        tight_1m = ratios['1m'] < prev2['1m'] * 0.80 and prev2['1m'] > 0.001
        contracted = tight_5m or tight_1m
        # Condition 4 – 1m range reigniting (expanding from tight base)
        reigniting = ratios['1m'] > prev['1m'] * 1.20 and ratios['1m'] > 0.0008
        score = sum([htf_peaked, at_key_level, contracted, reigniting])
        detail = f"peaked={int(htf_peaked)} key={int(at_key_level)} tight={int(contracted)} fire={int(reigniting)} 5m={ratios['5m']:.4f} 1m={ratios['1m']:.4f}"
        if score < 3:
            return False, score, "", detail
        direction = "LONG" if (sma200 <= 0 or price >= sma200) else "SHORT"
        return True, score, direction, detail

    def _check_htf_confirmation(self, is_long: bool, indicators: Dict, k_1h: float, k_1h_prev: float, k_4h: float, k_4h_prev: float, k_15m: float, k_15m_prev: float, d_15m: float, k_5m: float, k_5m_prev: float, wt1_5m: float, wt2_5m: float, wt1_15m: float, wt2_15m: float, current_price: float, sma_200_1m: float, sma_200_1m_prev: float, config) -> bool:
        """Replicate ez_manage HTF confirmation logic - returns True when trade should be blocked."""
        if getattr(config, 'EXTREME_MODE', False):
            return False
        if not getattr(config, 'HTF1_CONF', False) and not getattr(config, 'HTF4_CONF', False):
            return False
        def _f(val, default=0.0):
            try:
                return float(val)
            except (TypeError, ValueError):
                return default
        i = indicators or {}
        d_1h = _f(i.get('stoch_d_1h', 0))
        d_4h = _f(i.get('stoch_d_4h', 0))
        high_1h = _f(i.get('high_1h', 0)); high_1h_prev = _f(i.get('high_1h_prev', 0))
        low_1h = _f(i.get('low_1h', 0)); low_1h_prev = _f(i.get('low_1h_prev', 0))
        high_4h = _f(i.get('high_4h', 0)); high_4h_prev = _f(i.get('high_4h_prev', 0))
        low_4h = _f(i.get('low_4h', 0)); low_4h_prev = _f(i.get('low_4h_prev', 0))
        high_15m = _f(i.get('high_15m', 0)); high_15m_prev = _f(i.get('high_15m_prev', 0))
        low_15m = _f(i.get('low_15m', 0)); low_15m_prev = _f(i.get('low_15m_prev', 0))
        ha_1h = i.get('ha_1h', i.get('ha_15m', 'neutral')) or 'neutral'
        ha_4h = i.get('ha_4h', i.get('ha_1h', 'neutral')) or 'neutral'
        blocked = False
        if getattr(config, 'HTF1_CONF', False):
            higher_high_1h = high_1h > high_1h_prev and (low_1h > low_1h_prev or ha_1h == 'green')
            higher_high_15m = high_15m > high_15m_prev and low_15m > low_15m_prev
            lower_low_1h = high_1h < high_1h_prev and low_1h < low_1h_prev
            lower_low_15m = high_15m < high_15m_prev and low_15m < low_15m_prev
            if is_long:
                if not (k_1h > d_1h or (higher_high_1h and higher_high_15m)):
                    blocked = True
            else:
                if not (k_1h < d_1h or (lower_low_1h and lower_low_15m)):
                    blocked = True
        if getattr(config, 'HTF4_CONF', False):
            higher_high_4h = high_4h > high_4h_prev and (low_4h > low_4h_prev or ha_4h == 'green')
            higher_high_15m = high_15m > high_15m_prev and low_15m > low_15m_prev
            lower_low_4h = (high_4h < high_4h_prev or ha_4h == 'red') and low_4h < low_4h_prev
            lower_low_15m = high_15m < high_15m_prev and low_15m < low_15m_prev
            if is_long:
                if not (k_4h > d_4h or (higher_high_4h and higher_high_15m)):
                    blocked = True
            else:
                if not (k_4h < d_4h or (lower_low_4h and lower_low_15m)):
                    blocked = True
        if blocked:
            logger.debug(f"[HTF_CHECK] block={blocked} is_long={is_long} htf1={getattr(config,'HTF1_CONF',False)} htf4={getattr(config,'HTF4_CONF',False)} k_1h={k_1h:.1f} k_4h={k_4h:.1f}")
        return blocked

    def load_symbols(self) -> List[str]:
        try:
            if config.SYMBOLS_FILE.exists():
                with open(config.SYMBOLS_FILE, 'r') as f:
                    symbols = json.load(f)
                    if isinstance(symbols, list): return symbols
                    return []
            return []
        except Exception as e:
            logger.error(f"Error loading symbols: {e}")
            return []
    def _adapt_indicators_for_ez_manage(self, i: Dict[str, Any]) -> Dict[str, Any]:
        if not isinstance(i, dict): return {}
        
        # Standard Mappings
        i['stoch_k_3m'] = i.get('stoch_k_5m')
        i['stoch_d_3m'] = i.get('stoch_d_5m')
        
        # Punctual Timestamp Fix: Convert all your specific JSON keys to UTC Datetimes
        ts_keys = ['timestamp', 'timestamp_1m', 'timestamp_5m', 'timestamp_15m', 'timestamp_1h', 'timestamp_4h']
        for k in ts_keys:
            if k in i and isinstance(i[k], str):
                i[k] = safe_datetime(i[k])    
        return i

    # def _adapt_indicators_for_ez_manage(self, i: Dict[str, Any]) -> Dict[str, Any]:
    #     if not isinstance(i, dict): return {}
    #     i['stoch_k_3m'] = i.get('stoch_k_5m')
    #     i['stoch_d_3m'] = i.get('stoch_d_5m')
    #     for ts_key in ['timestamp', 'timestamp_1m', 'timestamp_5m', 'timestamp_15m', 'timestamp_1h', 'timestamp_4h', 'timestamp_D']:
    #         if ts_key in i and isinstance(i[ts_key], str):
    #             i[ts_key] = safe_datetime(i[ts_key])
    #     adapted = i.copy()
    #     now = datetime.now(timezone.utc)
    #     ts_1m_val = adapted.get('1m_updated_at') or adapted.get('timestamp_1m')
    #     if ts_1m_val:
    #         dt_1m = safe_datetime(ts_1m_val)
    #         if dt_1m and (now - dt_1m).total_seconds() > 120.0:
    #             # Remove 1m keys to prevent stale decision making, but keep timestamps for accurate Age logging
    #             m1_keys = [k for k in adapted.keys() if '_1m' in k or k in ['k_1m', 'd_1m']]
    #             m1_keys = [k for k in m1_keys if k not in ['1m_updated_at', 'timestamp_1m']]
    #             for k in m1_keys: adapted.pop(k, None)
    #     mappings_1m = {  'k_1m': 'stoch_k_1m', 'd_1m': 'stoch_d_1m',  'k_1m_prev': 'stoch_k_1m_prev', 'd_1m_prev': 'stoch_d_1m_prev' }
    #     for target_key, source_key in mappings_1m.items():
    #         if source_key in adapted:
    #             adapted[target_key] = adapted[source_key]
    #     return adapted

    async def _expire_limbo_order(self, position_key: str, side: str, delay_seconds: float):
        """Remove order from limbo dict after delay if not confirmed"""
        await asyncio.sleep(delay_seconds)
        if position_key in self.orders_in_limbo and side in self.orders_in_limbo[position_key]:
            limbo_time = self.orders_in_limbo[position_key][side]
            if time.time() - limbo_time >= delay_seconds:
                del self.orders_in_limbo[position_key][side]
                if not self.orders_in_limbo[position_key]:
                    del self.orders_in_limbo[position_key]
                logger.debug(f"[{position_key}] Limbo order expired (not confirmed): {side}")
                # CRITICAL: Clear deduplication entry since order was NOT executed
                async with self.dedupe_lock:
                    if position_key in self.order_deduplication:
                        self.order_deduplication.pop(position_key, None)
                        logger.debug(f"[CLEAR_DEDUPE_NOT_EXECUTED] {position_key}: Cleared deduplication entry (order not executed)")

    def validate_order_side(self, side: str, position_side: str, action: str, position_key: str) -> tuple[str, bool]:
        is_opening_or_augmenting = action in ['OPEN', 'QUICK_OPEN', 'AUGMENT', 'REENTER']
        is_reducing_or_closing = action in ['REDUCE', 'QUICK_CLOSE', 'PROFIT_TAKE', 'CLOSE']
        if is_opening_or_augmenting:
            expected_side = "BUY" if position_side == "LONG" else "SELL"
        elif is_reducing_or_closing:
            expected_side = "SELL" if position_side == "LONG" else "BUY"
        else:
            expected_side = "BUY" if position_side == "LONG" else "SELL"
        if side == expected_side:
            return side, True
        logger.error(f"🚨 CRITICAL ORDER SIDE ERROR: {position_key}")
        logger.error(f"   Position: {position_side}, Action: {action}")
        logger.error(f"   Received side: {side}, Expected side: {expected_side}")
        logger.error(f"   CORRECTING to: {expected_side}")
        return expected_side, False
   
    def get_indicators(self, symbol: str, use_cache: bool = True) -> Dict[str, Any]:
        symbol = symbol.strip().upper()
        data = self.market_snapshot.get(symbol, {})
        if not data: return {}
        price_entry = self.price_cache.get(symbol)
        if price_entry:
            data['current_price'] = price_entry['price']
        ts_raw = data.get('timestamp_1m') or data.get('timestamp_5m') or data.get('timestamp_15m') or data.get('timestamp_1m_mid') or data.get('timestamp')
        if ts_raw:
            dt_obj = ts_raw if isinstance(ts_raw, datetime) else safe_datetime(ts_raw)
            if dt_obj:
                now_utc = datetime.now(timezone.utc)
                true_lag = (now_utc - dt_obj).total_seconds()
                if true_lag < -1.0:
                    true_lag = 0.1
                if true_lag > 600:
                    logger.warning(f"[get_indicators] [{symbol}] Snapshot internal age is stale ({true_lag:.1f}s)")
        return data

    def _get_extras(self, symbol: str) -> Dict[str, Any]:
        """Get extra indicators for symbol, computing on demand if not cached (TTL 120s)."""
        now = time.time()
        if symbol in self._extra_indicators_cache and (now - self._extra_indicators_cache_time.get(symbol, 0)) < 120:
            return self._extra_indicators_cache[symbol]
        try:
            extras = compute_extra_indicators(symbol, config.KLINES_CACHE_DIR)
            if extras:
                self._extra_indicators_cache[symbol] = extras
                self._extra_indicators_cache_time[symbol] = now
                return extras
        except Exception:
            pass
        return self._extra_indicators_cache.get(symbol, {})

    def get_enriched_indicators(self, symbol: str) -> Dict[str, Any]:
        """Get indicators + YouTube Consensus extras (VWAP, 9/21 EMA, Keltner, Squeeze, ORB, EP)."""
        data = self.get_indicators(symbol)
        if not data:
            return data
        now = time.time()
        cache_ttl = 60.0  # Refresh extras every 60s
        if symbol in self._extra_indicators_cache and (now - self._extra_indicators_cache_time.get(symbol, 0)) < cache_ttl:
            data.update(self._extra_indicators_cache[symbol])
        else:
            try:
                extras = compute_extra_indicators(symbol, config.KLINES_CACHE_DIR)
                if extras:
                    # Squeeze detection: combine 1h BB (from existing indicators) with 1h KC (from extras)
                    bb_upper = float(data.get('bb_upper_1h', 0) or 0)
                    bb_lower = float(data.get('bb_lower_1h', 0) or 0)
                    kc_upper = float(extras.get('kc_upper_1h', 0) or 0)
                    kc_lower = float(extras.get('kc_lower_1h', 0) or 0)
                    if bb_upper and bb_lower and kc_upper and kc_lower:
                        squeeze_on = bb_upper < kc_upper and bb_lower > kc_lower
                        extras['squeeze_on_1h'] = squeeze_on
                        # Squeeze fire: was on, now off (need prev state — approximate with current)
                        extras['squeeze_momentum_long'] = squeeze_on and float(data.get('wt1_1h', 0) or 0) > 0
                        extras['squeeze_momentum_short'] = squeeze_on and float(data.get('wt1_1h', 0) or 0) < 0
                    self._extra_indicators_cache[symbol] = extras
                    self._extra_indicators_cache_time[symbol] = now
                    data.update(extras)
            except Exception as e:
                logger.debug(f"[EXTRA_IND] {symbol}: {e}")
        return data

    def _get_full_market_snapshot(self) -> Dict[str, Dict]:
        return self.market_snapshot
   
    async def _on_indicators_update(self, data: Any):
        """FAST-PATH: Instant update via Redis message."""
        try:
            payload = data.get("data", data) if isinstance(data, dict) else data
            if isinstance(payload, dict) and len(payload) > 0:
                # Update individual keys so the existing dictionary reference stays alive
                # but the content is fresh.
                for sym, vals in payload.items():
                    self.market_snapshot[sym.upper()] = self._adapt_indicators_for_ez_manage(vals)
                
                # Update mtime tracker to match (prevents the Poller from re-reading same data)
                self.last_json_mtime = time.time()
                
                if not self.ready_to_trade:
                    self.ready_to_trade = True
                    logger.info("🚀 [SYSTEM] Ready via PubSub.")
        except Exception as e:
            logger.error(f"PubSub Update Error: {e}")



    def _finalize_indicators(self, symbol: str, raw_indicators: dict) -> dict:
        """Helper to normalize data and inject real-time price"""
        if not raw_indicators: return {}

        # 1. Normalize (Timestamps & Floats)
        final = self._normalize_indicators(raw_indicators)

        # 2. Inject Price Cache (if available locally and fresher)
        # Fixes "0.00" price logs
        price_entry = self.price_cache.get(symbol, {})
        cached_price = price_entry.get('current_price', 0)

        if cached_price > 0:
            final['current_price'] = cached_price
            final['current_price'] = cached_price
        return final

    def _normalize_indicators(self, indicators: Dict[str, Any]) -> Dict[str, Any]:
        if not indicators or not isinstance(indicators, dict):
            return {}
        adapted = indicators.copy()
        ts_keys = ['timestamp', 'timestamp_1m', 'timestamp_5m', 'timestamp_15m', '1m_updated_at']
        for k in ts_keys:
            if k in adapted and isinstance(adapted[k], str):
                try:
                    dt = isoparse(adapted[k])
                    if dt.tzinfo is None: dt = dt.replace(tzinfo=timezone.utc)
                    adapted[k] = dt
                except Exception: pass

        # 2. NUMERIC CONVERSION
        float_keys = [
            'current_price', 'current_price',
            'stoch_k_1m', 'stoch_d_1m', 'stoch_k_5m', 'stoch_d_5m',
            'rsi_1m', 'rsi_5m', '0sentiment_rank'
        ]
        for k in float_keys:
            if k in adapted:
                try: adapted[k] = float(adapted[k])
                except Exception: pass
        return adapted

    async def last_hour_balancing_loop(self):
        logger.info("⚖️ [TASK] Afternoon Slim-Down Balancer Active")
        while self.running:
            try:
                est = ZoneInfo("America/New_York")
                now_est = datetime.now(timezone.utc).astimezone(est)
                
                if now_est.weekday() < 5 and dt_time(15, 15) <= now_est.time() <= dt_time(15, 58):
                    
                    # PUNCTUAL FIX: Removed 'await' because these are now synchronous memory lookups
                    target_long_pct = self.calculate_unified_market_ratio()
                    balance = self.get_current_portfolio_balance()
                    
                    actual_long_pct = balance['long_pct']
                    deviation = actual_long_pct - target_long_pct

                    if abs(deviation) > 0.10:
                        side_to_trim = "LONG" if deviation > 0 else "SHORT"
                        
                        candidates = []
                        for pk, pos in self.position_manager.positions.items():
                            if side_to_trim in pk and abs(float(getattr(pos, 'positionAmt', 0))) > 0:
                                candidates.append((pk, pos))
                        
                        candidates.sort(key=lambda x: getattr(x[1], 'gain', 0))
                        
                        if candidates:
                            target_pk, target_pos = candidates[0]
                            qty = abs(float(target_pos.positionAmt))
                            trim_qty = max(1, int(qty * 0.30))
                            
                            # PUNCTUAL FIX: Removed 'await'
                            price_data = self.get_indicators(target_pos.symbol)
                            price = float(price_data.get('current_price', 0))
                            
                            await self.execute_trade_action(
                                account_key=target_pk.split(':')[0],
                                position_key=target_pk,
                                symbol=target_pos.symbol,
                                quantity=trim_qty,
                                current_price=price,
                                side="SELL" if side_to_trim == "LONG" else "BUY",
                                position_side=side_to_trim,
                                unique_id=f"slim_{int(time.time())}",
                                action="REDUCE",
                                reason=f"EOD_SLIM_RATIO_{target_long_pct:.2f}",
                                override_qty=trim_qty
                            )

                await asyncio.sleep(240) 
            except Exception as e:
                # This catches the "object dict" error and prevents loop death
                logger.error(f"Balancer Error: {e}")
                await asyncio.sleep(60)
                
                
    async def get_rankings(self) -> Dict[str, Any]:
        """Get rankings from Redis or cache"""
        try:
            if not self.redis_manager:
                try:
                    pass  # DISABLED: tradier does not use server Redis
                except Exception:
                    self.redis_manager = None
            rankings_key = "tradier_rankings"
            data = await self.redis_manager.get(rankings_key)
            if data:
                self.rankings_cache = data if isinstance(data, dict) else {}
                return self.rankings_cache

            return self.rankings_cache
        except Exception as e:
            logger.debug(f"Error getting rankings: {e}")
            return {}
    
    async def get_current_price(self, symbol: str) -> Tuple[Optional[float], Optional[datetime]]:

        symbol = symbol.strip().upper()
        now = datetime.now(timezone.utc)
        MAX_AGE_SECONDS = 40.0  
        symbol = symbol.strip().upper()
        now = datetime.now(timezone.utc)
        MAX_AGE_SECONDS = 15.0  

        def safe_parse_ts(ts_val):
            if not ts_val: return None
            try:
                if isinstance(ts_val, datetime):
                    return ts_val if ts_val.tzinfo else ts_val.replace(tzinfo=timezone.utc)
                if isinstance(ts_val, (int, float)):
                    val = ts_val / 1000.0 if ts_val > 1e12 else ts_val
                    return datetime.fromtimestamp(val, tz=timezone.utc)
                if isinstance(ts_val, str):
                    # Manual parse for speed if format is known, else isoparse
                    if 'T' in ts_val:
                        dt = isoparse(ts_val)
                        return dt if dt.tzinfo else dt.replace(tzinfo=timezone.utc)
            except Exception: return None
            return None

        try:
            # --- LAYER 1: LOCAL CACHE (Zero I/O) ---
            if hasattr(self, 'price_cache') and symbol in self.price_cache:
                entry = self.price_cache[symbol]
                p = float(entry.get('price', 0.0))
                t = safe_parse_ts(entry.get('timestamp'))
                if p > 0 and t and (now - t).total_seconds() < MAX_AGE_SECONDS:
                    return p, t
            if hasattr(self, 'redis_manager') and self.redis_manager:
                try:
                    raw_md = await asyncio.wait_for(
                        self.redis_manager.get("tradier_prices_latest"), 
                        timeout=2.0
                    )
                    
                    if raw_md:
                        md = json.loads(raw_md) if isinstance(raw_md, str) else raw_md
                        if isinstance(md, dict):
                            # FIX: Unwrap the 'data' key!
                            symbol_data = md.get("data", md)
                            if symbol in symbol_data:
                                item = symbol_data[symbol]
                                p = float(item.get('price') or item.get('last', 0.0))
                                t = safe_parse_ts(item.get('timestamp') or item.get('date'))
                                if p > 0 and t:
                                    self.price_cache[symbol] = {'price': p, 'timestamp': t}
                                    if (now - t).total_seconds() < MAX_AGE_SECONDS:
                                        return p, t
                except (asyncio.TimeoutError, Exception):
                    pass 

            # --- LAYER 3: DISK JSON (Reliable Fallback) ---
            try:
                latest_file = self.config.DATA_DIR / "tradier_prices_latest.json"
                if latest_file.exists():
                    if (time.time() - latest_file.stat().st_mtime) < 30:
                        async with aiofiles.open(latest_file, 'r') as f:
                            content = await f.read()
                            if content:
                                md = json.loads(content)
                                if isinstance(md, dict):
                                    # FIX: Unwrap the 'data' key!
                                    symbol_data = md.get("data", md)
                                    if symbol in symbol_data:
                                        item = symbol_data[symbol]
                                        p = float(item.get('price') or item.get('last', 0.0))
                                        t = safe_parse_ts(item.get('timestamp'))
                                        if p > 0 and t:
                                            self.price_cache[symbol] = {'price': p, 'timestamp': t}
                                            if (now - t).total_seconds() < MAX_AGE_SECONDS:
                                                return p, t
            except Exception: pass
            if hasattr(self, 'api_client') and self.api_client:
                # Add a check: don't hammer API if we've recently been banned
                if time.time() > getattr(self.api_client, '_global_ban_expires', 0):
                    try:
                        quote = await self.api_client.get_quote(symbol)
                        if quote:
                            p = float(quote.get('last') or 0.0)
                            t = safe_parse_ts(quote.get('date')) or now
                            if p > 0:
                                self.price_cache[symbol] = {'price': p, 'timestamp': t}
                                return p, t
                    except Exception: pass

            # --- FINAL FALLBACK ---
            if hasattr(self, 'price_cache') and symbol in self.price_cache:
                entry = self.price_cache[symbol]
                return float(entry.get('price', 0.0)), safe_parse_ts(entry.get('timestamp'))

            return None, None

        except Exception as e:
            # Critical log to see why it crashed if it gets past the internal tries
            logger.error(f"get_current_price crash for {symbol}: {e}")
            return None, None
            
    async def place_order(self, symbol: str, side: str, quantity: float, order_type: str = "limit",  price: float = None, stop: float = None, duration: str = "day",
                          action: str = None, position_side: str = None, account_key: str="tra") -> Dict:
        current_account.set(account_key)
        now = time.time()
        if symbol in self.rejection_cooldowns:            
            if now < self.rejection_cooldowns[symbol]:
                wait_remaining = int(self.rejection_cooldowns[symbol] - now)
                logger.warning(f"🚫 [BACK-OFF] Skipping {symbol}. Cooldown: {wait_remaining}s")
                return {}
        if not is_regular_trading_hours():
            logger.debug(f"[PLACE_ORDER] Not in trading hours, skipping order: {symbol} {action} {side} qty={quantity}")
            return {}
        client = TradierAPIClient(config, account_key=account_key)
        try:
            # 2. TRADEABILITY & HOURS CHECK
            if not self.is_symbol_tradeable(symbol, account_key, position_side):
                return {}
            
            if not is_regular_trading_hours(): 
                return {}

            await client.connect()

            # 3. PRICE & POSITION SIZING
            quote = await client.get_quote(symbol)
            if not quote: 
                return {}

            bid, ask = float(quote.get('bid', 0)), float(quote.get('ask', 0))
            last = float(quote.get('last', ask))
            
            # --- FETCH TRUE API HOLDINGS ---
            api_positions = await client.get_account_positions(account_key)
            pos_list =[]
            if isinstance(api_positions, dict):
                p = api_positions.get('positions', {})
                if isinstance(p, dict) and 'position' in p:
                    pos_list = p['position'] if isinstance(p['position'], list) else [p['position']]
                elif isinstance(p, list):
                    pos_list = p
            elif isinstance(api_positions, list):
                pos_list = api_positions

            # Determine API quantity based on intended position side
            current_qty = 0.0
            found_in_api = False
            has_opposing_in_api = False
            for p in pos_list:
                api_sym = p.get('symbol', '').strip().upper()
                if api_sym == symbol.strip().upper():
                    found_in_api = True
                    q = float(p.get('quantity', 0))
                    if (q > 0 and position_side == 'LONG') or (q < 0 and position_side == 'SHORT'):
                        current_qty = abs(q)
                        break
                    elif (q > 0 and position_side == 'SHORT') or (q < 0 and position_side == 'LONG'):
                        has_opposing_in_api = True
                        break
            if has_opposing_in_api and action in ["OPEN", "AUGMENT", "REENTER", "REENTRY", "QUICK_OPEN", "REVERSE", "HEDGE_OPEN"]:
                logger.critical(f"[OPPOSING_BLOCK] {account_key}:{symbol}_{position_side}: BLOCKED — API shows opposing position held. Cannot open {position_side} while opposite side exists in Tradier.")
                return {"errors": {"error": [f"Opposing position held in API for {symbol}"]}}

            is_exception = symbol.upper() in self.exceptions
            max_order = self.limit_exception_order if is_exception else self.limit_normal_order
            max_pos = self.limit_exception_total_pos if is_exception else self.limit_total_pos

            # --- QUANTITY CALCULATION ---
            if action in ["OPEN", "AUGMENT", "REENTER", "REENTRY"]:
                remaining_usd = max(0, max_pos - (current_qty * last))
                base_target = config.START_POSITION_SIZE if current_qty == 0 else (quantity * last)
                target_usd = base_target * 3.0 if is_exception else base_target
                calc_qty = int(min(target_usd, max_order, remaining_usd) / last)
            else:
                # EXITS: Strictly clamp to true API holdings to prevent rejection loops
                # Only "ghost clear" if we got a valid response and the symbol IS NOT in the API map for that side
                if not found_in_api or current_qty == 0:
                    if api_positions is None or (isinstance(api_positions, dict) and 'errors' in api_positions):
                         logger.warning(f"[{account_key}] ⚠️ API Check failed for {symbol} on {account_key}. Skipping exit attempt.")
                         return {}
                         
                    logger.warning(f"[{account_key}] 👻 GHOST POSITION DETECTED: {symbol} side {position_side} not held in API {account_key}. Faking close to clear local memory.")
                    held_symbols = [p.get('symbol') for p in pos_list if float(p.get('quantity', 0)) != 0]
                    logger.debug(f"[{account_key}] API Holdings for {account_key}: {held_symbols}")
                    return {"order": {"id": "GHOST_CLEARED", "status": "filled"}}
                
                calc_qty = int(min(quantity, current_qty))

            if calc_qty < 1: 
                logger.warning(f"[{account_key}] ⚠️ Order size too small: {calc_qty} shares ({symbol})")
                return {"errors": {"error":["Order size < 1"]}}

            # --- PREPARE SIDE ---
            tradier_side = None
            _ENTRY_ACTIONS = {"OPEN", "AUGMENT", "REENTER", "REENTRY", "QUICK_OPEN", "REVERSE", "HEDGE_OPEN"}
            _EXIT_ACTIONS = {"CLOSE", "REDUCE", "SELL", "PROFIT_TAKE", "HEDGE_CLOSE", "QUICK_CLOSE"}
            if action in _ENTRY_ACTIONS:
                tradier_side = "buy" if position_side == "LONG" else "sell_short"
            elif action in _EXIT_ACTIONS:
                tradier_side = "sell" if position_side == "LONG" else "buy_to_cover"
            else:
                logger.critical(f"[UNMAPPED_ACTION_BLOCK] {account_key}:{symbol}_{position_side}: action='{action}' not entry or exit — BLOCKING order")
                return {"errors": {"error": [f"Unmapped action: {action}"]}}

            # 4. THE 10-SECOND AGGRESSIVE CHASE LOOP
            loop_start = time.time()
            remaining_to_fill = calc_qty
            attempt = 0
            last_order_result = {}

            while (time.time() - loop_start) < 10 and remaining_to_fill > 0:
                attempt += 1
                q_loop = await client.get_quote(symbol)
                curr_bid, curr_ask = float(q_loop.get('bid', 0)), float(q_loop.get('ask', 0))
                
                # PRICE LOGIC: Aim for Midpoint + aggressive offset
                midpoint = (curr_bid + curr_ask) / 2
                if tradier_side in ["buy", "buy_to_cover"]:
                    # Buy at midpoint or slightly above to jump the queue
                    limit_price = round(midpoint + 0.01, 2)
                    if limit_price > curr_ask: limit_price = curr_ask
                else:
                    # Sell at midpoint or slightly below
                    limit_price = round(midpoint - 0.01, 2)
                    if limit_price < curr_bid: limit_price = curr_bid

                logger.info(f"[{account_key}] Loop Attempt {attempt}: {tradier_side} {symbol} {remaining_to_fill} @ {limit_price}")

                order_res = await client.place_order(
                    account_key=account_key, symbol=symbol, side=tradier_side,
                    quantity=remaining_to_fill, order_type="limit", price=limit_price, duration="day"
                )

                order_info = order_res.get('order', {})
                order_id = order_info.get('id')
                
                if not order_id or order_info.get('status') == 'rejected':
                    logger.error(f"❌ Rejection on {symbol}. Reason: {order_res.get('errors')}")
                    self.rejection_cooldowns[symbol] = time.time() + 900
                    return order_res

                # Wait short burst to see if it fills
                await asyncio.sleep(2.5)
                
                status_res = await client.get_order_status(account_key, order_id)
                status = status_res.get('status', '').lower()
                exec_qty = float(status_res.get('exec_quantity', 0))
                
                if status == 'filled':
                    logger.info(f"✅ Filled {symbol} via aggressive limit.")
                    return status_res
                
                # If not filled, cancel and loop back for a better price
                await client.cancel_order(account_key, order_id)
                remaining_to_fill -= exec_qty
                last_order_result = status_res
                logger.info(f"⏳ Partial Fill: {exec_qty}/{remaining_to_fill + exec_qty}. Re-calculating...")

            # 5. FINAL FALLBACK: MARKET ORDER (If still not filled after 10s)
            if remaining_to_fill > 0:
                logger.warning(f"🚨 Chase timeout for {symbol}. Executing MARKET ORDER for remaining {remaining_to_fill} qty.")
                market_res = await client.place_order(
                    account_key=account_key,
                    symbol=symbol,
                    side=tradier_side,
                    quantity=remaining_to_fill,
                    order_type="market",
                    duration="day"
                )
                return market_res

            return last_order_result

        except Exception as e:
            logger.error(f"[smart_place_order] Error: {e}",  exc_info=True )
            return {}
        finally:
            await client.close()

    def _infer_htf_direction_fast(self, is_long, k_5m, k_5m_prev, k_15m, k_15m_prev, d_15m, wt1_5m, wt2_5m, wt1_15m, wt2_15m, current_price, dc_basis_1h, dc_basis_4h, sma_200_15m, sma_200_1m, sma_200_1m_prev):
        """Fast inference of 1h/4h direction using shorter timeframe momentum - reduces lag by not waiting for hourly updates"""
        if is_long:
            momentum_5m = k_5m > k_5m_prev and k_5m < 50
            momentum_15m = k_15m > k_15m_prev and k_15m < 50
            wt_bullish = wt1_5m > wt2_5m and wt1_15m > wt2_15m
            price_above_dc1h = current_price >= dc_basis_1h
            price_above_dc4h = current_price >= dc_basis_4h
            price_above_sma = current_price > sma_200_15m and sma_200_1m_prev < sma_200_1m
            stoch_aligned = k_15m > d_15m
            score = sum([momentum_5m, momentum_15m, wt_bullish, price_above_dc1h, price_above_dc4h, price_above_sma, stoch_aligned])
            return score >= 4
        else:
            momentum_5m = k_5m < k_5m_prev and k_5m > 50
            momentum_15m = k_15m < k_15m_prev and k_15m > 50
            wt_bearish = wt1_5m < wt2_5m and wt1_15m < wt2_15m
            price_below_dc1h = current_price <= dc_basis_1h
            price_below_dc4h = current_price <= dc_basis_4h
            price_below_sma = current_price < sma_200_15m and sma_200_1m_prev > sma_200_1m
            stoch_aligned = k_15m < d_15m
            score = sum([momentum_5m, momentum_15m, wt_bearish, price_below_dc1h, price_below_dc4h, price_below_sma, stoch_aligned])
            return score >= 4

    def _infer_htf_reversal_fast(self, is_long, k_5m, k_5m_prev, k_15m, k_15m_prev, d_15m, wt1_5m, wt2_5m, wt1_15m, wt2_15m, current_price, dc_basis_1h, dc_basis_4h, sma_200_15m, sma_200_1m, sma_200_1m_prev):
        """Fast inference of 1h/4h momentum reversal using shorter timeframe data - detects if HTF momentum is weakening/reversing"""
        if is_long:
            momentum_reversing_5m = k_5m < k_5m_prev
            momentum_reversing_15m = k_15m < k_15m_prev
            wt_bearish = wt1_5m < wt2_5m or wt1_15m < wt2_15m
            price_below_dc1h = current_price < dc_basis_1h
            price_below_dc4h = current_price < dc_basis_4h
            price_below_sma = current_price < sma_200_15m or sma_200_1m_prev > sma_200_1m
            stoch_misaligned = k_15m < d_15m
            score = sum([momentum_reversing_5m, momentum_reversing_15m, wt_bearish, price_below_dc1h, price_below_dc4h, price_below_sma, stoch_misaligned])
            return score >= 3
        else:
            momentum_reversing_5m = k_5m > k_5m_prev
            momentum_reversing_15m = k_15m > k_15m_prev
            wt_bullish = wt1_5m > wt2_5m or wt1_15m > wt2_15m
            price_above_dc1h = current_price > dc_basis_1h
            price_above_dc4h = current_price > dc_basis_4h
            price_above_sma = current_price > sma_200_15m or sma_200_1m_prev < sma_200_1m
            stoch_misaligned = k_15m > d_15m
            score = sum([momentum_reversing_5m, momentum_reversing_15m, wt_bullish, price_above_dc1h, price_above_dc4h, price_above_sma, stoch_misaligned])
            return score >= 3

    async def _maybe_restart_indicators(self, reason: str) -> None:
        """Simplified indicator restart for Tradier"""
        logger.warning(f"[INDICATOR_HEALTH] Potential indicator issue detected: {reason}")

    async def clear_recent_signal(self, position_key: str):
        """Placeholder for clearing recent signal cache"""
        self.recently_processed_signals.pop(position_key, None)

    async def clear_all_cooldowns_for_position(self, position_key: str, side: str = None):
        """Clear ALL cooldown-related data for a position"""
        try:
            self.recently_queued_signals.pop(position_key, None)
            self.augmentation_cooldown_map.pop(position_key, None)
            self.reduction_cooldown_map.pop(position_key, None)
            await self.clear_recent_signal(position_key)
            if hasattr(self, 'orders_in_limbo') and position_key in self.orders_in_limbo:
                if side:
                    self.orders_in_limbo[position_key].pop(side, None)
                    if not self.orders_in_limbo[position_key]:
                        del self.orders_in_limbo[position_key]
                else:
                    del self.orders_in_limbo[position_key]
            logger.debug(f"[CLEAR_ALL_COOLDOWNS] {position_key}: Cleared all cooldown data (side={side})")
        except Exception as e:
            logger.warning(f"[CLEAR_ALL_COOLDOWNS] {position_key}: Error clearing cooldowns: {e}")

    def _robust_json_decode(self, content):
        """Robust JSON decoder for tradeable keys and other files"""
        try:
            if isinstance(content, bytes):
                content = content.decode('utf-8')
            return json.loads(content)
        except Exception as e:
            logger.error(f"JSON Decode Error: {e}")
            return None

    async def load_tradeable(self):
        """Load tradeable keys from tradeable_keys.json"""
        current_time = time.time()
        if hasattr(self, 'tradeable_keys') and self.tradeable_keys and (current_time - self._last_tradeable_update < 180):
            return list(self.tradeable_keys)

        async with self.tradeable_lock:
            if hasattr(self, 'tradeable_keys') and self.tradeable_keys and (current_time - self._last_tradeable_update < 180):
                return list(self.tradeable_keys)

            temp_keys = set()
            # 1. Try Redis
            if self.redis_manager:
                try:
                    redis_data = await asyncio.wait_for(self.redis_manager.get("tradeable_keys"), timeout=0.5)
                    if redis_data:
                        data = self._robust_json_decode(redis_data)
                        if isinstance(data, list):
                            temp_keys = {str(k).strip() for k in data}
                except Exception: pass

            # 2. Try File
            if not temp_keys:
                main_path = self.config.BASE_PATH / "tradeable_keys.json"
                if main_path.exists():
                    try:
                        async with aiofiles.open(main_path, "r") as f:
                            content = await f.read()
                            data = self._robust_json_decode(content)
                            if isinstance(data, list):
                                temp_keys = {str(k).strip() for k in data}
                    except Exception as e:
                        logger.error(f"[SYMBOLS] Error loading tradeable_keys.json: {e}")

            if temp_keys:
                self.tradeable_keys = temp_keys
                self._last_tradeable_update = current_time
            return list(self.tradeable_keys)

    def _get_json_symbols(self, account_key: str, side: str) -> set:
        # 2026-05-07: JSON files (symbols_<acc>_<side>.json) are the source of truth — refreshed every ~10 min by tradier_rankings. Memory list was being wiped between LOAD and CHECK, blocking 99% of approved syms (only 5 fresh OPENs in 30 days). Read directly with 30s TTL cache.
        cache = getattr(self, '_json_symbols_cache', None)
        if cache is None:
            cache = {}
            self._json_symbols_cache = cache
        key = (account_key, side)
        now = time.time()
        cached = cache.get(key)
        if cached and cached[1] > now:
            return cached[0]
        try:
            base = Path(self.config.BASE_PATH)
            paths = []
            if account_key == 'tra' and getattr(self.config, 'TRA_SATOSHIT_ONLY', False):
                paths.append(base / f"symbols_tra_satoshit_{side}.json")
            paths.append(base / f"symbols_{account_key}_{side}.json")
            syms = set()
            for path in paths:
                if path.exists():
                    with open(path, 'r') as fh:
                        raw = json.load(fh)
                    if isinstance(raw, list):
                        syms = {str(s).upper().strip() for s in raw if s}
                    elif isinstance(raw, dict):
                        syms = {str(k).upper().strip() for k in raw.keys() if k}
                    if syms:
                        break
        except Exception as e:
            if cached:
                return cached[0]
            logger.warning(f"[_get_json_symbols] {account_key}/{side} read failed: {e}")
            syms = set()
        cache[key] = (syms, now + 30.0)
        return syms

    def is_symbol_tradeable(self, symbol: str, account_key: str, side: str) -> bool:
        """Centralized tradeability check with debug logging."""
        symbol = symbol.upper()
        if not side:
            side = "LONG"
        side = side.upper()

        # tra is a CASH (non-margin) account — shorts are physically impossible.
        # The check goes BEFORE blacklist/whitelist so even ALWAYS_TRADEABLE
        # symbols cannot be shorted on tra.
        if account_key == 'tra' and side == 'SHORT' and getattr(config, 'TRA_LONG_ONLY', True):
            return False

        # 1. Blacklist
        if symbol in self.blacklist:
            return False

        # 2. Hard non-shortable list (Tradier-side restriction; not overridable)
        if side == 'SHORT' and symbol in self.non_shortable_symbols:
            return False

        # 3. Same-side discovery list is the SOLE permission for new entries.
        #    Callers that need to manage (close/reduce) an existing position must NOT route through
        #    this function — the caller already bypasses is_symbol_tradeable when _is_exit_or_reduce
        #    (see execute_trade_action ~line 8690, execute_now ~line 9203). The previous code path
        #    "if positionAmt > 0 → return True" became a self-perpetuating opener: once one share
        #    leaked through (via ALWAYS_TRADEABLE bypass or any other mechanism), this branch said
        #    "we have a position, so OPEN/AUGMENT is allowed" — which is exactly how the MU_SHORT
        #    accumulated to 14 shares. Killed 2026-05-11.
        side_lower = 'long' if side == 'LONG' else 'short'
        json_syms = self._get_json_symbols(account_key, side_lower)
        mem_syms = {s.upper() for s in getattr(self, f"symbols_{side_lower}_{account_key}", [])}
        same_side_syms = json_syms | mem_syms

        if symbol not in same_side_syms:
            return False

        # 4. ALWAYS_TRADEABLE/EXCEPTIONS are now only an enabler ON TOP of same_side_syms —
        #    they were already de-fanged in step 3, but kept here for any future restrictive use.
        return True

    async def execute_trade_action(self, account_key, position_key, symbol, quantity, current_price, side, position_side, unique_id, is_full_close=False, action='', reason='', override_qty=None):
        if not is_regular_trading_hours(): return "MARKET_CLOSED"
        current_account.set(account_key)
        _is_exit_or_reduce = action in ('REDUCE', 'CLOSE', 'FULL_CLOSE', 'PROFIT_TAKE', 'QUICK_CLOSE')
        if symbol.upper() in self.blacklist and not _is_exit_or_reduce:
            logger.critical(f"[{account_key}] 🛑 CRITICAL: Attempted to trade BLACKLISTED symbol {symbol}. ACTION BLOCKED.")
            return "BLOCKED_BLACKLIST"

        # 1. Variables and Flags
        is_long = position_side == 'LONG'
        is_entry_action = action in ['OPEN', 'REENTRY', 'QUICK_OPEN', 'REVERSE', 'HEDGE_OPEN']
        is_reduce = action in ['REDUCE', 'PROFIT_TAKE']
        is_exit_action = action in ['CLOSE', 'FULL_CLOSE', 'PROFIT_TAKE', 'REDUCE']
        is_hedge = action in ['HEDGE_OPEN', 'HEDGE_CLOSE']
        hedge_for = None # Tradier logic usually doesn't use hedge_for like ez_manage
        # BACKTEST_CHANGE_T31: hedge disabled, negative EV in all 5 backtest configs
        if is_hedge and not getattr(config, 'HEDGE_MODE_TRADIER', True):
            logger.info(f"[{account_key}] [HEDGE_DISABLED] Blocking {action} for {symbol}: HEDGE_MODE_TRADIER=False")
            return "BLOCKED_HEDGE_DISABLED"
        # tra is a CASH account — block any SHORT order at the execution layer too,
        # so even if a stale code path reaches here, no sell_short ever leaves the box.
        # Exits/closes on existing shorts are still permitted (in case any old positions exist).
        if not _is_exit_or_reduce and account_key == 'tra' and position_side == 'SHORT' and getattr(config, 'TRA_LONG_ONLY', True):
            logger.warning(f"[tra] ⛔ EXECUTE BLOCK {symbol} SHORT: tra is a CASH (non-margin) account — shorts are not permitted (TRA_LONG_ONLY=True)")
            return "BLOCKED_TRA_LONG_ONLY"
        # Tradeability Check — exits/reduces always allowed (you must be able to close any position)
        if not _is_exit_or_reduce and not self.is_symbol_tradeable(symbol, account_key, position_side):
            logger.warning(f"[{account_key}] ⛔ EXECUTE BLOCK {symbol} {position_side}: Not in approved list.")
            return "BLOCKED_NOT_TRADEABLE"
        # 2026-04-26 Per-sector L/S ratio check — block opens that worsen sector imbalance
        if (is_entry_action and not is_hedge and not _is_exit_or_reduce and _SECTOR_LS_MODULE_AVAILABLE
            and getattr(config, 'SECTOR_LS_RATIO_ENABLED', False)):
            try:
                # Build positions dict in {pk: {positionAmt}} format
                _all_pos = {}
                for _pk, _p in (self.position_manager.positions if self.position_manager else {}).items():
                    _all_pos[_pk] = {'positionAmt': float(getattr(_p, 'positionAmt', 0) or 0)}
                _slr_ok, _slr_reason = check_sector_ls_ratio(account_key, symbol, position_side, _all_pos, config, is_hedge=is_hedge)
                if not _slr_ok:
                    logger.warning(f"[{account_key}] ⛔ {_slr_reason}")
                    return f"BLOCKED_SECTOR_LS_{_slr_reason}"
            except Exception as _slr_e:
                logger.warning(f"[{account_key}] [SECTOR_LS] check failed: {_slr_e}")

        # 2. Indicators Fetch & Unpack
        i = self.get_indicators(symbol)
        if not i: 
            logger.error(f"[EXECUTE_DEBUG] {symbol}: NO_INDICATORS")
            return "NO_INDICATORS"

        # Map 5m to 3m for logic compatibility if needed, though we'll use 5m variables directly
        def g(k, default=0.0): return float(i.get(k, default))
        def gs(k, default='neutral'): return str(i.get(k, default))
        
        timestamp_str = i.get('timestamp', '')
        timestamp = safe_datetime(timestamp_str)
        
        # Unpack variables (Using 5m instead of 3m as per user instruction)
        k_1m, k_1m_prev, d_1m = g('stoch_k_1m', 50), g('stoch_k_1m_prev', 50), g('stoch_d_1m', 50)
        k_5m, k_5m_prev, d_5m = g('stoch_k_5m', 50), g('stoch_k_5m_prev', 50), g('stoch_d_5m', 50)
        k_15m, k_15m_prev, d_15m = g('stoch_k_15m', 50), g('stoch_k_15m_prev', 50), g('stoch_d_15m', 50)
        k_1h, k_1h_prev, d_1h = g('stoch_k_1h', 50), g('stoch_k_1h_prev', 50), g('stoch_d_1h', 50)
        k_4h, k_4h_prev, d_4h = g('stoch_k_4h', 50), g('stoch_k_4h_prev', 50), g('stoch_d_4h', 50)
        k_D, k_D_prev, d_D = g('stoch_k_D', 50), g('stoch_k_D_prev', 50), g('stoch_d_D', 50)

        wt1_5m, wt2_5m = g('wt1_5m'), g('wt2_5m')
        wt1_15m, wt2_15m = g('wt1_15m'), g('wt2_15m')
        wt1_1h, wt2_1h = g('wt1_1h'), g('wt2_1h')
        
        sma_200_1m, sma_200_1m_prev = g('sma_200_1m'), g('sma_200_1m_prev')
        sma_200_15m, sma_200_15m_prev = g('sma_200_15m'), g('sma_200_15m_prev')
        sma_200_1h = g('sma_200_1h')
        
        dc_basis_5m, dc_basis_15m, dc_basis_1h, dc_basis_4h = g('dc_basis_5m'), g('dc_basis_15m'), g('dc_basis_1h'), g('dc_basis_4h')
        dc_high_5m, dc_low_5m = g('dc_high_5m'), g('dc_low_5m')
        dc_high_15m, dc_low_15m = g('dc_high_15m'), g('dc_low_15m')
        dc_high_1h, dc_low_1h = g('dc_high_1h'), g('dc_low_1h')
        dc_high_4h, dc_low_4h = g('dc_high_4h'), g('dc_low_4h')
        
        dc_high_5m_ant, dc_low_5m_ant = g('dc_high_5m_ant', dc_high_5m), g('dc_low_5m_ant', dc_low_5m)
        dc_high_15m_ant, dc_low_15m_ant = g('dc_high_15m_ant', dc_high_15m), g('dc_low_15m_ant', dc_low_15m)
        dc_high_1h_ant, dc_low_1h_ant = g('dc_high_1h_ant', dc_high_1h), g('dc_low_1h_ant', dc_low_1h)
        dc_high_4h_ant, dc_low_4h_ant = g('dc_high_4h_ant', dc_high_4h), g('dc_low_4h_ant', dc_low_4h)
        dc_basis_15m_ant = g('dc_basis_15m_ant', dc_basis_15m)
        
        dc_high4_5m, dc_low4_5m = g('dc_high4_5m'), g('dc_low4_5m')
        high_5m, low_5m, high_5m_prev, low_5m_prev = g('high_5m'), g('low_5m'), g('high_5m_prev'), g('low_5m_prev')
        high_15m, low_15m = g('high_15m'), g('low_15m')
        high_1h, low_1h, high_1h_prev, low_1h_prev = g('high_1h'), g('low_1h'), g('high_1h_prev'), g('low_1h_prev')
        high_4h, low_4h, high_4h_prev, low_4h_prev = g('high_4h'), g('low_4h'), g('high_4h_prev'), g('low_4h_prev')
        
        ha_5m, ha_15m, ha_1h, ha_4h = gs('ha_5m'), gs('ha_15m'), gs('ha_1h'), gs('ha_4h')
        atr_5m, atr_15m = g('atr_5m'), g('atr_15m')
        t_up_5m = i.get('t_up_5m', False)

        # ═══ TRADIER V1: ZONE GATE + ALIGNMENT ═══
        # Primary TF = 1h for swing. During market open (9:30-10:00) and last 2h (14:00-16:00) use 15m for faster entries.
        _is_reentry = action == 'REENTRY' or 'REENTRY' in (reason or '').upper()
        _is_augment_or_entry = not _is_exit_or_reduce
        _reason_upper = (reason or '').upper()
        _is_rotation = 'ROTATION_ENTRY' in _reason_upper
        _is_rsi2 = 'RSI2_MEAN_REVERSION' in _reason_upper
        _is_gap_fill = 'GAP_FILL' in _reason_upper
        # RZ baseline / red zone entries: bounce logic in wt_dc_delta._run_redzone
        # already validates HTF alignment + LTF velocity bounce. Skip the broad
        # alignment counter (which would never be satisfied at the bounce moment
        # since the WT cross hasn't completed yet — that's the whole point of
        # trading the pullback bounce, not waiting for the cross).
        _is_rz_entry = (
            _reason_upper.startswith('RZ_')
            or 'DELTA_ENTRY' in _reason_upper
            or 'RED_ZONE' in _reason_upper
            or 'BASELINE_BOUNCE' in _reason_upper
            or 'BREAKDOWN_TRUCK' in _reason_upper
            or 'BOTTOM_HUGE' in _reason_upper
            or 'REJECTION_OLD_REDZONE' in _reason_upper
            or 'TRUCK_LOAD' in _reason_upper
        )
        _is_proven_strategy = _is_rotation or _is_rsi2 or _is_gap_fill or _is_rz_entry
        try:
            from zoneinfo import ZoneInfo; _now_est = datetime.now(ZoneInfo("America/New_York")).time()
        except ImportError:
            import pytz; _now_est = datetime.now(pytz.timezone("America/New_York")).time()
        _is_fast_window = (dt_time(9, 30) <= _now_est <= dt_time(10, 0)) or (dt_time(14, 0) <= _now_est <= dt_time(16, 0))
        _zone_k = k_15m if _is_fast_window else k_1h
        _zone_tf = '15m' if _is_fast_window else '1h'
        if _is_augment_or_entry and not is_hedge:
            _ez = getattr(self.config, 'ENTRY_ZONE_LONG', 22.0); _esz = getattr(self.config, 'ENTRY_ZONE_SHORT', 100.0 - _ez)
            if action in ('OPEN', 'QUICK_OPEN') and not _is_reentry and not _is_rotation and not _is_gap_fill and not _is_rz_entry:
                if (is_long and _zone_k > _ez) or (not is_long and _zone_k < _esz):
                    logger.warning(f"[TRADIER_ZONE_BLOCK] {position_key}: k_{_zone_tf}={_zone_k:.0f} outside zone {_ez}/{_esz} ({'FAST' if _is_fast_window else 'SWING'})")
                    return f"BLOCKED_ZONE_k{_zone_tf}={_zone_k:.0f}"
            # Alignment: count conditions agreeing with direction
            _al = 0
            for _k, _d in [(k_15m,d_15m),(k_1h,d_1h),(k_4h,d_4h),(k_D,d_D)]:
                if (is_long and _k > _d) or (not is_long and _k < _d): _al += 1
            for _ha in [ha_5m, ha_15m, ha_1h, ha_4h]:
                if (is_long and _ha == 'green') or (not is_long and _ha == 'red'): _al += 1
            for _dcb in [dc_basis_15m, dc_basis_1h, dc_basis_4h]:
                if _dcb > 0 and ((is_long and current_price > _dcb) or (not is_long and current_price < _dcb)): _al += 1
            for _w1, _w2 in [(wt1_5m,wt2_5m),(wt1_15m,wt2_15m),(wt1_1h,wt2_1h)]:
                if (is_long and _w1 > _w2) or (not is_long and _w1 < _w2): _al += 1
            _rsi_1h = g('rsi_1h', 50); _rsi_4h = g('rsi_4h', 50)
            if (is_long and _rsi_1h > 50) or (not is_long and _rsi_1h < 50): _al += 1
            if (is_long and _rsi_4h > 50) or (not is_long and _rsi_4h < 50): _al += 1
            if sma_200_1h > 0 and ((is_long and current_price > sma_200_1h) or (not is_long and current_price < sma_200_1h)): _al += 1
            _min_al = 6 if _is_proven_strategy else getattr(self.config, 'ENTRY_MIN_ALIGNMENT', 10)
            # TF_ALIGNMENT_MIN_* switches: exit-side (used by sweep — read here for sweep visibility, no-op on entry)
            _tf_align_long = getattr(self.config, 'TF_ALIGNMENT_MIN_LONG', 2)
            _tf_align_short = getattr(self.config, 'TF_ALIGNMENT_MIN_SHORT', 2)
            _tf_focus_weight = getattr(self.config, 'TF_FOCUS_WEIGHT', 8.0)  # Focus-TF weight (read for sweep)
            _tf_htf1 = getattr(self.config, 'TF_HTF1', '1h')  # HTF1 name (read for sweep)
            _tf_htf3 = getattr(self.config, 'TF_HTF3', 'D')   # HTF3 name (read for sweep)
            _tf_macro = getattr(self.config, 'TF_MACRO', 'D') # Macro name (read for sweep)
            _tf_focus_entry_gate = getattr(self.config, 'TF_FOCUS_ENTRY_HARD_GATE', True)  # Focus entry hard gate (read for sweep)
            _tf_focus_exit_gate = getattr(self.config, 'TF_FOCUS_EXIT_HARD_GATE', True)  # Focus exit hard gate (read for sweep)
            if _is_reentry:
                if _al >= _min_al: quantity = quantity * min(1.5, 1.0 + (_al - _min_al) * 0.05)
            elif not _is_reentry and 'RATIO_RECOVERY' not in _reason_upper:
                if _al < _min_al:
                    logger.warning(f"[TRADIER_ALIGNMENT_BLOCK] {position_key}: alignment={_al}/{_min_al}{' (relaxed)' if _is_proven_strategy else ''}")
                    return f"BLOCKED_ALIGNMENT_{_al}/{_min_al}"
            # dc_low4 hard bottom (applies to all entries including reentries)
            _dc4l_5m = dc_low4_5m; _dc4h_5m = dc_high4_5m
            if is_long and _dc4l_5m > 0 and current_price < _dc4l_5m:
                logger.warning(f"[TRADIER_DC4_BLOCK] {position_key}: price {current_price:.2f} < dc_low4_5m {_dc4l_5m:.2f}")
                return f"BLOCKED_DC4_BOTTOM"
            if not is_long and _dc4h_5m > 0 and current_price > _dc4h_5m:
                logger.warning(f"[TRADIER_DC4_BLOCK] {position_key}: price {current_price:.2f} > dc_high4_5m {_dc4h_5m:.2f}")
                return f"BLOCKED_DC4_TOP"
        # 3. Price Fetch & Validation
        current_price, _ = await self.get_current_price(symbol)
        current_price = float(current_price) if current_price is not None else 0.0
        if current_price <= 0: return "NO_PRICE"

        position = self.get_position(position_key)
        has_local_pos = position and abs(float(getattr(position, 'positionAmt', 0))) > 0.00001

        # ═══ REENTRY = positionAmt==0 ONLY (user 2026-05-06) — REFUSE if non-flat ═══
        # REENTRY is NOT an augment. AUGMENT is its own path. Empty positions have no
        # gain so REENTRY's only valid state is positionAmt==0. NEVER reclassify.
        if action == "REENTRY" and has_local_pos:
            _re_amt_t = abs(float(getattr(position, 'positionAmt', 0))) if position else 0.0
            logger.critical(f"🚫 [TRADIER_REENTRY_REFUSED_NONZERO_AMT] {position_key}: positionAmt={_re_amt_t:.6f} — REENTRY only valid on flat. reason={(reason or '')[:80]}")
            return f"BLOCKED_REENTRY_POS_AMT_NONZERO_{_re_amt_t:.6f}"
        if action == "REENTRY":
            action = "OPEN"
            is_entry_action = True

        # Auto-switch AUGMENT to OPEN if no position exists
        if action == "AUGMENT" and not has_local_pos:
            logger.info(f"[{position_key}] Switching AUGMENT to OPEN (No local position found)")
            action = "OPEN"
            is_entry_action = True

        is_valid, validation_reason, actual_qty = await self.validate_position_before_trade(position_key, action, quantity)
        if not is_valid: return f"VALIDATION_FAILED:{validation_reason}"
        if validation_reason == "ADJUSTED_QTY" and actual_qty: quantity = actual_qty

        if position is None:
            position = await self.ensure_position_present(account_key, symbol, position_side, position_key)
            if not position:
                logger.error(f"[TRADE] {position_key}: Position not found in memory or disk — cannot execute trade without position data")
                return "POSITION_NOT_FOUND"

        # 4. Entry/Augment Blockers — REENTRY and WT_D_BOUNCE_AUG are EXEMPT
        _is_wt_d_aug = "WT_D_BOUNCE_AUG" in str(reason)
        if action == "AUGMENT" and not _is_reentry and not _is_wt_d_aug and getattr(config, 'AUGMENT_ONLY_WHEN_PROFITABLE_TRADIER', True) and position.gain < 0:
            logger.warning(f"[AUGMENT_PROFITABLE_ONLY] {position_key}: BLOCKED — losing position (gain={position.gain:.2f}%)")
            return "BLOCKED_NO_GAIN"
        if action == "AUGMENT" and not _is_reentry and not _is_wt_d_aug and position.gain <= 0:
            logger.warning(f"[{account_key}] BLOCKED AUGMENT {symbol}: Gain is {position.gain:.2f}% (Must be > 0%)")
            return "BLOCKED_NO_GAIN"
        # MIN GAIN AUGMENT GUARD — parity with crypto. NEVER augment a position below MIN_GAIN%.
        _min_aug_gain = getattr(config, 'MIN_GAIN', 3.0)
        if action == "AUGMENT" and not _is_reentry and not _is_wt_d_aug and position.gain < _min_aug_gain:
            logger.warning(f"[AUGMENT_MIN_GAIN_BLOCK] {position_key}: gain={position.gain:.2f}% < {_min_aug_gain}% — BLOCKED")
            return f"BLOCKED_MIN_GAIN_{position.gain:.2f}pct<{_min_aug_gain}pct"
        # HARD WALL: position already open (positionAmt > 0) → NO buy of any kind without MIN_GAIN.
        # Applies to ALL action labels (AUGMENT, REENTRY, OPEN, etc.) — no _is_reentry bypass.
        # Only exemption: exit actions (CLOSE, REDUCE, PROFIT_TAKE) and WT_D_BOUNCE_AUG.
        _pos_qty_hw = abs(float(getattr(position, 'positionAmt', 0) or 0))
        if not is_exit_action and not _is_wt_d_aug and _pos_qty_hw > 0 and position.gain < _min_aug_gain:
            logger.warning(f"[HARD_MIN_GAIN_WALL] {position_key}: positionAmt={_pos_qty_hw} gain={position.gain:.2f}% < {_min_aug_gain}% — BLOCKED action={action}")
            return f"BLOCKED_MIN_GAIN_WALL_{position.gain:.2f}pct<{_min_aug_gain}pct"

        if action == "OPEN" and position_side == "SHORT" and symbol.upper() in self.non_shortable_symbols:
            logger.warning(f"✋ {symbol} BLOCKED: Symbol not available for short sales on Tradier")
            return "NON_SHORTABLE"

        # 5. Execute Exit/Reduce if needed
        if is_exit_action and not is_entry_action:
            qty_to_close = float(quantity)
            pos_qty = abs(float(getattr(position, 'positionAmt', 0)))
            if is_full_close or qty_to_close <= 0: qty_to_close = pos_qty
            final_qty = max(1, int(min(qty_to_close, pos_qty)))
            if final_qty < 1: return "QTY_ZERO"
            logger.info(f"[{position_key}] EXIT EXECUTE: {action} {final_qty} @ {current_price}")
            return await self.execute_now(position_key, account_key, symbol, pos_qty, side, position_side, float(final_qty), current_price, unique_id, reason, is_full_close, action)

        # 6. Entry/Augment Quantity Calculation (The Quantizing Logic)
        SP = config.START_POSITION_SIZE / current_price
        
        # Exception Boost (3x size)
        if symbol.upper() in self.exceptions:
            SP *= 3.0
            logger.info(f"[{position_key}] 🚀 EXCEPTION BOOST: Using 3x base size")

        quantity_before_quantizing = float(quantity)
        
        # User Logic Block (Adapted)
        if account_key in ['trb', 'trc']:
            if dc_high_5m - dc_low_5m > 5 * float(atr_15m): quantity += 0.8 * SP
            if (is_long and current_price > dc_high_1h) or (not is_long and current_price < dc_low_1h): quantity += 0.3 * SP
            if (is_long and k_5m < 30 and k_5m > d_5m) or (not is_long and k_5m > 70 and k_5m < d_5m): quantity += 0.3 * SP
            if (is_long and current_price > dc_high_4h) or (not is_long and current_price < dc_low_4h):
                quantity += 0.3 * SP
                if (is_long and k_5m < 30 and k_5m > d_5m) or (not is_long and k_5m > 70 and k_5m < d_5m): quantity += 0.3 * SP
            
            # HTF Direction checks
            higher_high_15m = high_15m >= high_5m_prev
            lower_low_15m = low_15m <= low_5m_prev
            k_ok = (k_15m >= d_15m or wt1_15m >= wt2_15m or higher_high_15m) if is_long else (k_15m <= d_15m or wt1_15m <= wt2_15m or lower_low_15m)
            stoch1_ok = self._infer_htf_direction_fast(is_long, k_5m, k_5m_prev, k_15m, k_15m_prev, d_15m, wt1_5m, wt2_5m, wt1_15m, wt2_15m, current_price, dc_basis_1h, dc_basis_4h, sma_200_15m, sma_200_1m, sma_200_1m_prev)
            
            reduction_factor = 1.0
            if not k_ok: reduction_factor *= 0.6
            if not stoch1_ok: reduction_factor *= 0.8
            quantity = reduction_factor * quantity

            # Sentiment Boosts
            market_sentiment = g('0market_sentiment_score', 0)
            sentiment_classification = i.get('0sentiment_classification', 'NEUTRAL')
            if is_long and sentiment_classification == "EXTREME_BULLISH" and market_sentiment > 20: quantity += 0.3 * SP
            elif not is_long and sentiment_classification == "EXTREME_BEARISH" and market_sentiment < -20: quantity += 0.3 * SP
            
            # Minimum viable checks
            exchange_min_qty = self.min_qty.get(symbol, 1.0)
            min_viable_qty = max(exchange_min_qty, 6.0 / current_price)
            if quantity < min_viable_qty: quantity = min_viable_qty

        # 7. Final Execution
        final_shares = int(round(quantity))
        if final_shares < 1: return "QTY_ZERO_FINAL"
        if action in ('OPEN', 'AUGMENT', 'REENTRY', 'QUICK_OPEN', 'REVERSE', 'HEDGE_OPEN'):
            _gr_min_tfs_tr = int(getattr(config, "GOLDEN_RULE_HTF_MIN_TFS", 0))
            if _gr_min_tfs_tr > 0:
                try:
                    from golden_rule_htf import score_entry_htf as _gr_score_entry_tr
                    _gr_min_ind_tr = int(getattr(config, "GOLDEN_RULE_MIN_IND", 2))
                    _gr_pass_tr, _gr_n_tfs_tr, _gr_detail_tr = _gr_score_entry_tr(i, is_long, "tradier", _gr_min_tfs_tr, _gr_min_ind_tr, current_price, True)
                    if not _gr_pass_tr:
                        logger.warning(f"[GOLDEN_RULE_CONSENSUS_BLOCK_ENTRY] {position_key} {'LONG' if is_long else 'SHORT'}: {_gr_detail_tr} — REFUSED ({reason})")
                        return f"BLOCKED_GR_CONSENSUS_{_gr_n_tfs_tr}of{_gr_min_tfs_tr}tfs"
                except Exception as _gr_tr_err:
                    logger.error(f"[GOLDEN_RULE_SCORER_ENTRY_FAIL] {position_key}: {_gr_tr_err}")

        logger.info(f"[{position_key}] {action} EXECUTE: {final_shares} shares @ {current_price}")
        return await self.execute_now(position_key, account_key, symbol, abs(position.positionAmt), side, position_side, float(final_shares), current_price, unique_id, reason, is_full_close, action)

    async def _get_settled_cash_from_broker(self, account_key: str) -> float:
        # Query Tradier for ACTUAL settled cash. Fail-closed: return 0.0 on any error.
        # Prevents GFV: buying with unsettled proceeds crashes cash accounts.
        # Cache 30s — fresh enough to catch same-day sells, cheap enough not to rate-limit.
        try:
            if not hasattr(self, '_settled_cash_cache'):
                self._settled_cash_cache = {}  # {account_key: {ts, settled_cash}}
            _now = time.time()
            _entry = self._settled_cash_cache.get(account_key)
            if _entry and (_now - _entry.get('ts', 0)) < 30.0:
                return _entry['settled_cash']
            _client = None
            _existing = getattr(self, 'api_client', None)
            if _existing is not None and getattr(_existing, 'account_key', None) == account_key:
                _client = _existing
            else:
                _client = TradierAPIClient(config, account_key=account_key)
                try:
                    await asyncio.wait_for(_client.connect(), timeout=5.0)
                except Exception as _ce:
                    logger.critical(f"[GFV_SETTLED_CASH] {account_key}: API connect failed ({_ce}) — FAIL CLOSED, returning 0.0")
                    return 0.0
            try:
                _balances = await asyncio.wait_for(_client.get_account_balances(account_key), timeout=5.0)
            except Exception as _be:
                logger.critical(f"[GFV_SETTLED_CASH] {account_key}: balances fetch failed ({_be}) — FAIL CLOSED, returning 0.0")
                return 0.0
            _cash_block = _balances.get('cash', {})
            _cash_available = float(_cash_block.get('cash_available') or _balances.get('total_cash') or 0.0)
            _unsettled = float(_cash_block.get('unsettled_funds') or _balances.get('uncleared_funds') or 0.0)
            _settled = max(0.0, _cash_available - _unsettled)
            self._settled_cash_cache[account_key] = {'ts': _now, 'settled_cash': _settled}
            logger.info(f"[GFV_SETTLED_CASH] {account_key}: cash_available=${_cash_available:.2f} unsettled=${_unsettled:.2f} settled=${_settled:.2f}")
            return _settled
        except Exception as _err:
            logger.critical(f"[GFV_SETTLED_CASH] {account_key}: unexpected error ({_err}) — FAIL CLOSED, returning 0.0")
            return 0.0

    async def _broker_preflight_check(self, account_key: str, symbol: str, position_side: str, quantity: float) -> tuple:
        # Self-verify with broker before opening — returns (block: bool, tag: str).
        # User mandate 2026-05-11: tradier_manage must hit the broker API directly even if
        # tradier_positions isn't running. Cache last fetch for BROKER_PREFLIGHT_CACHE_S seconds.
        try:
            if not hasattr(self, '_broker_preflight_cache'):
                self._broker_preflight_cache = {}  # {account_key: {ts, positions}}
            _ttl = float(getattr(config, 'BROKER_PREFLIGHT_CACHE_S', 3.0))
            _now = time.time()
            _entry = self._broker_preflight_cache.get(account_key)
            _positions = None
            if _entry and (_now - _entry.get('ts', 0)) < _ttl:
                _positions = _entry.get('positions')
            if _positions is None:
                _client = None
                _existing_client = getattr(self, 'api_client', None)
                if _existing_client is not None and getattr(_existing_client, 'account_key', None) == account_key:
                    _client = _existing_client
                else:
                    _client = TradierAPIClient(config, account_key=account_key)
                    try:
                        await asyncio.wait_for(_client.connect(), timeout=5.0)
                    except Exception:
                        return (False, "")
                try:
                    _positions = await asyncio.wait_for(_client.get_account_positions(), timeout=5.0)
                except Exception as _api_err:
                    logger.warning(f"[BROKER_PREFLIGHT_API_ERR] {account_key}:{symbol}: {_api_err} — fail-open")
                    return (False, "")
                self._broker_preflight_cache[account_key] = {'ts': _now, 'positions': _positions or []}
            if not _positions:
                return (False, "")
            _broker_long_qty = 0.0
            _broker_short_qty = 0.0
            for _p in _positions:
                _sym = str(_p.get('symbol', '')).upper()
                if _sym != symbol.upper():
                    continue
                _q = float(_p.get('quantity', 0) or 0)
                if _q > 0: _broker_long_qty += _q
                elif _q < 0: _broker_short_qty += abs(_q)
            _is_long = (position_side == 'LONG')
            # opposite side already at broker → refuse
            if _is_long and _broker_short_qty > 0.5:
                logger.critical(f"🛑 [BROKER_PREFLIGHT_OPPOSITE] {account_key}:{symbol}: broker shows SHORT={_broker_short_qty} but trying to open LONG — REFUSING. Close the SHORT first.")
                return (True, "BROKER_HAS_OPPOSITE_SHORT")
            if (not _is_long) and _broker_long_qty > 0.5:
                logger.critical(f"🛑 [BROKER_PREFLIGHT_OPPOSITE] {account_key}:{symbol}: broker shows LONG={_broker_long_qty} but trying to open SHORT — REFUSING. Close the LONG first.")
                return (True, "BROKER_HAS_OPPOSITE_LONG")
            # same-side already filled → refuse adding more
            _same_qty = _broker_long_qty if _is_long else _broker_short_qty
            _max_per_key = float(getattr(config, 'BROKER_PREFLIGHT_MAX_SAME_SIDE_QTY', 50.0))
            if _same_qty >= _max_per_key:
                logger.critical(f"🛑 [BROKER_PREFLIGHT_FULL] {account_key}:{symbol}_{position_side}: broker already holds {_same_qty} (cap {_max_per_key}) — REFUSING further entry.")
                return (True, "BROKER_SAME_SIDE_AT_CAP")
            return (False, "")
        except Exception as _err:
            logger.error(f"[BROKER_PREFLIGHT_INTERNAL_ERR] {account_key}:{symbol}: {_err}")
            return (False, "")

    def _disaster_guard_for_entry(self, position_key: str, account_key: str, symbol: str, position_side: str, quantity: float, current_price: float, reason: str) -> tuple:
        # Returns (block: bool, block_tag: str). 10 controls — see config_tradier.DISASTER_GUARD section.
        # User mandate 2026-05-11 after $12k MU short opened against +17.89% rally.
        # Never call this for REDUCE/CLOSE/HEDGE_CLOSE — exits must remain unobstructed.
        if not bool(getattr(config, 'DISASTER_GUARD_ENABLED', True)):
            return (False, "")
        # ═══ 2026-05-17 PATCH A — PENNY STOCK LONG BLOCK (DEFAULT OFF behind PENNY_STOCK_LONG_BLOCK_ENABLED) ═══
        # Live 30d evidence: LEXX LONG lost -8.33% (trc:404) and -5.65% (trb:425) on a $0.60 stock.
        # Both were RATIO_BOOST_L(R=0.00) LONG entries on sub-$1 names. SHORTs unaffected (they
        # historically work on penny names — the bias is LONG-buying-into-zero). Fail-closed before
        # any other DG control: hard-reject LONG when last_price < PRICE_USD. Flag default False —
        # sweep-validate before flip.
        if bool(getattr(config, 'PENNY_STOCK_LONG_BLOCK_ENABLED', False)):
            _penny_thr = float(getattr(config, 'PENNY_STOCK_LONG_BLOCK_PRICE_USD', 5.0))
            _penny_px = float(current_price or 0)
            if (position_side == 'LONG') and _penny_thr > 0.0 and _penny_px > 0.0 and _penny_px < _penny_thr:
                logger.critical(f"🛑 [BLOCKED_PENNY_LONG] {position_key}: last_price=${_penny_px:.4f} < ${_penny_thr:.2f} threshold — REFUSING LONG (penny-stock long bleed pattern). reason={reason}")
                return (True, f"BLOCKED_PENNY_LONG_{_penny_px:.4f}")
        try:
            is_long = (position_side == 'LONG')
            ind = None
            try:
                ind = self.get_indicators(symbol)
            except Exception:
                ind = None
            if not ind:
                if not is_long:
                    logger.critical(f"🛑 [DG_0_NO_INDICATORS_SHORT_BLOCK] {position_key}: indicators unavailable — REFUSING SHORT (fail-closed, cannot verify direction). reason={reason}")
                    return (True, "DG_NO_INDICATORS_SHORT_BLOCK")
                return (False, "")
            px = float(current_price or 0)
            if px <= 0:
                return (False, "")
            notional = float(quantity or 0) * px
            is_force_open = 'WT_3M_FORCE_OPEN' in (reason or '').upper() or 'AGENT_FORCE_OPEN' in (reason or '').upper()
            # ── Control 1+2: daily intraday return guard ──
            day_open = safe_fetch_float(ind.get('open_D', 0)) or safe_fetch_float(ind.get('open_1h', 0))
            if day_open > 0:
                day_ret_pct = (px - day_open) / day_open * 100.0
                _short_thr = float(getattr(config, 'DG_DAILY_GAIN_BLOCK_SHORT_PCT', 2.5))
                _long_thr = float(getattr(config, 'DG_DAILY_LOSS_BLOCK_LONG_PCT', 2.5))
                if (not is_long) and day_ret_pct >= _short_thr:
                    logger.critical(f"🛑 [DG_1_DAILY_GAIN_SHORT_BLOCK] {position_key}: day_ret={day_ret_pct:+.2f}% >= +{_short_thr:.2f}% — REFUSING SHORT (don't short winners). reason={reason}")
                    return (True, "DG_DAILY_GAIN_SHORT_BLOCK")
                if is_long and day_ret_pct <= -_long_thr:
                    logger.critical(f"🛑 [DG_2_DAILY_LOSS_LONG_BLOCK] {position_key}: day_ret={day_ret_pct:+.2f}% <= -{_long_thr:.2f}% — REFUSING LONG (don't catch knives). reason={reason}")
                    return (True, "DG_DAILY_LOSS_LONG_BLOCK")
            # ── Control 3: HTF directional alignment ──
            def _bias(close_now, close_prev):
                if close_now <= 0 or close_prev <= 0: return 0
                return 1 if close_now > close_prev else (-1 if close_now < close_prev else 0)
            close_D = safe_fetch_float(ind.get('close_D', 0))
            open_D = safe_fetch_float(ind.get('open_D', 0))
            close_4h = safe_fetch_float(ind.get('close_4h', 0))
            open_4h = safe_fetch_float(ind.get('open_4h', 0))
            close_1h = safe_fetch_float(ind.get('close_1h', 0))
            open_1h = safe_fetch_float(ind.get('open_1h', 0))
            bias_D = _bias(close_D, open_D)
            bias_4h = _bias(close_4h, open_4h)
            bias_1h = _bias(close_1h, open_1h)
            want = 1 if is_long else -1
            if bool(getattr(config, 'DG_HTF_ALIGN_REQUIRE_D', True)):
                # For shorts: block if D is actively bullish (bias_D == 1) OR neutral/stale (bias_D == 0)
                # Neutral = close_D ≈ open_D = stale pre-market data; "uncertain" must block shorts
                # For longs: only block if D is actively bearish (bias_D == -1)
                if (not is_long) and bias_D >= 0:
                    logger.critical(f"🛑 [DG_3D_HTF_D_NOT_BEAR] {position_key}: D bias={'bull' if bias_D>0 else 'neutral/stale'} (need bear=-1 to allow short) — REFUSING SHORT. reason={reason}")
                    return (True, "DG_HTF_D_NOT_BEAR")
                elif is_long and bias_D != 0 and bias_D == -want:
                    logger.critical(f"🛑 [DG_3D_HTF_D_AGAINST] {position_key}: D bias={'bull' if bias_D>0 else 'bear'} but want bull entry — REFUSING. reason={reason}")
                    return (True, "DG_HTF_D_AGAINST")
            if bool(getattr(config, 'DG_HTF_ALIGN_REQUIRE_4H', True)):
                if (not is_long) and bias_4h >= 0:
                    logger.critical(f"🛑 [DG_3H4_HTF_4H_NOT_BEAR] {position_key}: 4h bias={'bull' if bias_4h>0 else 'neutral/stale'} (need bear=-1 to allow short) — REFUSING SHORT. reason={reason}")
                    return (True, "DG_HTF_4H_NOT_BEAR")
                elif is_long and bias_4h != 0 and bias_4h == -want:
                    logger.critical(f"🛑 [DG_3H4_HTF_4H_AGAINST] {position_key}: 4h bias={'bull' if bias_4h>0 else 'bear'} but want bull entry — REFUSING. reason={reason}")
                    return (True, "DG_HTF_4H_AGAINST")
            if bool(getattr(config, 'DG_HTF_ALIGN_REQUIRE_1H', False)) and bias_1h != 0 and bias_1h == -want:
                logger.critical(f"🛑 [DG_3H1_HTF_1H_AGAINST] {position_key}: 1h bias={'bull' if bias_1h>0 else 'bear'} but want {'bull' if is_long else 'bear'} entry — REFUSING. reason={reason}")
                return (True, "DG_HTF_1H_AGAINST")
            # ── Control 4: RSI momentum guard ──
            rsi_15m = safe_fetch_float(ind.get('rsi_15m', 50))
            rsi_1h = safe_fetch_float(ind.get('rsi_1h', 50))
            if (not is_long) and rsi_15m >= float(getattr(config, 'DG_MOMENTUM_BLOCK_RSI15M_FOR_SHORT', 65.0)):
                logger.critical(f"🛑 [DG_4A_RSI15M_TOO_BULL_FOR_SHORT] {position_key}: rsi_15m={rsi_15m:.1f} — REFUSING SHORT. reason={reason}")
                return (True, "DG_RSI15M_TOO_BULL_FOR_SHORT")
            if (not is_long) and rsi_1h >= float(getattr(config, 'DG_MOMENTUM_BLOCK_RSI1H_FOR_SHORT', 65.0)):
                logger.critical(f"🛑 [DG_4C_RSI1H_TOO_BULL_FOR_SHORT] {position_key}: rsi_1h={rsi_1h:.1f} — REFUSING SHORT. reason={reason}")
                return (True, "DG_RSI1H_TOO_BULL_FOR_SHORT")
            if is_long and rsi_15m <= float(getattr(config, 'DG_MOMENTUM_BLOCK_RSI15M_FOR_LONG', 35.0)):
                logger.critical(f"🛑 [DG_4B_RSI15M_TOO_BEAR_FOR_LONG] {position_key}: rsi_15m={rsi_15m:.1f} — REFUSING LONG. reason={reason}")
                return (True, "DG_RSI15M_TOO_BEAR_FOR_LONG")
            if is_long and rsi_1h <= float(getattr(config, 'DG_MOMENTUM_BLOCK_RSI1H_FOR_LONG', 35.0)):
                logger.critical(f"🛑 [DG_4D_RSI1H_TOO_BEAR_FOR_LONG] {position_key}: rsi_1h={rsi_1h:.1f} — REFUSING LONG. reason={reason}")
                return (True, "DG_RSI1H_TOO_BEAR_FOR_LONG")
            # ── Control 5: max notional on force-opens ──
            if is_force_open:
                _max_force = float(getattr(config, 'DG_MAX_FORCE_OPEN_NOTIONAL_USD', 500.0))
                if notional > _max_force:
                    logger.critical(f"🛑 [DG_5_FORCE_OPEN_NOTIONAL_CAP] {position_key}: notional=${notional:.0f} > ${_max_force:.0f} cap — REFUSING. qty={quantity} px={px} reason={reason}")
                    return (True, "DG_FORCE_OPEN_NOTIONAL_CAP")
            # ── Control 6: broker-vs-memory sync guard ──
            # Inspects last cached broker snapshot from _broker_preflight_cache (populated by
            # the async pre-flight). If broker has the key with amt>0 but local memory has 0,
            # refuse. This is a fast in-process check; the real-time broker poll is the
            # _broker_preflight_check above.
            if bool(getattr(config, 'DG_BROKER_MEMORY_SYNC_BLOCK', True)):
                try:
                    _broker_qty = 0.0
                    _bpc = getattr(self, '_broker_preflight_cache', None)
                    if isinstance(_bpc, dict):
                        _snap = _bpc.get(account_key) or {}
                        for _p in (_snap.get('positions') or []):
                            if str(_p.get('symbol', '')).upper() != symbol.upper():
                                continue
                            _q = float(_p.get('quantity', 0) or 0)
                            if (is_long and _q > 0) or ((not is_long) and _q < 0):
                                _broker_qty = abs(_q)
                                break
                    _local_pos = self.position_manager.positions.get(position_key) if self.position_manager else None
                    _local_amt = abs(float(getattr(_local_pos, 'positionAmt', 0) or 0)) if _local_pos else 0.0
                    if _broker_qty > 0.5 and _local_amt < 0.5:
                        logger.critical(f"🛑 [DG_6_BROKER_MEMORY_DESYNC] {position_key}: broker_qty={_broker_qty} but local_amt={_local_amt} — REFUSING further opens until sync. reason={reason}")
                        return (True, "DG_BROKER_MEMORY_DESYNC")
                except Exception:
                    pass
            # ── Control 7: opposite-side profit block ──
            _opp_thr = float(getattr(config, 'DG_OPPOSITE_SIDE_PROFIT_BLOCK_PCT', 1.0))
            if _opp_thr > 0:
                try:
                    _opp_key = f"{account_key}:{symbol}_{'SHORT' if is_long else 'LONG'}"
                    _opp_pos = self.position_manager.positions.get(_opp_key) if self.position_manager else None
                    _opp_gain = float(getattr(_opp_pos, 'gain', 0.0) or 0.0) if _opp_pos else 0.0
                    _opp_amt = abs(float(getattr(_opp_pos, 'positionAmt', 0) or 0)) if _opp_pos else 0.0
                    if _opp_amt > 0 and _opp_gain >= _opp_thr:
                        logger.critical(f"🛑 [DG_7_OPPOSITE_SIDE_WINNING] {position_key}: opposite-side {_opp_key} gain={_opp_gain:.2f}% >= {_opp_thr:.2f}% — REFUSING entry (don't fight your winner). reason={reason}")
                        return (True, "DG_OPPOSITE_SIDE_WINNING")
                except Exception:
                    pass
            # ── Control 8: per-day fire cap on force-opens ──
            if is_force_open:
                _max_per_day = int(getattr(config, 'DG_REPEAT_OPEN_PER_DAY_MAX', 3))
                try:
                    if not hasattr(self, '_dg_force_open_count'):
                        self._dg_force_open_count = {}
                    if not hasattr(self, '_dg_force_open_day'):
                        self._dg_force_open_day = ""
                    _today = datetime.now(timezone.utc).strftime('%Y%m%d')
                    if self._dg_force_open_day != _today:
                        self._dg_force_open_count = {}
                        self._dg_force_open_day = _today
                    _count = int(self._dg_force_open_count.get(position_key, 0))
                    if _count >= _max_per_day:
                        logger.critical(f"🛑 [DG_8_FORCE_OPEN_DAY_CAP] {position_key}: already {_count}/{_max_per_day} force-opens today — REFUSING. reason={reason}")
                        return (True, "DG_FORCE_OPEN_DAY_CAP")
                except Exception:
                    pass
            # ── Control 9: high-volatility day skip on force-opens ──
            if is_force_open:
                _atr_thr = float(getattr(config, 'DG_HIGH_VOLATILITY_ATR_PCT', 4.0))
                _atr_1h = safe_fetch_float(ind.get('atr_1h', 0))
                if _atr_1h > 0 and px > 0 and (_atr_1h / px) * 100.0 >= _atr_thr:
                    logger.critical(f"🛑 [DG_9_HIGH_VOL_FORCE_BLOCK] {position_key}: atr_1h/px={(_atr_1h/px)*100.0:.2f}% >= {_atr_thr:.2f}% — REFUSING force-open on volatile day. reason={reason}")
                    return (True, "DG_HIGH_VOL_FORCE_BLOCK")
            # ── Control 10: ALL short entries require at least one HTF TF confirming bear ──
            # Extended from force-open-only to ALL short entries (2026-05-12: MU disaster repeat).
            # At market open, bias_D/bias_4h are often 0 (stale = neutral). Neutral fails the
            # "agree" test, so this blocks any short entry when HTF state is unknown.
            if (not is_long) and bool(getattr(config, 'DG_WT_3M_REQUIRE_HTF_CONFIRM', True)):
                _agree = False
                if bias_D == want and bias_D != 0: _agree = True
                if bias_4h == want and bias_4h != 0: _agree = True
                if not _agree:
                    logger.critical(f"🛑 [DG_10_SHORT_NO_HTF_CONFIRM] {position_key}: short requires D OR 4h bearish, got D={bias_D} 4h={bias_4h} want={want} — REFUSING SHORT. reason={reason}")
                    return (True, "DG_SHORT_NO_HTF_CONFIRM")
            # ── Control 11: WT daily-level directional block for shorts ──
            # If D-level WaveTrend is bullish (wt1_D > wt2_D), refuse to SHORT.
            if not is_long:
                _wt1_D = safe_fetch_float(ind.get('wt1_D', 0))
                _wt2_D = safe_fetch_float(ind.get('wt2_D', 0))
                if _wt1_D != 0 and _wt2_D != 0 and _wt1_D > _wt2_D:
                    logger.critical(f"🛑 [DG_11_WT_D_BULL_SHORT_BLOCK] {position_key}: wt1_D={_wt1_D:.2f} > wt2_D={_wt2_D:.2f} (daily WT bullish) — REFUSING SHORT. reason={reason}")
                    return (True, "DG_WT_D_BULL_SHORT_BLOCK")
            _dg_day_ret_str = f"{(px - day_open) / day_open * 100.0:+.2f}%" if day_open > 0 else "n/a"
            logger.info(f"[DG_PASS] {position_key}: disaster guard PASSED — side={position_side} day_ret={_dg_day_ret_str} bias_D={bias_D} bias_4h={bias_4h} rsi_15m={rsi_15m:.1f} rsi_1h={rsi_1h:.1f} reason={reason}")
            return (False, "")
        except Exception as _dg_err:
            logger.error(f"[DG_GUARD_ERR] {position_key}: {_dg_err}")
            if position_side == 'SHORT':
                logger.critical(f"🛑 [DG_ERR_SHORT_FAILCLOSED] {position_key}: disaster guard exception → fail-closed for SHORT. err={_dg_err}")
                return (True, "DG_EXCEPTION_SHORT_FAILCLOSED")
            return (False, "")

    async def execute_now(self, position_key: str, account_key: str, symbol: str, original_position_amt: float, side: str, position_side: str, quantity: float, old_price: float, unique_id: str, reason: str, is_full_close: bool, action: str = None) -> str:
        if not is_regular_trading_hours(): return "MARKET_CLOSED"
        exec_lock_key = f"execute_now:{position_key}:{side}"
        MAX_EXECUTION_TIME = 120
        lock_acquired = False
        try:
            if self.redis_manager:
                try:
                    existing = await self.redis_manager.get(exec_lock_key)
                    if existing:
                        if isinstance(existing, dict) and existing.get('status') == 'ghost_cleared':
                            logger.warning(f"[EXECUTE_NOW_BLOCKED] {position_key}: GHOST_CLEARED cooldown active, skipping")
                            return "GHOST_CLEARED_COOLDOWN"
                        lock_age = 0
                        if isinstance(existing, dict) and 'timestamp' in existing:
                            lock_age = time.time() - existing.get('timestamp', 0)
                        if lock_age < MAX_EXECUTION_TIME:
                            logger.error(f"[EXECUTE_NOW_BLOCKED] {position_key}: REDIS LOCK EXISTS - Another execution is in progress!")
                            return "REDIS_EXECUTION_LOCK_EXISTS"
                    await self.redis_manager.set(exec_lock_key, {"timestamp": time.time(), "unique_id": unique_id}, ex=MAX_EXECUTION_TIME)
                    lock_acquired = True
                except Exception as lock_err:
                    logger.error(f"[EXECUTE_NOW_LOCK_ERROR] {position_key}: Error during lock acquisition: {lock_err}")
                    return "LOCK_ACQUISITION_ERROR"
            
            # Check cooldowns
            recent_signal_ts = self.recently_processed_signals.get(position_key, 0)
            is_reduce = (side.upper() == "SELL" and position_side == "LONG") or (side.upper() == "BUY" and position_side == "SHORT")
            is_augment = not is_reduce
            is_hedge = action in ('HEDGE_OPEN', 'HEDGE_CLOSE') if action else False
            is_long = position_side == 'LONG'
            _is_augment_or_entry = is_augment
            is_entry_action = action in ['OPEN', 'REENTRY', 'QUICK_OPEN', 'REVERSE', 'HEDGE_OPEN'] if action else False
            _is_exit_or_reduce = action in ('REDUCE', 'CLOSE', 'FULL_CLOSE', 'PROFIT_TAKE', 'QUICK_CLOSE') if action else False
            # ═══════════════════════════════════════════════════════════════════════════
            # 🛡️ MTF FILTER (USER 2026-05-20) — Phase I REJ_1h winner gates entries.
            # Phase J validated on 293 stocks × 2.13y: pool_S +0.28, avg DD 6.5%, +113%/sym/yr.
            # FILTER mode: existing entries (Rule A, GR direct, WT_3M_FORCE_OPEN, REENTRY etc.)
            # still fire, but must ADDITIONALLY pass: (1) per-sym blocklist, (2) >=1 armed state,
            # (3) GR multi-confirm filter. Per-sym MTF_SIZE_MULT (1.0/0.5/0.0) applied to qty.
            # ROLLBACK: MTF_ARMED_ENTRY_ENABLED=False in config_tradier.py.
            # ═══════════════════════════════════════════════════════════════════════════
            try:
                if is_entry_action and bool(_cfg("MTF_ARMED_ENTRY_ENABLED", False, account_key, symbol, position_side)):
                    import mtf_live_evaluator as _mle
                    if not hasattr(self, "mtf_states"):
                        self.mtf_states = {}
                    _mtf_ind = self.get_indicators(symbol) if hasattr(self, "get_indicators") else self.indicators.get(symbol, {})
                    if _mtf_ind:
                        _mtf_blocked = bool(_cfg("MTF_ENTRY_BLOCKED", False, account_key, symbol, position_side))
                        _mtf_ok, _mtf_reason = _mle.mtf_entry_filter_passes(
                            self.mtf_states, symbol, position_side, _mtf_ind, "tradier", config,
                            blocked=_mtf_blocked,
                        )
                        if not _mtf_ok:
                            logger.warning(f"[MTF_FILTER_BLOCK] {position_key} act={action}: {_mtf_reason}")
                            if lock_acquired and self.redis_manager:
                                await self.redis_manager.delete(exec_lock_key)
                            return f"BLOCKED_{_mtf_reason}"
                        _mtf_mult = float(_cfg("MTF_SIZE_MULT", 1.0, account_key, symbol, position_side))
                        if 0 < _mtf_mult < 1.0:
                            _orig_qty = quantity
                            quantity = quantity * _mtf_mult
                            logger.warning(f"[MTF_LUMPY_HALF] {position_key}: qty {_orig_qty:.4f}→{quantity:.4f} (mult={_mtf_mult})")
            except Exception as _mtf_e:
                logger.warning(f"[MTF_FILTER] {position_key}: check error (fail-open): {_mtf_e}")
            # ═══ SAFETY SWITCH 1: TRADEABLE_KEY GATE (2026-04-16) ═══
            # trb/trc/tra use symbols_trb_long/short (not tradeable_keys.json which is crypto-only)
            if is_augment and getattr(config, 'TRADIER_REQUIRE_TRADEABLE_KEY', True):
                _tradier_accts = {'trb', 'trc', 'tra'}
                if account_key in _tradier_accts:
                    if not self.is_symbol_tradeable(symbol, account_key, position_side):
                        logger.critical(f"🚫 [TRADIER_TRADEABLE_GATE] {position_key}: NOT in symbols_{account_key}_long/short — entry/augment BLOCKED. action={action} reason={reason}")
                        if lock_acquired and self.redis_manager:
                            await self.redis_manager.delete(exec_lock_key)
                        return "BLOCKED_NON_TRADEABLE"
                else:
                    await self.load_tradeable()
                    if position_key not in self.tradeable_keys:
                        logger.critical(f"🚫 [TRADIER_TRADEABLE_GATE] {position_key}: NOT in tradeable_keys — entry/augment BLOCKED. action={action} reason={reason}")
                        if lock_acquired and self.redis_manager:
                            await self.redis_manager.delete(exec_lock_key)
                        return "BLOCKED_NON_TRADEABLE"
            
            # ═══ DISASTER_GUARD (2026-05-11 after MU $12k short-against-rally) ═══
            # 10 controls that REFUSE entry before any order is queued. Applies to
            # every entry path including WT_3M_FORCE_OPEN. Exits/reduces are NOT gated.
            # Only fires on fresh opens where the position is currently flat (positionAmt==0).
            # Augments on existing open positions are NOT gated here — they pass through.
            _dg_is_flat_open = abs(float(original_position_amt or 0)) < 0.0001
            if account_key in {'trb', 'trc', 'tra'} and is_entry_action and not _is_exit_or_reduce and _dg_is_flat_open:
                _dg_block, _dg_tag = self._disaster_guard_for_entry(position_key, account_key, symbol, position_side, float(quantity), float(old_price), reason)
                if _dg_block:
                    logger.critical(f"🛑 [DISASTER_GUARD_BLOCKED_FORCE_OPEN] {position_key}: {_dg_tag} reason={reason}")
                    if lock_acquired and self.redis_manager:
                        await self.redis_manager.delete(exec_lock_key)
                    return f"BLOCKED_{_dg_tag}"

            # ═══ TRADIER_PHYSICS_OPPOSITE_SIDE_BLOCK (2026-05-11 user mandate) ═══
            # Tradier brokerage CANNOT hold both LONG and SHORT on the same equity in the
            # same account at the same time. If the opposite side has any positionAmt in
            # local memory, refuse this entry from queue time — never even attempt the order.
            if account_key in {'trb', 'trc', 'tra'} and is_entry_action and not _is_exit_or_reduce and self.position_manager:
                _opp_side = 'SHORT' if position_side == 'LONG' else 'LONG'
                _opp_pk = f"{account_key}:{symbol}_{_opp_side}"
                _opp_pos = self.position_manager.positions.get(_opp_pk)
                _opp_amt = abs(float(getattr(_opp_pos, 'positionAmt', 0) or 0)) if _opp_pos else 0.0
                if _opp_amt > 0.0001:
                    logger.critical(f"🛑 [TRADIER_OPPOSITE_SIDE_BLOCK] {position_key}: opposite-side {_opp_pk} has amt={_opp_amt} — Tradier cannot hold both sides, REFUSING entry. reason={reason}")
                    if lock_acquired and self.redis_manager:
                        await self.redis_manager.delete(exec_lock_key)
                    return "BLOCKED_OPPOSITE_SIDE_OPEN"

            # ═══ BROKER_PREFLIGHT (2026-05-11 user mega-urgent: trust the broker over local memory) ═══
            # Before ANY new entry, fetch live broker positions for this symbol and refuse if:
            #   - opposite-side position already exists at broker (don't fight your own LONG with a SHORT)
            #   - same-side position is already >= 95% of intended size (already filled, don't double up)
            # Cached for 3s so back-to-back calls don't flood Tradier's rate-limit.
            # If the API call itself fails, we DO NOT block — that would freeze legitimate exits.
            if account_key in {'trb', 'trc', 'tra'} and is_entry_action and not _is_exit_or_reduce and bool(getattr(config, 'BROKER_PREFLIGHT_ENABLED', True)):
                try:
                    _bp_block, _bp_tag = await self._broker_preflight_check(account_key, symbol, position_side, float(quantity))
                    if _bp_block:
                        if lock_acquired and self.redis_manager:
                            await self.redis_manager.delete(exec_lock_key)
                        return f"BLOCKED_{_bp_tag}"
                except Exception as _bp_err:
                    logger.error(f"[BROKER_PREFLIGHT_ERR] {position_key}: {_bp_err} — allowing (fail-open to not freeze trading)")

            # ═══════════════════════════════════════════════════════════════════════
            # HTF_TREND_VETO — Daily-trend gate on OPEN/AUGMENT (USER 2026-05-17, mirror of ez_manage).
            # Source: data/research_20260516/PLAN.md §3.2. Cited +0.47 Sharpe lift.
            # Complementary to DISASTER_GUARD (which checks D close-vs-prev bias):
            # this checks WT momentum on D. ROLLBACK: HTF_TREND_VETO_ENABLED=False.
            # ═══════════════════════════════════════════════════════════════════════
            # 2026-05-18 per-sym overlay
            if (bool(_cfg('HTF_TREND_VETO_ENABLED', False, account_key, symbol, position_side))
                and is_entry_action
                and not _is_exit_or_reduce
                and 'HEDGE' not in (reason or '').upper()
                and 'RULE_C' not in (reason or '').upper()):
                try:
                    _htfv_ind = await self.tradier_indicators.get_indicators(symbol) if getattr(self, 'tradier_indicators', None) else None
                except Exception:
                    _htfv_ind = None
                if _htfv_ind:
                    _htfv_wt1_D = safe_fetch_float(_htfv_ind.get('wt1_D'), 0)
                    _htfv_wt2_D = safe_fetch_float(_htfv_ind.get('wt2_D'), 0)
                    _htfv_data_ok = abs(_htfv_wt1_D) > 1e-9 and abs(_htfv_wt2_D) > 1e-9
                    if _htfv_data_ok:
                        _htfv_aligned = (is_long and _htfv_wt1_D > _htfv_wt2_D) or ((not is_long) and _htfv_wt1_D < _htfv_wt2_D)
                        if not _htfv_aligned:
                            logger.warning(f"[HTF_TREND_VETO] {position_key}: BLOCKED action={action} is_long={is_long} wt1_D={_htfv_wt1_D:.2f} wt2_D={_htfv_wt2_D:.2f} reason={(reason or '')[:50]}")
                            if lock_acquired and self.redis_manager:
                                await self.redis_manager.delete(exec_lock_key)
                            return f"BLOCKED_HTF_TREND_VETO_action={action}"

            # 2026-05-10 USER NON-NEGOTIABLE: WT_3M_FORCE_OPEN bypasses augment cooldown so any
            # tradeable_key with wt1_3m vs wt2_3m condition met can reopen immediately.
            _wt3m_force_open = 'WT_3M_FORCE_OPEN' in (reason or '').upper() and bool(getattr(config, 'WT_3M_FORCE_OPEN_BYPASS_GATES', True))
            if is_augment and recent_signal_ts > 0 and time.time() - recent_signal_ts < getattr(config, 'AUGMENTATION_COOLDOWN_SECONDS', 120.0) and not _wt3m_force_open:
                logger.warning(f"[EXECUTE_NOW_BLOCKED] {position_key}: Augmentation cooldown active")
                if lock_acquired and self.redis_manager:
                    await self.redis_manager.delete(exec_lock_key)
                return "AUGMENTATION_COOLDOWN"
            
            if is_reduce and recent_signal_ts > 0 and time.time() - recent_signal_ts < getattr(config, 'REDUCTION_COOLDOWN_SECONDS', 30.0):
                position = self.position_manager.positions.get(position_key) if self.position_manager else None
                position_gain = getattr(position, 'gain', 0.0) if position else 0.0
                # Cooldown applies regardless of gain — no bypass for any loss level
                if True:
                    logger.warning(f"[EXECUTE_NOW_BLOCKED] {position_key}: Reduction cooldown active (gain={position_gain:.2f}%)")
                    if lock_acquired and self.redis_manager:
                        await self.redis_manager.delete(exec_lock_key)
                    return "REDUCTION_COOLDOWN"
            
            # STRICT_NO_LOSS guard — block reductions at a loss (unless Part 15 sweep disables it)
            # ═══ SAFETY SWITCH 5: SRS REASON-STRING NOLOSS BYPASS (2026-04-16) ═══
            if is_reduce and not is_hedge:
                _srs_bypass_allowed = getattr(config, 'TRADIER_NOLOSS_SRS_BYPASS', True)
                _en_srs = _srs_bypass_allowed and ('STRUCTURAL_RANGE_SHIFT' in str(reason or '').upper() or 'DD_BOUNCE_STOP' in str(reason or '').upper() or 'REENTRY_BREAKOUT' in str(reason or '').upper() or 'OVERNIGHT_GAP_HEDGE_REMOVE' in str(reason or '').upper())
                _en_pos = self.position_manager.positions.get(position_key) if self.position_manager else None
                _en_gain = getattr(_en_pos, 'gain', 0.0) if _en_pos else 0.0
                _en_noloss_min = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0)
                # === BB RECOVERY-TO-ENTRY EXIT BYPASS (2026-04-15, default OFF) ===
                _bb_recov_bypass = False
                if getattr(config, 'BB_RECOVERY_EXIT_ENABLED_TRADIER', False) and _en_pos is not None:
                    try:
                        _br_entry = float(getattr(_en_pos, 'entry_price', 0) or 0)
                        _br_is_long = position_side == 'LONG'
                        _br_px = float(old_price or 0)
                        _br_ind = None
                        try:
                            _br_ind = self.get_indicators(symbol)
                        except Exception:
                            _br_ind = None
                        if _br_ind and _br_entry > 0 and _br_px > 0:
                            _bbh_1h = float(_br_ind.get('bb_high_1h', 0) or 0)
                            _bbl_1h = float(_br_ind.get('bb_low_1h', 0) or 0)
                            _br_atr = float(_br_ind.get('atr_3m', 0) or 0)
                            _br_ha = _br_ind.get('ha_3m', 'neutral')
                            _br_k3 = float(_br_ind.get('stoch_k_3m', 0) or 0)
                            _br_k3p = float(_br_ind.get('k_3m_prev', _br_k3) or _br_k3)
                            _br_tol_pct = float(getattr(config, 'BB_RECOVERY_EXIT_TOLERANCE_PCT_TRADIER', 0.30) or 0.30)
                            _br_tol_atr_m = float(getattr(config, 'BB_RECOVERY_EXIT_TOLERANCE_ATR_MULT_TRADIER', 0.0) or 0.0)
                            _br_tol_abs = (_br_tol_atr_m * _br_atr) if (_br_tol_atr_m > 0 and _br_atr > 0) else (_br_entry * _br_tol_pct / 100.0)
                            _br_within = abs(_br_px - _br_entry) <= _br_tol_abs
                            if _br_is_long and _br_entry > _bbh_1h > 0 and _br_within:
                                if _br_ha == 'red' or _br_k3 < _br_k3p:
                                    _bb_recov_bypass = True
                                    logger.warning(f"[BB_RECOVERY_EXIT_BYPASS] {position_key}: LONG entry={_br_entry:.4f}>bb_high_1h={_bbh_1h:.4f}, recovered to {_br_px:.4f} (tol={_br_tol_abs:.4f}), 3m reversal — allowing close at gain={_en_gain:.2f}%")
                            elif (not _br_is_long) and 0 < _br_entry < _bbl_1h and _br_within:
                                if _br_ha == 'green' or _br_k3 > _br_k3p:
                                    _bb_recov_bypass = True
                                    logger.warning(f"[BB_RECOVERY_EXIT_BYPASS] {position_key}: SHORT entry={_br_entry:.4f}<bb_low_1h={_bbl_1h:.4f}, recovered to {_br_px:.4f} (tol={_br_tol_abs:.4f}), 3m reversal — allowing close at gain={_en_gain:.2f}%")
                    except Exception as _br_e:
                        logger.debug(f"[BB_RECOVERY_EXIT_BYPASS] {position_key}: error: {_br_e}")
                if _en_srs:
                    logger.warning(f"[EXECUTE_NOW_NOLOSS_SRS_BYPASS] {position_key}: gain={_en_gain:.2f}% — STRUCTURAL_RANGE_SHIFT allowed at loss")
                elif _bb_recov_bypass:
                    pass
                elif _en_noloss_min > 0 and _en_gain < _en_noloss_min:
                    logger.warning(f"[EXECUTE_NOW_NOLOSS_BLOCK] {position_key}: BLOCKED reduce at gain={_en_gain:.2f}% < noloss_min={_en_noloss_min}% reason={reason}")
                    if lock_acquired and self.redis_manager:
                        await self.redis_manager.delete(exec_lock_key)
                    return "NOLOSS_BLOCK"
            # Get current position and price
            position = self.position_manager.positions.get(position_key)
            current_price = old_price
            if not current_price or current_price <= 0:
                current_price,ts = await self.get_current_price(symbol)
                current_price = float(current_price)
            if current_price <= 0:
                logger.error(f"[EXECUTE_NOW] {position_key}: No valid price")
                if lock_acquired and self.redis_manager:
                    await self.redis_manager.delete(exec_lock_key)
                return "NO_VALID_PRICE"
            
            # Validate quantity
            if quantity <= 0:
                logger.error(f"[EXECUTE_NOW] {position_key}: Invalid quantity {quantity}")
                if lock_acquired and self.redis_manager:
                    await self.redis_manager.delete(exec_lock_key)
                return "INVALID_QUANTITY"
            
            # Place market order directly
            # ═══ RATIO-AWARE SIZING — bull/bear-scenario-aware L/S rebalancing for stocks ═══
            # BEAR_SCENARIO_SYMBOLS (GLD, USO, XLE, GDX, etc.) go UP when the market goes DOWN.
            # A LONG on a bear_scenario symbol = bearish market bet → flip its contribution to the ratio.
            _pm = self.position_manager
            if _pm and current_price > 0 and not is_hedge:
                _bear_set = getattr(config, 'BEAR_SCENARIO_SYMBOLS', set())
                _long_val = 0.0
                _short_val = 0.0
                for _pk_s, _p_s in _pm.get_positions_by_account(account_key).items():
                    _qty_s = abs(float(getattr(_p_s, 'positionAmt', 0) or getattr(_p_s, 'quantity', 0) or 0))
                    if _qty_s == 0:
                        continue
                    _pr_s = float(getattr(_p_s, 'current_price', 0) or getattr(_p_s, 'entry_price', 0) or current_price)
                    _v_s = _qty_s * _pr_s
                    from utils import pk_symbol as _pk_sym
                    _sym_s = _pk_sym(_pk_s)
                    _is_bear_s = _sym_s in _bear_set
                    if _pk_s.endswith('_LONG'):
                        if _is_bear_s:
                            _short_val += _v_s  # LONG bear_scenario = bearish market bet
                        else:
                            _long_val += _v_s
                    elif _pk_s.endswith('_SHORT'):
                        if _is_bear_s:
                            _long_val += _v_s   # SHORT bear_scenario = bullish market bet
                        else:
                            _short_val += _v_s
                _r = _long_val / max(_short_val, 1.0)
                _this_bear = symbol in _bear_set
                _effective_long = is_long and not _this_bear or not is_long and _this_bear
                _r_min = getattr(config, "LS_RATIO_MIN_TRADIER", 0.50)
                _r_max = getattr(config, "LS_RATIO_MAX_TRADIER", 2.00)
                if _is_augment_or_entry:
                    # ═══ SAFETY SWITCH 2: RATIO BOOST REQUIRES MIN GAIN (2026-04-16) ═══
                    _ratio_gain_gate = getattr(config, 'TRADIER_RATIO_REQUIRE_MIN_GAIN', False)
                    _ratio_min_gain = getattr(config, 'TRADIER_RATIO_BOOST_MIN_GAIN_PCT', 1.0)
                    _pos_for_gain = self.position_manager.positions.get(position_key) if self.position_manager else None
                    _pos_gain = float(getattr(_pos_for_gain, 'gain', 0.0) or 0) if _pos_for_gain else 0.0
                    _allow_boost = (not _ratio_gain_gate) or (_pos_gain >= _ratio_min_gain)
                    if _effective_long and _r < _r_min and _allow_boost:
                        quantity = quantity * 1.5; reason = (reason or '') + f"|RATIO_BOOST_L(R={_r:.2f})"
                    elif not _effective_long and _r > _r_max and _allow_boost:
                        quantity = quantity * 1.5; reason = (reason or '') + f"|RATIO_BOOST_S(R={_r:.2f})"
                    elif _effective_long and _r > _r_max:
                        quantity = quantity * 0.6; reason = (reason or '') + f"|RATIO_CUT_L(R={_r:.2f})"
                    elif not _effective_long and _r < _r_min:
                        quantity = quantity * 0.6; reason = (reason or '') + f"|RATIO_CUT_S(R={_r:.2f})"
            original_quantity = quantity
            if quantity < 1.0:
                logger.warning(f"[EXECUTE_NOW] {position_key}: Quantity {quantity} < 1, enforcing minimum of 1 share")
                quantity = 1.0
            quantity = max(1, int(round(quantity)))
            # ═══ 2026-04-27 FUNDING_GATE_TRADIER + OI_CONFIRM_TRADIER ═══
            # Stocks have no native funding rate; put/call OI ratio is the bullish/bearish flow analogue.
            # Mirrors crypto FUNDING_GATE + OI_CONFIRM (ez_positions_quick.py:11515-11574).
            # Reads data/stocks_oi_cache/{symbol}.json (READ-ONLY, populated by tradier_options_oi_fetcher.py).
            # FAIL-OPEN: any exception or missing data → PASS through (never block on gate bug).
            # Gates only fire on entry actions (OPEN/REENTRY/QUICK_OPEN/REVERSE/HEDGE_OPEN); skipped on exits/reduces.
            if is_entry_action and not _is_exit_or_reduce:
                try:
                    _fg_enabled = bool(getattr(config, 'FUNDING_GATE_ENABLED_TRADIER', False))
                    _oi_enabled = bool(getattr(config, 'OI_CONFIRM_ENABLED_TRADIER', False))
                    if _fg_enabled or _oi_enabled:
                        _oi_cache_path = Path(config.BASE_PATH) / "data" / "stocks_oi_cache" / f"{symbol}.json"
                        _oi_doc = None
                        if _oi_cache_path.is_file():
                            try:
                                with open(_oi_cache_path) as _ofh: _oi_doc = json.load(_ofh)
                            except Exception: _oi_doc = None
                        if _oi_doc:
                            _stale_h = float(getattr(config, 'FUNDING_GATE_TRADIER_STALE_MAX_HOURS', 4.0))
                            _oi_age_sec = time.time() - float(_oi_doc.get('ts') or 0)
                            _oi_fresh = (_oi_age_sec <= _stale_h * 3600.0)
                            if _oi_fresh:
                                # ── FUNDING_GATE_TRADIER (P/C ratio analogue) ──
                                if _fg_enabled:
                                    _fg_apply = (not is_hedge) or bool(getattr(config, 'FUNDING_GATE_TRADIER_HEDGE_GATE_ENABLED', False))
                                    if _fg_apply:
                                        _prefer_near = bool(getattr(config, 'FUNDING_GATE_TRADIER_NEAR_MONEY_PREFER', True))
                                        _pc_long_max = float(getattr(config, 'FUNDING_GATE_PC_RATIO_LONG_MAX', 1.2))
                                        _pc_short_min = float(getattr(config, 'FUNDING_GATE_PC_RATIO_SHORT_MIN', 0.83))
                                        _pc_val = None
                                        if _prefer_near:
                                            _v = _oi_doc.get('near_money_pc_ratio')
                                            try: _pc_val = float(_v) if _v is not None else None
                                            except Exception: _pc_val = None
                                        if _pc_val is None:
                                            _v = _oi_doc.get('pc_ratio')
                                            try: _pc_val = float(_v) if _v is not None else None
                                            except Exception: _pc_val = None
                                        if _pc_val is not None and _pc_val > 0:
                                            if is_long and _pc_val >= _pc_long_max:
                                                logger.warning(f"🚫 [FUNDING_GATE_TRADIER] {position_key}: BLOCKED LONG entry — pc_ratio={_pc_val:.3f} >= {_pc_long_max:.3f} (bearish options flow). action={action}")
                                                if lock_acquired and self.redis_manager:
                                                    await self.redis_manager.delete(exec_lock_key)
                                                return f"FUNDING_GATE_TRADIER_LONG_REJECT_pc={_pc_val:.3f}"
                                            if (not is_long) and _pc_val <= _pc_short_min:
                                                logger.warning(f"🚫 [FUNDING_GATE_TRADIER] {position_key}: BLOCKED SHORT entry — pc_ratio={_pc_val:.3f} <= {_pc_short_min:.3f} (bullish options flow). action={action}")
                                                if lock_acquired and self.redis_manager:
                                                    await self.redis_manager.delete(exec_lock_key)
                                                return f"FUNDING_GATE_TRADIER_SHORT_REJECT_pc={_pc_val:.3f}"
                                            logger.debug(f"[FUNDING_GATE_TRADIER_OK] {position_key}: pc_ratio={_pc_val:.3f} within [{_pc_short_min:.3f}, {_pc_long_max:.3f}]")
                                # ── OI_CONFIRM_TRADIER (4-quadrant OI×price) ──
                                if _oi_enabled:
                                    _oi_apply = (not is_hedge) or bool(getattr(config, 'OI_CONFIRM_TRADIER_HEDGE_GATE_ENABLED', False))
                                    if _oi_apply:
                                        _oi_min_pct = float(getattr(config, 'OI_CONFIRM_MIN_OI_CHANGE_PCT_TRADIER', 0.5))
                                        _oi_px_min_pct = float(getattr(config, 'OI_CONFIRM_MIN_PRICE_PCT_TRADIER', 0.3))
                                        # Total OI delta: weighted call+put change. Cache stores call_oi_change_pct/put_oi_change_pct.
                                        _call_oi = float(_oi_doc.get('total_call_oi') or 0.0)
                                        _put_oi = float(_oi_doc.get('total_put_oi') or 0.0)
                                        _total_oi = _call_oi + _put_oi
                                        _call_chg = _oi_doc.get('call_oi_change_pct')
                                        _put_chg = _oi_doc.get('put_oi_change_pct')
                                        _oi_chg = None
                                        if _call_chg is not None and _put_chg is not None and _total_oi > 0:
                                            try:
                                                _oi_chg = (float(_call_chg) * _call_oi + float(_put_chg) * _put_oi) / _total_oi
                                            except Exception: _oi_chg = None
                                        # Price delta: cache's underlying_price (older) vs current_price (now).
                                        _px_chg_pct = None
                                        try:
                                            _px_old = float(_oi_doc.get('underlying_price') or 0.0)
                                            if _px_old > 0 and current_price > 0:
                                                _px_chg_pct = (float(current_price) - _px_old) / _px_old * 100.0
                                        except Exception: _px_chg_pct = None
                                        if _oi_chg is not None and _px_chg_pct is not None and abs(_oi_chg) >= _oi_min_pct and abs(_px_chg_pct) >= _oi_px_min_pct:
                                            _px_up = _px_chg_pct > 0; _oi_up = _oi_chg > 0
                                            if is_long:
                                                if _px_up and not _oi_up:
                                                    logger.warning(f"🚫 [OI_CONFIRM_TRADIER] {position_key}: BLOCKED LONG — squeeze rally (px↑{_px_chg_pct:+.2f}% + OI↓{_oi_chg:+.2f}%) action={action}")
                                                    if lock_acquired and self.redis_manager:
                                                        await self.redis_manager.delete(exec_lock_key)
                                                    return f"OI_CONFIRM_TRADIER_BLOCK_LONG_SQUEEZE_px={_px_chg_pct:.2f}%_oi={_oi_chg:.2f}%"
                                                if (not _px_up) and _oi_up:
                                                    logger.warning(f"🚫 [OI_CONFIRM_TRADIER] {position_key}: BLOCKED LONG — smart-money shorting (px↓{_px_chg_pct:+.2f}% + OI↑{_oi_chg:+.2f}%) action={action}")
                                                    if lock_acquired and self.redis_manager:
                                                        await self.redis_manager.delete(exec_lock_key)
                                                    return f"OI_CONFIRM_TRADIER_BLOCK_LONG_NEW_SHORTS_px={_px_chg_pct:.2f}%_oi={_oi_chg:.2f}%"
                                            else:
                                                if (not _px_up) and (not _oi_up):
                                                    logger.warning(f"🚫 [OI_CONFIRM_TRADIER] {position_key}: BLOCKED SHORT — long liquidation (px↓{_px_chg_pct:+.2f}% + OI↓{_oi_chg:+.2f}%) action={action}")
                                                    if lock_acquired and self.redis_manager:
                                                        await self.redis_manager.delete(exec_lock_key)
                                                    return f"OI_CONFIRM_TRADIER_BLOCK_SHORT_LIQUIDATION_px={_px_chg_pct:.2f}%_oi={_oi_chg:.2f}%"
                                                if _px_up and _oi_up:
                                                    logger.warning(f"🚫 [OI_CONFIRM_TRADIER] {position_key}: BLOCKED SHORT — bullish breakout (px↑{_px_chg_pct:+.2f}% + OI↑{_oi_chg:+.2f}%) action={action}")
                                                    if lock_acquired and self.redis_manager:
                                                        await self.redis_manager.delete(exec_lock_key)
                                                    return f"OI_CONFIRM_TRADIER_BLOCK_SHORT_NEW_LONGS_px={_px_chg_pct:.2f}%_oi={_oi_chg:.2f}%"
                except Exception as _fgoi_e:
                    logger.debug(f"[FUNDING_OI_GATE_TRADIER] {position_key}: gate skipped on exception (fail-open): {_fgoi_e}")
            if not is_regular_trading_hours():
                if lock_acquired and self.redis_manager:
                    await self.redis_manager.delete(exec_lock_key)
                return "MARKET_CLOSED"
            # ═══ GFV PROTECTION 1: Block sells on positions bought with unsettled funds ═══
            if is_reduce:
                _gfv_ok, _gfv_reason = self.gfv_tracker.can_sell(position_key)
                if not _gfv_ok:
                    logger.critical(f"[GFV_BLOCK] {position_key}: {_gfv_reason} — SELL BLOCKED to prevent Good Faith Violation")
                    if lock_acquired and self.redis_manager:
                        await self.redis_manager.delete(exec_lock_key)
                    return "GFV_BLOCKED"
            # ═══ GFV PROTECTION 2: AUTHORITATIVE settled-cash gate — blocks buys using unsettled proceeds ═══
            # Internal GFV tracker can drift/lose state on restart. This gate queries Tradier directly.
            # Fail-closed: API error → return 0.0 → buy blocked. No exceptions. No bypasses.
            if not is_reduce:
                _sc = await self._get_settled_cash_from_broker(account_key)
                _buy_cost = float(quantity) * float(current_price)
                if _buy_cost > _sc:
                    _msg = f"settled=${_sc:.2f} need=${_buy_cost:.2f} — BUY BLOCKED: would use unsettled funds and trigger GFV"
                    logger.critical(f"[GFV_SETTLED_CASH_BLOCK] 🚨 {position_key}: {_msg}")
                    if lock_acquired and self.redis_manager:
                        await self.redis_manager.delete(exec_lock_key)
                    # Invalidate cache so next attempt re-checks fresh balance
                    if hasattr(self, '_settled_cash_cache'):
                        self._settled_cash_cache.pop(account_key, None)
                    return f"GFV_SETTLED_CASH_BLOCK_{_sc:.0f}"
            # ═══ TRA DAILY TRADE LIMIT — 1 BUY per day, hard cap, no exceptions ═══
            # tra = cash account (ending 627), 4th GFV → account close risk.
            # 1 trade/day ensures absolute cash settlement control.
            if not is_reduce and account_key == 'tra':
                if not hasattr(self, '_tra_daily_buy_count'):
                    self._tra_daily_buy_count = {'date': '', 'count': 0}
                _today_et = datetime.now(ZoneInfo("US/Eastern")).strftime('%Y%m%d')
                if self._tra_daily_buy_count['date'] != _today_et:
                    self._tra_daily_buy_count = {'date': _today_et, 'count': 0}
                _tra_max = int(getattr(config, 'TRA_MAX_BUYS_PER_DAY', 1))
                if self._tra_daily_buy_count['count'] >= _tra_max:
                    logger.critical(f"[TRA_DAILY_BUY_LIMIT] 🚨 {position_key}: already {self._tra_daily_buy_count['count']}/{_tra_max} buy(s) today ({_today_et}) — BUY BLOCKED to prevent GFV. reason={reason}")
                    if lock_acquired and self.redis_manager:
                        await self.redis_manager.delete(exec_lock_key)
                    return f"TRA_DAILY_BUY_LIMIT_{self._tra_daily_buy_count['count']}of{_tra_max}"
            result = await self.place_order(  symbol, side, quantity, "market", duration="day",
                action=action, position_side=position_side, account_key=account_key  )
            
            # --- 5. RESPONSE PARSING (THE FIX) ---
            if result and isinstance(result, dict):
                # Tradier wraps success in 'order', but errors might be top level
                order_data = result.get('order', result) 
                
                order_id = order_data.get('id')
                order_status = order_data.get('status')
                
                # Check for errors
                errors = result.get('errors', {}).get('error', [])
                if isinstance(errors, list) and errors:
                    order_reject_reason = errors[0]
                elif not order_id:
                    # If no ID and no explicit error list, dump the whole thing
                    order_reject_reason = str(result)
                else:
                    order_reject_reason = None

                if order_id:
                    # SUCCESS
                    log_msg = f"[TRADE] SENT: ✅ {symbol:<5} {action:<7} {side:<4} | Qty: {quantity:<4} @ ${current_price:<7.2f} | Status: {order_status} | ID: {order_id}"
                    logger.info(log_msg)
                    tradier_action_logger.info(f"{position_key}: {log_msg}")
                    
                    self.recently_processed_signals[position_key] = time.time()
                    # ═══ TRA DAILY BUY COUNTER: increment on successful buy ═══
                    if not is_reduce and account_key == 'tra':
                        if hasattr(self, '_tra_daily_buy_count'):
                            self._tra_daily_buy_count['count'] += 1
                            logger.critical(f"[TRA_DAILY_BUY_LIMIT] {position_key}: buy #{self._tra_daily_buy_count['count']} of {getattr(config, 'TRA_MAX_BUYS_PER_DAY', 1)} today ({self._tra_daily_buy_count['date']})")
                        # Invalidate settled cash cache — balance changed
                        if hasattr(self, '_settled_cash_cache'):
                            self._settled_cash_cache.pop(account_key, None)
                    # ═══ GFV TRACKING: record sales + mark buys with unsettled funds ═══
                    _trade_value = quantity * current_price
                    if is_reduce and order_id != "GHOST_CLEARED":
                        self.gfv_tracker.record_sale(account_key, symbol, _trade_value)
                    elif is_augment:
                        _unsettled = self.gfv_tracker.get_unsettled_total(account_key)
                        if _unsettled > 0:
                            _settle_date = self.gfv_tracker.get_earliest_settlement(account_key)
                            if _settle_date:
                                self.gfv_tracker.record_buy_with_unsettled(position_key, _settle_date)
                    # BACKTEST_CHANGE_T37: track daily realized P&L for circuit breaker
                    if is_reduce and self.position_manager:
                        _pos_for_pnl = self.position_manager.positions.get(position_key)
                        if _pos_for_pnl:
                            _ep = float(getattr(_pos_for_pnl, 'entry_price', 0) or 0)
                            _ps = position_side
                            if _ep > 0 and current_price > 0:
                                _trade_pnl = (current_price - _ep) * quantity if _ps == 'LONG' else (_ep - current_price) * quantity
                                # 2026-04-30 Job 3 (iii): per-trade returns audit logger — risk-zero additive.
                                try:
                                    from per_trade_logger import log_close as _ptl_log_close
                                    _ptl_pnl_pct = ((current_price - _ep) / _ep * 100.0) if _ps == 'LONG' else ((_ep - current_price) / _ep * 100.0)
                                    _ptl_opened = getattr(_pos_for_pnl, 'opened_at', None)
                                    _ptl_hold_min = 0.0
                                    if _ptl_opened is not None:
                                        try:
                                            _now = datetime.now(timezone.utc)
                                            _opn = _ptl_opened if isinstance(_ptl_opened, datetime) else datetime.fromisoformat(str(_ptl_opened))
                                            _ptl_hold_min = max(0.0, (_now - _opn).total_seconds() / 60.0)
                                        except Exception:
                                            _ptl_hold_min = 0.0
                                    _ptl_log_close(account=account_key, symbol=symbol, side=str(_ps), entry_price=_ep, exit_price=float(current_price), pnl_pct=_ptl_pnl_pct, fees_pct=0.0, hold_minutes=_ptl_hold_min, qty=float(quantity), reason=str(reason))
                                except Exception:
                                    pass
                                _daily_loss_tracker["realized_pnl"] += _trade_pnl
                                _max_loss_pct = getattr(config, 'MAX_DAILY_LOSS_PCT', 3.0)
                                _portfolio_bal = self.get_current_portfolio_balance()
                                _total_exp = _portfolio_bal.get('total', 10000.0) or 10000.0
                                _loss_pct = (_daily_loss_tracker["realized_pnl"] / _total_exp) * 100.0
                                if _loss_pct < -_max_loss_pct and not _daily_loss_tracker["halted"]:
                                    _daily_loss_tracker["halted"] = True
                                    logger.critical(f"[DAILY_LOSS_BREAKER] Daily P&L=${_daily_loss_tracker['realized_pnl']:.2f} ({_loss_pct:.2f}%) exceeds -{_max_loss_pct}% limit. HALTING ALL NEW OPENS.")
                    # Update Local Memory Instantly
                    if self.position_manager:
                        local_pos = self.position_manager.positions.get(position_key)
                        now_utc = datetime.now(timezone.utc)
                        if local_pos:
                            if is_reduce:
                                new_qty = 0.0 if order_id == "GHOST_CLEARED" else max(0.0, float(local_pos.positionAmt) - float(quantity))
                                local_pos.positionAmt = new_qty
                                local_pos.last_reduction_time = now_utc
                                local_pos.last_reduction_amount = float(quantity)
                                local_pos.last_reduction_price = float(current_price)
                                local_pos.was_reduced = True
                                local_pos.reduction_reason = reason or action
                                # 2026-04-26 — record exit time/price on EVERY reduce (not only full close).
                                # Was: only on `if new_qty <= 0` (full close), causing reopen-cooldown to read stale data
                                # on partial reduces. Inconsequential while TRADIER_REOPEN_WAIT_S=0.0 but should be correct.
                                self.last_exit_times[symbol] = time.time()
                                self.last_exit_prices[symbol] = float(current_price)
                                # Full close: ALSO record reentry candidate snapshot for reopen logic.
                                if new_qty <= 0:
                                    if order_id != "GHOST_CLEARED":
                                        _ep = float(getattr(local_pos, 'entry_price', 0) or 0)
                                        _mg = float(getattr(local_pos, 'max_gain', 0) or 0)
                                        self._record_reentry_candidate(position_key, float(current_price), _ep, reason or action, _mg, float(original_position_amt))
                                    if order_id == "GHOST_CLEARED":
                                        # ONLY zero positionAmt + gain + cycle_peak_gain. NEVER zero entry_price/max_gain — they are sacred.
                                        # max_gain has its own decay function for reentry; cycle_peak_gain resets per cycle for exit logic.
                                        local_pos.positionAmt = 0.0
                                        local_pos.gain = 0.0
                                        try: local_pos.cycle_peak_gain = 0.0
                                        except Exception: pass
                                        logger.warning(f"[GHOST_CLEAR] {position_key}: positionAmt + gain + cycle_peak_gain zeroed. entry_price/max_gain PRESERVED.")
                            elif is_augment:
                                new_qty = float(local_pos.positionAmt) + float(quantity)
                                # 2026-04-08 FIX: Do NOT update positionAmt locally.
                                # Let tradier_positions API sync (every 5s) be the ONLY source of truth.
                                # Local optimistic updates caused drift: 125 real → 175 local from double-counting.
                                # local_pos.positionAmt = float(local_pos.positionAmt) + float(quantity)  # DISABLED
                                local_pos.last_augmentation_time = now_utc
                                local_pos.augment_reason = reason or action
                                old_cost = (new_qty - quantity) * local_pos.entry_price
                                new_cost = quantity * current_price
                                if new_qty > 0:
                                    local_pos.entry_price = (old_cost + new_cost) / new_qty

                    await self._append_to_history(position_key, action, quantity, current_price, reason)
                    if lock_acquired and self.redis_manager:
                        # Ghost-cleared positions get 5-min cooldown to stop repeat loops
                        ghost_ttl = 300 if order_id == "GHOST_CLEARED" else 5
                        ghost_status = "ghost_cleared" if order_id == "GHOST_CLEARED" else "filled"
                        await self.redis_manager.set(exec_lock_key, {"status": ghost_status, "timestamp": time.time()}, ex=ghost_ttl)

                    return "SUCCESS"

                elif order_reject_reason:
                    # REJECTION
                    logger.error(f"[TRADE] ❌ {symbol} {side} REJECTED: {order_reject_reason}")
                    if lock_acquired and self.redis_manager: await self.redis_manager.delete(exec_lock_key)
                    return f"ORDER_REJECTED:{order_reject_reason}"
            else:
                logger.error(f"[TRADE] ❌ {symbol} {side} Failed: Invalid API response.")
                if lock_acquired and self.redis_manager: await self.redis_manager.delete(exec_lock_key)
                return "ORDER_API_ERROR"

        except Exception as e:
            logger.error(f"[EXECUTE_CRASH] {position_key}: {e}",  exc_info=True )
            if lock_acquired and self.redis_manager:
                try: await self.redis_manager.delete(exec_lock_key)
                except Exception: pass
            return f"EXCEPTION_{str(e)[:50]}"

    async def _append_to_history(self, position_key: str, trade_type: str, qty: float, price: float, reason: str = ""):
        try:
            parts = position_key.split(':')
            account_key = parts[0] if len(parts) > 0 else "unknown"
            symbol_side = parts[1] if len(parts) > 1 else position_key
            hist_dir = Path(self.config.DATA_DIR) / "history" / account_key
            hist_dir.mkdir(parents=True, exist_ok=True)
            filename = hist_dir / f"{symbol_side}.jsonl"
            MAX_SIZE_BYTES = 200 * 1024
            if filename.exists():
                try:
                    if filename.stat().st_size > MAX_SIZE_BYTES:
                        backup = filename.with_name(f"{filename.name}.bak")
                        if backup.exists(): backup.unlink()
                        filename.rename(backup)
                except Exception: pass
            entry = {"ts": datetime.now(timezone.utc).isoformat(), "type": trade_type, "qty": round(float(qty), 6), "price": round(float(price), 4), "value": round(float(qty) * float(price), 2), "reason": reason or "Manual/System Detection", "indicators": {}}
            async with aiofiles.open(filename, "a") as f:
                await f.write(orjson.dumps(entry).decode('utf-8') + "\n")
        except Exception as e:
            logger.error(f"[HISTORY] Write error {position_key}: {e}")

    def _load_reentry_candidates(self, account_key: str) -> dict:
        path = Path(config.BASE_PATH) / account_key / "reentry_candidates.json"
        if path.exists():
            try:
                return json.loads(path.read_text())
            except Exception:
                return {}
        return {}

    def _save_reentry_candidates(self, account_key: str, candidates: dict):
        path = Path(config.BASE_PATH) / account_key / "reentry_candidates.json"
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(json.dumps(candidates, indent=2, default=str))

    def _record_reentry_candidate(self, position_key: str, exit_price: float, entry_price: float, reason: str, max_gain: float, position_amt: float):
        try:
            parts = position_key.split(':')
            if len(parts) < 2:
                return
            account_key = parts[0]
            symbol_side = parts[1]
            symbol = symbol_side.rsplit('_', 1)[0] if '_' in symbol_side else symbol_side
            side = "LONG" if symbol_side.endswith("_LONG") else "SHORT"
            candidates = self._load_reentry_candidates(account_key)
            candidates[position_key] = {"symbol": symbol, "side": side, "exit_price": round(exit_price, 4), "exit_time": datetime.now(timezone.utc).isoformat(), "exit_reason": reason or "UNKNOWN", "entry_price": round(entry_price, 4), "max_gain_at_exit": round(max_gain, 2), "position_amt_at_exit": round(position_amt, 2), "reentry_target_price": round(exit_price, 4), "status": "WAITING", "attempts": 0, "last_check": datetime.now(timezone.utc).isoformat()}
            self._save_reentry_candidates(account_key, candidates)
            logger.info(f"[REENTRY_RECORD] {position_key}: Recorded reentry candidate @ exit=${exit_price:.2f} entry=${entry_price:.2f} reason={reason}")
        except Exception as e:
            logger.error(f"[REENTRY_RECORD] Error recording {position_key}: {e}")

    def _seed_reentry_from_positions(self):
        try:
            now_utc = datetime.now(timezone.utc)
            for acc in self.target_accounts:
                candidates = self._load_reentry_candidates(acc)
                seeded = 0
                for side_file in ["long_positions.json", "short_positions.json"]:
                    pos_path = Path(config.BASE_PATH) / acc / side_file
                    if not pos_path.exists():
                        continue
                    try:
                        pos_data = json.loads(pos_path.read_text())
                    except Exception:
                        continue
                    for pk, pv in pos_data.items():
                        amt = float(pv.get("positionAmt", 0) or 0)
                        ep = float(pv.get("entry_price", 0) or 0)
                        if amt != 0 or ep <= 0:
                            continue
                        red_time_str = pv.get("last_reduction_time", "")
                        red_price = float(pv.get("last_reduction_price", 0) or 0)
                        if not red_time_str or red_price <= 0:
                            continue
                        try:
                            red_dt = datetime.fromisoformat(red_time_str.replace("Z", "+00:00"))
                        except Exception:
                            continue
                        hours_since = (now_utc - red_dt).total_seconds() / 3600.0
                        if hours_since > 24.0:
                            continue
                        if pk in candidates and candidates[pk].get("status") in ("WAITING", "REENTERED", "EXPIRED"):
                            continue
                        symbol = pk.split(":")[-1].rsplit("_", 1)[0] if ":" in pk else pk.rsplit("_", 1)[0]
                        side = "LONG" if pk.endswith("_LONG") else "SHORT"
                        mg = float(pv.get("max_gain", 0) or 0)
                        reason = pv.get("reduction_reason", "SEEDED_FROM_POSITIONS")
                        candidates[pk] = {"symbol": symbol, "side": side, "exit_price": round(red_price, 4), "exit_time": red_time_str, "exit_reason": reason or "SEEDED_FROM_POSITIONS", "entry_price": round(ep, 4), "max_gain_at_exit": round(mg, 2), "position_amt_at_exit": 0, "reentry_target_price": round(red_price, 4), "status": "WAITING", "attempts": 0, "last_check": now_utc.isoformat()}
                        seeded += 1
                decisions_dir = Path(config.BASE_PATH) / "data" / "decisions"
                today_str = now_utc.strftime("%Y%m%d")
                for jsonl_name in [f"decisions_{acc}_{today_str}.jsonl"]:
                    jsonl_path = decisions_dir / jsonl_name
                    if not jsonl_path.exists():
                        continue
                    try:
                        for line in jsonl_path.read_text().strip().split("\n"):
                            if not line.strip():
                                continue
                            try:
                                rec = json.loads(line)
                            except Exception:
                                continue
                            action_str = rec.get("action", "")
                            if "CLOSE" not in action_str.upper():
                                continue
                            rpk = rec.get("position_key", "")
                            if not rpk or rpk in candidates:
                                continue
                            trade = rec.get("trade", {})
                            pos_amt = float(trade.get("position_amt", 0) or 0)
                            if pos_amt <= 0:
                                continue
                            exit_p = float(trade.get("price", 0) or 0)
                            entry_p = float(trade.get("entry_price", 0) or 0)
                            if exit_p <= 0 or entry_p <= 0:
                                continue
                            symbol = rpk.split(":")[-1].rsplit("_", 1)[0] if ":" in rpk else rpk.rsplit("_", 1)[0]
                            side = "LONG" if rpk.endswith("_LONG") else "SHORT"
                            ts_str = rec.get("timestamp", now_utc.isoformat())
                            reason_text = rec.get("reason_text", "SEEDED_FROM_DECISIONS")
                            candidates[rpk] = {"symbol": symbol, "side": side, "exit_price": round(exit_p, 4), "exit_time": ts_str, "exit_reason": reason_text, "entry_price": round(entry_p, 4), "max_gain_at_exit": round(float(trade.get("gain_pct", 0) or 0), 2), "position_amt_at_exit": round(pos_amt, 2), "reentry_target_price": round(exit_p, 4), "status": "WAITING", "attempts": 0, "last_check": now_utc.isoformat()}
                            seeded += 1
                    except Exception as e:
                        logger.error(f"[REENTRY_SEED] Error reading {jsonl_path}: {e}")
                if seeded > 0:
                    self._save_reentry_candidates(acc, candidates)
                    logger.info(f"[REENTRY_SEED] {acc}: Seeded {seeded} reentry candidates from positions and decisions")
        except Exception as e:
            logger.error(f"[REENTRY_SEED] Error: {e}", exc_info=True)

    def would_exit_trigger_now(self, symbol, indicators, side, *, current_price=None):
        # Symmetric gate: returns (blocked: bool, reason: str).
        # Blocks an entry/reentry whenever the mirror exit rubric would fire for `side`.
        # Consulted rubrics: wt_dc_score_exit, DELTA engine (zone+exit flags), DELTA speed floor.
        try:
            i = indicators or {}
            is_long = str(side).upper() == "LONG"
            px = float(current_price) if current_price else float(i.get("current_price", 0) or 0)
            if px <= 0:
                return False, "no_price"
            _s, _r = wt_dc_score_exit(i, is_long, px)
            _th = getattr(config, 'WT_DC_EXIT_THRESHOLD', 25)
            if _s >= _th:
                return True, f"WT_DC_SCORE_{_s:.0f}>={_th}_{_r[:40]}"
            _dt = getattr(self, 'delta_tracker', None)
            if config.DELTA_ENGINE_ENABLED and _dt:
                _sig = _dt.update(symbol, i, {"side": "LONG" if is_long else "SHORT"})
                if _sig:
                    if is_long and _sig.exit_long:
                        return True, f"DELTA_EXIT_LONG_{_sig.zone}_{_sig.zone_reason or ''}"[:90]
                    if (not is_long) and _sig.exit_short:
                        return True, f"DELTA_EXIT_SHORT_{_sig.zone}_{_sig.zone_reason or ''}"[:90]
                    if is_long and _sig.zone in ("TOP", "TOP_TOP"):
                        return True, f"DELTA_TOP_ZONE_LONG_{_sig.zone}"
                    if (not is_long) and _sig.zone in ("BOTTOM", "BOTTOM_BOTTOM"):
                        return True, f"DELTA_BOTTOM_ZONE_SHORT_{_sig.zone}"
                    _speed_min = float(getattr(config, 'REENTRY_SYMGATE_SPEED_MIN', 1.0))
                    _bs = float(getattr(_sig, 'bull_speed', 0.0) or 0.0)
                    _es = float(getattr(_sig, 'bear_speed', 0.0) or 0.0)
                    if is_long and _bs < _speed_min:
                        return True, f"DELTA_SPEED_SLOW_LONG_bs={_bs:.1f}<{_speed_min:.1f}"
                    if (not is_long) and _es < _speed_min:
                        return True, f"DELTA_SPEED_SLOW_SHORT_es={_es:.1f}<{_speed_min:.1f}"
            return False, "ok"
        except Exception as e:
            logger.warning(f"[SYMGATE] {symbol} {side}: error {e} — failing OPEN (allow)")
            return False, f"error:{e}"

    async def reentry_monitor_loop(self):
        logger.info("[REENTRY_MONITOR] Dedicated reentry monitor started (15s cycle)")
        await asyncio.sleep(30)
        while self.running:
            try:
                if not is_regular_trading_hours():
                    await asyncio.sleep(60)
                    continue
                for acc in self.target_accounts:
                    candidates = self._load_reentry_candidates(acc)
                    if not candidates:
                        continue
                    changed = False
                    now_utc = datetime.now(timezone.utc)
                    for pk, cand in list(candidates.items()):
                        if cand.get("status") != "WAITING":
                            continue
                        symbol = cand.get("symbol", "")
                        side = cand.get("side", "LONG")
                        exit_price = float(cand.get("exit_price", 0))
                        exit_time_str = cand.get("exit_time", "")
                        if not symbol or exit_price <= 0:
                            continue
                        try:
                            exit_dt = datetime.fromisoformat(exit_time_str.replace("Z", "+00:00")) if exit_time_str else now_utc
                        except Exception:
                            exit_dt = now_utc
                        hours_since = (now_utc - exit_dt).total_seconds() / 3600.0
                        _min_gap_min = float(getattr(config, 'REENTRY_MIN_GAP_MINUTES', 15.0))
                        if hours_since * 60.0 < _min_gap_min:
                            logger.info(f"[REENTRY_MONITOR] {pk}: GAP {hours_since*60.0:.1f}m<{_min_gap_min:.0f}m — waiting for min-gap window")
                            continue
                        # REENTRIES NEVER EXPIRE — WT always cycles back. Weekend gaps can be 66+ hours.
                        if hours_since > 168.0:  # Only expire after 1 WEEK (absolute safety net)
                            cand["status"] = "EXPIRED"
                            changed = True
                            logger.critical(f"[REENTRY_MONITOR] {pk}: EXPIRED after {hours_since:.1f}h (1 week) — THIS SHOULD NEVER HAPPEN")
                            continue
                        if hours_since > 24.0:
                            logger.warning(f"[REENTRY_OVERDUE] {pk}: {hours_since:.1f}h since exit — still waiting for 2/3 WT favor")
                        i = self.get_indicators(symbol) or {}
                        current_price = float(i.get("current_price", 0))
                        if current_price <= 0:
                            try:
                                client = TradierAPIClient(config, account_key=acc)
                                await client.connect()
                                quote = await client.get_quote(symbol)
                                current_price = float(quote.get("last", 0)) if quote else 0
                            except Exception:
                                pass
                        if current_price <= 0:
                            logger.warning(f"[REENTRY_MONITOR] {pk}: No price available for {symbol}, skipping")
                            continue
                        k_5m = float(i.get("stoch_k_5m", 50))
                        k_5m_prev = float(i.get("stoch_k_5m_prev", 50))
                        d_5m = float(i.get("stoch_d_5m", 50))
                        k_15m = float(i.get("stoch_k_15m", 50))
                        k_15m_prev = float(i.get("stoch_k_15m_prev", 50))
                        k_1h_rm = float(i.get("stoch_k_1h", 50))
                        wt1_5m = float(i.get("wt1_5m", 0))
                        wt2_5m = float(i.get("wt2_5m", 0))
                        wt1_15m = float(i.get("wt1_15m", 0))
                        wt2_15m = float(i.get("wt2_15m", 0))
                        wt1_1h = float(i.get("wt1_1h", 0))
                        wt2_1h = float(i.get("wt2_1h", 0))
                        wt1_4h = float(i.get("wt1_4h", 0))
                        wt2_4h = float(i.get("wt2_4h", 0))
                        wt1_D = float(i.get("wt1_D", 0))
                        wt2_D = float(i.get("wt2_D", 0))
                        cand["last_check"] = now_utc.isoformat()
                        cand["attempts"] = cand.get("attempts", 0) + 1
                        changed = True
                        reenter = False
                        wt_cross_1h = str(i.get("wt_cross_1h", ""))
                        # Check the current exit score — if it's still high, DO NOT reenter
                        _rm_exit_score, _rm_exit_reason = wt_dc_score_exit(i, side == "LONG", current_price)
                        _rm_threshold = getattr(config, 'WT_DC_EXIT_THRESHOLD', 25)
                        if _rm_exit_score >= _rm_threshold:
                            logger.info(f"[REENTRY_MONITOR] {pk}: exit score {_rm_exit_score:.0f}>={_rm_threshold} — signal still says EXIT, waiting for reversal")
                            continue
                        if getattr(config, 'REENTRY_SYMGATE_ENABLED', True):
                            _sg_blocked, _sg_reason = self.would_exit_trigger_now(symbol, i, side, current_price=current_price)
                            if _sg_blocked:
                                logger.info(f"[REENTRY_MONITOR] {pk}: SYMGATE block — {_sg_reason}")
                                continue
                        # ═══ PATHWAY 0: 5m-exit rescue (user directive 2026-04-16) ═══
                        _er_str = str(cand.get("exit_reason", "")).upper()
                        _was_5m_exit = any(t in _er_str for t in ('5M', '_5M_', 'DELTA_EXIT', 'STOCH_CROSS', 'WT_CROSSUNDER', 'WT_CROSSOVER', 'MANDATORY_REENTRY', 'WT_DC_EXIT'))
                        if side == "LONG":
                            _1h_still_trending = wt1_1h > wt2_1h
                            _5m_entry_fires = (wt1_5m > wt2_5m) and (k_5m > d_5m)
                        else:
                            _1h_still_trending = wt1_1h < wt2_1h
                            _5m_entry_fires = (wt1_5m < wt2_5m) and (k_5m < d_5m)
                        _p0_rescue = _was_5m_exit and _1h_still_trending and _5m_entry_fires
                        if _p0_rescue:
                            logger.warning(f"🟢 [REENTRY_P0_5M_RESCUE] {pk}: exit_reason='{_er_str[:40]}' 1h_trending={_1h_still_trending} 5m_entry=TRUE — BYPASS FILTERS")
                        # ═══ PATHWAY F (2026-04-17) — FAVORABLE MOVE: market kept moving in our direction after exit.
                        # The exit was premature. Force reenter. Mirror of crypto ez_manage FIX B.
                        # User rule: k_15m > 90 (LONG) / < 10 (SHORT) → size down to 50% (rally may be ending). ═══
                        _fav_pct = float(getattr(config, 'REENTRY_FAVORABLE_MOVE_PCT', 1.0)) / 100.0
                        _fav_exit_px = float(cand.get("exit_price", 0) or 0)
                        if side == "LONG":
                            _fav_price = _fav_exit_px > 0 and current_price >= _fav_exit_px * (1.0 + _fav_pct)
                        else:
                            _fav_price = _fav_exit_px > 0 and current_price <= _fav_exit_px * (1.0 - _fav_pct)
                        _fav_htf_count = sum(1 for w1, w2 in [(wt1_1h, wt2_1h), (wt1_4h, wt2_4h), (wt1_D, wt2_D)] if (w1 > w2 if side == "LONG" else w1 < w2))
                        _fav_htf_ok = _fav_htf_count >= int(getattr(config, 'REENTRY_FAVORABLE_HTF_MIN', 2))
                        _fav_fired = (not _p0_rescue) and _fav_price and _fav_htf_ok
                        _fav_k15m_partial = False
                        if _fav_fired:
                            _k15m_thr = float(getattr(config, 'REENTRY_K15M_PARTIAL_THRESHOLD', 90.0))
                            _fav_k15m_partial = (side == "LONG" and k_15m >= _k15m_thr) or (side == "SHORT" and k_15m <= (100.0 - _k15m_thr))
                            _fav_mult_pct = int(getattr(config, 'REENTRY_K15M_PARTIAL_MULT', 0.5) * 100) if _fav_k15m_partial else 100
                            logger.critical(f"🚀 [REENTRY_FAVORABLE_MOVE] {pk}: exit={_fav_exit_px:.2f} cur={current_price:.2f} move={(current_price/_fav_exit_px-1)*100:+.2f}% htf={_fav_htf_count}/3 k15m={k_15m:.0f} size={_fav_mult_pct}%{' (OVERHEAT — rally maturing)' if _fav_k15m_partial else ''} — FORCING REENTRY")
                        # ═══ AGGRESSIVE FAST-PATH (2026-04-17) — bypass tier gates if fresh dc_x1h or stoch_x15m
                        # fires within the aggressive window. NEVER delays — only ACCELERATES when signal is there.
                        # If window triggers absent, logic falls through to the normal Tier 1/2/3 gates below. ═══
                        _t0_window_min = float(getattr(config, 'REENTRY_AGGRESSIVE_WINDOW_MIN', 5.0))
                        _dc_x1h_now = (i.get("dc_basis_crossover_1h", False) if side == "LONG" else i.get("dc_basis_crossunder_1h", False))
                        _stoch_15m_now = (i.get("stoch_crossover_15m", False) if side == "LONG" else i.get("stoch_crossunder_15m", False))
                        _aggr_fired = (
                            not _p0_rescue and not _fav_fired
                            and hours_since * 60.0 < _t0_window_min
                            and (_dc_x1h_now or _stoch_15m_now)
                        )
                        if _aggr_fired:
                            logger.warning(f"[REENTRY_MONITOR] {pk}: 🎯 AGGRESSIVE fast-path (0-{_t0_window_min:.0f}m) — dc_x1h={_dc_x1h_now} stoch_x15m={_stoch_15m_now} @ {hours_since*60:.0f}m post-exit. Skipping tier gates.")
                        # ═══ PATHWAY G: 60-MIN UNCONDITIONAL — price kept running in our direction. No questions asked, 50% size. ═══
                        _g60_enabled = getattr(config, 'REENTRY_60MIN_UNCONDITIONAL_ENABLED', True)
                        _g60_window_min = float(getattr(config, 'REENTRY_60MIN_WINDOW_MIN', 60.0))
                        _g60_min_pct = float(getattr(config, 'REENTRY_60MIN_MIN_PCT', 0.3)) / 100.0
                        _g60_fired = False
                        if _g60_enabled and not _p0_rescue and not _fav_fired and not _aggr_fired and hours_since * 60.0 <= _g60_window_min:
                            _g60_exit_px = float(cand.get("exit_price", 0) or 0)
                            if side == "LONG":
                                _g60_price_ok = _g60_exit_px > 0 and current_price >= _g60_exit_px * (1.0 + _g60_min_pct)
                            else:
                                _g60_price_ok = _g60_exit_px > 0 and current_price <= _g60_exit_px * (1.0 - _g60_min_pct)
                            if _g60_price_ok:
                                _g60_fired = True
                                logger.critical(f"⚡ [REENTRY_60MIN_UNCONDITIONAL] {pk}: exit={_g60_exit_px:.2f} cur={current_price:.2f} move={(current_price/_g60_exit_px-1)*100:+.2f}% {hours_since*60:.0f}min since exit — 50% REENTRY NO QUESTIONS ASKED")
                        # STOCH GATE — 3 tiers based on hours since exit (skipped if P0 rescue OR aggressive fast-path fires):
                        # Tier 1 (0–3h): rally reentry — skip k5m<50, need k5m rising + k15m rising + 2/3 HTF (1h/4h/D) WT aligned
                        # Tier 2 (3–48h): strict — k5m MUST drop below 50 before reentering (whipsaw zone)
                        # Tier 3 (48h+): bypass stoch gate entirely, rely on exit score + WT alignment only
                        if not _p0_rescue and not _aggr_fired and not _fav_fired and not _g60_fired and hours_since < 3.0:
                            _k5m_rising = k_5m > k_5m_prev
                            _k15m_rising = k_15m > k_15m_prev
                            _htf_wt_fav = sum(1 for w1, w2 in [(wt1_1h, wt2_1h), (wt1_4h, wt2_4h), (wt1_D, wt2_D)] if (w1 > w2 if side == "LONG" else w1 < w2))
                            _rally_k15m_max = float(getattr(config, 'REENTRY_RALLY_K15M_MAX', 100.0))
                            _rally_htf_min = int(getattr(config, 'REENTRY_RALLY_HTF_MIN', 2))
                            if side == "LONG":
                                _k15m_lvl_ok = k_15m < _rally_k15m_max
                                if not (_k5m_rising and _k15m_rising and _k15m_lvl_ok and _htf_wt_fav >= _rally_htf_min):
                                    logger.info(f"[REENTRY_MONITOR] {pk}: LONG rally gate FAIL k5m={k_5m:.0f}(rising={_k5m_rising}) k15m={k_15m:.0f}<{_rally_k15m_max:.0f}(rising={_k15m_rising}) htf={_htf_wt_fav}/{_rally_htf_min} (<3h)")
                                    continue
                            else:
                                _k15m_lvl_ok = k_15m > (100.0 - _rally_k15m_max) if _rally_k15m_max < 100.0 else True
                                if not (k_5m < k_5m_prev and k_15m < k_15m_prev and _k15m_lvl_ok and _htf_wt_fav >= _rally_htf_min):
                                    logger.info(f"[REENTRY_MONITOR] {pk}: SHORT rally gate FAIL k5m={k_5m:.0f}(falling={k_5m < k_5m_prev}) k15m={k_15m:.0f}(falling={k_15m < k_15m_prev},lvl={_k15m_lvl_ok}) htf={_htf_wt_fav}/{_rally_htf_min} (<3h)")
                                    continue
                        elif not _p0_rescue and not _aggr_fired and not _fav_fired and not _g60_fired and hours_since < 48.0:
                            # ═══ SAFETY SWITCH 4: BOUNCE REENTRY K-GATE (2026-04-16, hardcoded 50/50 → config 2026-04-26) ═══
                            _bounce_enabled = getattr(config, 'BOUNCE_REENTRY_ENABLED_TRADIER', True)
                            if _bounce_enabled:
                                # 2026-04-26 — wires DEAD switches BOUNCE_REENTRY_K_RESET_{LONG,SHORT}_TRADIER. Was hardcoded 50/50.
                                _kr_long_thr = float(getattr(config, 'BOUNCE_REENTRY_K_RESET_LONG_TRADIER', 35))
                                _kr_short_thr = float(getattr(config, 'BOUNCE_REENTRY_K_RESET_SHORT_TRADIER', 65))
                                if side == "LONG" and not (k_5m < _kr_long_thr and k_5m > k_5m_prev):
                                    logger.info(f"[REENTRY_MONITOR] {pk}: LONG stoch gate FAIL k5m={k_5m:.0f} prev={k_5m_prev:.0f} (need <{_kr_long_thr:.0f} and rising, {hours_since:.1f}h)")
                                    continue
                                if side == "LONG" and (k_1h_rm > 80.0 or k_15m > 80.0):
                                    _rm_pp_up, _rm_pp_dn, _, _ = _parabolic_state(i, config)
                                    if _rm_pp_up:
                                        logger.warning(f"🌟 [REENTRY_MONITOR_PARABOLIC_BYPASS] {pk}: LONG HTF overbought BUT parabolic uptrend (rsi/bb confluence) — reentry OBLIGATORY")
                                    else:
                                        logger.info(f"[REENTRY_MONITOR] {pk}: LONG HTF overbought BLOCK k1h={k_1h_rm:.0f} k15m={k_15m:.0f} (need both ≤80)")
                                        continue
                                if side == "SHORT" and not (k_5m > _kr_short_thr and k_5m < k_5m_prev):
                                    logger.info(f"[REENTRY_MONITOR] {pk}: SHORT stoch gate FAIL k5m={k_5m:.0f} prev={k_5m_prev:.0f} (need >{_kr_short_thr:.0f} and falling, {hours_since:.1f}h)")
                                    continue
                                if side == "SHORT" and (k_1h_rm < 20.0 or k_15m < 20.0):
                                    _rm_pp_up, _rm_pp_dn, _, _ = _parabolic_state(i, config)
                                    if _rm_pp_dn:
                                        logger.warning(f"🌟 [REENTRY_MONITOR_PARABOLIC_BYPASS] {pk}: SHORT HTF oversold BUT parabolic downtrend — reentry OBLIGATORY")
                                    else:
                                        logger.info(f"[REENTRY_MONITOR] {pk}: SHORT HTF oversold BLOCK k1h={k_1h_rm:.0f} k15m={k_15m:.0f} (need both ≥20)")
                                        continue
                        elif not _p0_rescue and not _aggr_fired and not _fav_fired and not _g60_fired:
                            # ═══ SAFETY SWITCH 3: OVERDUE BYPASS GATE (2026-04-16) ═══
                            if not getattr(config, 'TRADIER_REENTRY_OVERDUE_BYPASS_ENABLED', True):
                                logger.info(f"[REENTRY_MONITOR] {pk}: {hours_since:.1f}h overdue BUT TRADIER_REENTRY_OVERDUE_BYPASS_ENABLED=False — stoch gate still applies")
                                continue
                            logger.warning(f"[REENTRY_MONITOR] {pk}: {hours_since:.1f}h overdue — stoch gate BYPASSED, relying on exit score + WT alignment")
                        # The exit signal has cleared. Now check that the TRADE-DIRECTION WT is aligned (or a fast-path fired)
                        if _p0_rescue or _aggr_fired or _fav_fired or _g60_fired:
                            wt_support = 3  # for logging
                            reenter = True
                        else:
                            if side == "LONG":
                                htf_aligned = (wt1_4h > wt2_4h) and (wt_cross_1h != "BEAR")
                                wt_support = sum(1 for w1, w2 in [(wt1_5m, wt2_5m), (wt1_15m, wt2_15m), (wt1_1h, wt2_1h)] if w1 > w2)
                            else:
                                htf_aligned = (wt1_4h < wt2_4h) and (wt_cross_1h != "BULL")
                                wt_support = sum(1 for w1, w2 in [(wt1_5m, wt2_5m), (wt1_15m, wt2_15m), (wt1_1h, wt2_1h)] if w1 < w2)
                            if wt_support >= 2 and htf_aligned:
                                reenter = True
                        if reenter:
                            pos_amt_at_exit = float(cand.get("position_amt_at_exit", 0))
                            reentry_qty = max(1, int(pos_amt_at_exit)) if pos_amt_at_exit > 0 else max(1, int(await self.calculate_position_size(symbol, current_price, account_key=acc)))
                            if _g60_fired or (_fav_fired and _fav_k15m_partial):
                                reentry_qty = max(1, int(reentry_qty * 0.5))
                            position_side = side
                            order_side = "BUY" if side == "LONG" else "SELL"
                            # 2026-04-27 — engine boost (default mult=1.0 = no size change, just +ENGINES tag)
                            _ee_mult, _ee_tag = _ee_reentry_boost(symbol, i, side == "LONG", config)
                            reentry_qty = max(1, int(reentry_qty * _ee_mult))
                            reentry_reason = f"REENTRY_MONITOR exit@{exit_price:.2f} now@{current_price:.2f} k5m={k_5m:.0f} wt={wt_support}" + _ee_tag
                            api_side = "buy" if side == "LONG" else "sell_short"
                            logger.warning(f"[REENTRY_MONITOR] {pk}: PLACING REAL ORDER {api_side} {reentry_qty} {symbol} @ market | k5m={k_5m:.0f} wt={wt_support}")
                            result = {}
                            try:
                                result = await self.place_order(symbol, api_side, float(reentry_qty), "market", duration="day", action="REENTRY", position_side=side, account_key=acc)
                                order_id = result.get("order", {}).get("id", "NONE") if isinstance(result, dict) else str(result)
                                status = result.get("order", {}).get("status", "UNKNOWN") if isinstance(result, dict) else "UNKNOWN"
                                logger.warning(f"[REENTRY_MONITOR] {pk}: ORDER RESULT id={order_id} status={status}")
                                cand["status"] = "REENTERED" if "GHOST" not in str(order_id) and order_id != "NONE" else f"FAILED:{order_id}"
                            except Exception as oe:
                                logger.error(f"[REENTRY_MONITOR] {pk}: ORDER FAILED: {oe}")
                                cand["status"] = f"FAILED:{oe}"
                            cand["reentry_price"] = current_price
                            cand["reentry_time"] = now_utc.isoformat()
                            # V4: Immediately record ACTUAL fill price as entry_price
                            # so NOLOSS/bounce-top can track gain from real cost basis
                            _fill_price = float(result.get("order", {}).get("avg_fill_price", 0)) if isinstance(result, dict) else 0
                            if _fill_price <= 0:
                                _fill_price = current_price
                            
                            _pos = self.get_position(pk)
                            if _pos:
                                _pos.entry_price = _fill_price
                                _pos.mark_price = current_price
                                _pos.opened_at = now_utc
                                logger.warning(f"[REENTRY_ENTRY_PRICE] {pk}: Set entry_price={_fill_price:.2f} from fill (was {cand.get('entry_price', 0):.2f})")
                            logger.info(f"[REENTRY_MONITOR] {pk}: Result={result}")
                        elif cand["attempts"] % 20 == 0:
                            logger.info(f"[REENTRY_MONITOR] {pk}: Still waiting ({cand['attempts']} checks) price=${current_price:.2f} target=${exit_price:.2f} k5m={k_5m:.0f} wt_sup={wt_support if side == 'LONG' else wt_support}")
                    if changed:
                        self._save_reentry_candidates(acc, candidates)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[REENTRY_MONITOR] Loop error: {e}", exc_info=True)
            await asyncio.sleep(15)

    async def should_enter_long(self, symbol: str, indicators: Dict[str, Any]) -> bool:
        """Configurable entry gate. SATOSHIT default; SHOULD_ENTER_FALLBACK_ENABLED allows other paths."""
        try:
            if getattr(config, 'ENTRY_SYMGATE_ENABLED', True):
                _sg_blocked, _sg_reason = self.would_exit_trigger_now(symbol, indicators, "LONG")
                if _sg_blocked:
                    if config.VERBOSE: logger.info(f"[SYMGATE] LONG {symbol} BLOCKED: {_sg_reason}")
                    return False
            # ═══ 2026-04-30 HTF PORT — F1. HTF_W_M_ALIGN_GATE (default OFF) ═══
            # Block LONG entries unless N of 2 (W, M) WaveTrend agree with side.
            if getattr(config, 'HTF_W_M_ALIGN_GATE_TRADIER_ENABLED', False):
                try:
                    _w1_W = float(indicators.get('wt1_W', 0) or 0)
                    _w2_W = float(indicators.get('wt2_W', 0) or 0)
                    _w1_M = float(indicators.get('wt1_M', 0) or 0)
                    _w2_M = float(indicators.get('wt2_M', 0) or 0)
                    _w_has = (_w1_W != 0 or _w2_W != 0)
                    _m_has = (_w1_M != 0 or _w2_M != 0)
                    _w_ok = (_w1_W > _w2_W) if _w_has else True   # pass through pre-cache bars
                    _m_ok = (_w1_M > _w2_M) if _m_has else True
                    _req = int(getattr(config, 'HTF_W_M_ALIGN_TRADIER_REQUIRED', 2))
                    _agree = int(_w_ok) + int(_m_ok)
                    if _agree < _req:
                        if config.VERBOSE: logger.info(f"[HTF_W_M_ALIGN_GATE] LONG {symbol} BLOCKED: agree={_agree}/{_req} W_ok={_w_ok}(has={_w_has}) M_ok={_m_ok}(has={_m_has})")
                        return False
                except Exception as _htfg:
                    logger.debug(f"[HTF_W_M_ALIGN_GATE][LONG] {symbol}: {type(_htfg).__name__} {_htfg} — skipped")
            # ═══ 2026-04-30 HTF PORT — F2. HTF_DC_BREAKOUT entry (default OFF, fast path) ═══
            # Additive entry: close > dc_high_TF (prev) * (1+thr) AND optional W WT bullish.
            if getattr(config, 'HTF_DC_BREAKOUT_TRADIER_ENABLED', False):
                try:
                    _bk_tf = str(getattr(config, 'HTF_DC_BREAKOUT_TRADIER_TF', '4h'))
                    _bk_thr = float(getattr(config, 'HTF_DC_BREAKOUT_TRADIER_THRESHOLD_PCT', 0.0)) / 100.0
                    _bk_req_w = bool(getattr(config, 'HTF_DC_BREAKOUT_TRADIER_REQUIRE_W_WT', True))
                    _dch_prev = float(indicators.get(f'dc_high_{_bk_tf}_prev', indicators.get(f'dc_high_{_bk_tf}', 0)) or 0)
                    _cur_p = float(indicators.get('current_price', 0) or 0)
                    if _dch_prev > 0 and _cur_p > _dch_prev * (1.0 + _bk_thr):
                        _w_ok_bk = True
                        if _bk_req_w:
                            _w1_W_bk = float(indicators.get('wt1_W', 0) or 0)
                            _w2_W_bk = float(indicators.get('wt2_W', 0) or 0)
                            if (_w1_W_bk != 0 or _w2_W_bk != 0):
                                _w_ok_bk = _w1_W_bk > _w2_W_bk
                        if _w_ok_bk:
                            logger.warning(f"[HTF_DC_BREAKOUT_LONG] {symbol}: close={_cur_p:.4f} > dc_high_{_bk_tf}_prev={_dch_prev:.4f}*(1+{_bk_thr:.4f}) W_ok={_w_ok_bk}")
                            return True
                except Exception as _htfb:
                    logger.debug(f"[HTF_DC_BREAKOUT][LONG] {symbol}: {type(_htfb).__name__} {_htfb} — skipped")
            # SATOSHIT — fast path (opt-in via SATOSHIT_ENTRY_FILTER, default True for backward compat)
            if getattr(config, 'SATOSHIT_ENTRY_FILTER', True):
                from ez_satoshit import satoshit_entry_signal
                _sat_ok, _sat_votes, _sat_reason = satoshit_entry_signal(indicators, True, config)
                if _sat_ok:
                    logger.warning(f"[SATOSHIT_ENTRY] LONG {symbol}: {_sat_reason}")
                    return True
            # STDEV_BREAKOUT: stateless BB %B breakout entry — works in backtest + live without state machine
            if getattr(config, 'STDEV_BREAKOUT_ENABLED', False):
                _sb_htf_list = list(getattr(config, 'STDEV_BREAKOUT_HTF_LIST', None) or ['D', '4h'])
                _sb_pctb_long = float(getattr(config, 'STDEV_BREAKOUT_PCTB_LONG', 1.0))
                _sb_rvol_min = float(getattr(config, 'STDEV_BREAKOUT_RVOL_MIN', 1.2))
                for _sb_htf in _sb_htf_list:
                    _sb_pctb_val = float(indicators.get(f'bb_pct_b_{_sb_htf}', 0.5) or 0.5)
                    _sb_rvol_val = float(indicators.get(f'relative_volume_{_sb_htf}', 1.0) or 1.0)
                    if _sb_pctb_val >= _sb_pctb_long and _sb_rvol_val >= _sb_rvol_min:
                        logger.warning(f"[STDEV_BREAKOUT_LONG] {symbol}: bb_pct_b_{_sb_htf}={_sb_pctb_val:.3f} rvol={_sb_rvol_val:.2f}")
                        return True
            # STDEV_BOUNCE: stateless mean-reversion entry at lower BB band (LONG when pctb ≤ threshold)
            if getattr(config, 'STDEV_BOUNCE_ENABLED', False):
                _bn_htf_list = list(getattr(config, 'STDEV_BOUNCE_HTF_LIST', None) or ['D', '4h'])
                _bn_pctb_long = float(getattr(config, 'STDEV_BOUNCE_PCTB_LONG', 0.05))
                _bn_rvol_min = float(getattr(config, 'STDEV_BOUNCE_RVOL_MIN', 1.2))
                for _bn_htf in _bn_htf_list:
                    _bn_pctb_val = float(indicators.get(f'bb_pct_b_{_bn_htf}', 0.5) or 0.5)
                    _bn_rvol_val = float(indicators.get(f'relative_volume_{_bn_htf}', 1.0) or 1.0)
                    if _bn_pctb_val <= _bn_pctb_long and _bn_rvol_val >= _bn_rvol_min:
                        logger.warning(f"[STDEV_BOUNCE_LONG] {symbol}: bb_pct_b_{_bn_htf}={_bn_pctb_val:.3f} rvol={_bn_rvol_val:.2f}")
                        return True
            # If SHOULD_ENTER_FALLBACK_ENABLED is False (default), block here. Set True to test other paths.
            if not getattr(config, 'SHOULD_ENTER_FALLBACK_ENABLED', False):
                return False
            # YOUTUBE_CONSENSUS: Lunch dead zone — block momentum entries 11:30-14:00 ET
            if getattr(config, 'LUNCH_DEADZONE_ENABLED', False) and is_lunch_dead_zone():
                _ldz_mode = getattr(config, 'LUNCH_DEADZONE_MODE', 'BLOCK_MOMENTUM')
                if _ldz_mode == "BLOCK_ALL":
                    if config.VERBOSE: logger.info(f"[LUNCH_DEADZONE] LONG {symbol} BLOCKED: lunch dead zone (BLOCK_ALL)")
                    return False
                elif _ldz_mode == "BLOCK_MOMENTUM":
                    pass  # Allow through — mean-reversion strategies bypass via SATOSHIT fast path above
            # YOUTUBE_CONSENSUS: VWAP filter — LONG only above VWAP
            if getattr(config, 'VWAP_FILTER_ENABLED', False):
                _vwap = float(indicators.get('vwap', 0) or 0)
                _cur_p_v = float(indicators.get('current_price', 0) or 0)
                if _vwap > 0 and _cur_p_v > 0 and _cur_p_v < _vwap:
                    if config.VERBOSE: logger.info(f"[VWAP_FILTER] LONG {symbol} BLOCKED: price={_cur_p_v:.2f} < VWAP={_vwap:.2f}")
                    return False
            # YOUTUBE_CONSENSUS: 9/21 EMA alignment confirmation
            if getattr(config, 'EMA_9_21_FILTER_ENABLED', False):
                _ema9_above = float(indicators.get(f'ema_9_above_21_{getattr(config, "EMA_9_21_TIMEFRAME", "5m")}', -1) or -1)
                if _ema9_above == 0.0:
                    if config.VERBOSE: logger.info(f"[EMA_9_21] LONG {symbol} BLOCKED: 9 EMA < 21 EMA on {getattr(config, 'EMA_9_21_TIMEFRAME', '5m')}")
                    return False
            # YOUTUBE_CONSENSUS: RVOL gate for non-mean-reversion strategies
            _rvol_5m = float(indicators.get('rel_vol_5m', 1.0) or 1.0)
            _rvol_min = getattr(config, 'RVOL_MOMENTUM_MIN', 1.5)
            if _rvol_5m < _rvol_min and _rvol_5m > 0:
                if config.VERBOSE: logger.info(f"[RVOL_GATE] LONG {symbol} BLOCKED: RVOL_5m={_rvol_5m:.2f} < {_rvol_min}")
                return False
            # BACKTEST_CHANGE_T55: RSI entry gate — ONLY if RSI data actually exists
            _rsi_period = getattr(config, 'RSI_ENTRY_PERIOD_TRADIER', 10)
            _rsi_key = f'rsi_{_rsi_period}_D'
            _rsi_val = indicators.get(_rsi_key, indicators.get('rsi_D', indicators.get('rsi_1h', None)))
            if _rsi_val is not None:
                _rsi_val = float(_rsi_val or 50)
                _rsi_thresh = getattr(config, 'RSI_ENTRY_LONG_TRADIER', 42.0)
                if _rsi_val > _rsi_thresh:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol} BLOCKED: RSI({_rsi_period})={_rsi_val:.1f} > {_rsi_thresh}")
                    return False
            # SMA filter — ONLY if SMA data exists
            _sma_key = f'sma_{getattr(config, "SMA_FILTER_PERIOD_TRADIER", 100)}_D'
            _sma_val = indicators.get(_sma_key, indicators.get('sma_200_D', indicators.get('sma_200_1h', None)))
            _cur_p = float(indicators.get('current_price', 0) or 0)
            if _sma_val is not None and _cur_p > 0:
                _sma_val = float(_sma_val or 0)
                if _sma_val > 0 and _cur_p < _sma_val:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol} BLOCKED: price < SMA")
                    return False
            final_score = float(indicators.get('0final_score_norm', 0))
            ranking_points = float(indicators.get('0ranking_points', 0))
            lr_trend_15m = float(indicators.get('lr_trend_15m', 0))
            k_1h = float(indicators.get('stoch_k_1h', 50))
            d_1h = float(indicators.get('stoch_d_1h', 50))
            k_4h = float(indicators.get('stoch_k_4h', 50))
            d_4h = float(indicators.get('stoch_d_4h', 50))
            if config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][LONG] {symbol} ENTRY CALCULATION START: final_score={final_score:.3f} ranking_points={ranking_points:.1f} lr_trend_15m={lr_trend_15m:.3f} k_1h={k_1h:.1f} d_1h={d_1h:.1f} k_4h={k_4h:.1f} d_4h={d_4h:.1f}")
            # BACKTEST_CHANGE_T19: time zone stoch threshold gate
            if getattr(config, 'TIME_ZONE_ENABLED', False):
                _zone = _get_trade_zone()
                if _zone == "open":
                    _zt = getattr(config, 'ZONE_OPEN_THRESHOLD', 25)
                elif _zone == "mid":
                    _zt = getattr(config, 'ZONE_MID_THRESHOLD', 30)
                elif _zone == "close":
                    _zt = getattr(config, 'ZONE_CLOSE_THRESHOLD', 20)
                else:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol} BLOCKED: market closed zone")
                    return False
                if k_1h > (100.0 - _zt):
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol} BLOCKED: zone={_zone} k_1h={k_1h:.1f} > threshold {100.0 - _zt:.0f}")
                    return False
            # BACKTEST_CHANGE_T3: SMA200 distance check on 4h
            # 2026-04-27: when LH_HL_FILTER_REPLACE_SMA200D is True, this filter is bypassed (LH/HL filter replaces it).
            _lh_replace = bool(getattr(config, 'LH_HL_FILTER_ENABLED', False)) and bool(getattr(config, 'LH_HL_FILTER_REPLACE_SMA200D', False))
            if getattr(config, 'SMA200_DIST_ENTRY_ENABLED', False) and not _lh_replace:
                _sma200_4h = float(indicators.get('sma_200_4h', 0) or 0)
                _cur_price = float(indicators.get('current_price', 0) or 0)
                if _sma200_4h > 0 and _cur_price > 0:
                    _dist_pct = ((_cur_price - _sma200_4h) / _sma200_4h) * 100.0
                    if _dist_pct < getattr(config, 'SMA200_DIST_LONG_THRESHOLD_4H', -10.0):
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol} BLOCKED: SMA200_4h dist={_dist_pct:.1f}% < threshold {config.SMA200_DIST_LONG_THRESHOLD_4H}")
                        return False
            # 2026-04-27 LOWER-HIGHS / HIGHER-LOWS FILTER (LONG side) — sweep-testable, default OFF.
            # User: "block long trades while 1h/4h charts make lower highs (shorts vv) instead of the sma_200_D filter (or on top of it)".
            # User 2026-04-27 23:13: "test lh with or without ll for longs hl with or without hh for shorts on both platforms".
            if bool(getattr(config, 'LH_HL_FILTER_ENABLED', False)):
                _lh_apply = True
                # Per-action gating: tradier should_enter_long only fires for OPEN-class actions, no AUGMENT distinction here.
                if _lh_apply:
                    try:
                        _lh_mode = str(getattr(config, 'LH_HL_FILTER_MODE', 'STRICT_2BAR'))
                        _lh_tf_req = int(getattr(config, 'LH_HL_FILTER_TF_REQ', 2))
                        _lh_dc_th = float(getattr(config, 'LH_HL_FILTER_DC_THRESHOLD_PCT', 0.5)) / 100.0
                        _lh_req_both = bool(getattr(config, 'LH_HL_FILTER_REQUIRE_BOTH', False))
                        _lh_h1h = float(indicators.get('high_1h', 0) or 0); _lh_h1hp = float(indicators.get('high_1h_prev', 0) or 0)
                        _lh_h4h = float(indicators.get('high_4h', 0) or 0); _lh_h4hp = float(indicators.get('high_4h_prev', 0) or 0)
                        _lh_l1h = float(indicators.get('low_1h', 0) or 0); _lh_l1hp = float(indicators.get('low_1h_prev', 0) or 0)
                        _lh_l4h = float(indicators.get('low_4h', 0) or 0); _lh_l4hp = float(indicators.get('low_4h_prev', 0) or 0)
                        if _lh_mode == "DC_REGRESS":
                            _lh_dch1h = float(indicators.get('dc_high_1h', 0) or 0)
                            _lh_dch4h = float(indicators.get('dc_high_4h', 0) or 0)
                            _lh_1h = (_lh_h1h > 0 and _lh_dch1h > 0 and _lh_h1h < _lh_dch1h * (1.0 - _lh_dc_th))
                            _lh_4h = (_lh_h4h > 0 and _lh_dch4h > 0 and _lh_h4h < _lh_dch4h * (1.0 - _lh_dc_th))
                        else:
                            _lh_1h = (_lh_h1h > 0 and _lh_h1hp > 0 and _lh_h1h < _lh_h1hp)
                            _lh_4h = (_lh_h4h > 0 and _lh_h4hp > 0 and _lh_h4h < _lh_h4hp)
                        _ll_1h = (_lh_l1h > 0 and _lh_l1hp > 0 and _lh_l1h < _lh_l1hp)
                        _ll_4h = (_lh_l4h > 0 and _lh_l4hp > 0 and _lh_l4h < _lh_l4hp)
                        _lh_count = int(_lh_1h) + int(_lh_4h)
                        _ll_count = int(_ll_1h) + int(_ll_4h)
                        _primary_hit = _lh_count >= _lh_tf_req
                        _confirm_hit = (not _lh_req_both) or (_ll_count >= _lh_tf_req)
                        if _primary_hit and _confirm_hit:
                            _tag = "LH+LL" if _lh_req_both else "LH"
                            logger.info(f"📉 [LH_HL_FILTER] {symbol} BLOCKED LONG — {_tag} on {_lh_count}/2 TFs (1h LH={_lh_1h} LL={_ll_1h} | 4h LH={_lh_4h} LL={_ll_4h}) mode={_lh_mode} req={_lh_tf_req}TF reqBoth={_lh_req_both}")
                            return False
                    except Exception as _lh_exc:
                        logger.debug(f"[LH_HL_FILTER][LONG] {symbol}: {type(_lh_exc).__name__} {_lh_exc} — skipped")
            # BACKTEST_CHANGE_T4: MFI check on Daily — OVERBOUGHT FILTER (fixed 2026-05-18)
            # Blocks LONG entries when mfi_D > threshold (default 80 = overbought zone, reversal expected).
            # Prior semantics ("only LONG if mfi<20 oversold") blocked ~95% of opportunities — see config note.
            if getattr(config, 'MFI_ENTRY_ENABLED', False):
                _mfi_d = float(indicators.get('mfi_D', 50) or 50)
                if _mfi_d > getattr(config, 'MFI_LONG_THRESHOLD_D', 80.0):
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol} BLOCKED: MFI_D={_mfi_d:.1f} > {config.MFI_LONG_THRESHOLD_D} (overbought)")
                    return False
            # BACKTEST_CHANGE_T8: alignment gate
            if getattr(config, 'ALIGNMENT_GATE_MIN', 0) > 0:
                _al_count = 0
                if k_1h > d_1h: _al_count += 1
                if k_4h > d_4h: _al_count += 1
                if float(indicators.get('stoch_k_15m', 50)) > float(indicators.get('stoch_d_15m', 50)): _al_count += 1
                if lr_trend_15m > 0: _al_count += 1
                _al_total_max = getattr(config, 'ALIGNMENT_GATE_TOTAL', 12)
                if _al_count < getattr(config, 'ALIGNMENT_GATE_MIN', 4):
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol} BLOCKED: alignment={_al_count}/{_al_total_max} < {config.ALIGNMENT_GATE_MIN}")
                    return False
            # Leaderboard filter check
            if config.LEADERBOARD_FILTER:
                rankings = await self.get_rankings()
                if rankings:
                    top_symbols = rankings.get('top_20', [])
                    in_top_20 = symbol in top_symbols
                    if config.VERBOSE:
                        logger.info(f"[VERBOSE][CALC][LONG] {symbol} LEADERBOARD_FILTER: enabled={config.LEADERBOARD_FILTER} in_top_20={in_top_20}")
                    if not in_top_20:
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol}  BLOCKED: not in top_20 leaderboard")
                        return False
                elif config.VERBOSE:
                    logger.info(f"[VERBOSE][CALC][LONG] {symbol} LEADERBOARD_FILTER: enabled but no rankings data available")
            elif config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][LONG] {symbol} LEADERBOARD_FILTER: disabled (skipping)")
            # Trend gates check
            if config.TREND_GATES:
                trend_ok = lr_trend_15m > 0
                if config.VERBOSE:
                    logger.info(f"[VERBOSE][CALC][LONG] {symbol} TREND_GATES: enabled={config.TREND_GATES} lr_trend_15m={lr_trend_15m:.3f} trend_ok={trend_ok}")
                if not trend_ok:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol}  BLOCKED: lr_trend_15m={lr_trend_15m:.3f} <= 0")
                    return False
            elif config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][LONG] {symbol} TREND_GATES: disabled (skipping)")
            # HTF confirmation checks
            if config.HTF1_CONF or config.HTF4_CONF:
                htf1_ok = True
                htf4_ok = True
                if config.HTF1_CONF:
                    htf1_ok = k_1h > d_1h
                    if config.VERBOSE:
                        logger.info(f"[VERBOSE][CALC][LONG] {symbol} HTF1_CONF: enabled={config.HTF1_CONF} k_1h={k_1h:.1f} d_1h={d_1h:.1f} htf1_ok={htf1_ok}")
                    if not htf1_ok:
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol}  BLOCKED: HTF1_CONF k_1h={k_1h:.1f} <= d_1h={d_1h:.1f}")
                        return False
                if config.HTF4_CONF:
                    htf4_ok = k_4h > d_4h
                    if config.VERBOSE:
                        logger.info(f"[VERBOSE][CALC][LONG] {symbol} HTF4_CONF: enabled={config.HTF4_CONF} k_4h={k_4h:.1f} d_4h={d_4h:.1f} htf4_ok={htf4_ok}")
                    if not htf4_ok:
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][LONG] {symbol}  BLOCKED: HTF4_CONF k_4h={k_4h:.1f} <= d_4h={d_4h:.1f}")
                        return False
            elif config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][LONG] {symbol} HTF_CONF: disabled (skipping)")
            # Final score check
            score_threshold = 0.6
            ranking_threshold = 50
            score_ok = True  # BACKTEST_CHANGE_T42: near-zero signal in 0xxx sweep
            ranking_ok = True  # BACKTEST_CHANGE_T41: near-zero signal in 0xxx sweep
            final_result = score_ok and ranking_ok
            if config.VERBOSE:
                logger.info(f"\n[DECISION SCORECARD] {symbol} LONG EVALUATION")
                logger.info(f"{'Metric':<15} | {'Value':<10} | {'Threshold':<10} | {'Result'}")
                logger.info("-" * 55)
                logger.info(f"{'Leaderboard':<15} | {str(in_top_20):<10} | {'True':<10} | {'✅' if in_top_20 else '!!'}")
                logger.info(f"{'Trend (15m)':<15} | {lr_trend_15m:<10.3f} | {'> 0':<10} | {'✅' if trend_ok else '!!'}")
                logger.info(f"{'HTF1 (1h)':<15} | {k_1h:.1f}/{d_1h:.1f}  | {'K > D':<10} | {'✅' if htf1_ok else '!!'}")
                logger.info(f"{'HTF4 (4h)':<15} | {k_4h:.1f}/{d_4h:.1f}  | {'K > D':<10} | {'✅' if htf4_ok else '!!'}")
                logger.info(f"{'Final Score':<15} | {final_score:<10.3f} | {'> '+str(score_threshold):<10} | {'✅' if final_score>score_threshold else '!!'}")
                logger.info(f"{'Rank Points':<15} | {ranking_points:<10.1f} | {'BYPASSED':<10} | {'✅ (T41)'}")
                logger.info("-" * 55)
                logger.info(f"FINAL DECISION: {'✅ ENTER LONG' if final_result else '⛔ BLOCKED'}\n")
                logger.info(f"[VERBOSE][CALC][LONG] {symbol} FINAL SCORE CHECK: final_score={final_score:.3f} > {score_threshold} = {score_ok} | ranking_points=BYPASSED(T41) | RESULT={final_result}")
            return final_result
        except Exception as e:
            logger.error(f"Error in should_enter_long for {symbol}: {e}",  exc_info=True )
            return False

    async def should_enter_short(self, symbol: str, indicators: Dict[str, Any]) -> bool:
        """Configurable entry gate. SATOSHIT default; SHOULD_ENTER_FALLBACK_ENABLED allows other paths."""
        try:
            if getattr(config, 'ENTRY_SYMGATE_ENABLED', True):
                _sg_blocked, _sg_reason = self.would_exit_trigger_now(symbol, indicators, "SHORT")
                if _sg_blocked:
                    if config.VERBOSE: logger.info(f"[SYMGATE] SHORT {symbol} BLOCKED: {_sg_reason}")
                    return False
            # ═══ 2026-04-30 HTF PORT — F1. HTF_W_M_ALIGN_GATE SHORT (default OFF) ═══
            if getattr(config, 'HTF_W_M_ALIGN_GATE_TRADIER_ENABLED', False):
                try:
                    _w1_W = float(indicators.get('wt1_W', 0) or 0)
                    _w2_W = float(indicators.get('wt2_W', 0) or 0)
                    _w1_M = float(indicators.get('wt1_M', 0) or 0)
                    _w2_M = float(indicators.get('wt2_M', 0) or 0)
                    _w_has = (_w1_W != 0 or _w2_W != 0)
                    _m_has = (_w1_M != 0 or _w2_M != 0)
                    _w_ok = (_w1_W < _w2_W) if _w_has else True   # pass through pre-cache bars
                    _m_ok = (_w1_M < _w2_M) if _m_has else True
                    _req = int(getattr(config, 'HTF_W_M_ALIGN_TRADIER_REQUIRED', 2))
                    _agree = int(_w_ok) + int(_m_ok)
                    if _agree < _req:
                        if config.VERBOSE: logger.info(f"[HTF_W_M_ALIGN_GATE] SHORT {symbol} BLOCKED: agree={_agree}/{_req} W_ok={_w_ok}(has={_w_has}) M_ok={_m_ok}(has={_m_has})")
                        return False
                except Exception as _htfg:
                    logger.debug(f"[HTF_W_M_ALIGN_GATE][SHORT] {symbol}: {type(_htfg).__name__} {_htfg} — skipped")
            # ═══ 2026-04-30 HTF PORT — F2. HTF_DC_BREAKDOWN entry SHORT (default OFF) ═══
            if getattr(config, 'HTF_DC_BREAKOUT_TRADIER_ENABLED', False):
                try:
                    _bk_tf = str(getattr(config, 'HTF_DC_BREAKOUT_TRADIER_TF', '4h'))
                    _bk_thr = float(getattr(config, 'HTF_DC_BREAKOUT_TRADIER_THRESHOLD_PCT', 0.0)) / 100.0
                    _bk_req_w = bool(getattr(config, 'HTF_DC_BREAKOUT_TRADIER_REQUIRE_W_WT', True))
                    _dcl_prev = float(indicators.get(f'dc_low_{_bk_tf}_prev', indicators.get(f'dc_low_{_bk_tf}', 0)) or 0)
                    _cur_p = float(indicators.get('current_price', 0) or 0)
                    if _dcl_prev > 0 and _cur_p > 0 and _cur_p < _dcl_prev * (1.0 - _bk_thr):
                        _w_ok_bk = True
                        if _bk_req_w:
                            _w1_W_bk = float(indicators.get('wt1_W', 0) or 0)
                            _w2_W_bk = float(indicators.get('wt2_W', 0) or 0)
                            if (_w1_W_bk != 0 or _w2_W_bk != 0):
                                _w_ok_bk = _w1_W_bk < _w2_W_bk
                        if _w_ok_bk:
                            logger.warning(f"[HTF_DC_BREAKOUT_SHORT] {symbol}: close={_cur_p:.4f} < dc_low_{_bk_tf}_prev={_dcl_prev:.4f}*(1-{_bk_thr:.4f}) W_ok={_w_ok_bk}")
                            return True
                except Exception as _htfb:
                    logger.debug(f"[HTF_DC_BREAKOUT][SHORT] {symbol}: {type(_htfb).__name__} {_htfb} — skipped")
            if getattr(config, 'SATOSHIT_ENTRY_FILTER', True):
                from ez_satoshit import satoshit_entry_signal
                _sat_ok, _sat_votes, _sat_reason = satoshit_entry_signal(indicators, False, config)
                if _sat_ok:
                    logger.warning(f"[SATOSHIT_ENTRY] SHORT {symbol}: {_sat_reason}")
                    return True
            # STDEV_BREAKOUT: stateless BB %B breakdown entry for SHORT
            if getattr(config, 'STDEV_BREAKOUT_ENABLED', False):
                _sb_htf_list_s = list(getattr(config, 'STDEV_BREAKOUT_HTF_LIST', None) or ['D', '4h'])
                _sb_pctb_short = float(getattr(config, 'STDEV_BREAKOUT_PCTB_SHORT', 0.0))
                _sb_rvol_min_s = float(getattr(config, 'STDEV_BREAKOUT_RVOL_MIN', 1.2))
                for _sb_htf_s in _sb_htf_list_s:
                    _sb_pctb_val_s = float(indicators.get(f'bb_pct_b_{_sb_htf_s}', 0.5) or 0.5)
                    _sb_rvol_val_s = float(indicators.get(f'relative_volume_{_sb_htf_s}', 1.0) or 1.0)
                    if _sb_pctb_val_s <= _sb_pctb_short and _sb_rvol_val_s >= _sb_rvol_min_s:
                        logger.warning(f"[STDEV_BREAKOUT_SHORT] {symbol}: bb_pct_b_{_sb_htf_s}={_sb_pctb_val_s:.3f} rvol={_sb_rvol_val_s:.2f}")
                        return True
            # STDEV_BOUNCE: stateless mean-reversion SHORT entry at upper BB band (pctb ≥ threshold)
            if getattr(config, 'STDEV_BOUNCE_ENABLED', False):
                _bn_htf_list_s = list(getattr(config, 'STDEV_BOUNCE_HTF_LIST', None) or ['D', '4h'])
                _bn_pctb_short = float(getattr(config, 'STDEV_BOUNCE_PCTB_SHORT', 0.95))
                _bn_rvol_min_s = float(getattr(config, 'STDEV_BOUNCE_RVOL_MIN', 1.2))
                for _bn_htf_s in _bn_htf_list_s:
                    _bn_pctb_val_s = float(indicators.get(f'bb_pct_b_{_bn_htf_s}', 0.5) or 0.5)
                    _bn_rvol_val_s = float(indicators.get(f'relative_volume_{_bn_htf_s}', 1.0) or 1.0)
                    if _bn_pctb_val_s >= _bn_pctb_short and _bn_rvol_val_s >= _bn_rvol_min_s:
                        logger.warning(f"[STDEV_BOUNCE_SHORT] {symbol}: bb_pct_b_{_bn_htf_s}={_bn_pctb_val_s:.3f} rvol={_bn_rvol_val_s:.2f}")
                        return True
            if not getattr(config, 'SHOULD_ENTER_FALLBACK_ENABLED', False):
                return False
            # YOUTUBE_CONSENSUS: Lunch dead zone — block momentum entries 11:30-14:00 ET
            if getattr(config, 'LUNCH_DEADZONE_ENABLED', False) and is_lunch_dead_zone():
                _ldz_mode = getattr(config, 'LUNCH_DEADZONE_MODE', 'BLOCK_MOMENTUM')
                if _ldz_mode == "BLOCK_ALL":
                    if config.VERBOSE: logger.info(f"[LUNCH_DEADZONE] SHORT {symbol} BLOCKED: lunch dead zone (BLOCK_ALL)")
                    return False
                elif _ldz_mode == "BLOCK_MOMENTUM":
                    pass  # Allow through — mean-reversion strategies bypass via SATOSHIT fast path above
            # YOUTUBE_CONSENSUS: VWAP filter — SHORT only below VWAP
            if getattr(config, 'VWAP_FILTER_ENABLED', False):
                _vwap = float(indicators.get('vwap', 0) or 0)
                _cur_p_v = float(indicators.get('current_price', 0) or 0)
                if _vwap > 0 and _cur_p_v > 0 and _cur_p_v > _vwap:
                    if config.VERBOSE: logger.info(f"[VWAP_FILTER] SHORT {symbol} BLOCKED: price={_cur_p_v:.2f} > VWAP={_vwap:.2f}")
                    return False
            # YOUTUBE_CONSENSUS: 9/21 EMA alignment confirmation
            if getattr(config, 'EMA_9_21_FILTER_ENABLED', False):
                _ema9_above = float(indicators.get(f'ema_9_above_21_{getattr(config, "EMA_9_21_TIMEFRAME", "5m")}', -1) or -1)
                if _ema9_above == 1.0:
                    if config.VERBOSE: logger.info(f"[EMA_9_21] SHORT {symbol} BLOCKED: 9 EMA > 21 EMA on {getattr(config, 'EMA_9_21_TIMEFRAME', '5m')}")
                    return False
            # YOUTUBE_CONSENSUS: RVOL gate for non-mean-reversion strategies
            _rvol_5m = float(indicators.get('rel_vol_5m', 1.0) or 1.0)
            _rvol_min = getattr(config, 'RVOL_MOMENTUM_MIN', 1.5)
            if _rvol_5m < _rvol_min and _rvol_5m > 0:
                if config.VERBOSE: logger.info(f"[RVOL_GATE] SHORT {symbol} BLOCKED: RVOL_5m={_rvol_5m:.2f} < {_rvol_min}")
                return False
            # BACKTEST_CHANGE_T55: RSI entry gate — ONLY if RSI data exists
            _rsi_period = getattr(config, 'RSI_ENTRY_PERIOD_TRADIER', 10)
            _rsi_key = f'rsi_{_rsi_period}_D'
            _rsi_val = indicators.get(_rsi_key, indicators.get('rsi_D', indicators.get('rsi_1h', None)))
            if _rsi_val is not None:
                _rsi_val = float(_rsi_val or 50)
                _rsi_thresh = getattr(config, 'RSI_ENTRY_SHORT_TRADIER', 58.0)
                if _rsi_val < _rsi_thresh:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: RSI({_rsi_period})={_rsi_val:.1f} < {_rsi_thresh}")
                    return False
            # SMA filter — ONLY if SMA data exists
            _sma_key = f'sma_{getattr(config, "SMA_FILTER_PERIOD_TRADIER", 100)}_D'
            _sma_val = indicators.get(_sma_key, indicators.get('sma_200_D', indicators.get('sma_200_1h', None)))
            _cur_p = float(indicators.get('current_price', 0) or 0)
            if _sma_val is not None and _cur_p > 0:
                _sma_val = float(_sma_val or 0)
                if _sma_val > 0 and _cur_p > _sma_val:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: price > SMA")
                    return False
            final_score = indicators.get('0final_score_norm', 0)
            ranking_points = indicators.get('0ranking_points', 0)
            lr_trend_15m = indicators.get('lr_trend_15m', 0)
            k_1h = indicators.get('stoch_k_1h', 50)
            d_1h = indicators.get('stoch_d_1h', 50)
            k_4h = indicators.get('stoch_k_4h', 50)
            d_4h = indicators.get('stoch_d_4h', 50)
            if config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][SHORT] {symbol} ENTRY CALCULATION START: final_score={final_score:.3f} ranking_points={ranking_points:.1f} lr_trend_15m={lr_trend_15m:.3f} k_1h={k_1h:.1f} d_1h={d_1h:.1f} k_4h={k_4h:.1f} d_4h={d_4h:.1f}")
            # BACKTEST_CHANGE_T19: time zone stoch threshold gate
            if getattr(config, 'TIME_ZONE_ENABLED', False):
                _zone = _get_trade_zone()
                if _zone == "open":
                    _zt = getattr(config, 'ZONE_OPEN_THRESHOLD', 25)
                elif _zone == "mid":
                    _zt = getattr(config, 'ZONE_MID_THRESHOLD', 30)
                elif _zone == "close":
                    _zt = getattr(config, 'ZONE_CLOSE_THRESHOLD', 20)
                else:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: market closed zone")
                    return False
                if k_1h < _zt:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: zone={_zone} k_1h={k_1h:.1f} < threshold {_zt:.0f}")
                    return False
                # BACKTEST_CHANGE_T24: mid zone requires WT crossunder on 15m for shorts
                if _zone == "mid" and getattr(config, 'WT_CROSSUNDER_15M_SHORT', False):
                    _wt1_15m = float(indicators.get('wt1_15m', 0) or 0)
                    _wt2_15m = float(indicators.get('wt2_15m', 0) or 0)
                    if _wt1_15m >= _wt2_15m:
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: mid zone requires wt_crossunder_15m (wt1={_wt1_15m:.1f} >= wt2={_wt2_15m:.1f})")
                        return False
            # 2026-04-27 LOWER-HIGHS / HIGHER-LOWS FILTER (SHORT side) — sweep-testable, default OFF.
            # User: "block long trades while 1h/4h charts make lower highs (shorts vv)".
            # User 2026-04-27 23:13: "test lh with or without ll for longs hl with or without hh for shorts on both platforms".
            if bool(getattr(config, 'LH_HL_FILTER_ENABLED', False)):
                try:
                    _lh_mode = str(getattr(config, 'LH_HL_FILTER_MODE', 'STRICT_2BAR'))
                    _lh_tf_req = int(getattr(config, 'LH_HL_FILTER_TF_REQ', 2))
                    _lh_dc_th = float(getattr(config, 'LH_HL_FILTER_DC_THRESHOLD_PCT', 0.5)) / 100.0
                    _lh_req_both = bool(getattr(config, 'LH_HL_FILTER_REQUIRE_BOTH', False))
                    _lh_h1h = float(indicators.get('high_1h', 0) or 0); _lh_h1hp = float(indicators.get('high_1h_prev', 0) or 0)
                    _lh_h4h = float(indicators.get('high_4h', 0) or 0); _lh_h4hp = float(indicators.get('high_4h_prev', 0) or 0)
                    _lh_l1h = float(indicators.get('low_1h', 0) or 0); _lh_l1hp = float(indicators.get('low_1h_prev', 0) or 0)
                    _lh_l4h = float(indicators.get('low_4h', 0) or 0); _lh_l4hp = float(indicators.get('low_4h_prev', 0) or 0)
                    if _lh_mode == "DC_REGRESS":
                        _lh_dcl1h = float(indicators.get('dc_low_1h', 0) or 0)
                        _lh_dcl4h = float(indicators.get('dc_low_4h', 0) or 0)
                        _hl_1h = (_lh_l1h > 0 and _lh_dcl1h > 0 and _lh_l1h > _lh_dcl1h * (1.0 + _lh_dc_th))
                        _hl_4h = (_lh_l4h > 0 and _lh_dcl4h > 0 and _lh_l4h > _lh_dcl4h * (1.0 + _lh_dc_th))
                    else:
                        _hl_1h = (_lh_l1h > 0 and _lh_l1hp > 0 and _lh_l1h > _lh_l1hp)
                        _hl_4h = (_lh_l4h > 0 and _lh_l4hp > 0 and _lh_l4h > _lh_l4hp)
                    _hh_1h = (_lh_h1h > 0 and _lh_h1hp > 0 and _lh_h1h > _lh_h1hp)
                    _hh_4h = (_lh_h4h > 0 and _lh_h4hp > 0 and _lh_h4h > _lh_h4hp)
                    _hl_count = int(_hl_1h) + int(_hl_4h)
                    _hh_count = int(_hh_1h) + int(_hh_4h)
                    _primary_hit = _hl_count >= _lh_tf_req
                    _confirm_hit = (not _lh_req_both) or (_hh_count >= _lh_tf_req)
                    if _primary_hit and _confirm_hit:
                        _tag = "HL+HH" if _lh_req_both else "HL"
                        logger.info(f"📈 [LH_HL_FILTER] {symbol} BLOCKED SHORT — {_tag} on {_hl_count}/2 TFs (1h HL={_hl_1h} HH={_hh_1h} | 4h HL={_hl_4h} HH={_hh_4h}) mode={_lh_mode} req={_lh_tf_req}TF reqBoth={_lh_req_both}")
                        return False
                except Exception as _lh_exc:
                    logger.debug(f"[LH_HL_FILTER][SHORT] {symbol}: {type(_lh_exc).__name__} {_lh_exc} — skipped")
            # BACKTEST_CHANGE_T9: conviction threshold for shorts
            if getattr(config, 'CONVICTION_SHORT_THRESHOLD', 0) > 0:
                _conviction = float(indicators.get('0conviction', 0) or 0)
                if _conviction < getattr(config, 'CONVICTION_SHORT_THRESHOLD', 20):
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: conviction={_conviction:.0f} < {config.CONVICTION_SHORT_THRESHOLD}")
                    return False
            # BACKTEST_CHANGE_T8: alignment gate
            if getattr(config, 'ALIGNMENT_GATE_MIN', 0) > 0:
                _al_count = 0
                if k_1h < d_1h: _al_count += 1
                if k_4h < d_4h: _al_count += 1
                if float(indicators.get('stoch_k_15m', 50)) < float(indicators.get('stoch_d_15m', 50)): _al_count += 1
                if lr_trend_15m < 0: _al_count += 1
                _al_total_max = getattr(config, 'ALIGNMENT_GATE_TOTAL', 12)
                if _al_count < getattr(config, 'ALIGNMENT_GATE_MIN', 4):
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: alignment={_al_count}/{_al_total_max} < {config.ALIGNMENT_GATE_MIN}")
                    return False
            # BACKTEST_CHANGE_T10: LR %B daily threshold for shorts
            if getattr(config, 'LR_PCTB_D_SHORT_THRESHOLD', 0) > 0:
                _lr_pctb_d = float(indicators.get('lr_pctb_D', 0.5) or 0.5)
                if _lr_pctb_d < getattr(config, 'LR_PCTB_D_SHORT_THRESHOLD', 0.1):
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} BLOCKED: lr_pctb_D={_lr_pctb_d:.2f} < {config.LR_PCTB_D_SHORT_THRESHOLD}")
                    return False
            # Leaderboard filter check
            if config.LEADERBOARD_FILTER:
                rankings = await self.get_rankings()
                if rankings:
                    bottom_symbols = rankings.get('bottom_20', [])
                    in_bottom_20 = symbol in bottom_symbols
                    if config.VERBOSE:
                        logger.info(f"[VERBOSE][CALC][SHORT] {symbol} LEADERBOARD_FILTER: enabled={config.LEADERBOARD_FILTER} in_bottom_20={in_bottom_20}")
                    if not in_bottom_20:
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} !! BLOCKED: not in bottom_20 leaderboard")
                        return False
                elif config.VERBOSE:
                    logger.info(f"[VERBOSE][CALC][SHORT] {symbol} LEADERBOARD_FILTER: enabled but no rankings data available")
            elif config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][SHORT] {symbol} LEADERBOARD_FILTER: disabled (skipping)")
            # Trend gates check
            if config.TREND_GATES:
                trend_ok = lr_trend_15m < 0
                if config.VERBOSE:
                    logger.info(f"[VERBOSE][CALC][SHORT] {symbol} TREND_GATES: enabled={config.TREND_GATES} lr_trend_15m={lr_trend_15m:.3f} trend_ok={trend_ok}")
                if not trend_ok:
                    if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} !! BLOCKED: lr_trend_15m={lr_trend_15m:.3f} >= 0")
                    return False
            elif config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][SHORT] {symbol} TREND_GATES: disabled (skipping)")
            # HTF confirmation checks
            if config.HTF1_CONF or config.HTF4_CONF:
                htf1_ok = True
                htf4_ok = True
                if config.HTF1_CONF:
                    htf1_ok = k_1h < d_1h
                    if config.VERBOSE:
                        logger.info(f"[VERBOSE][CALC][SHORT] {symbol} HTF1_CONF: enabled={config.HTF1_CONF} k_1h={k_1h:.1f} d_1h={d_1h:.1f} htf1_ok={htf1_ok}")
                    if not htf1_ok:
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} !! BLOCKED: HTF1_CONF k_1h={k_1h:.1f} >= d_1h={d_1h:.1f}")
                        return False
                if config.HTF4_CONF:
                    htf4_ok = k_4h < d_4h
                    if config.VERBOSE:
                        logger.info(f"[VERBOSE][CALC][SHORT] {symbol} HTF4_CONF: enabled={config.HTF4_CONF} k_4h={k_4h:.1f} d_4h={d_4h:.1f} htf4_ok={htf4_ok}")
                    if not htf4_ok:
                        if config.VERBOSE: logger.info(f"[VERBOSE][CALC][SHORT] {symbol} !! BLOCKED: HTF4_CONF k_4h={k_4h:.1f} >= d_4h={d_4h:.1f}")
                        return False
            elif config.VERBOSE:
                logger.info(f"[VERBOSE][CALC][SHORT] {symbol} HTF_CONF: disabled (skipping)")
            # Final score check
            score_threshold = 0.4
            ranking_threshold = -50
            score_ok = True  # BACKTEST_CHANGE_T42: near-zero signal in 0xxx sweep
            ranking_ok = True  # BACKTEST_CHANGE_T41: near-zero signal in 0xxx sweep
            final_result = score_ok and ranking_ok
            if config.VERBOSE:
                in_bottom_20_val = in_bottom_20 if config.LEADERBOARD_FILTER and 'in_bottom_20' in locals() else (True if not config.LEADERBOARD_FILTER else False)
                trend_ok_val = trend_ok if config.TREND_GATES and 'trend_ok' in locals() else (True if not config.TREND_GATES else (lr_trend_15m < 0))
                htf1_ok_val = htf1_ok if config.HTF1_CONF and 'htf1_ok' in locals() else (True if not config.HTF1_CONF else (k_1h < d_1h))
                htf4_ok_val = htf4_ok if config.HTF4_CONF and 'htf4_ok' in locals() else (True if not config.HTF4_CONF else (k_4h < d_4h))
                logger.info(f"\n[DECISION SCORECARD] {symbol} SHORT EVALUATION")
                logger.info(f"{'Metric':<15} | {'Value':<10} | {'Threshold':<10} | {'Result'}")
                logger.info("-" * 55)
                logger.info(f"{'Leaderboard':<15} | {str(in_bottom_20_val):<10} | {'True':<10} | {'✅' if in_bottom_20_val else '!!'}")
                logger.info(f"{'Trend (15m)':<15} | {lr_trend_15m:<10.3f} | {'< 0':<10} | {'✅' if trend_ok_val else '!!'}")
                logger.info(f"{'HTF1 (1h)':<15} | {k_1h:.1f}/{d_1h:.1f}  | {'K < D':<10} | {'✅' if htf1_ok_val else '!!'}")
                logger.info(f"{'HTF4 (4h)':<15} | {k_4h:.1f}/{d_4h:.1f}  | {'K < D':<10} | {'✅' if htf4_ok_val else '!!'}")
                logger.info(f"{'Final Score':<15} | {final_score:<10.3f} | {'< '+str(score_threshold):<10} | {'✅' if final_score<score_threshold else '!!'}")
                logger.info(f"{'Rank Points':<15} | {ranking_points:<10.1f} | {'BYPASSED':<10} | {'✅ (T41)'}")
                logger.info("-" * 55)
                logger.info(f"FINAL DECISION: {'✅ ENTER SHORT' if final_result else '⛔ BLOCKED'}\n")
                logger.info(f"[VERBOSE][CALC][SHORT] {symbol} FINAL SCORE CHECK: final_score={final_score:.3f} < {score_threshold} = {score_ok} | ranking_points=BYPASSED(T41) | RESULT={final_result}")
            return final_result
        except Exception as e:
            logger.error(f"Error in should_enter_short for {symbol}: {e}",  exc_info=True )
            return False

        # DEAD_CODE_START — should_exit_long: only called from dead process_symbol — commented out 2026-03-31
    #     async def should_exit_long(self, symbol: str, position: TradierPosition, indicators: Dict[str, Any]) -> bool:
    #         """Determine if we should exit a long position"""
    #         try:
    #             current_price,ts = await self.get_current_price(symbol)
    #             current_price = float(current_price)
    #             entry_price = position.entry_price
    #             stop_loss = position.stop_loss
    #             take_profit = position.take_profit
    #             gain = ((current_price - entry_price) / entry_price * 100) if entry_price > 0 else 0
    #             cu_1h = indicators.get('wt_cross_1h') == "BEAR"  # WT cross replaces stoch
    #             stop_loss_hit = stop_loss and current_price <= stop_loss
    #             take_profit_hit = take_profit and current_price >= take_profit
    #             crossunder_hit = cu_1h == True
    #             # BACKTEST_CHANGE_T16: ATR 2x trailing stop exit
    #             _atr_trail_exit = False
    #             if getattr(config, 'ATR_TRAIL_2X_EXIT_ENABLED', False) and entry_price > 0:
    #                 _atr_1h = float(indicators.get('atr_1h', 0) or 0)
    #                 if _atr_1h > 0 and current_price < entry_price - (2.0 * _atr_1h) and gain > 0:
    #                     _atr_trail_exit = True
    #                     logger.info(f"[ATR_TRAIL_EXIT] LONG {symbol}: price={current_price:.2f} < entry-2*ATR={entry_price - 2*_atr_1h:.2f}")
    #             # BACKTEST_CHANGE_T17: stoch cross 1h exit (k crosses below d = exit long if in profit)
    #             _stoch_cross_1h_exit = False
    #             if getattr(config, 'STOCH_CROSS_1H_EXIT_ENABLED', False) and gain > 0:
    #                 _k_1h = float(indicators.get('stoch_k_1h', 50) or 50)
    #                 _d_1h = float(indicators.get('stoch_d_1h', 50) or 50)
    #                 _k_1h_prev = float(indicators.get('stoch_k_1h_prev', 50) or 50)
    #                 if _k_1h < _d_1h and _k_1h_prev >= _d_1h:
    #                     _stoch_cross_1h_exit = True
    #                     logger.info(f"[STOCH_CROSS_1H_EXIT] LONG {symbol}: k_1h={_k_1h:.1f} crossed below d_1h={_d_1h:.1f} gain={gain:.2f}%")
    #             # BACKTEST_CHANGE_148: WT exhaustion exit — MFI shows overbought THEN short-TF WT crosses under (momentum turning)
    #             _mfi_flip_exit = False
    #             if getattr(config, 'MFI_FLIP_EXIT_ENABLED', True) and gain > 0.3:
    #                 _mfi_1h = float(indicators.get('mfi_1h', 50) or 50)
    #                 _wt_cu_5m = indicators.get('wt_cross_bear_5m', False) or indicators.get('wt_cross_5m') == "BEAR"
    #                 _wt_cu_15m = indicators.get('wt_cross_bear_15m', False) or indicators.get('wt_cross_15m') == "BEAR"
    #                 _wt1_5m = float(indicators.get('wt1_5m', 0) or 0)
    #                 # Exit when MFI was high (overbought flow) AND short-TF WT crosses under (flow exhausting)
    #                 if _mfi_1h > 65 and (_wt_cu_5m or _wt_cu_15m):
    #                     _mfi_flip_exit = True
    #                     logger.info(f"[WT_EXHAUST_EXIT] LONG {symbol}: MFI_1h={_mfi_1h:.0f}>65 + WT crossunder {'5m' if _wt_cu_5m else '15m'}, gain={gain:.2f}% — momentum turning")
    #                 # Also exit on strong WT crossunder alone (without MFI) if gain is good
    #                 elif gain > 1.0 and (_wt_cu_15m and _wt1_5m < 0):
    #                     _mfi_flip_exit = True
    #                     logger.info(f"[WT_TURN_EXIT] LONG {symbol}: WT crossunder 15m + wt1_5m={_wt1_5m:.0f}<0, gain={gain:.2f}% — taking profit on momentum turn")
    #                 # LTF MFI dying — volume drying up from rally (5m MFI dropping while 1h still high)
    #                 elif gain > 0.5:
    #                     _mfi_5m = float(indicators.get('mfi_5m', 50) or 50)
    #                     _mfi_15m = float(indicators.get('mfi_15m', 50) or 50)
    #                     if _mfi_1h > 60 and _mfi_5m < 35 and _mfi_15m < 40:
    #                         _mfi_flip_exit = True
    #                         logger.info(f"[MFI_VOLUME_DRY] LONG {symbol}: MFI 1h={_mfi_1h:.0f} still high but 5m={_mfi_5m:.0f} 15m={_mfi_15m:.0f} dying — rally losing steam, gain={gain:.2f}%")
    #             # SATOSHIT EXIT — 15m RSI+StochK reach opposite extreme
    #             _sat_exit_long = False
    #             if getattr(config, 'SATOSHIT_ENTRY_FILTER', False) and gain > 0.3:
    #                 from ez_satoshit import satoshit_exit_check
    #                 _sat_pk = f"tradier:{symbol}_LONG"
    #                 _sat_exit_long, _sat_exit_reason = satoshit_exit_check(indicators, True, config, position_key=_sat_pk)
    #                 if _sat_exit_long:
    #                     logger.warning(f"[SATOSHIT_EXIT] LONG {symbol}: {_sat_exit_reason} gain={gain:.2f}%")
    #             # STRICT_NO_LOSS: NEVER exit at a loss. Only exit when gain > 0.
    #             _in_profit = gain > 0.3
    #             final_result = _in_profit and (take_profit_hit or _mfi_flip_exit or crossunder_hit or _atr_trail_exit or _stoch_cross_1h_exit or _sat_exit_long)
    #             if not _in_profit and (stop_loss_hit or crossunder_hit):
    #                 logger.info(f"[STRICT_NO_LOSS] LONG {symbol}: BLOCKED exit at loss gain={gain:.2f}%. Holding until recovery.")
    #                 return False
    #             if config.VERBOSE:
    #                 logger.info(f"\n[DECISION SCORECARD] {symbol} LONG EXIT EVALUATION")
    #                 logger.info(f"{'Metric':<20} | {'Value':<15} | {'Condition':<20} | {'Result'}")
    #                 logger.info("-" * 75)
    #                 logger.info(f"{'Current Price':<20} | ${current_price:<14.2f} | {'N/A':<20} | {'─'}")
    #                 logger.info(f"{'Entry Price':<20} | ${entry_price:<14.2f} | {'N/A':<20} | {'─'}")
    #                 logger.info(f"{'Gain %':<20} | {gain:<14.2f}% | {'N/A':<20} | {'─'}")
    #                 logger.info(f"{'MFI Flip':<20} | {str(_mfi_flip_exit):<14} | {'MFI > 70 + gain':<20} | {'✅ EXIT' if _mfi_flip_exit else '!!'}")
    #                 logger.info(f"{'WT Crossunder':<20} | {str(cu_1h):<14} | {'Crossunder 1h':<20} | {'✅ EXIT' if crossunder_hit else '!!'}")
    #                 logger.info(f"{'Take Profit':<20} | ${take_profit if take_profit else 'None':<14} | {'Price >= TP':<20} | {'✅ EXIT' if take_profit_hit else '!!'}")
    #                 logger.info("-" * 75)
    #                 logger.info(f"FINAL DECISION: {'✅ EXIT LONG' if final_result else '⛔ HOLD'}\n")
    #             return final_result
    #         except Exception as e:
    #             logger.debug(f"Error in should_exit_long for {symbol}: {e}")
    #             return False
        # DEAD_CODE_END — should_exit_long: only called from dead process_symbol

        # DEAD_CODE_START — should_exit_short: only called from dead process_symbol — commented out 2026-03-31
    #     async def should_exit_short(self, symbol: str, position: TradierPosition, indicators: Dict[str, Any]) -> bool:
    #         """Determine if we should exit a short position"""
    #         try:
    #             current_price,ts = await self.get_current_price(symbol)
    #             current_price = float(current_price)
    #             entry_price = position.entry_price
    #             stop_loss = position.stop_loss
    #             take_profit = position.take_profit
    #             gain = ((entry_price - current_price) / entry_price * 100) if entry_price > 0 else 0
    #             co_1h = indicators.get('wt_cross_1h') == "BULL"  # WT cross replaces stoch
    #             stop_loss_hit = stop_loss and current_price >= stop_loss
    #             take_profit_hit = take_profit and current_price <= take_profit
    #             crossover_hit = co_1h == True
    #             # BACKTEST_CHANGE_T16: ATR 2x trailing stop exit
    #             _atr_trail_exit = False
    #             if getattr(config, 'ATR_TRAIL_2X_EXIT_ENABLED', False) and entry_price > 0:
    #                 _atr_1h = float(indicators.get('atr_1h', 0) or 0)
    #                 if _atr_1h > 0 and current_price > entry_price + (2.0 * _atr_1h) and gain > 0:
    #                     _atr_trail_exit = True
    #                     logger.info(f"[ATR_TRAIL_EXIT] SHORT {symbol}: price={current_price:.2f} > entry+2*ATR={entry_price + 2*_atr_1h:.2f}")
    #             # BACKTEST_CHANGE_T17: stoch cross 1h exit (k crosses above d = exit short if in profit)
    #             _stoch_cross_1h_exit = False
    #             if getattr(config, 'STOCH_CROSS_1H_EXIT_ENABLED', False) and gain > 0:
    #                 _k_1h = float(indicators.get('stoch_k_1h', 50) or 50)
    #                 _d_1h = float(indicators.get('stoch_d_1h', 50) or 50)
    #                 _k_1h_prev = float(indicators.get('stoch_k_1h_prev', 50) or 50)
    #                 if _k_1h > _d_1h and _k_1h_prev <= _d_1h:
    #                     _stoch_cross_1h_exit = True
    #                     logger.info(f"[STOCH_CROSS_1H_EXIT] SHORT {symbol}: k_1h={_k_1h:.1f} crossed above d_1h={_d_1h:.1f} gain={gain:.2f}%")
    #             # BACKTEST_CHANGE_148: WT exhaustion exit for SHORT — MFI oversold THEN short-TF WT crosses over
    #             _mfi_flip_exit = False
    #             if getattr(config, 'MFI_FLIP_EXIT_ENABLED', True) and gain > 0.3:
    #                 _mfi_1h = float(indicators.get('mfi_1h', 50) or 50)
    #                 _wt_co_5m = indicators.get('wt_cross_bull_5m', False) or indicators.get('wt_cross_5m') == "BULL"
    #                 _wt_co_15m = indicators.get('wt_cross_bull_15m', False) or indicators.get('wt_cross_15m') == "BULL"
    #                 _wt1_5m = float(indicators.get('wt1_5m', 0) or 0)
    #                 # Exit SHORT when MFI was low (oversold) AND short-TF WT crosses over (bounce starting)
    #                 if _mfi_1h < 35 and (_wt_co_5m or _wt_co_15m):
    #                     _mfi_flip_exit = True
    #                     logger.info(f"[WT_EXHAUST_EXIT] SHORT {symbol}: MFI_1h={_mfi_1h:.0f}<35 + WT crossover {'5m' if _wt_co_5m else '15m'}, gain={gain:.2f}% — bounce starting")
    #                 elif gain > 1.0 and (_wt_co_15m and _wt1_5m > 0):
    #                     _mfi_flip_exit = True
    #                     logger.info(f"[WT_TURN_EXIT] SHORT {symbol}: WT crossover 15m + wt1_5m={_wt1_5m:.0f}>0, gain={gain:.2f}% — covering on momentum turn")
    #                 # LTF MFI rising — buying volume picking up while 1h still oversold (bounce forming)
    #                 elif gain > 0.5:
    #                     _mfi_5m = float(indicators.get('mfi_5m', 50) or 50)
    #                     _mfi_15m = float(indicators.get('mfi_15m', 50) or 50)
    #                     if _mfi_1h < 40 and _mfi_5m > 65 and _mfi_15m > 60:
    #                         _mfi_flip_exit = True
    #                         logger.info(f"[MFI_VOLUME_RISE] SHORT {symbol}: MFI 1h={_mfi_1h:.0f} still low but 5m={_mfi_5m:.0f} 15m={_mfi_15m:.0f} rising — bounce forming, gain={gain:.2f}%")
    #             # SATOSHIT EXIT — 15m RSI+StochK reach opposite extreme
    #             _sat_exit_short = False
    #             if getattr(config, 'SATOSHIT_ENTRY_FILTER', False) and gain > 0.3:
    #                 from ez_satoshit import satoshit_exit_check
    #                 _sat_pk = f"tradier:{symbol}_SHORT"
    #                 _sat_exit_short, _sat_exit_reason = satoshit_exit_check(indicators, False, config, position_key=_sat_pk)
    #                 if _sat_exit_short:
    #                     logger.warning(f"[SATOSHIT_EXIT] SHORT {symbol}: {_sat_exit_reason} gain={gain:.2f}%")
    #             # STRICT_NO_LOSS: NEVER exit at a loss
    #             _in_profit = gain > 0.3
    #             final_result = _in_profit and (take_profit_hit or _mfi_flip_exit or crossover_hit or _atr_trail_exit or _stoch_cross_1h_exit or _sat_exit_short)
    #             if not _in_profit and (stop_loss_hit or crossover_hit):
    #                 logger.info(f"[STRICT_NO_LOSS] SHORT {symbol}: BLOCKED exit at loss gain={gain:.2f}%. Holding until recovery.")
    #                 return False
    #             if config.VERBOSE:
    #                 logger.info(f"\n[DECISION SCORECARD] {symbol} SHORT EXIT EVALUATION")
    #                 logger.info(f"{'Metric':<20} | {'Value':<15} | {'Condition':<20} | {'Result'}")
    #                 logger.info("-" * 75)
    #                 logger.info(f"{'Current Price':<20} | ${current_price:<14.2f} | {'N/A':<20} | {'─'}")
    #                 logger.info(f"{'Entry Price':<20} | ${entry_price:<14.2f} | {'N/A':<20} | {'─'}")
    #                 logger.info(f"{'Gain %':<20} | {gain:<14.2f}% | {'N/A':<20} | {'─'}")
    #                 logger.info(f"{'MFI Flip':<20} | {str(_mfi_flip_exit):<14} | {'MFI < 30 + gain':<20} | {'✅ EXIT' if _mfi_flip_exit else '!!'}")
    #                 logger.info(f"{'WT Crossover':<20} | {str(co_1h):<14} | {'Crossover 1h':<20} | {'✅ EXIT' if crossover_hit else '!!'}")
    #                 logger.info(f"{'Take Profit':<20} | ${take_profit if take_profit else 'None':<14} | {'Price <= TP':<20} | {'✅ EXIT' if take_profit_hit else '!!'}")
    #                 logger.info("-" * 75)
    #                 logger.info(f"FINAL DECISION: {'✅ EXIT SHORT' if final_result else '⛔ HOLD'}\n")
    #             return final_result
    #             return False
    #         except Exception as e:
    #             logger.debug(f"Error in should_exit_short for {symbol}: {e}")
    #             return False
        # DEAD_CODE_END — should_exit_short: only called from dead process_symbol
                
    async def calculate_position_size(self, symbol: str, price: float, account_key: str = "tra", side: str = "LONG") -> float:
        """Calculate position size based on config — per-symbol overrides via rolling_config_optimizer."""
        try:
            if price <= 0:
                logger.error(f"Error calculating position size for {symbol}: Price is {price}")
                return 0.0
            target_value = _cfg('START_POSITION_SIZE', 600, account_key, symbol, side)
            # BACKTEST_CHANGE_T23: close zone size multiplier
            if getattr(config, 'TIME_ZONE_ENABLED', False):
                _zone = _get_trade_zone()
                if _zone == "close":
                    target_value *= getattr(config, 'CLOSE_ZONE_SIZE_MULT', 1.5)
            max_value = min(target_value, _cfg('MAX_ORDER_VALUE', 2000, account_key, symbol, side))
            # Congress conviction sizing boost (stock_trader_scanner multi-source agreement)
            congress_boost = _get_congress_conviction_boost(symbol)
            if congress_boost > 1.0:
                max_value *= congress_boost
                max_value = min(max_value, config.MAX_ORDER_VALUE * 1.5)
            # 2026-04-26 VIX volatility regime size multiplier (VIX vs VIX-200dMA)
            if _VIX_MODULE_AVAILABLE and getattr(config, 'VIX_VOLATILITY_REGIME_ENABLED', False):
                try:
                    _vix_mult = get_vix_regime_size_mult(config)
                    if _vix_mult <= 0.0:
                        logger.warning(f"[{account_key}] [VIX_REGIME] {get_vix_regime_label(config)} — entry blocked for {symbol}")
                        return 0.0
                    if _vix_mult < 1.0:
                        max_value *= _vix_mult
                        logger.info(f"[{account_key}] [VIX_REGIME] {get_vix_regime_label(config)} size_mult={_vix_mult:.2f} → max_value=${max_value:.0f}")
                except Exception as _vix_e:
                    logger.warning(f"[{account_key}] [VIX_REGIME] check failed: {_vix_e}")
            shares = max_value / price
            shares_int = max(1, int(round(shares)))
            return float(shares_int)
        except Exception as e:
            logger.error(f"Error calculating position size for {symbol}: {e}")
            return 0.0

    async def calculate_stop_loss(self, symbol: str, entry_price: float, side: str, indicators: Dict[str, Any]) -> float:
        """
        Calculate stop loss.
        Rule 1: Respect Daily DC levels (Macro structure).
        Rule 2: MUST be outside 15m DC levels (Micro structure) - never stop inside the noise.
        Rule 3: Max risk cap of 6% to prevent "infinite" structural stops.
        """
        try:
            d = self.strategy.parse_market_data(indicators)
            
            # Daily Levels (Macro)
            dc_low_d = d.get('dc_low_D', 0) or d.get('dc_low_D', 0)
            dc_high_d = d.get('dc_high_D', 0) or d.get('dc_high_D', 0)
            
            # 15m Levels (Micro)
            dc_low_15m = d.get('dc_low_15m', 0)
            dc_high_15m = d.get('dc_high_15m', 0)
            
            atr = d.get('atr_15m', 0)
            stop_price = 0.0

            if side == "LONG":
                # 1. Base Stop: 2% below Daily Low, or 2x ATR if Daily missing
                if dc_low_d > 0:
                    base_stop = dc_low_d * 0.98
                else:
                    base_stop = entry_price - (atr * 2.0 if atr > 0 else entry_price * 0.03)  # Marathon winner: stop_atr=2.0

                # 2. Structural Constraint: Stop MUST be below 15m Low
                # If base_stop is higher than 15m Low (inside the noise), push it down.
                if dc_low_15m > 0:
                    structural_limit = dc_low_15m * 0.995  # 0.5% buffer below 15m low
                    stop_price = min(base_stop, structural_limit)
                else:
                    stop_price = base_stop

                # 3. Sanity Cap: Don't risk more than 6% on entry
                max_risk_price = entry_price * 0.94
                if stop_price < max_risk_price:
                    stop_price = max_risk_price
                    logger.info(f"[{symbol}] Structural stop too wide. Capping at -6%: {stop_price:.2f}")

                logger.info(f"[{symbol}] Stop Loss: {stop_price:.2f} (Entry: {entry_price:.2f}, 15mLow: {dc_low_15m:.2f})")

            else: # SHORT
                # 1. Base Stop: 2% above Daily High
                if dc_high_d > 0:
                    base_stop = dc_high_d * 1.02
                else:
                    base_stop = entry_price + (atr * 2.0 if atr > 0 else entry_price * 0.03)

                # 2. Structural Constraint: Stop MUST be above 15m High
                if dc_high_15m > 0:
                    structural_limit = dc_high_15m * 1.005 # 0.5% buffer above 15m high
                    stop_price = max(base_stop, structural_limit)
                else:
                    stop_price = base_stop

                # 3. Sanity Cap: 6% max risk
                max_risk_price = entry_price * 1.06
                if stop_price > max_risk_price:
                    stop_price = max_risk_price
                    logger.info(f"[{symbol}] Structural stop too wide. Capping at -6%: {stop_price:.2f}")

                logger.info(f"[{symbol}] Stop Loss: {stop_price:.2f} (Entry: {entry_price:.2f}, 15mHigh: {dc_high_15m:.2f})")

            return float(stop_price)

        except Exception as e:
            logger.error(f"Error calculating stop loss for {symbol}: {e}")
            # Emergency Fallback: 4% fixed
            return entry_price * 0.96 if side == "LONG" else entry_price * 1.04
            
        # DEAD_CODE_START — process_symbol: DEAD — never called, all trading via evaluate_stop/open/reentry — commented out 2026-03-31
    #     async def process_symbol(self, symbol: str, account_key: str):  # DEAD CODE — never called. Kept for reference only. All trading goes through evaluate_stop/evaluate_open/evaluate_reentry.
    #         """
    #         DEAD CODE — process_symbol is never called. The active system uses evaluate_stop/evaluate_open.
    #         This function and should_enter_long/should_exit_long/should_enter_short/should_exit_short below it
    #         are the OLD entry/exit system that bypasses execute_now. DO NOT CALL.
    #         Strictly enforces allow-lists for new entries.
    #         """
    #         try:
    #             symbol = symbol.upper().strip()
                
    #             # 1. Get Indicators (Shared for both sides) — enriched with YouTube Consensus extras
    #             indicators = self.get_enriched_indicators(symbol)
    #             if not indicators:
    #                 logger.debug(f"No indicators available for {symbol}")
    #                 return

    #             current_price,_ = await self.get_current_price(symbol)
    #             current_price = float(current_price)
    #             # BACKTEST_CHANGE: k_4h cross (not k_1h) — tradier uses higher TF
    #             global _ls_ratio_4h_adj_tradier
    #             _k4h_ps = float(indicators.get('stoch_k_4h', 50) or 50)
    #             _d4h_ps = float(indicators.get('stoch_d_4h', 50) or 50)
    #             _k4h_prev_ps = float(indicators.get('stoch_k_4h_prev', 50) or 50)
    #             if _k4h_ps > _d4h_ps and _k4h_prev_ps <= _d4h_ps:
    #                 _ls_ratio_4h_adj_tradier["adj"] = min(0.50, _ls_ratio_4h_adj_tradier.get("adj", 0.0) + 0.10)
    #                 _ls_ratio_4h_adj_tradier["last_cross_ts"] = time.time()
    #                 _ls_ratio_4h_adj_tradier["adj"] = max(-0.50, min(0.50, _ls_ratio_4h_adj_tradier["adj"]))
    #             elif _k4h_ps < _d4h_ps and _k4h_prev_ps >= _d4h_ps:
    #                 _ls_ratio_4h_adj_tradier["adj"] = max(-0.50, _ls_ratio_4h_adj_tradier.get("adj", 0.0) - 0.10)
    #                 _ls_ratio_4h_adj_tradier["last_cross_ts"] = time.time()
    #                 _ls_ratio_4h_adj_tradier["adj"] = max(-0.50, min(0.50, _ls_ratio_4h_adj_tradier["adj"]))
    #             # 2. Get Positions directly
    #             from utils import construct_position_key, orjson_default
    #             long_key = construct_position_key(account_key, symbol, 'LONG')
    #             short_key = construct_position_key(account_key, symbol, 'SHORT')
                
    #             long_pos = self.position_manager.positions.get(long_key)
    #             short_pos = self.position_manager.positions.get(short_key)

    #             has_long = long_pos and abs(float(getattr(long_pos, 'positionAmt', 0))) > 0
    #             has_short = short_pos and abs(float(getattr(short_pos, 'positionAmt', 0))) > 0
    #             # BACKTEST_CHANGE_T37: daily loss circuit breaker
    #             global _daily_loss_tracker
    #             _today_str = datetime.now(timezone.utc).strftime('%Y%m%d')
    #             if _daily_loss_tracker["date"] != _today_str:
    #                 _daily_loss_tracker = {"date": _today_str, "realized_pnl": 0.0, "halted": False}
    #             _entry_blocked_daily_loss = _daily_loss_tracker["halted"]
    #             # BACKTEST_CHANGE_T35: max concurrent positions check
    #             _entry_blocked_max_pos = False
    #             _max_concurrent = getattr(config, 'MAX_CONCURRENT_POSITIONS', 999)
    #             if _max_concurrent < 999:
    #                 _open_count = sum(1 for _pk, _p in self.position_manager.positions.items() if abs(float(getattr(_p, 'positionAmt', 0))) > 0)
    #                 if _open_count >= _max_concurrent:
    #                     _entry_blocked_max_pos = True
    #             # BACKTEST_CHANGE_T36: L/S ratio enforcement
    #             _entry_blocked_ls_long, _entry_blocked_ls_short = False, False
    #             if getattr(config, 'LS_RATIO_ENFORCE_TRADIER', False):
    #                 _lc = sum(1 for _pk in self.position_manager.positions if _pk.endswith("_LONG") and abs(float(getattr(self.position_manager.positions[_pk], 'positionAmt', 0))) > 0)
    #                 _sc = sum(1 for _pk in self.position_manager.positions if _pk.endswith("_SHORT") and abs(float(getattr(self.position_manager.positions[_pk], 'positionAmt', 0))) > 0)
    #                 # BACKTEST_CHANGE: k_4h cross (not k_1h) — tradier uses higher TF
    #                 _adj = _ls_ratio_4h_adj_tradier.get("adj", 0.0)
    #                 if _ls_ratio_4h_adj_tradier.get("last_cross_ts", 0) > 0 and (time.time() - _ls_ratio_4h_adj_tradier["last_cross_ts"]) > 14400:
    #                     _ls_ratio_4h_adj_tradier["adj"] = 0.0
    #                     _adj = 0.0
    #                 _ls_min = max(0.30, getattr(config, 'LS_RATIO_MIN_TRADIER', 0.50) + _adj)
    #                 _ls_max = min(3.00, getattr(config, 'LS_RATIO_MAX_TRADIER', 2.00) + _adj)
    #                 _ratio = _lc / max(_sc, 1)
    #                 if _ratio > _ls_max:
    #                     _entry_blocked_ls_long = True
    #                     logger.warning(f"[LS_RATIO_BLOCK] L/S ratio {_ratio:.2f} > max {_ls_max:.2f}, blocking LONG open")
    #                 if _ratio < _ls_min:
    #                     _entry_blocked_ls_short = True
    #                     logger.warning(f"[LS_RATIO_BLOCK] L/S ratio {_ratio:.2f} < min {_ls_min:.2f}, blocking SHORT open")
    #             # ==========================================================
    #             # LONG SIDE PROCESSING
    #             # ==========================================================
    #             # TRA_SATOSHIT_ONLY: tra account only enters via SATOSHIT
    #             _tra_satoshit_only = getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra'

    #             # A. EXIT CHECK (Always allowed if we have a position)
    #             if has_long:
    #                 if await self.should_exit_long(symbol, long_pos, indicators):
    #                     logger.info(f"!! Exit signal for LONG {symbol}")
    #                     qty_to_close = abs(float(long_pos.positionAmt))
    #                     result = await self.place_order(symbol, "SELL", qty_to_close, "market", duration="day", action="REDUCE", position_side="LONG", account_key=account_key)
    #                     self._log_order_result(result, symbol, "LONG", "CLOSE")

    #             # B. ENTRY CHECK (Only if allowed AND no opposing position)
    #             elif not has_short:
    #                 if _entry_blocked_daily_loss:  # BACKTEST_CHANGE_T37
    #                     logger.warning(f"[DAILY_LOSS_HALT] Blocking LONG {symbol}: daily loss limit breached")
    #                 elif _entry_blocked_max_pos:  # BACKTEST_CHANGE_T35
    #                     logger.debug(f"[MAX_POS_BLOCK] Blocking LONG {symbol}: at max concurrent positions ({_max_concurrent})")
    #                 elif _entry_blocked_ls_long:  # BACKTEST_CHANGE_T36
    #                     pass  # already logged above
    #                 elif self.is_symbol_allowed(account_key, symbol, 'LONG'):
    #                     if _tra_satoshit_only:
    #                         # tra: SATOSHIT-only — call dedicated satoshit check, skip full should_enter_long
    #                         _sat_enter = False
    #                         if getattr(config, 'SATOSHIT_ENTRY_FILTER', False):
    #                             from ez_satoshit import satoshit_entry_signal
    #                             _sat_ok, _sat_votes, _sat_reason = satoshit_entry_signal(indicators, True, config)
    #                             if _sat_ok:
    #                                 logger.warning(f"[SATOSHIT_ENTRY] LONG {symbol} (tra-only): {_sat_reason}")
    #                                 _sat_enter = True
    #                         if _sat_enter:
    #                             quantity = await self.calculate_position_size(symbol, current_price, account_key=account_key)
    #                             if quantity > 0:
    #                                 stop_loss = await self.calculate_stop_loss(symbol, current_price, "LONG", indicators)
    #                                 result = await self.place_order(symbol, "BUY", quantity, "market", duration="day", action="OPEN", position_side="LONG", account_key=account_key)
    #                                 if result and isinstance(result, dict) and result.get('id'):
    #                                     self.position_manager.set_stop_loss(symbol, "LONG", stop_loss)
    #                                 self._log_order_result(result, symbol, "LONG", "OPEN")
    #                     elif await self.should_enter_long(symbol, indicators):
    #                         quantity = await self.calculate_position_size(symbol, current_price, account_key=account_key)
    #                         if quantity > 0:
    #                             stop_loss = await self.calculate_stop_loss(symbol, current_price, "LONG", indicators)
    #                             result = await self.place_order(symbol, "BUY", quantity, "market", duration="day", action="OPEN", position_side="LONG", account_key=account_key)
    #                             if result and isinstance(result, dict) and result.get('id'):
    #                                 self.position_manager.set_stop_loss(symbol, "LONG", stop_loss)
    #                             self._log_order_result(result, symbol, "LONG", "OPEN")
    #                 # else: Symbol ignored for LONG because it's not in Leaderboard/Whitelist

    #             # ==========================================================
    #             # SHORT SIDE PROCESSING
    #             # ==========================================================

    #             # A. EXIT CHECK (Always allowed if we have a position)
    #             if has_short:
    #                 if await self.should_exit_short(symbol, short_pos, indicators):
    #                     logger.info(f"!! Exit signal for SHORT {symbol}")
    #                     qty_to_close = abs(float(short_pos.positionAmt))
    #                     result = await self.place_order(symbol, "BUY", qty_to_close, "market", duration="day", action="REDUCE", position_side="SHORT", account_key=account_key)
    #                     self._log_order_result(result, symbol, "SHORT", "CLOSE")

    #             # B. ENTRY CHECK (Only if allowed AND no opposing position)
    #             elif not has_long:
    #                 if _entry_blocked_daily_loss:  # BACKTEST_CHANGE_T37
    #                     logger.warning(f"[DAILY_LOSS_HALT] Blocking SHORT {symbol}: daily loss limit breached")
    #                 elif _entry_blocked_max_pos:  # BACKTEST_CHANGE_T35
    #                     logger.debug(f"[MAX_POS_BLOCK] Blocking SHORT {symbol}: at max concurrent positions ({_max_concurrent})")
    #                 elif _entry_blocked_ls_short:  # BACKTEST_CHANGE_T36
    #                     pass  # already logged above
    #                 elif self.is_symbol_allowed(account_key, symbol, 'SHORT'):
    #                     if symbol not in self.non_shortable_symbols:
    #                         if _tra_satoshit_only:
    #                             # tra: SATOSHIT-only — dedicated satoshit check for shorts
    #                             _sat_enter_s = False
    #                             if getattr(config, 'SATOSHIT_ENTRY_FILTER', False):
    #                                 from ez_satoshit import satoshit_entry_signal
    #                                 _sat_ok_s, _sat_votes_s, _sat_reason_s = satoshit_entry_signal(indicators, False, config)
    #                                 if _sat_ok_s:
    #                                     logger.warning(f"[SATOSHIT_ENTRY] SHORT {symbol} (tra-only): {_sat_reason_s}")
    #                                     _sat_enter_s = True
    #                             if _sat_enter_s:
    #                                 quantity = await self.calculate_position_size(symbol, current_price, account_key=account_key)
    #                                 if quantity > 0:
    #                                     stop_loss = await self.calculate_stop_loss(symbol, current_price, "SHORT", indicators)
    #                                     result = await self.place_order(symbol, "SELL", quantity, "market", duration="day", action="OPEN", position_side="SHORT", account_key=account_key)
    #                                     if result and isinstance(result, dict) and result.get('id'):
    #                                         self.position_manager.set_stop_loss(symbol, "SHORT", stop_loss)
    #                                     self._log_order_result(result, symbol, "SHORT", "OPEN")
    #                         elif await self.should_enter_short(symbol, indicators):
    #                             quantity = await self.calculate_position_size(symbol, current_price, account_key=account_key)
    #                             if quantity > 0:
    #                                 stop_loss = await self.calculate_stop_loss(symbol, current_price, "SHORT", indicators)
    #                                 result = await self.place_order(symbol, "SELL", quantity, "market", duration="day", action="OPEN", position_side="SHORT", account_key=account_key)
    #                                 if result and isinstance(result, dict) and result.get('id'):
    #                                     self.position_manager.set_stop_loss(symbol, "SHORT", stop_loss)
    #                                 self._log_order_result(result, symbol, "SHORT", "OPEN")
    #                 # else: Symbol ignored for SHORT because it's not in Leaderboard/Whitelist

    #         except Exception as e:
    #             logger.error(f"Error processing symbol {symbol}: {e}",  exc_info=True )
        # DEAD_CODE_END — process_symbol: DEAD — never called, all trading via evaluate_stop/open/reentry

    def _log_order_result(self, result, symbol, side, action):
        """Helper to log order outcomes"""
        if result and isinstance(result, dict) and result.get('id'):
            logger.info(f"✅ {action} {side} {symbol} OrderID={result.get('id')} Status={result.get('status')}")
        elif result and isinstance(result, dict):
            logger.error(f"!! Failed {action} {side} {symbol}: {result.get('reject_reason') or result.get('error')}")
        else:
            logger.error(f"!! Failed {action} {side} {symbol}: No valid response")
            
    async def trading_loop(self):
        """Main trading loop - waits for shutdown"""
        while self.running:
            try:
                await asyncio.sleep(60)
                logger.debug("[trading_loop] System running - all processing via background tasks")
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"Error in trading loop: {e}",  exc_info=True )
                await asyncio.sleep(5)

    async def update_all_positions_prices(self):
        """Update all positions' mark_price from Redis/cache — pubsub handles real-time, this is a fallback"""
        logger.info("[update_all_positions_prices] Task started (15s interval)")
        while self.running:
            try:
                await asyncio.sleep(15.0)  # Was 5.0 — pubsub is real-time, this is just a fallback
                symbols = set()               

                if self.position_manager:
                    for pos in self.position_manager.positions.values():
                        if pos and pos.symbol:
                            symbols.add(pos.symbol)
                if self.symbols_long_tra:
                    symbols.update(self.symbols_long_tra[:50])
                if self.symbols_short_tra:
                    symbols.update(self.symbols_short_tra[:50])
                if self.symbols_long_trb:
                    symbols.update(self.symbols_long_trb[:50])
                if self.symbols_short_trb:
                    symbols.update(self.symbols_short_trb[:50])                                              
                if self.symbols_long_trc:
                    symbols.update(self.symbols_long_trc[:50])
                if self.symbols_short_trc:
                    symbols.update(self.symbols_short_trc[:50])
                    
                if not symbols: continue
                
                now = datetime.now(timezone.utc)
                for symbol in symbols:
                    try:
                        indicators = self.get_indicators(symbol, use_cache=True)
                        if indicators:
                            price,ts = await self.get_current_price(symbol)
                            if price > 0:
                                self.price_cache[symbol] = {"price": price, "timestamp": ts}
                                self.price_update_time[symbol] = ts
                    except Exception:
                        pass
            except asyncio.CancelledError:
                logger.info("[update_all_positions_prices] Task cancelled")
                break
            except Exception as e:
                logger.error(f"[update_all_positions_prices] Error: {e}",  exc_info=True )
                await asyncio.sleep(1)

    async def periodic_tasks(self, account_key, order_queue):
        """Main periodic task loop"""
        logger.info(f"☀️[periodic_tasks] Task started for {account_key}")
        while self.running:
            try:
                await asyncio.sleep(30)
                await self.load_leaderboards()
                await self.load_account_symbols()
                
                if self.position_manager and self.order_queue:
                    position_keys = []
                    for pk, pos in self.position_manager.positions.items():
                        if pos and abs(getattr(pos, 'quantity', getattr(pos, 'positionAmt', 0))) > 0:
                            if pk.startswith(f"{account_key}:"):
                                position_keys.append(pk)

                    if position_keys:
                        await monitor_entries(self.order_queue, self, account_key, position_keys, event_type="periodic_tasks", is_priority_add=False, force=False)

                    # --- CANDIDATE SCAN: symbols with no open position ---
                    # Without this, only AAPL/NVDA (with open positions) ever get evaluated
                    await self.reload_account_symbols()
                    longs  = getattr(self, f"symbols_long_{account_key}", [])
                    shorts = getattr(self, f"symbols_short_{account_key}", [])
                    open_set = set(position_keys)
                    candidates = (
                        [f"{account_key}:{s}_LONG"  for s in longs  if f"{account_key}:{s}_LONG"  not in open_set] +
                        [f"{account_key}:{s}_SHORT" for s in shorts if f"{account_key}:{s}_SHORT" not in open_set]
                    )
                    if candidates:
                        logger.info(f"[{account_key}] 🔍 CANDIDATE SCAN: {len(candidates)} symbols ({len(longs)}L/{len(shorts)}S)")
                        await monitor_entries(self.order_queue, self, account_key, candidates[:60], event_type="candidate_scan", is_priority_add=False, force=False)

            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[PERIODIC] Error: {e}", exc_info=True)

    def _sync_positions_from_manager(self):
        """Sync positions from ALL managers to local tracking - with ALL fields"""
        # Sync from managers — NEVER clear() first, merge instead to prevent data loss
        updated_count = 0
        for acc_key, mgr in self.managers.items():
            if not mgr.positions:
                logger.warning(f"[SYNC_SKIP] {acc_key}: manager has 0 positions — NOT clearing local cache (would destroy data)")
                continue
            
            # Iterate this manager's positions
            for pk, position in mgr.positions.items():
                if not position: continue

                # 1. Enforce Key Integrity
                # Ensure the position key starts with the correct account prefix
                if not pk.startswith(f"{acc_key}:"):
                    # If we find a 'tra:' key inside the 'trc' manager, FIX IT.
                    parts = pk.split(":")
                    if len(parts) > 1:
                        # strip old prefix, add correct one
                        clean_suffix = parts[-1] 
                        new_pk = f"{acc_key}:{clean_suffix}"
                        # logger.warning(f"[SYNC] 🧹 Fixing corrupted key: {pk} -> {new_pk}")
                        pk = new_pk
                    else:
                        continue

                # 2. Ensure position object has ALL fields
                if not hasattr(position, 'price_fetch_count'):
                    position.price_fetch_count = 0
                
                # 3. Copy to local tracking
                self.positions_by_account[acc_key][pk] = position
                self.positions[pk] = position
                
                # 4. Update timestamp
                self._position_update_timestamps[pk] = datetime.now(timezone.utc)
                if not position.last_updated:
                    position.last_updated = datetime.now(timezone.utc)
                
                updated_count += 1
        # logger.debug(f"[_sync_positions_from_manager] Synced {updated_count} positions across {len(self.managers)} accounts")

    async def monitor_ladder_levels(self, order_queue):
        """
        Monitors active ladder levels. 
        Triggers REENTER if price hits level AND momentum confirms (Stoch K cross).
        """
        logger.info("[monitor_ladder_levels] Task started")
        while self.running:
            try:
                await asyncio.sleep(5)
                
                if not self.position_manager:
                    continue

                # Iterate over a copy of the keys to avoid modification issues during iteration
                ladder_keys = list(self.position_manager.ladder_levels.keys())
                
                for position_key in ladder_keys:
                    ladder_data = self.position_manager.ladder_levels.get(position_key)
                    if not ladder_data: continue

                    symbol = ladder_data.get('symbol')
                    is_long = ladder_data.get('is_long', True)
                    levels = ladder_data.get('levels', [])
                    
                    # Cleanup empty or expired ladders
                    if not levels or all(l.get('crossed', False) for l in levels):
                        # Optional: Remove empty entries to clean up map
                        # self.position_manager.ladder_levels.pop(position_key, None)
                        continue
                    i = self.get_indicators(symbol)
                    if not i: continue
                    
                    current_price,ts = await self.get_current_price(symbol)
                    current_price = float(current_price)
                    if current_price <= 0: continue
                    k_5m = i.get('stoch_k_5m', 50)
                    d_5m = i.get('stoch_d_5m', 50)
                    
                    momentum_confirmed = (k_5m > d_5m) if is_long else (k_5m < d_5m)

                    levels_changed = False
                    
                    for level_blob in levels:
                        if level_blob.get('crossed', False):
                            continue

                        target_price = level_blob.get('level', 0)
                        qty = level_blob.get('quantity', 0)
                        
                        # LOGIC: 
                        # 1. Price must be "at or beyond" the level (Dip occurred)
                        # 2. Momentum must be reversing (Price coming back up/stabilizing)
                        
                        hit_zone = False
                        if is_long:
                            # Price fell to or below target
                            if current_price <= target_price * 1.001: 
                                hit_zone = True
                        else:
                            # Price rose to or above target
                            if current_price >= target_price * 0.999:
                                hit_zone = True
                        
                        if hit_zone and momentum_confirmed:
                            # EXECUTE REENTRY
                            reason = f"LADDER_LEVEL_HIT target={target_price:.2f} cur={current_price:.2f}"
                            logger.info(f"🪜 {symbol} LADDER TRIGGER: {reason}")
                            
                            await queue_trade_action(
                                order_queue, self, position_key, 
                                "REENTER", reason, 85.0, override_qty=qty  )
                            level_blob['crossed'] = True
                            levels_changed = True
                    if levels_changed:
                        await self.position_manager.save_ladder_levels(position_key)

            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[monitor_ladder_levels] Error: {e}",  exc_info=True )
                await asyncio.sleep(10)

    async def _options_watchdog_loop(self):
        """Run options analyzer watch cycle every 5 min during market hours."""
        logger.info("[OPTIONS_WATCHDOG] Background task started")
        while self.running:
            try:
                if is_regular_trading_hours():
                    import subprocess
                    account = self.target_accounts[0] if self.target_accounts else "trb"
                    cmd = [sys.executable, "tradier_options_analyzer.py", "watch", "--account", account, "--auto-sell"]
                    result = subprocess.run(cmd, capture_output=True, text=True, timeout=120, cwd=str(config.BASE_PATH))
                    if result.returncode != 0:
                        logger.warning(f"[OPTIONS_WATCHDOG] Exit code {result.returncode}: {result.stderr[-200:] if result.stderr else ''}")
                    else:
                        # Log any sells
                        for line in result.stdout.split('\n'):
                            if 'SELL' in line or 'Smart-selling' in line:
                                logger.warning(f"[OPTIONS_WATCHDOG] {line.strip()}")
                await asyncio.sleep(300)  # 5 min
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[OPTIONS_WATCHDOG] Error: {e}")
                await asyncio.sleep(60)

    async def monitor_system_state(self):
        logger.info("☀️ [SYSTEM] MONITOR STATE Started (Unified Logic)")
        await asyncio.sleep(5)
        
        while self.running:
            try:
                await self.load_leaderboards()
                scan_set = set()
                if self.position_manager:
                    for pk, pos in self.position_manager.positions.items():
                        if abs(float(getattr(pos, 'positionAmt', 0))) > 0:
                            scan_set.add(pk)
                
                # B. Discovery / Target Lists (Monitor for New Entries)
                for acc in self.target_accounts:
                    longs = getattr(self, f"symbols_long_{acc}",[])
                    shorts = getattr(self, f"symbols_short_{acc}",[])
                    for s in longs: scan_set.add(f"{acc}:{s}_LONG")
                    for s in shorts: scan_set.add(f"{acc}:{s}_SHORT")
              
                unique_keys = sorted(list(scan_set))
                if not unique_keys:
                    logger.info(f"[MONITOR] No symbols found. Sleeping...")
                    await asyncio.sleep(30)
                    continue
                
                # FIX 2: CONCURRENT BATCH EXECUTION (Kills the lag)
                batch_size = 40
                for i in range(0, len(unique_keys), batch_size):
                    batch = unique_keys[i:i + batch_size]
                    tasks =[]
                    
                    for position_key in batch:
                        account_key = position_key.split(':')[0]
                        tasks.append(  asyncio.wait_for( process_position(  account_key, position_key, self.order_queue,  self, event_type="MONITOR", force=False ), timeout=15.0  )   )
                    results = await asyncio.gather(*tasks, return_exceptions=True)
                    for r in results:
                        if isinstance(r, Exception) and not isinstance(r, asyncio.TimeoutError):
                            logger.error(f"[MONITOR] Batch task error: {r}")
                    await asyncio.sleep(0.15) # Tiny breather between big batches
                     

                # Wait before next full scan
                await asyncio.sleep(30)

            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[MONITOR] 💥 Error in loop: {e}",  exc_info=True )
                await asyncio.sleep(15)


    async def monitor_redis_connectivity(self):
        """Monitor Redis connection health"""
        while self.running:
            try:
                await asyncio.sleep(30)
                if not self.redis_manager:
                    try:
                        pass  # DISABLED: tradier does not use server Redis
                    except Exception:
                        self.redis_manager = None
                else:
                    try:
                        await self.redis_manager.get("tradier_indicators_latest")
                        logger.debug("[REDIS] Connection healthy")
                    except Exception as e:
                        logger.warning(f"[REDIS] Health check failed: {e}")
                        self.redis_manager = None
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.debug(f"[REDIS] Monitor error: {e}")
        

    async def load_leaderboards(self):
        try:
            await self.load_account_symbols()
            w20 = await self._get_symbol_set("winners_20")
            w15 = await self._get_symbol_set("winners_15m")
            l20 = await self._get_symbol_set("losers_20")
            l15 = await self._get_symbol_set("losers_15m")

            valid_longs_ctx = w20.union(w15)
            valid_shorts_ctx = l20.union(l15)
            
            # Load Master
            master_set = await self._get_symbol_set("symbols_tradier")
            if not master_set:
                # Fallback to config accounts if master missing
                for acc in self.target_accounts:
                    master_set.update(await self._get_symbol_set(f"symbols_{acc}_long"))
                    master_set.update(await self._get_symbol_set(f"symbols_{acc}_short"))

            # Calculate Intersection (The "Smart" List)
            if valid_longs_ctx or valid_shorts_ctx:
                dyn_long = {s for s in master_set if s in valid_longs_ctx}
                dyn_short = {s for s in master_set if s in valid_shorts_ctx}
            else:
                # If no context, use master set directly
                dyn_long = master_set.copy()
                dyn_short = master_set.copy()

            # --- 2. Build Final Account Lists ---
            whitelist = set(getattr(config, 'ALWAYS_TRADEABLE', []))

            for acc in self.target_accounts:
                # tra is satoshit-only and long-only. Its symbol list comes from
                # symbols_tra_satoshit_long.json via reload_account_symbols() and
                # MUST NOT be overwritten by the leaderboard loader (which would
                # drop the user's preferred symbols and re-add shorts).
                if acc == 'tra' and getattr(config, 'TRA_SATOSHIT_ONLY', False):
                    continue
                # A. Start with Dynamic Smart List
                final_longs = dyn_long.copy()
                final_shorts = dyn_short.copy()

                # tra is a CASH (non-margin) account — never populate the shorts
                # list. Without this guard the leaderboard loader pulls in
                # winners/losers + ALWAYS_TRADEABLE and re-adds short candidates,
                # undoing the empty symbols_tra_short.json.
                _tra_long_only_acc = (acc == 'tra' and getattr(config, 'TRA_LONG_ONLY', True))
                if _tra_long_only_acc:
                    final_shorts = set()

                # B. Merge Explicit Files (FORCE MONITOR these)
                # This ensures your requested "symbols_{acc}_long" are ALWAYS included
                file_longs = await self._get_symbol_set(f"symbols_{acc}_long")
                file_shorts = await self._get_symbol_set(f"symbols_{acc}_short")

                final_longs.update(file_longs)
                if not _tra_long_only_acc:
                    final_shorts.update(file_shorts)

                # C. Whitelist is NEVER a source of new symbols — JSON files are the sole gate.
                # ALWAYS_TRADEABLE / always_tradeable_trb can only RE-AFFIRM symbols already
                # present in the JSON files. A symbol not in symbols_{acc}_long.json CANNOT
                # be monitored as long, and same for shorts. No exceptions.
                acc_whitelist = whitelist.copy()
                if acc == 'trb' and hasattr(self, 'always_tradeable_trb'):
                    acc_whitelist.update(self.always_tradeable_trb)

                for sym in acc_whitelist:
                    sym = sym.strip().upper()
                    if not sym: continue
                    # Only add if the symbol is already in the JSON file for that side.
                    if sym in file_longs:
                        final_longs.add(sym)
                    if not _tra_long_only_acc and sym not in self.non_shortable_symbols and sym in file_shorts:
                        final_shorts.add(sym)

                # D. Save to Memory
                setattr(self, f"symbols_long_{acc}", sorted(list(final_longs)))
                setattr(self, f"symbols_short_{acc}", sorted(list(final_shorts)))
                
                # logger.info(f"[{acc}] Loaded Lists: Long={len(final_longs)} Short={len(final_shorts)}")

        except Exception as e:
            logger.error(f"[LEADERBOARDS] Error: {e}",  exc_info=True )


    async def _get_symbol_set(self, filename: str, is_context: bool = False) -> set:
        try:
            # Context files (winners/losers) are in DATA_DIR. Targets are in BASE_PATH.
            dir_path = config.BASE_PATH if filename.startswith('symbols') else config.DATA_DIR
            path = dir_path / f"{filename}.json"
            
            if not path.exists(): return set()

            data = await load_json_safe(path)
            if not data: return set()

            if isinstance(data, dict):
                return {str(k).upper().strip() for k in data.keys()}

            # Handle List of dicts: [{"symbol": "SNDK", "score": 100.0}, ...]
            if isinstance(data, list) and len(data) > 0 and isinstance(data[0], dict):
                return {item['symbol'].upper().strip() for item in data if 'symbol' in item}

            # Handle List of Strings: ["UNH", "MSTR"]
            if isinstance(data, list):
                return {str(s).upper().strip() for s in data if s}

            return set()
        except Exception as e:
            logger.error(f"Error loading {filename}: {e}")
            return set()

    async def reload_account_symbols(self):
        """Strictly loads symbols_{acc}_long.json from BASE_PATH. For tra+SATOSHIT_ONLY: reads _satoshit_ files instead."""
        try:
            found_any = False
            for acc in self.target_accounts:
                # TRA_SATOSHIT_ONLY: read from scanner-generated files that tradier_rankings doesn't overwrite
                if getattr(config, 'TRA_SATOSHIT_ONLY', False) and acc == 'tra':
                    l_path = config.BASE_PATH / f"symbols_{acc}_satoshit_long.json"
                    s_path = config.BASE_PATH / f"symbols_{acc}_satoshit_short.json"
                    # Fallback to regular files if satoshit files don't exist yet
                    if not l_path.exists(): l_path = config.BASE_PATH / f"symbols_{acc}_long.json"
                    if not s_path.exists(): s_path = config.BASE_PATH / f"symbols_{acc}_short.json"
                else:
                    l_path = config.BASE_PATH / f"symbols_{acc}_long.json"
                    s_path = config.BASE_PATH / f"symbols_{acc}_short.json"
                
                l_list, s_list = [], []
                if l_path.exists():
                    with open(l_path, 'r') as f:
                        l_list = [str(s).upper().strip() for s in json.load(f) if s]
                        setattr(self, f"symbols_long_{acc}", l_list)
                
                if s_path.exists():
                    with open(s_path, 'r') as f:
                        s_list = [str(s).upper().strip() for s in json.load(f) if s]
                        setattr(self, f"symbols_short_{acc}", s_list)
                
                if l_list or s_list:
                    found_any = True
                    # We use 'Sc:' so this log passes your WeatherLogFilter
                    logger.info(f"☀️ [LOAD] {acc.upper()} Sc: {len(l_list)}L / {len(s_list)}S")

            if not found_any:
                logger.warning(f"☁️ [LOAD] No symbols found in {config.BASE_PATH}. Sc: 0")
        except Exception as e:
            logger.error(f"!! [LOAD_ERR] Sc: {e}")

    async def periodic_cleanup(self):
        """Periodic cleanup task"""
        while self.running:
            try:
                await asyncio.sleep(30)  # Run every 30 seconds instead of 600
                # AGGRESSIVE cleanup: Remove processing_keys stuck >30 seconds
                if self.processing_keys:
                    now_ts = time.time()
                    stale_keys = []
                    for position_key in list(self.processing_keys):
                        added_at = self.processing_keys.get_added_at(position_key)
                        last_processed = self.position_last_processed.get(position_key, 0)
                        # Use added_at if available, otherwise last_processed, otherwise assume stale
                        age = 0
                        if added_at > 0:
                            age = now_ts - added_at
                        elif last_processed > 0:
                            age = now_ts - last_processed
                        else:
                            age = 999  # No timestamp - assume very stale
                        if age > 30:  # 30 seconds
                            stale_keys.append(position_key)
                    for key in stale_keys:
                        self.processing_keys.discard(key)
                    if stale_keys:
                        logger.warning(f"[CLEANUP] Removed {len(stale_keys)} stale processing_keys (stuck >30s): {stale_keys[:5]}")
                if self.indicators_cache:
                    old_keys = [k for k, v in self.indicators_cache.items() if time.time() - self.last_indicators_update > 3600]
                    for key in old_keys:
                        self.indicators_cache.pop(key, None)
                    if old_keys:
                        logger.debug(f"[CLEANUP] Removed {len(old_keys)} stale indicator entries")
            except asyncio.CancelledError:
                logger.info("[update_all_positions_prices] Task cancelled")
                break
            except Exception as e:
                logger.error(f"[update_all_positions_prices] Error: {e}",  exc_info=True )
                await asyncio.sleep(1)
    
    async def initialize_all_monitored_positions(self):
        if not self.position_manager: return
        
        await self.load_leaderboards()
        
        # Context needed ONLY for Whitelist logic
        w20 = await self._get_symbol_set("winners_20")
        w15 = await self._get_symbol_set("winners_15m")
        l20 = await self._get_symbol_set("losers_20")
        l15 = await self._get_symbol_set("losers_15m")
        
        valid_longs_ctx = w20.union(w15)
        valid_shorts_ctx = l20.union(l15)
        context_avail = len(valid_longs_ctx) > 0

        count = 0
        
        for acc in self.target_accounts:
            # 1. Get the base list (Populated by load_leaderboards)
            c_long = set(getattr(self, f"symbols_long_{acc}", []))
            c_short = set(getattr(self, f"symbols_short_{acc}", []))

            # 2. JSON files are the SOLE source of monitored symbols.
            # ALWAYS_TRADEABLE / always_tradeable_trb cannot inject symbols that are
            # absent from symbols_{acc}_long.json or symbols_{acc}_short.json.
            # c_long and c_short were already populated from those files above — no injection.

            # 3. Create Positions
            for sym in c_long:
                pk = f"{acc}:{sym}_LONG"
                if pk not in self.position_manager.positions:
                    await self.ensure_position_present(acc, sym, 'LONG', pk)
                    count += 1

            for sym in c_short:
                if sym in self.non_shortable_symbols: continue
                pk = f"{acc}:{sym}_SHORT"
                if pk not in self.position_manager.positions:
                    await self.ensure_position_present(acc, sym, 'SHORT', pk)
                    count += 1

        if count > 0:
            logger.info(f"[INIT] Created {count} placeholders")
            await self.position_manager.save_all_positions()

    _phantom_killed_keys = set()

    async def sync_real_positions_from_api(self):
        try:
            for account_key in self.target_accounts:
                client = TradierAPIClient(config, account_key=account_key)
                await client.connect()
                
                # 1. Get Active Positions from API
                api_positions = await client.get_account_positions(account_key)
                await client.close()
                
                # Handle API response structure
                pos_list =[]
                if isinstance(api_positions, dict):
                    if 'positions' in api_positions:
                        p = api_positions['positions']
                        if p == 'null' or p is None:
                            pass
                        elif isinstance(p, dict) and 'position' in p:
                            pos_list = p['position'] if isinstance(p['position'], list) else [p['position']]
                        elif isinstance(p, list):
                            pos_list = p
                    elif 'position' in api_positions:
                        pos_list = api_positions['position'] if isinstance(api_positions['position'], list) else [api_positions['position']]
                elif isinstance(api_positions, list):
                    pos_list = api_positions

                logger.info(f"[SYNC] API returned {len(pos_list)} active positions for {account_key}")

                # Map API data for easy lookup
                api_map = {}
                for pos in pos_list:
                    sym = pos.get('symbol')
                    if sym:
                        api_map[sym.upper()] = pos

                # 2. Iterate ALL Monitored Symbols for this account
                longs = getattr(self, f"symbols_long_{account_key}",[])
                shorts = getattr(self, f"symbols_short_{account_key}",[])
                all_symbols = set(longs + shorts)
                
                if self.position_manager:
                    for pk, pos in self.position_manager.positions.items():
                        if pk.startswith(f"{account_key}:") and pos and getattr(pos, 'symbol', None):
                            all_symbols.add(pos.symbol)
                
                # Add any symbols found in API that we might not be tracking
                for sym in api_map.keys():
                    all_symbols.add(sym)

                updated_count = 0
                now = datetime.now(timezone.utc)

                for symbol in all_symbols:
                    symbol = symbol.upper()
                    
                    # Check for active data from API
                    api_data = api_map.get(symbol)
                    
                    # Determine values
                    if api_data:
                        raw_qty = float(api_data.get('quantity', 0))
                        cost_basis = float(api_data.get('cost_basis', 0))
                        avg_price = abs(cost_basis / raw_qty) if raw_qty != 0 else 0.0
                        date_acquired = api_data.get('date_acquired', '')
                        unrealized_profit = float(api_data.get('unrealized_profit', 0))
                        realized_profit = float(api_data.get('realized_profit', 0))
                    else:
                        raw_qty = 0.0
                        avg_price = 0.0
                        date_acquired = ""
                        unrealized_profit = 0.0
                        realized_profit = 0.0

                    # Get current price (5s cap per symbol to prevent startup hang)
                    current_price = 0.0
                    try:
                        current_price, ts = await asyncio.wait_for(self.get_current_price(symbol), timeout=5.0)
                        current_price = float(current_price)
                        if current_price <= 0:
                            price_cache_entry = self.price_cache.get(symbol)
                            if price_cache_entry:
                                current_price = float(price_cache_entry.get('price', 0) or 0)
                    except (asyncio.TimeoutError, Exception):
                        pass

                    # 3. Update LONG side — create if missing from memory but in API
                    long_key = f"{account_key}:{symbol}_LONG"
                    long_pos = self.position_manager.positions.get(long_key) if self.position_manager else None
                    if not long_pos and raw_qty > 0 and self.position_manager:
                        try:
                            from tradier_positions import TradierPosition
                            long_pos = TradierPosition(symbol=symbol, position_side="LONG", positionAmt=0.0, entry_price=0.0, mark_price=0.0)
                            self.position_manager.positions[long_key] = long_pos
                            logger.info(f"[SYNC] Created missing position {long_key} from API (qty={raw_qty})")
                        except Exception as e:
                            logger.error(f"[SYNC] Failed to create {long_key}: {e}")
                    if long_pos:
                        if raw_qty > 0:
                            long_pos.positionAmt = abs(raw_qty)
                            if avg_price > 0: long_pos.entry_price = avg_price
                            if date_acquired: long_pos.entry_time = date_acquired
                            long_pos.unrealized_pnl = unrealized_profit
                            long_pos.realized_pnl = realized_profit
                            long_pos.price_fetch_count = getattr(long_pos, 'price_fetch_count', 0) + 1
                        
                        # Update mark price
                        if current_price > 0:
                            long_pos.mark_price = current_price
                            long_pos.mark_price_last_updated = now
                        
                        # Calculate gain and update ALL related fields
                        entry_price = long_pos.entry_price if long_pos.entry_price > 0 else (current_price if current_price > 0 else 0.0)
                        if long_pos.positionAmt == 0.0:
                            long_pos.gain = 0.0
                        elif entry_price > 0 and current_price > 0:
                            base_entry = entry_price if entry_price > 0 else current_price
                            if base_entry > 0 and not (base_entry < current_price * 0.01 or base_entry > current_price * 100):
                                long_pos.gain = calculate_gain('LONG', current_price, base_entry)
                                if long_pos.positionAmt > 0:
                                    long_pos.max_gain = max(long_pos.gain, long_pos.max_gain)
                        
                        # Apply deteriorations
                        if self.position_manager and hasattr(self.position_manager, 'deteriorate_max_gain'):
                            if long_pos.max_gain != 0.0 and long_pos.positionAmt == 0.0:
                                deteriorated_max_gain = self.position_manager.deteriorate_max_gain(account_key, long_pos)
                                if abs(deteriorated_max_gain - long_pos.max_gain) > 0.01:
                                    long_pos.max_gain = deteriorated_max_gain
                            if long_pos.positionAmt > 0 and current_price > 0:
                                deteriorated_max_qty = self.position_manager.deteriorate_max_quantity(account_key, long_pos, current_price)
                                if long_pos.positionAmt > deteriorated_max_qty:
                                    long_pos.max_quantity = long_pos.positionAmt
                                else:
                                    long_pos.max_quantity = deteriorated_max_qty
                        
                        # Update other calculated fields
                        if long_pos.positionAmt > 0 and entry_price > 0 and current_price > 0:
                            long_pos.unrealized_pnl = (current_price - entry_price) * long_pos.positionAmt
                        
                        # Set ALL timestamp fields
                        long_pos.last_updated = now
                        if not getattr(long_pos, 'opened_at', None) and long_pos.positionAmt > 0:
                            long_pos.opened_at = now
                        
                        updated_count += 1

                    # 4. Update SHORT side — create if missing from memory but in API
                    short_key = f"{account_key}:{symbol}_SHORT"
                    short_pos = self.position_manager.positions.get(short_key) if self.position_manager else None
                    if not short_pos and raw_qty < 0 and self.position_manager:
                        try:
                            from tradier_positions import TradierPosition
                            short_pos = TradierPosition(symbol=symbol, position_side="SHORT", positionAmt=0.0, entry_price=0.0, mark_price=0.0)
                            self.position_manager.positions[short_key] = short_pos
                            logger.info(f"[SYNC] Created missing position {short_key} from API (qty={raw_qty})")
                        except Exception as e:
                            logger.error(f"[SYNC] Failed to create {short_key}: {e}")
                    if short_pos:
                        if raw_qty < 0:
                            short_pos.positionAmt = abs(raw_qty)
                            if avg_price > 0: short_pos.entry_price = avg_price
                            if date_acquired: short_pos.entry_time = date_acquired
                            short_pos.unrealized_pnl = unrealized_profit
                            short_pos.realized_pnl = realized_profit
                            short_pos.price_fetch_count = getattr(short_pos, 'price_fetch_count', 0) + 1
                        
                        # Update mark price
                        if current_price > 0:
                            short_pos.mark_price = current_price
                            short_pos.mark_price_last_updated = now
                        
                        # Calculate gain and update ALL related fields
                        entry_price = short_pos.entry_price if short_pos.entry_price > 0 else (current_price if current_price > 0 else 0.0)
                        if short_pos.positionAmt == 0.0:
                            short_pos.gain = 0.0
                        elif entry_price > 0 and current_price > 0:
                            base_entry = entry_price if entry_price > 0 else current_price
                            if base_entry > 0 and not (base_entry < current_price * 0.01 or base_entry > current_price * 100):
                                short_pos.gain = calculate_gain('SHORT', current_price, base_entry)
                                if short_pos.positionAmt > 0:
                                    short_pos.max_gain = max(short_pos.gain, short_pos.max_gain)
                        
                        # Apply deteriorations
                        if self.position_manager and hasattr(self.position_manager, 'deteriorate_max_gain'):
                            if short_pos.max_gain != 0.0 and short_pos.positionAmt == 0.0:
                                deteriorated_max_gain = self.position_manager.deteriorate_max_gain(account_key, short_pos)
                                if abs(deteriorated_max_gain - short_pos.max_gain) > 0.01:
                                    short_pos.max_gain = deteriorated_max_gain
                            if short_pos.positionAmt > 0 and current_price > 0:
                                deteriorated_max_qty = self.position_manager.deteriorate_max_quantity(account_key, short_pos, current_price)
                                if short_pos.positionAmt > deteriorated_max_qty:
                                    short_pos.max_quantity = short_pos.positionAmt
                                else:
                                    short_pos.max_quantity = deteriorated_max_qty
                        
                        # Update other calculated fields
                        if short_pos.positionAmt > 0 and entry_price > 0 and current_price > 0:
                            short_pos.unrealized_pnl = (entry_price - current_price) * short_pos.positionAmt
                        
                        # Set ALL timestamp fields
                        short_pos.last_updated = now
                        if not getattr(short_pos, 'opened_at', None) and short_pos.positionAmt > 0:
                            short_pos.opened_at = now
                        
                        updated_count += 1

                api_symbols_with_side = set()
                for sym, pos_data in api_map.items():
                    qty = float(pos_data.get('quantity', 0))
                    if qty > 0:
                        api_symbols_with_side.add(f"{account_key}:{sym}_LONG")
                    elif qty < 0:
                        api_symbols_with_side.add(f"{account_key}:{sym}_SHORT")
                zeroed_count = 0
                if self.position_manager:
                    for pk, pos in list(self.position_manager.positions.items()):
                        if not pk.startswith(f"{account_key}:"):
                            continue
                        sym = getattr(pos, 'symbol', '')
                        if not sym or len(sym) > 5:
                            continue
                        amt = abs(float(getattr(pos, 'positionAmt', 0) or 0))
                        if amt > 0 and pk not in api_symbols_with_side:
                            logger.warning(f"[SYNC] PHANTOM KILL: {pk} has amt={amt} in memory but NOT in API — removing from memory")
                            pos.positionAmt = 0.0
                            pos.gain = 0.0
                            pos.unrealized_pnl = 0.0
                            pos.last_updated = now
                            # 2026-04-29: reset cycle_peak_gain only (NOT max_gain — separate decay path)
                            try: pos.cycle_peak_gain = 0.0
                            except Exception: pass
                            self._phantom_killed_keys.add(pk)
                            zeroed_count += 1
                    for pk in list(self._phantom_killed_keys):
                        if pk in self.position_manager.positions:
                            _pk_pos = self.position_manager.positions[pk]
                            _pk_was_red = getattr(_pk_pos, 'was_reduced', False)
                            _pk_red_time = getattr(_pk_pos, 'last_reduction_time', None)
                            _pk_red_age_h = 999
                            if _pk_red_time:
                                try:
                                    _rt = _pk_red_time if not isinstance(_pk_red_time, str) else datetime.fromisoformat(str(_pk_red_time).replace("Z", "+00:00"))
                                    if _rt.tzinfo is None: _rt = _rt.replace(tzinfo=timezone.utc)
                                    _pk_red_age_h = (now - _rt).total_seconds() / 3600
                                except Exception: pass
                            if _pk_was_red and _pk_red_age_h < 4:
                                logger.info(f"[SYNC] PRESERVE reentry candidate {pk}: reduced {_pk_red_age_h:.1f}h ago — NOT deleting")
                                self._phantom_killed_keys.discard(pk)
                                continue
                            del self.position_manager.positions[pk]
                logger.info(f"[SYNC] Synchronized {updated_count} positions, zeroed {zeroed_count} phantoms for {account_key}")

            # Force a save to disk to persist the sync
            if self.position_manager and hasattr(self.position_manager, 'save_all'):
                await self.position_manager.save_all()

        except Exception as e:
            logger.error(f"[SYNC] Critical error syncing positions: {e}",  exc_info=True )

    async def validate_and_complete_position(self, position_key: str, position: Any) -> bool:
        """Validate that a position has ALL required fields, fill in missing ones"""
        if not position:
            return False
        
        required_fields = [
            'symbol', 'position_side', 'positionAmt', 'entry_price', 'mark_price',
            'unrealized_pnl', 'realized_pnl', 'gain', 'max_gain', 'prev_gain',
            'max_quantity', 'last_augmentation_amount', 'last_augmentation_price',
            'last_reduction_amount', 'last_reduction_price', 'max_positionSize',
            'was_reentered', 'was_reduced', 'last_signal', 'augment_reason',
            'reduction_reason', 'price_fetch_count'
        ]
        
        now = datetime.now(timezone.utc)
        
        # Check and set missing fields
        missing_fields = []
        
        for field in required_fields:
            if not hasattr(position, field):
                missing_fields.append(field)
                
                # Set default values based on field type
                if field in ['symbol', 'position_side', 'last_signal', 'augment_reason', 'reduction_reason']:
                    setattr(position, field, '')
                elif field in ['positionAmt', 'entry_price', 'mark_price', 'unrealized_pnl', 'realized_pnl',
                            'gain', 'max_gain', 'prev_gain', 'max_quantity', 'last_augmentation_amount',
                            'last_augmentation_price', 'last_reduction_amount', 'last_reduction_price',
                            'max_positionSize']:
                    setattr(position, field, 0.0)
                elif field in ['was_reentered', 'was_reduced']:
                    setattr(position, field, False)
                elif field == 'price_fetch_count':
                    setattr(position, field, 0)
        
        # Ensure datetime fields exist
        datetime_fields = [
            'last_augmentation_time', 'last_reduction_time', 'opened_at',
            'last_updated', 'prev_gain_last_updated', 'mark_price_last_updated'
        ]
        
        for field in datetime_fields:
            if not hasattr(position, field):
                setattr(position, field, None)
        
        # Ensure nullable fields exist
        nullable_fields = ['stop_loss', 'take_profit', 'entry_time', 'last_update']
        for field in nullable_fields:
            if not hasattr(position, field):
                setattr(position, field, None)
        
        # Update timestamps
        if not position.last_updated:
            position.last_updated = now
        
        if missing_fields:
            logger.debug(f"[VALIDATE] Position {position_key} had missing fields: {missing_fields}")
        
        return True

    async def validate_positions_periodic(self):
        """Validates API holdings to clean ghost positions every 45s"""
        logger.info("[validate_pos itions_periodic] Task started")
        while self.running:
            try:
                await asyncio.sleep(45)
                await self.sync_real_positions_from_api()
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[VALIDATE] Error: {e}")
                await asyncio.sleep(5)

    def _update_position_entry(self, account_key: str, symbol: str, position_side: str, position: TradierPosition):
        if not position or not symbol or not position_side:
            return
        pk = construct_position_key(account_key, symbol, position_side)
        self.positions_by_account[account_key][pk] = position
        self.positions[pk] = position
        self._position_update_timestamps[pk] = datetime.now(timezone.utc)
        position.last_updated = datetime.now(timezone.utc)
        
    async def is_order_locked(self, position_key: str) -> bool:
        """Compatibility helper for ez_manage queu e_trade_action."""
        if not position_key:
            return False
        try:
            keys = [position_key]
            if not position_key.startswith("execute_now:"):
                keys.append(f"execute_now:{position_key}")
            keys.append(f"execute_now:{position_key}:BUY")
            keys.append(f"execute_now:{position_key}:SELL")
            checked = set(keys)
            if self.redis_manager:
                for key in checked:
                    try:
                        value = await self.redis_manager.get(key)
                        if value is not None:
                            return True
                    except Exception:
                        continue
            now = time.time()
            for key in checked:
                lock_payload = self.active_order_locks.get(key)
                if lock_payload and now - lock_payload.get('timestamp', 0.0) < 180.0:
                    return True
                local_payload = self._local_locks.get(key)
                if local_payload and local_payload.get('expires', 0.0) > now:
                    return True
            return False
        except Exception as e:
            logger.debug(f"[is_order_locked] Error checking {position_key}: {e}")
            return False
                
    async def cleanup_stale_execution_locks(self):
        now = time.time()
        pending_ttl = 70.0
        complete_ttl = 45.0
        stale = []
        for key, payload in list(self.active_order_locks.items()):
            ts = payload.get('timestamp', 0.0)
            status = payload.get('status', 'pending')
            ttl = pending_ttl if status == 'pending' else complete_ttl
            if now - ts > ttl:
                stale.append(key)
        for key in stale:
            self.active_order_locks.pop(key, None)
            
    async def periodic_lock_cleanup_loop(self):
        logger.info("[periodic_lock_cleanup]  Task started")
        while self.running:
            try:
                await self.cleanup_stale_execution_locks()
                await asyncio.sleep(30)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[periodic_lock_cleanup] Error: {e}")
                await asyncio.sleep(5)
                
    async def heartbeat_loop(self):
        logger.info("[heartbeat_loop]  Task started")
        heartbeat_file = Path.home() / "logs" / "tradier_manage_heartbeat"
        while self.running:
            try:
                heartbeat_file.parent.mkdir(parents=True, exist_ok=True)
                heartbeat_file.write_text(datetime.now(timezone.utc).isoformat())
                await asyncio.sleep(30)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.warning(f"[heartbeat_loop] Error writing heartbeat: {e}")
                await asyncio.sleep(5)
                
    async def log_timing_summary_loop(self):
        logger.info("[log_timing_summary]  Task started")
        while self.running:
            try:
                await asyncio.sleep(60)
                queue_sizes = {ak: len(data.get('queue', [])) for ak, data in self.account_position_queues.items()}
                processing_len = len(self.processing_keys)
                order_queue_depth = self.order_queue._orders.qsize() if self.order_queue else 0
                logger.info(f"[TIMING] queues={queue_sizes} processing={processing_len} order_queue={order_queue_depth}")
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.debug(f"[log_timing_summary] Error: {e}")
                
    async def symbol_watchdog_loop(self):
        logger.info("[symbol_watchdog]  Task started")
        while self.running:
            try:
                await asyncio.sleep(30)
                now = time.time()
                stale_positions = [pk for pk, ts in self.last_monitored_positions.items() if now - ts > 60]
                for pk in stale_positions[:100]:
                    account_key = pk.split(':', 1)[0]
                    queue = self.account_position_queues[account_key]
                    if pk not in queue['set']:
                        queue['queue'].append(pk)
                        queue['set'].add(pk)
                        logger.warning(f"[symbol_watchdog] Re-queued stale position {pk}")
                stale_symbols = [sym for sym, ts in self.last_symbol_monitored.items() if now - ts > 90]
                for symbol in stale_symbols[:50]:
                    indicators = self.get_indicators(symbol)
                    if indicators:
                        self.indicators_cache[symbol.upper()] = indicators
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.debug(f"[symbol_watchdog] Error: {e}")
    
    async def market_data_sync_loop(self):
        """Safe-Path: Read NEWEST timestamped tradier_indicators file. NEVER use latest (gets stuck in OS cache)."""
        logger.info("🔍 [SAFE-PATH] File Poller Active — reading newest timestamped tradier_indicators every 1s")
        while self.running:
            try:
                # Find newest timestamped file (NOT latest)
                ts_files = sorted(config.DATA_DIR.glob("tradier_indicators_[0-9]*.json"), key=lambda f: f.stat().st_mtime, reverse=True)
                json_path = ts_files[0] if ts_files else (config.DATA_DIR / "tradier_indicators_latest.json")
                if json_path.exists():
                    mtime = os.path.getmtime(json_path)
                    if mtime > self.last_json_mtime:
                        try:
                            with open(json_path, 'r') as f:
                                raw_data = json.loads(f.read())
                            if isinstance(raw_data, dict) and len(raw_data) > 0:
                                new_snapshot = {}
                                for sym, vals in raw_data.items():
                                    if isinstance(vals, dict):
                                        new_snapshot[sym.upper()] = self._adapt_indicators_for_ez_manage(vals)
                                self.market_snapshot = new_snapshot
                                self.last_json_mtime = mtime
                                logger.info(f"💾 [SAFE-PATH] Snapshot loaded: {len(new_snapshot)} symbols from {json_path.name}")
                        except Exception as je:
                            logger.warning(f"[SAFE-PATH] JSON read error (will retry): {je}")
                await asyncio.sleep(1.0)
            except Exception as e:
                logger.error(f"Sync Loop Error: {e}")
                await asyncio.sleep(5)
    
    async def market_snapshot_refresh_loop(self, interval: float = 5.0):
        logger.info("[market_snapshot_refresh]  Task started")
        while self.running:
            try:
                await asyncio.sleep(interval)
                sample_symbols = (self.symbols_long_trc + self.symbols_short_trc)[:20]
                for symbol in sample_symbols:
                    self.get_indicators(symbol)
                self.last_indicators_update = time.time()
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.debug(f"[market_snapshot_refresh] Error: {e}")
                
    async def enforce_data_sync_loop(self, interval: float = 60.0):
        logger.info("[enforce_data_sync]  Task started")
        while self.running:
            try:
                self._sync_positions_from_manager()
                await asyncio.sleep(interval)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[enforce_data_sync] Error: {e}")
                await asyncio.sleep(5)

    async def refresh_augmented_reduced_loop(self, interval: float = 10.0):
        logger.info("[refresh_augmented_reduced]  Task started")
        while self.running:
            try:
                await asyncio.sleep(interval)
                now = time.time()
                self.augmentation_cooldown_map = {k: v for k, v in self.augmentation_cooldown_map.items() if now - v < 600}
                self.reduction_cooldown_map = {k: v for k, v in self.reduction_cooldown_map.items() if now - v < 600}
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.debug(f"[refresh_augmented_reduced] Error: {e}")
                
    async def cleanup_stale_locks_loop(self):
        logger.info("[cleanup_stale_locks]  Task started")
        while self.running:
            try:
                now = time.time()
                async with self.dedupe_lock:
                    stale = []
                    for key, ts in list(self.order_deduplication.items()):
                        if isinstance(ts, (int, float)):
                            ts_val = float(ts)
                        elif isinstance(ts, dict):
                            ts_val = float(ts.get('timestamp', 0.0) or 0.0)
                        else:
                            ts_val = 0.0
                        if ts_val and (now - ts_val) > 120.0:
                            stale.append(key)
                    for key in stale:
                        self.order_deduplication.pop(key, None)
                await asyncio.sleep(20)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.debug(f"[cleanup_stale_locks] Error: {e}")
                
    async def monitor_memory_loop(self, max_gb: float = 3.0):
        logger.info("[monitor_memory]  Task started")
        process = psutil.Process(os.getpid())
        while self.running:
            try:
                await asyncio.sleep(60)
                mem_gb = process.memory_info().rss / (1024 ** 3)
                if mem_gb > max_gb:
                    logger.warning(f"[monitor_memory] Memory usage high: {mem_gb:.2f} GB")
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.debug(f"[monitor_memory] Error: {e}")
                
                
    async def metrics_server_loop(self, host: str = "127.0.0.1", port: int = 8820):
        logger.info(f"[metrics_server]  Starting on {host}:{port}")
        async def handler(reader, writer):
            data = {
                "timestamp": time.time(),
                "queues": {ak: len(qd.get('queue', [])) for ak, qd in self.account_position_queues.items()},
                "processing": len(self.processing_keys),
                "order_queue": self.order_queue._orders.qsize() if self.order_queue else 0
            }
            writer.write((json.dumps(data) + "\n").encode())
            await writer.drain()
            writer.close()
        try:
            server = await asyncio.start_server(handler, host, port)
        except Exception as e:
            logger.warning(f"[metrics_server] Failed to start: {e}")
            return
        self._metrics_runner = server
        try:
            while self.running:
                await asyncio.sleep(60)
        except asyncio.CancelledError:
            pass
        finally:
            server.close()
            await server.wait_closed()
            logger.info("[metrics_server] Stopped")
    
    async def validate_position_before_trade(self, position_key: str, action: str, proposed_order_qty: float) -> Tuple[bool, str, Optional[float]]:
        try:
            from utils import orjson_default, parse_position_key
            account_key, symbol, position_side = parse_position_key(position_key)
            
            local_pos = self.position_manager.get_position(position_key)
            local_holdings = abs(float(getattr(local_pos, 'positionAmt', 0))) if local_pos else 0.0
            
            # --- ACTION LOGIC ---
            if action in["CLOSE", "REDUCE", "FULL_CLOSE", "PROFIT_TAKE"]:
                if local_holdings > 0.00001:
                    final_qty = min(proposed_order_qty, local_holdings)
                    return True, "VALID_LOCAL", final_qty
                return False, "NO_POS_HELD", 0.0

            if action == "AUGMENT":
                if local_holdings > 0.00001:
                    return True, "VALID", proposed_order_qty
                return False, "CANNOT_AUGMENT_FLAT", 0.0

            if action in ["OPEN", "REENTER", "REENTRY", "QUICK_OPEN", "REVERSE", "HEDGE_OPEN"]:
                if local_holdings > 0.00001:
                    return False, f"ALREADY_HELD ({local_holdings})", local_holdings

                # Ultimate Safety Net: Block execution if opposing position is held!
                opposing_side = "SHORT" if position_side == "LONG" else "LONG"
                from utils import construct_position_key
                opposing_key = construct_position_key(account_key, symbol, opposing_side)
                opposing_pos = self.position_manager.get_position(opposing_key)
                if opposing_pos and abs(float(getattr(opposing_pos, 'positionAmt', 0))) > 0.00001:
                    logger.critical(f"[OPPOSING_HELD_BLOCK] {position_key}: BLOCKED {action} — opposing {opposing_side} position exists (positionAmt={getattr(opposing_pos, 'positionAmt', 0)})")
                    return False, f"OPPOSING_HELD ({opposing_side})", 0.0
                    
                return True, "VALID", proposed_order_qty
            
            return True, "VALID", proposed_order_qty
        except Exception as e:
            logger.error(f"[validate_position_before_trade] Error: {e}",  exc_info=True )
            if action in ["CLOSE", "REDUCE"]: return True, "FAILSAFE_OPEN", proposed_order_qty
            return False, f"EXCEPTION: {str(e)[:50]}", None         
            
    async def redis_listener_indicators(self):
        logger.info("⚡ [FAST-PATH] PubSub Listener Active")
        while self.running:
            try:
                if not self.redis_manager:
                    pass  # DISABLED: tradier does not use server Redis
                client = await self.redis_manager._get_or_connect("local")
                pubsub = client.pubsub()
                await pubsub.subscribe("tradier_indicators_channel")
                while self.running:
                    message = await pubsub.get_message(ignore_subscribe_messages=True, timeout=5.0)
                    if message and message['data']:
                        data = orjson.loads(message['data'])
                        payload = data.get("data", data)
                        if isinstance(payload, dict) and len(payload) > 0:
                            for sym, vals in payload.items():
                                self.market_snapshot[sym.upper()] = self._adapt_indicators_for_ez_manage(vals)
                            self.last_json_mtime = time.time()
                            self.last_market_data_ts = time.time()
            except Exception:
                await asyncio.sleep(5)

    async def sync_positions_loop(self):
        """Continuous sync from tradier_positions via Redis (every 5s). Falls back to disk if Redis unavailable."""
        logger.info("[SyncLoop] Started — source: Redis (tradier_positions) every 5s")
        while self.running:
            try:
                if self.reader.redis_client:
                    await self.reader.sync(self.target_accounts)
                else:
                    ok = await self.reader.connect()
                    if ok:
                        logger.info("[SyncLoop] Redis reconnected")
                        await self.reader.sync(self.target_accounts)
                await asyncio.sleep(5.0)
            except Exception as e:
                logger.error(f"[SyncLoop] Error: {e}")
                self.reader.redis_client = None
                await asyncio.sleep(10)

    async def redis_listener_prices(self):
        """Listen for real-time price updates via Redis Pub/Sub"""
        logger.info("[redis_listener_prices] Task started")
        while self.running:
            try:
                if not self.redis_manager:
                    await asyncio.sleep(10)
                    continue
                client = await self.redis_manager._get_or_connect("local")
                if not client:
                    logger.warning("[redis_listener_prices] No Redis connections available, retrying in 10s")
                    await asyncio.sleep(10)
                    continue

                channel_name = "tradier_prices_channel"
                pubsub = client.pubsub()
                await pubsub.subscribe(channel_name)
                
                while self.running:
                    try:
                        message = await pubsub.get_message(ignore_subscribe_messages=True, timeout=5.0)
                        if message:
                            try:
                                payload = message['data']
                                if isinstance(payload, bytes): payload = payload.decode('utf-8')
                                data = orjson.loads(payload)
                                if data and isinstance(data, dict):
                                    symbol_data = data.get("data", data)
                                    for sym, entry in symbol_data.items():
                                        if not isinstance(entry, dict): continue
                                        p_raw = entry.get('price') or entry.get('last') or entry.get('mark')
                                        if p_raw is None: continue
                                        p = float(p_raw)
                                        t_raw = entry.get('timestamp') or entry.get('date')
                                        if p > 0:
                                            t_obj = parse_ts(t_raw) or datetime.now(timezone.utc)
                                            current = self.price_cache.get(sym, {})
                                            curr_ts = current.get('timestamp', datetime.min.replace(tzinfo=timezone.utc))
                                            if t_obj >= curr_ts:
                                                self.price_cache[sym] = {'price': p, 'timestamp': t_obj}
                            except Exception as e:
                                logger.debug(f"Redis Price Parse Error: {e}")
                        
                        # Fallback for prices if needed? 
                        # Price cache is usually updated by other loops too, but we can add a check here.
                        
                    except Exception as e:
                        logger.debug(f"[redis_listener_prices] Loop error: {e}")
                        await asyncio.sleep(1)
                        if "Timeout" in str(e): break

            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[redis_listener_prices] Connection error: {e}")
                await asyncio.sleep(5)
                
    async def evaluate_orb_entry(self, account_key):
        """Opening Range Breakout (YouTube Consensus): Track first 15min high/low, enter on breakout with RVOL."""
        if not getattr(config, 'ORB_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        # Only run ORB in first 2 hours of session (9:30-11:30 ET = no lunch zone trades)
        if is_lunch_dead_zone(): return
        _est = timezone(timedelta(hours=-4))
        _now_est = datetime.now(timezone.utc).astimezone(_est)
        _now_t = _now_est.time()
        _today_key = _now_est.strftime('%Y%m%d')
        # Reset daily ORB state
        if self._orb_ran_today != _today_key:
            self._orb_ran_today = _today_key
            self._orb_levels = {}
            self._orb_positions = {}
            self._orb_entries_today = {}
        # Only scan for ORB levels in first 20 minutes, trade breakouts until 12:00 ET
        if _now_t > dt_time(12, 0):
            return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        orb_size = getattr(config, f'{account_key.upper()}_ORB_POSITION_SIZE', getattr(config, 'ORB_POSITION_SIZE', 600.0))
        orb_max_per_day = getattr(config, 'ORB_MAX_PER_DAY', 3)
        orb_rvol_min = getattr(config, 'ORB_RVOL_MIN', 1.5)
        _orb_long_budget = getattr(config, 'ORB_LONG_BUDGET', 2000.0)
        _orb_short_budget = getattr(config, 'ORB_SHORT_BUDGET', 2000.0)
        _entries_today = self._orb_entries_today.get(account_key, 0)
        if _entries_today >= orb_max_per_day:
            return
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            # Compute ORB levels from extra indicators (cached)
            orb_high = float(data.get('orb_high', 0) or 0)
            orb_low = float(data.get('orb_low', 0) or 0)
            orb_range = float(data.get('orb_range', 0) or 0)
            if orb_high <= 0 or orb_low <= 0 or orb_range <= 0:
                # Try from enriched cache
                extras = self._get_extras(sym)
                orb_high = float(extras.get('orb_high', 0) or 0)
                orb_low = float(extras.get('orb_low', 0) or 0)
                orb_range = float(extras.get('orb_range', 0) or 0)
            if orb_high <= 0 or orb_range <= 0:
                continue
            orb_midpoint = (orb_high + orb_low) / 2.0
            # RVOL check
            _rvol = safe_fetch_float(data.get('rel_vol_5m', 0))
            if _rvol < orb_rvol_min and _rvol > 0:
                continue
            _entries_today = self._orb_entries_today.get(account_key, 0)
            if _entries_today >= orb_max_per_day:
                break
            # LONG ORB breakout: price > orb_high
            if price > orb_high * 1.001:
                pk = f"{account_key}:{sym}_LONG"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos: continue
                if pk in self._orb_positions: continue
                if long_pct > 0.67: continue
                if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
                target_mult = getattr(config, 'ORB_TARGET_MULT', 1.5)
                stop_price = orb_midpoint if getattr(config, 'ORB_STOP_MIDPOINT', True) else orb_low
                target_price = orb_high + target_mult * orb_range
                qty = max(1, int(orb_size / price))
                if _orb_long_budget > 0:
                    _total_orb_long = sum(v['entry'] * max(1, int(orb_size / v['entry'])) for k, v in self._orb_positions.items() if v.get('direction') == 'LONG')
                    if _total_orb_long + (qty * price) > _orb_long_budget:
                        logger.info(f"[{account_key}] [ORB] long budget cap hit ${_total_orb_long:.0f}/${_orb_long_budget:.0f}")
                        continue
                logger.info(f"[{account_key}] [ORB] LONG BREAKOUT {sym}: price={price:.2f} > orb_high={orb_high:.2f} range={orb_range:.2f} RVOL={_rvol:.2f} stop={stop_price:.2f} target={target_price:.2f} qty={qty}")
                self._orb_positions[pk] = {'entry': price, 'stop': stop_price, 'target': target_price, 'direction': 'LONG', 'orb_range': orb_range, 'opened_at': datetime.now(timezone.utc)}
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"ORB_BREAKOUT_LONG range={orb_range:.2f} RVOL={_rvol:.1f}", 88.0, override_qty=qty)
                self._orb_entries_today[account_key] = _entries_today + 1
            # SHORT ORB breakdown: price < orb_low
            elif price < orb_low * 0.999:
                if sym in self.non_shortable_symbols: continue
                pk = f"{account_key}:{sym}_SHORT"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos: continue
                if pk in self._orb_positions: continue
                if long_pct < 0.33: continue
                if not self.is_symbol_tradeable(sym, account_key, 'SHORT'): continue
                target_mult = getattr(config, 'ORB_TARGET_MULT', 1.5)
                stop_price = orb_midpoint if getattr(config, 'ORB_STOP_MIDPOINT', True) else orb_high
                target_price = orb_low - target_mult * orb_range
                qty = max(1, int(orb_size / price))
                if _orb_short_budget > 0:
                    _total_orb_short = sum(v['entry'] * max(1, int(orb_size / v['entry'])) for k, v in self._orb_positions.items() if v.get('direction') == 'SHORT')
                    if _total_orb_short + (qty * price) > _orb_short_budget:
                        logger.info(f"[{account_key}] [ORB] short budget cap hit ${_total_orb_short:.0f}/${_orb_short_budget:.0f}")
                        continue
                logger.info(f"[{account_key}] [ORB] SHORT BREAKDOWN {sym}: price={price:.2f} < orb_low={orb_low:.2f} range={orb_range:.2f} RVOL={_rvol:.2f} stop={stop_price:.2f} target={target_price:.2f} qty={qty}")
                self._orb_positions[pk] = {'entry': price, 'stop': stop_price, 'target': target_price, 'direction': 'SHORT', 'orb_range': orb_range, 'opened_at': datetime.now(timezone.utc)}
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"ORB_BREAKDOWN_SHORT range={orb_range:.2f} RVOL={_rvol:.1f}", 88.0, override_qty=qty)
                self._orb_entries_today[account_key] = _entries_today + 1

    async def monitor_orb_exits(self, account_key):
        """Monitor ORB positions for stop/target/time exit."""
        if not self._orb_positions: return
        _est = timezone(timedelta(hours=-4))
        _now_est = datetime.now(timezone.utc).astimezone(_est)
        max_hold = getattr(config, 'ORB_MAX_HOLD_MINUTES', 150.0)
        for pk, orb in list(self._orb_positions.items()):
            if not pk.startswith(f"{account_key}:"): continue
            _parts = pk.split(":")
            if len(_parts) < 2: continue
            _sym_side = _parts[1]
            symbol = _sym_side.rsplit("_", 1)[0]
            direction = orb['direction']
            price, _ = await self.get_current_price(symbol)
            price = float(price) if price else 0
            if price <= 0: continue
            entry = orb['entry']
            stop = orb['stop']
            target = orb['target']
            opened = orb.get('opened_at', datetime.now(timezone.utc))
            age_min = (datetime.now(timezone.utc) - opened).total_seconds() / 60.0
            should_exit = False
            exit_reason = ""
            if direction == "LONG":
                gain = ((price - entry) / entry * 100) if entry > 0 else 0
                if price >= target:
                    should_exit = True
                    exit_reason = f"ORB_TARGET_HIT gain={gain:.2f}%"
                elif price <= stop and gain <= 0:
                    should_exit = True
                    exit_reason = f"ORB_STOP_HIT gain={gain:.2f}%"
                elif age_min >= max_hold:
                    should_exit = True
                    exit_reason = f"ORB_TIME_EXIT age={age_min:.0f}min gain={gain:.2f}%"
            else:
                gain = ((entry - price) / entry * 100) if entry > 0 else 0
                if price <= target:
                    should_exit = True
                    exit_reason = f"ORB_TARGET_HIT gain={gain:.2f}%"
                elif price >= stop and gain <= 0:
                    should_exit = True
                    exit_reason = f"ORB_STOP_HIT gain={gain:.2f}%"
                elif age_min >= max_hold:
                    should_exit = True
                    exit_reason = f"ORB_TIME_EXIT age={age_min:.0f}min gain={gain:.2f}%"
            # STRICT_NO_LOSS: NEVER exit at a loss — removed TIME/STOP exemptions that were closing at losses
            if should_exit:
                if gain < 0.1:
                    logger.info(f"[STRICT_NO_LOSS] ORB {pk}: BLOCKED exit at loss gain={gain:.2f}% reason={exit_reason}")
                    continue
                logger.info(f"[{account_key}] [ORB_EXIT] {pk}: {exit_reason}")
                await queue_trade_action(self.order_queue, self, pk, "CLOSE", exit_reason, 90.0)
                self._orb_positions.pop(pk, None)

    async def evaluate_episodic_pivot(self, account_key):
        """Qullamaggie Episodic Pivot: gap up 5%+ on 3x volume → tight consolidation → breakout entry."""
        if not getattr(config, 'EPISODIC_PIVOT_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        ep_size = getattr(config, f'{account_key.upper()}_EP_POSITION_SIZE', getattr(config, 'EP_POSITION_SIZE', 800.0))
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        _entered = 0
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            # Check EP from enriched indicators cache
            extras = self._get_extras(sym)
            ep_detected = extras.get('ep_detected', False)
            if not ep_detected:
                continue
            ep_direction = extras.get('ep_direction', '')
            ep_gap_pct = extras.get('ep_gap_pct', 0)
            ep_vol_mult = extras.get('ep_vol_mult', 0)
            ep_breakout = extras.get('ep_breakout_level', 0)
            ep_consol_days = extras.get('ep_consolidation_days', 0)
            if ep_direction == "LONG":
                pk = f"{account_key}:{sym}_LONG"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos: continue
                if long_pct > 0.67: continue
                if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
                qty = max(1, int(ep_size / price))
                logger.info(f"[{account_key}] [EPISODIC_PIVOT] LONG {sym}: gap={ep_gap_pct:.1f}% vol={ep_vol_mult:.1f}x consol={ep_consol_days}d breakout={ep_breakout:.2f} qty={qty}")
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"EPISODIC_PIVOT_LONG gap={ep_gap_pct:.1f}% vol={ep_vol_mult:.1f}x", 92.0, override_qty=qty)
                _entered += 1
            elif ep_direction == "SHORT":
                if sym in self.non_shortable_symbols: continue
                pk = f"{account_key}:{sym}_SHORT"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos: continue
                if long_pct < 0.33: continue
                if not self.is_symbol_tradeable(sym, account_key, 'SHORT'): continue
                qty = max(1, int(ep_size / price))
                logger.info(f"[{account_key}] [EPISODIC_PIVOT] SHORT {sym}: gap={ep_gap_pct:.1f}% vol={ep_vol_mult:.1f}x consol={ep_consol_days}d breakout={ep_breakout:.2f} qty={qty}")
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"EPISODIC_PIVOT_SHORT gap={ep_gap_pct:.1f}% vol={ep_vol_mult:.1f}x", 92.0, override_qty=qty)
                _entered += 1
        if _entered > 0:
            logger.info(f"[{account_key}] [EPISODIC_PIVOT] {_entered} EP entries queued")

    async def evaluate_clenow_entry(self, account_key):
        """Clenow Exp Regression: rank by slope*R², buy top N, monthly rebalance, SPY regime filter. Sharpe 5.17."""
        if not getattr(config, 'CLENOW_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        # VIX Regime Filter: block if SPY < SMA200
        if getattr(config, 'VIX_REGIME_FILTER_ENABLED', False) and getattr(config, 'CLENOW_REGIME_FILTER', True):
            spy_data = self._get_extras('SPY')
            spy_sma200 = float(self.market_snapshot.get('SPY', {}).get('sma_200_D', 0) or 0)
            spy_price = float(self.market_snapshot.get('SPY', {}).get('current_price', 0) or 0)
            if spy_sma200 > 0 and spy_price > 0 and spy_price < spy_sma200:
                logger.debug(f"[{account_key}] [CLENOW] BLOCKED: SPY regime filter (SPY={spy_price:.0f} < SMA200={spy_sma200:.0f})")
                return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        # Score all symbols by clenow_score from enriched indicators
        scored = []
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            extras = self._get_extras(sym)
            cs = float(data.get('clenow_score', 0) or extras.get('clenow_score', 0) or 0)
            min_score = getattr(config, 'CLENOW_MIN_SCORE', 5.0)
            if cs >= min_score:
                scored.append({'symbol': sym, 'score': cs, 'price': price})
        if len(scored) < 5: return
        scored.sort(key=lambda x: x['score'], reverse=True)
        top_n = getattr(config, 'CLENOW_TOP_N', 20)
        winners = scored[:top_n]
        positions = self.position_manager.get_positions_by_account(account_key)
        clenow_size = getattr(config, 'CLENOW_POSITION_SIZE', 800.0)
        rebal_days = getattr(config, 'CLENOW_REBALANCE_DAYS', 21)
        now_ts = time.time()
        _entered = 0
        for item in winners:
            sym = item['symbol']
            pk = f"{account_key}:{sym}_LONG"
            pos = positions.get(pk)
            has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
            if has_pos:
                # Check rebalance exit
                opened_at = getattr(pos, 'opened_at', None)
                if opened_at:
                    ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at) if isinstance(opened_at, (int, float)) else now_ts
                    age_days = (now_ts - ots) / 86400.0
                    if age_days >= rebal_days and 'CLENOW' in str(getattr(pos, 'augment_reason', '')):
                        _clenow_gain = getattr(pos, 'gain', 0) or 0
                        if _clenow_gain < 0.1:
                            logger.info(f"[{account_key}] [CLENOW] REBALANCE BLOCKED BY STRICT_NO_LOSS {sym}: gain={_clenow_gain:.2f}%")
                        else:
                            logger.info(f"[{account_key}] [CLENOW] REBALANCE EXIT {sym}: held {age_days:.1f}d >= {rebal_days}d gain={_clenow_gain:.2f}%")
                            await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"CLENOW_REBALANCE_{age_days:.0f}d gain={_clenow_gain:.2f}%", 80.0)
                continue
            if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
            qty = max(1, int(clenow_size / item['price']))
            logger.info(f"[{account_key}] [CLENOW] LONG {sym}: score={item['score']:.1f} (slope*R²) qty={qty}")
            await queue_trade_action(self.order_queue, self, pk, "OPEN", f"CLENOW_ENTRY score={item['score']:.1f}", 85.0, override_qty=qty)
            _entered += 1
            if _entered >= 5: break
        if _entered > 0:
            logger.info(f"[{account_key}] [CLENOW] {_entered} entries queued from {len(scored)} scored symbols")

    async def evaluate_smfi_entry(self, account_key):
        """Smart Money Flow Index: enter on bullish divergence (price<SMA but SMFI>SMA). Sharpe 4.20."""
        if not getattr(config, 'SMFI_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        smfi_size = getattr(config, 'SMFI_POSITION_SIZE', 600.0)
        max_per_side = getattr(config, 'SMFI_MAX_PER_SIDE', 5)
        _smfi_long_budget = getattr(config, 'SMFI_LONG_BUDGET', 3000.0)
        _smfi_short_budget = getattr(config, 'SMFI_SHORT_BUDGET', 3000.0)
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        # Count existing SMFI positions
        smfi_long_count = sum(1 for pk, p in positions.items() if pk.endswith('_LONG') and 'SMFI' in str(getattr(p, 'augment_reason', '')) and abs(float(getattr(p, 'positionAmt', 0))) > 0)
        smfi_short_count = sum(1 for pk, p in positions.items() if pk.endswith('_SHORT') and 'SMFI' in str(getattr(p, 'augment_reason', '')) and abs(float(getattr(p, 'positionAmt', 0))) > 0)
        _entered = 0
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            extras = self._get_extras(sym)
            signal = data.get('smfi_signal') or extras.get('smfi_signal', 'NEUTRAL')
            smfi_max_hold = getattr(config, 'SMFI_MAX_HOLD_DAYS', 7)
            if signal == 'LONG' and smfi_long_count < max_per_side and long_pct < 0.67:
                pk = f"{account_key}:{sym}_LONG"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos:
                    if 'SMFI' in str(getattr(pos, 'augment_reason', '')):
                        opened_at = getattr(pos, 'opened_at', None)
                        if opened_at:
                            ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at) if isinstance(opened_at, (int, float)) else time.time()
                            age_days = (time.time() - ots) / 86400.0
                            entry_p = float(getattr(pos, 'entry_price', 0) or 0)
                            gain = (price - entry_p) / entry_p * 100 if entry_p > 0 else 0
                            if age_days >= smfi_max_hold and gain > 0:
                                logger.info(f"[{account_key}] [SMFI] MAX_HOLD EXIT LONG {sym}: age={age_days:.1f}d gain={gain:.1f}%")
                                await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"SMFI_MAX_HOLD_{age_days:.0f}d gain={gain:.1f}%", 82.0)
                    continue
                if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
                qty = max(1, int(smfi_size / price))
                if _smfi_long_budget > 0:
                    _total_smfi_long = sum(float(getattr(p, 'entry_price', 0) or 0) * abs(float(getattr(p, 'positionAmt', 0))) for k, p in positions.items() if k.endswith('_LONG') and 'SMFI' in str(getattr(p, 'augment_reason', '')))
                    if _total_smfi_long + (qty * price) > _smfi_long_budget:
                        logger.info(f"[{account_key}] [SMFI] long budget cap hit ${_total_smfi_long:.0f}/${_smfi_long_budget:.0f}")
                        continue
                logger.info(f"[{account_key}] [SMFI] LONG {sym}: bull divergence (price<SMA, SMFI>SMA) qty={qty}")
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"SMFI_BULL_DIV", 82.0, override_qty=qty)
                smfi_long_count += 1
                _entered += 1
            elif signal == 'SHORT' and smfi_short_count < max_per_side and long_pct > 0.33:
                if sym in self.non_shortable_symbols: continue
                pk = f"{account_key}:{sym}_SHORT"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos: continue
                if not self.is_symbol_tradeable(sym, account_key, 'SHORT'): continue
                qty = max(1, int(smfi_size / price))
                if _smfi_short_budget > 0:
                    _total_smfi_short = sum(float(getattr(p, 'entry_price', 0) or 0) * abs(float(getattr(p, 'positionAmt', 0))) for k, p in positions.items() if k.endswith('_SHORT') and 'SMFI' in str(getattr(p, 'augment_reason', '')))
                    if _total_smfi_short + (qty * price) > _smfi_short_budget:
                        logger.info(f"[{account_key}] [SMFI] short budget cap hit ${_total_smfi_short:.0f}/${_smfi_short_budget:.0f}")
                        continue
                logger.info(f"[{account_key}] [SMFI] SHORT {sym}: bear divergence (price>SMA, SMFI<SMA) qty={qty}")
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"SMFI_BEAR_DIV", 82.0, override_qty=qty)
                smfi_short_count += 1
                _entered += 1
        if _entered > 0:
            logger.info(f"[{account_key}] [SMFI] {_entered} SMFI entries queued")

    async def evaluate_minervini_entry(self, account_key):
        """Minervini SEPA: enter when trend template passes + volume surge. Sharpe 3.68. LONG only."""
        if not getattr(config, 'MINERVINI_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        # VIX regime filter
        if getattr(config, 'VIX_REGIME_FILTER_ENABLED', False):
            spy_sma200 = float(self.market_snapshot.get('SPY', {}).get('sma_200_D', 0) or 0)
            spy_price = float(self.market_snapshot.get('SPY', {}).get('current_price', 0) or 0)
            if spy_sma200 > 0 and spy_price > 0 and spy_price < spy_sma200:
                return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        min_size = getattr(config, 'MINERVINI_POSITION_SIZE', 800.0)
        min_sepa = getattr(config, 'MINERVINI_MIN_SEPA_SCORE', 5)
        _min_long_budget = getattr(config, 'MINERVINI_LONG_BUDGET', 4000.0)  # Total budget cap across MINERVINI longs
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        _entered = 0
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            extras = self._get_extras(sym)
            sepa_pass = data.get('sepa_pass') or extras.get('sepa_pass', False)
            sepa_score = int(data.get('sepa_score', 0) or extras.get('sepa_score', 0) or 0)
            if not sepa_pass or sepa_score < min_sepa: continue
            if long_pct > 0.67: break
            pk = f"{account_key}:{sym}_LONG"
            pos = positions.get(pk)
            has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
            if has_pos:
                # Check exit: hold > max_hold_days or target hit
                opened_at = getattr(pos, 'opened_at', None)
                entry_p = float(getattr(pos, 'entry_price', 0) or 0)
                if opened_at and entry_p > 0:
                    ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at) if isinstance(opened_at, (int, float)) else time.time()
                    age = (time.time() - ots) / 86400.0
                    gain = (price - entry_p) / entry_p * 100
                    max_hold = getattr(config, 'MINERVINI_MAX_HOLD_DAYS', 40)
                    target = getattr(config, 'MINERVINI_TARGET_PCT', 25.0)
                    if (gain >= target or age >= max_hold) and 'MINERVINI' in str(getattr(pos, 'augment_reason', '')):
                        if gain > 0:
                            logger.info(f"[{account_key}] [MINERVINI] EXIT {sym}: gain={gain:.1f}% age={age:.0f}d")
                            await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"MINERVINI_EXIT gain={gain:.1f}%", 80.0)
                continue
            if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
            qty = max(1, int(min_size / price))
            pct_52 = extras.get('sepa_pct_from_52w_high', 0)
            # Budget cap: stop opening once total MINERVINI long exposure exceeds budget (only enforced if > 0)
            if _min_long_budget > 0:
                _total_minervini = sum(float(getattr(p, 'entry_price', 0) or 0) * abs(float(getattr(p, 'positionAmt', 0))) for k, p in positions.items() if 'MINERVINI' in str(getattr(p, 'augment_reason', '')) and k.endswith('_LONG'))
                if _total_minervini + (qty * price) > _min_long_budget:
                    logger.info(f"[{account_key}] [MINERVINI] budget cap hit ${_total_minervini:.0f}/${_min_long_budget:.0f}")
                    break
            logger.info(f"[{account_key}] [MINERVINI] LONG {sym}: SEPA={sepa_score}/6 vol_surge=True pct_52wk={pct_52:.1f}% qty={qty}")
            await queue_trade_action(self.order_queue, self, pk, "OPEN", f"MINERVINI_SEPA score={sepa_score}", 85.0, override_qty=qty)
            _entered += 1
            if _entered >= 3: break

    async def evaluate_connors_rsi_entry(self, account_key):
        """Connors RSI Composite: buy when CRSI < 10 above SMA200, sell > 70. Sharpe 2.06, 85% WR."""
        if not getattr(config, 'CONNORS_RSI_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        crsi_size = getattr(config, 'CONNORS_RSI_POSITION_SIZE', 600.0)
        entry_thresh = getattr(config, 'CONNORS_RSI_ENTRY_THRESHOLD', 10.0)
        exit_thresh = getattr(config, 'CONNORS_RSI_EXIT_THRESHOLD', 70.0)
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        _entered = 0
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            extras = self._get_extras(sym)
            crsi = float(data.get('connors_rsi', 0) or extras.get('connors_rsi', 50) or 50)
            signal = data.get('connors_signal') or extras.get('connors_signal', 'NEUTRAL')
            sma200 = float(data.get('sma_200_D', 0) or 0)
            pk_long = f"{account_key}:{sym}_LONG"
            pos_long = positions.get(pk_long)
            has_long = pos_long and abs(float(getattr(pos_long, 'positionAmt', 0))) > 0
            # Exit check for existing CRSI positions
            if has_long and 'CONNORS' in str(getattr(pos_long, 'augment_reason', '')):
                entry_p = float(getattr(pos_long, 'entry_price', 0) or 0)
                gain = (price - entry_p) / entry_p * 100 if entry_p > 0 else 0
                opened_at = getattr(pos_long, 'opened_at', None)
                age_days = 0.0
                if opened_at:
                    ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at) if isinstance(opened_at, (int, float)) else time.time()
                    age_days = (time.time() - ots) / 86400.0
                max_hold = getattr(config, 'CONNORS_RSI_MAX_HOLD_DAYS', 5)
                if crsi > exit_thresh and gain > 0:
                    logger.info(f"[{account_key}] [CONNORS_RSI] EXIT LONG {sym}: CRSI={crsi:.0f} > {exit_thresh} gain={gain:.1f}%")
                    await queue_trade_action(self.order_queue, self, pk_long, "CLOSE", f"CONNORS_EXIT crsi={crsi:.0f}", 85.0)
                elif age_days >= max_hold and gain > 0:
                    logger.info(f"[{account_key}] [CONNORS_RSI] MAX_HOLD EXIT LONG {sym}: age={age_days:.1f}d gain={gain:.1f}%")
                    await queue_trade_action(self.order_queue, self, pk_long, "CLOSE", f"CONNORS_MAX_HOLD_{age_days:.0f}d gain={gain:.1f}%", 82.0)
                continue
            # Entry: CRSI < threshold + price > SMA200
            if signal == 'LONG' or crsi < entry_thresh:
                if has_long: continue
                if long_pct > 0.67: continue
                if sma200 > 0 and price < sma200: continue  # SMA200 filter
                if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
                qty = max(1, int(crsi_size / price))
                logger.info(f"[{account_key}] [CONNORS_RSI] LONG {sym}: CRSI={crsi:.0f} < {entry_thresh} rsi3={extras.get('connors_rsi3', '?')} streak={extras.get('connors_streak_rsi', '?')} qty={qty}")
                await queue_trade_action(self.order_queue, self, pk_long, "OPEN", f"CONNORS_RSI_ENTRY crsi={crsi:.0f}", 82.0, override_qty=qty)
                _entered += 1
                if _entered >= 3: break

    async def evaluate_spike_fade(self, account_key):
        """BC_161: Contrarian spike fade — SHORT big spikers, LONG big fallers.
        Backtest: +480%, 67% WR, 4.69 W/L, 1.7% DD on 24 stocks × 2yr.
        Entry: >2% move in 30min + K exhausted + 2/3 WT confirms fade direction.
        Exit: 2/3 WT against (standard WT exit in evaluate_stop). Mandatory reentry."""
        if not getattr(config, 'SPIKE_FADE_ENABLED', False):
            return
        thresh = getattr(config, 'SPIKE_FADE_THRESHOLD_PCT', 2.0)
        lb = getattr(config, 'SPIKE_FADE_LOOKBACK_BARS', 6)
        k_exh = getattr(config, 'SPIKE_FADE_K_EXHAUSTION', 70.0)
        pos_size = getattr(config, 'SPIKE_FADE_POSITION_SIZE', 600.0)
        max_pos = getattr(config, 'SPIKE_FADE_MAX_POSITIONS', 10)
        cd_bars = getattr(config, 'SPIKE_FADE_COOLDOWN_BARS', 6)
        if not hasattr(self, '_spike_fade_cooldowns'):
            self._spike_fade_cooldowns = {}
            self._spike_fade_count = 0
        now = time.time()
        active_fades = sum(1 for pk, pos in self.trade_manager.positions.items() if pk.startswith(f"{account_key}:") and hasattr(pos, 'entry_reason') and 'SPIKE_FADE' in str(getattr(pos, 'entry_reason', '')))
        if active_fades >= max_pos:
            return
        snap_size = len(self.trade_manager.market_snapshot)
        if snap_size == 0:
            logger.debug(f"[SPIKE_FADE] {account_key}: market_snapshot empty — no symbols to scan")
            return
        _sf_scanned = 0; _sf_no_price = 0; _sf_no_prev = 0; _sf_below_thresh = 0; _sf_candidates = 0
        for symbol, data in self.trade_manager.market_snapshot.items():
            cd_key = f"{account_key}:{symbol}"
            if now - self._spike_fade_cooldowns.get(cd_key, 0) < cd_bars * 300:
                continue
            _sf_scanned += 1
            current_price = float(data.get('current_price', 0) or 0)
            if current_price <= 0:
                _sf_no_price += 1; continue
            k5m = float(data.get('stoch_k_5m', 50) or 50)
            wt1_5m = float(data.get('wt1_5m', 0) or 0); wt2_5m = float(data.get('wt2_5m', 0) or 0)
            wt1_15m = float(data.get('wt1_15m', 0) or 0); wt2_15m = float(data.get('wt2_15m', 0) or 0)
            wt1_1h = float(data.get('wt1_1h', 0) or 0); wt2_1h = float(data.get('wt2_1h', 0) or 0)
            prev_5m = float(data.get('close_5m_prev', 0) or 0)
            prev_1h = float(data.get('close_1h_prev', 0) or 0)
            if prev_5m <= 0 and prev_1h <= 0:
                _sf_no_prev += 1; continue
            ret_5m = (current_price - prev_5m) / prev_5m * 100 if prev_5m > 0 else 0
            ret_1h = (current_price - prev_1h) / prev_1h * 100 if prev_1h > 0 else 0
            ret_pct = ret_1h if abs(ret_1h) > abs(ret_5m) else ret_5m  # Use whichever shows bigger move
            if ret_pct > thresh and k5m > k_exh:
                wt_short = int(wt1_5m < wt2_5m) + int(wt1_15m < wt2_15m) + int(wt1_1h < wt2_1h)
                if wt_short >= 2:
                    pk = f"{account_key}:{symbol}_SHORT"
                    if pk in self.trade_manager.positions: continue
                    if symbol in getattr(config, 'NON_SHORTABLE', set()): continue
                    qty = max(1, int(pos_size / current_price))
                    reason = f"SPIKE_FADE_SHORT_ret={ret_pct:+.1f}%_k5={k5m:.0f}_wt={wt_short}/3"
                    logger.warning(f"[SPIKE_FADE] {symbol}: {reason} → SHORT {qty} @ ${current_price:.2f}")
                    try:
                        await queue_trade_action(self.order_queue, self.trade_manager, pk, "OPEN", reason, 80.0, override_qty=float(qty))
                        self._spike_fade_cooldowns[cd_key] = now; self._spike_fade_count += 1
                    except Exception as e:
                        logger.error(f"[SPIKE_FADE] {symbol} SHORT error: {e}")
            elif ret_pct < -thresh and k5m < (100 - k_exh):
                wt_long = int(wt1_5m > wt2_5m) + int(wt1_15m > wt2_15m) + int(wt1_1h > wt2_1h)
                if wt_long >= 2:
                    pk = f"{account_key}:{symbol}_LONG"
                    if pk in self.trade_manager.positions: continue
                    qty = max(1, int(pos_size / current_price))
                    reason = f"SPIKE_FADE_LONG_ret={ret_pct:+.1f}%_k5={k5m:.0f}_wt={wt_long}/3"
                    logger.warning(f"[SPIKE_FADE] {symbol}: {reason} → LONG {qty} @ ${current_price:.2f}")
                    try:
                        await queue_trade_action(self.order_queue, self.trade_manager, pk, "OPEN", reason, 80.0, override_qty=float(qty))
                        self._spike_fade_cooldowns[cd_key] = now; self._spike_fade_count += 1
                    except Exception as e:
                        logger.error(f"[SPIKE_FADE] {symbol} LONG error: {e}")
        if self._spike_fade_count % 100 == 0 or _sf_scanned == 0 or (_sf_scanned > 0 and _sf_no_prev > _sf_scanned * 0.9):
            logger.info(f"[SPIKE_FADE] {account_key}: scanned={_sf_scanned} no_price={_sf_no_price} no_prev={_sf_no_prev} below_thresh={_sf_scanned - _sf_no_price - _sf_no_prev} total_trades={self._spike_fade_count}")

    async def spike_fade_result_tracker(self):
        """Monitor SPIKE_FADE positions — when they close, record win/loss. Auto-kill if 3/5 lose."""
        await asyncio.sleep(60.0)
        _known = {}
        _results = []
        _auto_killed = False
        logger.info("[SPIKE_FADE_TRACKER] Started — monitoring stock spike fade outcomes")
        while self.trade_manager.running:
            try:
                for pk, pos in list(self.trade_manager.positions.items()):
                    reason = str(getattr(pos, 'entry_reason', '') or getattr(pos, 'augment_reason', '') or getattr(pos, 'last_signal', '') or '')
                    if 'SPIKE_FADE' not in reason:
                        continue
                    amt = abs(float(getattr(pos, 'positionAmt', 0) or getattr(pos, 'qty', 0) or 0))
                    entry = float(getattr(pos, 'entry_price', 0) or getattr(pos, 'avg_cost', 0) or 0)
                    if amt > 0 and entry > 0 and pk not in _known:
                        _known[pk] = entry
                        logger.info(f"[SPIKE_FADE_TRACKER] Tracking: {pk} entry={entry:.2f}")
                closed = []
                for pk, entry_px in list(_known.items()):
                    pos = self.trade_manager.positions.get(pk)
                    amt = abs(float(getattr(pos, 'positionAmt', 0) or getattr(pos, 'qty', 0) or 0)) if pos else 0
                    if amt == 0:
                        mark = float(getattr(pos, 'mark_price', 0) or getattr(pos, 'current_price', 0) or 0) if pos else 0
                        if mark <= 0:
                            continue
                        is_long = pk.endswith('_LONG')
                        pnl_pct = ((mark - entry_px) / entry_px * 100) if is_long else ((entry_px - mark) / entry_px * 100)
                        _results.append(pnl_pct)
                        closed.append(pk)
                        logger.critical(f"[SPIKE_FADE_TRACKER] {'WIN' if pnl_pct > 0 else 'LOSS'}: {pk} pnl={pnl_pct:+.2f}%")
                for pk in closed:
                    _known.pop(pk, None)
                if len(_results) >= 5 and not _auto_killed:
                    recent = _results[-5:]
                    losses = sum(1 for p in recent if p < 0)
                    if losses >= 3:
                        _auto_killed = True
                        config.SPIKE_FADE_ENABLED = False
                        logger.critical(f"🛑 [SPIKE_FADE_AUTO_KILL] DISABLED stocks — {losses}/5 recent lost. All: {len(_results)} trades, net={sum(_results):+.2f}%")
            except asyncio.CancelledError:
                break
            except Exception as exc:
                logger.error(f"[SPIKE_FADE_TRACKER] Error: {exc}")
            await asyncio.sleep(30.0)

    async def evaluate_rotation_entry(self, account_key):
        """Relative Strength Rotation: rank all stocks by N-day return (config ROTATION_LOOKBACK_DAYS), BUY top N, SHORT bottom N. Hold 5 days then rebalance."""
        if not getattr(config, 'ROTATION_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        scored = []
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            if sym.upper() in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            klines_path = config.KLINES_CACHE_DIR / f"{sym.upper()}_D.json"
            if not klines_path.exists(): continue
            try:
                raw = json.loads(klines_path.read_text())
                bars = raw if isinstance(raw, list) else raw.get('bars', [])
                _lb = getattr(self.config, 'ROTATION_LOOKBACK_DAYS', 10)
                if len(bars) < _lb + 1: continue
                closes = [float(b.get('close') or b.get('c') or 0) for b in bars]
                closes = [c for c in closes if c > 0]
                if len(closes) < _lb + 1: continue
                ret_nd = (closes[-1] - closes[-1 - _lb]) / closes[-1 - _lb] if closes[-1 - _lb] > 0 else 0
                scored.append({'symbol': sym.upper(), 'return_nd': ret_nd, 'price': price})
            except Exception:
                continue
        if len(scored) < 8:
            logger.debug(f"[{account_key}] [ROTATION] Not enough symbols with daily data ({len(scored)})")
            return
        scored.sort(key=lambda x: x['return_nd'], reverse=True)
        # Antonacci absolute-momentum filter: block new longs when SPY 12-month return < 0
        # (bear-market protection — avoids long exposure during sustained downtrends)
        _abs_mom_enabled = getattr(config, 'ROTATION_ANTONACCI_ABS_MOM_ENABLED', True)
        _abs_mom_block_longs = False
        if _abs_mom_enabled:
            try:
                spy_path = config.KLINES_CACHE_DIR / "SPY_D.json"
                if spy_path.exists():
                    _spy_raw = json.loads(spy_path.read_text())
                    _spy_bars = _spy_raw if isinstance(_spy_raw, list) else _spy_raw.get('bars', [])
                    _spy_closes = [float(b.get('close') or b.get('c') or 0) for b in _spy_bars if float(b.get('close') or b.get('c') or 0) > 0]
                    if len(_spy_closes) >= 252:
                        _spy_12m_ret = (_spy_closes[-1] - _spy_closes[-252]) / _spy_closes[-252]
                        if _spy_12m_ret < 0:
                            _abs_mom_block_longs = True
                            logger.info(f"[{account_key}] [ROTATION] Antonacci: SPY 12m={_spy_12m_ret:.1%} < 0 → blocking new longs")
            except Exception:
                pass
        top_n = getattr(config, 'ROTATION_TOP_N', 5)
        bottom_n = getattr(config, 'ROTATION_BOTTOM_N', 3)
        winners = scored[:top_n]
        losers = scored[-bottom_n:]
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        rot_size = getattr(config, 'ROTATION_POSITION_SIZE', 800.0)
        hold_days = getattr(config, 'ROTATION_HOLD_DAYS', 5)
        now_ts = time.time()
        for item in winners:
            sym = item['symbol']
            pk = f"{account_key}:{sym}_LONG"
            pos = positions.get(pk)
            has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
            if has_pos:
                opened_at = getattr(pos, 'opened_at', None)
                if opened_at:
                    ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at) if isinstance(opened_at, (int, float)) else now_ts
                    age_days = (now_ts - ots) / 86400.0
                    if age_days >= hold_days and 'ROTATION' in str(getattr(pos, 'augment_reason', '')):
                        _rot_gain = getattr(pos, 'gain', 0) or 0
                        if _rot_gain < 0.1:
                            logger.info(f"[{account_key}] [ROTATION] REBALANCE BLOCKED BY STRICT_NO_LOSS {sym}: gain={_rot_gain:.2f}%")
                        else:
                            logger.info(f"[{account_key}] [ROTATION] REBALANCE EXIT {sym}: held {age_days:.1f} days >= {hold_days} gain={_rot_gain:.2f}%")
                            await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"ROTATION_REBALANCE_{age_days:.0f}d gain={_rot_gain:.2f}%", 80.0)
                continue
            if _abs_mom_block_longs:
                logger.debug(f"[{account_key}] [ROTATION] Skip LONG {sym}: Antonacci abs-mom bear regime")
                continue
            if long_pct > 0.67:
                logger.debug(f"[{account_key}] [ROTATION] Skip LONG {sym}: L/S ratio too high ({long_pct:.2f})")
                continue
            if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
            # BACKTEST_CHANGE_T47: rotation SMA200 filter — only long when price below SMA200 (mean-reversion)
            if getattr(config, 'ROTATION_SMA200_FILTER', False):
                _rot_sma = safe_fetch_float(data.get('sma_200_D', 0)) or safe_fetch_float(data.get('sma_200_1h', 0))
                if _rot_sma > 0 and item['price'] > _rot_sma:
                    logger.debug(f"[{account_key}] [ROTATION] Skip LONG {sym}: price {item['price']:.2f} > SMA200 {_rot_sma:.2f}")
                    continue
            qty = max(1, int(rot_size / item['price']))
            logger.info(f"[{account_key}] [ROTATION] LONG {sym}: 10d-return={item['return_nd']:.2%} qty={qty}")
            await queue_trade_action(self.order_queue, self, pk, "OPEN", f"ROTATION_ENTRY_L ret={item['return_nd']:.2%}", 75.0, override_qty=qty)
        for item in losers:
            sym = item['symbol']
            if sym in self.non_shortable_symbols: continue
            pk = f"{account_key}:{sym}_SHORT"
            pos = positions.get(pk)
            has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
            if has_pos:
                opened_at = getattr(pos, 'opened_at', None)
                if opened_at:
                    ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at) if isinstance(opened_at, (int, float)) else now_ts
                    age_days = (now_ts - ots) / 86400.0
                    if age_days >= hold_days and 'ROTATION' in str(getattr(pos, 'augment_reason', '')):
                        _rot_gain = getattr(pos, 'gain', 0) or 0
                        if _rot_gain < 0.1:
                            logger.info(f"[{account_key}] [ROTATION] REBALANCE BLOCKED BY STRICT_NO_LOSS {sym}: gain={_rot_gain:.2f}%")
                        else:
                            logger.info(f"[{account_key}] [ROTATION] REBALANCE EXIT {sym}: held {age_days:.1f} days >= {hold_days} gain={_rot_gain:.2f}%")
                            await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"ROTATION_REBALANCE_{age_days:.0f}d gain={_rot_gain:.2f}%", 80.0)
                continue
            if long_pct < 0.33:
                logger.debug(f"[{account_key}] [ROTATION] Skip SHORT {sym}: L/S ratio too low ({long_pct:.2f})")
                continue
            if not self.is_symbol_tradeable(sym, account_key, 'SHORT'): continue
            # BACKTEST_CHANGE_T47: rotation SMA200 filter — only short when price above SMA200
            if getattr(config, 'ROTATION_SMA200_FILTER', False):
                _rot_sma_s = safe_fetch_float(data.get('sma_200_D', 0)) or safe_fetch_float(data.get('sma_200_1h', 0))
                if _rot_sma_s > 0 and item['price'] < _rot_sma_s:
                    logger.debug(f"[{account_key}] [ROTATION] Skip SHORT {sym}: price {item['price']:.2f} < SMA200 {_rot_sma_s:.2f}")
                    continue
            qty = max(1, int(rot_size / item['price']))
            logger.info(f"[{account_key}] [ROTATION] SHORT {sym}: 10d-return={item['return_nd']:.2%} qty={qty}")
            await queue_trade_action(self.order_queue, self, pk, "OPEN", f"ROTATION_ENTRY_S ret={item['return_nd']:.2%}", 75.0, override_qty=qty)

    async def evaluate_rsi2_entry(self, account_key):
        """MFI(2) Mean Reversion: MFI(2)<5 + above SMA200 = BUY, MFI(2)>95 + below SMA200 = SHORT. Exit on MFI(2) cross 65/35.
        BACKTEST_CHANGE_MT2: Replaced RSI(2) with MFI(2) — volume-weighted flow. RSI is bullshit, only MFI."""
        if not getattr(config, 'RSI2_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        rsi2_entry = getattr(config, 'RSI2_ENTRY_THRESHOLD', 5.0)
        rsi2_exit_long = getattr(config, 'RSI2_EXIT_THRESHOLD_LONG', 65.0)
        rsi2_exit_short = getattr(config, 'RSI2_EXIT_THRESHOLD_SHORT', 35.0)
        rsi2_size = getattr(config, 'RSI2_POSITION_SIZE', 800.0)
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            klines_path = config.KLINES_CACHE_DIR / f"{sym}_D.json"
            if not klines_path.exists(): continue
            try:
                raw = json.loads(klines_path.read_text())
                bars = raw if isinstance(raw, list) else raw.get('bars', [])
                if len(bars) < 4: continue
                closes = [float(b.get('close') or b.get('c') or 0) for b in bars]
                highs = [float(b.get('high') or b.get('h') or 0) for b in bars]
                lows = [float(b.get('low') or b.get('l') or 0) for b in bars]
                volumes = [float(b.get('volume') or b.get('v') or 0) for b in bars]
                closes = [c for c in closes if c > 0]
                if len(closes) < 4 or len(highs) < 4 or len(lows) < 4 or len(volumes) < 4: continue
            except Exception:
                continue
            # MFI(2) computation — volume-weighted RSI using typical price × volume
            typical_prices = [(highs[j] + lows[j] + closes[j]) / 3.0 for j in range(len(closes))]
            raw_money_flows = [typical_prices[j] * volumes[j] for j in range(len(typical_prices))]
            pos_flow = 0.0
            neg_flow = 0.0
            for j in range(-2, 0):
                if j + len(typical_prices) < 1:
                    continue
                idx = j + len(typical_prices)
                if typical_prices[idx] > typical_prices[idx - 1]:
                    pos_flow += raw_money_flows[idx]
                else:
                    neg_flow += raw_money_flows[idx]
            if neg_flow == 0:
                rsi2 = 100.0
            elif pos_flow == 0:
                rsi2 = 0.0
            else:
                money_ratio = pos_flow / neg_flow
                rsi2 = 100.0 - (100.0 / (1.0 + money_ratio))
            sma_200 = safe_fetch_float(data.get('sma_200_D', 0)) or safe_fetch_float(data.get('sma_200_1h', 0))
            pk_long = f"{account_key}:{sym}_LONG"
            pk_short = f"{account_key}:{sym}_SHORT"
            pos_long = positions.get(pk_long)
            pos_short = positions.get(pk_short)
            has_long = pos_long and abs(float(getattr(pos_long, 'positionAmt', 0))) > 0
            has_short = pos_short and abs(float(getattr(pos_short, 'positionAmt', 0))) > 0
            if has_long and ('RSI2' in str(getattr(pos_long, 'augment_reason', '')) or 'MFI2' in str(getattr(pos_long, 'augment_reason', ''))):
                if rsi2 > rsi2_exit_long:
                    _rsi2_gain = getattr(pos_long, 'gain', 0) or 0
                    if _rsi2_gain < 0.1:
                        logger.info(f"[{account_key}] [MFI2] EXIT LONG BLOCKED BY STRICT_NO_LOSS {sym}: gain={_rsi2_gain:.2f}%")
                    else:
                        logger.info(f"[{account_key}] [MFI2] EXIT LONG {sym}: MFI(2)={rsi2:.1f} > {rsi2_exit_long} gain={_rsi2_gain:.2f}%")
                        await queue_trade_action(self.order_queue, self, pk_long, "CLOSE", f"MFI2_EXIT_LONG mfi2={rsi2:.1f} gain={_rsi2_gain:.2f}%", 85.0)
                continue
            if has_short and ('RSI2' in str(getattr(pos_short, 'augment_reason', '')) or 'MFI2' in str(getattr(pos_short, 'augment_reason', ''))):
                if rsi2 < rsi2_exit_short:
                    _rsi2_gain = getattr(pos_short, 'gain', 0) or 0
                    if _rsi2_gain < 0.1:
                        logger.info(f"[{account_key}] [MFI2] EXIT SHORT BLOCKED BY STRICT_NO_LOSS {sym}: gain={_rsi2_gain:.2f}%")
                    else:
                        logger.info(f"[{account_key}] [MFI2] EXIT SHORT {sym}: MFI(2)={rsi2:.1f} < {rsi2_exit_short} gain={_rsi2_gain:.2f}%")
                        await queue_trade_action(self.order_queue, self, pk_short, "CLOSE", f"MFI2_EXIT_SHORT mfi2={rsi2:.1f} gain={_rsi2_gain:.2f}%", 85.0)
                continue
            if not has_long and rsi2 < rsi2_entry and sma_200 > 0 and price > sma_200:
                if long_pct > 0.67:
                    continue
                if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
                qty = max(1, int(rsi2_size / price))
                logger.info(f"[{account_key}] [MFI2] LONG {sym}: MFI(2)={rsi2:.1f} < {rsi2_entry}, price={price:.2f} > SMA200={sma_200:.2f}, qty={qty}")
                await queue_trade_action(self.order_queue, self, pk_long, "OPEN", f"MFI2_MEAN_REVERSION_L mfi2={rsi2:.1f}", 80.0, override_qty=qty)
            if not has_short and rsi2 > (100.0 - rsi2_entry) and sma_200 > 0 and price < sma_200:
                if sym in self.non_shortable_symbols: continue
                if long_pct < 0.33:
                    continue
                if not self.is_symbol_tradeable(sym, account_key, 'SHORT'): continue
                qty = max(1, int(rsi2_size / price))
                logger.info(f"[{account_key}] [MFI2] SHORT {sym}: MFI(2)={rsi2:.1f} > {100.0 - rsi2_entry}, price={price:.2f} < SMA200={sma_200:.2f}, qty={qty}")
                await queue_trade_action(self.order_queue, self, pk_short, "OPEN", f"MFI2_MEAN_REVERSION_S mfi2={rsi2:.1f}", 80.0, override_qty=qty)

    async def evaluate_gap_fill(self, account_key):
        """Gap Fill: at 9:35 AM, find stocks that gapped 1-5%, enter opposite direction targeting 50% fill. OOS Sharpe 9.05."""
        if not getattr(config, 'GAP_FILL_ENABLED', False): return
        if getattr(config, 'TRA_SATOSHIT_ONLY', False) and account_key == 'tra': return
        if not is_regular_trading_hours(): return
        _est = ZoneInfo("America/New_York")
        _today_key = datetime.now(timezone.utc).astimezone(_est).strftime('%Y%m%d')
        if self._gap_fill_ran_today == _today_key: return
        self._gap_fill_ran_today = _today_key
        snapshot = self._get_full_market_snapshot()
        if not snapshot: return
        min_gap = getattr(config, 'GAP_FILL_MIN_GAP_PCT', 1.0)
        max_gap = getattr(config, 'GAP_FILL_MAX_GAP_PCT', 5.0)
        stop_mult = getattr(config, 'GAP_FILL_STOP_MULT', 0.3)
        tp_fill = getattr(config, 'GAP_FILL_TP_FILL_PCT', 0.5)
        gf_size = getattr(config, 'GAP_FILL_POSITION_SIZE', 400.0)
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        _entered = 0
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            sym = sym.upper()
            if sym in self.blacklist: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            klines_path = config.KLINES_CACHE_DIR / f"{sym}_D.json"
            if not klines_path.exists(): continue
            try:
                raw = json.loads(klines_path.read_text())
                bars = raw if isinstance(raw, list) else raw.get('bars', [])
                if len(bars) < 2: continue
                closes = [float(b.get('close') or b.get('c') or 0) for b in bars]
                closes = [c for c in closes if c > 0]
                if len(closes) < 2: continue
                prev_close = closes[-1]
            except Exception:
                continue
            today_open = price
            if prev_close <= 0: continue
            gap_pct = (today_open - prev_close) / prev_close * 100.0
            gap_abs = today_open - prev_close
            if abs(gap_pct) < min_gap or abs(gap_pct) > max_gap: continue
            if gap_pct < -min_gap:
                pk = f"{account_key}:{sym}_LONG"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos: continue
                if long_pct > 0.67: continue
                if not self.is_symbol_tradeable(sym, account_key, 'LONG'): continue
                stop_price = today_open - (stop_mult * abs(gap_abs))
                tp_price = today_open + (tp_fill * abs(gap_abs))
                qty = max(1, int(gf_size / price))
                logger.info(f"[{account_key}] [GAP_FILL] BUY {sym}: gap={gap_pct:.2f}% prev_close={prev_close:.2f} open={today_open:.2f} stop={stop_price:.2f} tp={tp_price:.2f} qty={qty}")
                self._gap_fill_positions[pk] = {'stop': stop_price, 'tp': tp_price, 'entry': today_open, 'gap_pct': gap_pct, 'prev_close': prev_close, 'opened_at': datetime.now(timezone.utc)}
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"GAP_FILL_BUY gap={gap_pct:.2f}%", 90.0, override_qty=qty)
                _entered += 1
            elif gap_pct > min_gap:
                if sym in self.non_shortable_symbols: continue
                pk = f"{account_key}:{sym}_SHORT"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos: continue
                if long_pct < 0.33: continue
                if not self.is_symbol_tradeable(sym, account_key, 'SHORT'): continue
                stop_price = today_open + (stop_mult * abs(gap_abs))
                tp_price = today_open - (tp_fill * abs(gap_abs))
                qty = max(1, int(gf_size / price))
                logger.info(f"[{account_key}] [GAP_FILL] SHORT {sym}: gap={gap_pct:.2f}% prev_close={prev_close:.2f} open={today_open:.2f} stop={stop_price:.2f} tp={tp_price:.2f} qty={qty}")
                self._gap_fill_positions[pk] = {'stop': stop_price, 'tp': tp_price, 'entry': today_open, 'gap_pct': gap_pct, 'prev_close': prev_close, 'opened_at': datetime.now(timezone.utc)}
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"GAP_FILL_SELL gap={gap_pct:.2f}%", 90.0, override_qty=qty)
                _entered += 1
        logger.info(f"[{account_key}] [GAP_FILL] Scan complete: {_entered} gap fill entries queued")

    async def evaluate_fh_momentum(self, account_key):
        """First-Hour Momentum: After first 30min, if move > 0.5%, enter WITH direction.
        BACKTEST_CHANGE_MT3: 82% hit rate on 3,560 days. Momentum WITH trend, not against.
        Entry: 30min after open if |move| > threshold AND MFI(D) confirms AND DC position confirms.
        Exit: WT 5-TF patience (same as all other strategies). EOD fallback at 15:55 ET."""
        if not getattr(config, 'FH_MOMENTUM_ENABLED', False):
            return
        if not is_regular_trading_hours():
            return
        _now_utc = datetime.now(timezone.utc)
        _tod_utc = _now_utc.hour * 60 + _now_utc.minute
        _eval_minutes = getattr(config, 'FH_MOMENTUM_EVAL_MINUTES', 30)
        _eval_start_utc = 13 * 60 + 30 + _eval_minutes
        _eval_end_utc = _eval_start_utc + 30
        if _tod_utc < _eval_start_utc or _tod_utc > _eval_end_utc:
            return
        _today_key = _now_utc.strftime('%Y%m%d')
        if not hasattr(self, '_fh_momentum_ran_today'):
            self._fh_momentum_ran_today = ""
        if self._fh_momentum_ran_today == _today_key:
            return
        self._fh_momentum_ran_today = _today_key
        snapshot = self._get_full_market_snapshot()
        if not snapshot:
            return
        min_move = getattr(config, 'FH_MOMENTUM_MIN_MOVE_PCT', 0.5)
        fh_size = getattr(config, 'FH_MOMENTUM_POSITION_SIZE', 600.0)
        max_pos = getattr(config, 'FH_MOMENTUM_MAX_POSITIONS', 5)
        mfi_confirm = getattr(config, 'FH_MOMENTUM_MFI_CONFIRM', True)
        dc_confirm = getattr(config, 'FH_MOMENTUM_DC_CONFIRM', True)
        positions = self.position_manager.get_positions_by_account(account_key)
        balance = self.get_current_portfolio_balance()
        long_pct = balance.get('long_pct', 0.5)
        _entered = 0
        for sym, data in snapshot.items():
            if not isinstance(data, dict):
                continue
            sym = sym.upper()
            if sym in self.blacklist:
                continue
            if _entered >= max_pos:
                break
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0:
                continue
            day_open = safe_fetch_float(data.get('open_D', 0))
            if day_open <= 0:
                day_open = safe_fetch_float(data.get('open_1h', 0))
            if day_open <= 0:
                continue
            move_pct = (price - day_open) / day_open * 100.0
            if abs(move_pct) < min_move:
                continue
            is_long_signal = move_pct > 0
            mfi_d = safe_fetch_float(data.get('mfi_D', data.get('mfi_4h', 50)))
            dc_pos_d = safe_fetch_float(data.get('dc_position_D', data.get('dc_position_4h', 0.5)))
            if mfi_confirm:
                if is_long_signal and mfi_d < 50:
                    continue
                if not is_long_signal and mfi_d > 50:
                    continue
            if dc_confirm:
                if is_long_signal and dc_pos_d < 0.4:
                    continue
                if not is_long_signal and dc_pos_d > 0.6:
                    continue
            if is_long_signal:
                pk = f"{account_key}:{sym}_LONG"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos:
                    continue
                if long_pct > 0.67:
                    continue
                if not self.is_symbol_tradeable(sym, account_key, 'LONG'):
                    continue
                qty = max(1, int(fh_size / price))
                logger.info(f"[{account_key}] [FH_MOM] LONG {sym}: 30min move={move_pct:+.2f}% MFI(D)={mfi_d:.0f} DC_pos={dc_pos_d:.2f} qty={qty}")
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"FH_MOMENTUM_L move={move_pct:+.2f}%_mfi={mfi_d:.0f}_dc={dc_pos_d:.2f}", 85.0, override_qty=qty)
                _entered += 1
            else:
                if sym in self.non_shortable_symbols:
                    continue
                pk = f"{account_key}:{sym}_SHORT"
                pos = positions.get(pk)
                has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
                if has_pos:
                    continue
                if long_pct < 0.33:
                    continue
                if not self.is_symbol_tradeable(sym, account_key, 'SHORT'):
                    continue
                qty = max(1, int(fh_size / price))
                logger.info(f"[{account_key}] [FH_MOM] SHORT {sym}: 30min move={move_pct:+.2f}% MFI(D)={mfi_d:.0f} DC_pos={dc_pos_d:.2f} qty={qty}")
                await queue_trade_action(self.order_queue, self, pk, "OPEN", f"FH_MOMENTUM_S move={move_pct:+.2f}%_mfi={mfi_d:.0f}_dc={dc_pos_d:.2f}", 85.0, override_qty=qty)
                _entered += 1
        if _entered > 0:
            logger.info(f"[{account_key}] [FH_MOM] {_entered} momentum entries queued (move>{min_move}% + MFI + DC confirmed)")

    async def monitor_gap_fill_exits(self, account_key):
        """Monitor open gap fill positions for stop, TP, or EOD forced exit."""
        if not self._gap_fill_positions: return
        _est = ZoneInfo("America/New_York")
        _now_est = datetime.now(timezone.utc).astimezone(_est)
        _now_t = _now_est.time()
        _is_eod = _now_t >= dt_time(15, 55)
        _today_key = _now_est.strftime('%Y%m%d')
        _to_remove = []
        positions = self.position_manager.get_positions_by_account(account_key)
        for pk, gf in list(self._gap_fill_positions.items()):
            if not pk.startswith(f"{account_key}:"): continue
            pos = positions.get(pk)
            has_pos = pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0
            if not has_pos:
                _to_remove.append(pk)
                continue
            sym = pk.split(':')[1].rsplit('_', 1)[0]
            price, _ = await self.get_current_price(sym)
            price = float(price)
            if price <= 0: continue
            is_long = pk.endswith('_LONG')
            stop_hit = (is_long and price <= gf['stop']) or (not is_long and price >= gf['stop'])
            tp_hit = (is_long and price >= gf['tp']) or (not is_long and price <= gf['tp'])
            # Compute gain for STRICT_NO_LOSS guard
            _gf_entry = gf.get('entry_price', 0)
            _gf_gain = ((price - _gf_entry) / _gf_entry * 100) if (is_long and _gf_entry > 0) else ((_gf_entry - price) / _gf_entry * 100 if _gf_entry > 0 else 0)
            if stop_hit:
                if _gf_gain < 0.1:
                    logger.info(f"[{account_key}] [GAP_FILL] STOP BLOCKED BY STRICT_NO_LOSS {pk}: gain={_gf_gain:.2f}%")
                else:
                    logger.info(f"[{account_key}] [GAP_FILL] STOP HIT {pk}: price={price:.2f} stop={gf['stop']:.2f} gain={_gf_gain:.2f}%")
                    await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"GAP_FILL_STOP price={price:.2f} gain={_gf_gain:.2f}%", 95.0)
                    _to_remove.append(pk)
            elif tp_hit:
                logger.info(f"[{account_key}] [GAP_FILL] TP HIT {pk}: price={price:.2f} tp={gf['tp']:.2f}")
                await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"GAP_FILL_TP price={price:.2f}", 95.0)
                _to_remove.append(pk)
            elif _is_eod:
                if _gf_gain < 0.1:
                    logger.info(f"[{account_key}] [GAP_FILL] EOD BLOCKED BY STRICT_NO_LOSS {pk}: gain={_gf_gain:.2f}%")
                else:
                    logger.info(f"[{account_key}] [GAP_FILL] EOD EXIT {pk}: price={price:.2f} gain={_gf_gain:.2f}%")
                    await queue_trade_action(self.order_queue, self, pk, "CLOSE", f"GAP_FILL_EOD price={price:.2f} gain={_gf_gain:.2f}%", 95.0)
                    _to_remove.append(pk)
        for pk in _to_remove:
            self._gap_fill_positions.pop(pk, None)
        if _now_t >= dt_time(16, 0):
            _stale = [pk for pk in self._gap_fill_positions if pk.startswith(f"{account_key}:")]
            for pk in _stale:
                self._gap_fill_positions.pop(pk, None)

    async def _rotation_rsi2_loop(self):
        """Background loop: all strategies — rotation, RSI2, gap fill, ORB, EP, Clenow, SMFI, Minervini, Connors RSI."""
        logger.info("[STRATEGY_LOOP] All strategies started (Rotation+RSI2+GapFill+ORB+EP+Clenow+SMFI+Minervini+ConnorsRSI)")
        await asyncio.sleep(20)
        _last_rotation_run = 0
        _last_rsi2_run = 0
        _last_gf_monitor = 0
        _last_orb_run = 0
        _last_orb_monitor = 0
        _last_ep_run = 0
        _last_clenow_run = 0
        _last_smfi_run = 0
        _last_minervini_run = 0
        _last_connors_run = 0
        while self.running:
            try:
                if not is_regular_trading_hours():
                    await asyncio.sleep(60)
                    continue
                _est = ZoneInfo("America/New_York")
                _now_est = datetime.now(timezone.utc).astimezone(_est)
                _now_t = _now_est.time()
                _today_key = _now_est.strftime('%Y%m%d')
                rotation_windows = [(dt_time(9, 34), dt_time(9, 40)), (dt_time(15, 50), dt_time(15, 59))]
                in_rotation_window = any(lo <= _now_t <= hi for lo, hi in rotation_windows)
                rot_key = f"{_today_key}_{_now_t.hour}"
                if in_rotation_window and _last_rotation_run != rot_key:
                    _last_rotation_run = rot_key
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_rotation_entry(acc)
                        except Exception as e:
                            logger.error(f"[ROTATION] Error for {acc}: {e}", exc_info=True)
                # BC_161: Contrarian spike fade — scan every 30s during market hours
                if not hasattr(self, '_last_spike_fade_run'): self._last_spike_fade_run = 0
                if time.time() - self._last_spike_fade_run >= 30:
                    self._last_spike_fade_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_spike_fade(acc)
                        except Exception as e:
                            logger.error(f"[SPIKE_FADE] Error for {acc}: {e}", exc_info=True)
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_gap_fill(acc)
                        except Exception as e:
                            logger.error(f"[GAP_FILL] Error for {acc}: {e}", exc_info=True)
                if time.time() - _last_rsi2_run >= 900:
                    _last_rsi2_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_rsi2_entry(acc)
                        except Exception as e:
                            logger.error(f"[MFI2] Error for {acc}: {e}", exc_info=True)
                        try:
                            await self.evaluate_fh_momentum(acc)
                        except Exception as e:
                            logger.error(f"[FH_MOM] Error for {acc}: {e}", exc_info=True)
                # YOUTUBE_CONSENSUS: ORB — scan every 30s between 9:45-12:00 ET
                if dt_time(9, 45) <= _now_t <= dt_time(12, 0) and time.time() - _last_orb_run >= 30:
                    _last_orb_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_orb_entry(acc)
                        except Exception as e:
                            logger.error(f"[ORB] Error for {acc}: {e}", exc_info=True)
                # YOUTUBE_CONSENSUS: ORB exit monitoring every 30s
                if self._orb_positions and time.time() - _last_orb_monitor >= 30:
                    _last_orb_monitor = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.monitor_orb_exits(acc)
                        except Exception as e:
                            logger.error(f"[ORB_MONITOR] Error for {acc}: {e}", exc_info=True)
                # YOUTUBE_CONSENSUS: Episodic Pivot — scan every 15min
                if time.time() - _last_ep_run >= 900:
                    _last_ep_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_episodic_pivot(acc)
                        except Exception as e:
                            logger.error(f"[EPISODIC_PIVOT] Error for {acc}: {e}", exc_info=True)
                if self._gap_fill_positions and time.time() - _last_gf_monitor >= 60:
                    _last_gf_monitor = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.monitor_gap_fill_exits(acc)
                        except Exception as e:
                            logger.error(f"[GAP_FILL_MONITOR] Error for {acc}: {e}", exc_info=True)
                # BACKTEST WINNERS: Clenow — monthly rebalance (every 30min check)
                if time.time() - _last_clenow_run >= 1800:
                    _last_clenow_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_clenow_entry(acc)
                        except Exception as e:
                            logger.error(f"[CLENOW] Error for {acc}: {e}", exc_info=True)
                # BACKTEST WINNERS: SMFI — every 15min
                if time.time() - _last_smfi_run >= 900:
                    _last_smfi_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_smfi_entry(acc)
                        except Exception as e:
                            logger.error(f"[SMFI] Error for {acc}: {e}", exc_info=True)
                # BACKTEST WINNERS: Minervini — every 30min
                if time.time() - _last_minervini_run >= 1800:
                    _last_minervini_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_minervini_entry(acc)
                        except Exception as e:
                            logger.error(f"[MINERVINI] Error for {acc}: {e}", exc_info=True)
                # BACKTEST WINNERS: Connors RSI — every 15min
                if time.time() - _last_connors_run >= 900:
                    _last_connors_run = time.time()
                    for acc in self.target_accounts:
                        try:
                            await self.evaluate_connors_rsi_entry(acc)
                        except Exception as e:
                            logger.error(f"[CONNORS_RSI] Error for {acc}: {e}", exc_info=True)
                await asyncio.sleep(30)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"[STRATEGY_LOOP] Loop error: {e}", exc_info=True)
                await asyncio.sleep(30)

    async def start(self):
        print("[START] Loading JSON snapshot...", flush=True)
        json_path = config.DATA_DIR / "tradier_indicators_latest.json"
        if json_path.exists():
            try:
                with open(json_path, 'r') as f:
                    raw = json.loads(f.read())
                    self.market_snapshot = {k.upper(): self._adapt_indicators_for_ez_manage(v) for k, v in raw.items() if isinstance(v, dict)}
                print(f"[START] Snapshot loaded: {len(self.market_snapshot)} symbols", flush=True)
            except Exception as e:
                print(f"[START] Snapshot load failed: {e}", flush=True)
        self.redis_manager = None
        redis_ok = await self.reader.connect()
        if redis_ok:
            print(f"[START] PositionReader connected to Redis (localhost:{getattr(config, 'REDIS_PORT', 6379)}). Will sync from tradier_positions every 5s.", flush=True)
            await self.reader.sync(self.target_accounts)
            active_count = sum(1 for p in self.reader.positions.values() if abs(float(getattr(p, 'positionAmt', 0) or 0)) > 0)
            print(f"[START] Redis sync: {active_count} active positions loaded from tradier_positions", flush=True)
        else:
            print("[START] Redis unavailable. Falling back to disk load.", flush=True)
            for acc in self.target_accounts:
                await self.reader._load_from_disk(acc)
            print(f"[START] Positions loaded from disk for {self.target_accounts}", flush=True)
        self.api_client = TradierAPIClient(config, account_key=self.target_accounts[0])
        try:
            await asyncio.wait_for(self.api_client.connect(), timeout=10.0)
            print("[START] API client connected.", flush=True)
        except Exception as e:
            print(f"[START] API client connect failed: {e}", flush=True)
        try:
            print("[START] Running immediate API sync to fix phantom positions...", flush=True)
            await asyncio.wait_for(self.sync_real_positions_from_api(), timeout=45.0)
            print(f"[START] API sync complete. Phantom-killed keys: {len(self._phantom_killed_keys)}", flush=True)
        except asyncio.TimeoutError:
            print("[START] Initial API sync timed out after 45s — will retry in sync_positions_loop", flush=True)
        except Exception as e:
            print(f"[START] Initial API sync failed: {e}", flush=True)
        self.running = True
        self._seed_reentry_from_positions()
        all_syms = list(set(self.symbols or [s for acc in self.target_accounts for s in getattr(self, f"symbols_long_{acc}", []) + getattr(self, f"symbols_short_{acc}", [])]))
        if all_syms:
            self.corr_matrix = CorrelationMatrix(all_syms, lookback=60)
            self.hedge_engine = TradierHedgeEngine(self.corr_matrix, all_syms)
            logger.info(f"[HEDGE_ENGINE] Initialized with {len(all_syms)} symbols (HEDGE_MODE_TRADIER={getattr(config, 'HEDGE_MODE_TRADIER', False)})")
        _metrics_port = {"tra": 8818, "trb": 8819, "trc": 8820}.get(self.target_accounts[0], 8820) if self.target_accounts else 8820
        self.background_tasks.append(asyncio.create_task(self.metrics_server_loop(host="0.0.0.0", port=_metrics_port)))
        self.background_tasks.append(asyncio.create_task(self.periodic_sentiment_rebalancing()))
        self.scalp_strategy = StockScalpStrategy(self)
        self.background_tasks.append(asyncio.create_task(self.scalp_strategy.run_loop()))
        self.daytrade_wing = StockDaytradeWing(self)
        self.background_tasks.append(asyncio.create_task(self.daytrade_wing.run_loop()))
        # Stock outlier scalper — catches huge movers every 10s on trc sandbox (paper money, big positions) #should record its own gains
        self.breakout_scalper = StockBreakoutScalper.get()
        async def _scalper_loop():
            while self.running:
                try: await self.breakout_scalper.run(trade_manager=self)
                except Exception as e: logger.error(f"[STOCK_OUTLIER] Loop error: {e}")
                await asyncio.sleep(10)
        self.background_tasks.append(asyncio.create_task(_scalper_loop()))
        self.background_tasks.append(asyncio.create_task(self.spike_fade_result_tracker()))  # BC_161: Auto-kill if 3/5 lose
        self.background_tasks.append(asyncio.create_task(self.monitor_ladder_levels(self.order_queue)))
        self.background_tasks.append(asyncio.create_task(monitor_direct_high_gain_positions(self, self.order_queue)))
        self.background_tasks.append(asyncio.create_task(self.sync_positions_loop())) 
        self.background_tasks.append(asyncio.create_task(self.validate_positions_periodic()))
        self.background_tasks.append(asyncio.create_task(redis_listener_signals(self)))
        self.background_tasks.append(asyncio.create_task(self.redis_listener_indicators())) # Fast Path
        self.background_tasks.append(asyncio.create_task(self.redis_listener_prices()))
        self.background_tasks.append(asyncio.create_task(self.market_data_sync_loop())) # Safe Path
        self.background_tasks.append(asyncio.create_task(self.update_all_positions_prices()))
        self.background_tasks.append(asyncio.create_task(self.monitor_system_state()))
        self.background_tasks.append(asyncio.create_task(self.order_queue.process_orders()))
        self.background_tasks.append(asyncio.create_task(self._options_watchdog_loop()))
        self.background_tasks.append(asyncio.create_task(self.last_hour_balancing_loop()))
        #self.background_tasks.append(asyncio.create_task(self._rotation_rsi2_loop())) #SUSPICIOUS - should be MFI if credible at all
        for acc in self.target_accounts:
            self.background_tasks.append(asyncio.create_task(monitor_stale_augmentations(self, acc, self.order_queue)))
            self.background_tasks.append(asyncio.create_task(periodic_direct_high_gain_reopen(self, self.order_queue, acc)))
            self.background_tasks.append(asyncio.create_task(self.periodic_tasks(acc, self.order_queue)))
            self.background_tasks.append(asyncio.create_task(continuous_queue_processor(self.order_queue, self, acc)))
            self.background_tasks.append(asyncio.create_task(process_symbols_periodically(self.order_queue, self, acc)))
            self.background_tasks.append(asyncio.create_task(periodic_override_check(self, acc)))
        if 'trc' in self.target_accounts and getattr(config, 'TRC_5M_SWEEP_ENABLED', True):
            self.background_tasks.append(asyncio.create_task(trc_5m_sweep_loop(self.order_queue, self)))
        self.background_tasks.append(asyncio.create_task(tradier_performance_report_loop(self)))
        self.background_tasks.append(asyncio.create_task(tradier_outlier_scan_loop(self)))
        self.background_tasks.append(asyncio.create_task(tradier_capital_reallocation_loop(self)))
        self.background_tasks.append(asyncio.create_task(self.copilot_bridge_loop()))
        self.background_tasks.append(asyncio.create_task(self.reentry_monitor_loop()))
        # 2026-05-09 USER MANDATE: trb obligatory sector hedge / fallback close. Approximate hedge — monitor.
        self.background_tasks.append(asyncio.create_task(obligatory_sector_hedge_or_close_loop(self)))
        # 2026-05-15: overnight gap hedge — same-sector opposite-side before close (default OFF).
        self.background_tasks.append(asyncio.create_task(overnight_gap_hedge_loop(self)))
        print(f"[START] {len(self.background_tasks)} background tasks launched. Entering trading loop...", flush=True)
        logger.info("☀️ ✅ Manager Running.")
        await self.trading_loop()

    async def copilot_bridge_loop(self):
        """Listen for copilot commands via Redis and execute through execute_now."""
        try:
            r = redis.Redis(host='127.0.0.1', port=config.REDIS_PORT, decode_responses=True)
            pubsub = r.pubsub()
            await pubsub.subscribe("haiku_agent_reversals_tradier", "haiku_agent_augments")
            logger.info("[COPILOT_BRIDGE] Listening for copilot commands on tradier channels")
            async for message in pubsub.listen():
                if message["type"] != "message":
                    continue
                try:
                    data = json.loads(message["data"])
                    acct = data.get("account_key", "")
                    if acct not in self.target_accounts:
                        continue
                    pk = data.get("position_key", "")
                    reason = data.get("reason", "COPILOT")
                    price = float(data.get("price", 0) or 0)
                    qty = float(data.get("qty", 0) or 0)
                    channel = message.get("channel", "")
                    pos = self.position_manager.get_position(pk) if self.position_manager else None
                    if not pos:
                        logger.warning(f"[COPILOT_BRIDGE] Position {pk} not found — skipping")
                        continue
                    pos_amt = abs(float(getattr(pos, 'positionAmt', 0) or 0))
                    if pos_amt <= 0:
                        continue
                    is_long = pk.endswith("_LONG")
                    symbol = pk.split(":")[1].rsplit("_", 1)[0] if ":" in pk else ""
                    mark_price = price if price > 0 else float(getattr(pos, 'markPrice', 0) or 0)
                    if mark_price <= 0:
                        continue
                    if "reversals" in channel:
                        reduce_qty = qty if qty > 0 else pos_amt * 0.5
                        side = "SELL" if is_long else "BUY"
                        position_side = "LONG" if is_long else "SHORT"
                        uid = f"copilot_rev_{int(time.time())}"
                        logger.warning(f"[COPILOT_BRIDGE] REVERSAL: {pk} {side} qty={reduce_qty:.4f} — {reason}")
                        await self.execute_now(pk, acct, symbol, pos_amt, side, position_side, reduce_qty, mark_price, uid, f"COPILOT_REVERSAL: {reason}", reduce_qty >= pos_amt, action="REDUCE")
                    else:
                        if qty <= 0:
                            continue
                        side = "BUY" if is_long else "SELL"
                        position_side = "LONG" if is_long else "SHORT"
                        uid = f"copilot_aug_{int(time.time())}"
                        logger.info(f"[COPILOT_BRIDGE] AUGMENT: {pk} {side} +{qty:.4f} — {reason}")
                        await self.execute_now(pk, acct, symbol, pos_amt, side, position_side, qty, mark_price, uid, f"COPILOT_AUGMENT: {reason}", False, action="AUGMENT")
                except Exception as e:
                    logger.error(f"[COPILOT_BRIDGE] Error processing command: {e}")
        except Exception as e:
            logger.error(f"[COPILOT_BRIDGE] Bridge loop error: {e}")
            await asyncio.sleep(10)

    async def stop(self):
        self.running = False
        logger.info("Stopping Manager...")
        for t in self.background_tasks: t.cancel()
        if self.background_tasks:
            await asyncio.gather(*self.background_tasks, return_exceptions=True)
        await self.reader.close()
        if self.api_client: await self.api_client.close()

# ═══════════════════════════════════════════════════════════════════
# PERFORMANCE TRACKING + OUTLIER DETECTION + CAPITAL ALLOCATION
# Inline loops — write reports to data/reports/tradier_*
# ═══════════════════════════════════════════════════════════════════

async def tradier_performance_report_loop(trade_manager):
    """Every 5 min: compute per-symbol rolling stats, write data/reports/tradier_performance_report.txt + .json."""
    await asyncio.sleep(30.0)
    report_dir = Path(config.BASE_PATH) / 'data' / 'reports'
    report_dir.mkdir(parents=True, exist_ok=True)
    while True:
        try:
            if not config.SYMBOL_PERF_ENABLED:
                await asyncio.sleep(300)
                continue
            from utils import refresh_cache
            stats = refresh_cache()
            if not stats:
                await asyncio.sleep(300)
                continue
            tiers = {'A': [], 'B': [], 'C': []}
            for sym, s in sorted(stats.items(), key=lambda x: x[1].get('total_pnl_pct', 0), reverse=True):
                tiers[s.get('tier', 'B')].append(s)
            now_str = datetime.now(timezone.utc).strftime('%Y-%m-%d %H:%M UTC')
            open_symbols = set()
            for pk, pos in trade_manager.positions.items():
                if pos and abs(getattr(pos, 'positionAmt', 0) or getattr(pos, 'quantity', 0) or 0) > 0:
                    sym = pk.split(':')[-1].rsplit('_', 1)[0] if ':' in pk else pk.rsplit('_', 1)[0]
                    open_symbols.add(sym)
            lines = [f"TRADIER PERFORMANCE REPORT — {now_str}", f"{'=' * 80}", f"Total: {len(stats)} symbols | A={len(tiers['A'])} B={len(tiers['B'])} C={len(tiers['C'])}", ""]
            for tier_name in ('A', 'B', 'C'):
                items = tiers[tier_name]
                lines.append(f"--- TIER {tier_name} ({len(items)} symbols) ---")
                for s in items:
                    marker = " ** OPEN **" if s['symbol'] in open_symbols else ""
                    lines.append(f"  {s['symbol']:>15s}  WR={s['win_rate']:.0%}  avg={s['avg_gain_pct']:+.2f}%  total={s['total_pnl_pct']:+.1f}%  trades={s['trade_count']:3d}  mult={s['order_multiplier']:.2f}{marker}")
                lines.append("")
            total_pnl = sum(s['total_pnl_pct'] for s in stats.values())
            lines.extend(["SUMMARY:", f"  Total PnL: {total_pnl:+.1f}%", ""])
            with open(report_dir / 'tradier_performance_report.txt', 'w') as f:
                f.write('\n'.join(lines))
            logger.info(f"[PERF_REPORT] Written: {len(stats)} symbols (A={len(tiers['A'])} B={len(tiers['B'])} C={len(tiers['C'])})")
        except Exception as e:
            logger.error(f"[PERF_REPORT] Error: {e}", exc_info=True)
        await asyncio.sleep(config.SYMBOL_PERF_REFRESH_SECONDS)

async def tradier_outlier_scan_loop(trade_manager):
    """Every 60s: scan all positions for stuck/runaway/stale, write data/reports/tradier_outlier_report.txt."""
    await asyncio.sleep(45.0)
    report_dir = Path(config.BASE_PATH) / 'data' / 'reports'
    report_dir.mkdir(parents=True, exist_ok=True)
    while True:
        try:
            if not config.OUTLIER_DETECTOR_ENABLED:
                await asyncio.sleep(60)
                continue
            now = datetime.now(timezone.utc)
            all_alerts = []
            total_positions = 0
            for pk, pos in trade_manager.positions.items():
                if not pos:
                    continue
                amt = abs(getattr(pos, 'positionAmt', 0) or getattr(pos, 'quantity', 0) or 0)
                if amt == 0:
                    continue
                total_positions += 1
                entry_price = safe_fetch_float(getattr(pos, 'entry_price', 0) or getattr(pos, 'entryPrice', 0) or getattr(pos, 'average_cost', 0), 0.0)
                mark_price = safe_fetch_float(getattr(pos, 'mark_price', 0) or getattr(pos, 'markPrice', 0) or getattr(pos, 'current_price', 0), 0.0)
                if entry_price <= 0 or mark_price <= 0:
                    continue
                sym = pk.split(':')[-1].rsplit('_', 1)[0] if ':' in pk else pk.rsplit('_', 1)[0]
                is_long = pk.endswith('_LONG')
                # RUNAWAY_LOSS
                loss_pct = ((entry_price - mark_price) / entry_price * 100) if is_long else ((mark_price - entry_price) / entry_price * 100)
                if loss_pct > 5.0:
                    loss_usd = loss_pct / 100 * amt * mark_price
                    severity = 'CRITICAL' if loss_pct > 10.0 else 'WARNING'
                    all_alerts.append({'position_key': pk, 'symbol': sym, 'type': 'RUNAWAY_LOSS', 'severity': severity, 'loss_pct': round(loss_pct, 3), 'est_loss_usd': round(loss_usd, 2)})
                # STALE_ACTIVITY
                last_activity = getattr(pos, 'last_augmentation_time', None) or getattr(pos, 'last_reduction_time', None)
                if last_activity:
                    try:
                        if isinstance(last_activity, datetime):
                            idle_hours = (now - last_activity).total_seconds() / 3600
                        else:
                            idle_hours = (now - datetime.fromisoformat(str(last_activity).replace('Z', '+00:00'))).total_seconds() / 3600
                    except Exception:
                        idle_hours = 0
                    if idle_hours >= config.OUTLIER_STALE_HOURS:
                        all_alerts.append({'position_key': pk, 'symbol': sym, 'type': 'STALE_ACTIVITY', 'severity': 'WARNING', 'idle_h': round(idle_hours, 1)})
            now_str = now.strftime('%Y-%m-%d %H:%M UTC')
            lines = [f"TRADIER OUTLIER REPORT — {now_str}", f"{'=' * 80}", f"Scanned: {total_positions} positions | Alerts: {len(all_alerts)}", ""]
            for atype in ('RUNAWAY_LOSS', 'STALE_ACTIVITY'):
                typed = [a for a in all_alerts if a['type'] == atype]
                if typed:
                    lines.append(f"--- {atype} ({len(typed)}) ---")
                    for a in typed:
                        sev = 'CRIT' if a['severity'] == 'CRITICAL' else 'WARN'
                        detail_parts = [f"{k}={v}" for k, v in a.items() if k not in ('position_key', 'symbol', 'type', 'severity')]
                        lines.append(f"  [{sev}] {a['position_key']:>30s}  {' '.join(detail_parts)}")
                    lines.append("")
            if not all_alerts:
                lines.append("  No outliers detected.")
            with open(report_dir / 'tradier_outlier_report.txt', 'w') as f:
                f.write('\n'.join(lines))
            if all_alerts:
                logger.info(f"[OUTLIER_SCAN] {len(all_alerts)} alerts across {total_positions} positions")
        except Exception as e:
            logger.error(f"[OUTLIER_SCAN] Error: {e}", exc_info=True)
        await asyncio.sleep(config.OUTLIER_SCAN_INTERVAL)

async def tradier_capital_reallocation_loop(trade_manager):
    """Every 5 min: capital distribution analysis, write data/reports/tradier_capital_report.txt."""
    await asyncio.sleep(60.0)
    report_dir = Path(config.BASE_PATH) / 'data' / 'reports'
    report_dir.mkdir(parents=True, exist_ok=True)
    while True:
        try:
            if not config.SYMBOL_PERF_ENABLED:
                await asyncio.sleep(300)
                continue
            from utils import get_performance_multiplier, get_symbol_tier
            now_str = datetime.now(timezone.utc).strftime('%Y-%m-%d %H:%M UTC')
            positions_data = []
            total_capital = 0.0
            for pk, pos in trade_manager.positions.items():
                if not pos:
                    continue
                amt = abs(getattr(pos, 'positionAmt', 0) or getattr(pos, 'quantity', 0) or 0)
                if amt == 0:
                    continue
                mark = safe_fetch_float(getattr(pos, 'mark_price', 0) or getattr(pos, 'current_price', 0), 0.0)
                if mark <= 0:
                    continue
                value_usd = amt * mark
                gain = safe_fetch_float(getattr(pos, 'gain', 0), 0.0)
                sym = pk.split(':')[-1].rsplit('_', 1)[0] if ':' in pk else pk.rsplit('_', 1)[0]
                tier = get_symbol_tier(sym, 'B')
                perf_mult = get_performance_multiplier(sym, 1.0)
                total_capital += value_usd
                positions_data.append({'pk': pk, 'symbol': sym, 'value_usd': round(value_usd, 2), 'gain_pct': round(gain, 3), 'tier': tier, 'perf_mult': round(perf_mult, 2)})
            if not positions_data:
                await asyncio.sleep(300)
                continue
            positions_data.sort(key=lambda p: p['value_usd'], reverse=True)
            tier_capital = {'A': 0.0, 'B': 0.0, 'C': 0.0}
            tier_count = {'A': 0, 'B': 0, 'C': 0}
            for p in positions_data:
                tier_capital[p['tier']] += p['value_usd']
                tier_count[p['tier']] += 1
            winners = [p for p in positions_data if p['tier'] == 'A' and p['gain_pct'] > 0.1]
            losers = [p for p in positions_data if p['tier'] == 'C' or (p['tier'] == 'B' and p['gain_pct'] < -2.0)]
            mediocre = [p for p in positions_data if abs(p['gain_pct']) < 1.0 and p['perf_mult'] <= 1.0 and p['tier'] != 'A']
            lines = [f"TRADIER CAPITAL REPORT — {now_str}", f"{'=' * 80}", f"Total: ${total_capital:,.2f} across {len(positions_data)} positions", ""]
            lines.append("TIER DISTRIBUTION:")
            for t in ('A', 'B', 'C'):
                pct = tier_capital[t] / total_capital * 100 if total_capital > 0 else 0
                lines.append(f"  Tier {t}: ${tier_capital[t]:>10,.2f} ({pct:5.1f}%) — {tier_count[t]} positions")
            lines.append("")
            lines.append(f"WINNERS [{len(winners)}]:")
            for p in sorted(winners, key=lambda x: x['gain_pct'], reverse=True)[:10]:
                lines.append(f"  {p['pk']:>30s}  ${p['value_usd']:>8,.2f}  gain={p['gain_pct']:+.2f}%")
            lines.append("")
            lines.append(f"LOSERS [{len(losers)}]:")
            for p in sorted(losers, key=lambda x: x['gain_pct'])[:10]:
                lines.append(f"  {p['pk']:>30s}  ${p['value_usd']:>8,.2f}  gain={p['gain_pct']:+.2f}%  tier={p['tier']}")
            lines.append("")
            lines.append(f"MEDIOCRE [{len(mediocre)}]:")
            for p in sorted(mediocre, key=lambda x: x['value_usd'], reverse=True)[:10]:
                lines.append(f"  {p['pk']:>30s}  ${p['value_usd']:>8,.2f}  gain={p['gain_pct']:+.2f}%")
            lines.append("")
            with open(report_dir / 'tradier_capital_report.txt', 'w') as f:
                f.write('\n'.join(lines))
            logger.info(f"[CAPITAL_REPORT] ${total_capital:,.0f} | A=${tier_capital['A']:,.0f}({tier_count['A']}) B=${tier_capital['B']:,.0f}({tier_count['B']}) C=${tier_capital['C']:,.0f}({tier_count['C']})")
        except Exception as e:
            logger.error(f"[CAPITAL_REPORT] Error: {e}", exc_info=True)
        await asyncio.sleep(config.SYMBOL_PERF_REFRESH_SECONDS)

class StockSentimentStrategy:
    def __init__(self, trade_manager):
        self.trade_manager = trade_manager
        self.config = config
        self.target_accounts = ['trc'] # Start conservative, maybe add 'tra' later
        self.strategy_tag = "SENT_STRAT"
        self.base_target_gain = 3.0
        self.stop_loss_pct = -0.3 # Stocks need slightly wider stops than crypto scalps
        self.ratio_sensitivity = 0.015 
        
        # History for velocity calc (Symbol -> Deque)
        self.history = defaultdict(lambda: deque(maxlen=30)) 
        
        self._lock = asyncio.Lock()
        
    async def run_loop(self):
        logger.info(f"🧠 [SENT_STRAT] Stock Strategy Started. Accounts: {self.target_accounts}")
        await asyncio.sleep(30) # Warmup
        
        while self.trade_manager.running:
            try:
                # Market Hours Check
                if not is_regular_trading_hours():
                    await asyncio.sleep(60)
                    continue

                async with self._lock:
                    # 1. Fetch Global Snapshot (Using Redis or File)
                    snapshot = self.trade_manager._get_full_market_snapshot()
                    if not snapshot: 
                        await asyncio.sleep(5)
                        continue

                    # 2. Determine Regime (Global Score vs EMA)
                    # We pick a proxy symbol (e.g. SPY or AAPL) to find the global score stored in indicators
                    global_score = 0.0
                    global_ema = 0.0
                    if 'SPY' in snapshot:
                        global_score = safe_fetch_float(snapshot['SPY'].get('0market_sentiment_score', 0))
                        global_ema = safe_fetch_float(snapshot['SPY'].get('0market_sentiment_score_ema', 0))
                    
                    self._update_history(snapshot, global_score)

                    # Dynamic Ratio
                    diff = global_score - global_ema
                    target_long_ratio = 0.5 + (diff * self.ratio_sensitivity)
                    target_long_ratio = max(0.2, min(0.8, target_long_ratio)) # Clamp

                    for acc in self.target_accounts:
                        await self._manage_active_positions(acc, target_long_ratio)
                        await self._scan_for_entries(acc, target_long_ratio, snapshot, global_score)
                
                await asyncio.sleep(10)

            except Exception as e:
                logger.error(f"🧠 [SENT_STRAT] Loop Error: {e}",  exc_info=True )
                await asyncio.sleep(30)


    def _update_history(self, snapshot, global_score):
        now = time.time()
        for symbol, data in snapshot.items():
            if not isinstance(data, dict): continue
            local = safe_fetch_float(data.get('0market_sentiment_local', 0.0))
            self.history[symbol].append((now, local, global_score))

    def _get_5m_deltas(self, symbol):
        dq = self.history.get(symbol)
        if not dq or len(dq) < 2: return 0.0, 0.0
        
        cur_ts, cur_loc, cur_glob = dq[-1]
        target_ts = cur_ts - 180
        past = dq[0]
        
        for item in dq:
            if item[0] >= target_ts:
                past = item
                break
        if (cur_ts - past[0]) < 60: return 0.0, 0.0
        local_delta = cur_loc - past[1]
        div_delta = (cur_loc - cur_glob) - (past[1] - past[2])
        return local_delta, div_delta

    async def _manage_active_positions(self, account_key, target_long_ratio):
        positions = self.trade_manager.position_manager.get_positions_by_account(account_key)
        
        for pk, pos in positions.items():
            reasons = (str(getattr(pos, 'augment_reason', '')) + str(getattr(pos, 'reduction_reason', '')))
            if self.strategy_tag not in reasons: 
                continue # Skip positions not opened by this strat

            symbol = pos.symbol
            current_price,_ = await self.trade_manager.get_current_price(symbol)
            current_price = float(current_price)
            if current_price <= 0: continue

            amt = abs(float(pos.positionAmt))
            entry = float(pos.entry_price)
            is_long = pos.position_side == 'LONG'
            
            if amt == 0 or entry == 0: continue
            
            gain_pct = ((current_price - entry) / entry) * 100 if is_long else ((entry - current_price) / entry) * 100
            
            # 1. STOP LOSS — DISABLED: STRICT_NO_LOSS. NEVER close at a loss. L/S ratio is the hedge.
            # if gain_pct < self.stop_loss_pct:
            #     reason = f"{self.strategy_tag}_STOP_LOSS"
            #     await self.trade_manager.execute_trade_action(...)
            #     continue

            # 2. ADAPTIVE PROFIT TARGET
            dynamic_target = self.base_target_gain
            if is_long and target_long_ratio < 0.4: dynamic_target = 1.0
            elif not is_long and target_long_ratio > 0.6: dynamic_target = 1.0

            # BACKTEST_CHANGE_T51: NO-LOSS gate — only trim above min profit
            _noloss_min_sent = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 1.0)
            if gain_pct > max(dynamic_target, _noloss_min_sent):
                reduce_qty = max(1, int(amt * 0.25))
                action = 'REDUCE'
                
                # Close dust
                if (amt - reduce_qty) * current_price < 50.0:
                    action = 'CLOSE'
                    reduce_qty = amt

                reason = f"{self.strategy_tag}_TRIM_g{gain_pct:.1f}"
                logger.info(f"🧠 [SENT_STRAT][{account_key}] 💰 {action} {symbol} at {gain_pct:.2f}%")
                
                await self.trade_manager.execute_trade_action(
                    account_key, pk, symbol, reduce_qty, current_price,
                    "SELL" if is_long else "BUY", pos.position_side,
                    f"sent_trim_{int(time.time())}", action=action, reason=reason, override_qty=reduce_qty
                )

    async def _scan_for_entries(self, account_key, target_long_ratio, snapshot, global_score):
        max_pos = 15 # Conservative for stocks
        
        # Count Active
        current_longs = 0
        current_shorts = 0
        positions = self.trade_manager.position_manager.get_positions_by_account(account_key)
        for p in positions.values():
            if abs(float(p.positionAmt)) > 0:
                if p.position_side == 'LONG': current_longs += 1
                else: current_shorts += 1

        # Capacity Logic
        max_long = int(max_pos * target_long_ratio)
        max_short = max_pos - max_long
        
        allow_long = current_longs < max_long
        allow_short = current_shorts < max_short
        
        if not allow_long and not allow_short: return

        # 1. Build restricted candidate set (Waste no resources)
        longs = [s.upper() for s in getattr(self.trade_manager, f"symbols_long_{account_key}", [])]
        shorts = [s.upper() for s in getattr(self.trade_manager, f"symbols_short_{account_key}", [])]
        whitelist = [s.upper() for s in getattr(config, 'ALWAYS_TRADEABLE', [])]
        exceptions = [s.upper() for s in getattr(config, 'EXCEPTIONS', [])]
        
        allowed_symbols = set(longs + shorts + whitelist + exceptions)
        
        candidates = []
        for symbol, data in snapshot.items():
            symbol = symbol.upper()
            if symbol not in allowed_symbols: continue
            if not isinstance(data, dict): continue
            
            # --- TRADEABILITY CHECK ---
            is_long_allowed = self.trade_manager.is_symbol_tradeable(symbol, account_key, 'LONG')
            is_short_allowed = self.trade_manager.is_symbol_tradeable(symbol, account_key, 'SHORT')
            
            if not is_long_allowed and not is_short_allowed:
                continue

            # Skip if ANY side is open (Proper Quantity Check)
            pk_long = f"{account_key}:{symbol}_LONG"
            pk_short = f"{account_key}:{symbol}_SHORT"
            
            pos_long = positions.get(pk_long)
            pos_short = positions.get(pk_short)
            
            has_long = pos_long and abs(float(getattr(pos_long, 'positionAmt', 0))) > 0.00001
            has_short = pos_short and abs(float(getattr(pos_short, 'positionAmt', 0))) > 0.00001
            
            if has_long or has_short: continue

            # Metrics
            local = safe_fetch_float(data.get('0market_sentiment_local', 0))
            divergence = local - global_score
            local_delta, div_delta = self._get_5m_deltas(symbol)
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue

            # Entry Logic (Adjusted for Stocks - Slightly slower pace)
            # LONG
            if allow_long and is_long_allowed and divergence > 8 and div_delta > 3:
                k15 = safe_fetch_float(data.get('stoch_k_15m', 50))
                if k15 < 85:
                    candidates.append({
                        'symbol': symbol, 'side': 'LONG',
                        'score': divergence + div_delta, 'price': price,
                        'reason': f"DIV:{divergence:.1f}_VEL:{div_delta:.1f}"
                    })

            # SHORT (Check non-shortable)
            elif allow_short and is_short_allowed and divergence < -8 and div_delta < -3:
                if symbol in self.trade_manager.non_shortable_symbols: continue
                k15 = safe_fetch_float(data.get('stoch_k_15m', 50))
                if k15 > 15:
                    candidates.append({
                        'symbol': symbol, 'side': 'SHORT',
                        'score': abs(divergence + div_delta), 'price': price,
                        'reason': f"DIV:{divergence:.1f}_VEL:{div_delta:.1f}"
                    })

        # Execute Top 1
        candidates.sort(key=lambda x: x['score'], reverse=True)
        
        for cand in candidates[:1]: # One per loop to avoid spam
            symbol = cand['symbol']
            side = cand['side']
            price = cand['price']
            
            # Size Calc
            base_qty = (config.START_POSITION_SIZE / price)
            qty = max(1, int(base_qty))
            
            pk = f"{account_key}:{symbol}_{side}"
            logger.info(f"🧠 [SENT_STRAT] Opening {pk} (Score: {cand['score']:.1f})")
            
            await self.trade_manager.execute_trade_action(
                account_key, pk, symbol, qty, price,
                "BUY" if side == "LONG" else "SELL", side,
                f"sent_entry_{int(time.time())}", action="OPEN", 
                reason=f"{self.strategy_tag}_{cand['reason']}", override_qty=qty
            )
            return # Only one per scan loop
            
async def monitor_direct_high_gain_positions(trade_manager, order_queue):
    while getattr(trade_manager, "running", False):
        try:
            await asyncio.sleep(75)
            positions = trade_manager.position_manager.get_positions_by_account('tra')
            # Find high gain positions (> 5%)
            high_gain_positions = []
            for position_key, position in positions.items():
                try:
                    position_amt = abs(float(getattr(position, 'positionAmt', 0)))
                    if position_amt <= 0:
                        continue
                    
                    current_price = getattr(position, 'mark_price', 0) or 0
                    if current_price <= 0: current_price,ts=await trade_manager.get_current_price(position.symbol)
                    
                    entry_price = getattr(position, 'entry_price', 0) or current_price
                    if entry_price <= 0:
                        continue
                    
                    position_side = getattr(position, 'position_side', 'LONG')
                    gain = calculate_gain(position_side, current_price, entry_price)
                    
                    if gain > 5.0:  # 5% gain threshold
                        high_gain_positions.append(position_key)
                        
                except Exception:
                    continue
            
            # Process high gain positions
            if high_gain_positions:
                logger.info(f"[HIGH_GAIN_MONITOR] Found {len(high_gain_positions)} positions with >5% gain")
                
                for position_key in high_gain_positions[:25]:  # Limit to 25 at a time
                    try:
                        await direct_high_gain_augmentation(position_key, order_queue, trade_manager)
                    except Exception as e:
                        logger.debug(f"[ ] Error processing {position_key}: {e}")
            
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.debug(f"[monitor_dir ect_high_gain_positions] {e}")
            await asyncio.sleep(10)


async def periodic_direct_high_gain_reopen(trade_manager, order_queue, account_key):
    """Periodically check for re-opening high gain positions that were reduced"""
    current_account.set(account_key)
    while getattr(trade_manager, "running", False):
        try:
            current_account.set(account_key)
            await asyncio.sleep(150)
            await monitor_entries(
                order_queue, trade_manager, account_key, None, 
                event_type="reopen_high_gain", is_priority_add=False, force=False
            )
            
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.debug(f"[per iodic_direct_high_gain_reopen] {e}")
            await asyncio.sleep(10)


async def direct_high_gain_augmentation(position_key: str, order_queue: OrderQueue, trade_manager: TradierTradeManager):
    """Direct high gain augmentation for Tradier - checks if profitable position should be augmented"""
    try:
        account_key, symbol, position_side = parse_position_key(position_key)
        current_account.set(account_key)
        
        # Check if symbol is shortable before trying to augment short
        if position_side == "SHORT" and symbol.upper() in trade_manager.non_shortable_symbols:
            logger.debug(f"[HIGH_GAIN_AUGMENT] {symbol} BLOCKED: Symbol not available for short sales on Tradier")
            return
        
        # Get position
        position =  trade_manager.position_manager.get_position(position_key)
        if not position:
            logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: Position not found")
            return
        
        # Check if position has positionAmt
        position_amt = abs(float(getattr(position, 'positionAmt', 0)))
        if position_amt <= 0:
            logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: No position positionAmt")
            return
        
        # Get current price
        current_price = getattr(position, 'mark_price', 0) or 0
        if current_price <= 0:
            current_price, _ = await trade_manager.get_current_price(symbol)
            current_price = float(current_price)
            if current_price <= 0:
                logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: No valid price")
                return
        
        # Calculate gain
        entry_price = getattr(position, 'entry_price', 0) or current_price
        if entry_price <= 0:
            logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: No entry price")
            return
        
        gain = calculate_gain(position_side, current_price, entry_price)
        
        # Only augment if gain is positive and significant
        if gain < 0.5:  # At least 0.5% gain
            logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: Gain too low ({gain:.2f}%)")
            return
        
        # Check cooldown
        now = datetime.now(timezone.utc)
        last_aug = getattr(position, 'last_augmentation_time', None)
        if last_aug:
            try:
                if isinstance(last_aug, str):
                    last_aug = isoparse(last_aug)
                if (now - last_aug).total_seconds() < 300:  # 5 minute cooldown
                    logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: Augmentation cooldown active")
                    return
            except Exception:
                pass
        indicators = trade_manager.get_indicators(symbol)
        if not indicators:
            logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: No indicators")
            return
        
        # Parse market data
        i = trade_manager.strategy.parse_market_data(indicators)
        
        # Check if conditions are favorable for augmentation
        is_long = position_side == "LONG"
        if is_long:
            # For LONG: RSI not overbought, Stoch crossing up, Heikin Ashi green
            rsi_ok = i.get('rsi_5m', 50) < 75
            stoch_ok = i.get('stoch_k_5m', 50) > i.get('stoch_d_5m', 50)
            ha_ok = i.get('ha_5m', 'neutral') == 'green'
            volume_ok = i.get('rel_vol_5m', 1.0) > 1.2
            
            if not (rsi_ok and stoch_ok and ha_ok and volume_ok):
                logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: Conditions not met for LONG augment")
                return
        else:
            # For SHORT: RSI not oversold, Stoch crossing down, Heikin Ashi red
            rsi_ok = i.get('rsi_5m', 50) > 25
            stoch_ok = i.get('stoch_k_5m', 50) < i.get('stoch_d_5m', 50)
            ha_ok = i.get('ha_5m', 'neutral') == 'red'
            volume_ok = i.get('rel_vol_5m', 1.0) > 1.2
            
            if not (rsi_ok and stoch_ok and ha_ok and volume_ok):
                logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: Conditions not met for SHORT augment")
                return
        base_qty = (config.START_POSITION_SIZE * 0.5) / current_price
        final_qty = await trade_manager.strategy.calculate_quantity_complex(
            symbol, "AUGMENT", position_side, base_qty, indicators, position )
        final_qty = max(1, int(round(final_qty))) 
        if final_qty <= 0:
            logger.debug(f"[HIGH_GAIN_AUGMENT] {position_key}: Calculated quantity <= 0")
            return
        reason = f"High_Gain_Augment_{gain:.1f}%_Vol_{i.get('rel_vol_5m', 1.0):.1f}x"
        _hga_stop = safe_fetch_float(i.get('dc_low4_5m' if position_side == 'LONG' else 'dc_high4_5m'), 0.0)
        if _hga_stop > 0 and position:
            _hga_is_long = (position_side == 'LONG')
            _cur_r1_hga = float(getattr(position, 'r1_stop_price', 0.0) or 0.0)
            if _cur_r1_hga <= 0 or (_hga_is_long and _hga_stop > _cur_r1_hga) or (not _hga_is_long and _hga_stop < _cur_r1_hga):
                position.r1_stop_price = _hga_stop
        await queue_trade_action( order_queue, trade_manager, position_key,   "AUGMENT", reason, 80.0, override_qty=final_qty  )
        logger.info(f"[HIGH_GAIN_AUGMENT] {position_key}: Queued augmentation, gain={gain:.2f}%, qty={final_qty}")
        
    except Exception as e:
        logger.error(f"[HIGH_GAIN_AUGMENT] Error: {e}",  exc_info=True )


class StockScalpStrategy:
    """
    Scalp wing: fast in/out trades on the biggest movers of the moment.
    Runs on trb alongside the swing wing. Budget: $10k long / $10k short.
    Entry: top movers by rel_vol × 5m-deviation, triggered by stoch hook on 5m.
    Exit: 1.5% stop, 2% target, or 45-min timeout.
    """
    def __init__(self, trade_manager):
        self.trade_manager = trade_manager
        self.account_key = "trb"
        self.movers_cache: list = []
        self._lock = asyncio.Lock()

    async def run_loop(self):
        logger.info("⚡ [SCALP] Scalp wing started")
        await asyncio.sleep(15)
        while self.trade_manager.running:
            try:
                if not is_regular_trading_hours():
                    await asyncio.sleep(30)
                    continue
                async with self._lock:
                    snapshot = self.trade_manager._get_full_market_snapshot()
                    if not snapshot:
                        await asyncio.sleep(5)
                        continue
                    self._refresh_movers(snapshot)
                    await self._manage_scalp_positions()
                    await self._scan_entries(snapshot)
                await asyncio.sleep(10)
            except Exception as e:
                logger.error(f"⚡ [SCALP] Loop Error: {e}", exc_info=True)
                await asyncio.sleep(15)

    def _refresh_movers(self, snapshot: dict):
        """Rank symbols by rel_vol × |deviation from 5m EMA|. Cache top N."""
        n = getattr(config, 'SCALP_TOP_MOVERS_N', 14)
        min_rv = getattr(config, 'SCALP_MIN_REL_VOL', 1.3)
        min_move = getattr(config, 'SCALP_MIN_MOVE_PCT', 0.003)
        scored = []
        for sym, data in snapshot.items():
            if not isinstance(data, dict): continue
            rv = safe_fetch_float(data.get('relative_volume_5m', 0))
            price = safe_fetch_float(data.get('current_price', 0))
            ema5 = safe_fetch_float(data.get('ema_20_5m', price))
            if price <= 0 or ema5 <= 0 or rv < min_rv: continue
            move_pct = abs(price - ema5) / ema5
            if move_pct < min_move: continue
            score = rv * move_pct * 100
            scored.append({'symbol': sym.upper(), 'score': score, 'price': price, 'data': data})
        scored.sort(key=lambda x: x['score'], reverse=True)
        self.movers_cache = scored[:n]

    async def _manage_scalp_positions(self):
        """Exit scalp positions on stop, profit target, or timeout."""
        acc = self.account_key
        stop_pct = getattr(config, 'SCALP_STOP_PCT', 0.015)
        target_pct = getattr(config, 'SCALP_TARGET_PCT', 0.02)
        max_hold = getattr(config, 'SCALP_MAX_HOLD_MINUTES', 45.0)
        positions = self.trade_manager.position_manager.get_positions_by_account(acc)
        for pk, pos in list(positions.items()):
            if getattr(pos, 'trade_wing', 'swing') != 'scalp': continue
            qty = abs(float(getattr(pos, 'positionAmt', 0)))
            if qty <= 0: continue
            entry = float(getattr(pos, 'entry_price', 0))
            if entry <= 0: continue
            is_long = pos.position_side == 'LONG'
            price, _ = await self.trade_manager.get_current_price(pos.symbol)
            price = float(price or 0)
            if price <= 0: continue
            gain_pct = ((price - entry) / entry) if is_long else ((entry - price) / entry)
            opened_at = getattr(pos, 'opened_at', None)
            age_min = 0.0
            if opened_at:
                ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at)
                age_min = (time.time() - ots) / 60.0
            should_exit, exit_reason = False, ""
            # BACKTEST_CHANGE_T51: NO-LOSS gate for scalps — only exit if profitable above min
            _noloss_min_s = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 1.0) / 100.0
            if gain_pct <= -stop_pct and gain_pct < -0.015:
                logger.warning(f"⚡ [SCALP] SCALP_STOP BLOCKED by STRICT_NO_LOSS: {pos.symbol} gain={gain_pct:.2%} — NEVER close at a loss")
                should_exit = False; exit_reason = ""
            elif gain_pct >= max(target_pct, _noloss_min_s):
                should_exit = True; exit_reason = f"SCALP_TARGET {gain_pct:.2%}"
            elif age_min >= max_hold and gain_pct >= _noloss_min_s:
                should_exit = True; exit_reason = f"SCALP_TIMEOUT_PROFIT {age_min:.0f}m {gain_pct:.2%}"
            if should_exit:
                logger.info(f"⚡ [SCALP] EXIT {pos.symbol} ({pos.position_side}): {exit_reason}")
                await self.trade_manager.execute_trade_action(acc, pk, pos.symbol, qty, price, "SELL" if is_long else "BUY", pos.position_side, f"scalp_exit_{int(time.time())}", action="CLOSE", reason=exit_reason, override_qty=qty)

    async def _scan_entries(self, snapshot: dict):
        """Enter on top movers with stoch hook trigger. One entry per loop."""
        acc = self.account_key
        long_budget = getattr(config, 'SCALP_LONG_BUDGET', 10000.0)
        short_budget = getattr(config, 'SCALP_SHORT_BUDGET', 10000.0)
        max_per_side = getattr(config, 'SCALP_MAX_POSITIONS_PER_SIDE', 8)
        long_exp = self.trade_manager.get_wing_exposure('scalp', 'LONG')
        short_exp = self.trade_manager.get_wing_exposure('scalp', 'SHORT')
        positions = self.trade_manager.position_manager.get_positions_by_account(acc)
        n_long = sum(1 for p in positions.values() if getattr(p, 'trade_wing', 'swing') == 'scalp' and p.position_side == 'LONG' and abs(float(getattr(p, 'positionAmt', 0))) > 0)
        n_short = sum(1 for p in positions.values() if getattr(p, 'trade_wing', 'swing') == 'scalp' and p.position_side == 'SHORT' and abs(float(getattr(p, 'positionAmt', 0))) > 0)
        allow_long = long_exp < long_budget and n_long < max_per_side
        allow_short = short_exp < short_budget and n_short < max_per_side
        if not allow_long and not allow_short: return
        for mover in self.movers_cache:
            symbol = mover['symbol']
            price = mover['price']
            data = mover['data']
            if symbol in self.trade_manager.blacklist: continue
            if not self.trade_manager.is_symbol_tradeable(symbol, acc, 'LONG') and not self.trade_manager.is_symbol_tradeable(symbol, acc, 'SHORT'): continue
            pk_long = f"{acc}:{symbol}_LONG"
            pk_short = f"{acc}:{symbol}_SHORT"
            pos_l = positions.get(pk_long)
            pos_s = positions.get(pk_short)
            has_any = (pos_l and abs(float(getattr(pos_l, 'positionAmt', 0))) > 0) or (pos_s and abs(float(getattr(pos_s, 'positionAmt', 0))) > 0)
            if has_any: continue
            k5 = safe_fetch_float(data.get('stoch_k_5m', 50))
            k5_prev = safe_fetch_float(data.get('stoch_k_5m_prev', k5))
            d5 = safe_fetch_float(data.get('stoch_d_5m', 50))
            k1 = safe_fetch_float(data.get('stoch_k_1m', 50))
            # Check pullback-reexpansion setup for heavy artillery sizing
            ha_setup, ha_score, ha_dir, ha_detail = self.trade_manager.check_pullback_reexpansion(symbol, data)
            if allow_long and k5 < 25 and k5 > k5_prev and k5 > d5 and k1 < 40:
                heavy = ha_setup and ha_dir == 'LONG'
                logger.info(f"⚡ [SCALP] LONG {'🔫HEAVY ' if heavy else ''}signal {symbol}: k5={k5:.0f} score={mover['score']:.2f}{' '+ha_detail if heavy else ''}")
                await self._open_scalp(acc, symbol, 'LONG', price, ha_score if heavy else 0)
                return
            if allow_short and k5 > 75 and k5 < k5_prev and k5 < d5 and k1 > 60:
                if symbol in self.trade_manager.non_shortable_symbols: continue
                heavy = ha_setup and ha_dir == 'SHORT'
                logger.info(f"⚡ [SCALP] SHORT {'🔫HEAVY ' if heavy else ''}signal {symbol}: k5={k5:.0f} score={mover['score']:.2f}{' '+ha_detail if heavy else ''}")
                await self._open_scalp(acc, symbol, 'SHORT', price, ha_score if heavy else 0)
                return

    async def _open_scalp(self, acc: str, symbol: str, side: str, price: float, ha_score: int = 0):
        """Open a scalp position and tag it as scalp wing. ha_score 3/4 = heavy artillery."""
        start_size = getattr(config, 'SCALP_START_SIZE', 800.0)
        max_pos = getattr(config, 'SCALP_MAX_POSITION_SIZE', 2000.0)
        if ha_score == 4: start_size = min(start_size * 3.0, max_pos)
        elif ha_score == 3: start_size = min(start_size * 2.0, max_pos)
        qty = max(1, int(start_size / max(price, 0.01)))
        pk = f"{acc}:{symbol}_{side}"
        api_side = "buy" if side == 'LONG' else "sell_short"
        logger.info(f"⚡ [SCALP] OPEN {side} {symbol} @ {price:.2f} × {qty} shares")
        await self.trade_manager.execute_trade_action(acc, pk, symbol, qty, price, api_side, side, f"scalp_{int(time.time())}", action="OPEN", reason=f"SCALP_{side}", override_qty=qty)
        await asyncio.sleep(3)
        pos = self.trade_manager.position_manager.get_position(pk)
        if pos:
            pos.trade_wing = "scalp"
            await self.trade_manager.position_manager.save_all_positions()


class StockBreakoutScalper:
    """Ultra-fast stock outlier scalper for trc sandbox — HUGE positions.
    Every 10s during market hours:
    1. Read ALL stock prices from indicator_cache (already computed, zero API calls)
    2. Compare price change across ALL symbols — find outliers vs median
    3. LONG biggest jumpers, SHORT biggest fallers
    4. Exit on lower_high (long) or higher_low (short) on 5m candles
    Paper money (trc) = go big to test the edge."""
    STATE_FILE = config.BASE_PATH / "data" / "stock_outlier_state.json" if hasattr(config, 'BASE_PATH') else Path("data/stock_outlier_state.json")
    ACCT = "trc"  # Paper money sandbox
    MAX_POS = 10; BASE_USD = 5000.0; MAX_USD = 20000.0; TRAIL_PCT = 0.3; STOP_PCT = -0.5
    OUTLIER_THRESHOLD = 0.3  # Stocks move less than crypto — 0.3% vs median is significant
    _instance = None
    @classmethod
    def get(cls):
        if cls._instance is None: cls._instance = cls()
        return cls._instance
    def __init__(self):
        self._state = self._load()
        self._prev_prices = {}
        self._cooldowns = {}
    def _load(self):
        try: return json.loads(self.STATE_FILE.read_text()) if self.STATE_FILE.exists() else {"positions": {}}
        except: return {"positions": {}}
    def _save(self):
        self.STATE_FILE.parent.mkdir(parents=True, exist_ok=True)
        self.STATE_FILE.write_text(json.dumps(self._state, indent=2, default=str))
    async def run(self, trade_manager=None):
        if not trade_manager: return
        if not is_regular_trading_hours(): return
        if self.ACCT not in (trade_manager.target_accounts or []):
            return
        now = time.time()
        try:
            # Read ALL prices from indicator cache + market snapshot
            snapshots = {}
            cache = getattr(trade_manager, 'indicator_cache', {}) or getattr(trade_manager, 'market_snapshot', {})
            for sym in list(cache.keys()):
                try:
                    i = cache[sym]
                    if not isinstance(i, dict): continue
                    cp = safe_fetch_float(i.get('current_price', i.get('last', i.get('price'))), 0)
                    if cp <= 0: continue
                    k_5m = safe_fetch_float(i.get('stoch_k_5m'), 50)
                    d_5m = safe_fetch_float(i.get('stoch_d_5m'), 50)
                    wt1_5m = safe_fetch_float(i.get('wt1_5m', i.get('wt1_15m')), 0)
                    wt2_5m = safe_fetch_float(i.get('wt2_5m', i.get('wt2_15m')), 0)
                    high_5m = safe_fetch_float(i.get('high_5m', i.get('high_15m', i.get('high'))), cp)
                    low_5m = safe_fetch_float(i.get('low_5m', i.get('low_15m', i.get('low'))), cp)
                    snapshots[sym] = {'price': cp, 'k': k_5m, 'd': d_5m, 'wt1': wt1_5m, 'wt2': wt2_5m, 'high': high_5m, 'low': low_5m}
                except: continue
            if len(snapshots) < 5: return
            # Compare: find outliers vs median
            changes = {}
            for sym, snap in snapshots.items():
                prev = self._prev_prices.get(sym)
                if prev and prev[0] > 0 and (now - prev[1]) < 120:
                    changes[sym] = (snap['price'] - prev[0]) / prev[0] * 100
                self._prev_prices[sym] = (snap['price'], now)
            if len(changes) < 5: return
            vals = sorted(changes.values())
            median = vals[len(vals) // 2]
            # Long outliers: jumped more than median + threshold
            long_cands = sorted([(s, c) for s, c in changes.items() if (c - median > self.OUTLIER_THRESHOLD) and (s not in self._cooldowns or now - self._cooldowns.get(s, 0) > 120)], key=lambda x: -x[1])
            # Short outliers: fell more than median - threshold
            short_cands = sorted([(s, c) for s, c in changes.items() if (median - c > self.OUTLIER_THRESHOLD) and (s not in self._cooldowns or now - self._cooldowns.get(s, 0) > 120)], key=lambda x: x[1])
            # Enter
            cur = len(self._state.get("positions", {}))
            for sym, chg in long_cands[:3]:
                if cur >= self.MAX_POS: break
                key = f"{sym}_LONG"
                if key in self._state.get("positions", {}) or f"{sym}_SHORT" in self._state.get("positions", {}): continue
                snap = snapshots.get(sym, {})
                if snap.get('wt1', 0) <= snap.get('wt2', 0): continue  # WT must confirm
                cp = snap['price']
                qty = max(1, int(min(self.MAX_USD, self.BASE_USD * max(1, chg / 0.5)) / cp))
                try:
                    order = await trade_manager.place_order(sym, "buy", qty, order_type="market")
                    if order:
                        logger.warning(f"[STOCK_OUTLIER] 🟢 LONG {sym} chg={chg:+.2f}% med={median:+.2f}% qty={qty} ${qty*cp:.0f}")
                        self._state["positions"][key] = {"sym": sym, "side": "LONG", "ep": cp, "qty": qty, "ts": now, "max_g": 0, "prev_high": snap['high'], "prev_low": snap['low']}
                        cur += 1
                except Exception as e:
                    logger.error(f"[STOCK_OUTLIER] ❌ LONG {sym}: {e}")
            for sym, chg in short_cands[:3]:
                if cur >= self.MAX_POS: break
                key = f"{sym}_SHORT"
                if key in self._state.get("positions", {}) or f"{sym}_LONG" in self._state.get("positions", {}): continue
                snap = snapshots.get(sym, {})
                if snap.get('wt1', 0) >= snap.get('wt2', 0): continue
                cp = snap['price']
                qty = max(1, int(min(self.MAX_USD, self.BASE_USD * max(1, abs(chg) / 0.5)) / cp))
                try:
                    order = await trade_manager.place_order(sym, "sell_short", qty, order_type="market")
                    if order:
                        logger.warning(f"[STOCK_OUTLIER] 🔴 SHORT {sym} chg={chg:+.2f}% med={median:+.2f}% qty={qty} ${qty*cp:.0f}")
                        self._state["positions"][key] = {"sym": sym, "side": "SHORT", "ep": cp, "qty": qty, "ts": now, "max_g": 0, "prev_high": snap['high'], "prev_low": snap['low']}
                        cur += 1
                except Exception as e:
                    logger.error(f"[STOCK_OUTLIER] ❌ SHORT {sym}: {e}")
            # Manage exits: lower_high (long) or higher_low (short) on 5m
            for key, pos in list(self._state.get("positions", {}).items()):
                snap = snapshots.get(pos["sym"])
                if not snap: continue
                cp = snap['price']; ep = pos['ep']
                g = ((cp - ep) / ep * 100) if pos['side'] == 'LONG' else ((ep - cp) / ep * 100)
                pos['max_g'] = max(pos.get('max_g', 0), g)
                h = snap['high']; l = snap['low']
                ph = pos.get('prev_high', h); pl = pos.get('prev_low', l)
                should_exit = False; reason = ""
                if pos['side'] == 'LONG':
                    if h < ph and g > 0.05: should_exit = True; reason = f"LOWER_HIGH(h={h:.2f}<{ph:.2f},g={g:.2f}%)"
                    if snap.get('wt1', 0) < snap.get('wt2', 0) and g > 0.03: should_exit = True; reason = f"WT_FLIP(g={g:.2f}%)"
                else:
                    if l > pl and g > 0.05: should_exit = True; reason = f"HIGHER_LOW(l={l:.2f}>{pl:.2f},g={g:.2f}%)"
                    if snap.get('wt1', 0) > snap.get('wt2', 0) and g > 0.03: should_exit = True; reason = f"WT_FLIP(g={g:.2f}%)"
                if pos['max_g'] > 0.3 and g < pos['max_g'] - self.TRAIL_PCT: should_exit = True; reason = f"TRAIL(g={g:.2f}%,max={pos['max_g']:.2f}%)"
                # STOP_PCT DISABLED: STRICT_NO_LOSS — NEVER close at a loss
                held = now - pos.get('ts', now)
                # TIME exit only if profitable above noloss_min
                _noloss_pct_outlier = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 3.0)
                if held > 1800 and g >= _noloss_pct_outlier: should_exit = True; reason = f"TIME_PROFIT({held:.0f}s,g={g:.2f}%)"
                if should_exit:
                    try:
                        close_side = "sell" if pos['side'] == 'LONG' else "buy_to_cover"
                        await trade_manager.place_order(pos['sym'], close_side, pos['qty'], order_type="market")
                        logger.warning(f"[STOCK_OUTLIER] ⚡ EXIT {key} {reason}")
                    except Exception as e:
                        logger.error(f"[STOCK_OUTLIER] ❌ EXIT {key}: {e}")
                    self._cooldowns[pos['sym']] = now
                    del self._state["positions"][key]
                else:
                    pos['prev_high'] = h; pos['prev_low'] = l
            self._save()
        except Exception as e:
            logger.error(f"[STOCK_OUTLIER] Error: {e}", exc_info=True)

class StockDaytradeWing:
    """BACKTEST_CHANGE_T66: Daytrade wing — DC breakout on 5m/15m with 1h expansion filter.
    Opens positions in the morning, flattens/rebalances 2h before market close.
    Parallel to HODL system. DC works on lower TFs for intraday stock moves."""
    def __init__(self, trade_manager):
        self.trade_manager = trade_manager
        self.account_key = getattr(config, 'DC_DAYTRADE_ACCOUNT', 'trb')
        self._lock = asyncio.Lock()
        self._last_entry_ts: Dict[str, float] = {}
        self._entry_cooldown = 60.0
    async def run_loop(self):
        logger.info("📊 [DAYTRADE] DaytradeWing started")
        await asyncio.sleep(20)
        while self.trade_manager.running:
            try:
                if not getattr(config, 'DC_DAYTRADE_ENABLED', True):
                    await asyncio.sleep(30); continue
                if not is_regular_trading_hours():
                    await asyncio.sleep(30); continue
                async with self._lock:
                    snapshot = self.trade_manager._get_full_market_snapshot()
                    if not snapshot:
                        await asyncio.sleep(5); continue
                    await self._manage_daytrade_positions(snapshot)
                    if self._is_pre_close():
                        await self._pre_close_rebalance(snapshot)
                    else:
                        await self._scan_dc_entries(snapshot)
                await asyncio.sleep(10)
            except Exception as e:
                logger.error(f"📊 [DAYTRADE] Loop Error: {e}", exc_info=True)
                await asyncio.sleep(15)
    def _is_pre_close(self) -> bool:
        try:
            from datetime import datetime
            from zoneinfo import ZoneInfo
            now_et = datetime.now(ZoneInfo("America/New_York"))
            close_h, close_m = getattr(config, 'MARKET_CLOSE_HOUR', 16), getattr(config, 'MARKET_CLOSE_MINUTE', 0)
            close_minutes = close_h * 60 + close_m
            now_minutes = now_et.hour * 60 + now_et.minute
            pre_close_min = getattr(config, 'DC_DAYTRADE_PRE_CLOSE_MINUTES', 120)
            return 0 < (close_minutes - now_minutes) <= pre_close_min
        except Exception:
            return False
    def _check_dc_break(self, data: dict, symbol: str) -> list:
        signals = []
        buf = getattr(config, 'DC_DAYTRADE_BUFFER', 0.001)
        price = safe_fetch_float(data.get('current_price', 0))
        if price <= 0: return signals
        require_expansion = getattr(config, 'DC_DAYTRADE_REQUIRE_1H_EXPANSION', True)
        stoch_filter = getattr(config, 'DC_DAYTRADE_STOCH_FILTER', True)
        k_15m = safe_fetch_float(data.get('stoch_k_15m', 50))
        k_exh_long = getattr(config, 'DC_DAYTRADE_K_EXHAUSTED_LONG', 85)
        k_exh_short = getattr(config, 'DC_DAYTRADE_K_EXHAUSTED_SHORT', 15)
        for tf, size_mult in [('5m', 1.0), ('15m', 1.5)]:
            dc_high = safe_fetch_float(data.get(f'dc_high_{tf}', 0))
            dc_low = safe_fetch_float(data.get(f'dc_low_{tf}', 0))
            dc_high_x = bool(data.get(f'dc_high_crossover_{tf}', False))
            dc_low_x = bool(data.get(f'dc_low_crossunder_{tf}', False))
            above = dc_high > 0 and price > dc_high * (1 + buf)
            below = dc_low > 0 and price < dc_low * (1 - buf)
            if dc_high_x or above:
                if stoch_filter and k_15m > k_exh_long: continue
                if require_expansion:
                    dc_high_1h = safe_fetch_float(data.get('dc_high_1h', 0))
                    dc_high_1h_ant = safe_fetch_float(data.get('dc_high_1h_ant', dc_high_1h))
                    if dc_high_1h <= dc_high_1h_ant: continue
                stop = safe_fetch_float(data.get(f'dc_basis_{tf}', dc_low))
                dc_basis_val = safe_fetch_float(data.get(f'dc_basis_{tf}', 0))
                _sig_mult = size_mult
                _dc_break_phase = 0
                if getattr(config, 'DC_BREAK_GR_MULT_ENABLED', False):
                    _sig_mult = float(getattr(config, 'DC_BREAK_GR_MULT_BREAKOUT', 0.1))
                    _dc_break_phase = 1
                signals.append({'side': 'LONG', 'tf': tf, 'size_mult': _sig_mult, 'stop': stop, 'reason': f'DC_BREAK_HIGH_{tf.upper()}', 'dc_break_phase': _dc_break_phase, 'dc_basis': dc_basis_val})
            if dc_low_x or below:
                if stoch_filter and k_15m < k_exh_short: continue
                if require_expansion:
                    dc_low_1h = safe_fetch_float(data.get('dc_low_1h', 0))
                    dc_low_1h_ant = safe_fetch_float(data.get('dc_low_1h_ant', dc_low_1h))
                    if dc_low_1h >= dc_low_1h_ant: continue
                stop = safe_fetch_float(data.get(f'dc_basis_{tf}', dc_high))
                dc_basis_val = safe_fetch_float(data.get(f'dc_basis_{tf}', 0))
                _sig_mult = size_mult
                _dc_break_phase = 0
                if getattr(config, 'DC_BREAK_GR_MULT_ENABLED', False):
                    _sig_mult = float(getattr(config, 'DC_BREAK_GR_MULT_BREAKOUT', 0.1))
                    _dc_break_phase = 1
                # ═══ 2026-05-17 PATCH B — DC_BREAK_LOW_15M SHORT requires HTF alignment ═══
                # Live evidence: ASTS SHORT lost -5.70% (trb:423) on bare DC_BREAK_LOW_15M with no
                # MTF or ratio confirmation. The same template wins when HTF bear-aligned (WDAY, TTD,
                # MP all needed D+4h+1h cross BEAR). Block when proposed reason starts with
                # DC_BREAK_LOW_15M AND wt_bear_alignment < threshold. Flag default False —
                # sweep-validate before flip. Fail-open on missing alignment data.
                _dc_bl_block = False
                if bool(getattr(config, 'DC_BREAK_LOW_REQUIRE_HTF_ENABLED', False)) and tf == '15m':
                    _dc_bl_min = int(getattr(config, 'DC_BREAK_LOW_REQUIRE_HTF_MIN_TFS', 2))
                    _dc_bl_align = int(data.get('wt_bear_alignment', -1) or -1)
                    if _dc_bl_align >= 0 and _dc_bl_align < _dc_bl_min:
                        _dc_bl_block = True
                        logger.info(f"[BLOCKED_DC_BREAK_LOW_HTF] {symbol} SHORT tf={tf}: wt_bear_alignment={_dc_bl_align}<{_dc_bl_min} — REFUSING (reason=BLOCKED_DC_BREAK_LOW_HTF_{_dc_bl_align}lt{_dc_bl_min})")
                if _dc_bl_block:
                    continue
                signals.append({'side': 'SHORT', 'tf': tf, 'size_mult': _sig_mult, 'stop': stop, 'reason': f'DC_BREAK_LOW_{tf.upper()}', 'dc_break_phase': _dc_break_phase, 'dc_basis': dc_basis_val})
        return signals
    async def _scan_dc_entries(self, snapshot: dict):
        acc = self.account_key
        long_budget = getattr(config, 'DC_DAYTRADE_LONG_BUDGET', 3000.0)
        short_budget = getattr(config, 'DC_DAYTRADE_SHORT_BUDGET', 3000.0)
        max_per_side = getattr(config, 'DC_DAYTRADE_MAX_PER_SIDE', 5)
        long_exp = self.trade_manager.get_wing_exposure('daytrade', 'LONG')
        short_exp = self.trade_manager.get_wing_exposure('daytrade', 'SHORT')
        positions = self.trade_manager.position_manager.get_positions_by_account(acc)
        n_long = sum(1 for p in positions.values() if getattr(p, 'trade_wing', 'swing') == 'daytrade' and p.position_side == 'LONG' and abs(float(getattr(p, 'positionAmt', 0))) > 0)
        n_short = sum(1 for p in positions.values() if getattr(p, 'trade_wing', 'swing') == 'daytrade' and p.position_side == 'SHORT' and abs(float(getattr(p, 'positionAmt', 0))) > 0)
        allow_long = long_exp < long_budget and n_long < max_per_side
        allow_short = short_exp < short_budget and n_short < max_per_side
        if not allow_long and not allow_short: return
        now = time.time()
        for symbol, data in snapshot.items():
            if not isinstance(data, dict): continue
            if symbol.upper() in self.trade_manager.blacklist: continue
            if now - self._last_entry_ts.get(symbol, 0) < self._entry_cooldown: continue
            signals = self._check_dc_break(data, symbol)
            if not signals: continue
            best = max(signals, key=lambda s: s['size_mult'])
            side = best['side']
            if side == 'LONG' and not allow_long: continue
            if side == 'SHORT' and not allow_short: continue
            if side == 'SHORT' and symbol.upper() in getattr(config, 'NON_SHORTABLE', set()): continue
            if not self.trade_manager.is_symbol_tradeable(symbol, acc, side): continue
            pk = f"{acc}:{symbol}_{side}"
            pos = positions.get(pk)
            if pos and abs(float(getattr(pos, 'positionAmt', 0))) > 0: continue
            price = safe_fetch_float(data.get('current_price', 0))
            if price <= 0: continue
            start_size = getattr(config, 'DC_DAYTRADE_START_SIZE', 600.0) * best['size_mult']
            max_pos = getattr(config, 'DC_DAYTRADE_MAX_POSITION_SIZE', 2000.0)
            start_size = min(start_size, max_pos)
            qty = max(1, int(start_size / price))
            api_side = "buy" if side == 'LONG' else "sell_short"
            logger.info(f"📊 [DAYTRADE] OPEN {side} {symbol} @ {price:.2f} x {qty} | {best['reason']} (mult={best['size_mult']:.1f}x, stop={best['stop']:.2f})")
            await self.trade_manager.execute_trade_action(acc, pk, symbol, qty, price, api_side, side, f"daytrade_{int(now)}", action="OPEN", reason=best['reason'], override_qty=qty)
            self._last_entry_ts[symbol] = now
            await asyncio.sleep(3)
            pos = self.trade_manager.position_manager.get_position(pk)
            if pos:
                pos.trade_wing = "daytrade"
                # V4: Record entry_price IMMEDIATELY from order price
                # so gain tracking starts from real cost basis, not stale/zero
                if not pos.entry_price or pos.entry_price <= 0:
                    pos.entry_price = price
                    pos.opened_at = datetime.now(timezone.utc)
                    logger.info(f"[DAYTRADE_ENTRY_PRICE] {pk}: Set entry_price={price:.2f} immediately")
                # P2-C: store dc_break_phase and dc_basis for GR Phase 2 detection
                if getattr(config, 'DC_BREAK_GR_MULT_ENABLED', False) and best.get('dc_break_phase', 0) == 1:
                    try:
                        pos._dc_break_phase = 1
                        pos._dc_basis = float(best.get('dc_basis', 0))
                        logger.info(f"[DC_BREAK_GR] {pk}: Phase 1 entry, dc_basis={pos._dc_basis:.4f}")
                    except Exception:
                        pass
                await self.trade_manager.position_manager.save_all_positions()
            if side == 'LONG': allow_long = False
            else: allow_short = False
            if not allow_long and not allow_short: return
    async def _manage_daytrade_positions(self, snapshot: dict):
        acc = self.account_key
        stop_pct = getattr(config, 'DC_DAYTRADE_STOP_PCT', 0.015)
        target_pct = getattr(config, 'DC_DAYTRADE_TARGET_PCT', 0.01)
        max_hold = getattr(config, 'DC_DAYTRADE_MAX_HOLD_MINUTES', 240.0)
        noloss_min = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 1.0) / 100.0
        positions = self.trade_manager.position_manager.get_positions_by_account(acc)
        for pk, pos in list(positions.items()):
            if getattr(pos, 'trade_wing', 'swing') != 'daytrade': continue
            qty = abs(float(getattr(pos, 'positionAmt', 0)))
            if qty <= 0: continue
            entry = float(getattr(pos, 'entry_price', 0))
            if entry <= 0: continue
            is_long = pos.position_side == 'LONG'
            price, _ = await self.trade_manager.get_current_price(pos.symbol)
            price = float(price or 0)
            if price <= 0: continue
            gain_pct = ((price - entry) / entry) if is_long else ((entry - price) / entry)
            opened_at = getattr(pos, 'opened_at', None)
            age_min = 0.0
            if opened_at:
                ots = opened_at.timestamp() if hasattr(opened_at, 'timestamp') else float(opened_at)
                age_min = (time.time() - ots) / 60.0
            should_exit, exit_reason = False, ""
            # 2026-05-16: ATR-aware effective DT target (DEFAULT OFF behind DT_TARGET_ATR_ENABLED flag).
            # Pull short-horizon ATR (5m primary, 15m fallback) from snapshot. Use 2× ATR / entry
            # as target when ATR available — beats a fixed 0.5–1% target that often clips winners
            # before they breathe. `target_pct` (config knob) is the no-ATR fallback. Reason label
            # switches to DT_TARGET_ATR when ATR-driven so logs are grep-able. Flag default False:
            # legacy fixed-target behavior preserved until sweep-validated.
            _dt_atr_enabled = bool(getattr(config, 'DT_TARGET_ATR_ENABLED', False))
            _effective_target = max(target_pct, noloss_min)
            _atr_target_pct = 0.0
            if _dt_atr_enabled:
                _sym_data_t1 = snapshot.get(pos.symbol.upper(), {}) or {}
                _atr_5m_t1 = safe_fetch_float(_sym_data_t1.get('atr_5m', 0))
                _atr_15m_t1 = safe_fetch_float(_sym_data_t1.get('atr_15m', 0))
                _atr_t1 = _atr_5m_t1 if _atr_5m_t1 > 0 else _atr_15m_t1
                _atr_target_pct = (2.0 * _atr_t1 / entry) if (_atr_t1 > 0 and entry > 0) else 0.0
                _effective_target = max(_atr_target_pct, target_pct, noloss_min)
            if gain_pct <= -stop_pct and gain_pct < -0.015:
                logger.warning(f"📊 [DAYTRADE] DT_STOP BLOCKED by STRICT_NO_LOSS: {pos.symbol} gain={gain_pct:.2%} — NEVER close at a loss")
                should_exit = False; exit_reason = ""
            elif gain_pct >= _effective_target:
                should_exit = True
                exit_reason = f"DT_TARGET_ATR {gain_pct:.2%}" if (_dt_atr_enabled and _atr_target_pct > target_pct) else f"DT_TARGET {gain_pct:.2%}"
            elif age_min >= max_hold and gain_pct >= noloss_min:
                should_exit = True; exit_reason = f"DT_TIMEOUT {age_min:.0f}m {gain_pct:.2%}"
            else:
                sym_data = snapshot.get(pos.symbol.upper(), {})
                if sym_data:
                    dc_basis_5m = safe_fetch_float(sym_data.get('dc_basis_5m', 0))
                    if dc_basis_5m > 0 and gain_pct >= noloss_min:
                        if (is_long and price < dc_basis_5m) or (not is_long and price > dc_basis_5m):
                            should_exit = True; exit_reason = f"DT_BASIS_CROSS {gain_pct:.2%}"
            # P2-C: GR Phase 2 augment — if Phase 1 entry and price retest dc_basis + WT confirm
            if (not should_exit and getattr(config, 'DC_BREAK_GR_MULT_ENABLED', False)
                    and getattr(pos, '_dc_break_phase', 0) == 1):
                try:
                    _p2_basis = float(getattr(pos, '_dc_basis', 0))
                    _p2_tol = float(getattr(config, 'DC_BREAK_GR_RETEST_TOLERANCE_PCT', 0.3)) / 100.0
                    _p2_sym_data = snapshot.get(pos.symbol.upper(), {}) or {}
                    _p2_wt1_3m = safe_fetch_float(_p2_sym_data.get('wt1_3m', _p2_sym_data.get('wt1_5m', 0)))
                    _p2_wt2_3m = safe_fetch_float(_p2_sym_data.get('wt2_3m', _p2_sym_data.get('wt2_5m', 0)))
                    _p2_wt_confirm = (_p2_wt1_3m > _p2_wt2_3m) if is_long else (_p2_wt1_3m < _p2_wt2_3m)
                    if _p2_basis > 0 and _p2_wt_confirm:
                        _p2_at_basis = (is_long and abs(price - _p2_basis) / _p2_basis <= _p2_tol and price >= _p2_basis * (1 - _p2_tol)) or (not is_long and abs(price - _p2_basis) / _p2_basis <= _p2_tol and price <= _p2_basis * (1 + _p2_tol))
                        if _p2_at_basis:
                            _p2_mult = float(getattr(config, 'DC_BREAK_GR_MULT_RETEST', 3.0))
                            _p2_start = getattr(config, 'DC_DAYTRADE_START_SIZE', 600.0) * _p2_mult
                            _p2_max = getattr(config, 'DC_DAYTRADE_MAX_POSITION_SIZE', 2000.0)
                            _p2_qty = max(1, int(min(_p2_start, _p2_max) / price))
                            _p2_api = "buy" if is_long else "sell_short"
                            _p2_reason = f"DC_BREAK_GR_RETEST_basis{_p2_basis:.2f}_mult{_p2_mult:.1f}x"
                            logger.info(f"[DC_BREAK_GR] {pk}: Phase 2 augment {_p2_qty}@{price:.2f} basis={_p2_basis:.2f} wt={_p2_wt1_3m:.1f}/{_p2_wt2_3m:.1f}")
                            await self.trade_manager.execute_trade_action(acc, pk, pos.symbol, _p2_qty, price, _p2_api, pos.position_side, f"dt_p2_{int(time.time())}", action="AUGMENT", reason=_p2_reason, override_qty=_p2_qty)
                            try:
                                pos._dc_break_phase = 2
                            except Exception:
                                pass
                except Exception as _p2_err:
                    logger.debug(f"[DC_BREAK_GR_P2] {pk}: {_p2_err}")
            if should_exit:
                logger.info(f"📊 [DAYTRADE] EXIT {pos.symbol} ({pos.position_side}): {exit_reason}")
                api_side = "sell" if is_long else "buy_to_cover"
                await self.trade_manager.execute_trade_action(acc, pk, pos.symbol, qty, price, api_side, pos.position_side, f"dt_exit_{int(time.time())}", action="CLOSE", reason=exit_reason, override_qty=qty)
    async def _pre_close_rebalance(self, snapshot: dict):
        acc = self.account_key
        noloss_min = getattr(config, 'NOLOSS_MIN_PROFIT_PCT_TRADIER', 1.0) / 100.0
        positions = self.trade_manager.position_manager.get_positions_by_account(acc)
        dt_positions = []
        for pk, pos in positions.items():
            if getattr(pos, 'trade_wing', 'swing') != 'daytrade': continue
            qty = abs(float(getattr(pos, 'positionAmt', 0)))
            if qty <= 0: continue
            entry = float(getattr(pos, 'entry_price', 0))
            if entry <= 0: continue
            is_long = pos.position_side == 'LONG'
            price, _ = await self.trade_manager.get_current_price(pos.symbol)
            price = float(price or 0)
            if price <= 0: continue
            gain_pct = ((price - entry) / entry) if is_long else ((entry - price) / entry)
            dt_positions.append({'pk': pk, 'pos': pos, 'qty': qty, 'price': price, 'gain_pct': gain_pct, 'is_long': is_long})
        if not dt_positions: return
        long_val = sum(p['qty'] * p['price'] for p in dt_positions if p['is_long'])
        short_val = sum(p['qty'] * p['price'] for p in dt_positions if not p['is_long'])
        profitable = sorted([p for p in dt_positions if p['gain_pct'] >= noloss_min], key=lambda p: p['gain_pct'], reverse=True)
        if not profitable: return
        if long_val > 0 and short_val > 0:
            ratio = long_val / short_val
            if ratio > 2.0:
                overweight = [p for p in profitable if p['is_long']]
            elif ratio < 0.5:
                overweight = [p for p in profitable if not p['is_long']]
            else:
                overweight = profitable
        else:
            overweight = profitable
        for p in overweight:
            logger.info(f"📊 [DAYTRADE] PRE_CLOSE flatten {p['pos'].symbol} ({p['pos'].position_side}): gain={p['gain_pct']:.2%}")
            api_side = "sell" if p['is_long'] else "buy_to_cover"
            await self.trade_manager.execute_trade_action(acc, p['pk'], p['pos'].symbol, p['qty'], p['price'], api_side, p['pos'].position_side, f"dt_preclose_{int(time.time())}", action="CLOSE", reason=f"PRE_CLOSE {p['gain_pct']:.2%}", override_qty=p['qty'])
            await asyncio.sleep(2)


async def main():
    import argparse
    def signal_handler(signum, frame):
        """Handle shutdown signals"""
        logger.info(f"Received signal {signum}, shutting down...")
        raise KeyboardInterrupt
        
    parser = argparse.ArgumentParser(description="Tradier Trade Manager")
    parser.add_argument(
        "--accounts", 
        nargs="+", 
        default=[ "trb", "trc"], 
        help="Space-separated list of accounts to manage (e.g. --accounts tra)"   )
    args = parser.parse_args()
    
    # Validate accounts against config
    valid_accounts = [acc for acc in args.accounts if acc in config.ACCOUNTS]
    if not valid_accounts:
        print(f"!! No valid accounts found in config. Looking for: {args.accounts}")
        return
    setup_logger_per_account("tradier_manage", config.LOG_DIR, valid_accounts)   
    logger.info(f"🚀 INSTANCE STARTED for: {valid_accounts}")
    logger.info(f"📁 Weather Logs -> Stdout (captured in tradier_manage_{valid_accounts[0]}.log)")
    logger.info(f"📁 General Logs -> tradier_manage_general_{valid_accounts[0]}.log")

    signal.signal(signal.SIGINT, signal_handler)
    signal.signal(signal.SIGTERM, signal_handler)
    
    print(f"[STARTUP] Creating TradierTradeManager...", flush=True)
    manager = TradierTradeManager(account_list=valid_accounts)
    print(f"[STARTUP] Creating StockSentimentStrategy...", flush=True)
    sent_strat = StockSentimentStrategy(manager)
    print(f"[STARTUP] Launching...", flush=True)
    try:
        asyncio.create_task(sent_strat.run_loop())
        print(f"[STARTUP] Calling manager.start()...", flush=True)
        await manager.start()
    except KeyboardInterrupt:
        logger.info("Interrupted by user")
    except Exception as e:
        logger.error(f"Fatal error: {e}",  exc_info=True )
    finally:
        await manager.stop()

if __name__ == "__main__":
    asyncio.run(main())